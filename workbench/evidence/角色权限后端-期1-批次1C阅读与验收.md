# 角色权限后端：批次1C阅读与验收

## 状态与范围

- 基线：`99d03e6b2`；固定提取源：`b38493b07`；交付分支：`feat/user-permissions-phase1`。
- 用户已认可1C整体框架，并选择跳过细节阅读；此确认不替代独立正式评审或期1整体Gate 2。
- 1A/1B Go文件不变。新增九个角色/权限接口，连接应用启动、身份策略和离线恢复。
- 初次候选`32730c2cb`的Go业务新增798行、删除25行；新Go测试125行；真实HTTP/升级脚本204行。host/auth.go超过300行，保留在单文件是因为现有认证、临时授权和WS检查共用同一个适配器；未达到500行上限。
- 无rbac/host或上游改动；Provider、配置、通知、WS仍保留原固定主管理员限制。

## 阅读顺序

先看HTTP请求的共同入口，再挑一个接口沿着Service走到已确认的业务层。数据库实现不用重读。

| 入口 | 用途 | 要求 |
|---|---|---|
| POST /api/permissions/list | 返回可分配的权限目录 | Users.View |
| POST /api/permissions/me | 返回自己的角色及权限 | 正常登录会话 |
| POST /api/roles/list、get | 查看角色列表或详情 | Users.View |
| POST /api/roles/create、update、delete | 创建、修改或删除角色 | Users.Manage |
| POST /api/accounts/get-roles | 查询某个账号的角色和权限来源 | Users.View |
| POST /api/accounts/set-roles | 替换某个账号的角色 | Users.Manage |

文件入口：`rbac/http/handler.go`集中放路由与共同认证；`api.go`按角色管理、账号分配和权限查询分组，响应字段就近定义；`matrix.go`整理前端功能开关；`protocol.go`约束输入JSON并将业务错误转HTTP状态。`app/rbac.go`组装已确认的业务与存储，`app/identity.go`连接迁移和HTTP，`app/recovery.go`连接离线恢复。宿主适配器只对精确匹配的角色端点交回接口自己鉴权。

## 行为验证

SQLite单节点与PostgreSQL双节点均通过：九接口闭环、匿名/跨源/Bearer拒绝、强制改密限制、严格ID及重复JSON键检查、权限来源、角色关联数量、预置角色删除保护、最后主管理员保护、跨节点撤权立即生效。Users.Manage可以管理账号和角色，但即使带ModelProvider.Manage、Notifications.View也不能提前访问未接入的宿主入口。

升级恢复两种数据库均通过：从1B认证接线升级，首次迁移、编辑预置角色后重启、停机回退旧版恢复、再次升级及新版离线恢复；预置角色编辑不丢失，恢复补回主管理员角色并保留其他角色及密码事件。主管理员的展开权限不会写入权限关系表。

初次验证留存目录（应用进程已由脚本停止；PG测试库已删除）：

- SQLite HTTP：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-1c-smoke-dvjii4nh`
- PG HTTP：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-1c-smoke-mmx33bc6`
- SQLite升级：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-1c-smoke-bqzz3mqs`
- PG升级：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-1c-smoke-xc4rptqo`

回归检查以[1C清单](../cases/角色权限后端/期1/checklist.yaml)和[1C预飞](角色权限后端-期1-批次1C预飞.md)为准，包括EE vet、race、构建、范围冻结及工作台校验。

## 发现与处理

实际HTTP发现GET角色接口会进入宿主前端兜底并返回HTML 200。现由GuardMethods只保护九个已知角色路径，返回405和Allow: POST。完整路由分类仍留期2。

配置响应用户名是既有SecretVar对象，初次冒烟误按字符串断言，已将测试改为读取value，并检查密码保持脱敏；生产格式没有调整。

新增宿主测试确认：固定管理员查询故障返回503，不能转用临时令牌继续访问。

## 复现与环境

候选构建：`cd ee && GOWORK=off go build -o tmp/bifrost-rbac-phase1c ./cmd/bifrost-http`。

升级基线使用上一批预飞产物`ee/tmp/bifrost-rbac-phase1b`，SHA256为`fb085312615c360ac736c8170635e33e097aeca77a17604d1c6f47b76fd1471c`。

需要重新生成基线时，在独立checkout构建`99d03e6b2`的EE入口，并将二进制放到清单指定位置。PostgreSQL冒烟要求BIFROST_TEST_POSTGRES_DSN，使用专用socket实例`/tmp/codex-rbac-1b-pg-9jb6lbw3`，每次创建独立UTF8数据库并在结束时删除；从未使用其他会话数据库。升级和恢复前先停止所有测试节点。

未部署、未推送；不把本批验证扩展为后续宿主模块授权已完成。

最终预飞全部通过。本任务专用PostgreSQL实例已正常停止，数据目录及server.log留存；未操作其他数据库实例。

## 用户阅读修改：HTTP文件组织

用户指出HTTP层虽然行为容易理解，但代码堆放在一起。以`32730c2cb`为起点，仅整理HTTP包：按用户后续反馈，路由与共同处理集中放在handler.go；三个业务组各自放处理函数和响应结构；前端权限映射单列；角色输入解析放回角色操作旁。请求字段改为http、subject，处理函数按业务明确命名，长结构体改为带字段名的多行写法。根包、数据库、app、host以及接口路径、JSON字段和授权顺序均保持原样。

上方完整预飞对应初次候选；本次结构整理的验证结果另记于本节，不将旧预飞当作新代码的验证。

本次验证通过：EE全量vet、race测试、构建，1A/1B代码冻结范围检查、README链接及工作台校验；SQLite真实HTTP覆盖九接口、严格输入、授权/撤权及宿主边界。证据目录：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-1c-smoke-ho7k_f3b`。本次仅结构整理，未重复执行PostgreSQL和升级恢复演练。改动保留工作区，供用户继续阅读。

## 用户阅读修改：按1A风格重写注释

用户要求1C按已确认的1A标准重写注释。本次覆盖HTTP接口及响应类型、JSON解析、前端权限表、app启动与事务连接、管理员恢复、host中的角色接入和配置处理，以及对应测试与冒烟说明。用具体用途替换“认证投影”“恢复锚点”“存储范围”等抽象说法；补充空数组、null、权限来源和初始化管理员的含义。

修改前保存工作区快照，使用Go词法分析逐个比较16个Go文件，忽略注释后所有代码完全一致；Python脚本除文档字符串和注释外，语法树一致。此次为注释修改，不重复运行数据库或HTTP演练，不将先前执行结果冒充本次重新执行。改动保留工作区供继续阅读。

## 用户确认HTTP采用四文件组织

用户同意将HTTP生产代码收为四个文件：handler.go负责路由和共同检查，api.go集中九个接口并保留三个业务分组，protocol.go集中公共JSON校验及错误响应，matrix.go保留前端权限映射。各组的输入与返回结构仍在附近，保留1A风格注释。api.go超过300行但未超过500行，已按规范评估：用户明确选择文件内业务分组，避免薄HTTP层重复根包目录；本次不再细拆。

此次仅移动原有声明并合并import，没有改变路由、参数、响应或业务调用。

四文件整理验证通过：修改前后63个Go声明忽略位置和注释后完全一致，EE全量vet、race测试及构建通过；范围冻结、README链接、工作台和空白检查通过。api.go共327行，两个测试文件保留。此次没有重复运行真实HTTP或数据库升级演练。

## 用户确认

2026-09-16，用户表示：“好吧. 细节我就不看了, 大致的框架没什么问题.” 据此记录为认可1C整体框架并跳过细节阅读，收口本轮四文件组织与注释修改；不记录为已逐行检查，也不扩展为期1整体Gate 2或后续阶段验收。按照此前确认的分批本地提交方式，将阅读修改保存在当前交付分支；本轮不提取期2代码。
