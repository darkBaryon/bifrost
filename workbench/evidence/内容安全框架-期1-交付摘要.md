# 内容安全框架：交付摘要

## 验收对象

本期交付公共检查与处置框架，以及显式开启的开发假检测器和 BF 装配。真实检测算法、配置管理和生产启用留到后续。用户已亲测 Playground 输入拦截并确认，在补审和集成验证通过后合入自有 `develop`。

原候选 `958fc9830` 的第 4 轮代码评审通过、收敛无碍。其后的整理与开发联调已补做第 5 轮评审；代码通过，收敛提出的 F8–F11 已修正，等待定向复核。合并最新 `origin/develop`（`53d83a9ee`）无冲突，集成候选 `f6c9437e6` 的七项预飞检查全部通过。最终候选及合入事实见本期验收记录。

## 已完成与抽查入口

| 验收口径 | 实现与证据 |
|---|---|
| 本地检测契约、规则和阶段 | [检测器契约](../../ee/internal/guardrails/detector.go)、[规则](../../ee/internal/guardrails/rule.go) |
| 按阈值放行、拦截、仅记录，显式失败策略 | [检查器](../../ee/internal/guardrails/checker.go)、[规则执行结果](../../ee/internal/guardrails/evaluation.go)、[测试](../../ee/internal/guardrails/checker_test.go) |
| Chat 全部历史输入、全部候选输出，拒绝未支持载体 | [文本提取](../../ee/internal/guardrails/plugin/text.go)、[边界测试](../../ee/internal/guardrails/plugin/text_test.go) |
| 真正阻断、禁止 fallback、日志不含正文 | [插件入口](../../ee/internal/guardrails/plugin/plugin.go)、[拒绝映射](../../ee/internal/guardrails/plugin/deny.go) |
| 输出规则拒绝流式，输入规则允许流式 | [管线测试](../../ee/internal/guardrails/plugin/integration_test.go)、[HTTP 联调](../../ee/scripts/guardrails-smoke.py) |
| 显式开发启用、默认关闭、关闭后重启无残留 | [app 装配](../../ee/internal/app/guardrails.go)、[开发联调证据](内容安全框架-期1-开发联调.md) |
| 上游字段变化提醒复审 | [结构检查](../../ee/internal/guardrails/plugin/schema_test.go) |

## 验证与边界

- EE vet/race/build、格式、差异、工作台视图及真实 HTTP 联调通过；第 5 轮代码评审独立重跑确认，并追加 SQLite 身份冒烟通过。
- Playground 普通消息正常返回，标记消息显示拦截提示，用户也完成了实测。模型和检测器均为本地替身，不代表真实检测效果。
- 默认不注册插件。开发开关只支持输入拦截、输入仅记录、输出拦截，不写入持久配置。
- 不涉及真实模型/检测服务、PostgreSQL、生产插件组合、容器或集群运行验证。早期品牌冒烟因缺 UI 失败不计通过，保留既有 FIND-026。

## 转交与遗留

- 历史漏检、限长顺序修复继续保持关闭；logprobs、消息名称、超大正文透传等按已声明兼容边界处理。详情保留于第 2–5 轮原始评审。
- 第 5 轮代码转收敛：方案接口名、旧摘要中的类型和文件链接已同步。
- 第 5 轮收敛转代码：HTTP smoke 必过入口、临时凭据权限与现场保留、从 develop 带入的部署限制，交定向复核逐项处置。
- 范围外工具和文档问题沿用 FIND-026/027，新增 FIND-028/029，见[台账](../findings.md)。

宏观流程、支持范围和启动方式见[模块 README](../../ee/internal/guardrails/README.md)。
