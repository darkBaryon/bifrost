# 独立浏览器验证结果

结论：指定范围全部通过，未发现阻断项。本结果是独立真实浏览器补充验证，不替代正式代码评审或收敛评审结论。

- 完成时间：2026-09-22T19:12:27.450728+00:00
- 候选：`030c362a57ff2b0e538b87ac3549af7b9cad6ded`；业务提交：`4adcf9b6b`。
- 候选目录：`/Users/xinyue/VSCode/ws_2026/bifrost-console-ui`。
- 浏览器：Playwright CLI，专用 session `console-bootstrap-af43cdc3be35`；真实临时 EE 服务：`http://127.0.0.1:54933`。
- 没有修改候选代码、评审记录、提示词或 Git 提交；未联系正式评审会话。

## 构建来源

复用指定正式代码评审构建清单，未重新构建。独立读取清单并逐文件比对当前文件和指定候选 Git blob，共 2,796 文件，差异均为 0；完整文件清单由 smoke 再次重新计算比对。源码总摘要 `f14a38c938cb10d046c933ac410abc92385e6f6474e6379e518891d2a2b81b56`，二进制摘要 `597b7d9a699ed96d115bae901fb5ba493cbc1b6d63f5962bf82ee1be1a594d6e`。结束后重新计算源码指纹仍一致，见 `provenance.json` 与 `source-after.json`。

## 实际执行

```sh
python3 -B ee/scripts/console-bootstrap-smoke.py --build-manifest /Users/xinyue/VSCode/ws_2026/bifrost-console-ui/workbench/tmp/codereview/工作台基础信息解耦-期1-代码评审1/scratch/bifrost-console-check-b6eip3bj/build/build-manifest.json --output /private/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/independent-console-browser-t7ar48dl/smoke
```

退出码 0，结果 `passed`。已独立检查 `routing.png` 实际截图、原始 DOM 快照及请求记录，并另行从原始 CLI 输出计算断言，见 `independent-assertions.json`。

| 验证项 | 结果 | 原始证据（相对 smoke/output/playwright） |
|---|---|---|
| 唯一权限 RoutingRules.View，已完成改密，无 Settings.View | 通过 | cli-009.txt；result.json fixture |
| 真实 bootstrap POST / {} 200 | 通过 | cli-008.txt |
| 真实 routing GET 200；完整 config GET 403 | 通过 | cli-008.txt、cli-009.txt |
| 路由页面呈现，无 config-unreachable | 通过；截图人工检查 | routing.png、cli-006.txt、cli-009.txt |
| 页面自动请求无 /api/config | 通过，记录在主动 HTTP 探针之前截取 | cli-008.txt |
| 菜单保留退出登录入口 | 通过；没有执行真正退出操作 | cli-012.txt、cli-013.txt |
| bootstrap 注入 503，显示错误页，解除拦截后点击重试恢复真实后端 | 通过 | cli-018.txt、cli-022.txt |
| 无 DB/无 logs 提示；仅 logs 时日志页挂载；无 reason 的重启通用文案 | 通过，均为 bootstrap 响应注入 UI 证据 | cli-026.txt、cli-032.txt、cli-039.txt |
| 清除模拟后真实 bootstrap 恢复 200 | 通过 | cli-047.txt |
| 公共认证成功页不请求 bootstrap，部署启用认证且浏览器匿名 | 通过 | cli-052.txt、cli-054.txt、cli-055.txt |
| 临时页面不请求 bootstrap，部署启用认证且浏览器匿名 | 通过 | cli-060.txt、cli-062.txt、cli-063.txt |

## 证据边界

503、DB/logs 和重启分支由 Playwright 拦截 bootstrap 响应产生，仅证明 UI 分支与恢复行为，不证明真实数据库故障、日志数据授权或重启存储故障。日志页面快照确实挂载，但日志数据区出现 `Not_found`，因此不将其记作日志数据访问成功。临时页使用无效测试 fragment 且缺少 flow，页面显示“缺少流程标识”；本次仅验证其匿名最小外壳跳过 bootstrap，不代表完成有效临时令牌的完整 MCP 流程。

初始自动请求中 providers、keys、notifications 和 ws-ticket 存在 403；路由页面和 bootstrap 仍成功，本次不据此宣称这些能力获授权。

## 清理

独立核对 fixture owner/server 和三次 browser open 记录的进程共 5 个 PID 均已不存在，测试端口 54933 已关闭，private-state.json 与 *.storage.json 已删除；详见 `cleanup.json`。只清理本次启动的资源，未操作用户 59817 服务或全局进程。
