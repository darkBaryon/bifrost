# 最终候选独立浏览器差量确认

结论：指定浏览器验证范围全部通过，未发现新的阻断项。本记录仅为独立补充浏览器验证，不替代正式代码评审或收敛评审结论。

- 完成时间：2026-09-22T19:29:27.663750+00:00
- 最终候选：`fe626961c86acda99a7fddd394f51f137989cd3b`。
- 首轮候选：`030c362a57ff2b0e538b87ac3549af7b9cad6ded`。
- 候选目录：`/Users/xinyue/VSCode/ws_2026/bifrost-console-ui`。
- 差量独立检查：5 个非 Workbench 文件，内容为注释、README 和 59817 等值常量提取；完整差量保存在 `delta.patch`。
- 使用已读取的 playwright 技能，以独立 Playwright CLI session `console-bootstrap-570043a8eb24` 执行；隔离临时 EE 端口 `56518`。
- 源码、正式评审记录、提示词和 Git 提交均保持只读；未联系正式评审会话，未复制 Cookie/私有状态到仓库。

## 构建来源与最终提交对应关系

复用 `/private/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-console-check-xvdoydq7/build/build-manifest.json`，`complete=true`，未重建。

该清单的 `head` 元数据仍为旧 HEAD `030c362a57ff2b0e538b87ac3549af7b9cad6ded`，因此没有单凭 HEAD 字段认定来源。独立逐一比对清单中 2,796 个文件的 SHA-256：清单、构建源码快照、最终候选 Git blob、当前工作树全部相同；另重新生成完整源码文件清单也一致。该内容核对把构建准确关联到最终候选。

- 最终源码指纹：`99206cf7f5d30e389d1e11393f9641cf9239e391b2a2b0790e4c03130477d901`。
- 二进制指纹：`90799d2fb73d375ad7f191cb4f4560261ffa7fa7f5ca08dfc2bfd5cba4e0c604`，与清单一致。
- 结束后源码指纹再次核对一致，见 `source-after.json`；详细四方逐文件核对摘要见 `provenance.json`。

## 独立执行与原始证据

```sh
python3 -B ee/scripts/console-bootstrap-smoke.py --build-manifest /var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-console-check-xvdoydq7/build/build-manifest.json --output /private/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/independent-console-final-tmpiw45g/smoke
```

退出码 0，`smoke/output/playwright/result.json` 状态 `passed`。已人工查看 `routing.png` 实际截图，并读取关键 DOM 快照和原始 API 状态记录；独立从 CLI 输出重算 17 个断言，全部通过，见 `independent-assertions.json`。

| 验证项 | 结果 | 原始证据（相对 smoke/output/playwright） |
|---|---|---|
| 唯一权限 RoutingRules.View，改密完成且无 Settings.View | 通过 | cli-009.txt、result.json fixture |
| 真实 POST bootstrap，JSON 空对象，返回 200 | 通过 | cli-008.txt |
| 真实 routing 返回 200，完整 config 返回 403 | 通过 | cli-008.txt、cli-009.txt |
| 路由页呈现且无 config-unreachable | 通过，已人工检查截图 | routing.png、cli-006.txt、cli-009.txt |
| 页面自动请求不包含 /api/config | 通过，使用主动探针之前的请求记录 | cli-008.txt |
| 菜单保留退出入口 | 通过，只验证入口存在 | cli-012.txt、cli-013.txt |
| 注入 bootstrap 503 后显示错误页，解除拦截后点击重试恢复 | 通过，恢复使用真实后端 | cli-018.txt、cli-022.txt |
| 无 DB/无 logs 提示；logs-only 页面挂载；无 reason 重启通用文案 | 通过，仅为注入响应的 UI 证据 | cli-026.txt、cli-032.txt、cli-039.txt |
| 移除所有注入后真实 bootstrap 再次 200 | 通过 | cli-047.txt |
| 公共认证成功页：启用认证的部署、匿名浏览器、0 bootstrap | 通过 | cli-052.txt、cli-054.txt、cli-055.txt |
| 临时页面：启用认证的部署、匿名浏览器、0 bootstrap | 通过 | cli-060.txt、cli-062.txt、cli-063.txt |

## 验证边界

503、DB/logs、restart 分支由浏览器拦截 bootstrap 响应产生，仅证明 UI 分支及重试恢复，不证明真实存储故障或日志数据权限。日志页面存在刷新控件和内容区，但不是日志数据访问成功的证明。

临时页面使用无效测试 token fragment，且缺少 flow；实际 DOM 显示“缺少流程标识”。本次确认最小外壳跳过 bootstrap，不宣称验证了有效临时令牌的完整 MCP 流程。退出仅确认入口存在，没有执行真正退出。

初始自动请求中 providers、keys、notifications、ws-ticket 返回 403；bootstrap 和 routing 仍成功。本轮不将这些受限能力记作已授权。

## 清理核对

本次创建的 fixture owner/server 及三次 browser open 所记录的 5 个 PID 均已不存在；测试端口 `56518` 已关闭。`private-state.json` 和 `*.storage.json` 已删除；详见 `cleanup.json`。未占用或操作用户 59817 服务，也未操作全局进程。
