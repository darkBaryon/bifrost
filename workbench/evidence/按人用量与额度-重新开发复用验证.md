# 重新开发：既有治理计数器复用边界

基线：`d93fa8ad2082c716f76c55720b2272e718a7f649`。实验位于[reuse_test.go](按人用量与额度-复用验证/reuse_test.go)，不向网关装配，不改上游。

从`ee/`运行：

```sh
GOCACHE=/tmp/codex-bifrost-user-usage-go-cache GOWORK=off go test -p 1 -count=1 -v ../workbench/evidence/按人用量与额度-复用验证/reuse_test.go
```

2026-09-23，源文件先在本机临时目录运行，三个测试全部通过；入分支后用上面路径再次运行，仍为3个测试通过、退出0。源文件共101行，计入本分支代码审阅量。测试通过表示观察到下列**既有行为**，不表示新功能通过。

| 测试 | 实测结论 | 设计含义 |
|---|---|---|
| SharedCounterAndConfigReplacement | 两次按同一预算ID分别扣10/15后为25；调限额不清零；用满后拒绝 | 可以复用累计和配置热更新原语；没有测试真实VK识别 |
| LateChargeUsesCurrentMonth | 先捕获旧预算ID，重置到新月，再扣旧请求费用；费用进入新月 | Limit没有周期身份。单纯复用稳定预算ID不足以保存调用开始周期；不代表上游自身有bug |
| MissingCounterIsNotAnAdmissionFailure | 预算不存在时检查允许、扣费不新建也不报错 | EE必须明确验证受管配置存在和装配成功，不能把查库失败当普通调用 |

测试不调用外部模型，无凭据、真实员工数据或付费请求。没有验证数据库持久化、旧周期保留、HTTP全链、流式、各协议或并发容量；也没有证明独立账本是唯一方案。

源码另确认`GatherLimits`、`ChargeBudgets`等接口已存在；具体如何装配需要当期方案和实际链路验证。现阶段不把旧实现的7张表、重试器和各协议适配当作重新开发的既定前提。
