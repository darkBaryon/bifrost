# 账号认证期1 Handler复核纠偏

用户在Gate2前指出handler存在大量硬编码。本次撤回此前“可进入验收”的建议，保留历史评审原文，由新的独立收敛会话重读整个handler与直接受影响边界，不沿用历史“无碍”作为代码质量依据。

## 为什么此前通过

首轮收敛评审已看到裸状态码，却以“HTTP映射集中且可直接对照契约；不以常见HTTP数字本身要求整批重写”判为无需修复；对魔数主要核查迁移锁与限流，没有完整处理handler的请求体/深度、Retry-After等限制。二、三轮只对新增差量复核，继承了首轮判断。问题在评审执行与局部质量判断，不能用测试通过、报告齐全或缩小差量范围说明代码已符合要求。

## 本轮修复

- HTTP状态码/方法与Cookie删除到期值复用fasthttp已有命名；请求体16KiB和JSON深度64就近命名。
- 数据库存储的限流窗口和HTTP Retry-After采用同一业务常量；IP/用户名阈值有业务名称。
- 原散落action字符串与按路径前缀判断兼容行为，收敛为单一路由表、类型化operation及显式匿名/空体属性。精确归属与注册共用同一表，新增路由默认要求会话。
- 使用命名响应类型和集中消息，固定错误序列化降级复用业务错误码；不再重复拼写响应键值/原始错误JSON。
- 在本次重读范围内检查同类HTTP适配，保持原接口字段、响应码、权限和兼容行为；不将协议路径和标准JSON标记机械地改为部署配置。

上述为已批准实现的质量修正，没有新增产品能力。已补实际HTTP契约/路由边界回归；正式候选及独立结果见下文。原签发/限流/过期数值不变。

## 开发验证记录

定向HTTP和宿主适配race通过，覆盖15条POST/1条GET的精确归属与匿名边界、POST来源拒绝、旧空体兼容、16KiB与深度边界、错误/Retry-After及JSON序列化降级。

预飞7恢复专用PG集群时漏带原随机监听端口，身份PG测试连接原端口被拒；属于开发测试环境恢复错误，非业务失败。已停止本次错误启动的自有集群，按该任务元信息中的原回环地址/端口恢复并执行SELECT 1确认；保留预飞7及完整输出，后续重跑不可覆盖本次红表。原故意失败库未删除。

预飞8的11条命令及全部产物/符号检查全绿，见[预飞8](账号认证-期1-预飞8.md)。最后移除status中被endpoint覆盖的重复no-store设置并补充辅助函数注释后，再次运行HTTP/宿主适配race通过。正式独立复核针对随后提交的候选重新构建与检查。

## 新候选与定向闭环

- `eef048ffb18e3a9a8b4f51730bfdb2d56568e45a`提交本轮Handler整理。新独立收敛会话完整重读Handler、直接关联实现、测试及实际品牌HTTP惯例，未沿用前三轮局部豁免。
- 初读再指出README测试归属不准确及最后注释前缺gofmt空行；`4a61ab59f454aff57ea5609158a6f340e347bd6b`仅修正这两项，新评审已定向确认关闭。原候选至最终候选仅README一句说明和五处空行，无行为差异。
- 最终独立构建：`/tmp/bifrost-auth-review4-4a61ab59f/bifrost-http`，VCS revision为4a61ab59完整提交，modified=false；SHA256为`889f984eff1a743baad51c2e44a88228c3f67fb745c69e6641e929808fe7c257`。
- 独立代码复核实测SQLite/PG限流：账号5次、来源IP30次，60s前1ns仍拒绝、恰好60s放行；最终二进制验证强制改密受限权限、CSRF、仅旧登出/票据空体、实际Cookie删除及旧会话失效均通过。详细范围及精确测试对象以独立记录为准。

第四轮[代码评审](../cases/账号认证/期1/代码评审4.md)通过、[新独立收敛评审](../cases/账号认证/期1/收敛评审4.md)无碍、[独立测试](账号认证-期1-独立测试4.md)通过。README转交项与收敛C1对应并已关闭，无新增正确性转交。测试方Cookie属性大小写断言纠偏保留在独立测试4中，没有为迎合夹具修改业务代码。

本机开发二进制已替换为上述最终独立产物并复核SHA一致；各自有EE测试进程结束，专用PG已停止，原数据/故意失败库/日志保留。Gate2未通过，不归档、不标记交付、不集成发布。
