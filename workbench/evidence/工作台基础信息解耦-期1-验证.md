# 工作台基础信息解耦：验证记录

## 修改前回归红灯（2026-09-23）

使用已编译的原始基线 EE（业务源码 d93fa8ad，fa3eaced 只增加调研记录）及完整前端，在全新临时数据目录和随机端口50953创建只含 RoutingRules.View 的账号，先完成改密后加载全新浏览器上下文。

回归断言“路由查看账号不得出现 config-unreachable”失败，脚本退出码1。完整config403、主动路由列表探针200、权限列表唯一代码RoutingRules.View；浏览器实际配置请求403，正文config-unreachable。这个失败来自预期页面行为，不是编译/启动/工具失败。浏览器及自建EE服务已停止。

[原始结果](工作台基础信息解耦-期1-验证/baseline/result.json) · [修改前截图](工作台基础信息解耦-期1-验证/baseline/routing.png)

二进制SHA256：`281a1a97d119d668908f024e2654761cbcc7fcef7c9403e5542a93d5c83e398e`。本次没有运行成功页面的模拟HTTP，也未授予Settings.View。

## 候选验证

本节记录首个实施候选的开发自测，独立评审与最终候选结果见后文；不表示全站权限已接入或获准合入。

真实EE使用临时SQLite与随机端口52982，账号先完成改密，权限唯一为RoutingRules.View。浏览器自动POST bootstrap返回200且正文为{}，自动路由请求200；外壳没有自动请求完整config。主动探测完整config仍403，路由页面正常显示（无config-unreachable）。已人工查看候选截图。

[候选结果](工作台基础信息解耦-期1-验证/candidate/result.json) · [候选截图](工作台基础信息解耦-期1-验证/candidate/routing.png) · [预飞事实表](工作台基础信息解耦-期1-预飞.md)

- Go vet与定向race全部通过。
- EE/OSS分别构建及完整UI类型检查通过；22项组件/协议测试通过。
- 浏览器退出入口存在。首次bootstrap注入503显示重试，撤销拦截后通过真实接口恢复。
- 仅对bootstrap响应模拟三种配置：无配置/日志存储显示原提示、仅日志模式挂载日志页、无内部reason的重启标记显示通用提示。此三项只证明UI分支，不证明日志数据授权；拦截撤销后重新验证真实bootstrap200。
- 匿名公共页、临时令牌页面均无bootstrap请求。测试Cookie文件已删除，自建EE进程与浏览器已关闭。
- 通知、WS票据、providers及keys仍出现403，属于后续登录/权限接入及业务数据依赖范围。导航与新建按钮仍是现有all-allow fallback；这次仅证明外壳与Settings.View解耦，不能解释成已接通完整前端RBAC。

构建二进制SHA256：`a4aae3f2d730c7dce94e8a7cbbf7e9dd2603b10c812baa5d9492f6db5f949ea8`。2796个源码文件及根EditorConfig的逐文件指纹见[构建清单](工作台基础信息解耦-期1-验证/checks/build-manifest.json)，聚合SHA256为`f14a38c938cb10d046c933ac410abc92385e6f6474e6379e518891d2a2b81b56`。验证结束再次核对与工作树内容完全一致；构建清单HEAD为实施基线，待提交工作树的实际内容由逐文件散列约束，提交后再核对候选。

## 过程事实

- 首轮统一预飞：Workbench、全EE vet和定向race通过；隔离源码格式检查报告11个文件不匹配，尚未进入构建与浏览器。不把检查脚本失败算作产品回归红灯；[失败事实表](工作台基础信息解耦-期1-验证/checks/preflight-format-failed.md)保留供复核。
- 用户误拖动导致ee短暂位于examples/ee；确认743个跟踪文件及新增实现完整后，经用户明确指示原样移回。未还原/覆盖源码内容，未访问用户59817服务。

首轮格式失败根因：隔离快照漏带根目录 `.editorconfig`，其中明确 `insert_final_newline=false`。因此快照格式器错误要求11个文件补末尾换行；真实源码符合仓库契约。修正为将该文件纳入源码快照和指纹，未改动这11个文件。Sidebar既有格式债务仍独立按固定基线对照。

## 候选与独立评审状态

本地候选 `4adcf9b6b53aeeea9b2cc1b33deb3b04afe30303` 已提交，提交后重新核对2796个文件的指纹与已通过构建完全一致。业务工作树干净。

尝试按既有 `code_review.py` 启动独立评审时，被自动审批拒绝执行；没有创建评审运行目录，也未启动后续Claude收敛评审。拒绝理由为向外部Codex服务发送可能敏感的源码需要用户明确授权。目前已单独询问是否授权Codex与Claude两项评审，不把本地自测冒充独立评审通过。未推送、合并或发布。

用户随后明确授权：“允许这两项独立评审”。按原脚本已启动 Codex 代码评审和 Claude 收敛评审，候选为 `030c362a57ff2b0e538b87ac3549af7b9cad6ded`（相比4adcf9b6b仅补WorkBench验证状态说明，业务源码完全相同）。等待独立结论，不修改评审提示词或评审原文。

## 评审后收尾候选

首轮代码评审通过、收敛评审仅立债挂账，均无阻塞项。首轮浏览器环境限制已由[独立浏览器验证](工作台基础信息解耦-期1-验证/independent-browser-r1/独立浏览器验证.md)补齐，其2796个文件逐一与候选Git blob核对一致，17项独立断言通过。

收尾仅增加维护注释/文档和将59817提取为同值常量，未改变接口、权限或UI代码。对收尾内容已重新运行完整[预飞2](工作台基础信息解耦-期1-预飞2.md)，全绿：Go vet/race、EE/OSS构建及类型、22项前端测试、真实浏览器及故障/配置注入场景均通过。完整config仍403，唯一权限RoutingRules.View的页面正常；测试服务与浏览器已回收。

[收尾候选结果](工作台基础信息解耦-期1-验证/final-candidate/result.json) · [截图](工作台基础信息解耦-期1-验证/final-candidate/routing.png) · [构建来源](工作台基础信息解耦-期1-验证/final-candidate/build-manifest.json)。源码指纹 `99206cf7f5d30e389d1e11393f9641cf9239e391b2a2b0790e4c03130477d901`，二进制 `90799d2fb73d375ad7f191cb4f4560261ffa7fa7f5ca08dfc2bfd5cba4e0c604`；保存时再次核对当前源码完全一致。后续代码评审2只处理转交核对和这五个文件的差量，不把已完成的功能重新扩大。

## 最终候选独立浏览器复验

源码最终提交 `fe626961c86acda99a7fddd394f51f137989cd3b`。未参与实施的独立验证任务对该版本再次执行浏览器回归，退出码0，17项独立重算断言全部通过；[正式独立记录](工作台基础信息解耦-期1-验证/independent-browser-final/最终候选独立浏览器验证.md)保留原文，[原始结果](工作台基础信息解耦-期1-验证/independent-browser-final/smoke/output/playwright/result.json)与[截图](工作台基础信息解耦-期1-验证/independent-browser-final/smoke/output/playwright/routing.png)另存，不覆盖首轮证据。

独立比对2796个文件在构建清单、构建源码快照、最终候选Git blobs和当前工作树之间完全一致，二进制SHA256匹配。构建清单里的head字段是提交收尾前的旧值，最终版本归属靠完整输入清单及逐文件内容核对确认，未把旧HEAD元数据当作最终版本标识。

真实账号唯一权限RoutingRules.View，bootstrap/routing均200、完整config403，页面没有自动config请求；退出入口、503后真实重试恢复、明确注入的配置分支、公共/临时页面skip均通过。注入与临时页面验证边界保持前文说明。临时端口56518已关闭、本轮5个进程已退出、私有会话文件删除，用户59817未操作；结束后源码指纹未改变。

## 最终独立评审结论

[代码评审2](../cases/工作台基础信息解耦/期1/代码评审2.md) 对最终源码 fe626961 给出“通过”，四项转交均撤回阻塞质疑或撤回，无新修复项、nit或转收敛项；明确内容指纹不覆盖权限元数据。亲跑双构建/类型/格式、22项前端测试、强制重跑四包race与27项真实HTTP检查通过。本轮浏览器同样在访问产品前因Chrome沙箱SIGABRT失败，保留失败现场；评审者已独立核验上述最终浏览器任务的原始网络、DOM、截图和来源，未冒充为自己的页面执行。

代码评审1/2通过，收敛评审1仅立债挂账且无阻塞，FIND-031保留。两侧转交、全部nit处置和测试边界已汇总到[验收记录](../cases/工作台基础信息解耦/期1/验收记录.md)。技术交付证据齐备，仍待用户Gate2；没有推送、合并或发布，不关闭原完整登录/权限接入需求。

最终记录的空白检查排除原始 `independent-browser-final/delta.patch`：patch 的上下文前导空格和空白上下文行属于格式本身，原始字节已与 `git diff 030c362a..fe626961 -- . :\(exclude\)workbench` 的输出逐字节核对一致，不改写独立证据。其余暂存文件的 `git diff --cached --check` 通过，Workbench 217篇文档校验通过。
