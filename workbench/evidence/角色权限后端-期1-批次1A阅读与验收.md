# 角色权限后端：批次1A阅读与验收

当前只需读5个业务文件，共829行；3个测试文件共372行，单列审阅。数据库、HTTP和大型JSON均不在本批。四期六批的整体安排见[蓝图](../cases/角色权限后端/开发蓝图.md)。

## 比较范围

- 当前分支：`feat/user-permissions-phase1`。
- 独立代码提交：`3bed654e0`（业务829行、测试372行、README 27行；分期文档另一个提交）。
- 比较基线：`f371548b5444693d7e259033bec9f4a467e3748b`（认证已交付）。
- 原完整候选保留在 `feat/user-permissions-backend`，固定源 `b38493b07`；没有删除、覆盖或重写原分支。
- 当前只包含期1的首个代码验收批次，1B/1C尚未抽取。新方案与正式评审尚未完成，不提请完整期1 Gate 2。

本批提交后可在仓库根查看纯业务增量：

```bash
git diff f371548b5444693d7e259033bec9f4a467e3748b..HEAD -- ee/internal/rbac/model.go ee/internal/rbac/permissions.go ee/internal/rbac/authorize.go ee/internal/rbac/roles.go ee/internal/rbac/assignments.go
```

## 阅读顺序

| 顺序 | 文件 | 行数 | 需要确认的业务规则 |
|---|---|---:|---|
| 1 | [permissions.go](../../ee/internal/rbac/permissions.go) | 175 | 固定目录、三个预置角色、chief动态全权；Manage不隐含其他权限 |
| 2 | [model.go](../../ee/internal/rbac/model.go) | 184 | 错误与业务对象、身份句柄、统一快照和事务契约 |
| 3 | [authorize.go](../../ee/internal/rbac/authorize.go) | 143 | 每次调用重验、多个角色取并集、保留授权来源 |
| 4 | [roles.go](../../ee/internal/rbac/roles.go) | 204 | 输入校验、增删改查、分页、预置/关联保护 |
| 5 | [assignments.go](../../ee/internal/rbac/assignments.go) | 123 | 角色分配、禁止自改、最后chief保护、恢复只补关系 |

5个文件沿用原候选；仅把 `authorize.go` 的19行通知受众检查留到期4。范围检查逐文件与固定源比较，不靠人工声明“没带其他功能”。

## 可复现的规则验收

在 `ee/` 运行：

```bash
GOWORK=off go test -race -count=1 -v ./internal/rbac
```

| 场景 | 期望 | 测试 |
|---|---|---|
| 创建重复权限角色、未知码、删有关联角色、修改chief | 去重；未知码拒绝；有关联/预置不可删；chief权限不可缩减 | TestRoleLifecycleAndProtection |
| 两个角色共享查看权、其中一个有管理权 | 权限并集且保留两个来源；管理不隐含查看/正文；撤回后下次调用拒绝 | TestMultipleRolesSourcesAndRevocation |
| 自己给自己加chief、撤最后chief、恢复锚点 | 前两者拒绝；恢复幂等补chief并保留其他角色 | TestChiefAssignmentAndRecoveryRules |
| 停用/强制改密主体、仓储报错 | 拒绝访问；不输出仓储内部错误 | TestSubjectAndStorageFailuresDenyAccess |
| 分页期间新增角色、无权查不存在的角色 | 游标后续页不插入新增角色；先拒绝授权再查询存在性 | TestPaginationAndAuthorizationOrder |

## 实际检查结果

- 规则测试：7个顶层测试通过，包含2个身份状态子场景；`-race`通过。
- 独立业务包：`GOWORK=off go vet ./internal/rbac` 和 `GOWORK=off go build ./internal/rbac` 均通过。
- 范围核对、格式及Workbench检查通过；完整EE vet/race测试/构建未通过，详见[首次预飞事实表](角色权限后端-期1-批次1A预飞.md)。
- 整工程检查的环境问题：`ee/tmp/rbac-review5-probes/main.go` 是原分支遗留的忽略文件，仍依赖未抽取的 `rbac/http`；随后race测试/构建出现 `No space left on device`。当前只剩约0.82GB实际可用空间，本批开始后创建的Go缓存约1.94GiB。无开始前磁盘基线，不能推断此前20GB空间的全部去向。
- 已停止大范围编译。checklist的整工程范围修正为实际源码目录 `./cmd/... ./internal/...`，避免再次把旧探针纳入；空间恢复后仍需重跑，当前不宣称整工程预飞通过。独立业务包的检查另行记录。
- 正式独立方案/代码/收敛评审及用户验收：未完成；不能沿用原整包的通过记录。

## 本批证明不了什么

内存仓储不模拟数据库事务和锁，也不校验真实会话。SQL回滚、并发管理员保护、初始化迁移、双节点、HTTP越权及WS撤权需要后续批次证明。当前应用没有调用rbac包，不改变账号登录、管理权限或数据表。

用户确认本批规则与差异后，才抽取1B存储/身份接入；1C完成后再做期1完整功能验收。后续不会重新交付累计四千行让用户重复阅读。
