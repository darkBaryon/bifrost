# 角色权限后端：批次1A阅读与验收

## 当前结论

2026-09-16，用户逐文件阅读后确认1A业务代码“没什么问题”，要求将ReadView改名，完成Queries改名后同意收口提交。测试代码按用户要求跳过阅读；自动验证仍执行。这里只记录1A用户确认，不代表期1整体交付或正式独立评审完成。

## 比较范围

- 交付分支：`feat/user-permissions-phase1`。
- 认证基线：`f371548b5444693d7e259033bec9f4a467e3748b`。
- 用户确认后的收口提交：`d811dfd07`。
- 原抽取提交：`3bed654e0`；本次阅读修改以前一候选`d09e2a2e9`比较。
- 固定来源：`b38493b07`，保留在`feat/user-permissions-backend`，本批修改不回合该分支。
- 当前6个Go业务文件913行，3个测试文件414行；无数据库、HTTP、app及上游接线。下一批以本次收口提交为基线。

## 已确认文件

| 文件 | 行数 |
|---|---:|
| [permissions.go](../../ee/internal/rbac/permissions.go) | 169 |
| [roles.go](../../ee/internal/rbac/roles.go) | 327 |
| [authorize.go](../../ee/internal/rbac/authorize.go) | 190 |
| [assignments.go](../../ee/internal/rbac/assignments.go) | 132 |
| [repository.go](../../ee/internal/rbac/repository.go) | 51 |
| [errors.go](../../ee/internal/rbac/errors.go) | 44 |

## 阅读期间的调整

- 权限按模块分组，补直白的中文说明；模块目录先于逐项权限。
- 内置角色改为多行定义，并与角色类型、操作一起移入roles.go。
- 删除model.go：账号/权限结果归authorize.go，存储接口归repository.go，错误归errors.go。
- Authorize先检查身份，再读取账号权限并匹配；私有辅助函数改为checkLogin、accountPermissions、checkPermission。
- ReadView改名Queries，查询参数统一queries；Tx继续在查询能力上增加修改能力。
- README按用户指定的新编码规范整理；补充Snapshot随角色变化的回归验证。

## 自动验证

2026-09-16完成：正式源码目录`./cmd/... ./internal/...`的`go vet`、`go test -race`及HTTP可执行文件构建通过。1A规则测试8个顶层测试通过。格式、范围及文档检查以[收口预飞](角色权限后端-期1-批次1A收口预飞.md)为准。

`go test ./...`及`go vet ./...`会包含`ee/tmp/rbac-review5-probes`中的历史忽略脚本，该脚本引用尚未抽取的rbac/http；因此清单固定检查正式源码目录，不删除旧脚本。旧磁盘故障是历史事实，不再当作当前阻塞。

[首次预飞](角色权限后端-期1-批次1A预飞.md)和[原候选代码核查](角色权限后端-期1-批次1A代码核查.md)保留当时事实，后者仅覆盖d09e2a2e9，不覆盖后续阅读修改。

## 证明边界和后续

内存仓库不验证真实会话、SQL锁和回滚。本批不改变运行时行为，不注册接口或迁移。1B接入存储和身份事务协作，1C接HTTP和app；后续仍逐批给用户阅读确认。
