# 账号认证期1关联启动修复验证

代码评审2在候选 `5b784f32ae66404c3a55a51d2ceae3b0bd007064` 的全新 default-valid 数据目录启动遇到 `ee: migrate branding: database is locked`，现场 `bifrost-identity-upgrade-x1v7aw9h` 保留。该既有品牌路径同时影响新版本可启动性，作为R5本轮修复，不以重跑通过关闭。

## 修复范围

仅品牌存储的MigrateBranding外层增加SQLite typed busy/locked的有限整事务重试，最多3次，间隔20/40ms；表、单例、版本在同一事务中回滚/提交。非锁错误不重试，耗尽返回原错误，取消中止等待。PG既有事务锁保持不变，没有依赖identity模块或建立额外公共抽象。README与方案偏差同步。

## 开发验证

- SQLite：`cd ee && GOWORK=off go test -race -count=1 ./internal/branding/persistence`通过（2.884s）。短暂busy第二次成功且表/版本各一条；持续busy第三次耗尽无残留表；非锁故障仅一次；取消不继续迁移；既有真实连接持锁/释放后重试、旧库兼容、事务写入与并发合并均通过。
- PG：同命令，BRANDING_TEST_POSTGRES_DSN经环境注入专用集群，通过（2.507s）。真实PG锁/取消/重试及既有存储回归通过；SQLite专用测试按说明跳过，与前一项结果配对使用。
- [预飞6](账号认证-期1-预飞6.md)原11条命令及全部产物/符号全绿，包含真实SQLite/PG节点启动、会话全流程和品牌回归；本轮身份与品牌Go测试均注入专用PG环境。完整每命令输出现场 `bifrost-preflight6-output-pb0ietwh`。新固定提交的独立差量复核待回填。旧基线二进制不修改；本轮修复不能宣称旧产物初始化锁冲突已经消失。
