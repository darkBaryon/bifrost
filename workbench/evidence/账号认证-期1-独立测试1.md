# 账号认证期1独立测试1

**结论：未通过（当前固定候选不作为 T1 放行依据）。** 指定构建、8项命令及最后一轮43项补充断言全部通过；但独立补充夹具首次启动发生真实 SQLite 身份迁移失败，原库重启成功、随后5个全新库启动均成功，仍缺安全诊断定位首次失败原因。本记录保留红灯，不把未定位的启动失败记成通过。另有独立代码评审待修事项，由对应评审记录负责，不用其结论替代本次执行事实。

测试职责：独立于实现的测试 agent，仅写证据和专有临时脚本，未修改业务源码、未提交、未生成 views。时间：2026-09-11 05:32–05:40（Asia/Shanghai）。已阅读根/workbench AGENTS、测试规范、批准方案v2、checklist、交测包。

## 测试对象与 T0

- 分支：`feat/account-authentication`；完整候选：`eb93291fcae2248054309e09a69e01aac84ed6f0`。
- 最终业务提交：`00860f57c1a9b55de8bcbe4a752a19a5502aaa88`；基线：`3e15a640d89902772445d3fb4c04023e58828eba`。
- 开始 `git status --porcelain` 为空；候选相对最终业务提交只增加交测包/预飞3两份文档。基线至候选共49文件、4873增/34删，主要为EE身份模块、宿主可选认证接入、WS回调、脚本和产品/Workbench文档。
- 不继承开发二进制。独立命令：`cd ee && GOWORK=off go build -o /var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-auth-independent1-9u9sorv2/bifrost-http ./cmd/bifrost-http`，退出0。
- 本次二进制 SHA256：`8a51b285f5e62a9bf58c443c695306fed073aa19cd63966e4e86ce3c4caa71e4`。
- `go version -m`：Go1.27.1；`vcs.revision=eb93291fcae2248054309e09a69e01aac84ed6f0`、`vcs.time=2026-09-10T21:31:58Z`、`vcs.modified=false`。本轮无需用文档差异解释 modified。
- UI复用交测包允许的同源构建产物：构建前逐文件核对，embed除`.keep`外543文件与`ui/out`完全相同；lockfile SHA256=`511957b09ca6a2b76cf0bfafc8280d1d593f36347f98aa0817050bf63a678a8f`。完整路径/哈希清单保存于`t0.json`，包含`.keep`的规范JSON清单SHA256=`1cfc052c2c36a5efcc1a4893535344c903d771da09ae0d6209fc676b9226ba71`。
- 当前主agent按评审进行后续修复前，所有依赖工作树源码的Go命令均已结束并通知解冻；后续反例只用上述固定二进制及`identity-smoke-fixed.py`副本。本记录不适用于后续新提交。

T0：通过。对象可获取、源码干净、构建成功，预飞3交测资料齐全；亲核运行时为本机隔离实例，无真实业务库/账号、无模拟器、无provider请求。

## 环境与隔离

macOS Darwin25.4.0 arm64，Go1.27.1。PG通过专用连接实际执行`SHOW server_version`得到`17.11 (Homebrew)`；DSN从`/tmp/bifrost-auth-test-pg.json`仅注入环境，报告与命令输出不含凭据。EE从模块目录以`GOWORK=off`使用当前仓库local replace。

HTTP SQLite使用临时config.db；PG smoke随机创建自己的database并启动两个回环端口节点；PG Go夹具各创建独立schema。执行脚本只停止自己启动的进程、删除自己创建的PG命名空间，未停止主agent持有PG集群。运行态由实际HTTP启动探针、跨节点Cookie使用和数据库读取确认，不仅依赖静态配置。

基线二进制只读复用`/tmp/bifrost-auth-baseline.json`指向的git archive产物，亲算SHA256与交测包一致：`35c042223f2bf0bec47fc9a3d8d16cfb5a131daf18bab39137b0f258a5f4ad21`。archive不带VCS字段；该限制明确记录，未捏造基线二进制VCS元数据。

## 自动化命令

命令中的`$BIN`表示本报告专有二进制绝对路径；`$LEGACY`表示已核对哈希的基线二进制。Go命令都从`ee`执行，显式`GOWORK=off`，`-count=1`避免继承测试缓存。

| 编号 | 命令 | 期望 | 实际 | 结论 |
|---|---|---|---|---|
| A01 | `python3 ee/scripts/identity-smoke.py --binary $BIN --database sqlite` | 所有HTTP/DB断言通过 | 退出0，13.44秒 | 通过 |
| A02 | 同上`--database postgres` | 共享PG双节点撤销/幂等/WS成立 | 退出0，16.69秒 | 通过 |
| A03 | `python3 ee/scripts/branding-smoke.py --binary $BIN` | HTML/图片/缓存/权限/重启保留 | 退出0，13.82秒 | 通过 |
| A04 | `python3 ee/scripts/identity-upgrade-smoke.py --binary $BIN --legacy-binary $LEGACY` | 迁移与一致备份降级可用 | 退出0，65.46秒 | 通过 |
| A05 | `go vet ./...` | 无静态问题 | 退出0，1.64秒 | 通过 |
| A06 | `go test -race -count=1 ./...`，PG环境 | EE已有测试及PG身份夹具通过 | 退出0，98.11秒 | 通过 |
| A07 | `go test -race -count=1 github.com/maximhq/bifrost/transports/bifrost-http/handlers github.com/maximhq/bifrost/transports/bifrost-http/server` | 宿主回归通过 | 退出0，85.60秒 | 通过 |
| A08 | `go test -race -count=1 ./internal/identity/persistence`，移除PG变量 | SQLite持久化/事务反例通过 | 退出0，56.49秒 | 通过 |

完整命令、退出码、耗时在`results.json`；对应原始输出为同名`.log`。A06有6个有测试包通过、3个包无测试文件；A07两个包通过，A08一个包通过。没有把“无测试文件”记为额外测试覆盖。

## 行为和最终状态

以下均亲自执行交测脚本，未以开发摘要代替。每项动作后脚本核对HTTP、WS帧或DB状态；原脚本内一个流程可能含多项断言，故不捏造总用例数量。

| 编号/优先级 | 前置与动作顺序 | 每步期望和实际 | 结论 |
|---|---|---|---|
| B01/P0 | 空SQLite/PG实例；错密钥初始化；并发两次正确初始化；登录跨节点查询 | 403；恰好201+409；新会话可跨节点使用 | 通过 |
| B02/P0 | chief创建普通成员；默认密码登录；先访问管理与事件，再本人改密 | 受限访问403；改密200；复制旧Cookie401；新密码可登录 | 通过 |
| B03/P0 | 成员两端登录后重置；旧Cookie访问；原operation_id重试；停用/启用 | 旧Cookie401；相同event_id且不撤销重置后新会话；启用不复活旧会话 | 通过 |
| B04/P0 | chief GET config并修改普通设置回送；再改旧认证字段同时改普通设置 | 首次200且37天保留生效；第二次409，最终普通设置仍37 | 通过 |
| B05/P0 | chief签ticket并在另一节点建WS；发送角色通知；本人改密；再触发通知 | 首条通知收到；旧HTTP401；旧WS关闭/关闭帧而不收到新通知 | 通过 |
| B06/P0 | 停全部本测试节点；stdin离线恢复；重启并查询事件；旧logout空体 | 恢复退出0；旧会话401；operator/admin_recovery及既有重置记录可查；logout真正撤销 | 通过 |
| B07/P1 | 旧开关开/关、短密码和特殊用户名；73字节文件密码回退DB；新改密后改文件重启 | 旧凭据可迁移；文件不覆盖新EE密码；旧Cookie拒绝 | 通过 |
| B08/P0 | 保留EE事件，停新实例，恢复完整合成SQLite备份，清旧sessions，旧二进制回环启动 | 无凭据401；旧密码登录200；新EE密码在降级库401 | 通过 |
| B09/P0 | 残缺旧凭据/缺DB/无setup密钥 | 前两者不开监听；无密钥初始化403且管理401 | 通过 |
| B10/P1 | 两节点累计错误登录；审查节点日志、SQLite会话表 | 达限429；已签Cookie/ticket/setup不在日志；库只存64字符摘要 | 通过 |
| B11/P1 | 品牌公开HTML/图标/字节、缓存；权限拒绝；合法更新及两次重启 | 脚本所有实际内容和保留断言通过 | 通过 |

## 独立补充反例

独立设计脚本`counterexamples3.py`，只复用基础进程/HTTP夹具，不调用开发脚本`exercise()`。新建独立SQLite、chief及boundary/another测试成员；最后43项HTTP/数据库断言通过。精确顺序、逐项期望与实际保存在`counterexamples3/results.json`，每项执行后立即断言，未并行发送状态变更。

| 组/优先级 | 操作 | 期望与实际 |
|---|---|---|
| C01/P0 | 缺Origin/Referer、只给同源Referer、错Origin+正确Referer、null Origin | 403、200、403、403 |
| C02/P1 | 表单、重复JSON键、尾随JSON、超过16KiB、非法UTF-8 | 均400 |
| C03/P1 | 无效cursor、limit101、limit负数 | 均400 |
| C04/P0 | 默认密码/7字符/73字节/25个汉字作为新密码 | 均400，直接查询账号status/version/hash保持完全相同 |
| C05/P1 | 24个汉字恰72字节改密；再用带首尾空格的新密码；尝试trim后密码 | 改密200且旧会话401；原样密码有效；trim后401 |
| C06/P0 | 正常普通成员GET config与签WS ticket | 均403 |
| C07/P0 | 重复active、重复disabled；禁用账号重置 | 同状态200且version/hash不变；重置后仍disabled且version恰加1；旧Cookie401 |
| C08/P0 | 已成功operation_id换target；同target再重试 | 换target409且原目标DB不变；重试同event_id，DB中该operation_id只有一条事件 |
| C09/P0 | 旧空体ws-ticket；用ticket建WS再重放 | 签发200、握手101、重放401 |
| C10/P1 | 无Origin公开品牌POST；近似不存在路径POST | 公开读200；近似路径由框架拒绝405，无受保护数据 |

补充脚本轮次没有覆盖旧结果：第一轮在初始化前因启动失败中断；第二轮前41项通过，第42项把不存在路由过度指定成401，实得405，属测试期望错误；核实路由层拒绝后第三轮期望405，43项全通过。没有把405解释成认证旁路，也未改业务代码适配测试。

## 未通过项与失败证据

**F01/P1：新SQLite首次启动偶发失败，当前未定位。** 前置：专有目录、新config.db、空providers、禁用日志、回环监听、正确setup密钥，显式`-app-dir`。动作：一次启动固定候选二进制。期望：完成迁移并响应启动探针。实际：2026-09-11 05:35:54进程在监听前退出，末条错误为`failed to bootstrap server: initialize console authentication: EE identity migration failed`。直接查库没有`ee_identity_*`表，未发生部分身份DDL提交。

证据：`counterexamples.log`保存首次启动异常；`counterexamples/node/server.log`保留首轮及后续原库重启日志。原库无修补直接重启成功；新库第二/第三轮反例可启动；另5个全新SQLite库逐一启动均成功，见`startup-repeat.json`及`startup-repeat-*/node/server.log`。这只能说明暂未稳定复现，不能证明第一次错误无害。未停止其他进程、未重启数据库、未修改库结构去使其变绿。

迁移实现将原始错误归一成同一安全文案，日志没有数据库原因码，本次无法区分锁竞争或其他原因；“并发宿主写入触发SQLite busy”只是待验证假设，不写成已证根因。失败证据已交主agent，修复应形成新提交，再针对启动失败及SQLite/PG迁移回归。

## 原始证据与限制

专有证据根目录：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-auth-independent1-9u9sorv2`。关键文件：`t0.json`、`binary-vcs.txt`、`legacy-vcs.txt`、`build.log`、`results.json`、8份测试日志、`counterexamples*.py/.log`及逐项JSON、`startup-repeat.json`。临时日志可能含合成账号资料；不将配置文件/完整服务日志粘贴到外部系统。

SQLite主冒烟原始证据：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-smoke-su3wr63x`；PG双节点证据：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-smoke-87rb26h4`；升级证据：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-upgrade-rubeci32`。品牌脚本临时目录按原脚本自动清理，保留本次stdout及exit。

未执行：真实provider计费请求、生产TLS/反代、完整浏览器认证/日志页面（本期未实现）、生产部署；不在这些范围给出通过结论。UI未由本测试agent重新安装依赖，按交测授权使用已核对同源产物，独立代码评审另行构建；未执行Workbench生成工具，避免改动其他评审生成视图。默认AppDir遗漏检查由独立代码评审复现，本测试为避免读取真实用户默认目录未对该路径直接运行。

本报告仅为首轮精确候选独立测试事实。业务验收T2、Gate2和发布均未作出；新提交必须固定版本、重新构建并复测受影响项，不能覆盖本轮记录。
