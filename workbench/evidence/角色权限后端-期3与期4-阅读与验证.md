# 角色权限后端期3与期4：阅读与验证

## 本次结果

2026-09-17，按用户要求以4420d1b12为基线，将b38493b07中的三、四期一起搬到feat/user-permissions-phase1工作区。保留期1/2命名、注释风格、Queries和分组路由。没有推送、合并主线或部署。

全部817条路由登记中，205条managed接入角色检查，比期2新增74条。完整规则与固定源逐条语义一致（非managed统一标记交给原处理器）；排序摘要为`b5c8bb41457e87e0717855df78067515e578c4315c7a429e69105ec6679e596d`。这一摘要证明提取一致，不代替业务正确性评审。

## 只看框架

入口见[rbac/host阅读说明](../../ee/internal/rbac/host/README.md)。原主流程没有改成另一套：登录 → 接口权限 → 特殊请求检查 → 原handler → 响应字段处理。

- 日志：概要、正文、导出分别检查。
- Webhook：向外部地址发送事件；带日志正文要额外权限，地址凭据不能泄露。
- 插件：先检查插件本身及所影响功能的权限，再处理秘密配置的读取和更新。
- 设置：比较真正改了哪些字段，再检查相应功能权限。
- 通知：按角色筛选，长连接逐条消息重新读取权限。

根包只补发布通知的受众角色检查；身份模块把连接票据接到Notifications.View。上游仅新增可选调用点，数据库及角色HTTP实现没有修改。未注入角色组件的host保留期2固定初始化管理员限制。

## 已执行验证

| 命令/范围 | 结果 |
|---|---|
| `cd ee && GOWORK=off go vet ./cmd/... ./internal/...` | 通过 |
| `cd ee && GOWORK=off go test -race ./cmd/... ./internal/...` | 全部通过；存储单测使用SQLite |
| `cd ee && GOWORK=off go build -o tmp/bifrost-rbac-phase34 ./cmd/bifrost-http` | 通过 |
| `go test ./transports/bifrost-http/handlers` | 整包通过，包括可选回调、通知和WS |
| `python3 ee/scripts/rbac-smoke.py --binary ee/tmp/bifrost-rbac-phase34 --database sqlite` | HTTP/WS及可选注册组合通过 |
| 同一脚本 `--database postgres`，临时PG两节点 | HTTP/WS通过，跨节点授予和撤回生效 |

SQLite证据目录：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-5dabwd55`。
PG证据目录：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-a2gvwez0`。
PG仅使用本任务临时Unix socket实例，每轮创建独立数据库，结束后删除；未访问部署服务器。

两种数据库的输出均确认：Provider本地调用与隐藏字段写回、请求头替换/删除/清空、无效占位不写入、设置及插件拒绝早于修改、VK显值权限分离、Webhook错误不泄露地址、通知受众和权限撤回。新测试同时覆盖未知配置字段/插件、响应结构变化及逐条消息重新读取。

本次把旧测试中“通知权限尚不能签发票据”改为正式通知权限行为，保留票据单次消费和存储故障拒绝测试；保留期2Provider测试。没有导入历史authorization-v4.json等证据文件。完整冒烟的升级/回滚可选模式本次未执行，因为没有修改存储结构；前期迁移证据仍保留。

## 状态

技术验证通过后供用户阅读框架。尚未进行本合并候选的独立正式代码评审、收敛评审或用户验收，不宣称Gate2通过。机械检查见[预飞结果](角色权限后端-期3与期4-预飞.md)；功能测试结果由上表单独记录，机械预飞不重复运行全部长测试。
