# 角色权限后端期4：新增敏感权限验证

日期：2026-09-17。实现基于 `1d89df7d4`，本文件与新增权限代码同一提交；正式复核候选以之后的评审记录为准。

## 规则

用户已选择新增 `Security.ChangeCredentialDestination`，允许把已有凭据用于新地址或连接信任设置。默认仅主管理员拥有；其他角色可以显式授予。仍需所属模块管理权限，不开放密钥明文，也不回填角色权限。权限总数30，无数据库迁移。

覆盖厂商及单个Key的目标、厂商代理、全局代理认证、Webhook认证头、OTEL/Telemetry认证配置、MCP的TLS/OAuth/token exchange更新。判定在最终配置恢复完成之后、保存与连接验证之前。普通配置更新及完全替换认证信息的操作保留原管理权限规则。

## 已执行

- EE：`GOWORK=off go vet ./cmd/... ./internal/...`、`GOWORK=off go test -race ./cmd/... ./internal/...` 通过。
- 上游：`GOWORK=off go test github.com/maximhq/bifrost/transports/bifrost-http/handlers` 通过。
- 敏感权限单测与MCP回调时序测试通过；预飞全绿，见同目录敏感权限预飞记录。
- 实际二进制：`ee/tmp/bifrost-rbac-sensitive-permission`；SHA256 `c153fbd32c855460fe1cf2fbaafffb5320f1cdeace17320c491eef6f99a86b4e`。构建源为上述基线加本次生产代码，构建时工作树有改动。
- SQLite真实HTTP/WS：`python3 ee/scripts/rbac-smoke.py --binary ee/tmp/bifrost-rbac-sensitive-permission --database sqlite` 通过；含厂商和Webhook新权限的拒绝、授予、恢复与撤回，以及路由启动组合。
- PostgreSQL真实HTTP/WS：同命令 `--database postgres`，使用本会话专用Unix socket数据库，通过。
- SQLite证据：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-n8kl7pt9`。
- PostgreSQL证据：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-go2i77e2`。

## 测试修正和限制

初次测试发现权限数断言仍为29、MCP测试夹具缺少名称、Webhook GET测试误认为有endpoint外层；均修正测试夹具后重跑通过。没有把这些失败算作通过。测试全部使用本机隔离实例和合成凭据，没有操作线上配置或真实凭据。

独立代码评审和收敛复核尚未完成，本记录不代表最终验收通过。前端仍不在本期范围。
