# workbench外壳重设计 期2 内容无损审计

独立 agent 干净上下文执行(2026-09-02);对象:commit `2c97f22` 的规范重排(18 页 → 11 页),旧页取自 `git show 16ca696:`。粒度:一行规则(表格行/清单项/禁令句/状态机各算一条;导航行与模板路径引用不计)。

## Verdict

**基本无损,5 条轻微丢失**(无一条核心纪律丢失)——已全部当场补回(commit 见方案实施记录补充)。

## 统计

核对总数 **约 249 条**:迁移 ≈168 / 合并(语义无损)≈74 / 显式放弃(方案有声明,合规)2 / 丢失 5。

## 丢失清单(均已修复)

1. refactoring/03「**优先建立行为保护,再移动或替换结构**」(先护栏后动刀,重构核心顺序纪律)→ 已补入 差异/重构.md 行为保护节首条;
2. refactoring/02 复核项「**目标结构是否解决已经证明的问题**」→ 已补入 机制/方案与复核.md 复核清单(重构另查);
3. refactoring/01 固定起点含「**生成物状态**」→ 已补入 机制/实施纪律.md 固定起点;
4. testing/01 高风险判据「跨系统**或第三方**链路」→ 已补回 风险分级.md 测试组合表;
5. testing/02 故障记录含「**已尝试的安全检查**」→ 已补回 差异/测试.md 证据与历史节。

## 显式放弃(合规)

- CHG-###/DEV-### 等编号制与 `change:` 字段 → 流程.md 明写替代口径(case 名即主键 + triggered_by);
- 规范/本仓操作规范.md 占位页删除 → 方案 §2 显式裁决;审计指出其与铁律1 字面张力,已随修复在 AGENTS.md 铁律1 补注约束对象(过程文档,不含规范/工具页)。

## 逐页去向摘要

change 套 4 页(68 条)全部迁移(收敛评审/finding台账近逐字);development 套 5 页(88 条)全部迁移/合并;refactoring 套 5 页(81 条)3 丢失已修,其余迁移/合并("碰巧同形"逐字、终扫 7 项逐条);testing 套 3 页(50 条)2 丢失已修,其余迁移/合并(职责表含"不得做的事"、用例五态、状态图逐字)。旧树 testing 03/04 两页系 vendor 时即缺失(非本次引入),新 差异/测试.md 已声明并并入要点。新树无新增死链;无实质内部矛盾(L2 判据与旧重构标准级判据映射自洽,经核对)。
