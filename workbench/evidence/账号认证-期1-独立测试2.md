# 账号认证期1独立测试2

**本agent亲跑范围结论：通过；该候选整体放行：未通过。** 对固定候选`5b784f32ae66404c3a55a51d2ceae3b0bd007064`亲自独立构建及测试，第二轮规定修复影响范围没有实际失败：7项自动化命令退出0、20项独立PG黑盒断言通过、10/10个全新SQLite目录启动通过。该通过结论只适用于本轮亲跑的认证后端修复及回归范围。同轮代码评审实际发现本候选的branding迁移启动失败，详见下文交叉证据；Gate2仍不能放行。

首轮记录不覆盖、不改写。[独立测试1](账号认证-期1-独立测试1.md)的F01原始身份迁移错误原因仍不能追溯；本轮证明当前新库启动、可识别SQLite busy的有限事务重试和安全诊断成立，**不能倒推首轮一定是SQLite锁冲突**。PG旧COMMIT测试证据范围的纠正见下文。

## 职责、版本与T0

执行者：与实施隔离的独立测试agent。只读业务源码；专有临时脚本仅用于故障注入和取证；仅新增本报告，无业务修改、提交或生成views。读取批准方案v2/checklist、交测包、[首轮修复验证](账号认证-期1-首轮修复验证.md)和修复diff后执行。

- 开始：`2026-09-11T06:02:15.160043+08:00`；结束：`2026-09-11T06:08:35.242019+08:00`，均为Asia/Shanghai。
- 分支`feat/account-authentication`；完整候选`5b784f32ae66404c3a55a51d2ceae3b0bd007064`；原方案基线`3e15a640d89902772445d3fb4c04023e58828eba`；首轮候选`eb93291fcae2248054309e09a69e01aac84ed6f0`。
- 亲核起始`git status --porcelain`为空、HEAD等于候选。结束再次核对HEAD与二进制哈希未变。预飞5交测条件全绿，但未用开发产物或自测结论代替独立执行。
- 独立命令：`cd ee && GOWORK=off go build -o /var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-auth-independent2-d8gqmtkc/bifrost-http ./cmd/bifrost-http`，退出0。
- 独立产物SHA256：`88c3fad0879cd0a41f43f04637542743eb4d37622a6d9f63907933433e0a32ed`。
- `go version -m`：Go1.27.1；`vcs.revision=5b784f32ae66404c3a55a51d2ceae3b0bd007064`、`vcs.time=2026-09-10T22:00:50Z`、`vcs.modified=false`。
- UI无源改动，按交测范围复用预飞5产物。构建前对embed和`ui/out`逐文件SHA256比对，除`.keep`外543个文件完全一致。lockfile SHA256=`511957b09ca6a2b76cf0bfafc8280d1d593f36347f98aa0817050bf63a678a8f`，完整清单在`t0.json`。
- 将提交内3个smoke脚本复制至专有目录，结束亲核与固定commit中的文件字节相同，哈希在`end-identity.json`；后续运行未依赖其他agent可变的未提交脚本。

**T0：通过。** 固定源码可获取且干净、独立产物身份明确，运行环境和数据隔离实际验证完成。

## 环境与清理

macOS arm64、Go1.27.1；专用PG实际执行`SHOW server_version`返回`17.11 (Homebrew)`。从`/tmp/bifrost-auth-test-pg.json`读取DSN仅供测试进程使用，未输出DSN/凭据。EE及Go命令以`GOWORK=off`从ee模块使用当前仓库本地依赖。

SQLite均为新建临时config.db；PG成功smoke用独立database，Go夹具用独立schema，成功后只清理自己创建的命名空间。所有服务器只监听回环、空providers；未发计费provider请求，未触碰真实业务数据或模拟器。实际运行由HTTP探针、跨节点请求、最终数据库查询和进程/端口状态确认。

R2默认目录测试通过已有脚本将**仅测试子进程**的HOME/APPDATA及cwd指向自建临时目录，并省略`-app-dir`；未改当前会话/操作者环境或读取真实默认配置。默认目录真实读取证据及DB最终状态另存`default-directory-state.json`。

R4故意失败测试保留自己创建的库`identity_smoke_06e26c5e98e94ea7b24e6bd04f20203c`与双节点日志，已通知主agent。亲核两个进程退出、监听端口关闭、库仍存在，未停止/修改主agent持有的PG集群。主agent明确要求保留该库和集群data目录作为现场，故未删除这份证据库。

基线二进制只读复用`/tmp/bifrost-auth-baseline.json`指定产物，亲算SHA256=`35c042223f2bf0bec47fc9a3d8d16cfb5a131daf18bab39137b0f258a5f4ad21`与交测一致；基线git archive产物没有VCS字段，未宣称独立重建了基线。

## 执行命令及结果

`$BIN`为本报告独立产物；`$LEGACY`为上述已核对基线。Python脚本从本次固定副本执行。Go命令均`GOWORK=off`且`-count=1`，未继承测试缓存。

| 编号 | 命令/条件 | 期望 | 实际 | 结论 |
|---|---|---|---|---|
| T01 | `identity-smoke.py --binary $BIN --database sqlite` | 真实HTTP、诊断、WS、恢复/重启断言全通过 | 退出0，13.37秒 | 通过 |
| T02 | 同上`--database postgres`，专用PG两节点 | 跨节点授权/撤销/幂等/WS一致 | 退出0，16.48秒 | 通过 |
| T03 | `identity-upgrade-smoke.py --binary $BIN --legacy-binary $LEGACY` | 默认目录三场景、旧版迁移、恢复一致备份后基线登录 | 退出0，91.05秒；本轮首次运行通过 | 通过 |
| T04 | `go test -race -count=1 ./...`，PG身份夹具 | EE所有已有测试通过 | 退出0，95.64秒；6个有测试包通过、3包无测试文件 | 通过 |
| T05 | `go test -race -count=1 github.com/maximhq/bifrost/transports/bifrost-http/handlers github.com/maximhq/bifrost/transports/bifrost-http/server` | 宿主认证/日志/WS回归 | 退出0，79.36秒，两个包通过 | 通过 |
| T06 | `go test -race -count=1 -v ./internal/identity/persistence -run 'TestSafeDiagnostics\|TestMigrationRetriesSQLiteBusyAtomically\|TestMigrationBusyExhaustionPreservesFailure\|TestDeferredCommitFailure'`，去除PG变量 | SQLite诊断/重试/耗尽/COMMIT断言通过 | 退出0，20.91秒，4项通过 | 通过 |
| T07 | 同包`-run 'TestSafeDiagnostics\|TestDeferredCommitFailure'`，PG变量 | PG安全诊断及真正提交阶段失败 | 退出0，15.56秒，2项通过 | 通过 |

完整命令/退出码/耗时在`results.json`及同名日志。T04/T06/T07部分测试有意重叠：全量保证回归，定向详细输出固定故障注入证据，未将重叠累加为独立用例数量。

## 修复项逐项复测

| 编号/优先级 | 前置、单步和期望 | 实际证据与结论 |
|---|---|---|
| R1-A/P0 | SQLite真实HTTP：新账号/会话；给密码事件表加失败触发器；请求重置；查响应、日志和原会话 | 返回503；`X-Request-ID`与`operation=identity.reset-password stage=password_event.insert incident_id=... kind=sqlite code=19/...`关联；driver原文不在应用日志；原会话有效。T01通过 |
| R1-B/P0 | SQLite/PG Go：事件INSERT注入错误，随后关闭自己的DB连接并认证 | 错误按阶段记录、无SQL/密码/hash/token/driver原文；断库为`stage=session.read code=closed`，不放行。T06/T07通过 |
| R1-C/P0 | 真PG黑盒：两个实例共独立库；数据库AFTER事件触发器写入延迟外键表；重置成员；读取密码版本/事件/会话；删除触发器再重试同operation_id | 503且`stage=transaction.commit kind=postgres code=23503`；密码/hash/version不变，事件及子表行均回滚，原成员会话跨节点200；移除触发器后相同operation_id成功且旧会话401。独立20断言的一部分，通过 |
| R1-D/P0 | 真PG黑盒：BEFORE事件触发器抛含唯一秘密哨兵的异常；chief本人改密；查响应与应用日志 | 503且`stage=password_event.insert kind=postgres code=P0001`关联响应ID；无哨兵、原/新密码、Cookie及注入SQL原文；移除触发器后原chief会话仍能查询。通过 |
| R2-A/P0 | 默认目录残缺凭据，不传app-dir启动 | 监听前失败，日志明确残缺旧凭据；最终默认库initialized=0、account_count=0。T03与最终DB核对通过 |
| R2-B/P0 | 默认目录完整旧凭据，cwd放无关残缺config；启动、登录、本人改密、重启 | 只读取默认目录，导入一个账号；新密码成功，后改文件密码不能覆盖；最终initialized=1/account_count=1。通过 |
| R2-C/P0 | 默认目录无文件，cwd放无关config；启动、setup初始化、改密、改文件后重启 | 可初始化；无关cwd不阻断；文件不回写EE密码；最终一个账号。通过 |
| R3/P0 | SQLite和PG签有效票据；坏gzip GET同ticket；OPTIONS同ticket；旧token查询；再正常WS握手和通知/撤销 | 400、200、401；同张票据随后正常101，说明外层早返不消费/破坏票据；身份撤销后WS关闭；两节点应用日志均无已签票据/旧token/Cookie/setup。T01/T02及T05通过 |
| R4/P1 | 真PG smoke两个节点启动并完成独立断言后故意抛自定义异常；让main自身finally运行；查库、进程、端口及日志 | 异常传播、stdout/stderr给出现场及准确库名；pg_database中该库仍在；两个自有进程退出、端口均关闭，日志均存在。20项独立断言全通过 |
| F01-A/P1 | 10次各新建SQLite目录和库；逐次启动后POST status并读迁移表；停止当前节点再下一次 | 10/10成功；每次initialized=false且`ee_identity_v1`记录恰1条。`fresh-starts.json`通过 |
| F01-B/P1 | 可控驱动busy错误注入：首次state创建失败；再次迁移；再调用迁移 | 首次整事务回滚，第二次成功；总2次，已完成迁移再次调用不重复。T06通过 |
| F01-C/P1 | 持续busy驱动错误注入 | 恰3次停止，无部分身份表；保留错误与安全`kind=sqlite code=5/517`诊断。T06通过 |
| C1/P1 | 实际签发WS ticket并读expires_in；单项票据/失效回归 | 响应30秒，服务层和HTTP来自共享WSTicketTTL；T01/T02/T04通过。未额外墙钟等待30秒 |

R1-C使用独立数据库触发器，没有复用开发Go回调夹具。触发器中的`DEFERRABLE INITIALLY DEFERRED`约束只在COMMIT检查；诊断明确transaction.commit、失败后最终状态以及移除注入后同请求成功三者共同证明发生的是实际提交失败。独立脚本`pg-independent.py`未修改工作树业务源码。

R3同时由宿主测试覆盖日志目标处理不改实际查询参数；真实HTTP早返后能握手的结果证明日志脱敏没有破坏有效票据业务。这里的“无泄漏”只针对被测试的应用访问/诊断日志，不延伸为任意代理或数据库运维日志承诺。

## 旧PG COMMIT证据纠偏

首轮执行时`TestDeferredCommitFailure`虽退出0，但后来新增阶段断言揭示PG旧夹具在事件INSERT阶段因继承GORM绑定参数而失败，**旧503/回滚断言不足以证明到达COMMIT**。因此首轮/预飞旧结果中若表述“PG真实COMMIT已验证”，应收窄为“PG事务错误返回503及回滚已验证，失败阶段未证明”。原报告保留，纠偏在本报告及[首轮修复验证](账号认证-期1-首轮修复验证.md)追加。

本轮亲核新夹具使用同一事务中的新GORM Statement执行延迟FK INSERT，并显式要求注入成功、`stage=transaction.commit`、密码/version/会话/事件完整回滚；T07亲跑通过。再通过R1-C完全独立的真实PG HTTP触发器验证上述提交失败语义。这是本候选的新证据，不倒签首轮结论。

## 主路径与相邻回归

T01/T02重新覆盖初始化竞态、错误setup、默认密码受限会话、创建重复冲突、本人改密、成员边界、跨节点Cookie撤销、重置幂等及不撤销重置后会话、停启不复活旧会话、事件本人过滤、旧Basic/Bearer/Cookie拒绝、config普通设置回送及拒绝认证修改时不部分写入、WS通知/撤销、离线恢复、重启、限流与日志凭据检查。

T03重新覆盖旧开关开/关、特殊用户名及短密码、文件73字节密码失败回退DB、改文件不覆盖新密码、缺DB/残缺配置/无setup密钥拒绝路径，以及停所有测试实例→保存EE事件→恢复完整合成SQLite备份→清旧sessions→基线二进制登录验证。没有新旧节点混跑共库，未触碰生产备份。

首轮43个独立边界断言未机械整套重跑；其结果仍只属于首轮。当前通过依据是本轮主路径、受影响HTTP/诊断/迁移/宿主回归及独立PG新增反例。未将旧43项重复计入本轮数量。

## 证据位置与剩余边界

专有根目录：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-auth-independent2-d8gqmtkc`。包含`t0.json`、`binary-vcs.txt`、`build.log`、`end-identity.json`、固定脚本副本、`results.json`和7份测试日志、`pg-independent.py/.json/.log`、`pg-failure-output.log`、`default-directory-state.json`、`fresh-starts.json`与10份新库/节点日志。

- SQLite HTTP：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-smoke-44l2radl`。
- PG双节点HTTP：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-smoke-alrurqnf`。
- 默认目录/升级恢复：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-upgrade-ju4dm8zn`。
- 独立PG故意失败现场：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-smoke-1erv6cj9`，保留库名见“环境与清理”；真实COMMIT关联ID为`0dbe7b60-105f-4af4-b96a-b066a37f2779`。

本轮无非预期实际失败，不存在靠重跑覆盖本轮红灯；R4主动异常是被测路径，按预期保留。首轮F01和开发预飞4/旧基线branding失败历史仍保留，不能因为当前新候选绿灯而声称旧失败根因已确定。受控busy为驱动错误注入、真实新库启动为实际运行；两者不能等同于完整复现首轮OS锁竞争。

未执行：生产TLS/反代、真实provider付费调用、认证及日志浏览器页面、完整RBAC、发布；这些不在本轮后端修复验收范围。不重新安装npm依赖、不重新构建无变化UI、不运行Workbench生成工具；UI同源核对和Go独立构建已完成。品牌独立smoke本轮未重复，EE品牌Go回归与升级含品牌迁移已覆盖；首轮完整品牌smoke记录仍保留为首轮证据。

本轮通过可供后续独立评审与用户Gate2决策使用，不自动表示整个认证产品已交付。

## 同轮交叉失败证据（不改写本agent实跑结果）

本报告完成时，主agent转交代码评审agent的本轮实际失败：同一固定候选`5b784f32ae66404c3a55a51d2ceae3b0bd007064`运行默认目录升级，首跑在`default-valid`新候选启动时出现`ee: migrate branding: database is locked`，此前`incomplete`已正确拒启。现场为`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-upgrade-x1v7aw9h`。此项由代码评审agent亲跑，本agent仅记录来源，不冒充自己复现。

这是**当前候选**的branding启动失败，不能仅称旧基线故障；branding迁移源码本轮尚未改变，不意味着现有候选没有该风险。它不同于首轮F01被隐藏的identity迁移错误，仍不能倒推F01根因。当前候选整体不能据本agent的局部绿灯放行Gate2。主agent已决定对branding可识别busy/locked增加有限整事务重试、形成新提交；后续须固定新版本并复测对应迁移/启动范围。本轮7命令、20黑盒断言、10新库启动实际通过结果保持原样，不覆盖同行失败。
