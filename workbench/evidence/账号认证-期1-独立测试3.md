# 账号认证期1独立测试3

**综合结论：通过（认证后端本案规定测试范围）。** 固定候选`a73109dc34e2b9a9292419511f9cfa58c4f508af`的R5差量独立复测通过：品牌SQLite/PG race、短暂busy/重试耗尽/取消/非busy原子性、默认目录三场景、10个全新SQLite组合启动、一次完整SQLite identity smoke均通过。本轮无非预期失败或用重跑覆盖红灯。

本结论结合[独立测试2](账号认证-期1-独立测试2.md)已经亲跑通过的未变动认证/HTTP/宿主范围；本轮亲核这些源码无差异，重新验证改变的品牌迁移与组合启动后，第二轮因R5保留的当前候选启动阻塞已完成修复复测。测试通过供Gate2决策使用，不自动表示业务T2验收、页面交付或发布授权。

## 对象、产物和T0

- 基线：`3e15a640d89902772445d3fb4c04023e58828eba`；第二轮候选：`5b784f32ae66404c3a55a51d2ceae3b0bd007064`；第三轮完整候选：`a73109dc34e2b9a9292419511f9cfa58c4f508af`。
- 分支`feat/account-authentication`；亲核起始`git status --porcelain`为空且HEAD正确。执行期间源码冻结，结束HEAD及产物哈希不变。
- 起始时间`2026-09-11T06:13:33.959266+08:00`；结束`2026-09-11T06:18:03.235454+08:00`，Asia/Shanghai。独立测试agent未改业务代码、未提交、未生成views，仅新增本报告并保留专有临时证据。
- 独立构建：`cd ee && GOWORK=off go build -o /var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-auth-independent3-q7pvn9te/bifrost-http ./cmd/bifrost-http`，退出0。
- SHA256：`a154a7bae96e150502bc3bf3f123b1eaf2b240c4c23c547ed8f550f55602fb95`。
- VCS：`vcs.revision=a73109dc34e2b9a9292419511f9cfa58c4f508af`，`vcs.time=2026-09-10T22:12:48Z`，`vcs.modified=false`；Go1.27.1 darwin/arm64。没有复用前两轮二进制。
- UI无源码变更；按交测范围复用预飞6的构建产物。embed除`.keep`外543文件与`ui/out`逐SHA256一致，清单在`t0.json`；未重新安装npm依赖。

**T0通过。** 已读修复diff、迁移测试与交测边界；固定对象干净、产物身份明确，专用隔离数据库和实际运行环境就绪。

## 差量边界

亲核第二轮至本候选差异：运行时代码仅`ee/internal/branding/persistence/migration.go`增加最多3次typed SQLite busy/locked整事务重试；另有对应测试、README和过程文档。`ee/internal/identity`、`ee/internal/bifrost`、`ee/internal/app`、`ee/cmd`、`ee/scripts`、`transports`、`ui`均无差异，证据`scope-check.json`。

因此本轮不机械重跑前两轮43/20个独立反例，不重跑未变动的旧基线降级演练，也未把它们计入本轮统计。第二轮真实PG COMMIT回滚、安全诊断、WS外层早返无泄漏、PG失败现场保留、默认目录隔离、完整升级/恢复和EE/宿主race结果作为对应未变动代码的已有证据；新增迁移对启动顺序/回滚的影响由本轮组合启动及SQLite HTTP回归覆盖。

旧PG COMMIT夹具证据纠偏仍遵守独立测试2：仅第三方503/回滚不能证明到达COMMIT；第二轮已通过新夹具阶段断言和独立真实PG延迟FK触发器补证，不倒签首轮。

## 实际环境和资源隔离

macOS arm64、Go1.27.1，专用PG实际`SHOW server_version`为`17.11 (Homebrew)`。从`/tmp/bifrost-auth-test-pg.json`读取DSN只供测试进程使用，不输出DSN/凭据。品牌PG测试通过`BRANDING_TEST_POSTGRES_DSN`显式启用；每个夹具仅建立和清理自己的schema；SQLite为新临时库。

真实EE只监听回环、空providers，无付费provider调用；未触碰用户数据、模拟器或其他会话进程。默认目录测试仅在已有脚本的测试子进程内隔离HOME/APPDATA/cwd，省略app-dir，日志确认读取自建默认目录；未改变操作者环境或读取真实默认配置。

本轮全部自有服务器进程已由夹具停止。主agentPG集群未停止。第二轮故意失败库`identity_smoke_06e26c5e98e94ea7b24e6bd04f20203c`仍保留，完成时实际查询确认存在；没有删除原失败库或日志。

## 命令及用例结果

以下Go命令从ee执行，显式`GOWORK=off`、`-count=1`。`$BIN`为本轮独立二进制。

| 编号/优先级 | 执行与前置 | 期望 | 实际/证据 | 结论 |
|---|---|---|---|---|
| R5-01/P0 | 无PG变量，`go test -race -count=1 -v ./internal/branding/persistence` | SQLite品牌持久化和迁移组合通过 | 退出0，12.50秒；顶层8通过、1个PG专用测试不适用 | 通过 |
| R5-02/P0 | 专用品牌PG变量，同上 | PG品牌迁移、事务和锁回归通过 | 退出0，12.21秒；顶层6通过、3个SQLite专用测试不适用 | 通过 |
| R5-03/P0 | callback在首次品牌单例INSERT返回typed busy | 整事务回滚后第2次成功，再迁移幂等 | 恰2次；品牌单例和迁移版本各1条 | 通过 |
| R5-04/P0 | callback持续busy | 恰3次停止并保留错误，无部分表 | `busy-3`断言通过，返回typed错误且无branding表 | 通过 |
| R5-05/P0 | 实际SQLite另一连接`BEGIN IMMEDIATE`持锁；执行迁移；释放锁再执行 | 持锁失败且不留DDL；释放后成功 | 实际日志3次database is locked，typed判定通过；释放后版本恰1条 | 通过 |
| R5-06/P0 | 迁移前已取消；或首个busy回调同时取消 | 立即结束/不继续重试，无部分表 | cancel-retry恰1次；取消前置/重试期间两路径通过 | 通过 |
| R5-07/P0 | 分别在品牌行与版本行INSERT注入非busy错误 | 非busy只执行1次；DDL共同回滚；去掉注入后可重试 | SQLite/PG都通过，恢复后版本恰1条 | 通过 |
| R5-08/P0 | PG真实事务持advisory锁，另一迁移200ms超时，释放后重试 | 不部分提交；锁释放后迁移成功 | PG测试通过，超时为预期注入结果 | 通过 |
| R5-09/P0 | 固定副本`default_directory_scenarios($BIN, <本轮新目录>)` | 默认文件残缺拒启、完整凭据导入、无文件可初始化；cwd无关文件不干扰 | 三场景首次运行通过，26.03秒；真实日志/最终库核对见下文 | 通过 |
| R5-10/P0 | 10次独立新SQLite目录；逐次启动→POST status→查版本/单例→停止 | 无初始化账号；两迁移各1条，品牌单例1条 | 10/10通过；每次initialized=false、`ee_branding_v1=1`、`ee_identity_v1=1`、branding单例=1 | 通过 |
| R5-11/P0 | `python3 identity-smoke.py --binary $BIN --database sqlite` | 品牌/identity装配后的真实账号HTTP主路径与恢复/重启不回归 | 退出0，16.06秒，所有脚本断言通过 | 通过 |

R5-03–08是R5-01/02内的明确断言，未重复计数成额外自动化命令。标记为专用库不适用的测试没有算通过；另一数据库的对应执行提供覆盖。短暂busy/耗尽是typed驱动错误注入；R5-05和08是实际数据库锁，二者证据类型分开记录。

真实默认目录结果：残缺凭据场景在监听前退出，默认库initialized=0、账号0，identity迁移已执行而branding迁移未执行，符合初始化校验先失败的顺序；完整/无文件场景最终initialized=1、账号1、两迁移均恰1条。后两场景均成功本人改密再重启，文件内后来修改的密码不能覆盖EE密码，cwd无关残缺配置未阻断默认目录读取。没有把残缺场景的预期退出计作非预期失败。

R5-11覆盖初始化竞争、普通成员/强制改密、配置同值回送和拒绝修改时原子性、重置幂等、停启撤销、WS有效票据经坏gzip/OPTIONS后仍可使用与日志无泄漏、WS撤销、离线恢复及重启、限流；这是本轮组合验证，不是声称重新执行了全部第二轮PG反例。

## 失败历史与结论限制

- 第二轮代码评审在旧候选`5b784f32...`的`default-valid`发现`ee: migrate branding: database is locked`，真实现场仍保留：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-upgrade-x1v7aw9h`。本轮对此R5的新实现进行定向故障注入与实际新库/默认目录复测，全部通过，历史失败不改写为通过。
- 旧基线`3e15a640...`也实际发生过branding锁失败，其二进制没有本轮修复；旧失败现场及第二轮完整恢复演练成功记录保留。本轮没有重跑旧基线，不能宣称旧二进制的启动风险被修复。
- 首轮F01为identity迁移失败且原错误被隐藏，原始原因仍不可追溯；branding锁故障、当前typed busy重试通过，均不能证明F01一定同源。
- 三次有限重试只处理可识别SQLite busy/locked，持续占锁/非busy错误仍明确失败，不保证任何数据库故障下都能启动。取消和原子性已验证，未以无限重试掩盖问题。
- 未测试生产反代/TLS、付费provider、完整RBAC或未开发的认证/日志页面。测试通过仍只表示认证后端在本案规定范围可进入用户验收，不代表页面或完整认证产品完成。

## 原始证据

本轮独立根目录：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-auth-independent3-q7pvn9te`。

关键文件：`t0.json`、`binary-vcs.txt`、`build.log`、`scope-check.json`、`results.json`、`test-counts.json`、`branding-sqlite-race.log`、`branding-pg-race.log`、`sqlite-http.log`、`default-directory.json`、`default-state.json`、`fresh-starts.json`、`end-identity.json`；默认目录和10轮真实库/日志分别在`default-directory/`与`fresh-0/`至`fresh-9/`。

SQLite identity smoke独立现场：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-identity-smoke-ni1avpmo`。所有自动化输出、故障注入预期错误、库最终状态均保留；未覆盖前两轮原始证据。
