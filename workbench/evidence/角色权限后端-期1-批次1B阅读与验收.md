# 角色权限后端：批次1B阅读与验收

## 当前范围

- 比较基线：`d811dfd07`，已由用户确认的1A收口提交。
- 原始提取来源：`b38493b07`，保持原完整分支不动。
- 当前交付分支：`feat/user-permissions-phase1`；1B为独立候选，尚待用户阅读确认。
- 业务增量：825行新增、91行删除；集成测试新增849行。1A六个业务文件逐字不变；修改README介绍新存储文件。

## 阅读顺序

以下路径以`ee/internal/`为起点，先看表，再看数据库操作，最后看身份协作。

| 顺序 | 文件 | 看什么 |
|---|---|---|
| 1 | [rbac/persistence/rows.go](../../ee/internal/rbac/persistence/rows.go) | 角色、角色权限、账号角色分别存在哪张表 |
| 2 | [rbac/persistence/store.go](../../ee/internal/rbac/persistence/store.go) | Store/BoundStore如何实现Repository、Queries和Tx |
| 3 | [rbac/persistence/roles.go](../../ee/internal/rbac/persistence/roles.go) | 角色及分配关系的具体查询、插入、替换和删除 |
| 4 | [rbac/persistence/migration.go](../../ee/internal/rbac/persistence/migration.go) | 建表和三个预置角色如何只初始化一次 |
| 5 | [rbac/identity/policy.go](../../ee/internal/rbac/identity/policy.go) | 账号操作如何调用角色权限规则 |
| 6 | [identity/access.go](../../ee/internal/identity/access.go) | 身份模块提供哪些动作、查询及生命周期回调 |
| 7 | [identity/persistence/scope.go](../../ee/internal/identity/persistence/scope.go)、[store.go](../../ee/internal/identity/persistence/store.go) | 身份和角色如何共享读取快照、事务及写锁 |
| 8 | identity/accounts.go、passwords.go、session.go、core.go、model.go的增量 | 在既有账号流程的哪个位置增加检查和同事务绑定 |

已存在的identity文件只需对`d811dfd07`看差异；不重复阅读1A。测试按用户偏好单列，不作为讲解入口。

## 与原完整实现的必要差别

- rbac.ReadView适配为1A已经确认的rbac.Queries；新引入的身份查询接口也统一命名identity.Queries。
- 裁剪OpenConsoleStream动作及Notifications.View的票据授权映射；IssueTicket/ConsumeTicket继续要求固定主管理员。保留通知权限目录项，并验证它不能提前开放票据。
- 测试中的通知票据放行断言移出本批；保留主管理员票据可用及重复消费拒绝。权限表故障对通知票据的影响留期4，其余账号/角色故障回滚仍覆盖。
- 不提取rbac/http、rbac/host、app接线和上游修改。生产应用仍使用默认身份策略，也不调用角色迁移。
- README按用户指定规范集中在模块根包，既有身份存储README补充自身事务约束。

## 验证

检查清单：[checklist.yaml](../cases/角色权限后端/期1/checklist.yaml)。自动结果见[1B预飞](角色权限后端-期1-批次1B预飞.md)。本批重新执行存储测试，不沿用原整包通过结论；包含并发最后管理员保护、删除与分配竞争、生命周期失败回滚、迁移失败重试和哈希后撤权复查。

PostgreSQL测试使用本机专门启动的临时实例，只监听私有Unix socket，测试各自新建schema。初次默认SQL_ASCII库触发pgx编码检查，改用UTF8测试库后重跑；属于测试环境准备问题，未修改业务逻辑规避失败。测试结果在收口时记录，实例结束后停止，数据及日志保留临时目录便于追溯。

## 本批边界

这里只准备可独立阅读和测试的数据库/身份协作代码，未经过用户1B确认或独立正式评审。不宣称HTTP角色功能已交付；未运行真实宿主管理/WS端到端授权。用户确认1B后再提取1C。

## 已执行的数据库验证

- macOS arm64，Go 1.27.1，PostgreSQL 17.11。
- SQLite：正式EE源码目录race测试通过；RBAC存储14个顶层测试通过。
- PostgreSQL：`GOWORK=off go test -race -count=1 ./internal/rbac/... ./internal/identity/persistence`通过，14个RBAC存储测试及身份存储套件均执行；每个测试使用独立schema。
- 临时实例目录：`/tmp/codex-rbac-1b-pg-9jb6lbw3`；UTF8库`rbac_1b_test`，仅私有Unix socket，未连接既有数据库。
- 本批检查清单另包含格式、1A保持不变、范围、EE vet/构建和Workbench视图验证。

最终预飞全部通过。临时PostgreSQL实例已正常停止，测试数据和server.log保留在上述目录；未停止或修改机器上其他数据库实例。
