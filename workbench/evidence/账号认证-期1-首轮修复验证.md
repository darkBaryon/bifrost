# 账号认证期1首轮修复验证

日期：2026-09-11。开发者定向复测；不替代第二轮独立结论。首轮候选 `eb93291fcae2248054309e09a69e01aac84ed6f0` 的代码评审 R1–R4、收敛 C1/C2 和独立测试 F01 均保留原记录。

## 已完成的定向检查

- `cd ee && GOWORK=off go vet ./...` 与 `go test -race ./internal/identity/... ./internal/bifrost/...` 通过（SQLite）。诊断故障注入覆盖事件写入、断库查询、真实延迟约束 COMMIT，日志只包含操作、阶段、关联 ID、错误类型或数据库码，不含 SQL/凭据/driver 原文。
- 新增 migration busy 注入：首个事务在创建 state 时失败，整笔回滚后第二次成功且再次迁移幂等；持续 busy 时恰好三次结束，身份表未部分落库且记录 SQLite 5/517。对应定向 `-race` 测试通过。
- 真实 SQLite HTTP 冒烟通过：事件触发器中止写入返回固定 503，关联响应 X-Request-ID、安全 SQLite 约束码可见，触发器错误文本不可见，原会话保持可用。有效 WS ticket 经坏 gzip 请求返回 400、OPTIONS 返回 200 后仍可正常握手；所有路径日志均无该票据值。现场 `bifrost-identity-smoke-xcpoh5j1`。
- 默认目录真实启动三场景通过（包含于升级脚本）：不传 app-dir，隔离 HOME/APPDATA/cwd；默认文件残缺拒启、完整凭据导入、无文件可初始化；无关 cwd 文件不影响，改密码后重启不被旧文件重置。未访问操作者真实默认目录。
- PG 失败清理真实反例复测通过：两个新建节点启动后注入 AssertionError；主流程回收进程、保留自己的数据库、打印日志目录和库名；查询 pg_database 确认库仍存在，不输出 DSN。人工验证完成后只显式删除该自建库。现场 `bifrost-identity-smoke-aav_l9ue/failure-preservation-check.json`。
- 宿主 requestLogTarget 定向 `-race` 通过，日志脱敏不改实际查询参数，握手仍可消费票据。

- 10 个全新 SQLite 数据目录连续启动及身份状态查询全部通过，现场 `identity-fresh-starts-jzctanm5`。每次停止自己的节点后再创建下一实例；没有重用前一次迁移成功的库。

- 首次升级演练失败后另建隔离夹具，完整默认目录、旧版升级和恢复基线验证通过，现场 `bifrost-identity-upgrade-e3t0ur2x`；原失败现场继续保留。

## 原失败与边界

升级脚本本轮首次执行在旧基线 `3e15a640` 的 `case-False-False/node` 首次启动失败，日志末尾为 `ee: migrate branding: database is locked`，尚未运行该场景的新候选。此前默认目录场景及旧开关开启场景已完成。失败现场 `bifrost-identity-upgrade-qr1nzn2t` 保留；不得把重跑通过写成首次通过，也不能将这一旧基线故障归因于新身份迁移。

首轮独立测试 F01 的身份迁移原始错误被旧实现隐藏，不能证明就是 SQLite 锁冲突。当前修复为补诊断并仅对可识别 busy/locked 整事务做有限重试，真实新库启动复测结果另列；旧失败记录仍保留。

上述现场均在 `/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/`。产物由修改后的工作树构建，正式第二轮应从新的完整提交独立构建并固定 SHA256/VCS 信息。

## 预飞4暴露的测试证据修正

预飞4的11项命令中10项通过，EE race失败；原工具只保留末行FAIL，单独完整复跑保存于 `/tmp/bifrost-auth-preflight4-ee-rerun.log`，定位为 `TestDeferredCommitFailure` 新增的提交阶段断言失败。SQLite原测试通过，但PG回调继承了事件Create的绑定参数，造成写入阶段的 `*fmt.wrapError`，此前仅根据503与回滚判断不能证明PG实际COMMIT失败。

修正测试夹具为同一事务内使用新GORM Statement执行延迟约束插入，并断言插入已经成功，再要求诊断明确为transaction.commit，最后验证密码/version/会话/事件均回滚。旧预飞及独立报告中“PG真实COMMIT失败已验证”的表述范围应以本次修正后的实际结果为准，原记录不覆盖修改。业务事务实现未因该夹具问题改动。

修正夹具后，PG单项 `go test -race ./internal/identity/persistence -run TestDeferredCommitFailure -v` 通过，SQLite的安全诊断/迁移/真实提交失败定向集合通过（8.865s）；后续预飞5的EE全部race和宿主回归已通过。

[预飞5](账号认证-期1-预飞5.md)最终全部通过，11条命令原样执行。完整输出现场 `bifrost-preflight5-output-5u99d9s6`，每条命令单独保存；没有覆盖预飞4失败表。
