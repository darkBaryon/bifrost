# 角色权限后端最终验收：补充验证

## 固定候选与产物

- 候选：`cbc94b03114e852ea2f6a5500f64f492eef1fa26`。
- 构建：`cd ee && GOWORK=off go build -o tmp/bifrost-rbac-acceptance ./cmd/bifrost-http`。
- `go version -m`确认vcs.revision为上述提交、vcs.modified=false。
- 候选二进制SHA256：`8593c018d87ac97980f514d8e0311178846d7535dcdb06e9f6d0797716a53bcd`。
- 历史认证二进制：`ee/tmp/bifrost-auth-baseline`，SHA256 `3231c1497ae127596d9b67da7a9d03d986fea85f8522c8c4239eaa8652b9e3da`。该旧产物未嵌入Git revision，本次以精确二进制摘要识别，不将文件名当作源提交证明。

## 升级、回退、再升级

使用`ee/scripts/rbac-smoke.py --binary ee/tmp/bifrost-rbac-acceptance --upgrade-from ee/tmp/bifrost-auth-baseline`：

| 数据库 | 结果 | 证据目录 |
|---|---|---|
| `--database sqlite` | 通过 | `/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-1rq6pftn` |
| `--database postgres`，两节点 | 通过 | `/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-d44awxdc` |

覆盖旧账号升级、仅一次迁移、修改过的预置角色重启保留、旧版本离线恢复、新版本恢复补回主管理员且保留其他角色和密码历史。每次切换先停止全部隔离节点。该结果不替代不同部署版本组合或生产数据的升级演练。

## 原大文件的去向

原候选`b38493b07`保留在`feat/user-permissions-backend`，没有删除历史：

| 原文件 | 原行数 | 当前交付 |
|---|---:|---|
| `ee/internal/rbac/host/routes.json` | 6,969 | 改为按共同权限规则分组的`routes.txt`，748行，仍明确登记817条方法/路径 |
| `ee/internal/rbac/host/testdata/authorization-v4.json` | 11,878 | 不复制到新分支；当前逐路由测试、启动路由核对和固定源摘要验证承担对应检查 |
| `workbench/evidence/角色权限后端-期1-授权映射v4.json` | 11,878 | 与上项相同内容的历史证据副本，留在原分支 |
| 其他完整路由盘点JSON/Markdown、旧版本授权映射 | 各异 | 留在原分支供追溯，不属于运行所需数据 |

行数主要来自每条路由重复展开的字段，以及owner、注册条件、来源文件行号等盘点信息。去掉这些副本不代表删除权限功能。固定源摘要只能证明提取一致；独立业务正确性仍由实际handler检查和测试验证。后续若评审修正规则，需要单独记录，不能为了匹配历史摘要保留错误。

## 最终结论

独立代码评审1结论为需修改，收敛评审1结论为有Blocking，当前不能据本页宣称整体验收通过。评审修复后的验证另行追加，不把旧候选结果改写为新候选结果。

## 首轮评审处理

| 来源 | 处理情况 |
|---|---|
| 代码 R1 | `/metrics` 使用保留协议内容的策略；仍检查 Logs.View，补单位测试、真实指标文本、撤权403及指标关闭404的冒烟。验证结果在下方追加。 |
| 代码 R2 | 待用户裁决；不实现新的权限政策、不接受风险、不关闭。确认的动态证据仅为隔离的合成额外认证头。 |
| 收敛 R1 | 移除最终装配不可达的固定恢复账号授权分支；未注入权限组件的独立身份测试仍由 identity 策略决定权限。 |
| 收敛 R2–R4 | 同步身份、宿主、角色 README、账号接口指南与后端架构；新增九个角色接口指南，说明预留项与不含账号删除的当前边界。 |
| 收敛 R5 | 两个旧认证字段由上游 config.go 按 EnforceAuthOnInference 派生，不采用它们的输入；表项取消虚假的独立权限声明，注释说明派生来源。白名单仍由 host/config 检查。 |
| 收敛 R6–R7 | 复用身份唯一键冲突、UUID格式及正文上限；Prepare复用 requirePermissions。 |
| 收敛 R8 | 评审已撤回，无修改。 |
| 收敛 R9–R12 | 删除未使用错误字段和不可达守卫，补导出用途注释，规范 import 分组。 |
| S1/S2/S3/S5 | 删除历史阶段脚本与重复405、重复通知受众格式校验；RootGuard统一返回全部Allow方法及no-store；DSN统一RBAC前缀并接入Makefile。 |
| S4 | 采纳“路由清单并非全部行为来源”的说明，已写入host README。保留类型化常量及解析处的合法值列表：Go常量不是可遍历枚举，另建切片仍要再次列举值，并未消除重复；当前没有第二套运行期权限表，不为减少一处声明增加注册框架。 |
| F1及旧台账 | 新增FIND-022并写入接口指南；FIND-017/021补充本案触发与现状。 |

所有评审原文保留，不把处理说明当作独立复核结论。R2未关闭，暂不生成“验收通过”记录。

## 例行修复后的验证（2026-09-17）

本轮产物 `ee/tmp/bifrost-rbac-review-fixes`，SHA256 `7842412f7258fef215f54e348b6d639b96fa94f68c0e12c4a743a6dcfb6a8828`，基于 `cbc94b031` 的工作树构建（`vcs.modified=true`），不冒充原干净候选。构建后仅调整注释、说明与证据，没有再改产品行为。

| 检查 | 结果 |
|---|---|
| `cd ee && GOWORK=off go vet ./cmd/... ./internal/...` | 通过 |
| `cd ee && GOWORK=off go test -race ./cmd/... ./internal/...` | 通过；未变包可命中缓存，受改动包重新执行 |
| `cd ee && GOWORK=off go test github.com/maximhq/bifrost/transports/bifrost-http/handlers` | 通过，全部handler包测试 |
| `go build -o tmp/bifrost-rbac-review-fixes ./cmd/bifrost-http`（ee、GOWORK=off） | 通过 |
| `rbac-smoke.py --binary ee/tmp/bifrost-rbac-review-fixes --database sqlite` | 通过；现场 `.../T/bifrost-rbac-smoke-gb12vxgv` |
| 同上 `--database postgres`，RBAC_TEST_POSTGRES_DSN指向自建PG | 通过；双节点，现场 `.../T/bifrost-rbac-smoke-6b4jtxp3` |
| 期4preflight | 全绿，见[评审修复预飞](角色权限后端-期4-评审修复预飞.md) |

两个现场前缀均为 `/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn`。HTTP冒烟包括角色/账号接口、通知HTTP和长连接筛选/撤权、拒绝时数据库与运行时不变、普通配置回写。新增指标验证包含真实200文本、Logs.View撤销后403，以及SQLite启动配置关闭指标后的404。自有节点和临时数据库均由脚本清理，自建PostgreSQL进程已停止。

这些是实施方回归结果，不替代修复后的独立正式复核；本轮没有重跑升级回退，旧干净候选的升级证据仍只适用于上方指定产物。

独立代码评审已完成[补充静态阅读与普通测试](角色权限后端-期4-代码评审补充.md)，12项转入逐项核对，无新增已确认阻塞。指标修正的定向测试由评审者亲跑通过；真实指标HTTP、PG及本轮全量race为上方实施方验证。此补充不是第二轮正式通过结论；用户裁决后仍需固定最终候选，完成代码及收敛差量复核。
