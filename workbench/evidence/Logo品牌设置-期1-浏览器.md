# Logo 品牌设置浏览器验证（2026-09-08）

对象：基线 8fd392958288016f5c97756cac7b5d5461699bca 上的本案未暂存工作树，ee/tmp/bifrost-branding 构建产物；Chrome，通过 Browser 技能控制。独立临时 app-dir 和临时管理员，非个人/生产配置。

已执行：

- `/login` 正常填写临时管理员并登录，再进入 Branding，页面展示两个图片槽位、限制提示、两种背景预览，初始保存/重置禁用。
- 通过后端 API 放入几何测试 PNG/JPEG，再刷新浏览器：所有预览和侧栏图片 naturalWidth >0，当前主题浅色。注意：这是显示验证，不是浏览器上传通过。
- 点击移除小图标：预览改为 Logo，保存启用；独立 GET 仍 has_icon=true，证明草稿未落库。点击保存成功，按钮禁用，小图标回退 Logo。
- 折叠侧栏，切换深色主题；实际可见图标宽22px且引用 Logo URL，双背景仍同一份图。截图：[深色折叠预览](Logo品牌设置-期1-深色折叠预览.png)。
- 恢复默认打开确认框，取消后原 Logo 仍在；再次确认后出现“已恢复默认品牌图片”，重置禁用，六张预览恢复正确的浅/深默认资源并正常加载。
- 在本案临时 DB 上添加专用测试触发器，使 UPDATE 失败；页面移除 Logo 后保存：出现操作失败与重新读取提示，草稿仍为移除状态，数据库 Logo 字节不变。删除本案触发器、点击重新读取、再保存成功，证明失败恢复。测试触发器已清理。

未执行/受阻：

- Chrome 文件选择器已触发，但 chooser.setFiles 返回 `Not allowed`。按技能提示需在 ChatGPT Chrome Extension 开启 Allow access to file URLs；未修改用户扩展权限或使用隐藏页面注入绕过。
- 因此浏览器端选图/损坏文件/取消文件选择未完成；图片 API 校验和真实保存/重启由 Go 与二进制测试覆盖，两者不是同一验证事实。
- 补充已执行：设置测试 Logo 后通过账户菜单退出登录，登录页出现自定义 Logo，图片已正常加载（naturalWidth >0），证明未认证页面可读素材。
- 移动端窄屏尚未专门验证。

首次直接访问管理页时，未认证状态呈现上游“无法连接到控制台”；改用正常 `/login` 路径登录后成功。本案不修改上游认证导航。
