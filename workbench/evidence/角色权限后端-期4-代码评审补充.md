# 角色权限后端：期4代码评审补充

日期：2026-09-17。评审者：独立代码评审会话。本文补充[代码评审1](../cases/角色权限后端/期4/代码评审1.md)，不覆盖首轮报告，不构成第二轮正式通过结论。

## 对象与结论

认证基线仍为 `f371548b5444693d7e259033bec9f4a467e3748b`；HEAD 为 `cbc94b03114e852ea2f6a5500f64f492eef1fa26`，加本轮授权的未提交修复。读取时 `git diff --binary -- ee transports | shasum -a 256` 为 `81060678f92dfe50312ee8b287304f449f5498662e2b468c318f60e0b8f57a3f`；该指纹仅覆盖这两个目录的已跟踪工作树差异，不包含新增接口指南及工作台文档。产品代码由实施方修改，评审者仅新增本文。

本次补读和普通测试**未发现新的可确认阻塞问题**。R1 的工作树修正与文本协议相符，定向回归已亲跑通过；R2 的权限取舍仍待用户裁决，未关闭。整体验收保持未通过，后续正式复核应绑定包含最终修正的新候选。

本轮按主审新指令恢复防御性静态审阅和普通功能测试。没有新增凭据提取、转发复现或攻击测试。首轮 R2 的证据范围仍仅为合成 `NetworkConfig.ExtraHeaders`，不扩展为真实厂商 Key、云凭据或代理路径已验证。

## 首轮问题及修复核查

| 项目 | 亲核结果 | 状态 |
|---|---|---|
| R1 指标文本被 JSON 投影拒绝 | `rbac/host/routes.txt:518` 改为 `protocol-scoped`；仍要求 `Logs.View`。`response.go` 的协议分支保留原成功输出。上游 `server.go:2362-2371` 仍在关闭指标时返回 404。`TestMetricsAuthorizationPreservesText` 核对完整正文、媒体类型和撤权后不进入 handler，本次通过。 | 工作树定向修正有效；本轮没有亲自重新启动完整二进制验证真实指标服务，不将实施方的 HTTP 记录冒充本审实测。 |
| R2 厂商管理与保留凭据的传输目的地 | 本轮静态补读完整 `handlers/providers.go`，原策略调用仍在保存前，首轮所述信任边界未改变。 | 待用户裁决；未修正、未挂账、未关闭。 |
| 首轮转收敛：WS 说明 | `identity/README.md`、账号认证接口指南已按 `Notifications.View` 更新，身份策略和票据调用方一致。 | 本次说明同步已核。 |
| 首轮转收敛：功能范围 | 新角色接口指南明确无账号删除接口，三个权限为 `available:false`，并明确目录存在不等于功能开放。 | 文档不再宣称这些能力已开放；最终是否接受该交付范围仍由主审对账既有授权，不以本文代替用户验收。 |

## 收敛评审转入的12项

首轮独立报告完成后，按主审明确指令读取[收敛评审1](../cases/角色权限后端/期4/收敛评审1.md)的转入项。以下结论均重新追踪代码和调用方，没有将收敛侧质疑直接当作缺陷。

| 转入项 | 代码与契约证据 | 处置 |
|---|---|---|
| 两个旧认证字段不独立判权 | `handlers/config.go:588-592` 实际把两个旧字段都赋为请求中的 `EnforceAuthOnInference`，不采用请求旧字段；后者由 `host/settings.go` 要求 `Governance.Manage`。白名单另由 `ee/host/config.go` 拒绝变更。 | 无已确认权限绕过；工作树空权限列表及注释与实际派生语义一致。 |
| 405 的 Allow 只含首个方法 | `host/routes.go:234-248` 现收集所有已注册的匹配方法再输出；不把静态兜底算进去。app 移除重复 `GuardMethods`。 | 原问题已修；`TestRootGuardListsAllAllowedMethods` 本次通过，覆盖 GET/POST 同路及角色 POST 接口。 |
| 通知 HTTP 快照与 WS 不同 | `Prepare` 每个 HTTP 请求取新 Snapshot；`CanView` 用该请求快照。WS `messageFilter` 每条推送重新 Snapshot，ping 经会话/权限回调。产品方案第134-135行要求“下一次操作”，没有要求撤回已经进入处理的 HTTP 响应。 | 符合当前契约；并不承诺在途 HTTP 撤销或实时终止已开始的响应。 |
| 权限目录反复构造 | 目录固定29项；循环规模有界。没有性能测量证明形成容量故障。 | 无新增正确性阻塞；优化建议仍归收敛，不宣称零开销。 |
| route match 分配 RequestCtx | 错误方法路径最多遍历固定7种方法，额外含初次匹配；没有无界递归或随请求内容增长的匹配次数。 | 同上，未做吞吐/分配基准，不能给出性能通过结论。 |
| Webhook 回调额外读权限 | `host/webhooks.go:55` 在已解析目标端点之后重验操作者与 IncludeResponse 权限；`handlers/webhooks.go:235/300/628` 均在保存/重投前调用，读失败返回安全错误。 | 有独立授权时点的理由；没有确认多读造成行为错误。 |
| 临时令牌仅在401/403后备 | `ee/host/auth.go:306` 保留非管理员/失效会话的精确流程后备，存储故障不后备。账号认证方案v2第79行要求DB故障503不放行，第186行允许非chief/失效Cookie后备。 | 与两项契约兼容；`TestPermissionStorageFailureDoesNotFallBackToTemporaryToken` 本次通过。 |
| ResetPassword unavailable不写失败事件 | `identity/passwords.go:122-125` 对存储/未知故障回滚。账号认证方案v2第111行把权限/状态/目标业务拒绝与DB故障区分，后者不能声称持久化成功；权限拒绝仍通过 InsertEvent 提交失败事件。 | 无已确认退化；`TestPermissionQueryFailureDoesNotDowngradeOrCommit` 断言密码与事件都未提交，本次通过。 |
| 启用账号不加额外策略检查 | `identity/accounts.go:115` 先在同事务重验 `ChangeAccountStatus`，随后才读目标并调用 BeforeStatusChange；启用不会减少启用主管理员数，仍升版本并撤销旧会话。 | 足够；不是无权启用入口。 |
| 插件名请求体/路径差异 | 上游 `CreatePluginRequest.Name` 决定 POST 身份；`UpdatePluginRequest` 无 Name 字段，PUT/DELETE 以路径 UserValue 为准。EE `checkPluginMutation` 与该来源一致。 | 即使PUT正文附带name也不改变上游操作对象；无已确认错配。 |
| consolePolicy取值时写503 | 全局搜索生产调用点，完整读取通知create/list、WS connect、Provider update、Settings/Proxy update、Webhook create/update/redeliver；9个调用点均在 `!ok` 立即 return。 | 未发现继续产生副作用的调用点；错误类型/typed-nil回归本次通过。 |
| runpy依赖夹具内部名称 | `rbac-smoke.py` 使用的 Node、psql、websocket、frame 当前由 identity 脚本提供；脚本差异为DSN统一及指标断言。 | 当前未确认功能损坏；共享夹具组织仍归收敛。本轮未重新执行此完整冒烟。 |

另核对旧固定管理员 fallback 删除后的装配：app 总是注入 ConsoleAccess；`Prepare` 对非 managed 返回拒绝；公开/身份/角色路由由 AuthAdapter 提前交回各自入口。`server.go:2285-2371,2694-2794` 显示管理中间件装入管理路由，OAuth issuance 独立注册，推理使用独立中间件链；固定恢复账号不再参与正常管理授权。这里的静态调用核查不等于逐个执行全部协议端点。

共用唯一键判定保留原 PostgreSQL 23505 和 SQLite unique/primary-key 分类；UUID规则和16KiB限制只是复用既有值。没有发现值改变或错误被降级。去掉通知重复格式检查前，上游 create 已验证并规范化 input，Publish 自身继续验证；生产回调没有新的未校验输入来源。

## 本轮实际阅读范围

在首轮已读范围上，本次完成此前缺口的全文阅读：

- `ee/internal/rbac/http/matrix.go`、`protocol.go`，`identity/model.go`、`identity/persistence/store.go`、`identity/http/protocol.go`。
- 上游 `handlers/providers.go` 1–1486、`config.go` 1–1328、`notifications.go` 1–328、`websocket.go` 1–473、`consolepolicy.go` 1–102及其测试；Webhook 本次补读1–545，结合首轮已读545–659覆盖全文；未改动但关键调用方 `handlers/plugins.go` 1–688。
- `rbac/host/routes.txt` 当前748行逐段全文，`ee/host/permissions_test.go`、`rbac_test.go`，app README；新接口指南全文，当前接口/模块说明差异。
- 重新核对累计 Go 文件清单与首轮阅读清单；当前累计变更中的生产 Go 文件与新增测试已覆盖全文阅读。工作树 Go 差异逐项阅读，定向复读了授权、路由、设置、通知、Webhook、插件、账号启停、密码事件和宿主装配调用链。
- 当前两个 RBAC 冒烟脚本首轮已读全文，本轮逐项读修改部分及夹具引用。已删除的历史phase1/phase2脚本没有补读所有历史未变行，不是当前可执行交付物。没有把历史评审/实施自测附件当作本轮独立证据；生成视图只核对属于文档产物，未逐行审阅。

开放式补读重点为“修正是否改变实际调用顺序、失败是否继续产生副作用、契约是否把不同认证方式混用”。没有新的表外可操作发现。此次完成的是首轮全文阅读缺口与相关调用链复读，**没有对四期全部文件再做第二遍逐行阅读**，不将其写成已完成全量二次复读。

## 亲跑普通测试

在上述工作树中执行，均 `-count=1`；没有修改业务代码或测试代码。

| 命令 | 结果 |
|---|---|
| 在ee：`GOWORK=off go test -count=1 ./internal/rbac/... ./internal/identity/... ./internal/host/...` | 通过。rbac 0.008s、rbac/host 0.238s、rbac/http 0.627s、rbac/identity 0.075s、rbac/persistence 9.062s、identity/http 0.629s、identity/persistence 6.408s、ee/host 2.342s；identity根包及hasher无测试。 |
| 在ee：`GOWORK=off go test -count=1 github.com/maximhq/bifrost/transports/bifrost-http/handlers -run 'TestConsolePolicy\|TestProviderPolicy\|TestNotification\|TestWebSocket'` | 通过，0.578s；可选策略、通知及WS现有测试，不能外推上游整个测试集。 |
| `git diff --check` | 通过，无输出。 |

主审另通知其已完成当前工作树全模块race/vet、上游全部handlers测试、SQLite真实HTTP/WS含指标三状态、PG双节点及预飞；本文仅标为实施方通知，不作为评审者亲跑结果。为避免重复工作，没有再次扩展执行这些整套检查。

未完成/未覆盖：本轮独立真实指标HTTP重跑、当前工作树race/vet/构建/全量上游测试、PG独立复跑、旧二进制升级回退、容量压测、全文件第二遍逐行复读。首轮测试记录仍只对应原候选，不自动替代当前修复验证。前端、真实外部模型服务、部署与推送不在本审执行范围。R2用户裁决和新候选正式复核仍是后续必需步骤。
