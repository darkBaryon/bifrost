# 角色权限后端期4：账号补充与敏感权限复核修正

日期：2026-09-17；基于86d5f7db4，正式最终候选见后续评审记录。本文件与本次账号代码同批提交。

## 授权及实现

用户明确选择本轮补齐账号删除和最近登录时间。新增POST /api/accounts/delete及账号last_login_at；删除在共享事务内清理角色、票据、会话及账号，保留密码事件；固定恢复账号、自己与最后启用主管理员受保护。迁移ee_identity_v2_last_login增加可空列，旧值不回填。

同时关闭86d5f7db4定向静态复核的三个剩余条件：代理URL自带认证也受TLS检查；首次PUT配置内置上报插件沿用上游新建语义（仍拒绝无法恢复的标记）；Key别名inference_profile_arn纳入目标判定。对应纯配置回归均已补充，不运行凭据提取或攻击测试。

## 已完成验证

- EE `GOWORK=off go vet ./cmd/... ./internal/...` 与 `go test -race ./cmd/... ./internal/...` 通过。
- SQLite：账号删除、拒绝删除最后管理员、删除失败整笔回滚、最近登录写入失败时会话回滚；新增并发删除/停用的最后管理员保护测试通过。
- PostgreSQL：独立UTF8测试库内上述账号/时间测试及既有跨功能最后管理员竞争测试通过；新增删除/停用并发测试正在运行。
- 首次PG单测误用了本机SQL_ASCII控制库，因client_encoding要求在setup阶段失败；改为本会话创建的专用UTF8数据库后通过，没有据此跳过PG验证。
- 二进制 `ee/tmp/bifrost-rbac-account` SHA256 `8aa75301c3b8af449555febb31eb9c9d8e0d2da378b706dccf76c034c16c18d3`；构建时基于86d5f7db4加本次工作树，vcs.modified=true。
- SQLite真实HTTP/WS完整冒烟通过（含新删除接口、删除后旧会话及WS拒绝、last_login_at字段、敏感权限）：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-7dvt1ygn`。
- SQLite旧认证版本升级、回退、再升级通过；新增列在升级后为null、未推断历史时间：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-rbac-smoke-tvba4htn`。
- 预飞全绿，见账号补充预飞记录。PG双节点真实HTTP/WS和升级回退仍在运行，待追加实际结果。

本记录不是最终验收结论；第三轮独立代码评审与收敛复核完成后再决定是否满足已授权的后端验收条件。
