# 账号认证期1交测包

日期：2026-09-11。标准级/L3；Gate 1通过，Gate 2未请求。用户决定本地账号、默认初始密码123456（部署覆盖）、首次/重置强制改密、数据库会话、不加Redis、管理员重置留痕，后端先行。

## 对象与来源

- 基线：3e15a640d89902772445d3fb4c04023e58828eba。
- 首轮功能提交：00860f57c1a9b55de8bcbe4a752a19a5502aaa88；分支feat/account-authentication。后续交测包/评审记录为文档提交，正式审查以分发时固定的完整提交为准。
- [批准方案v2](../cases/账号认证/期1/方案v2.md)含实际实施和小偏差记录；[路由盘点](账号认证-期1-路由盘点.md)；[接口](../../ee/docs/账号认证接口.md)；[恢复](../../ee/docs/账号认证升级与恢复.md)。新增公开Service.Account仅用于set-status按约定返回account。
- 不包含账号页面、完整RBAC、项目日志页面、SSO或部署发布。页面尚未接入不能判定整个认证产品已交付。

## 开发自测

[预飞3](账号认证-期1-预飞3.md)由workbench/tools/preflight.py按checklist原样执行，11条命令及全部产物/符号检查全绿。预飞1/2保留失败历史：访问日志ticket泄漏，第二轮同时有相应回归失败；已修复URI原始query回退及raw header同步后全套重跑。

- EE go vet、go test -race ./...通过；该轮环境启用专用PG身份fixture，其余EE模块仍用原有独立夹具。身份模块另行在SQLite与PG各跑-race通过，含事件INSERT失败、真实延迟外键COMMIT失败、DDL中途失败、登录与重置竞争、过期/断库、独立salt与历史bcrypt格式。
- 宿主handlers/server -race通过（包括现有OAuth/MCP/WS并发回归）。这些结果不替代真实模型provider调用；本案没有发出计费推理请求。
- npm ci在NODE_TLS_REJECT_UNAUTHORIZED=1下通过，UI按lockfile构建并嵌入；无认证前端改动。npm报告现有依赖4项漏洞，未在本案升级依赖树。
- 真实EE二进制SQLite、共享PG两节点HTTP、品牌HTML/图标/重启回归通过。新日志断言覆盖已签发Cookie、WS票据、setup密钥不会进入应用访问日志。
- 升级/回滚脚本额外通过：旧开关开/关、历史短密码/特殊用户名、文件超72字节回退DB、改文件不覆盖EE、缺DB/残缺配置拒绝启动、无setup密钥、导出事件+恢复完整SQLite备份+旧版本回环鉴权复验。
- 基线二进制来自git archive基线，使用已构建真实UI，不改源；SHA256=35c042223f2bf0bec47fc9a3d8d16cfb5a131daf18bab39137b0f258a5f4ad21。

自测产物ee/tmp/bifrost-http的SHA256=3b6fff72340c0863fdff2b073c1c467700a8c20cb3bec62e479e8a1553c39588。它在功能提交前构建，VCS元数据为fb578de64+modified；源文件内容已完整提交到00860f57c。**独立测试应自行从固定提交重建独立二进制，并记录自己的SHA256及VCS字段，不沿用此自测产物身份。**

## 环境与可复现入口

macOS 26.4.1 arm64，Go1.27.1、Node26.5.0、PostgreSQL17.11。无go.work；从ee驱动GOWORK=off，模块local replace指向当前仓库。先ui/npm ci，再make -C ee build-ui，再ee/go build -o <独立路径> ./cmd/bifrost-http。

开发专用PG使用本次新建initdb集群，只监听回环。连接元信息在本机/tmp/bifrost-auth-test-pg.json；只能将dsn字段放入测试环境，禁止打印。库内测试各建独立schema，HTTP脚本各建独立database，完成只删自己创建的对象。禁止停止/改动其他进程；本次集群由主实施者在所有测试结束后统一停止。基线旧二进制位置在/tmp/bifrost-auth-baseline.json，可只读复用。

- 独立测试至少重建并核对版本、运行SQLite和PG两节点identity-smoke、branding-smoke及升级回滚；补反例不能仅接受开发者断言。
- 独立代码评审按Workbench代码方向对方案/实施/diff与失败、并发、旁路逐项检查。
- 独立收敛评审用干净上下文，按L3四维留痕，与代码评审分开；只写评审记录，不改业务文件。
- 评审/测试期间冻结业务源码。主实施者只整理证据与回复；发现问题由主实施者形成新commit，再按受影响范围复测。

## 剩余边界

默认密码在改密前可被冒用，审计不能阻止管理员代登录或主机管理员改库。反代共享IP限流桶；无认证缓存/Redis，WS每次推送/心跳查询DB；已验证在途响应不承诺撤回。第三方provider实调用、生产TLS/反代环境、真正浏览器认证页面与日志页面不在本期测试结论内。

正式结论待三份独立记录；不得将开发自测、可编译提交或当前文档状态解释为Gate 2通过。

## 第二轮交测

首轮代码评审需修改、收敛评审有Blocking、独立测试未通过，原记录保留。已完成R1–R4、C1/C2及F01关联修复，具体差异和原失败事实见[首轮修复验证](账号认证-期1-首轮修复验证.md)。当前正式预飞为[预飞5](账号认证-期1-预飞5.md)，原11条命令及所有符号/产物检查全绿；预飞4失败源于PG COMMIT测试夹具未真正走到提交阶段，已修正并验证，不能沿用旧证据范围。

完整命令输出现场 `/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-preflight5-output-5u99d9s6`，分别保存每条命令、退出码及stdout/stderr；DSN仅经环境注入。默认目录/升级恢复脚本完整重跑通过，连续10个独立空SQLite目录启动通过。旧版品牌迁移锁冲突及F01原始原因不可追溯均如实保留。

第二轮应从本轮分发的完整提交独立构建，固定SHA256和VCS；按受影响范围复核故障诊断、默认目录、外层日志、失败现场保留、真正PG COMMIT回滚与SQLite初始化。代码和收敛分别出新一轮记录；R2为跨评审转交项，需要明确关闭。Gate 2仍待用户验收。
