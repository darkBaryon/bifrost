---
title: Logo品牌设置 期1 收敛评审 #1
type: 收敛评审
case: Logo品牌设置
phase: 1
round: 1
verdict: 无碍
created: 2026-09-08
---

> 独立评审上下文，未参与实施，与代码评审并行。基线 `8fd392958288016f5c97756cac7b5d5461699bca`，分支 `feat/branding`；评审对象为基线到当前未暂存工作树，包含未跟踪业务文件，不能用基线..HEAD 的空 diff 代替。

## 结论

本次在已有 ee 扩展边界内增加品牌存储、接口和页面，未发现需要阻塞交付的新增结构损伤，收敛结论为**无碍**。这只评价系统结构，不代表验收完成或 Gate 2 放行：PostgreSQL 迁移实测仍为方案明确的验收阻塞，浏览器文件选择器上传、损坏文件和取消选择仍未验证。

## 事实来源

- 完整读取根 `AGENTS.md`、`workbench/AGENTS.md`、流程、收敛机制与模板、Finding 台账、需求、方案 v2（含 Gate 决策和实施三章节）、预飞及浏览器证据。
- 亲自执行 `git status --short`、`git diff --stat/--numstat`、业务文件完整 `git diff`、`git diff --cached --stat`、`git rev-parse HEAD`、`git branch --show-current`，核对分支/基线相符、暂存区为空；用 `rg --files` 枚举未跟踪实现，逐份读取 Store/表/迁移、handler、bootstrap、三个 UI 组件/schema、共享 hook 及 API 类型、所有本案 Go/TS 测试、新 smoke 和开发指南。
- `git log -5 --oneline -- ee/transports/bifrost-http/handlers/branding.go ui/lib/hooks/useBranding.ts`：handler 来自 `d33e7db99` 骨架；hook 已历经 `3319581fc` 品牌入口、`ce1b2a605` 缓存、`64208fa31` 更新屏消费。此次只调整既有解析函数的 icon 回退，没有另建展示入口。
- 核读上游 `framework/configstore/rdb.go:381` 的迁移连接出口与 `migrations.go:162` 的单迁移执行器，确认本案实际复用方式；核对未改动既有 API client。
- [预飞](../../../evidence/Logo品牌设置-期1-预飞.md)记录 Go build/vet/test、UI build/typecheck、5 条 hook 测试和真实二进制重启 smoke 全绿；[浏览器证据](../../../evidence/Logo品牌设置-期1-浏览器.md)明确区分已执行与受阻。此处为审阅执行证据，未冒称本评审重新运行测试、启动服务或完成独立验收。

## 六维核查

| 维度 | 发现 | 证据与反证条件 |
|---|---|---|
| 重复 | 无碍。两个槽位共用解码、上传组件和 Store 写逻辑；沿用 RTK API，无第二套持久化或 API client。预览与实际 hook 均表达“独立 icon → Logo → 默认”，目前一致，且分别消费草稿与服务状态，不要求本次扩大共享导出。 | `handlers/branding.go:93`、`configstore/branding.go:40`（均在 ee 下）；`brandingPreview.tsx:13` 与 `ui/lib/hooks/useBranding.ts:140`。反证条件：后续出现预览/实际回退分歧或第三套规则消费者，应重新评估复用。 |
| 膨胀与职责 | 无碍。handler 负责校验/HTTP，Store 负责事务和哈希，迁移独立；UI 分为表单协调、上传槽和预览。bootstrap 新增鉴权检查、迁移调用和依赖装配，未塞入图片业务。 | `ee/transports/bifrost-http/server/bootstrap.go:53`；`ee/framework/configstore/branding.go:35`；`ee/ui/app/enterprise/components/branding/brandingView.tsx:57`。反证条件：下一功能继续堆入业务判断、按功能开关或生命周期依赖时，按 FIND-012 重新评估拆分。 |
| 架构边界 | 无碍。迁移从 ConfigStore 出口取得连接，运行时 Store 接受注入 DB；写路由复用 APIMiddleware。共享 hook 修改位于 Gate 1 已批准例外内，无 core/上游存储层反向依赖，无新依赖包。 | `bootstrap.go:56,67`；`ee/framework/configstore/migrations.go:16`；`ui/lib/hooks/useBranding.ts` diff。反证条件：发现新增上游→ee import，或迁移绕开获批连接出口，则结论失效。 |
| 契约一致性 | 无碍。Go 状态/请求与既有 TS 字段、可选槽位契约一致；has_icon 表示独立资产，回退只在展示层；API、认证关闭/白名单、缓存、回滚边界已沉淀开发指南。本案无新 config.json 字段，未漏改配置 schema。 | `ee/transports/bifrost-http/handlers/branding.go:46,91`；`ui/lib/store/apis/brandingApi.ts:15,37`；`docs-zh/04-开发指南/08-Logo品牌设置.md`。反证条件：产生额外配置字段或客户端需要不同字段/空值语义时，须同步契约。 |
| 测试健康 | 无碍，但验收证据有明确边界。SQLite 测试使用临时文件、检查并发分槽合并/重开/失败回滚；HTTP 测试使用实际管理鉴权；hook 测试锁定回退与 OSS 默认。smoke 自建配置并只管理自己的 PID，未复制旧 smoke 的个人数据库依赖。 | `ee/framework/configstore/branding_test.go:28,81`、`migrations_test.go:13`；`ee/transports/bifrost-http/handlers/branding_test.go:150`；`ee/scripts/branding-smoke.py:35`；`ui/lib/hooks/useBranding.test.ts:29`。反证条件：声称这些 SQLite/mock hook 结果覆盖 PG 或真实上传，即属证据越界；两项当前明确未完成。 |
| 非功能 | 无碍。两槽先验证后事务写入，事务内读回；迁移外层唯一持有提交/回滚、PG 锁与版本记录同事务；图片限定格式/字节/尺寸并完整解码，先验证当前 hash 再 304。没有新增无限缓存或长期文件资源。 | `ee/framework/configstore/branding.go:37,57`；`migrations.go:17,27`；`handlers/branding.go:132,147,189`。反证条件：PG 隔离实测显示锁/DDL/版本不能共同回滚须修；本次静态检查不替代该实测。公开读每次加载单例图片有固定上限，未做负载测量，不承诺热点性能。 |

表中短文件名均对应“事实来源”列出的本案文件；UI 组件位于 `ee/ui/app/enterprise/components/branding/`。

## 出口清单

### Blocking（阻塞 Gate 2，交用户裁决）

- 无新增**结构** Blocking。方案 §6 的 PostgreSQL 验收阻塞仍有效：取得隔离实例完成验证，或由用户明确缩小交付范围后才能解除；收敛评审无权替代裁决。浏览器文件选择相关验收也未完成。

### 立债（REF）

- 无新增。公共构建与探针债沿用 Gate 1 整合边界，不因本次评审扩成公共重构。

### 挂账（Finding）

- 无新增。已有 FIND-012、FIND-013 再次观察结果见下，由主代理统一更新台账，本评审不改台账或生成视图。

## 台账对账

- **FIND-012**：bootstrap 本次净增 10 行，仍是薄装配，未见职责失控；继续观察，不凭行数要求重构。建议 `triggered_by` 追加本案，保留下次功能扩展时重评的触发条件。
- **FIND-013**：首个业务功能触发条件已到达；探针仍裸 AutoMigrate/公开路由，代码未扩大其能力。Gate 1 已明确接受本案延期到公共整合，未关闭，不重提为本案新增 Blocking。建议台账追加本案和已批准延期事实，正式对外构建前依旧需要处置。
- **FIND-015**：旧 smoke 没有修改；新品牌 smoke 使用独立临时 DB、动态端口、自有进程，避免继承旧债，但不据此关闭旧条目。
- **FIND-014/FIND-016**：未改 ee main 抄本或 ServerCallbacks 通路，无本次恶化。
