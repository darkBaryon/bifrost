---
title: Logo品牌设置 期1 方案评审 #2
type: 方案评审
case: Logo品牌设置
phase: 1
round: 2
verdict: 通过
created: 2026-09-08
---

## 结论

**通过（方案差量复核）**。v2 关闭评审1的 B1、B2，补充变化未扩大业务范围。**尚待用户 Gate 1，不允许据此进入实施**；这不是功能验证、迁移实测或交付通过。

范围：独立比较方案 v1/v2，重新核查 checklist 与迁移基线源码，沿用评审1未受影响的逐章证据。未改方案、业务代码或视图。

## 逐章核查

| 变化 | 亲核证据与结论 |
|---|---|
| §4.1 迁移事务，B1 | v2 明确外层 GORM Transaction 是唯一所有者，复制 options 并置 UseTransaction=false，将同一 tx 传入 RunSingleMigration；PG 在该事务内设置局部锁超时、取得事务锁后再查版本/DDL/种子/记版本，SQLite 跳过 PG SQL。重新读 `framework/migrator/migrator.go:179-186,681-702`，false 分支确实直接使用传入 DB 且不再 Commit/Rollback；`framework/configstore/migrations.go:162-178` 接受该 options；`postgres.go:83-97` 回调只提供临时池。已消除二次 Begin 与锁脱离迁移事务的设计歧义。**B1 关闭**。 |
| §4.1 写入与返回状态 | Update/Reset 的状态读取纳入写事务，读取失败回滚，提交后不再查库；区分提交后的网络中断并要求重读，解决上轮“写成功后读失败仍报失败”的建议。单槽 patch 和两槽原子更新范围不变。 |
| §4.2 HTTP 边界 | 当前 slot/hash 存在检查先于 304，避免 reset 后错误续用旧 ETag；3 MiB 明确为 handler 解码前限制，100 MiB 上游默认接收限制与首轮源码证据一致，不修改全局 ClientConfig。 |
| §4.3 展示/缓存 | 同步回退注释被列入共享 hook 的必要差量，首屏短暂默认/缓存及不保证零闪烁写明；无新增 ShellRewriter 品牌功能。现有独立 icon → Logo → 默认回退设计不变。 |
| §6 测试 | 新增事务内读失败、迁移种子/版本失败共同回滚、PG 并发初始化/锁超时/取消与重试、SQLite busy；认证明确启用且无品牌白名单前提，覆盖 Cookie/Basic/Bearer。PG 缺隔离条件明确为验收阻塞，只有补实测或用户裁决缩小交付范围并标记 PG 未交付方可解除，符合 L3 证据要求。此处均是待执行测试要求。 |
| §7 checklist，B2 | 亲读新增 allowed-paths：从精确基线取 tracked diff，再并入非忽略 untracked，业务文件逐项列出，文档限定本案及必要生成物；原调研另保留 hash。Go 格式检查使用只读 gofmt -l 并断言空输出；TS 使用 Oxfmt --check 指定本案路径。YAML 加载与嵌入 Python 语法解析通过；仅实际运行 allowed-paths 返回 0。它会拒绝未许可的 sidebar/导航/覆盖脚本，不再仅依赖 diff --check。**B2 关闭**。 |
| §8/Gate | 定档、公开读/鉴权写接口和新表、共享 hook 例外、公共基础整合、FIND-013 延期仍待用户裁决。方案未把本轮复核代替用户决定，范围与回滚口径未变。 |

## 可证伪质疑

1. B1：UseTransaction=false 后是否仍在 migrator 内 Begin/Commit？亲核源码答案为否，关闭设计阻塞；实际迁移须依 §6 实测。
2. B2：越界业务文件是否进入检查集合且被拒绝？代码将基线 diff 与 untracked 合并后逐项匹配，白名单不含 sidebar/导航/sync.sh，关闭设计阻塞。当前文档工作树执行返回 0；未伪造未来实现检查通过。
3. 尚未发生的事实：Gate 1 放行、数据库与浏览器验证、独立测试、代码/收敛评审、Gate 2。均不能由本记录推定已完成。

## 建议栏（不立案）

无新增需求。保留评审1的实施关注点：条件 GET 先查当前资源、认证例外清楚记录、草稿与持久化状态分离、仅清理自有临时资源。未来实现若需增加白名单外文件，应按偏差流程先说明，不能扩大通配范围规避守卫。

## 验证记录

- `diff -u 方案v1.md 方案v2.md`：确认声明变化与实际变化一致，v1 已冻结为需修改，v2 待评审。
- `cat checklist.yaml` 并用 `yaml.safe_load` 加载；对 allowed-paths/go-format 内嵌 Python 用 `ast.parse`：通过，共 17 项命令。
- 重新 `sed` 查看 DefaultOptions、migrator begin/commit/rollback、RunSingleMigration、PostgreSQL RunMigration 回调；`rg` 查看 preflight 的 YAML 加载与 shell 执行方式。
- 单独运行已审阅的 allowed-paths 只读守卫：退出码 0。
- 未运行完整 preflight、gofmt/Oxfmt 实现检查、构建、单测、数据库/二进制/浏览器验证，未起服务，未暂存/提交/推送。仅新增本评审记录，视图交设计职责统一生成。
