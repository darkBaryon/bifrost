---
title: Logo品牌设置 期1 方案 v5
type: 方案
case: Logo品牌设置
phase: 1
version: 5
status: 已归档
relates: [Logo品牌设置]
created: 2026-09-09
---

> 2026-09-09 按用户要求归并：这是 Logo品牌设置验收前的接口命名调整评审修改，沿用期1，不是独立需求。原记录的验证范围与结论保留。


## 本期方案范围

本版承接 [原功能方案 v2](方案v2.md) 的功能、存储和验收要求，以及 [布局修改 v3](方案v3.md)。下文只说明本轮接口与命名修改；没有再次列出的原需求范围和验收项仍有效。本方案实施时 PostgreSQL、浏览器上传与 Gate 2 验收尚未完成。2026-09-09 浏览器上传已复测通过，但发现图片错误未阻止保存的问题；当前待验收事项以[需求记录](../../../reports/Logo品牌设置.md)和[浏览器复测](../../../evidence/Logo品牌设置-期1-浏览器复测.md)为准，不能按单轮测试通过视为整期交付。当前统一检查入口为 [checklist.yaml](checklist.yaml)。

## 摘要与起点

落实用户刚确认的命名与 POST 路径，同时把 4 份 handler 测试合回一份。commit 为 `8fd392958288016f5c97756cac7b5d5461699bca`，实际起点是当前未提交代码；副本及全工作区 hash：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/branding-api-before-xeruces2`。

## 现状与设计

handler.go 注册 GET/PUT/DELETE /api/branding；settings.go 定义 brandingState/stateFor；真实生产调用为 ui/lib/store/apis/brandingApi.ts 的 get/update/reset（useBranding 和企业表单消费其 hooks）。smoke 使用相同旧路径。图片地址单独 GET，继续保持。

将业务接口替换为 POST /api/branding/get（公开）、POST /api/branding/update 与 /reset（原管理端 auth）。不保留旧业务路由别名；这是用户选定规则的落地，旧客户端需要随前后端一起更新。图片 GET、JSON 字段、失败码、鉴权、存取和缓存语义不变。

Go 私有符号 brandingState → brandingResponse、stateFor → toBrandingResponse，局部响应变量也使用 response，测试 decodeState → decodeResponse。前端 BrandingState 类型本次不做额外改名，JSON 契约不变。原 fixture/settings/validation/handler 四份测试合为 branding_test.go，保留全部测试函数和断言，再增加 POST 路由与错误方法回归。

## 文件白名单与步骤

1. 更新 handler 功能包、测试；bootstrap 只订正公开查询路由注释。
2. 共享文件 ui/lib/store/apis/brandingApi.ts 仅改三个请求的路径和方法，并同步附近中文说明；可新增该模块请求契约测试。保留 RTK endpoint/hook 名、缓存 tag、响应/请求类型。
3. 更新 ee/scripts/branding-smoke.py、ee/scripts/smoke.sh 的 T6 企业接口调用（POST /api/branding/get，包含 Content-Type 探测；OSS 的 GET 只作为原版回退探测保留）、docs-zh/04-开发指南/07/08、活跃 checklist 与过程记录。历史评审不改写。

## 验证

先让新 POST 用例在旧路由上失败，再改实现确认通过。前端请求定义测试覆盖三个路径、POST 与 update body；原 hook/图片用例继续通过。Go vet/test、UI 类型/构建、二进制与真实认证/重启 smoke，以及旧符号/调用路径终扫。另检查 smoke.sh 的 bash 语法；机械命令见 checklist.yaml。不触碰数据库迁移，不扩展 UI 功能。

## 风险与回退

旧业务路径退出，前后端必须同次交付；只影响本轮明确请求的品牌接口。查询仍公开供登录页读取，POST 不代表需要鉴权。回退使用本次副本，仅还原触及文件，不覆盖其他未提交代码。

## Gate 决策结果

用户已确认 POST /功能/动作，要求修改刚讨论的几处；共享 brandingApi 请求改动是接口变更必需接缝。无待追加业务取舍，方案复核通过后实施。


## 实施记录

完成三个 POST 动作接口、Go 响应命名、四份 handler 测试合并、前端三个请求和契约测试、两份 smoke 调用与指南同步。查询仍公开，写入仍复用 auth，图片 GET 不变。活跃 Logo checklist 的测试文件路径同步；布局案 checklist 原本未列拆分测试路径，无需改动。

新路由用例先红后绿；范围对账与验证边界见 [范围与回归](../../../evidence/Branding接口命名调整-期1-范围与回归.md)，完整机械验证见 [预飞](../../../evidence/Branding接口命名调整-期1-预飞.md)。

## 实施偏差记录

smoke.sh 的遗漏在方案评审后通过 v5 补齐，再开始实施；OSS GET 回退探测按方案保留。代码评审追到调用方 ui/lib/hooks/useBranding.ts 的缓存说明仍写 GET 查询和 long max-age，追加同步这一段中文注释，写明 POST 查询与图片 GET/ETag 验证；只改注释，符合用户要求注释随代码更新。未额外改前端类型命名或存储结构。

## 蓝图验证备忘

本案为标准级，无跨期蓝图。旧业务路由退出，交付时前后端一起更新。

## 最终验收结果（2026-09-09）

用户已确认功能测试通过；其后授权的图片校验错误保存修复及真实PostgreSQL实测已完成，代码评审4通过、收敛评审5无碍。本期统一验收归档，历史未完成项已在[验收记录](验收记录.md)和[收尾验证](../../../evidence/Logo品牌设置-期1-收尾验证.md)更新。
