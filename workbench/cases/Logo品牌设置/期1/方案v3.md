---
title: Logo品牌设置 期1 方案 v3
type: 方案
case: Logo品牌设置
phase: 1
version: 3
status: 已归档
relates: [Logo品牌设置]
created: 2026-09-09
---

> 2026-09-09 按用户要求归并：这是 Logo品牌设置验收前的代码布局整理评审修改，沿用期1，不是独立需求。原记录的验证范围与结论保留。


## 给用户的摘要

按已确认的编码规范整理 branding：handler 按职责拆进功能包，存储与迁移归到 branding 存储包，前端拆出图片读取与表单逻辑。业务行为不变，无新增业务拍板项。

## 1. 起点

commit `8fd392958288016f5c97756cac7b5d5461699bca`，分支 `feat/branding`，现有脏工作区是实际起点。文件副本与全工作区哈希存于 `/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/bifrost-branding-layout-j09qzy0i`；不把起点已有改动算成本轮实施。

## 2. 现状与调用方

- `ee/transports/bifrost-http/handlers/branding.go`：路由、状态转换、校验、设置读写和图片缓存。真实生产调用只有 ee `server/bootstrap.go`；测试同包。
- `ee/framework/configstore/branding.go`、`migrations.go`、`tables/branding.go`：存取、迁移、表。生产调用为 branding handler 与 bootstrap；无其他 ee 调用者。
- 企业 `components/branding/brandingView.tsx` 包含 readImage、failureMessage、表单状态与 JSX；Upload/Preview 已按职责拆开。schema 的既有企业路径保留，以保持覆盖接口；上游 `ui/` 文件只核验，不改动。
- 已有 Go 用例覆盖图片校验、鉴权、缓存、持久化、原子性、重复迁移与取消；原 smoke 验证真实二进制和重启。先运行当前测试，再移动。

## 3. 目标与边界

HTTP 路由、JSON、错误消息、限额、鉴权、ETag、SQL 与迁移 ID 保持；前端 testid、状态转换与提示保持。只调整文件位置、Go 包归属、import、前端函数位置与必要调用接线，补中文注释。不顺带修错误链、三元组、日志、锁键或 ORM 标签等既有问题。

## 4. 目标布局与符号白名单

- `handlers/branding/`：handler.go（定义/构造/注册）、settings.go（读写/响应视图）、validation.go（输入及图片校验）、asset.go（图片投递），测试随职责移入。
- `configstore/branding/`：store.go、migration.go、table.go 及对应测试；表归属该功能包，不改变表名或字段。原 configstore 根功能文件与 tables/branding.go 删除。
- 前端保留 brandingView/Upload/Preview，新增 image.ts（读图）、useBrandingForm.ts（表单状态与操作），已有 schema 原位补注释。
- 原 handlers.BrandingHandler/NewBrandingHandler 移到 handlers/branding，原 configstore.BrandingAsset/BrandingPatch/BrandingStore/NewBrandingStore/MigrateBranding 及 tables.Branding 移到 configstore/branding；名字与签名除包路径变化外不变，不留转发兼容层。前端新增 readImage、useBrandingForm 导出供同功能文件调用。
- bootstrap 两个 import 和调用接线同步；Probe 等其他功能不迁移。docs-zh 07/08 与活跃 checklist 更新路径，历史方案/评审保留事实，并补本次后续记录。

## 5. 实施与验证

按后端分包、前端职责拆分、引用清理顺序实施。基线与整理后均运行既有行为测试，比较测试名称与关键函数逻辑；新抽出的读图能力补有意义的异步成功/失败边界测试。Go vet/build/test（含 race）、UI typecheck/Vitest/build 与隔离二进制 smoke 验证接线。无新增表结构，不声称新做 PostgreSQL 实测。

## 6. 回退

只恢复本轮涉及文件至起点副本，删除本轮新增文件；不使用 git reset/checkout 覆盖用户其他未提交内容。

## Gate 决策结果

用户已明确要求按共同确认的规则重排；本轮没有新增业务规则或外部 API 变化，Gate 1 无新增拍板项，方案复核通过后自动继续。


## 实施记录

后端 handler、存储/迁移/表和原测试均已迁入功能包；旧文件删除，bootstrap 改到新包，Probe 仅更新目录注释。前端已拆 image 和 useBrandingForm，页面/上传/预览各自保留职责。schema 原位补说明并指向新校验路径；活跃 checklist 和开发指南同步。

本轮不提交，工作树为交付物。Go 声明、前端状态/操作和 JSX 与起点 AST 对账通过，详见 [行为与范围对账](../../../evidence/Branding代码布局整理-期1-行为与范围对账.md)。既有 Go 测试、vet/race、UI 类型、13 个前端用例和 UI 构建已通过；预飞统一记录最终命令结果。

## 实施偏差记录

无业务行为偏差。image.ts 用相对路径引用企业 schema，以便既有 Vitest 配置解析，不改测试配置或上游代码。原 `lib/tables.go` 的未来迁移指引按新布局修订，仅注释变化。新 schema 仍使用原位置。原 Logo 活跃 checklist 的路径白名单还缺同工作区先前的规范文件，本轮根据起点哈希逐文件补入 23 个已有路径；新改动范围仍由本轮起点哈希单独核对，不扩大应用范围。

## 蓝图验证备忘

不适用。

## 最终验收结果（2026-09-09）

用户已确认功能测试通过；其后授权的图片校验错误保存修复及真实PostgreSQL实测已完成，代码评审4通过、收敛评审5无碍。本期统一验收归档，历史未完成项已在[验收记录](验收记录.md)和[收尾验证](../../../evidence/Logo品牌设置-期1-收尾验证.md)更新。
