---
title: EE现有代码架构迁移 期1 验收记录
type: 验收记录
case: EE现有代码架构迁移
phase: 1
created: 2026-09-11
---

## 验收结论

通过。用户已明确授权：迁移完成后删除探针，在新分支提交并合回主 worktree，再创建认证 worktree，原权限 worktree 不动。本记录落实该授权；认证实现另行开发。

## 交付物核对

- 入口迁入 cmd，装配迁入 internal/app，品牌按业务根包、HTTP 与存储分层；UI 保留 ee/ui 覆盖方式。
- 删除骨架探针接口、空插件、表注册、响应头/HTML 标记及专用测试；不执行旧库删表。make smoke 替换为隔离品牌冒烟。
- 项目、EE、product 与 workbench 入口职责明确；编码和收敛规范引用已确认的产品架构。
- 在独立 refactor/ee-architecture worktree 收尾，原权限工作树文件哈希核对未变，其未提交权限方案没有带入本次提交。

## 验收方式与证据

[预飞](../../../evidence/EE现有代码架构迁移-期1-预飞.md)全绿：Go test/vet、完整 UI 与 EE 构建、隔离真实 HTTP 冒烟；覆盖旧 SQLite 兼容、品牌原子操作、Cookie/Basic/Bearer 鉴权、缓存及重启保留。追加 SPA fallback 断言后定向冒烟通过。12 份当前入口与包文档链接有效，工作台视图校验通过。

[代码评审1](代码评审1.md)、[代码评审2](代码评审2.md)通过；[收敛评审1](收敛评审1.md)、[收敛评审2](收敛评审2.md)无碍。PostgreSQL 专用锁测试因未配置测试库未运行，不以 SQLite 验证替代。

## 度量

计划、评审、开发、Gate 的分阶段模型、起止时刻及 token 数据缺失，不估编。

## 归档动作

方案已归档，需求已交付。长期架构、包职责、构建入口已回填 product 架构及各 README；FIND-013/015 转入本案处理。提交与合入后从 develop 创建认证 worktree。
