---
title: 工作台基础信息解耦 期1 代码评审 #1
type: 代码评审
case: 工作台基础信息解耦
phase: 1
round: 1
verdict: 通过
created: 2026-09-23
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 （见 checklist 基线） 到候选 030c362a57ff2b0e538b87ac3549af7b9cad6ded；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**代码评审 verdict：通过，本轮未发现阻塞修复项。** 指定候选实现了外壳与完整配置权限解耦，真实 HTTP 核验确认 bootstrap 200、路由列表 200、完整 config 403；鉴权、字段投影、错误处理及兼容路径符合方案。构建、类型检查和测试通过。**本轮完整浏览器亲核未完成**：Chrome 在打开页面前退出，详见验证记录；不把实施方证据视为本轮浏览器验收通过。

## 2. 对照方案与接口面对账

| 方案要求 | 代码与亲核结果 |
|---|---|
| 精确新增 `POST /api/console/bootstrap`，严格空对象请求 | 一致；错误方法、近似路径、非法正文均拒绝 |
| 有效个人会话可读，不要求 Settings.View | 一致；真实权限服务测试覆盖无角色、仅路由权限及未改密会话 |
| 完整 config 继续授权 | 一致；新构建二进制实测仍为 403 |
| 基础字段白名单，重启状态只返回 required | 一致；完整对象及嵌套字段断言通过，未返回内部 reason |
| EE 使用独立查询，OSS 保留原 config | 一致；缓存隔离、Config 失效刷新、双构建及兼容测试通过 |
| 保留退出、环境、重启提示；关闭 EE 旧向导 | 一致；组件测试覆盖，实际浏览器验证受环境限制 |
| 不改路由业务组件、登录或全站权限接入 | diff 符合范围，没有额外交付声明 |

新增导出符号与接口面均在方案声明内。未发现多做、少做或未登记的业务偏差；Sidebar 格式债务对照及隔离构建方式已登记。

失败路径核对结果：

| 路径 | 最终结果 |
|---|---|
| 来源不合法、Authorization、会话失效 | 403／401；禁止缓存 |
| JSON、媒体类型、长度不合法 | 400；认证先于解码 |
| 身份存储失败、宿主配置缺失 | 脱敏 503；身份存储诊断经既有日志出口关联请求 |
| 重启状态读取失败 | 按批准方案省略附属字段、返回基础状态；固定原因及 request_id 告警 |
| 首次查询失败 | 原失败视图及重试入口 |
| 已有缓存后刷新失败 | 按 Gate 决策保留正文 |
| smoke 失败 | 保留现场，回收自建服务，删除会话文件 |

## 3. 本轮修复项

无。

## 3b. 建议(nit)

无。

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：**无新增业务取舍。Gate 2 需明确处置本轮真实浏览器验证未完成这一限制：补跑完整 UI 验收，或由用户明确接受该验证限制。

**有依据撤回：**

- 首次二进制构建失败不是代码编译缺陷：默认 Go 缓存访问被拒，迁至 scratch 后构建通过。
- smoke 浏览器失败不能认定为页面回归：失败发生在 `open about:blank`，尚未访问产品页面；CLI 记录 EPERM、TargetClosedError 和 Chrome SIGABRT。

## 5. 转收敛评审 / 挂账

转收敛评审：无新增项。

挂账：无。

## 6. 开放式复读的表外发现

已做开放式复读，无表外发现。重点重新检查了失败诊断、缓存恢复、接口旁路、脚本失败现场及自建进程回收。

## 7. 起手表覆盖

| 信号 | 结果 |
|---|---|
| 验收标准有未覆盖项 | **发现：本轮真实浏览器亲核未完成，属于验证限制** |
| 方案、实施记录、diff 三方不一致 | 无 |
| 多出的功能、公共接口、抽象或依赖 | 无 |
| 偏差未登记 | 无 |
| 失败被吞或降级成假成功 | 无；重启附属读取降级已有明确批准 |
| 空值、空集合缺少分支 | 无；存储缺省、env 空值、restart nil/false/error 均有处理 |
| 取消、超时后仍有写入或副作用 | 无；新增接口只读，无业务写入 |
| 重发产生重复副作用 | 无；真实 HTTP 重复请求通过 |
| 事务外写后读、多方回滚不一致 | 不适用；未新增写事务 |
| 开关关闭、旧数据、旧客户端无处理 | 无；OSS fallback 与旧接口保留 |
| 昂贵检查先于廉价检查、校验缺层 | 无；按批准顺序检查，解码前限长 |
| 路由保护、认证关闭行为 | 无；EE 仍强制个人会话，未读取旧认证开关放行 |
| 敏感信息进入日志或响应 | 无；白名单及错误脱敏测试通过 |
| 热路径新增 I/O、无界缓冲或循环 | 无异常；仅方案声明的会话与重启状态读取 |
| 新配置、环境依赖、部署步骤未登记 | 无 |
| 服务端无法追溯失败 | 无；追踪至身份存储诊断及重启固定告警 |
| 对外泄露内部错误 | 无 |
| 脚本失败清理现场 | 无；实际失败日志及临时目录保留 |
| 新规则或失败路径无测试 | 无代码覆盖缺口；浏览器执行限制另列 |
| 断言绑定实现而非行为 | 无；检查状态、字段、请求、缓存及渲染结果 |
| 失败信息无法定位 | 无；本轮构建和 CLI 失败均可定位具体步骤 |
| 返回值、错误未检查 | 无新增阻塞问题 |
| fixture 依赖个人数据或固定端口 | 无；临时 SQLite、随机端口、自建账号，依赖目录可指定 |
| mock 被写成真实环境结论 | 无；配置分支注入明确限定为 UI 渲染证据 |
| 整体替换模块的 mock | 无不当替代；组件隔离 mock 之外有真实 RTK、权限服务及 HTTP 验证 |
| 调试输出、后门、本机配置 | 无 |
| 未使用代码、无关改动 | 无范围内发现 |
| 生成物与源不一致 | 无；构建前后源码指纹一致 |

## 8. 验证记录

### Git 与版本绑定

- 起始：`feat/console-ui`，HEAD 为 `030c362a57ff2b0e538b87ac3549af7b9cad6ded`。
- 执行了 `git status --porcelain=v1 --untracked-files=all`、`git diff --stat fa3eacedc28891cc45bc5107c350457cf196e42e`；起始工作树干净，无未跟踪文件。基线差异共 57 文件，其中本次指定评审范围 35 文件。
- 结束时业务源码及索引未变；验证说明被本会话之外追加两行评审授权状态，最终状态为该证据文件修改、`+2` 行，无未跟踪文件。
- 2796 个源码文件构建前后指纹一致：`f14a38c938cb10d046c933ac410abc92385e6f6474e6379e518891d2a2b81b56`。

### 实际执行的命令

| 命令 | 结果 |
|---|---|
| `python3 -B workbench/tools/build_views.py --check` | 通过，213 篇文档 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race ./internal/host/... ./internal/identity/http/... ./internal/rbac/host/... ./internal/app/...` | 四包通过，部分命中缓存 |
| 上述 race 命令增加 `-count=1` | 四包全部重新执行通过 |
| `python3 ee/scripts/console-bootstrap-smoke.py` | 已执行；Go 默认缓存 EPERM，未进入浏览器 |
| `GOCACHE="$TMPDIR/go-build-cache" python3 ee/scripts/console-bootstrap-smoke.py` | 全部构建检查通过；浏览器 daemon 默认缓存 EPERM |
| `PWTEST_DAEMON_SESSION_DIR="$TMPDIR/playwright-daemon" GOCACHE="$TMPDIR/go-build-cache" python3 ee/scripts/console-bootstrap-smoke.py --build-manifest "$TMPDIR/bifrost-console-check-b6eip3bj/build/build-manifest.json"` | 复用核验过的产物；Chrome 打开前 SIGABRT，smoke 退出 2 |
| `python3 -B` 内联自包含 HTTP 核验 | 复用 smoke 的产物校验、启动及回收函数；27 个请求检查通过 |
| `git diff --check <基线> -- ee ui product/上游改动/README.md` | 通过 |
| checklist 四项必存产物存在性检查 | 全部存在 |
| `rg -n -F '/api/console/bootstrap' ee/internal/host` | 18 处 |
| `rg -n -F 'console.log("DEBUG' ee/ui` | 0 处 |

smoke 内实际完成：Sidebar 格式债务对照、其余目标文件格式检查、EE/OSS Vite 构建、两套 `tsc --noEmit --incremental false`、**4 个测试文件共 22 项通过**、EE 二进制构建。构建仅有大 chunk 提示。

补充 HTTP 核验检查了真实权限前提、响应白名单、no-store、请求编号、来源、Authorization、无效/撤销会话、非法 JSON、16KiB 边界、错误方法及近似路径。使用二进制 SHA256：

`597b7d9a699ed96d115bae901fb5ba493cbc1b6d63f5962bf82ee1be1a594d6e`

产物均在指定 scratch：

- [本轮构建清单](</Users/xinyue/VSCode/ws_2026/bifrost-console-ui/workbench/tmp/codereview/工作台基础信息解耦-期1-代码评审1/scratch/bifrost-console-check-b6eip3bj/build/build-manifest.json>)
- [浏览器失败现场](</Users/xinyue/VSCode/ws_2026/bifrost-console-ui/workbench/tmp/codereview/工作台基础信息解耦-期1-代码评审1/scratch/bifrost-console-check-rwhoemd3/output/playwright/result.json>)

### 阅读范围与未执行项

全文阅读了指定 35 个文件、要求先读的规则/需求/方案/台账及预飞记录；核对了证据中的结果、截图和源码指纹。补充追踪了身份认证与存储诊断、宿主装配、原 config handler、RTK base/session/config API、向导 skip、覆盖脚本及 smoke 夹具。未读取旧代码评审或旧收敛评审。

checklist 命令没有跳过。未完成候选完整浏览器流程及基线浏览器复跑，原因是浏览器启动失败；既有截图仅作只读证据检查。额外 `ps` 只读检查被沙箱拒绝，未重试。未启动 dev server，未连接已有实例；自建 EE 服务已回收，会话文件剩余数量为 0。

verdict: 通过
