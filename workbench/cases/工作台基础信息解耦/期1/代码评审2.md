---
title: 工作台基础信息解耦 期1 代码评审 #2
type: 代码评审
case: 工作台基础信息解耦
phase: 1
round: 2
verdict: 通过
created: 2026-09-23
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 030c362a57ff2b0e538b87ac3549af7b9cad6ded 到候选 fe626961c86acda99a7fddd394f51f137989cd3b；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 本轮五个文件的差量为文档、注释及 `59817` 等值常量提取，未发现行为回归或阻塞项。四项转交问题均已核对。亲跑构建、类型检查、22 项前端测试、Go race 和 27 项真实 HTTP 检查通过；本会话 smoke 的浏览器启动失败，未完成页面亲跑。对随后新增的最终候选独立浏览器证据，已核验原始网络记录、DOM、截图及源码／产物来源，未将其冒充本会话执行结果。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 五文件差量与实施声明 | 一致；两个 Python 文件经 AST 比较，在常量等值替换后执行结构相同；Go 仅增加注释 |
| bootstrap 与完整配置权限分离 | 真实 HTTP：唯一权限 `RoutingRules.View`，bootstrap 200、routing 200、config 403 |
| 来源、身份、正文、字段边界 | 亲核通过；响应白名单、no-store、请求编号、非法来源／会话／正文及撤销路径符合方案 |
| 重启读取失败 | 保留基础响应、省略附属字段；测试核对固定脱敏告警及 request_id |
| EE／OSS、退出、环境和重启提示 | 双构建及组件测试通过；新增浏览器原始证据与声明一致 |
| 非目标 | 未改路由业务组件、登录／改密或全站权限接入；未扩大交付声明 |

未发现多做、少做、三方不一致或未登记业务偏差。无需恢复全量评审。

**上一轮修复项处置：**已读取 `代码评审1.md`，其「本轮修复项」为空，无待关闭条目。上轮浏览器验证限制与本轮执行情况分别记录，不沿用“已测过”代替亲核。

## 3. 本轮修复项

无。

## 3b. 建议(nit)

无。

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：**无新增事项；最终 Gate 2 仍按既定流程验收。

**四项转交逐条处置：**

| 转交项 | 处置及依据 |
|---|---|
| Topbar 多一次会话状态请求 | **撤回阻塞质疑。** [EE hook:27](/Users/xinyue/VSCode/ws_2026/bifrost-console-ui/ee/ui/app/enterprise/hooks/useConsoleConfig.ts:27) 确实调用既有状态查询，符合方案 §4.2。该接口读取状态及会话，无业务写入；同一 RTK 查询缓存可复用，没有轮询或无界请求。原始浏览器记录也显示该请求 200。 |
| 子测试共享 `a` | **撤回。** [投影测试:59](/Users/xinyue/VSCode/ws_2026/bifrost-console-ui/ee/internal/host/consoleprojection_test.go:59) 未使用 `t.Parallel`，每例重建配置、存储及日志，并重置 `a.host`、`a.consoleLog`；[不可用测试:118](/Users/xinyue/VSCode/ws_2026/bifrost-console-ui/ee/internal/host/consoleprotocol_test.go:118) 也是同步调用。强制重新执行的四包 race 测试通过，未见状态串扰。 |
| smoke 退出码、finally、`verified_binary` | **撤回阻塞质疑。** [产物校验:89](/Users/xinyue/VSCode/ws_2026/bifrost-console-ui/ee/scripts/console-bootstrap-smoke.py:89) 的缺参数、非法／未完成清单、源码不匹配、二进制不匹配等路径均拒绝。分支探针确认预期基线红灯返回 1，异常返回 2，成功返回 0。初始化阶段错误在进入 finally 前发生，**不生成 result.json**，但 stderr 明确诊断；进入运行阶段后保留结果和日志。本轮真实 Chrome 失败保留了步骤、异常及清理结果，没有伪装成成功。 |
| 指纹是否覆盖软链接和权限变化 | **撤回阻塞质疑，并明确边界。** [文件枚举:27](/Users/xinyue/VSCode/ws_2026/bifrost-console-ui/ee/scripts/console-bootstrap-check.py:27) 拒绝纳入清单的文件自身为软链接；内容变化会改变指纹。实测 `chmod 0600→0700` **不会改变指纹**：[指纹:44](/Users/xinyue/VSCode/ws_2026/bifrost-console-ui/ee/scripts/console-bootstrap-check.py:44) 仅覆盖路径和内容，不证明权限元数据不变。当前差量没有权限变化，构建脚本通过显式解释器执行，未找到该边界导致本候选产物或验收失真的证据。 |

## 5. 转收敛评审 / 挂账

**转收敛评审：**无新增项。

**挂账：**无新增项；既有 FIND-031 夹具归属挂账维持原处置，不重复转交。

## 6. 开放式复读的表外发现

已做开放式复读，无表外发现。

逐文件重新核对了故障识别与恢复：宿主告警关联请求编号；构建失败保留步骤日志；smoke 保留现场并回收自建服务；文档改动未引入新的运行或部署步骤。

## 7. 起手表覆盖

| 信号 | 结果 |
|---|---|
| 验收标准有未覆盖项 | **发现：本会话浏览器亲跑未完成**；另有已核验的最终候选独立浏览器证据 |
| 方案、实施记录、diff 三方不一致 | 无 |
| 多出的功能、公共接口、抽象或依赖 | 无 |
| 偏差未登记 | 无 |
| 失败被吞或降级成假成功 | 无；重启附属状态降级符合批准方案 |
| 空值、空集合缺少分支 | 无；nil／空环境／重启缺省及错误均有覆盖 |
| 取消或超时后仍产生写入、副作用 | 无；接口只读，脚本异常回收自建进程 |
| 重发产生重复副作用 | 无；真实重复读取通过 |
| 事务外写后读、多方回滚不一致 | 不适用；本轮未新增业务写事务 |
| 开关关闭、旧数据、旧客户端无处理 | 无；OSS fallback 和旧配置接口保留 |
| 昂贵检查先于廉价检查、校验缺层 | 无；按批准顺序检查，解码前限长 |
| 路由保护、认证关闭行为 | 无；个人会话仍必需，近似路径未获旁路 |
| 敏感信息进入日志或响应 | 无；白名单及脱敏断言通过 |
| 热路径新增 I/O、无界缓冲或循环 | 无异常；额外会话读取符合方案，无新增轮询 |
| 新配置、环境依赖、部署步骤未登记 | 无 |
| 服务端无法追溯失败原因 | 无；核对身份存储诊断及重启告警出口 |
| 对外泄露内部错误原文 | 无 |
| 脚本失败清理现场 | 无；本轮实际失败现场保留 |
| 新规则或失败路径无测试 | 无新增缺口；补做脚本分支及 HTTP 负例亲核 |
| 断言绑定实现而非行为 | 无阻塞发现；核对状态、权限、字段、缓存和渲染结果 |
| 失败信息无法定位条件 | 无；Chrome 失败定位至步骤 1，产物拒绝原因明确 |
| 返回值、错误未检查 | 无阻塞发现 |
| fixture 依赖个人数据或固定端口 | 无；临时数据、随机端口，依赖路径有显式参数 |
| mock 被写成真实环境结论 | 无；脚本分支模拟、UI 响应注入、真实 HTTP 分开记录 |
| 整体替换模块的 mock | 无不当替代；组件隔离之外有真实 RTK、权限及 HTTP 验证 |
| 调试输出、临时后门、本机配置 | 无 |
| 未使用代码、无关改动 | 无范围内发现 |
| 生成物与源不一致 | 无；2796 文件内容及二进制散列核验通过；权限元数据不属于指纹保证 |

## 8. 验证记录

**版本与实际范围**

- 分支 `feat/console-ui`，HEAD 始终为 `fe626961c86acda99a7fddd394f51f137989cd3b`。
- 已执行 `git status --porcelain=v1 --untracked-files=all`、基线差异统计及上一轮候选至本轮候选的差异核对。起始工作树干净。
- 完整阅读指定五个差量文件。
- 结束时外部会话修改了验证说明（`+9/-1`），新增 `independent-browser-final/` 下 **25 个未跟踪证据文件**：17 份 CLI 输出、结果与截图、来源／清理／断言／结束状态记录、差量及说明。已纳入只读核对；业务源码和索引未变。本会话未修改代码、文档或索引。

**实际执行**

| 命令 | 结果 |
|---|---|
| `python3 -B workbench/tools/build_views.py --check` | 通过，215 篇文档 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/host/... ./internal/identity/http/... ./internal/rbac/host/... ./internal/app/...` | 四包重新执行通过 |
| `PYTHONDONTWRITEBYTECODE=1 GOCACHE="$TMPDIR/go-build-cache" PWTEST_DAEMON_SESSION_DIR="$TMPDIR/playwright-daemon" python3 ee/scripts/console-bootstrap-smoke.py` | 双构建、类型、格式、22 项前端测试及 EE 二进制构建通过；Chrome 在打开页面前退出，smoke 返回 2 |
| `python3 -B - <<'PY' … PY`：脚本分支探针 | 8 项产物拒绝、4 条合法调度路径、内容／软链接／权限边界、5 类退出码与清理模拟符合所述结果 |
| 同上：实际 CLI 初始化失败探针 | 3 项均返回 2，并输出明确原因 |
| 同上：自包含真实 HTTP 核验 | **27 请求通过**；使用本轮新二进制，仅访问本次新建实例 |
| 同上：源码及证据来源核验 | 当前源码、最终候选 Git blobs、外部构建快照共 2796 文件逐一一致；二进制散列匹配 |
| Python AST 差量比较 | 两脚本在端口常量替换后执行结构一致 |
| `git diff --check <基线> -- ee ui product/上游改动/README.md` | 通过 |
| checklist 必存产物、必含符号、禁留标记检查 | 4 产物存在；接口符号命中 18 处；DEBUG 标记 0 处 |

本轮源码指纹：

`99206cf7f5d30e389d1e11393f9641cf9239e391b2a2b0790e4c03130477d901`

本轮新构建二进制 SHA256：

`2f4ead1093a8a252726fa94a2dc64e006126ce51d65048e6df270ce6d7b491fd`

产物全部位于指定 scratch：[构建清单](</Users/xinyue/VSCode/ws_2026/bifrost-console-ui/workbench/tmp/codereview/工作台基础信息解耦-期1-代码评审2/scratch/bifrost-console-check-2ld_w5cs/build/build-manifest.json>)、[smoke 失败结果](</Users/xinyue/VSCode/ws_2026/bifrost-console-ui/workbench/tmp/codereview/工作台基础信息解耦-期1-代码评审2/scratch/bifrost-console-check-2ld_w5cs/output/playwright/result.json>)。自建服务已回收，私有会话文件已删除。

**阅读范围与未执行项**

已读要求的规则、需求验收口径、方案及决策／偏差、findings、两次预飞、验证证据和允许读取的代码评审1；补充追踪转交涉及的 hook、会话查询、测试、身份错误诊断、装配与进程夹具。未读取收敛评审正文或其他旧评审。

checklist 命令没有跳过。未完成本会话候选浏览器流程，也未复跑基线浏览器：Chrome 启动阶段出现 `TargetClosedError`／SIGABRT，尚未访问产品页面；未绕过环境限制。新增独立浏览器证据已核对原始 API、DOM、截图及来源，作为补充证据。未启动 dev server，未连接已有实例，未暂存、提交或推送。

verdict: 通过
