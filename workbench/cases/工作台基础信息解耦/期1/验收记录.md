---
title: 工作台基础信息解耦 期1 验收记录
type: 验收记录
case: 工作台基础信息解耦
phase: 1
created: 2026-09-23
---

## 验收结论

**待用户确认 Gate 2。** 技术实施与验证记录供用户验收，不代表已经获准集成。需求保持“开发中”，方案保持“已实施”。

验收对象：分支 `feat/console-ui`，源码候选 `fe626961c86acda99a7fddd394f51f137989cd3b`；基线 `fa3eacedc28891cc45bc5107c350457cf196e42e`。本文件及最终记录提交只改 Workbench 文档/证据，不改变该源码候选。

EE 二进制 SHA256：`90799d2fb73d375ad7f191cb4f4560261ffa7fa7f5ca08dfc2bfd5cba4e0c604`。2,796 个源码输入的聚合 SHA256：`99206cf7f5d30e389d1e11393f9641cf9239e391b2a2b0790e4c03130477d901`。[完整构建清单](../../../evidence/工作台基础信息解耦-期1-验证/final-candidate/build-manifest.json) 的 `head` 是构建时尚未提交收尾改动的旧 HEAD，验收身份由逐文件内容指纹与最终候选 Git blobs 一致性确定，不能仅凭该旧 HEAD 推断源码版本。

## 交付物核对

| 验收口径 | 实际结果与证据 |
|---|---|
| 只有 RoutingRules.View 的账号可进入路由页面 | 真实 EE、SQLite、个人会话与完整 UI：bootstrap 200、路由列表 200，页面无 config-unreachable。修改前相同场景复现预期失败。见[验证记录](../../../evidence/工作台基础信息解耦-期1-验证.md)。 |
| 完整配置继续受 Settings.View 保护 | 同一账号主动请求 `/api/config` 仍为 403；未授予设置权限，未放宽原路由授权。 |
| 外壳只获取基础状态 | EE hook 请求 `POST /api/console/bootstrap`，只返回连接标记、环境名和可选重启布尔；业务组件、路由规则权限检查未改。 |
| 新端点认证、来源与输入边界 | Go 协议/真实 RBAC 回归通过；[代码评审2](代码评审2.md) 对最终源码新构建的二进制独立执行 27 项真实 HTTP 检查并通过，覆盖认证、来源、严格 JSON、字段与 no-store 边界。 |
| 保留 UI 状态和普通版行为 | EE/OSS 构建和完整类型检查、22 项组件/协议测试通过；加载、错误重试、缓存刷新失败、环境/重启、退出入口及公共/临时页面 skip 有分层验证。 |
| 实际前端改动范围受控 | 仅三个既有 UI 消费方换用 hook，新增窄类型与默认 fallback；EE 实现位于覆盖层。未改 RoutingRules 业务组件或前端权限矩阵。 |

## 验收方式

建议用户轻验收：看上述事实表与[最终页面截图](../../../evidence/工作台基础信息解耦-期1-验证/final-candidate/routing.png)，确认本次只交付“工作台外壳不再要求 Settings.View”。用户尚未选择实际验收方式，未记录为通过。

开发自测：两轮完整预飞通过，[最终预飞2](../../../evidence/工作台基础信息解耦-期1-预飞2.md) 包含全 EE vet、定向 race、双构建/类型检查、22 项前端测试和真实浏览器冒烟。

独立代码评审：[首轮](代码评审1.md)与[差量轮](代码评审2.md)均通过，无修复项或新 nit。差量轮亲跑双构建、22 项前端测试、强制重新执行的 Go race 和 27 项真实 HTTP 检查通过；本会话浏览器启动失败如实保留，独立浏览器补充证据已由代码评审者核验。

独立收敛评审：[收敛评审1](收敛评审1.md) 结论“仅立债挂账”，无阻塞。收尾只有注释/README/同值常量，不因 nit 另开收敛轮。

独立运行态测试：[最终候选独立浏览器验证](../../../evidence/工作台基础信息解耦-期1-验证/independent-browser-final/最终候选独立浏览器验证.md) 对 fe626961 重新执行，退出码 0；独立重算 17 项断言全部通过。2,796 个文件在构建清单、源码快照、候选 Git blobs、工作树之间一致；已亲核截图与 DOM，测试进程/端口和私有会话文件已清理。首轮正式代码评审的浏览器沙箱限制因此由实际独立执行补齐，不把原环境失败改写成通过。

## 两侧评审转交与处置

代码评审1/2均无转收敛条目。收敛评审1的四项转交由启动脚本原文交给代码评审2，以下保留原文及正式差量评审处置；无静默丢弃、未关闭阻塞或重复转交。

> EE 下 `topbar.tsx:136` 改用 `useConsoleAuthEnabled()` 后会无条件触发 `GET /api/session/is-auth-enabled`（原先复用 `getCoreConfig` 缓存，`clientLayout.tsx:61` 对该查询是 `skip: !tempTokenScoped`），请核对每次外壳加载多出的这一次请求是否可接受。

处置：撤回阻塞质疑。额外状态请求符合已批准方案，接口只读，复用同一 RTK 缓存，不轮询；真实请求200。

> `ee/internal/host/consoleprojection_test.go:41,73-77` 在子测试内直接改共享 `a`（`a.host = …`、`a.consoleLog = nil`、`WithConsoleLogger(log)(a)`），`consoleprotocol_test.go:123-129` 亦然；请核对子测试间的状态串扰与 `-race` 下的可信度。

处置：撤回。子测试没有 t.Parallel，相关状态逐例重建/重置，同步调用；强制重跑的四包 race 通过。

> `ee/scripts/console-bootstrap-smoke.py` 的退出码语义（预期基线红灯返回 1、异常返回 2、`finally` 中无条件写 `result.json`）以及 `verified_binary` 各分支的失败路径，请按失败可观测性核对。

处置：撤回阻塞质疑。8项产物拒绝、4条合法调度及失败探针通过；运行阶段保留 result.json 和日志。初始化失败发生在 finally 前，不生成 result.json，但 stderr 明确诊断；基线失败1、异常2、成功0。

> `ee/scripts/console-bootstrap-check.py:185` 的「源码在验证期间被改动即判废」与 `snapshot` 的两次指纹比对是否覆盖了软链接与权限变化，属正确性范畴。

处置：撤回阻塞质疑，保留保证边界。指纹覆盖路径与内容、拒绝文件自身为软链接，但不记录 chmod 权限位；本候选没有权限变化且脚本通过显式解释器执行，无当前失真证据。不得将内容指纹表述成所有文件系统元数据均受保护。

## 建议与遗留

- F1：已补宿主状态投影的来源注释及上游同步核对项。
- F2：[FIND-031](../../../findings.md) 挂账，测试夹具仍位于历史 evidence 目录；当前有明确入口及失败诊断。未来修改夹具启动/场景或第二个案子需要共享夹具时归入 `ee/scripts/`，历史证据保留。不影响产品运行。
- N6/N7/N8/N9：已补降级说明、保留端口常量、脚本入口和本机便利路径说明。
- N1：保留现有测试 helper，避免扩大既有测试变更；N2/N4：保留已批准的 hook 路径与名称；N3：保留明确的 request_id 和单处日志；N5：保留独立窄类型。均是不阻塞的组织偏好，详细依据见[方案处置记录](方案v1.md#首轮交付评审处置)。

## 验证边界

- 完整前端 RBAC、导航/按钮筛选、登录/建议改密、通知与 WS 生命周期仍归原“控制台登录与权限接入”案。本页可达不代表账号可执行页面所有操作；原后端仍最终授权。
- 503、无存储、仅日志及重启提示是明确标记的响应注入，只证明 UI 分支；成功进入路由页使用真实 EE 接口，无成功响应 mock。
- 公共页与无效临时令牌入口只验证“不请求 bootstrap”，未覆盖有效临时令牌的完整 MCP 认证流程；退出入口只核对存在，未将其写成完整退出行为测试。
- 源码指纹是路径/内容一致性，不覆盖权限元数据；正式评审已对该边界做探针并确认本候选无权限变化。
- 用户原 59817 服务未改动；测试使用独立临时账号、数据目录和随机端口。浏览器与自建 EE 服务完成后清理。

## 度量（标准级以上）

| 阶段 | 模型 | 起止时刻 | token |
|---|---|---|---|
| 计划 | 缺失 | 缺失 | 缺失 |
| 代码评审1 | gpt-6-astra / high | 2026-09-23T02:57:00+08:00 — 2026-09-23T03:10:10+08:00 | 输入 7426121（缓存 7216640），输出 15918 |
| 收敛评审1 | claude-opus-5 / high | 2026-09-23T02:57:03+08:00 — 2026-09-23T03:11:48+08:00 | 输入 150，缓存创建 260073，缓存读取 12168692，输出 63801 |
| 代码评审2 | gpt-6-astra / high | 2026-09-23T03:24:38+08:00 — 2026-09-23T03:34:39+08:00 | 输入 3158970（缓存 3006208），输出 15326 |
| 开发 | 缺失 | 缺失 | 缺失 |
| Gate | 用户决定 | 待用户确认 | 不适用 |

缺失数据不估编；评审时间来自各轮启动器 meta.json，时区 Asia/Shanghai。token 为服务原始计数，各服务缓存口径不同，不直接相加当作独立上下文或费用；[度量原始计数](../../../evidence/工作台基础信息解耦-期1-验证/review-measurements.json)。

## 归档动作

目前未推送、未合并、未发布，未归档。用户通过 Gate 2 后，按本仓集成流程确认集成对象仍为该候选，集成后最小冒烟；再将方案置为“已归档”、需求置为“已交付”。若不通过，回到本案修复、形成新候选并对受影响范围复测/差量评审。

长期口径已经回填 EE README、宿主包 README、接口文档和上游改动登记；本案不改 AGENTS。完整登录/权限原案继续保持草拟状态。

依据：[交付规范](../../../规范/流程/交付.md)：“没有明确通过 Gate 2,不得把‘代码已完成’‘分支已推送’‘MR 已创建’解释为获准集成。”
