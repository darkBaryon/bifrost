---
title: 账号认证 期1 收敛评审 #6
type: 收敛评审
case: 账号认证
phase: 1
round: 6
verdict: 无碍
created: 2026-09-12
---

> 评审者与实施上下文隔离（未参与实施，未读旧代码评审意见；按差量复核规则只读了收敛评审5 的「本轮修复项」及代码评审4 原样转来的「转收敛评审」两条）。基线 `3e15a640d89902772445d3fb4c04023e58828eba`；本轮为差量复核：上一轮候选 `4cdab985d` → 评审候选 `8527acc604e6676801cfdfe38d0da993403b1c6f` → 修复后最终候选 `a8666448d040cfde8491fceb37a920a537530793`（`git status --porcelain` 为空，无未跟踪文件，交付物即 HEAD）。
> L3：起手表逐行执行，另做开放式复读；范围为差量文件的完整现状，不重做整包。

## 结论

上一轮六项修复全部在代码里核到落实：EE 自有代码不再出现标准库 `log`（`git grep '"log"' a8666448d -- ee/` 无结果），日志经 `main.go` 已配置的 `schemas.Logger` 注入，`persistence`/`bifrost` 各自按需要定义了只含 `Error`/`Info` 的窄接口；`app.Bootstrap` 的工厂失败时返回真正的 nil 接口并在上游 Bootstrap 之后校验适配器存在；品牌迁移重试常量命名并与身份侧互相注明；`/ws` 精确 Origin 有了注释；stdin 上限从源常量推导；`Repository` 四个读方法交代了"零值 + ErrNotFound"。四个取舍项按方案记录处置（A/C/D 采纳、B 撤回登记 FIND-017），代码评审4 转来的两条也已关闭。按新增的行数规则做的四处拆分（service→service/accounts/passwords、handler→handler/protocol、store→store/rows、auth→auth/config）经归一化行集比对确认为纯搬动，唯一的非搬动差异就是本轮的 logger 注入与 gofmt 对齐。本轮提出的唯一修复项——两份规则清单在 logger 一条上分叉——已由 `a8666448d` 关闭，亲核依据见下方「本轮修复项」。至此本轮无未关闭的修复项、无需用户裁决事项、无新增立债或挂账，架构与实现质量适合交付，给出「无碍」。

## 事实来源

**跑过的命令**：`git status --porcelain`（两次，均为空）、`git rev-parse HEAD`、`git diff --stat 4cdab985d..8527acc60`、`git log --oneline 4cdab985d..8527acc60`（单提交）、`git diff 4cdab985d..8527acc60 -- <各文件>`、归一化行集比对（对 service/handler/store/auth 四组拆分，把旧文件与新文件集合去掉注释/空行/import 后 `sort` 再 `diff`，核实搬动是否纯净）、`/opt/homebrew/bin/gofmt -l ee/internal ee/cmd`（输出为空）、`git grep -n '^\s*"log"$' 8527acc60 -- ee/` 与 `git grep -n 'log\.SetOutput\|log\.Print' 8527acc60 -- ee/`（均无结果）；定向复核轮另跑 `git log --oneline 8527acc60..a8666448d`、`git diff --stat 8527acc60..a8666448d`、`git diff 8527acc60..a8666448d -- ee/AGENTS.md 编码规范.md 方案v2.md`、`git diff --stat 8527acc60..a8666448d -- ee/internal ee/cmd ee/scripts ee/Makefile ee/README.md ee/go.mod`（为空）、`git grep -n '^\s*"log"$' a8666448d -- ee/`（无结果）、若干 grep（`ConsoleAuthFactory`/`consoleAuth == nil`、`BIFROST_SETUP_TOKEN`、`/api/session/login`、`IDENTITY_TEST_POSTGRES_DSN`、`按缺口扩展`、`func Test`）。未运行构建、`go vet`、测试或服务（会写缓存/文件）。

**完整读过的范围内文件**（41 个差量文件中的 ee 部分，逐个读完整现状）：`ee/AGENTS.md`（修复后重读 5-19 行）、`ee/Makefile`、`ee/README.md`、`ee/cmd/bifrost-http/main.go`（95-120 区段，其余为上游抄本未变）、`ee/internal/app/{README.md,bootstrap.go,identity.go,recovery.go}`、`ee/internal/bifrost/{README.md,auth.go,auth_test.go(diff+330-387),config.go}`、`ee/internal/branding/persistence/{README.md,migration.go}`、`ee/internal/identity/{README.md,accounts.go,model.go,passwords.go,service.go}`、`ee/internal/identity/http/{README.md,handler.go,protocol.go}`、`ee/internal/identity/persistence/{README.md,diagnostics.go,diagnostics_test.go,migration.go,rows.go,service_test.go(1-80 与 diff),store.go}`、`ee/scripts/branding-smoke.py`。workbench 侧差量（`findings.md`、方案v2 新增段落、生成视图、编码规范修订）按提示只作输入与对账依据，不评。

**读过的同类实现与上下文**（判断惯例与核对契约的依据）：`ee/internal/identity/http/dto.go`（未变，核对 protocol.go 拆出后 DTO 引用）、`ee/internal/identity/http/handler_test.go`（未变，核对 README 归属）、`ee/scripts/identity-smoke.py:1-140,286-300`（`Node` 夹具与 CLI 参数，核对品牌冒烟复用与 Makefile 契约）、`ee/scripts/identity-upgrade-smoke.py:61-63`（旧登录路径覆盖）、`ee/internal/branding/persistence/{store.go,table.go}` 头部（包注释排列惯例）、`core/schemas/logger.go:28-44`（`schemas.Logger` 的 `Info/Warn/Error(msg string, args ...any)`）、`transports/bifrost-http/server/server.go:249-251,2746-2768`（工厂调用与 nil 判定）、`transports/bifrost-http/lib/config.go:5268`（setup token 读取方式）、`product/架构/EE后端架构.md` 全文（§7 第 113 行「按缺口扩展」含日志）、`workbench/规范/项目/编码规范.md` 全文（修复后重读硬规则一节）。

**核实过的关键假设**：
- 上游仍无条件调用工厂并在返回 nil 接口时报错：`server.go:2746-2750` `if s.ConsoleAuthFactory != nil { s.consoleAuth, err = s.ConsoleAuthFactory(ctx, s); if err == nil && s.consoleAuth == nil { err = ... } }`。因此 `bootstrap.go:23-30` 的"失败返回 `nil, err`"让宿主判定生效，`bootstrap.go:35-37` 的启动期校验是对上游控制流变化的兜底而非重复守卫。
- 宿主 logger 满足两个窄接口：`schemas.Logger` 含 `Info(msg string, args ...any)` 与 `Error(msg string, args ...any)`（`logger.go:36,44`），与 `persistence.Logger`（`diagnostics.go:18-20`）、`bifrost.Logger`（`auth.go:38-40`）签名一致；`main.go:104-113` 在 `SetOutputType/SetLevel` 之后才把同一个 `logger` 传给 `eeApp.Bootstrap`。
- 品牌冒烟改走 `Node` 后设置 `BIFROST_SETUP_TOKEN=''`（`identity-smoke.py:56`，`Fixture` 传空串）与原先不设该变量等价：上游用 `os.Getenv` 读取（`lib/config.go:5268`），空值与未设不可区分。
- 四处拆分为纯搬动：归一化行集比对下，service 拆分零差异；handler 拆分只有 const 块 gofmt 对齐差异；store 拆分只有 `Store.log` 字段、`NewStore` 签名与 `logDatabaseFailure(s.log, ...)` 传参差异；auth 拆分只有 `log.Print→log.Info`、`Logger` 接口与 `ImportLegacyAdministrator` 签名差异——全部对应本轮修复项，无夹带。
- 旧登录路径覆盖未因品牌冒烟改用 `/api/identity/login`（`identity-smoke.py:127`）而消失：`auth_test.go:356` 与 `identity-upgrade-smoke.py:61,63` 仍打 `/api/session/login`。
- 修复提交未触碰任何代码：`git diff --stat 8527acc60..a8666448d -- ee/internal ee/cmd ee/scripts ee/Makefile ee/README.md ee/go.mod` 输出为空，八个改动文件中除 `ee/AGENTS.md`、`编码规范.md`、`方案v2.md` 外全是生成视图（`SUMMARY.md`、`nav.js`、`cases/账号认证/index.md`、`views/all.md`）与本记录落盘。

**起手表覆盖**（每行一个结果）：

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无（`busyRetries/busyRetryStep` 两份已命名并互相注明，属 FIND-017；`maxPasswordLineBytes` 从 `MaxPasswordBytes` 推导；`expires_in`、`Retry-After` 仍从源常量算） |
| | 同一规则多份实现且互不注明 | 发现并已关闭（本轮修复项：`ee/AGENTS.md` 与编码规范两份规则清单在 logger 一条上分叉，`a8666448d` 已对齐）；`sqliteBusy` 两份属 FIND-017 已挂账 |
| | 重造已有 helper | 无（日志改用宿主 logger；品牌冒烟改复用 `identity_smoke.Node`，删掉了自己那份进程夹具，`branding-smoke.py:38-60` 只剩配置与断言） |
| | 派生常量无推导 | 无（`recovery.go:24` 写出 `MaxPasswordBytes + len("\r\n")`） |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无（差量内新增数字仅 `busyRetries=3`/`busyRetryStep=20ms`，均命名并注明） |
| | 状态码、错误码裸数字 | 无（`protocol.go` 沿用 `fasthttp.Status*`；测试与脚本内数字按契约独立书写） |
| | 无名的错误形状 | 无 |
| | 紧凑但绕的控制流 | 无新增（`bootstrap.go:23-30` 闭包只赋值一次并返回；`store.go` 的 `phase` 闭包为上一轮已判定保留项，未变） |
| | 文件集中或拆分是否妨碍理解 | 无（四处拆分逐一核实，见「看似可改但有理由保留」；每个新文件头部都写明职责与兄弟文件去向，`identity/README.md:8-10`、`http/README.md:7-8`、`persistence/README.md:7-10`、`bifrost/README.md:7-8` 同步） |
| | 多余状态或守卫 | 无（`attach` 的不可达 nil 守卫已删，`bootstrap.go:35` 的新守卫防的是上游控制流变化，与宿主 `server.go:2748` 防的对象不同） |
| | 同一效果做两次 | 无 |
| | 注释与现状矛盾 | 无（`config.go:45-47` 投影注释已改为"当前调用者，暂行策略下即主管理员"并写明 RBAC 替换时的改法；`auth.go:205-206` `/ws` 收紧注明；各 README 文件表与实际文件一一对应） |
| 架构与项目一致性 | 接缝形状与同类不同 | 无（`MigrateIdentity(log)` 返回闭包而 `MigrateBranding` 为普通函数，是因身份迁移需要诊断 logger，二者都以 `func(ctx, *gorm.DB) error` 交给 `ConfigStore.RunMigration`，接缝形状一致） |
| | **架构符合性** | 无（见下节） |
| | **依赖关系** | 无（见下节） |
| | 绕过既有出口 | 无（上一轮的日志出口偏离已关闭；存储/迁移/响应/鉴权仍走既有机制） |
| | 把上游现状当契约 | 无 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 发现并已关闭（同上，属开发指南两份清单不同步；HTTP 契约本轮未动，`dto.go`/`handler.go`/`protocol.go` 与 `ee/docs/账号认证接口.md` 的对应关系未受拆分影响） |
| | 状态码口径不一 | 无（`Error()` 映射整段搬入 `protocol.go:107-126`，未改） |
| | 省略、空、值三态语义不对应 | 无（`limit *int`/`next_cursor` 逻辑在 `dto.go` 未动） |

**开放式复读结果**：已做。以"半年后要改这里的人"的身份逐文件再读一遍：`service_test.go` 481 行是第一眼会皱眉的地方——离硬上限 500 行只差 19 行，下一个用例进来就得拆，但现在拆没有阅读收益（判为有理由保留并写明触发条件）；`branding-smoke.py` 现在把端口绑定在 `Node.__init__` 一次、重启复用同一端口，与旧 `Fixture` 每次 `start()` 重新取端口不同，属测试可靠性，转代码评审；其余（`store.go` 包注释排列、两个同名 `Logger` 窄接口、`recovery.go` 自建 error 级 logger）复核后均为有理由保留。无其他表外发现。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 1 条，已关闭：ee 规则清单与编码规范硬规则在 logger 一条上分叉 | 本轮修复项 |
| 职责与实现质量 | 无 | 拆分纯净、README 同步、守卫对象各异，见起手表 |
| 架构与项目一致性 | 无 | 见下方两段专项核对 |
| 契约一致性 | 同上一条（开发指南），已关闭 | HTTP 契约未动 |

**架构符合性**（对照 [EE 后端架构](../../../../product/架构/EE后端架构.md)）：
- §4 模块内部职责 / §5 `app` 只做装配：本轮改动没有把业务挪进 Handler 或 app。`app/bootstrap.go:19-39` 仍只做"注入工厂 → 上游 Bootstrap → 校验 → attach"，新增的 logger 参数只是把入口已有对象向下传递（`identity.go:49-50,70,73,77`）；`app/recovery.go` 仍复用 `identityOptions`/`newIdentityService`，只多传一个 logger。
- §4 文件粒度按编码规范「文件组织以阅读为先」核实而非按示例清单：`identity/` 由 model+service 变为 model+service+accounts+passwords，拆分线是"会话规则 / 账号操作 / 密码三条路径"，与 README:14 描述的规则归属一致，一次改密要读的东西（`ChangePassword`→`replacePassword`→`current`）在 `passwords.go` 内加 `service.go` 的两个共享辅助，跨文件跳转只增加一次；`http/` 把协议管道（origin、Cookie、JSON、错误映射）拆到 `protocol.go`，端点文件只剩路由表与 12 个三步端点，读一个端点不再被 60 行 JSON 校验器打断；`persistence/` 把六张表的行结构拆到 `rows.go`，`store.go` 留下读取/事务/限流/分页；`bifrost/` 把 `/api/config` 投影与预检拆到 `config.go`，`auth.go` 只剩导入与中间件。四处都没有出现"只剩声明或转发"的文件，判定拆分降低了阅读成本。
- §7「按缺口扩展」（架构文档第 113 行明列日志为须先评估复用的既有能力）：上一轮唯一的架构偏离（日志另起通道）已关闭——EE 不再持有第二个日志出口，`persistence.Logger`/`bifrost.Logger` 只声明各自实际用到的一个方法；修复提交进一步把这条约束写进编码规范硬规则并回引本节。
- §1/§7 与 Bifrost 边界：宿主接缝未变（仍是 `ConsoleAuthFactory` + `ConsoleAuthProvider`），本轮未触碰任何上游文件。

**依赖关系**（对照架构 §5、§7 与编码规范「依赖关系」）：
- 方向：`identity/{model,service,accounts,passwords}.go` 的 import 仍全部是标准库；`persistence` → `identity` + gorm/驱动/`framework/encrypt`；`http` → `identity` + fasthttp/router/schemas/lib；`bifrost` → `identity`、`identity/http` + 宿主包；`app` → 以上全部 + `schemas`。拆分后的新文件没有引入新的包级依赖，`accounts.go`/`passwords.go` 与 `service.go` 互相只用包内符号。
- 窄接口与显式注入：logger 由使用方定义（`persistence.Logger` 只要 `Error`，`bifrost.Logger` 只要 `Info`），通过 `NewStore(db, log)`、`MigrateIdentity(log)`、`ImportLegacyAdministrator(..., log)`、`app.Bootstrap(ctx, s, log)` 构造/调用时显式传入；没有全局 logger 查找，测试用 `recordedLogs`/`quietLogger` 注入假实现（`diagnostics_test.go:20-30`、`auth_test.go:29-34`），不再劫持进程级 `log.SetOutput`。
- 资源所有权：宿主 logger 由 `main.go` 创建并配置，EE 只持有引用；离线恢复自建 error 级 logger（`recovery.go:63`）并自行 `defer store.Close`，与在线路径互不干扰。数据库连接所有权未变。

**契约一致性**：HTTP 契约本轮零改动（`dto.go`、`handler_test.go`、`ee/docs/*.md` 均不在差量内）。运维契约三方一致：`Makefile:42-47` 的 `--database sqlite|postgres` 与 `IDENTITY_TEST_POSTGRES_DSN` ↔ `identity-smoke.py:292,302-304` ↔ `ee/README.md:34-35,40`。开发指南两份清单在 `a8666448d` 后一致，见本轮修复项。

## 上一轮修复项与转交处置

**收敛评审5「本轮修复项」逐条**：

1. 标准库 `log` 绕开宿主 logger —— **已修复**。`persistence/diagnostics.go:17-23,47` 改为注入 `Logger` 并 `log.Error(...)`；`bifrost/auth.go:37-44,75,77` 改为 `Logger.Info`；`app/bootstrap.go:19` 新增 `log schemas.Logger` 参数，`main.go:113` 传入已配置的 `logger`；`diagnostics_test.go:20-30` 改为注入 `recordedLogs`。候选上 `ee/` 无任何 `"log"` import、`log.Print`、`log.SetOutput`。
2. `app.Bootstrap` 先解引用再校验、typed nil —— **已修复**。`bootstrap.go:24-29` 失败分支 `return nil, err`，成功才 `auth = adapter`；`:35-37` 在上游 Bootstrap 后校验 `auth == nil` 并报明确错误；`attach` 的不可达守卫已删（`:43-50`）。
3. 品牌迁移重试裸字面量 —— **已修复**。`branding/persistence/migration.go:22-27` 定义 `busyRetries`/`busyRetryStep` 并注明与 identity `retryBusy` 同一规则、须同步；`README.md:5` 改为指向常量。
4. 来源校验三处形状不同、`/ws` 未注明 —— **已修复**（采用注释方案）。`auth.go:205-206` 写明"浏览器握手必然带 Origin，只接受精确 Origin，不像 SameOrigin 接受 Referer 兜底（方案 4.5）"。
5. stdin 上限 1025 无推导 —— **已修复**。`recovery.go:23-24` `maxPasswordLineBytes = int64(identity.MaxPasswordBytes + len("\r\n"))`，注释交代"最长可接受密码 + 行尾 CRLF，更长的读到上限即止随后被密码规则拒绝"。数值由 1025 变为 74，是有意收紧且正确：恢复设置的是新密码，上限应是 `MaxPasswordBytes` 而非登录输入的 1024。
6. `Repository` 读方法无注释、零值语义未交代 —— **已修复**。`model.go:209-226` 接口注释写明"未找到返回 ErrNotFound，返回值为零值，调用方不得使用；存储故障原样返回由服务折叠"，四个读方法各补一行。

**收敛评审5 取舍项**：A（品牌冒烟复用进程夹具）已采纳，`branding-smoke.py:22-24,38-60` 以 `Fixture(identity_smoke.Node)` 子类只保留旧管理员配置与品牌断言；其中"顺带对齐 `identity-smoke.py` 无空格赋值"未做（`identity-smoke.py:290-298` 仍为 `p=argparse...`），该文件不在本轮差量、且为纯风格，不重提。B（提取共享 busy 重试）撤回并登记 FIND-017，两侧注释互相指向。C（投影注释与 RBAC 提示）已采纳，`config.go:45-47`。D（`make smoke` 接入身份冒烟）已采纳，`Makefile:42-47`、`README.md:34-35,40`。

**代码评审4「转收敛评审」逐条**：

- README 把路由契约测试归到 `handler_test.go` —— **修复（关闭）**。`http/README.md:10` 现在把 `handler_test.go` 描述为"严格 JSON 限制、错误映射与序列化降级契约"，与该文件实际的 `TestErrorResponseContract`/`TestJSONFailureDoesNotExposeValue`/`TestStrictJSONBoundaries`（`handler_test.go:14,51,59`）一致；`README.md:12` 把精确路由归属与来源拒绝指向 `../../bifrost/auth_test.go` 的 `TestIdentityRouteContract`（`auth_test.go:336`）。文档质量结论：关闭。
- gofmt 空行整理 —— **修复（关闭）**。`gofmt -l ee/internal ee/cmd` 在候选上输出为空；对方"不据格式推断行为"的立场无需本侧处置，其余质量判定由本记录独立给出，未继承旧「无碍」。

## 出口清单

### 本轮修复项

1. **`ee/AGENTS.md` 的规则清单与编码规范硬规则在 logger 一条上分叉 —— 已修复并关闭（`a8666448d`）。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/AGENTS.md:7,15-16`；`/Users/xinyue/VSCode/ws_2026/bifrost-auth/workbench/规范/项目/编码规范.md:27-38`（硬规则一节）
   - 原代码事实：`AGENTS.md:7` 写"实施与评审都按下列规则逐条核对（详见编码规范「硬规则」一节）"，`:15`（候选 `8527acc60`）把"文件超过 300 行…"与"日志走宿主注入的 logger，不用标准库 `log`"并列在同一行；而同一提交对编码规范的修订只加了行数一条，硬规则一节没有 logger 条目，全文 grep `logger`/`标准库` 均无结果。
   - 原维护影响：两份规则清单在引入这条规则的同一提交上就不一致。编码规范硬规则明写"在收敛评审中按条核对，违反即为本轮修复项"，评审提示词让评审者读的是编码规范而不是 `ee/AGENTS.md`；下一个用标准库 `log` 的改动会通过按硬规则清单核对的评审，而实施方按 AGENTS.md 又认为它是硬规则——修复项 1 关闭的问题失去了机械核对的依据。
   - 修复结果与亲核依据（只读 `git diff 8527acc60..a8666448d`，并重读两份文件现状）：
     - `编码规范.md:37` 在硬规则一节新增「**日志经宿主注入的 logger 输出。** `ee/` 内不使用标准库 `log`；需要日志的包按实际用到的方法定义窄接口，由 `app` 把入口配置好的 logger 注入（架构 §7「按缺口扩展」）」，位置在「依赖显式注入」之后、「文件超过 300 行」之前，落在"违反即为本轮修复项"的同一列表内，机械核对依据成立。
     - 引用属实：`product/架构/EE后端架构.md:113`「**按缺口扩展**：先评估已有数据库、迁移、**日志**、向量库、插件与 Sidekiq，再增加实现」，该条正是上一轮判定日志偏离时引用的章节，回引未造假。
     - 条文与代码现状一致：新增条文的两个要件（不用标准库 `log`、按实际用到的方法定义窄接口）分别由 `git grep '"log"' a8666448d -- ee/` 无结果、`persistence.Logger` 只声明 `Error`（`diagnostics.go:18-20`）与 `bifrost.Logger` 只声明 `Info`（`auth.go:38-40`）佐证，规范不是写给一个不存在的实现看的。
     - `ee/AGENTS.md:15-16` 把原先并列的一行拆为两条，日志条在前、行数条在后，与编码规范硬规则的新顺序一致；日志条补上了"按实际用到的方法定义窄接口"，行数条补上了"拆分只搬动代码，不改行为"——两条的措辞现在都是编码规范对应条目的准确缩写，逐字核对无第三种口径。
     - 修复未夹带代码改动：`git diff --stat 8527acc60..a8666448d -- ee/internal ee/cmd ee/scripts ee/Makefile ee/README.md ee/go.mod` 输出为空；提交内其余文件为 `方案v2.md` 的本轮处置记录与四个生成视图（`SUMMARY.md`、`nav.js`、`cases/账号认证/index.md`、`views/all.md`），均与本记录落盘对应。生成视图里 `收敛评审6` 仍显示 `有Blocking`，是记录被本轮结论覆盖前的状态，覆盖后重跑 `build_views.py` 即随 frontmatter 更新，不属未关闭项。
   - 结论：关闭。本轮无未关闭修复项。

### 需用户裁决的 Blocking

- 无。

### 立债(REF:立 `triggered_by: [<本case>]` 的需求进 reports/)

- 无。

### 挂账(Finding:登记 findings.md,带四要件)

- 无新增。FIND-017 已由上一轮登记，本轮核实两侧注释互相指向，不再重复。

### 转代码评审(范围外发现,一行一条,不出结论)

- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/scripts/branding-smoke.py:38-49` 改继承 `Node` 后，端口在 `identity-smoke.py:47-50` 的构造函数里绑定一次，品牌冒烟的两次 `stop()/start()` 重启（`branding-smoke.py:106-107,114-115`）复用同一端口，旧 `Fixture` 是每次 `start()` 重新取随机端口。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/scripts/branding-smoke.py:56-57` 的 `login()` 现经 `Node.login` 打 `/api/identity/login`（原为 `/api/session/login`），旧路径的冒烟覆盖只剩 `identity-upgrade-smoke.py:61,63` 与 `auth_test.go:356`。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/app/recovery.go:24,88` stdin 读取上限由 1025 改为 74 字节，73～1024 字节的输入现在会被截断后再由 `validPassword` 拒绝，而不是整行读入后拒绝。

## 看似可改但有理由保留

- **`service.go` 321 行、`store.go` 322 行拆分后仍超过 300 行的评估线**：已评估。`service.go` 剩下的是构造、随机数/摘要/哈希槽等共享辅助与会话规则（`current`/`fullSession`/`authorize`/`manager`），`accounts.go`/`passwords.go` 全部依赖这些辅助，再拆会让每个业务文件都要回头跨文件读辅助；`store.go` 的读取方法、`transaction` 类型、`Transaction`/`ReserveLogin`/分页都经 `s.transaction` 与 `logDatabaseFailure(s.log, ...)` 串在一起，按"读/写"再拆只会把 `transaction` 的方法与其调用者分开。均未到 500 行硬线，保持集中。
- **`service_test.go` 481 行、`auth_test.go` 387 行（硬规则"测试文件同样适用"）**：已评估。`service_test.go` 的用例共享 `testStore`/`newService`/`fixture`（`:25-80`），现在拆没有阅读收益；但它离 500 行硬线只差 19 行，**下一次往里加用例时须先按主题拆（生命周期与回滚 / 初始化竞争 / 幂等与撤销），把 fixture 留在一处**。`auth_test.go` 七个用例各自独立、共享一个 `testAdapter`，同理保留。写在这里让下一轮不必再争。
- **`persistence.Logger` 与 `bifrost.Logger` 两个同名窄接口而非共享一个**：这是编码规范"接口由使用方按实际需要定义"的直接结果——存储只需 `Error`，宿主接入只需 `Info`；共享一个就得把两方都不用的方法塞进去，或者新建一个只放接口的包。修复提交新增的硬规则条文也明写"按实际用到的方法定义窄接口"，与此一致。保留。
- **`persistence/store.go:1-4` 先写文件职责、空行后再写 `Package persistence` 句**：与 `branding/persistence/store.go:1-4` 排列一致，是两个 persistence 包的既有惯例；`identity`/`identityhttp`/`bifrost`/`app` 则把 `Package` 句放第一行。两种排列并存是历史现状，本轮只改了第一行文字，不因此要求统一。
- **`MigrateIdentity(log)` 返回闭包而 `MigrateBranding` 是普通函数**：身份迁移要在耗尽重试后记一行安全诊断（`migration.go:24`），品牌迁移不记日志；两者交给 `RunMigration` 的形状相同。不为对称而给品牌迁移也加 logger。
- **`recovery.go:63` 离线命令自建 `NewDefaultLogger(LogLevelError)`**：CLI 没有 `-log-level/-log-style` 参数，用 error 级默认 logger 既让存储诊断仍能输出，又不在终端刷 info；该行在上一轮已存在，本轮只是同时把它喂给身份存储。与新增硬规则不冲突——它注入的仍是宿主 logger 实现，不是标准库 `log`。保留。
- **`diagnostics_test.go:27` 用 `store.log.(*recordedLogs)` 断言取回记录器**：包内测试直接读未导出字段，避免再给 `Store` 加只为测试存在的导出方法；所有夹具都注入 `recordedLogs`，断言不会失败。保留。
- **`branding/persistence/migration.go:47-50` 的 `sqliteBusy` 与身份侧同名同体**：属 FIND-017 的一部分（同一条驱动兼容规则两份、已互相注明），不单独重提。

## 台账对账

- **FIND-012（`attach` 惯性点）**：本轮 `bootstrap.go` 反而减了 3 行守卫、`attach` 仍是三步薄装配；身份装配继续独立在 `identity.go`。未恶化。
- **FIND-013 / FIND-015**：已在「EE 现有代码架构迁移」关账；本轮品牌冒烟改复用 `Node` 后仍自建临时目录/随机端口/只清理自有进程（`identity-smoke.py:37-50,84-93`），符合原整改口径。
- **FIND-014（`ee` main.go 是上游抄本）**：本轮 `main.go` 只改了同一个已知 hunk 的一行（`:113` 多传 `logger`），差异点数量不变；修复提交未再触碰。未恶化。
- **FIND-016**：未触及。
- **FIND-017（busy 重试两份）**：本轮登记；核实 `branding/persistence/migration.go:22-23` 与 `identity/persistence/diagnostics.go:57` 双向注明，`README.md:5` 指向常量。按 `convert_when` 观察。
- 方案「Gate 决策结果」「实施偏差记录」「用户复核：整体收敛纠偏」已裁决事项（默认密码、会话规则、后端先行、`Service.Account` 删除、`limit` 显式 0 报 400 等）本轮均未重提。
