---
title: 账号认证 期1 收敛评审 #5
type: 收敛评审
case: 账号认证
phase: 1
round: 5
verdict: 有Blocking
created: 2026-09-12
---

> 评审者与实施上下文隔离（未参与本次实施，未读旧代码评审/收敛评审意见）。基线 `3e15a640d89902772445d3fb4c04023e58828eba` 到 `4cdab985de96ed02624a09f07c26e46dba9ebd70`（`git status --porcelain` 为空，无未跟踪文件，交付物即 HEAD）。
> L3：逐维留痕，起手表逐行执行，另做开放式复读。

## 结论

这一轮内部重写是明显的质量跃升：`identity` 根包只依赖标准库，业务类型/DTO/数据库行三套结构各司其职并显式转换，对外枚举与业务限制全部收敛为命名常量且派生值写了推导（`tokenLength`、`expires_in`、`MaxSessionTTL`），装配从 `internal/bifrost` 移到 `internal/app` 后依赖方向单向清晰，包 README 与代码一致，会话重验（`current`）、受限会话（`fullSession`）、改密尾部（`replacePassword`）各只有一份实现——方案「用户复核：整体收敛纠偏」列出的七类问题在代码里都能核到落实。剩下的是六处有维护代价的收口没做完：EE 新代码用标准库 `log` 绕开了宿主已注入并按 `-log-level/-log-style` 配置的 logger；`app.Bootstrap` 先解引用适配器再校验、且把具体指针类型塞进接口从而废掉了宿主的 nil-provider 守卫；SQLite busy 重试同一条规则在 branding 与 identity 各写一份且 branding 侧是裸字面量；来源校验规则散在三处形状不同且 `/ws` 的收紧没有注明；`recovery.go` 的 stdin 上限是无推导的裸数字；`Store` 六个导出读方法无注释、"未找到返回零值+ErrNotFound" 的语义没有在 `Repository` 侧交代。这些都在授权范围内、无需用户裁决，修复后即可给「无碍」；在此之前按规范沿用 `有Blocking`。

## 事实来源

**跑过的命令**：`git status --porcelain=v1 -uall`（空）、`git rev-parse HEAD`、`git diff --stat <基线> HEAD`、`git diff <基线> HEAD -- <各文件>`、`git grep -n '"log"' <基线> -- ee/`（无结果）与同一命令在 HEAD（3 处命中）、若干 grep（`"/ws"`/`"Authorization"`/`X-Bifrost-Temp-Token`/`x-request-id`/`smoke`/`SetLogger`）。未运行任何构建、测试或服务。

**完整读过的范围内文件**：`ee/AGENTS.md`、`ee/README.md`、`ee/cmd/bifrost-http/{README.md,main.go}`、`ee/docs/账号认证接口.md`、`ee/docs/账号认证升级与恢复.md`、`ee/internal/app/{README.md,bootstrap.go,identity.go,recovery.go}`、`ee/internal/bifrost/{README.md,auth.go,auth_test.go}`、`ee/internal/branding/persistence/{README.md,migration.go}`、`ee/internal/identity/{README.md,model.go,service.go}`、`ee/internal/identity/http/{README.md,dto.go,handler.go,handler_test.go}`、`ee/internal/identity/persistence/{README.md,store.go,migration.go,diagnostics.go,diagnostics_test.go,service_test.go}`、`ee/scripts/{branding-smoke.py,identity-smoke.py,identity-upgrade-smoke.py}`、`product/架构/EE后端架构.md`、`product/需求/账号认证/方案讨论.md`、`transports/bifrost-http/{server/server.go(2536-2800 区段),handlers/middlewares.go(diff),handlers/websocket.go(diff),handlers/wslogtarget_test.go}`。`ee/go.mod`、`ee/internal/branding/persistence/migration_test.go`、`product/README.md`、`product/需求/管理操作审计/产品调研.md`、`product/需求/角色权限/产品方案.md` 只读 diff（改动为增量段落/依赖升格）。`product/需求/账号认证/竞品调研.md` 按提示作为原调研不评。

**读过的同类实现（判断惯例的依据）**：`transports/bifrost-http/handlers/session.go:34-215`（旧会话路由的响应形状、旧 `token` Cookie 的 Path/HttpOnly，用于核对兼容契约）、`transports/bifrost-http/handlers/middlewares.go:165`（访问日志读取 `x-request-id` 请求头）、`transports/bifrost-http/lib/lib.go:11-16` 与 `handlers/init.go:6-10`、`server/server.go:291-296`（宿主 logger 的注入方式）、`ee/internal/branding/persistence/migration.go` 与 `ee/internal/branding/http/handler.go:22-24`（既有 EE 模块的迁移与路由注册惯例）、`ee/Makefile:42-43`（既有冒烟入口）。

**核实过的关键假设**（结论依赖上游行为的部分）：
- 宿主一定调用 `ConsoleAuthFactory`：`transports/bifrost-http/server/server.go:2746-2766` 在 `Bootstrap`（2536-2944，唯一顶层 `return nil` 在 2944）中无条件执行，且 factory 报错时清理 `WSTicketStore`/`TempTokenSweepWorker`/`OAuth2SweepWorker` 后返回错误。故 `app.Bootstrap` 走到 `attach` 时 `auth` 必非 nil——但这个保证完全来自上游控制流，见修复项 2。
- OSS 语义未变：`server.go:2767-2768` 把原先嵌在 `else` 块内的 `apiMiddlewares = append(..., s.AuthMiddleware.APIMiddleware())` 外提并加 `s.AuthMiddleware != nil` 守卫；`ConfigStore == nil` 时 `AuthMiddleware` 保持 nil，与改动前一致。
- 访问日志确实消费 EE 写入的请求头：`handlers/middlewares.go:165` `ctx.Request.Header.Peek("x-request-id")`，对应 `identityhttp.OperationContext`（handler.go:75-81）。
- EE 中间件不覆盖静态 UI 与品牌读：中间件只进入 `apiMiddlewares`（`server.go:2794 RegisterAPIRoutes`），品牌路由由 `ee/internal/app/bootstrap.go:50` 单独注册并显式传 `auth`，所以 `public()` 不列品牌/静态资源不是遗漏。

**起手表覆盖**（每行一个结果）：

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 发现（修复项 3：重试次数/退避两份，一份裸字面量）。反例：PG 锁键 8342761901/…902 已互相注明；页大小、TTL、限流阈值均单点 |
| | 同一规则多份实现且互不注明 | 发现（修复项 4：来源校验三处形状不同；取舍项 A/B：进程夹具与 busy 重试各两份） |
| | 重造已有 helper | 发现（修复项 1：绕开宿主 logger）；取舍项 A（`Node` 与 `Fixture`） |
| | 派生常量无推导 | 发现（修复项 5：`recovery.go:85` 的 1025）。反例：`tokenLength`、`expires_in`、`MaxSessionTTL` 都从源值算 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无（迁移 ID/锁键/TTL/并发上限/游标上限均命名并注明来源；例外已计入修复项 3、5） |
| | 状态码、错误码裸数字 | 无（`handler.go` 全用 `fasthttp.Status*`，错误码用 `identity.Error` 常量；测试内数字属契约独立书写） |
| | 无名的错误形状 | 无（`identity.Error` 类型化，`SafeError` 单点折叠，未见以元组表达失败） |
| | 紧凑但绕的控制流 | 发现但有理由保留（`store.go` 的 `phase`/`t.stage` 闭包外部状态；`ResetPassword` 的 `outcome`/`err` 双错误已有注释交代） |
| | 文件集中或拆分是否妨碍理解 | 无（逐包核对，`service.go` 680 行为一个可连续阅读的单元，见有理由保留） |
| | 多余状态或守卫 | 发现（修复项 2：`attach` 的 nil 守卫不可达且顺序错位）；`authorizeTemporary` 的重复清理判定为有理由保留 |
| | 同一效果做两次 | 发现（`app/identity.go:66` 与 `NewHandler` 内各校验一次 origin，判定为有理由保留：先校验后迁移是刻意的失败前置） |
| | 注释与现状矛盾 | 发现（取舍项 C：`auth.go:286` 注释写"主管理员登录名"，实际取调用者账号名） |
| 架构与项目一致性 | 接缝形状与同类不同 | 无（详见下节「架构符合性」逐项对照） |
| | **架构符合性** | 无（逐项核对通过，证据见下节） |
| | **依赖关系** | 无（import/构造函数/所有权逐项核对通过，证据见下节） |
| | 绕过既有出口 | 发现（修复项 1：日志出口）。存储/迁移/响应/鉴权四类出口均走既有机制 |
| | 把上游现状当契约 | 无（兼容字段是方案 4.5 明确要求的兼容面，不是照抄上游风格；`is-auth-enabled`/`Login successful` 与 `handlers/session.go:44-81,166-169` 逐字段核对一致） |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无（本期不改前端；`ee/docs/账号认证接口.md` 与 `dto.go`/`handler.go` 逐条核对一致，见下节） |
| | 状态码口径不一 | 无（`Error()` 映射与文档 §"错误统一为…" 一一对应，`handler_test.go:14-49` 固定） |
| | 省略、空、值三态语义不对应 | 无（`limit *int` 区分省略/显式 0，`next_cursor *string` 空为 null，`target_id` 空串=缺省，三处都与文档一致） |

**开放式复读结果**：已做。放下表逐文件问"半年后第一眼皱眉的是什么"，得到两条表内未直接对应的发现，均已写入出口清单——取舍项 C（投影语义随策略替换而漂移）与取舍项 D（新模块的冒烟没有接进 `make smoke`，跑文档写的命令得到一个从未覆盖 identity 的绿色结果）。其余皱眉点（`service.go` 篇幅、`Store` 的 stage 追踪、`app` 承载离线恢复 CLI）复核后判为有理由保留，理由写在对应章节，下一轮不必重提。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 4 条：日志能力、进程夹具、busy 重试、来源校验各有两到三份实现 | 修复项 1/3/4、取舍项 A/B |
| 职责与实现质量 | 3 条：守卫顺序错位、裸数字、导出符号缺注释 | 修复项 2/5/6 |
| 架构与项目一致性 | 架构符合性与依赖关系均通过；唯一偏离是日志出口 | 见下方两段专项核对 |
| 契约一致性 | 无 | `ee/docs/账号认证接口.md` ↔ `dto.go`/`handler.go`/`service.go` 逐条核对 |

**架构符合性**（对照 [EE 后端架构](../../../../product/架构/EE后端架构.md)）：
- §3 总体布局 / §4 模块内部职责：`identity/` 根包只放业务模型、服务、规则、错误与所需接口（`model.go:108-256`、`service.go`），`http/` 只做解析→调用→映射（`handler.go:339-490` 每个端点都是 decode/调用/组装三步，没有任何规则判断），`persistence/` 只做行结构、事务与迁移（`store.go:24-122` 行结构与转换、`migration.go`）。没有出现"Handler 承载业务"。
- §4 文件粒度：按编码规范「文件组织以阅读为先」核实拆分是否降低阅读成本，不按示例清单要求。`identity/` 是 model+service 两文件、`http/` 是 handler+dto 两文件、`persistence/` 是 store+migration+diagnostics 三文件——一次改密要读的东西（校验、事务、事件）集中在 `service.go`，一次接口改动集中在 `handler.go`+`dto.go`，没有"只剩声明或转发"的文件，也没有为 Repository/Error/DTO 各建一个文件。判定合理。
- §1/§7 与 Bifrost 的边界：EE 仍内嵌完整上游应用，共享 Server/Router/配置数据库（`bootstrap.go:18-30`、`app/identity.go:55-72`），推理链未动（`server.go:2814-2838` 的 `inferenceMiddlewares` 未被本次改动触碰），未新建网关或第二套基础设施。宿主扩展面就是方案 4.5 白名单的 `ConsoleAuthProvider`+`ConsoleAuthFactory`（`server.go:207-211,249-251`）与 WS 回调键（`websocket.go:29-31`）。
- §6 插件与普通业务：本期无插件入口，`internal/bifrost` 只做跨功能接入（旧管理员导入、管理鉴权、`/api/config` 投影、WS 回调），具体业务不在其中——与 §7「集成归位」一致。
- §5「app 只做装配」：`app/identity.go` 只解析环境变量、迁移、构造、注入，没有运行期编排；`app/bootstrap.go:34-54` 仍是薄装配（品牌迁移+注册），FIND-012 关注的 attach 未恶化。

**依赖关系**（对照架构 §5、§7 与编码规范「依赖关系」）：
- 方向：`identity/model.go`、`service.go` 的 import 只有标准库（`context/crypto/encoding/errors/fmt/regexp/time/unicode`），没有 fasthttp、GORM、Bifrost 或任何适配包——业务根包不反向导入实现，核到位。`identity/http` 依赖 `identity`+fasthttp/router/schemas/lib；`identity/persistence` 依赖 `identity`+gorm/驱动/`framework/encrypt`；`internal/bifrost` 依赖 `identity`+`identity/http`+宿主包；`app` 依赖以上全部。全图单向，`identity/http` 与 `identity/persistence` 互不依赖，无包级双向依赖。
- 窄接口与显式注入：`Repository`/`Tx`/`PasswordHasher`/`AccountPolicy`（`model.go:210-256`）由使用方（服务）定义；`NewService(repo, hasher, options, policy)`、`NewStore(db)`、`NewHandler(service, origin)`、`NewAuthAdapter(host, service, handler)` 全部构造函数注入，没有全局容器、没有万能 Repository/BaseService。`AccountPolicy` 改用显式 `State` 参数（`model.go:253-256`）而非私有 context 键，是本轮的实质改善。
- 跨模块与资源所有权：identity 不碰 branding 表，反之亦然；`*gorm.DB` 只出现在 `app` 与 `persistence`，未进业务层；事务范围由服务决定（`Transaction(ctx, fn)`），存储执行。共享连接由宿主 ConfigStore 创建与关闭，`NewStore` 只包一层 `Session`（`store.go:147`）不接管生命周期；离线恢复自建 store 并 `defer store.Close(ctx)`（`recovery.go:61-65`）——所有权划分明确，与架构 §7「生命周期统一协调」一致。
- 唯一偏离：日志。架构 §7「按缺口扩展」把日志列为需先评估复用的既有能力，实际新代码直接用标准库 `log`（修复项 1）。

**契约一致性**（三方逐条核对）：`status/initialize/login/logout/me/change-password/accounts.create/list/set-status/reset-password/password-events/ws-ticket` 十二条路径、请求字段、成功码（201 仅 initialize 与 create）、`{account}` 五字段、事件十字段、`expires_in:30`、`Retry-After:60`、`limit` 三态、`next_cursor` null 语义、错误体 `{"error":{"code","message"}}` 与 7 个状态码，在 `ee/docs/账号认证接口.md`、`dto.go`、`handler.go`、`service.go` 四处一致。兼容面与 `handlers/session.go` 的 `is_auth_enabled/has_valid_token/auth_type`、`"Login successful"`、`"Logout successful"` 对齐，旧 `token` Cookie 的删除用同样的 `Path=/`（`handler.go:112-116` ↔ `session.go:153-158`）。前端本期不改，无第三方类型可对。

## 出口清单

### 本轮修复项

1. **EE 新代码用标准库 `log`，绕开宿主已注入并可配置的 logger。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/diagnostics.go:9,43`；`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/bifrost/auth.go:8,74,76`
   - 代码事实：这两处用 `log.Printf` / `log.Print` 写标准库默认 logger。EE 入口在 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/cmd/bifrost-http/main.go:30,104-109` 已构造 `schemas.Logger`、按 `-log-level`/`-log-style` 配置并注入 `lib`/`server`/`handlers`。基线 `ee/` 无任何标准库 `log` 使用（`git grep '"log"' 3e15a640d -- ee/` 无结果），这是本次新增的第二条日志通道。
   - 维护影响：身份诊断与迁移提示不受部署的日志级别与 json/pretty 格式约束，`--log-style json` 部署下这几行会破坏日志摄取；`log-level=error` 也拦不住 `auth.go:74,76` 的 info 级提示。测试因此必须劫持全局 `log.SetOutput`（`diagnostics_test.go:20-27`），这类用例天然不能并行。
   - 最小修正：在 `NewStore` / `NewAuthAdapter`（或统一在 `app` 装配处）通过构造函数注入一个窄日志接口（自定义 `interface{ Warn(string, ...any); Error(string, ...any) }` 或直接 `schemas.Logger`），由 `main.go` 已有的 `logger` 传入；保留现行字段与脱敏格式，测试改为注入假 logger。

2. **`app.Bootstrap` 先解引用适配器再校验，且把具体指针类型塞进接口，废掉了宿主的 nil-provider 守卫。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/app/bootstrap.go:21-25,29,42-44`
   - 代码事实：第 29 行 `attach(ctx, s, auth.APIMiddleware())` 先调用适配器方法，`attach` 内第 42 行的 `if auth == nil` 校验的是中间件值，而 `AuthAdapter.APIMiddleware`（`ee/internal/bifrost/auth.go:184-186`）永远返回非 nil 闭包，该守卫不可达；真正的风险（`auth` 为 nil）会在第 29 行直接 panic。另外第 21-25 行的 factory 闭包 `return auth, err` 把 `*eehost.AuthAdapter` 转成接口返回，若 `auth` 为 nil 指针，宿主 `transports/bifrost-http/server/server.go:2748` 的 `s.consoleAuth == nil` 判定为 false（typed nil），错误会被推迟到请求期。
   - 维护影响：看起来有两层防护，实际两层都不成立；一旦上游把 factory 调用挪到条件分支里（现为 `server.go:2746` 无条件调用），得到的是请求期 nil panic 而不是启动期明确错误——而启动失败正是方案 4.5 要求的行为（"provider 创建失败必须中止启动"）。
   - 最小修正：闭包内改为 `if err != nil { return nil, err }; return auth, nil`；并在第 26-29 行之间加 `if auth == nil { return errors.New("ee: console auth factory was not invoked") }`。`attach` 的中间件 nil 守卫可保留（成本为零）或一并删除。

3. **branding 迁移重试的次数与退避是裸字面量，与 identity 的同一条规则各写一份。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/branding/persistence/migration.go:25-37`；对照 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/diagnostics.go:47-73`
   - 代码事实：两段都是本次新增（`git diff <基线> HEAD -- ee/internal/branding/persistence/migration.go` 显示 +19 行）。规则相同：仅对 SQLite `ErrBusy`/`ErrLocked` 整笔重试、最多 3 次、退避按次数递增 20ms。identity 侧提取了 `sqliteBusy`/`retryBusy` 并命名 `busyRetries = 3`、`busyRetryStep = 20 * time.Millisecond`；branding 侧写成 `attempt == 2` 与 `20 * time.Millisecond` 内联，而"最多3次"同时写在 `ee/internal/branding/persistence/README.md:5` 与方案「第二轮复核修复记录」里。
   - 维护影响：违反硬规则「业务限制要命名」——读代码要从 `attempt == 2` 反推重试次数；调整策略要改两处且两处形状不同，`TestBrandingMigrationInternalRetry` 与 `TestMigrationRetriesSQLiteBusyAtomically`/`TestMigrationBusyExhaustionPreservesFailure` 也是两套孪生断言。
   - 最小修正：branding 侧至少提取 `brandingBusyRetries = 3`、`brandingBusyRetryStep = 20 * time.Millisecond` 两个常量，并在注释里指明与 identity `retryBusy` 是同一条驱动兼容规则（是否合并成一份见取舍项 B）。

4. **来源校验规则在三处各写一遍，`/ws` 的收紧没有注明。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/bifrost/auth.go:204`、`:233`；`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/handler.go:313`
   - 代码事实：`auth.go:204` 对 `/ws` 用 `string(c.Request.Header.Peek("Origin")) != a.http.Origin()` 精确比较（不接受 Referer 兜底）；`auth.go:233` 对非 GET/HEAD/OPTIONS 用 `a.http.SameOrigin(c)`；`handler.go:313` 对非 GET 用同一个 `SameOrigin`。三处的方法豁免集合与判定形状都不同，`/ws` 分支没有任何注释说明这是刻意收紧。
   - 维护影响：后续统一 CSRF 策略时很容易把 `/ws` 改成 `SameOrigin`，从而允许只带 Referer 的握手，违反方案 4.5「GET /ws 握手须匹配 Origin」；反向也一样，把 `handler.go:313` 收紧成精确 Origin 会打断脚本调用。
   - 最小修正：在 `auth.go:204` 加一行注释说明"浏览器握手必然带 Origin，故不接受 Referer 兜底"；或在 `identityhttp` 导出 `ExactOrigin(c) bool`，让三处都从同一个包取判定。

5. **离线恢复的 stdin 读取上限是无推导的裸数字。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/app/recovery.go:85`
   - 代码事实：`bufio.NewReader(io.LimitReader(os.Stdin, 1025))`。同一体系里与密码长度相关的上限是 `identity.MaxPasswordBytes = 72`（`model.go:72`）与未导出的 `maxLoginPasswordBytes = 1024`（`model.go:93`），1025 与二者的关系（1024+换行？）在代码里读不出来。
   - 维护影响：硬规则「业务限制要命名，派生值要写推导」；改密码长度上限时无从判断这个数字要不要跟着动。
   - 最小修正：定义命名常量并写出推导（例如 `// 读一行密码：内容上限 + 换行符` 配 `const maxRecoveryInput = <已命名上限> + 1`），不必改变实际数值——超长输入无论如何都会被 `validPassword` 拒绝。

6. **`Store` 的六个导出读方法没有注释，"未找到返回零值 + ErrNotFound" 的语义在 `Repository` 侧没有交代。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:172,178,216,221,392,406`；对应接口 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/model.go:211-214,219`
   - 代码事实：`State`/`CredentialByName`/`AuthByHash`/`AuthByID`/`Accounts`/`Events` 六个导出方法无文档注释（同文件的 `Transaction:323`、`ReserveLogin:362` 有）。`Repository` 接口只给 `Transaction`/`ReserveLogin`/`Events` 写了注释，四个读方法是裸签名；"读取未找到返回 `ErrNotFound`" 只写在 `Tx` 的注释里（`model.go:224`），而这几个方法在未找到时返回的是**零值 + 错误**（`store.go:181` `return r.credential(), notFound(...)`），调用方 `Login`（`service.go:290-298`）正是依赖这一点。
   - 维护影响：违反硬规则「导出符号必须有用途注释；特殊返回值必须交代」。零值+错误的组合是最容易被下一个实现者写错的返回约定（例如新增一个实现时返回 `nil, err` 或忘记判 `missing`），而契约目前只存在于调用点。
   - 最小修正：在 `model.go` 的 `Repository` 四个读方法上各补一行（"未找到返回 ErrNotFound，返回值为零值，调用方不得使用"），`store.go` 的实现方法沿用接口注释即可，无需逐个重复。

### 需用户裁决的 Blocking

- 无。以上六项均在本次授权范围内，不涉及范围、契约或风险接受。

### 立债(REF:立 `triggered_by: [<本case>]` 的需求进 reports/)

- 无。以下取舍项都可在本轮或下次触碰对应文件时处理，不值得单独立 REF。

### 挂账(Finding:登记 findings.md,带四要件)

以下四条为**建议取舍项**，各带反证条件，由实施者决定采纳或撤回；若整体撤回则按下列 `convert_when` 登记 Finding。

A. **三个冒烟脚本里的 EE 进程夹具是两份实现。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/scripts/identity-smoke.py:36-127`（`Node`）与 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/scripts/branding-smoke.py:35-104`（`Fixture`）
   - 代码事实：两者都做同一串事——建临时目录、写 0600 的 config.json、`socket.bind(('127.0.0.1', 0))` 取随机端口、按前缀过滤环境变量、`Popen` 带 `-host/-port/-app-dir`、60 秒轮询就绪、terminate→kill 停止、带 Origin 头与 CookieJar 的 JSON 请求封装、登录辅助。已经出现漂移：环境变量前缀 `Node` 多了 `AWS_`/`AZURE_`，超时 12s vs 10s，轮询间隔 0.1 vs 0.15。`identity-upgrade-smoke.py:10-12` 已经用 `importlib` 复用 `Node`，说明跨脚本复用在本目录是既成惯例。
   - 维护影响：启动契约变化（新增必需环境变量、新增启动参数、就绪端点变化）要改两处，且两处已经不一致。
   - 最小修正：`branding-smoke.py` 改为按 `identity-upgrade-smoke.py` 的方式复用 `Node`，只保留品牌自己的配置与断言。顺带把 `identity-smoke.py:289-334` 与 `identity-upgrade-smoke.py` 里无空格的赋值/字面量（`p=argparse...`、`{'username':'alice'}`）与同目录既有风格对齐。
   - 反证条件：若品牌冒烟必须能在 `identity-smoke.py` 缺失或损坏时独立运行（例如作为上游合并后的第一道验证），或它的旧 `auth_config` 启动契约与 identity 的部署契约会长期不同，则保留两份并在各自头部注明分叉原因。
   - 若撤回则挂账：`convert_when: "第三个冒烟脚本出现，或某次启动参数变更只改了一处导致误绿时，合并夹具"`

B. **SQLite busy 重试是否提取为 EE 内共享实现。**
   - 位置：修复项 3 的两处；identity 侧 `diagnostics.go:47-73`，branding 侧 `migration.go:25-37`
   - 代码事实：两个包各一份等价实现，连测试都是孪生的（`TestBrandingMigrationInternalRetry` ↔ `TestMigrationRetriesSQLiteBusyAtomically`+`TestMigrationBusyExhaustionPreservesFailure`）。
   - 维护影响：驱动兼容口径（哪些 `sqlite3.ErrCode` 算可重试）今后只要变一次，就有一处漏改的风险。
   - 最小修正：提取到一个 EE 内共享的小包（例如 `ee/internal/storage`），只导出 `RetryBusy(ctx, fn)` 与 `IsBusy(err)`。
   - 反证条件：若认为为 10 行驱动兼容代码新建共享包违背编码规范「按具体缺口引入，不重复包装整套 SDK」、且两个模块的重试策略今后可能刻意分叉，则维持两份，只做修复项 3 的命名 + 互相引用注释。
   - 若撤回则挂账：`convert_when: "第三个模块需要同样的 busy 重试，或两份策略出现实质分叉时"`

C. **`/api/config` 的 `auth_config` 投影用调用者账号名充当主管理员名。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/bifrost/auth.go:286-293`
   - 代码事实：注释写"主管理员登录名"，实现是 `a.service.Me(ctx, p)` 取**调用者**的 `Username`。当前二者等价，因为 `APIMiddleware` 已经过 `RequireAccountManager`，而暂行的 `ChiefPolicy`（`model.go:261-266`）要求 `p.AccountID == state.ChiefAccountID`。但 `AccountPolicy` 的存在意义就是被 RBAC 替换（`model.go:251` 注释明写）。
   - 维护影响：RBAC 落地后，非 chief 的账号管理员读 `/api/config` 会看到自己的用户名被当成 `admin_username`，与方案 4.5 B2「admin_username 为 chief 登录名」静默分叉；而且投影同时参与 PUT 同值回送的相等比较（`checkConfig:322-331`），不同管理员回送同一份配置会互相 409。
   - 最小修正：注释改为"当前调用者（暂行策略下即主管理员）"，并加一行 TODO 指向 RBAC 替换时需要改为按 `State.ChiefAccountID` 读取；或直接在投影里用 chief 账号读取。
   - 反证条件：若 4.5 B2 的投影语义实际只要求"当前管理员可见的一个用户名"，或 RBAC 期本来就会重写整个投影与预检，则只改注释即可。
   - 若撤回则挂账：`convert_when: "RBAC 接入、AccountPolicy 被替换时，同步核对投影与同值回送的相等语义"`

D. **identity 冒烟没有接进文档化的 `make smoke` 入口。**
   - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/Makefile:42-43`（仅 `branding-smoke.py`）与 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/README.md:34,39`
   - 代码事实：`make -C ee smoke` 只跑品牌冒烟；README 第 39 行描述冒烟"验证宿主接入、品牌操作与重启保留"，未提 identity。两个新脚本只在 workbench 的 checklist 里被调用。
   - 维护影响：改完 identity 代码、按 `ee/README.md` 跑 `make -C ee smoke` 会拿到一个从未执行过身份路径的绿色结果；同一目录下的两个模块，冒烟可达性不一致。
   - 最小修正：加一个 `smoke-identity` 目标（sqlite 模式不需要额外依赖），并在 `ee/README.md` 的命令块里列出；或在 README 明写 identity 冒烟的调用方式与它不在 `make smoke` 内的原因。
   - 反证条件：若刻意排除（单次运行分钟级、或希望 PG 模式与 SQLite 模式都必须显式选择），则只改 README 说明。
   - 若撤回则挂账：`convert_when: "下次有人改 identity 后只跑 make smoke 就提交时，或新增第三个模块冒烟时"`

### 转代码评审(范围外发现,一行一条,不出结论)

- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/bifrost/auth.go:70-72`：`BootstrapLegacy` 的任何失败（含 `SafeError` 折叠后的存储不可用）都报成"不是受支持的 bcrypt 哈希"。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:363-390`：`ReserveLogin` 复用 `s.transaction`，每次登录尝试都会递增 `state.revision` 并取全局写锁。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/service.go:94-102` 与 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/handler.go:150-153`：哈希槽耗尽与登录限流共用 `ErrLimited`，`Retry-After` 一律取登录窗口 60 秒。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/service.go:481-484`：`ResetPassword` 在鉴权与幂等判定之前就计算了 bcrypt 哈希并占用哈希槽。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/handler.go:78`：无条件覆盖客户端提供的 `x-request-id` 请求头（上游 `handlers/middlewares.go:165` 原本会透传该值）。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/transports/bifrost-http/handlers/websocket.go:146-166`：Pong 处理器改为在 `client.mu` 下执行，与 `writeSafely` 的加锁顺序及 `closed` 语义。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/transports/bifrost-http/handlers/websocket.go:253-262`：`authorize` 失败时用 `SetReadDeadline(time.Now())` 中断读循环以释放被撤销的连接。
- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:396,413`：分页 `WHERE` 里的 `OR` 条件与前置 `target_id` 条件的括号组合依赖 GORM 的表达式包裹行为。

## 看似可改但有理由保留

- **`service.go` 680 行单文件承载全部服务方法**：核实过拆分收益而不是行数。所有方法共享 `current`/`replacePassword`/`authorize`/`page`/`validPassword` 这几条私有规则，按"账号/会话/密码"拆开会让一次改密流程跨三个文件；符合编码规范「同一包内默认把需要一起理解、一起修改的代码放在一起」。保持集中。
- **`store.go` 的 `phase`/`t.stage` 阶段追踪（闭包捕获外部状态）**：这是起手表"紧凑但绕的控制流"的命中项，但替代方案（每个 `Tx` 方法包一层带阶段名的调用）代码量更大、更容易漏；当前写法已被 `diagnostics_test.go:42`、`service_test.go:449` 的 `stage=` 断言固定，改动会被测试挡住。保留。
- **`identity` 根包承载 `WithDiagnosticOperation`/`DiagnosticOperation`（`model.go:272-289`）虽非业务规则**：`app`、`http`、`persistence`、`bifrost` 四处共用，放在共同祖先包避免为两个函数新建基础设施包，也避免适配包之间互相依赖。保留。
- **`app/identity.go:66` 与 `NewHandler` 内各校验一次 origin**：这是"同一效果做两次"的命中项，但第 66 行的目的是在跑迁移之前把全部部署配置校验完（第 52 行注释已写明），删掉会让非法 origin 在建完六张表之后才失败。保留。
- **`authorizeTemporary` 开头的 `RemoveUserValue(principalKey{})` / `RemoveUserValue(IsLocalAdminContextKey)`（`auth.go:175-176`）在当前唯一调用路径下不可达**：已核实两处防的是同一对象——`APIMiddleware:187-189` 在同一次调用里已经清过，且清除与 `SetUserValue:237-238` 之间只有这一个调用点。但这是安全语义的兜底（"临时令牌绝不继承控制台身份"），成本两行，删除的收益低于新增一个调用点时忘记清理的风险。保留。
- **`app/recovery.go` 在 `app` 包里做 flag 解析与终端交互**：形式上比 `cmd/` 承担 CLI 更"重"，但 `cmd/bifrost-http/main.go` 是上游 `main.go` 的抄本（包注释与 `cmd/bifrost-http/README.md:10` 都要求按 diff 同步，FIND-014 记的就是这条），把 CLI 代码搬进 main.go 会加大与上游的漂移面；同时恢复流程必须复用 `identityOptions`/`newIdentityService`，与 `app/identity.go` 同包最直接。保留。
- **`server.go:2752-2763` 的清理块与 `:2733-2742` 近乎重复**：抽 helper 会扩大上游文件的改动面，超出方案 4.5 的宿主白名单；当前写法与相邻既有代码同构。保留，不按 EE 编码规范要求上游文件。
- **`service_test.go` 放在 `persistence` 包内测 `identity.Service`**：需要真实事务与驱动错误，`persistence/README.md:10` 已写明定位。保留。

## 台账对账

- **FIND-012（`attach` 四步惯性点）**：本次 `bootstrap.go` 净增约 12 行且新增内容是"注入 factory"而非第五件套；身份装配没有堆进 `attach`，而是独立到 `app/identity.go`。未恶化，继续观察。
- **FIND-013（骨架探针）**：已在「EE 现有代码架构迁移」中关账，本次未触及，`branding-smoke.py:109-121` 仍在断言探针已移除。无变化。
- **FIND-014（`ee` main.go 是上游抄本，无机械护栏）**：本次 `main.go` +7 行（identity 子命令分发），与上游的已知差异点从 4 处增加到 5 处，同步护栏仍未建立。未恶化但差异面变宽；本轮不重提，仅记录供下次合并上游时参照，另见「有理由保留」中恢复 CLI 的归属理由。
- **FIND-015（旧 smoke.sh 依赖本机状态）**：旧脚本已在上一个 case 删除，本次两个新脚本均自建临时目录/随机端口/只清理自有进程与自建数据库（`identity-smoke.py:46-49,317-330`），符合该条的整改口径。无恶化。
- **FIND-016（上游 `RegisterAPIRoutes` 硬编码 callbacks）**：本次通过新增 `ConsoleAuthProvider` 字段接入，未在 EE 内绕过该通路。无变化。
- FIND-001~011 属 UI 中文化与 workbench 外壳，与本案无关。
- 方案「Gate 决策结果」「实施偏差记录」「用户复核：整体收敛纠偏」中已裁决的事项（默认密码 123456、全部会话退出、后端先行、普通成员仅本人接口、`Service.Account` 删除、`limit` 显式 0 报 400、WS 连接级重验的失效定义、访问日志对 `/ws` 只记路径）本轮均未重提。
