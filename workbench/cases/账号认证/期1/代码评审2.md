---
title: 账号认证 期1 代码评审 #2
type: 代码评审
case: 账号认证
phase: 1
round: 2
verdict: 需修改
created: 2026-09-11
---

> 独立代码差量评审，与实施、收敛及独立测试职责隔离。前候选 `eb93291fcae2248054309e09a69e01aac84ed6f0` → 固定候选 `5b784f32ae66404c3a55a51d2ceae3b0bd007064`；总体业务基线仍为 `3e15a640d89902772445d3fb4c04023e58828eba`。起审及独立构建时工作树干净，差量34文件。构建后其他职责开始写过程文档，之后实施者开始修复；本记录只评价固定候选，不包含这些后续改动。评审者只新增本记录，不改业务、不提交、不生成视图。

## 结论

**需修改。R1–R4全部关闭，新增R5本轮修复项未关闭。** 安全数据库诊断、默认配置目录、WS外层提前返回脱敏和PG失败现场保留均有独立实证。固定候选在全新有效默认配置启动时实际因品牌迁移 `database is locked` 退出；这是当前可运行交付的失败，不能以另一套夹具重跑通过消除。主实施者已接受将该启动可靠性问题一并修复，待新固定候选复核。

首轮“PG真实COMMIT失败已验证”的证据表述在本轮纠正：旧夹具的PG辅助INSERT先失败，只能证明写入失败时回滚；本轮修正夹具才证明实际COMMIT失败，详见R1。旧记录保持原样，不覆盖失败历史。本结论不是Gate 2或发布授权；默认123456、管理员重置、后端先行等已批准业务决定不重新裁决。

## 对照方案核查

| 方案、实施记录及差量 | 亲核结果 |
|---|---|
| R1固定业务响应并保留安全故障诊断 | persistence边界在SafeError前分类；HTTP、启动、离线恢复与WS验证传固定操作名及独立关联ID。事件写入、断库查询、迁移与提交失败均有验证 |
| R2默认与显式配置目录一致 | adapter调用与宿主相同的GetDefaultConfigDir；省略app-dir的残缺/完整/无文件及重启场景已实际执行 |
| R3所有WS访问日志不含凭据 | 宿主http.target生成处对/ws只记路径，不再依赖内层改写URI；坏gzip及OPTIONS后同一票据仍可正常握手 |
| R4失败保留PG现场 | 创建成功与业务成功分开记录；finally先停止自有节点，失败保留库及日志并打印安全位置；独立注入断言确认 |
| C1共享票据TTL、C2文档同步 | 运行行为上签发与expires_in同用30秒常量；文档/结构结论归收敛评审 |
| F01身份迁移失败边界 | 可识别SQLite busy/locked最多尝试3个完整事务；耗尽报错、无部分身份表，非busy立即返回。旧F01原始原因不可追溯，不能认定其就是锁冲突或已被此修复彻底解决 |
| 首次启动及升级可运行 | 当前候选首跑在品牌迁移失败，见R5；第二套新夹具全流程通过只作为补充事实 |

方案v2末尾已登记R1–R4、TTL/文档调整及F01处理。未新增注册、删除账号、角色管理、管理API Key、HTTP恢复入口或推理鉴权能力。R5尚不属于本固定候选的修复，不能预先计入完成。

## 接口面对账

- HTTP路由、请求/响应业务结构及宿主公开扩展相对前候选不增加；`X-Request-ID`关联诊断已写入接口说明，值由入口生成，未信任客户端值作为诊断关联ID。
- 新增跨包协作符号为`identity.WithDiagnosticOperation`、`identity.DiagnosticOperation`、`identity.WSTicketTTL`与`identityhttp.OperationContext`，用于已登记的本模块诊断/TTL兑现，不产生额外授权能力；上下文不携带SQL、凭据或长期持有的HTTP对象。
- pgx由间接改为直接依赖，版本未改变，供持久化适配识别SQLSTATE；业务包未引入GORM。宿主`requestLogTarget`为非导出日志辅助函数，不改变实际WS查询参数。
- 结构、命名、常量位置与手维护契约同步由收敛评审独立裁决；本记录不据这些事项作风格结论。

## 预飞事实表

已读[预飞5](../../../evidence/账号认证-期1-预飞5.md)、[首轮修复验证](../../../evidence/账号认证-期1-首轮修复验证.md)、交测包第二轮及[checklist](checklist.yaml)。开发者11项全绿与本评审亲跑结果分别记录：前者不替代R1–R4反例复核，也不能排除本轮实际复现的R5。

## 可证伪质疑

### R1 · 原本轮修复项，已关闭：诊断安全且真实PG提交失败证据成立

**位置与事实：**[diagnostics.go:17](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/diagnostics.go:17)、[store.go:212](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:212)、[service_test.go:421](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/service_test.go:421)。未知错误仅记录类型；PG记录SQLSTATE，SQLite记录主码/扩展码；取消、超时、连接关闭使用固定分类。日志不格式化原始driver错误、不输出SQL，HTTP仍经SafeError返回固定错误。

**复核：**两种真实数据库的identity测试`-race -count=1`通过，含写入故障、断库查询、延迟约束失败和迁移回滚。真实SQLite HTTP事件触发器使重置返回503，日志含`identity.reset-password/password_event.insert`及响应X-Request-ID关联、SQLite约束码，未含触发器私有错误文本；原会话保持可用。日志排除密码、session、ticket及setup密钥断言通过。

**PG证据纠偏：**已逐行核对新夹具的`NewDB:true`在同一事务使用新Statement，避免继承事件INSERT的绑定参数；先断言辅助INSERT成功，再断言`transaction.commit`，最后检查hash/version、原会话及事件全部回滚。另以临时Go overlay只增强测试断言，要求日志同时包含`kind=postgres code=23503`；真实PG单项通过（5.156秒）。因此本轮能够证明延迟外键在COMMIT失败，而首轮PG结果应收窄为写入失败回滚。增强测试及输出保留于`identity-review2-commit-lgwozuwl`，未修改仓库源码。

**处置：**原最小要求已兑现，关闭R1；不将固定分类误称为保存完整SQL或原始错误。

### R2 · 原本轮修复项，已关闭：默认目录与宿主读取一致

**位置与事实：**[auth.go:89](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/bifrost/auth.go:89)现调用`validateLegacyFile(server.GetDefaultConfigDir(s.AppDir))`，与宿主Bootstrap解析路径一致。

**跨评审转交原文：**“`ee/internal/bifrost/auth.go:88` 将原始 `s.AppDir` 传给 validateLegacyFile，宿主 `server.go:2541` 实际通过 `GetDefaultConfigDir` 后 LoadConfig；请代码评审核实未传 --app-dir 的默认启动是否校验了同一份 config.json（已交主实施者转发）。”

**复核及处置：**第一次真实演练的默认残缺凭据准确拒启；第二套全新夹具完整执行三场景：省略app-dir、隔离子进程HOME/APPDATA/cwd，默认目录残缺拒启、有效旧凭据导入、无文件可初始化；无关cwd残缺文件不干扰，改密后重启不被旧文件覆盖。没有访问操作者真实默认目录。原最小要求已兑现，关闭R2及该跨评审转交项。第一次default-valid因品牌迁移失败单独列R5，不掩盖失败，也不混同目录校验错误。

### R3 · 原本轮修复项，已关闭：外层提前返回无WS票据泄漏

**位置与事实：**[middlewares.go:32](/Users/xinyue/VSCode/ws_2026/bifrost-auth/transports/bifrost-http/handlers/middlewares.go:32)及`:153`在访问日志实际生成处取安全target；`/ws`仅返回路径。无论内层认证是否执行都应用同一规则，且不消费/改写真实票据。

**复核及处置：**宿主单测与真实SQLite/PG HTTP均通过：有效票据先经坏gzip得400、OPTIONS得200，随后正常WS握手101、通知到达且改密后撤销；拒绝legacy token和各路径日志均不含凭据值。完整宿主handlers/server竞态回归通过。原最小要求已兑现，关闭R3。该日志实现偏差已登记，不增加认证豁免。

### R4 · 原本轮修复项，已关闭：PG失败保留数据库与日志

**位置与事实：**[identity-smoke.py:298](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/scripts/identity-smoke.py:298)、`:321`区分database_created与succeeded；失败不DROP，finally停止自有节点并报告证据位置。

**独立反例复核：**导入固定脚本，在两个真实节点启动后将exercise替换为受控AssertionError；沿原main执行finally后，查询pg_database确认自建库仍在、两个process均已退出且日志文件仍在，stderr有安全库名及证据位置。证明记录`bifrost-identity-smoke-_lvq9t6e/review-r4-proof.txt`。确认保留行为后，评审者显式仅删除这一个自建库，并将该人工清理事实追加到证明；共享PG集群未停止。

**处置：**原最小要求已兑现，关闭R4。正常SQLite和PG双节点smoke也通过，成功清理行为保持。

### R5 · 本轮修复 · 当前候选全新实例仍会因品牌迁移锁冲突退出

**位置与代码事实：**[branding/persistence/migration.go:22](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/branding/persistence/migration.go:22)只执行一次外层迁移事务；[app/bootstrap.go:47](/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/app/bootstrap.go:47)直接将错误作为启动失败返回。身份迁移新增的busy重试不覆盖品牌迁移。

**触发与后果（真实候选实证）：**独立产物运行升级脚本，在全新`default-valid`目录、完整有效旧凭据、不传app-dir的候选首次启动中退出，日志为`failed to bootstrap server: ee: migrate branding: database is locked`。此时没有运行该场景的旧基线进程，不能把它写成仅旧二进制失败。现场`bifrost-identity-upgrade-x1v7aw9h/default-valid/.config/bifrost/server.log`及数据库保留。锁持有者及精确driver扩展码未定位，不能凭字符串进一步断言BUSY_SNAPSHOT根因。

另建全新夹具`bifrost-identity-upgrade-e2ejqz27`完整通过，不改变首次失败事实。旧基线品牌迁移锁失败在首轮及开发者修复验证中另有现场；既有缺陷同样影响本次候选的首次可运行交付，主实施者已明确纳入本轮修复。

**最小修正：**对可识别的SQLite busy/locked做有限次数的整个品牌迁移事务重试；表、单例和迁移版本共同回滚后再开事务，非busy立即失败，取消与耗尽仍拒启并保留可诊断原因，保持PG迁移锁及版本ID不变。补一次busy后成功、持续busy耗尽、非busy/取消与原子性断言，并用新固定产物验证新目录启动。不要修改旧基线二进制、删除旧失败现场或把重跑成功写成原失败解决。由实施者修复，本轮不自动改代码。

## 挂账(Finding:范围内、裁决本轮不修、需登记findings.md)

- 无新增挂账。R5已由实施者接受为本轮修复项；本评审不代替用户接受未修风险。原F01保留“根因未知”的证据边界，诊断与有限重试已验证，不能伪造因果关闭。

## 转收敛评审

- R2转交已亲核关闭，后续Gate 2摘要应保留处置结果。
- C1/C2以及诊断、WS日志辅助函数的组织和契约同步仍由收敛职责裁决；无新增结构意见。
- 收到收敛2再次转交：共用默认目录解析出口的运行结论交代码侧；诊断/WS/身份重试/PG提交夹具不由结构结论代判；当前候选品牌锁失败及尚未提交的修复必须另行复核。本记录分别以R1/R2/R3关闭、PG证据纠偏、F01未知根因边界和R5待修作出处置；不以收敛侧“无碍”关闭R5。

## 验证记录

独立产物：`/tmp/bifrost-auth-review2-5b784f32`；SHA256 `88c3fad0879cd0a41f43f04637542743eb4d37622a6d9f63907933433e0a32ed`；Go1.27.1、darwin/arm64，`go version -m`中vcs.revision为固定候选完整SHA，vcs.modified=false。先独立构建，之后才落盘评审文档；未沿用开发者产物。

| 亲跑命令/检查 | 结果 |
|---|---|
| `cd ee && GOWORK=off go build -o /tmp/bifrost-auth-review2-5b784f32 ./cmd/bifrost-http` | 通过，SHA/VCS如上 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/identity/... ./internal/bifrost/...`，SQLite与PG分别运行 | 均通过；SQLite persistence46.595s、adapter15.837s；PG persistence48.900s、adapter15.640s。PG DSN仅注入环境 |
| `cd ee && GOWORK=off go test -race ./...` | 通过；identity/persistence44.408s，其余有缓存，未将缓存当新实跑 |
| `cd ee && GOWORK=off go test -race -count=1 github.com/maximhq/bifrost/transports/bifrost-http/handlers github.com/maximhq/bifrost/transports/bifrost-http/server` | 通过，59.041s/4.242s |
| PG `go test -race -count=1 -v -overlay <临时overlay> -run '^TestDeferredCommitFailure$' ./internal/identity/persistence` | 通过；仅增强测试要求实际PG23503及既有提交/回滚断言，5.156s |
| `python3 ee/scripts/identity-smoke.py --binary /tmp/bifrost-auth-review2-5b784f32 --database sqlite` | 通过，现场`bifrost-identity-smoke-olq4kpfa` |
| 同上`--database postgres`，专用DSN仅放环境 | 双节点通过，现场`bifrost-identity-smoke-9tj1q7f8`；成功分支清理自己的库 |
| 导入identity-smoke，对exercise注入AssertionError后检查库/进程/日志 | R4失败保留验证通过，证明及人工清理事实见上 |
| `python3 ee/scripts/branding-smoke.py --binary /tmp/bifrost-auth-review2-5b784f32` | 通过，品牌内容、静态资源及两次重启断言完成 |
| `python3 ee/scripts/identity-upgrade-smoke.py --binary /tmp/bifrost-auth-review2-5b784f32 --legacy-binary <基线元信息中的二进制>` | 第一次default-valid候选启动失败，R5；第二套新夹具完整通过，分别保留，未覆写首次结果 |
| `git diff --check eb93291fcae2248054309e09a69e01aac84ed6f0 5b784f32ae66404c3a55a51d2ceae3b0bd007064` | 通过 |

执行更正：一次误从transports模块在GOWORK=off下运行宿主测试，因该模块缺go.sum项在装载阶段失败；随后改回checklist规定的ee模块完整通过，未修改依赖。一次smoke误用`--backend`被参数解析拒绝、未启动实例；更正为`--database`后执行结果如表。没有把这两次未执行测试的调用写成业务失败或通过。

临时现场前缀均为`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/`。所有Go命令和smoke均已结束；检查独立产物运行PID为空，各脚本自有节点已停止。只操作自建schema/database及目录，未停止共享PG或操作既有业务实例；失败升级现场保持原样。

### 失败路径追踪

| 路径 | 最终出口 |
|---|---|
| 查询/事件写入/提交失败 | 持久化边界安全分类、关联ID；业务层固定503；密码/version/会话/事件回滚 |
| SQLite身份迁移busy/locked | 外层完整事务最多3次；一次busy后成功并幂等，持续busy恰好3次且无部分表，耗尽记录5/517并拒启 |
| 非busy迁移错误/取消 | 非busy直接返回；等待使用ctx.Done，GORM继承context；不脱离context另开后台写入。取消分支本轮静态追踪，未额外注入 |
| 默认残缺配置 | 读取宿主实际默认目录，拒启；已初始化时不重导入旧文件 |
| WS解压/OPTIONS/鉴权拒绝 | 访问日志先天不含query；正常票据消费与撤销仍按原协议 |
| PG脚本业务失败 | 自有节点停止，库和文件保留，安全位置可见 |
| 品牌迁移锁失败 | 当前候选单次事务后直接启动失败，R5待修 |

### 起手表覆盖

| 方面/信号 | 结果 |
|---|---|
| 验收标准未覆盖 | 发现R5首次启动失败；R1–R4回归闭合 |
| 方案、实施、diff不一致 | R1–R4已兑现；不把R5重跑通过当稳定启动证明 |
| 多出功能/公共接口/抽象/依赖 | 无未登记业务能力；诊断符号和直接pgx依赖已对账 |
| 偏差未登记 | 固定候选修复偏差已登记；R5是后续待修 |
| 失败被吞/假成功 | R1安全诊断闭合，业务仍失败；R5明确报错退出 |
| 空值/空集合无分支 | 无新增问题；原未初始化、空页及缺DB分支保持 |
| 取消/超时后副作用 | 未发现；GORM继承context，重试等待响应取消，无新增后台写入 |
| 重发两份副作用 | 无；重置operation_id及完整事务回滚保持，双节点HTTP验证 |
| 事务外写后读/部分回滚 | 无新增问题；PG实际COMMIT与SQLite迁移耗尽回滚亲核 |
| 开关关闭/旧数据/旧客户端 | R2关闭；完整升级覆盖开关关闭、历史短密码和旧会话拒绝 |
| 昂贵检查先行/校验缺层 | 无新增问题；本轮未放宽长度/来源/服务权限校验 |
| 路由保护/关闭认证 | 无新增旁路；adapter、真实Cookie/Basic/Bearer拒绝及临时令牌测试通过 |
| 敏感信息日志/响应 | R1/R3关闭；固定分类与安全target实证通过 |
| 热路径I/O/无界缓冲循环 | 无未声明I/O；重试3次有界，诊断仅失败时写日志 |
| 配置/部署步骤未登记 | 无新增环境变量或部署秘密；诊断与失败清理说明已补 |
| 无法追溯服务端失败 | R1关闭；R5可定位品牌阶段，精确锁根因仍未证明 |
| 对外内部错误原文 | 身份HTTP无，保持固定码；诊断不含driver原文 |
| 脚本失败现场清理 | R4关闭；R5失败目录及DB保留 |
| 新规则/失败路径无测试 | R1–R4和身份busy已有测试；R5需要补定向测试 |
| 断言绑定实现而非行为 | 已核对HTTP/最终DB状态；提交stage再辅以PG23503，避免只信标签 |
| 失败信息不可定位 | R4有位置；R5启动失败日志与路径已留存 |
| 返回值/错误未检查 | 首轮少量清理/辅助读取未检查边界保持；关键新断言检查辅助INSERT及提交，未发现新增阻塞 |
| fixture依赖个人环境 | 无；自建目录/随机端口/显式测试DSN，默认目录隔离 |
| mock结果冒充真实 | 发现并纠正首轮PG提交表述；busy注入明确为注入，实际HTTP及PG提交另列 |
| 整体替换模块mock | 不适用；仅局部失败注入，不替换身份或数据库模块 |
| 调试残留/后门/本机配置 | 无新增；overlay仅在临时目录，恢复仍是批准CLI |
| 未使用代码/无关改动 | 无新增业务残留；诊断/测试/文档均可对应修复项 |
| 生成物与源不一致 | 本轮UI无改动，复用首轮已独立构建的embed；过程视图由主实施者统一处理 |

### 实际阅读范围、未执行项与开放式复读

本轮逐项读完整现状的identity模型/服务/HTTP、store/migration/diagnostics与全部身份测试、bifrost adapter及测试、app恢复；完整读取修改的宿主middlewares.go、日志辅助测试、identity两份smoke脚本。复核新失败时另读完整branding迁移和app装配，沿宿主默认目录解析、路由中间件构成追踪；WS不经推理TracingMiddleware，因此没有把推理trace的URL字段错当作本次WS日志出口。前轮已完整审查的server/websocket及未改业务部分以固定差量交叉核对。读取方案v2、需求/验收口径、首轮评审、修复附件、预飞5、checklist、findings与相关README/接口/升级说明；契约同步结论留给收敛评审。

按本轮差量授权不重装、重建无改动UI（首轮已亲跑npm ci与build-ui）；没有以UI构建替代认证页面验收。未重复运行工作台视图生成/检查：构建后其他职责并行修改过程文档，由主实施者统一在提交前生成并校验；本评审不生成视图。没有生产TLS/反代、真实用户默认目录、浏览器页面或外部provider测试。F01旧失败原因不明，本轮不宣称复现了其精确根因。当前候选R5与旧基线锁失败、另一次全流程通过分别记录。

已做开放式复读，逐文件追问故障如何被发现与恢复；表外发现为本轮真实候选启动失败R5。除已列R5之外无新的代码正确性发现。未修R5前本记录保持需修改，待新候选极窄复核，不将实施者计划当作完成证据。
