---
title: 账号认证 期1 收敛评审 #7
type: 收敛评审
case: 账号认证
phase: 1
round: 7
verdict: 无碍
created: 2026-09-12
---

> 评审者与实施上下文隔离（未参与实施；按差量复核规则只读了 [收敛评审6](收敛评审6.md) 的「本轮修复项」与 [代码评审5](代码评审5.md) 原样转来的「转收敛评审」两条，未读其余旧评审意见）。基线 `3e15a640d89902772445d3fb4c04023e58828eba`；本轮为差量复核：上一轮候选 `a8666448d040cfde8491fceb37a920a537530793` → 评审候选 `97e58c94a6a023d8e2bfe897ceff23ebd63daf42` → 修复后最终候选 `aa2286a1156f0c0136e22050a381eb71fac84f84`（`git status --porcelain` 起审、修复复核与收尾三次均为空，无未跟踪文件，交付物即 HEAD）。
> L3：起手表逐行执行，另做开放式复读；范围为差量文件的完整现状，不重做整包。

## 结论

本轮差量是代码评审 5 三条修复项（ResetPassword 预检顺序、旧管理员导入错误分类、两处分页游标括号 + 冒烟翻页断言）加其转来的一个命名问题，改动小、边界干净：四个文件全部落在已有模块内，未新增包、未新增导出符号、未动 HTTP 契约与表结构，业务根包 `identity` 的 import 仍只有标准库，`bifrost`→`identity` 的依赖方向与窄接口注入未变，架构符合性与依赖关系两项均无发现。转来的命名问题已在候选中就地关闭（`allowed` 变量整个删掉，`:78` 直接判 `== nil`），我重读现状核实成立；转来的行数观察项经独立复评维持"已评估、保持集中"，并在本记录写明下一轮可直接引用的拆分触发线。上一轮（收敛评审6）唯一的修复项（`ee/AGENTS.md` 与编码规范硬规则在 logger 一条上分叉）在两个候选上均仍关闭。

我在评审候选 `97e58c94a` 上提出两条本轮修复项：一是 `ResetPassword` 新增了第二种 `ErrConflict` 成因和一条业务错误回滚路径，而该导出函数的注释仍写"只有存储故障才回滚""同 ID 不同 actor/target 返回 `ErrConflict`"，接口文档对 409 给出的重试指引对新成因恰好相反；二是 keyset 游标比较在 `store.go` 两处各写一份、互不注明，本轮 R3 的括号修正正是被迫改两处。两条均已由 `aa2286a11` 修复，我在只读前提下逐条亲核关闭：注释与文档现在与 `:125-129` 的实际分支一致且未引入新的口径冲突；`beforeCursor` 确实把比较规则收成全仓唯一一份，生成的 SQL 与修复前逐字等价，两个调用点的 column 与随后的 `Order` 排序键仍正确配对。修复提交只触及我指名的三个文件（`passwords.go` 注释 3 行、`store.go` 结构 18 行、接口文档 1 句），未夹带其他代码改动。

至此本轮无未关闭的修复项、无需用户裁决事项、无新增立债或挂账，架构与实现质量适合交付，给出「无碍」。

## 事实来源

**跑过的命令**（全部只读，未构建、未跑测试、未起服务、未改索引、未暂存或提交）：

- 起审与范围核对：`git status --porcelain`（三次，均为空）、`git rev-parse HEAD`、`git diff --stat a8666448d..97e58c94a`、`git diff --stat 3e15a640d..97e58c94a`、`git log --oneline a8666448d..97e58c94a`（两个提交：`a89d01355` 修复 + `97e58c94a` 记录与去掉误名变量）、`git diff a8666448d..97e58c94a -- ee/`（全文）、`git diff a89d01355..97e58c94a -- ee/internal/identity/passwords.go`。
- 定向复核：`git log --oneline 97e58c94a..aa2286a11`（单提交 `aa2286a11`）、`git diff --stat 97e58c94a..aa2286a11`、`git diff 97e58c94a..aa2286a11 -- ee/`（全文）、修复后重读 `store.go:285-326` 与 `passwords.go:64-92,120-135` 的现状。
- 事实查证：`git diff --stat a8666448d..97e58c94a -- ee/AGENTS.md workbench/规范/项目/编码规范.md`（为空）、`git show a8666448d:<store.go|service.go> | wc -l`、`wc -l` 于差量及邻近文件、`grep -rn "id < ?|created_at < ?|occurred_at < ?" ee/`（修复前后各一次）、`grep -rn beforeCursor ee/`、`grep -rn 'column+" = ?"' ee/`、`grep -rn 'hash == ""' ee/internal/`、`grep -rn ImportLegacyAdministrator ee/`、`grep -rn "not a supported bcrypt" ee/`、`grep -rn "gorm|fasthttp|server\." ee/internal/identity/{passwords,accounts,service}.go`（为空）、`grep -n "409也可能|ID复用于其他actor" ee/docs/账号认证接口.md`、`gofmt -l ee/internal/identity ee/internal/bifrost`（修复前后各一次，均无输出；不带 `-w`，不写文件）。

**完整读过的范围内文件**（四个差量文件全部读完整现状，diff 只用于定位新旧；修复后对被改的两个文件重读改动区间）：`ee/internal/identity/passwords.go`（203 行）、`ee/internal/bifrost/auth.go`（268 行）、`ee/internal/identity/persistence/store.go`（修复前 322 行 / 修复后 326 行）、`ee/scripts/identity-smoke.py`（346 行）。workbench 侧差量（代码评审5 记录、方案v2 新增段、findings.md、生成视图、预飞 9）只作输入与对账依据，不评。

**读过的同类实现与上下文**（判断惯例与核对契约的依据）：`ee/internal/identity/accounts.go` 全文（`CreateAccount:73-105` 是"事务外算哈希 + 事务内重验"的同类实现；`page/encodeCursor:142-163` 是游标编码侧的共享实现）、`ee/internal/identity/service.go` 全文（`RequireAccountManager:198-209`、`authorize:160-166`、`manager:168-175`、`withHashSlot/hash:91-105`）、`ee/internal/identity/model.go:1-60`（`identity.Error` 枚举与 `SafeError` 的脱敏口径）、`ee/internal/app/identity.go` 全文（装配与错误上抛链）、`ee/cmd/bifrost-http/main.go:95-125`（启动失败的最终打印形式）、`ee/internal/identity/README.md`、`ee/internal/identity/persistence/README.md`、`ee/internal/bifrost/README.md`（核对文件职责表是否仍与现状一致）、`ee/AGENTS.md:5-20`、`workbench/规范/项目/编码规范.md` 全文、`product/架构/EE后端架构.md` 全文、`ee/docs/账号认证接口.md` 全文（修复后重读 `:36`）、`workbench/findings.md` 全文、`workbench/cases/账号认证/期1/方案v2.md` 的 §4.2、「Gate 决策结果」「实施偏差记录」及其后各轮处置段。

**核实过的关键假设**（结论依赖的事实都找到了证据行）：

- 旧管理员导入路径上 `ErrInvalid` 唯一等价于"哈希不合法"：`auth.go:69` 已先拒空用户名，而 `BootstrapLegacy`（`accounts.go:21`）只在 `username == "" || !ValidHash(hash)` 时返回 `ErrInvalid`，故 `:76` 的文案不会误报。
- `fmt.Errorf("%w", err)` 带出的内容确实脱敏：`model.go:13-26` 的 `identity.Error` 是七个固定常量串，`SafeError`（`:29-38`）把一切非业务错误折叠为 `ErrUnavailable`，`BootstrapLegacy` 的返回值已过 `SafeError`。
- 包装不破坏任何调用方：`ImportLegacyAdministrator` 在 `ee/` 内只有一个调用点 `app/identity.go:77`，它原样上抛，`main.go:115-118` 打印后退出；无 `errors.Is` 消费方，也无测试断言该文案（`auth_test.go:318-333` 只测 `validateLegacyFile`）。
- 列名做参数是本包现成形状而非新发明：同文件 `store.go:101` 的 `authRecord` 已经用 `Where(column+" = ?", value)` 传列名，`beforeCursor` 与之一致。
- 上一轮修复项在两个候选上都仍成立：`git diff --stat a8666448d..97e58c94a -- ee/AGENTS.md workbench/规范/项目/编码规范.md` 为空，`aa2286a11` 亦未触碰这两份文件；重读 `编码规范.md:37` 的 logger 硬规则条与 `ee/AGENTS.md:15-16` 的两条，措辞与顺序仍一致。
- 转来的命名项确已关闭：`git diff a89d01355..97e58c94a -- passwords.go` 只有一行，把 `if allowed := s.RequireAccountManager(ctx, p); allowed == nil` 改为 `if s.RequireAccountManager(ctx, p) == nil`；现状 `passwords.go:78` 复读确认无残留变量。
- 游标比较修复后全仓只剩一处：`grep -rn "id < ?" ee/` 修复前命中 `store.go:295,312` 两处，修复后只命中 `store.go:297`（即 `beforeCursor` 函数体内），两个调用点 `:302`、`:317` 均不再自带条件。

**起手表覆盖**（每行一个结果）：

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无（差量新增数字只在冒烟脚本：`limit:2`、`pages < 20`、`>= 3`，按硬规则"测试和脚本按契约独立书写字面量，不受此限"，且与该文件通篇字面量风格一致） |
| | 同一规则多份实现且互不注明 | **发现并已关闭**（本轮修复项 2：游标比较两份 → `aa2286a11` 收敛为 `beforeCursor` 一份） |
| | 重造已有 helper | 无（预检直接调既有 `RequireAccountManager`；哈希仍走 `s.hash`→`withHashSlot`；错误包装用标准库 `fmt.Errorf`；`beforeCursor` 是收敛既有重复，不是新造第二套分页） |
| | 派生常量无推导 | 不适用（本轮未新增任何常量） |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无（Go 侧无新增裸数字；脚本侧同上） |
| | 状态码、错误码裸数字 | 无（`auth.go:75` 用 `identity.ErrInvalid` 而非字符串比较；`store.go` 仍用 `pgUniqueViolation`；HTTP 映射未动） |
| | 无名的错误形状（以元组表示失败） | 发现但判为有理由保留（`passwords.go:77,125` 用 `hash == ""` 承载"预检是否通过"，理由见「看似可改但有理由保留」） |
| | 紧凑但绕的控制流 | 无（`:78` 去掉误名变量后条件直读；冒烟 `:227` 只有一层三元；`beforeCursor` 是"不满足条件原样返回 q"的直线形状，无闭包捕获） |
| | 文件集中或拆分是否妨碍理解 | 无（`store.go` 322→326 行、`service.go` 321 行，均在下方记录的 400 行预拆触发线与 500 行硬线之下；独立复评见「上一轮修复项与转交处置」T2） |
| | 多余状态或守卫 | 无（预检与事务内判定防的不是同一对象：前者只决定"要不要占哈希槽"，后者是权威判定并负责写失败事件，`:74-76` 已写明这一分工） |
| | 同一效果做两次（响应头、注册、初始化） | 无（两次鉴权是刻意的、职责不同；两处括号原属同一规则两份，已随修复项 2 关闭） |
| | 注释与现状矛盾（尤其鉴权、边界、装配处） | **发现并已关闭**（本轮修复项 1：`ResetPassword` 导出注释） |
| 架构与项目一致性 | 接缝形状与同类不同 | 无（`ResetPassword` 的"事务外算哈希 + 事务内重验调用方"与同包 `CreateAccount`（`accounts.go:76,82,88`）同形；唯一差别是失败处置方式，且差别本身已在 `:76` 注明来源方案 4.2） |
| | **架构符合性** | 无（见下节） |
| | **依赖关系** | 无（见下节） |
| | 绕过既有出口 | 无（错误仍走 `identity.Error`/`SafeError`→`identityhttp.Error`；查询仍走 `Repository`/`Store`；旧凭据导入仍走 `BootstrapLegacy` 的同一事务；未新开日志、存储或响应通道） |
| | 把上游现状当契约 | 无（R3 的显式括号正是把"GORM 会自动包裹 OR"这条上游隐式行为从依赖里切断，`beforeCursor` 的注释把这条理由固定在原地；`%w` 包的是 EE 自有的 `identity.Error` 常量，不是驱动原文） |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **发现并已关闭**（本轮修复项 1 的文档侧：`ee/docs/账号认证接口.md:36` 的 409 段落已补齐） |
| | 状态码口径不一 | 无（新分支复用既有 `ErrConflict`→409 映射，`protocol.go` 未动；无新增状态码） |
| | 省略、空、值三态语义不对应 | 无（冒烟 `:227` 对 cursor 用"首页不传键 / 后续页传值"，与接口文档"limit 省略默认 20、显式 0 或越界 400""`next_cursor=null` 表示结束"对应；`:233` 正是按 `None` 终止；`beforeCursor` 把"游标为空 = 从头开始"写进注释，与该三态一致） |

**开放式复读结果**：已做。以"半年后要改这里的人"的身份把四个文件各再读一遍（修复后对被改的两个文件再读一遍改动区间），逐文件回答"第一眼会让维护者皱眉的是什么"：

- `passwords.go` → 皱眉点是 `:68-69` 的注释与 `:125-129` 的新分支对不上（已列修复项 1，已关闭）；`:125` 用空字符串当判据是第二个皱眉点，复核后判为有理由保留并写明理由，避免下一轮再争。
- `store.go` → 皱眉点是两条一模一样的 `Where`（已列修复项 2，已关闭）。另有一条**表外发现，不列修复项**：`store.go:1` 的文件头职责句写"实现读取、锁定 state 的事务、限流预占与分页"，但文件里还有 `Passwords` 这个 `identity.PasswordHasher` 适配（`:22-36`）和两个驱动错误映射（`:49-69`）；`persistence/README.md` 的文件表倒是把"bcrypt 适配"列上了。因为本轮完全没有触碰这一行、也没有因本轮新增内容而恶化，按"不扩大到无关旧债"不作为本轮修复项，仅记录在此。
- `auth.go` → 皱眉点：本轮把 `:78` 改成带上下文的包装，但同一函数 `:58` 仍是 `return identity.ErrUnavailable` 这样一个无上下文的裸业务错误，运维在 `main.go:117` 只会看到 `failed to bootstrap server: unavailable`，看不出是读宿主 AuthConfig 失败。这属于失败可观测，按规则只转代码评审，不出结论。
- `identity-smoke.py` → 皱眉点：`:227` 的 `{'limit':2} if cursor is None else {'limit':2,'cursor':cursor}` 把 `limit` 写了两遍。复核后**不列**：这个形状恰好把"首页不传 cursor 键"这个契约点显式化（与文档"省略/显式"的三态口径一致），改成先建 dict 再按需塞键反而弱化了这层意思；且该文件通篇是同样的紧凑风格。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 1 条，已关闭：keyset 游标比较在 `store.go` 两处各一份、互不注明，本轮 R3 被迫改两处 | 本轮修复项 2 |
| 职责与实现质量 | 1 条，已关闭：`ResetPassword` 导出注释与新增分支矛盾 | 本轮修复项 1 |
| 架构与项目一致性 | 无 | 见下方两段专项核对 |
| 契约一致性 | 1 条（与修复项 1 同源的文档侧），已关闭：接口文档 409 的重试指引对新成因相反 | 本轮修复项 1 |

**架构符合性**（对照 [EE 后端架构](../../../../product/架构/EE后端架构.md)）：

- §4「模块内部的职责」：本轮全部改动（含修复提交）落在各自该在的层。业务规则（预检顺序、竞态回滚）与其注释改在模块根包 `identity/passwords.go`；SQL 表达式与新抽出的 `beforeCursor` 改在 `identity/persistence/store.go`，未泄漏到业务层；错误到启动提示的映射改在宿主接入层 `internal/bifrost/auth.go`；对外口径改在 `ee/docs/`。没有把业务判断挪进 Handler，也没有让 `app` 承载运行期编排——`app/identity.go:77` 对导入失败仍只是 `return nil, err`，装配职责未变。
- §4 文件粒度（按编码规范「文件组织以阅读为先」核实，不按架构示例当清单）：`store.go` 因抽出 `beforeCursor` 由 322 增至 326 行，`service.go` 321 行不变，均未到 500 行硬线，也未到下方记录的 400 行预拆触发线；`beforeCursor` 紧贴两个唯一调用方放在同文件，没有为一个 7 行函数新建文件，符合"需要一起理解、一起修改的代码放在一起"。
- §5「事务范围由业务服务决定」：新增的预检是两次事务外只读（`RequireAccountManager` → `AuthByID` + `State`），权威判定与写入仍整体在 `s.repo.Transaction` 的同一个闭包内（`passwords.go:87-132`），`replacePassword` 的"写哈希 + 升版本 + 撤会话 + 写事件"四步没有被拆开，`*gorm.DB` 也没有外泄到业务层——`beforeCursor` 的 `*gorm.DB` 参数完全留在 `persistence` 包内，未进入任何导出符号的签名。
- §7「与 Bifrost 的协作边界」：宿主接缝（`ConsoleAuthFactory` / `ConsoleAuthProvider`、`GetAuthConfig` 快照优先级）未变，本轮未触碰任何上游文件。`fmt.Errorf` 包装与 `beforeCursor` 都没有另建一套错误或查询基础设施，仍复用 `identity.Error` 与宿主 GORM 连接这两个既有出口。

**依赖关系**（对照架构 §5、§7 与编码规范「依赖关系」）：

- 方向：`identity/passwords.go` 的 import 仍只有 `context` 与 `errors`（实测 `grep -rn "gorm|fasthttp|server\."` 在 `passwords.go`/`accounts.go`/`service.go` 无命中），业务根包没有因本轮改动反向导入 http/persistence/bifrost 任何一个。`internal/bifrost/auth.go` 新增的是标准库 `fmt`，仍是 `bifrost → identity` 单向。修复提交未新增任何 import（`gorm` 在 `store.go:17` 早已存在）。
- 窄接口与显式注入：`ImportLegacyAdministrator` 的签名未变，仍由 `app/identity.go:77` 在构造完 `service` 后显式传入 `log Logger`（`auth.go:38-41` 只声明 `Info`）；本轮没有从全局 Server/Config 取依赖，也没有新增导出符号或配置项——`beforeCursor` 是未导出的包内函数，符合"包外没有使用方的符号不导出"。
- 跨模块与资源所有权：未跨模块读写表，未触碰 `branding` 或宿主的表；数据库连接仍由宿主持有与关闭（`store.go:38,44-47` 注释与实现未变）。预检新增的两次读走的是同一个 `s.repo`，没有绕开 `Repository` 直接用连接。

## 出口清单

### 本轮修复项

**1. `ResetPassword` 新增了第二种 `ErrConflict` 成因和一条业务错误回滚路径，导出注释与接口文档都还停在旧口径 —— 已修复并关闭（`aa2286a11`）。**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/passwords.go:67-70`（注释）与 `:125-129`（新分支）；`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/docs/账号认证接口.md:36`。
- **原代码事实**：`:68` 写「只有存储故障才回滚」，`:69` 把 `ErrConflict` 的成因写成「同 ID 不同 actor/target」这唯一一种。评审候选 `97e58c94a` 的 `:125-129` 新增 `if hash == "" { return ErrConflict }`，从 `s.repo.Transaction` 的闭包内返回业务错误，整笔回滚且不写事件——既是第二种 `ErrConflict` 成因，也是一条非存储故障的回滚路径；两行注释在该候选上未被同步改动。文档侧 `:36` 对 409 的指引是「ID 复用于其他 actor/target 返回 409；失败事件同样占用 ID，再次尝试使用新 ID」，而新成因的正确重试方式恰恰是**同一** operation_id（代码 `:127` 自己写明了这一点）。
- **原维护影响**：编码规范硬规则两条同时被碰到——「导出符号必须有用途注释；特殊返回值必须交代」与「修改代码时必须同步检查并更新相关注释，与代码不一致的注释视为缺陷」；`ee/AGENTS.md:13,17` 也把这两条列为逐条核对项。具体代价：下一个读 `ResetPassword` 的人会按 `:68` 断定"拿到 `ErrConflict` 说明不是存储问题、事务里该写的都写了"，而新分支恰恰什么都没写；按 `:69` 断定 409 只有一种成因，就不会去想同 ID 重试这条路。这不是措辞偏好，是注释在教人做出与实现相反的判断。文档侧则是同一处的对外半边，三方（代码 / 函数注释 / 开发指南）口径不一。
- **修复结果与亲核依据**（只读 `git diff 97e58c94a..aa2286a11 -- ee/`，并重读两处现状）：
  - 注释现状 `passwords.go:68-70`：「…随后作为错误返回；**存储故障与下述预检冲突才回滚**。同一 actor/target 用同一 operation_id 重试时原样返回首次结果，不再改密。**`ErrConflict` 有两种成因：同 ID 不同 actor/target（换新 ID 重试）；以及预检拒绝而事务内放行的权限竞态，此时整笔回滚、不写事件，用同一 ID 重试即可。**」两种成因与各自的重试方式都写出来了，与 `:125-129` 的分支及其原地注释（`:126-128`「整笔回滚不写事件，调用方用同一 operation_id 重试即可」）逐字对得上。
  - **逐条比对注释与实际分支，无新的口径冲突**：我把 `ResetPassword` 事务闭包里所有产生错误的出口列了一遍——`:89-91`（会话重读失败，无事件）、`:95-97`（同 ID 不同 actor/target，`ErrConflict`，无事件）、`:103-105` 与 `:109-111`（存储故障）、`:121-124`（业务失败写事件后提交）、`:125-129`（新竞态分支，`ErrConflict`，无事件）、`:130-131`（`replacePassword` 存储故障）。对读者最关键的那个问题"拿到错误时是否已写失败事件"，新注释在每一类上都给出正确答案：只有 `:121-124` 这一类写事件并提交，两种 `ErrConflict` 都明确标注无事件/换 ID 或同 ID 重试。
  - **残留说明（已核实，不重开）**：「存储故障与下述预检冲突才回滚」仍未把 `:89-91` 与 `:95-97` 也算作回滚路径（两者同样会回滚 `s.transaction` 在 `store.go:234` 打的 `revision` 自增）。但这两条路径都不写任何业务可见状态，回滚与否不改变调用方能观察到的任何事实，且二者均早于本轮存在、本轮未使其恶化。按"最小修正、不扩大到无关旧债"，我判定这不构成未关闭项，记录在此以免下一轮重复推演。
  - 文档现状 `账号认证接口.md:36` 末句：「**409 也可能表示权限在服务端两次判定之间发生变化，此时未写入任何事件，用同一 ID 重试即可。**」三点与代码一致：整笔回滚 → 未写事件 ✓、幂等 → 同 ID 重试 ✓、成因 → 两次判定之间权限变化 ✓。与紧邻的前一句「失败事件同样占用 ID，再次尝试使用新 ID」不冲突——新句用「此时未写入任何事件」把两种情形显式区分开，读者不会把两条指引混用。文档其余段落（错误码表的「409 conflict」、重置接口行）未被触碰，无第二处需要同步。
  - 未夹带：`git diff --stat 97e58c94a..aa2286a11` 中 `ee/` 部分只有 `passwords.go`（+3-2，纯注释）、`store.go`（修复项 2）与该文档一句；`passwords.go` 的可执行代码逐行比对与评审候选完全相同，`gofmt -l` 无输出。
- **结论：关闭。**

**2. keyset 游标的比较条件在 `store.go` 两处各写一份、互不注明 —— 已修复并关闭（`aa2286a11`）。**

- **位置**：修复前 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:294-296` 与 `:311-313`；修复后 `:291-298`（`beforeCursor`）、`:302` 与 `:317`（两个调用点）。
- **原代码事实**：两处除列名（`created_at` / `occurred_at`）外完全相同：`if c.ID != "" { q = q.Where("(<col> < ? OR (<col> = ? AND id < ?))", c.At, c.At, c.ID) }`，且各自紧跟同样成对的 `Order("<col> DESC, id DESC")`，两处都没有注释互相指向。`grep -rn "id < ?" ee/` 确认全仓只有这两处。本轮 R3 的括号修正就是把同一个修改在两处各做一遍。游标的**编码**侧反而是共享的一份（`accounts.go:160-163` 的 `encodeCursor`，被 `accounts.go:180` 与 `passwords.go:197` 共用），解码/比较侧却是两份手抄，这个不对称本身就说明代码已经把游标当成一个概念。
- **原维护影响**：编码规范硬规则「同一条规则只在一处实现。同一校验、同一比较、同一重试或转换逻辑只有一份；多处需要时抽成包内函数并互相引用」，该节明写"违反即为本轮修复项，不适用不机械消灭数字的校准豁免"。代价是具体的、本轮刚发生过一次：抄错括号得到的是一条语法合法、类型正确、编译通过、只有在跨页时才泄漏数据的 SQL——除了逐句读没有任何机制能拦住。再加第三张可分页的表就要第三次手抄。
- **修复结果与亲核依据**（只读 `git diff 97e58c94a..aa2286a11 -- ee/`，并重读 `store.go:285-326` 现状）：
  - **规则确实收成一份**：新增未导出函数 `beforeCursor(q *gorm.DB, column string, c identity.Cursor) *gorm.DB`（`:293-298`），`Accounts`（`:302`）与 `Events`（`:317`）都改为调用它，各自不再自带条件。`grep -rn "id < ?|created_at < ?|occurred_at < ?" ee/` 修复前命中两处、修复后只命中 `:297`（函数体内那一处），`grep -rn beforeCursor ee/` 确认只有一处定义、两处调用，全仓无第三份手抄。
  - **生成的 SQL 与修复前逐字等价**，我按四点比对：①条件字符串完全照搬，包括 R3 加的外层括号与列名两次出现，只把字面列名换成 `"("+column+...` 拼接，两个调用点分别传 `"created_at"` / `"occurred_at"`，展开后与修复前的两个字面量字符串逐字相同；②三个占位参数的顺序仍是 `c.At, c.At, c.ID`，未变；③守卫由 `if c.ID != "" { 施加 }` 反写为 `if c.ID == "" { return q }`，覆盖的集合相同，空游标时原样返回入参 `q`，与修复前"不追加 Where"等价；④`Events` 的 Where 追加顺序仍是先 `target_id`（`:315-316`）后游标（`:317`），与修复前一致，服务端强制的 target 限定没有被挪到游标之后或丢失——这正是 R3 冒烟断言保护的那条组合。
  - **两个调用点的 column 与 Order 仍正确配对**：`Accounts` 传 `"created_at"`，紧接 `:303` `Order("created_at DESC, id DESC")`；`Events` 传 `"occurred_at"`，紧接 `:318` `Order("occurred_at DESC, id DESC")`。两处的排序键也仍与编码侧取值一致（`accounts.go:180` 用 `rows[n-1].CreatedAt`、`passwords.go:197` 用 `rows[n-1].OccurredAt`），三层（编码字段 / 比较列 / 排序键）逐个对上，无交叉。
  - **函数注释交代了我要求的两件事**（`:291-292`）：「column 必须与调用方随后的 Order 排序键一致，否则翻页会漏行或重复」把原先只存在于阅读者脑子里的配对关系显式化；「括号显式写出，不依赖 GORM 包裹 OR 表达式」把 R3 的修复理由钉在原地，防止后人"简化"掉外层括号。首句「是分页的唯一比较规则」现在是可验证的事实（见上条 grep），不是空话。
  - **形状与本包惯例一致、未引入新风险**：列名做字符串参数与同文件 `authRecord`（`:101` `Where(column+" = ?", value)`）同形，不是新惯例；两个调用点传的都是编译期字面量，`column` 不来自任何请求输入，字符串拼接没有引入注入面；`gorm` 早在 `:17` 已 import，修复未新增依赖。
  - 未夹带：`store.go` 本次净增 4 行（322→326），除上述结构调整外无其他改动；`gofmt -l ee/internal/identity ee/internal/bifrost` 无输出；`git status --porcelain` 复核前后均为空。
- **结论：关闭。本轮无未关闭修复项。**

### 需用户裁决的 Blocking

- 无。两条修复项都在已批准范围内，不改目标、契约、表结构或风险接受口径；修复后核实亦未产生需要取舍的新事项。

### 立债(REF:立 `triggered_by: [<本case>]` 的需求进 reports/)

- 无。

### 挂账(Finding:登记 findings.md,带四要件)

- 无新增。FIND-017 / 018 / 019 的对账见「台账对账」。

### 转代码评审(范围外发现,一行一条,不出结论)

- `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/bifrost/auth.go:58` 在本轮把 `:78` 改为带上下文的包装之后，同一函数读宿主 AuthConfig 失败时仍是 `return identity.ErrUnavailable`，经 `app/identity.go:77` 原样上抛后在 `main.go:117` 打印为 `failed to bootstrap server: unavailable`，不含来源阶段。

## 看似可改但有理由保留

- **`passwords.go:77,125` 用 `hash == ""` 而不是显式布尔承载"预检是否通过"**：看起来应该拆成 `authorized := s.RequireAccountManager(ctx, p) == nil`，但现状多守住了一件事——它同时保证任何情况下都不会把空哈希交给 `replacePassword`（`:130`）写进 `ee_identity_accounts`，换成布尔后这层就没了。`:126-128` 已经用注释交代了该分支的业务含义与重试方式，修复项 1 之后函数级注释也写明了这条成因，`grep -rn 'hash == ""' ee/internal/` 确认包内没有第二处依赖这个哨兵而产生歧义。保留，理由写在这里以免下一轮重提。
- **`ResetPassword` 预检失败后不提前返回，而 `CreateAccount`（`accounts.go:76`）提前返回**：两者用同一个 `RequireAccountManager`，形状却不同。这是刻意的：重置必须为被拒绝的调用写失败事件（方案 4.2），提前返回就会把这条契约删掉，`:76` 的注释已经把"为什么不直接返回"写在原地。这正是防止后来者顺手"统一"的护栏，保留。
- **`ResetPassword` 事务内用 `current(tx.AuthByID, p)` + 稍后 `s.authorize(tx.State(), actual)`，而不是 `s.manager(tx, p)`**：`manager`（`service.go:168-175`）把两步合成一步并直接返回错误，而这里需要先拿到 actor 身份去填 `event.ActorID/ActorName`（`:106-107`），再把授权结果收进 `outcome` 写事件。不是两份规则——授权判定本身仍只有 `authorize` 一份实现。保留。
- **`service.go:198` 的 `RequireAccountManager` 注释写"供宿主对管理路由使用"，而它现在也被包内的 `CreateAccount`、`ListAccounts` 和本轮的 `ResetPassword` 调用**：注释比实际用途窄，但这在本轮之前就已经如此（`accounts.go:76,168` 早有内部调用），本轮只是多了一个内部调用方，未使其恶化；按"不扩大到无关旧债"不列修复项，记录在此说明已看过。
- **`beforeCursor` 返回 `*gorm.DB` 而不是返回条件片段由调用方 `Where`**：返回构建器本身会让"这个函数可能什么都不做"成为调用方必须知道的隐含语义，但注释首句已写「游标为空表示从头开始」，且返回片段的替代方案要么得返回 `(string, []any)` 这种无名元组、要么得让调用方自己拼括号——正是这次要消灭的东西。保留。
- **`identity-smoke.py:225-235` 的翻页断言用字面量 `2` / `20` / `3` 而不是命名常量**：编码规范硬规则明写"测试和脚本按契约独立书写字面量，不受此限"，且该文件通篇如此（`:255` 的 `30`、`:246` 的 `37`、`:287` 的 `5`）。保留。

## 台账对账

- **FIND-017（SQLite busy 重试在 branding 与 identity 各一份）**：本轮差量与修复提交均不含 `branding/persistence/migration.go` 与 `identity/persistence/diagnostics.go`，两侧互相注明的现状未变。未恶化，不重提。
- **FIND-018（`ReserveLogin` 复用 `s.transaction` 抢全局写锁）**：`store.go:262-289` 本轮未被触碰（修复项 2 的改动全部在 `:291` 之后）。本轮 `ResetPassword` 新增的两次预检读是事务外只读，不经 `state.revision` 写锁，未加重该路径的争锁。未恶化。
- **FIND-019（哈希槽耗尽与登录限流共用 `Retry-After: 60`，已接受/豁免）**：`protocol.go` 本轮未改，契约口径不变；R1 让未授权重置不再进入哈希槽，暴露面比登记时更小。未恶化，按豁免不重提。
- **FIND-012（`attach` 惯性点）/ FIND-014（`ee` main.go 是上游抄本）/ FIND-016（上游 `callbacks` 不可替换）**：本轮差量与修复提交均不含 `app/bootstrap.go`、`cmd/bifrost-http/main.go` 与任何上游文件，均未触及。
- **收敛评审6 的三条「转代码评审」**（品牌冒烟改继承后端口复用、旧登录路径冒烟覆盖变化、离线恢复 stdin 上限由 1025 收紧为 74）已由代码评审5 亲核撤回并记入方案v2，本侧不再处置。
- 方案「Gate 决策结果」「实施偏差记录」「用户复核：整体收敛纠偏」中已裁决的事项（默认密码 `123456` 与会话规则、后端先行、删除 `Service.Account`、`limit` 显式 0 报 400、业务类型不带标签等）本轮均未重提；本轮提出并已关闭的两条与 findings.md 现有条目无重合，不需新增登记。

## 上一轮修复项与转交处置

**收敛评审6「本轮修复项」逐条**（该轮只有一项）：

1. `ee/AGENTS.md` 的规则清单与编码规范硬规则在 logger 一条上分叉 —— **仍关闭**。`git diff --stat a8666448d..97e58c94a -- ee/AGENTS.md workbench/规范/项目/编码规范.md` 输出为空，`aa2286a11` 的文件清单里也没有这两份文件，本轮两个候选都未回退该修复。重读现状核实：`编码规范.md:37`「**日志经宿主注入的 logger 输出。** `ee/` 内不使用标准库 `log`；需要日志的包按实际用到的方法定义窄接口，由 `app` 把入口配置好的 logger 注入（架构 §7「按缺口扩展」）」仍在"违反即为本轮修复项"的硬规则列表内；`ee/AGENTS.md:15-16` 仍是拆开的两条、日志在前行数在后，与编码规范顺序一致。本轮改动的文件未引入标准库 `log`（`passwords.go`/`store.go` 的 import 未变，`auth.go` 新增的是 `fmt`）。

**代码评审5「转收敛评审」逐条**（任务书原样转入的两条）：

- **`ee/internal/identity/passwords.go:78` 以 `allowed` 命名一个 `error` 值 —— 已在评审候选中关闭**。`git diff a89d01355..97e58c94a -- ee/internal/identity/passwords.go` 只有一行改动，把 `if allowed := s.RequireAccountManager(ctx, p); allowed == nil` 改为 `if s.RequireAccountManager(ctx, p) == nil`；重读 `passwords.go:78` 现状确认变量已整个删除，包内 `grep` 无残留，修复提交 `aa2286a11` 未改动该行。我独立复核这个修法本身：该变量只有一个使用点，删掉后条件直接读作"预检通过"，比改名成 `err`（会和闭包内 `:88` 的 `err` 语义撞车）或改成 `denied`（要反写）都更干净，没有引入新的可读性债。判定：关闭。
- **`store.go` 与 `service.go` 仍越过编码规范「300 行须评估拆分」阈值 —— 已评估，判定保持集中，不列修复项**。实测行数：评审候选上 `store.go` 322、`service.go` 321（`git show a8666448d:` 核实与上一轮同值；代码评审5 记的 324 与实际差 2 行，不影响结论），修复后 `store.go` 326、`service.go` 321，均在 500 行硬线以下，规范对 300–500 区间的要求是"评估"而非"必拆"。我独立复评了两种可能的拆法：`store.go` 若把 `transaction` 的 16 个 `Tx` 方法（`:132-220`）单独成文件，会把 `transaction` 类型、它的 `stage` 字段与唯一驱动者 `s.transaction`（`:228-259`，靠 `t.stage` 生成失败诊断）分到两个文件，读一次"事务里第几步失败"就要跨文件跳转，正是编码规范「拆分前检查」要避免的；`service.go` 若把 `randomID/randomToken/digest/withHashSlot/hash/compare/validPassword` 抽成第三个文件，`accounts.go` 与 `passwords.go` 两个业务文件每次都要回头跨文件读辅助，同样是负收益。判定：维持现状。触发条件写在这里供下一轮直接引用——**任一文件下次新增代码使其达到 400 行时，按主题预先拆分**（`store.go` 按"Tx 方法 / 事务驱动与分页"，`service.go` 按"会话规则 / 共享辅助"），不要等到撞 500 行硬线才被迫拆。

## 实际静态阅读范围与未执行的检查

**读了什么**：见「事实来源」三段（差量四文件完整现状 + 修复后对被改两文件的改动区间重读、同类实现与上下文共 13 个文件/片段、规范与架构文档四份、台账与方案的裁决段、接口文档全文及修复后的 `:36`）。

**跑了什么**：见「事实来源」的命令清单，全部为 `git` 只读查询、`grep`、`wc`、`sed -n` 阅读与 `gofmt -l`（不带 `-w`，不写文件）。复核前后 `git status --porcelain` 均为空，未创建、修改、暂存或提交任何文件。

**未执行的检查**：①未运行 `go build` / `go vet` / `go test` / 任何冒烟脚本或服务——按任务书只读边界，且这些属代码评审范围；实施方在 `aa2286a11` 上跑过的 `go vet`、`-race` 单测与四套冒烟按要求**未作为本记录的结论依据**，本轮两条修复项的关闭结论全部建立在上述只读比对之上。②未用 GORM DryRun 或真实数据库实测 `beforeCursor` 生成的 SQL 串——改为按"条件字符串逐字比对 + 占位参数顺序比对 + 守卫集合等价 + Where 追加顺序比对"四点做静态等价论证（依据见修复项 2），结论按"静态等价推断"记录；该函数的运行期行为由 R3 已有的成员事件翻页冒烟断言覆盖。③未对 `ErrConflict` 竞态分支做任何动态验证（无法在只读前提下注入故障，且该分支的可达性与压测证据由代码评审5 第 3 节给出）。④未复核 `workbench/` 下评审记录、`SUMMARY.md`、`nav.js` 等生成物与流程文档的内容正确性（不属本评审范围，仅用作裁决与对账输入）。⑤未重做整包评审——按差量轮规则，`a8666448d` 之前已评审过的文件（`service.go`、`accounts.go`、`model.go`、`app/*`、`http/*`、`rows.go` 等）只在需要核对惯例、契约与依赖方向时定点阅读，未对其重新出结论。
