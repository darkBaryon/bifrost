---
title: 按人用量与额度 完整后端蓝图 v3 方案评审 #4
type: 方案评审
case: 按人用量与额度
phase: 1
round: 4
verdict: 通过
created: 2026-09-23
---

## 结论

**通过。评审3的 B1、B2 已在蓝图层关闭，相关变更未引入新的实质阻塞。**

18期的交付、依赖、验收、启用边界及预算已形成完整规划。此结论不表示功能已经实现、测试通过或获得部署授权；本轮仍停在规划阶段。

## 范围与独立性

独立读取蓝图 v3、v2→v3 差异及当前产品输入，按上一轮亲核的基线接口检查修订是否回应实际缺口。不重开已经成立的细节，不要求提前提供后期精确签名。

当前 HEAD 仍为 `5c8d27d53030ca8c84b612b5b4d07a5c67c70b7f`。相对功能基线，EE及上游代码目录无差异。本轮未修改文件、运行数据库或真实调用，也未联网。

## 阻塞项关闭核查

| 项目 | 亲核结果 |
|---|---|
| B1：调用范围不明确 | [调用类别边界](/Users/xinyue/.codex/worktrees/user-usage-rebuild/bifrost-user-usage/workbench/cases/按人用量与额度/开发蓝图v3.md:149) 已逐类说明安排。Rerank、OCR、Compact 纳入期6专项验证并随期9联合验收；普通异步明确为网关 `/v1/async/*`，不能绕过内部操作的支持范围。Provider Responses background 与视频任务明确不纳入本次范围，不再隐含于普通异步。**关闭。** |
| B2：Batch 复用职责缺失 | [Batch适配职责](/Users/xinyue/.codex/worktrees/user-usage-rebuild/bifrost-user-usage/workbench/cases/按人用量与额度/开发蓝图v3.md:163) 明确 EE 生命周期、聚合存储适配、未定价结果接入、补价重驱及与日志插件并存时的协调责任。期17、18分别承担独立结算与恢复重算，未再假定只接 `ReportUsage` 即可。**关闭。** |
| 早期绑定启用 | [启用顺序](/Users/xinyue/.codex/worktrees/user-usage-rebuild/bifrost-user-usage/workbench/cases/按人用量与额度/开发蓝图v3.md:96) 明确期3仅隔离验收，生产绑定随期9完整闭环推进，消除了提前绑定却缺少运行时保护的歧义。 |
| Batch 启用与依赖 | 期18依赖期17，期16—17保持受管 Batch 生产入口关闭，完成期18及联合验收后才具备启用条件。未发现向后依赖或提前开放。 |

B1 的关闭依据是本次范围已经明确，不是把未实现类别认定为已支持。后续交付说明应按该边界表述“完整后端”，避免被理解为覆盖网关所有 Provider 任务。

## 预算与影响范围

独立计算18期预算：

- 生产：6,080行。
- 测试及实验：6,251行。
- 合计：12,331行，其中上游预算1,150行。
- 各行生产与测试加总、上游包含关系均正确。

期6增加200行后为950行；原期17拆成900行和700行两期，新增职责已有预算归属。新增上游预算仍以当期缺口论证为前提，没有形成自由改动上游的授权。

## 建议与剩余风险

无新增阻塞或建议。

实际接入规模、Batch并存协调及未定价重算仍须在当期方案和实现中验证。当前预算是规划估算；预计明显超出约1,000行时，应先修订当期边界，不通过削减测试、预埋实现或事后拆提交维持数字。

## 验证记录

实际执行了文档差异核对、Git基线及代码目录差异检查，并使用只读 Python 脚本重新计算18期预算。没有执行功能测试、构建或预飞；未将规划核查记为运行验证通过。
