---
title: 按人用量与额度 期1 方案评审 #2
type: 方案评审
case: 按人用量与额度
phase: 1
round: 2
verdict: 通过
created: 2026-09-23
---

## 结论

**通过。方案评审1的阻塞项 B1 已在方案层关闭，本轮未发现新增实质阻塞。**

本结论仅确认 v2 的实施与验收边界成立，不表示 PostgreSQL、HTTP 或模板功能已经实现或测试通过。仍须完成蓝图及本期范围的用户审阅，再进入实施。

## 独立性与范围

独立读取 v1、v2、更新后的 checklist，并核对差异及预飞脚本的执行方式；未仅依据作者说明作判断。基线仍为 `d93fa8ad2082c716f76c55720b2272e718a7f649`。

本轮只复核 B1 关闭情况及相关变更影响；未修改文件、运行真实 Go 测试、连接数据库或访问网络。

## 差量核查

| 项目 | 亲核结果 |
|---|---|
| B1：PostgreSQL 验收 | v2 固定 `TestTemplatesContract`，显式 PostgreSQL 模式要求 DSN 和隔离标记，缺项必须失败，禁止回退 SQLite 或 Skip；清单增加独立执行项并验证顶层 PASS。已关闭。 |
| 数据库隔离 | 明确先建立本任务临时实例与空数据库，记录归属，再创建唯一 schema；清理范围限于本次资源，不将连接凭据写入证据。没有授权连接既有共享库。 |
| 验收覆盖 | 专项包含迁移首次及重跑、CRUD、版本冲突、撤权拒绝和重建连接后读取。普通全量测试不能替代该项。 |
| 接线与权限 | 增补构造关系、组合 AdditionalRoutes、保留 RBAC 路由、Cookie 专用认证及其他预置角色不自动扩权，未扩大一期业务范围。 |
| 模板合同 | 补齐模型名含 `/`、大小写、通配、未配置 provider 及 UTC 自然周期解释；本期仍仅保存配置。 |
| 预算 | 101 行复用实验已计入本分支约 1,000 行审阅目标，PG 夹具和验证也计入测试预算。实际规模仍须实施时核对。 |

证据：[方案v2.md:84](/Users/xinyue/.codex/worktrees/user-usage-rebuild/bifrost-user-usage/workbench/cases/按人用量与额度/期1/方案v2.md:84)、[checklist.yaml:19](/Users/xinyue/.codex/worktrees/user-usage-rebuild/bifrost-user-usage/workbench/cases/按人用量与额度/期1/checklist.yaml:19)。

## 验证记录

独立解析 YAML，取出实际 `postgres-contract` 命令并执行 `bash -n`，结果通过。

随后用 Bash 函数替代 `go`，以固定文本验证清单控制逻辑，没有执行 Go 或建立数据库连接：

| 模拟结果 | 清单退出码 |
|---|---:|
| 指定测试 PASS | 0 |
| 指定测试 SKIP | 1 |
| 未匹配任何测试 | 1 |
| 输出 PASS，但 Go 命令失败 | 1 |
| 缺少 DSN | 1 |
| 缺少隔离标记 | 1 |

这些检查证明清单能拦截原 B1 所述误报情形；实际测试是否使用 PostgreSQL、执行完整断言且没有跳过，仍由实施后的代码评审及运行证据确认。

## 建议与剩余风险

无新增建议或阻塞。首轮关于预算偏紧的提醒继续有效：判断是否超量时，应将功能、测试、接线及 101 行实验合计；预计明显超出约 1,000 行时先调整当期边界，不削减必要验证。
