---
title: 内容安全框架 期1 收敛评审 #6
type: 收敛评审
case: 内容安全框架
phase: 1
round: 6
verdict: 无碍
created: 2026-09-17
---

> 评审者与实施上下文隔离（不是写代码的会话自评）。基线为 `7c81459aa0e62750f22f57b57c3c4a4750a98cfc`。本轮是差量复核：上一轮候选 `f6c9437e657af58e15204412fd2421ed98c5a81e`，本轮候选 `cf1a35646b7abd6dec8dbe9a871ee42649fa267e`。
> - `git rev-parse HEAD` 与本轮候选一致。
> - 评审开始和结束时，`git status --porcelain=v1 --untracked-files=all` 都没有输出：没有未提交或未跟踪的文件。
>
> 风险等级 L2，四维核查写成段落加表格。

## 结论

**总体判断：本轮适合交付，结论为无碍。**

上一轮的 4 个修复项（F8–F11）都已关闭，关闭方式与建议的最小修正一致：
- **F8：** 测试名、注释和失败信息已改用 Checker / RuleEvaluation。
- **F9：** 检测器 ID 和拒绝状态码改为命名常量，并放进原有的 const 块。
- **F10：** 删除了复述父包的子包 README，仓库里的现行文档没有留下断链。
- **F11：** README 文件表里，插件入口的三个文件已排在一起。

本轮改动只涉及上面这些命名、常量和文档整理，没有改变装配结构、依赖方向或任何运行行为。本轮没有新的修复项，也没有需要用户裁决的事项。

## 事实来源

**执行的命令**（均为只读）：
- `git status --short --untracked-files=all`（开始时）、`git status --porcelain=v1 --untracked-files=all`（结束时）：两次都没有输出。
- `git rev-parse HEAD`：`cf1a35646b7abd6dec8dbe9a871ee42649fa267e`。
- `git diff --stat f6c9437e6..cf1a35646`：共 15 个文件。
  - ee 下有 4 个，与评审范围清单一致。
  - 其余 11 个是 workbench 过程文档与生成物，不在被评范围内。
- `git show cf1a35646 --stat`：单个提交，文件与上面一致。
- `git diff f6c9437e6..cf1a35646 -- ee`：读了全部差量。
- `git diff f6c9437e6..cf1a35646 -- workbench/findings.md workbench/cases/内容安全框架/期1/方案v2.md`：用于核对裁决和登记。
- 用 `git grep` 核查了以下几项：
  - `Engine|Event|事件` 在 guardrails 包与 app 护栏文件中的残留；
  - `valyala/fasthttp`、`net/http` 在 `ee/internal/app` 与 `ee/go.mod` 中的使用；
  - ee 内 `fasthttp.Status*` 的用法；
  - `fake/README` 在全仓的引用；
  - `StatusCode`、`minErrorStatus` 在插件中的定义；
  - findings.md 的条目清单；
  - 方案v2 与 EE 后端架构的章节标题。
- 另用 Grep 工具在 workbench 以外查了 `fake/README`、`detectors/fake)`、`[guardrails-test]`。

**完整通读的范围内文件（现状），及各自职责：**
- `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails.go`（70 行）：读取 `EE_GUARDRAILS_FAKE`，构造开发规则、检查器和插件，再调用宿主的 `SyncLoadedPlugin` 注册。
- `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md`（110 行）：模块用途、整体流程、文件表、开发联调说明、接入约束。
- `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker_test.go`（271 行）：规则顺序、阈值、失败、取消与快照语义的测试。
- `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/fake/README.md`：本轮已删除。

**为核对而读的相关文件：**
- `ee/internal/guardrails/detectors/fake/detector.go`（全文）：删除 README 后，只剩包注释和类型注释说明假检测器，确认信息没有丢失。
- `ee/internal/app/bootstrap.go`（全文）：护栏装配的调用点在 `attach` 的第 50–52 行，本轮没有变化。
- `ee/internal/guardrails/plugin/plugin.go:17,35` 与 `deny.go:26,45`（grep）：确认 `Options.StatusCode` 是 `int`，允许范围为 400–599。`fasthttp.StatusBadRequest` 是无类型整数常量，直接可用。

**规则与裁决：**
- 已读：
  - 根 AGENTS.md、workbench/AGENTS.md；
  - 收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md、收敛评审模板；
  - EE后端架构.md 全文；
  - findings.md 的 FIND-022 至 FIND-029，其余条目只看了清单；
  - 方案v2 第 70–174 行：「Gate 决策结果」「实施偏差记录」「用户阅读与开发联调后的补充记录」「第五轮补审修复记录」。
- 已经裁决、本轮不重提的内容：
  - 2026-09-17 的用户裁决：
    - 开发假检测器、app 注册与 HTTP/Playground 联调在本期范围内；
    - 命名定为 Checker/CheckResult/RuleEvaluation；
    - README 保留宏观流程与使用说明，长度按用户裁决。
  - 输出规则开启时不支持流式请求（Gate 1 方案 1）。
  - FIND-026 至 FIND-029：上一轮的 S1、S2 已分别登记为 FIND-028、FIND-029。
- 上一轮记录 `收敛评审5.md` 已全文读过，只用于逐条处置 F8–F11 和核对保留项。按要求没有读其他旧评审。

**同类实现对照：**
- **状态码写法：** ee 内一律使用 `fasthttp.StatusXxx`，例如 `branding/http/handler.go:108`、`identity/http/protocol.go:112-122`、`host/config.go:32`。`guardrails.go:30` 改为 `fasthttp.StatusBadRequest` 后与此一致。
  - `fasthttp` 已是 `ee/go.mod:14` 的直接依赖，没有新增依赖。
- **环境变量与配置常量：** 本轮新增的 `fakeDetectorID`、`fakeDenyStatus` 放进原有的 const 块（`guardrails.go:19-33`），并配有注释。这与 `app/identity.go`、`app/pricing.go` 用 const 块集中定义配置常量的做法一致（上一轮已核对，本轮没有变化）。
- **子包是否需要 README：** 编码规范「模块根包要有 README」一节规定子包按需编写。`ee/internal/identity/hasher` 等适配子包也没有单独的 README；本轮删除 fake 子包 README 后，与此一致。

**起手表覆盖：** 见文末。

**开放式复读：** 已做，没有表外发现，详见文末。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无。上一轮 F9 的检测器 ID 重复已消除；F10 的三处重复描述已减为两处（父 README 与代码注释），属于正常的文件表加 godoc 关系 | `guardrails.go:26,41,54`；README:68；`fake/detector.go:1,14` |
| 职责与实现质量 | 无新问题。改名收尾完成，常量有名字，文件表顺序与阅读顺序一致 | 见下方「本轮修复项」的处置表 |
| 架构与项目一致性 | 无新问题。架构符合性、依赖关系均符合（见下） | 同下 |
| 契约一致性 | 无。README:86 写的「HTTP 400」与 `fakeDenyStatus = fasthttp.StatusBadRequest` 一致；错误码和类别取值本轮没有变化 | `guardrails.go:30`；README:86 |

**架构符合性**（依据：EE 后端架构 §4、§5、§6、§7）：
- **app：** `app/guardrails.go:35-70` 仍然只读环境变量、构造规则、检查器和插件，然后调用宿主注册，符合 §5「`app` 只做装配」。本轮只把字面量换成常量，没有加入运行期编排。
- **fake 子包：** 删除子包 README 不影响 `detectors/fake` 的归位。它仍然实现根包的 `Detector` 接口（`fake/detector.go:15-26`），与 §7「`<模块>/<子包>` 把根包定义的接口适配到某个库」的判定一致。§4 也写明子包表不是必建清单。
- **根包：** 本轮只改了测试名、注释和失败信息，职责划分没有变化，与 §4「模块根包：业务模型、服务、规则、错误及所需接口」一致。
- **内嵌 Bifrost：** 注册仍然走上游已导出的 `SyncLoadedPlugin`（`guardrails.go:64-65`），没有另建管线，也没有修改上游文件，符合 §7「按缺口扩展」。

**依赖关系**（依据：§5「依赖与协作 / 模块内部」、§7「模块根包只依赖标准库」「由 app 显式注入」）：
- **app：** 本轮只新增一个 import `github.com/valyala/fasthttp`（`guardrails.go:16`），用途仅是取状态码常量。
  - `app` 属于装配层，不是业务根包，§5「fasthttp……的具体类型留在适配实现中」约束的是根包，这里不违反。
  - 依赖方向仍为 app → plugin/fake → 根包。
- **插件：** `safetyplugin.Options.StatusCode` 仍然是普通 `int`（`plugin.go:17`），插件没有因此依赖 fasthttp。
- **根包：** 根包的 import 本轮没有变化，仍然只有标准库，差量里没有涉及根包的非测试文件。
- **注入与资源所有权：**
  - 检测器注册表仍由 app 通过 `guardrails.New(..., map[string]guardrails.Detector{fakeDetectorID: fake.Detector{}}, ...)`（`guardrails.go:54`）显式传入。
  - 日志仍由 app 传给 `safetyplugin.New`（`guardrails.go:58-60`）。
  - 资源所有权没有变化。

## 出口清单

### 本轮修复项

上一轮 `收敛评审5.md` 的「本轮修复项」共 4 条，本轮逐条处置如下：

| 编号 | 处置 | 证据 |
|---|---|---|
| F8 改名没有收尾（测试名、注释仍用 Engine/Event） | **已修复** | 见下方说明 |
| F9 检测器 ID 重复三次，状态码写成裸数字 | **已修复** | 见下方说明 |
| F10 fake 子包 README 全文复述父包 | **已修复** | 见下方说明 |
| F11 父 README 文件表顺序与阅读顺序不符 | **已修复** | 见下方说明 |

**F8 的核对说明：**
- 测试函数已改名：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker_test.go:219` 改为 `TestUninitializedCheckerAndContext`；
  - `:243` 改为 `TestCheckErrorPrecedenceAndRuleEvaluations`。
- 注释（`:242`）改为「出错不能丢失此前已完成的规则执行结果」。
- 失败信息（`:269`）改为 `"completed rule evaluation lost"`。
- 在 guardrails 包和 app 护栏文件里用 `git grep "Engine\|[Ee]vent\b\|事件"` 查找，只剩 `plugin/plugin.go:12` 的「安全事件」。那是日志语义的泛称，不是被改名的 `Event` 类型，而且不在本轮差量内，处理见保留项 K11。
- 局部变量 `e` 没有改（上一轮标为可选），处理见保留项 K10。

**F9 的核对说明：**
- `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails.go:26` 新增 `fakeDetectorID = "dev-fake"`，以下三处都改用这个常量：
  - `:41` 的 `ID`；
  - `:41` 的 `DetectorID`；
  - `:54` 的注册表键。
- `:30` 新增 `fakeDenyStatus = fasthttp.StatusBadRequest`，并在 `:29` 加了注释；`:59` 改用这个常量。
- 这个写法与 ee 其他地方的 `fasthttp.StatusXxx` 惯例一致。

**F10 的核对说明：**
- `ee/internal/guardrails/detectors/fake/README.md` 已在本提交中删除，`git ls-files` 与 Glob 都查不到这个文件。
- 在仓库里查 `fake/README`，只在历史记录 `收敛评审5.md` 中出现。现行 README、app/README 和 ee/README 都没有指向它的链接。
- 假检测器的说明仍然保留在两处：父 README 的第 68 行和第 76–96 行，以及 `fake/detector.go:1,11,14,17` 的注释。

**F11 的核对说明：**
- `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:63` 的 `plugin/deny.go` 已移到 `plugin/text.go`（:62）之后。
- 现在的表格顺序是：插件三文件 → 检查器 → 单条规则执行 → 规则 → 检测器契约 → 假检测器。这与第 57 行「先读插件入口……再读检查器和单条规则执行；开发具体算法时看检测器契约」一致。

**本轮新增：** 无。

### 需用户裁决的 Blocking

- 无

### 立债（REF：立 `triggered_by: [<本case>]` 的需求进 reports/）

- 无

### 挂账（Finding：登记 findings.md，带四要件）

- 无新增。上一轮的建议取舍项 S1、S2 已由实施方登记为 FIND-028、FIND-029，内容与原建议一致，本轮不再处理。

**建议取舍项：** 无（本轮差量中没有需要取舍的结构性问题）。

### 转代码评审（范围外发现，一行一条，不出结论）

- 无

## 台账对账

**涉及的已有 Finding：**
- **FIND-022**（README 违反规范明文却被评审放过）：本轮逐条对照了编码规范「模块根包要有 README」一节。
  - 子包按需：F10 已关闭。
  - 代码在哪：F11 已关闭，文件表与阅读顺序一致。
  - 少提外部名词：README:11 首次出现 Bifrost 时已就地说明。
  - 长度：110 行，超过一屏，但按 2026-09-17 用户裁决不提。
  - 结论：未恶化。
- **FIND-024**（`internal/host` 目录归位）：本轮没有新增宿主适配代码，插件注册仍放在 app，未恶化。
- **FIND-012**（attach 惯性点）：`attach` 本轮没有变化，未恶化。
- **FIND-028、FIND-029**：本轮登记，对应上一轮的 S1、S2。相关文件不在本轮差量内，未恶化。
- **FIND-025 至 FIND-027**：本轮没有涉及。

**看似可改但有理由保留（下一轮不再提）：**
- **K1–K9：** 沿用上一轮的保留理由，本轮差量没有让这些点恶化。
  - K3 的反证条件是「改夹具参数却没有同步 README」。本轮 README:86 的「HTTP 400」与 `fakeDenyStatus` 仍然一致，没有触发。
  - K4 的反证条件是「新增第四个模式」。`guardrails.go:52` 仍是三个模式，没有触发。
- **K10 `checker_test.go` 中 Checker 局部变量仍叫 `e`**（`:28,:50,:73,:102,:115,:132,:140,:167,:220,:226,:244,:258`）：
  - 上一轮把改名列为可选。
  - 这些都是测试内的短生命周期变量，类型在 `mustChecker` 的签名（`:26`）上一眼可见，函数名也已改为 Checker，不会再把读者引向不存在的类型。
  - 反证条件：如果后续测试在同一作用域内同时出现多个检查器或检测器变量，使 `e` 产生歧义，就应改名。
- **K11 `plugin/plugin.go:12` 注释写的是「安全事件所需的宿主日志能力」：**
  - 这里的「事件」指日志中的安全事件，不是被改名的 `Event`/`Events` 类型。
  - 这行不在本轮差量内，上一轮已核对 plugin.go 的注释同步情况。
  - 反证条件：如果以后引入名为「事件」的类型或概念，与 RuleEvaluation 并存，就应改为「规则执行结果日志」。
- **K12 `app/guardrails.go` 只为一个状态码常量导入 `fasthttp`：**
  - 用 `net/http.StatusBadRequest` 可以只依赖标准库。但 ee 内所有状态码都写成 `fasthttp.StatusXxx`，`fasthttp` 本来就是 ee 模块的直接依赖。
  - `app` 是装配层，§5 对 fasthttp 的限制针对业务根包，不适用于这里。按项目惯例统一比少一个 import 更重要。
  - 反证条件：如果项目约定 app 层只导入宿主、核心与标准库，就应改用 `net/http`。
- **K13 `guardrails.go:25` 的组注释「以下为开发夹具的固定配置」下面，`:29` 又有一行「开发夹具的拒绝状态」：**
  - 第二行与组注释有部分重复。但它标出了 const 块里唯一来自外部包的取值，也与 `:31` 插件顺序的单独注释形式一致。
  - 这是上一轮建议的写法，删不删都没有维护收益。

---

**起手表覆盖：**

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。`"dev-fake"` 只在 `guardrails.go:26` 定义一次；README:86 写的是面向联调者的使用说明，见 K3 |
| 重复 | 同一规则多份实现且互不注明 | 无。本轮差量没有新增规则实现；删除子包 README 后，重复描述减少 |
| 重复 | 重造已有 helper | 无。状态码复用 `fasthttp.StatusBadRequest` |
| 重复 | 派生常量无推导 | 无。`fakeMaxTextBytes = 64 * 1024` 写出了推导；本轮新增的两个常量都是原始值或引用值 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无。检测器 ID 已命名（F9 关闭）；`math.MinInt` 有注释（`:31-32`） |
| 职责与实现质量 | 状态码、错误码裸数字 | 无。`fakeDenyStatus` 已命名（F9 关闭）；测试中的 `1024`、`100` 是测试夹具取值，属于测试按契约独立书写 |
| 职责与实现质量 | 无名的错误形状 | 不适用。本轮差量没有涉及错误返回形状 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无。控制流没有变化，空 case 见上一轮 K5 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无。删除子包 README 减少了一处跳转；三个文件都不超过 300 行（70、110、271 行） |
| 职责与实现质量 | 多余状态或守卫 | 不适用。本轮差量没有新增状态或守卫 |
| 职责与实现质量 | 同一效果做两次 | 无 |
| 职责与实现质量 | 注释与现状矛盾 | 无。`checker_test.go:242` 已同步（F8 关闭）；`guardrails.go:25,29` 与代码一致；残留的「安全事件」见 K11 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。状态码写法：一致（`branding/http/handler.go:108`）。const 块集中配置：一致（`app/identity.go`、`app/pricing.go`）。子包 README 按需：一致（`identity/hasher` 没有 README）。插件注册接缝：没有变化，仍为 `SyncLoadedPlugin` |
| 架构与项目一致性 | **架构符合性** | 符合，见「四维核查」（§4、§5、§6、§7） |
| 架构与项目一致性 | **依赖关系** | 符合，见「四维核查」（§5、§7）；新增 `fasthttp` import 位于装配层，见 K12 |
| 架构与项目一致性 | 绕过既有出口 | 无。注册和拒绝的出口都没有变化 |
| 架构与项目一致性 | 把上游现状当契约 | 无。本轮没有新增依赖上游行为的判断 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无。README:61-68 文件表与实际文件一致（Glob 核对）；README:86 与常量一致；本期没有前端 |
| 契约一致性 | 状态码口径不一 | 无。README:86「HTTP 400」、`guardrails.go:30` 的 `StatusBadRequest`、插件允许范围 400–599（`deny.go:26`）三者一致 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。`EE_GUARDRAILS_FAKE` 的未设置和空值处理（`guardrails.go:36-39`）没有变化，与 README:78 一致 |

**开放式复读：** 已放下起手表，以半年后要改这里的维护者身份，把范围内每个文件重读一遍，问「第一眼会让人皱眉的是什么」：
- `app/guardrails.go`：
  - 常量块现在把开关、模式、夹具配置、拒绝状态、插件顺序放在一起，每组都有注释，读者能直接看出哪些是开发专用值。
  - `ID: fakeDetectorID, DetectorID: fakeDetectorID` 同时表达了「规则 ID 与检测器 ID 一致」这一约束。只有一条规则时，这比再起一个 `fakeRuleID` 更直接。
  - 两个次要点已记为 K12、K13，不构成问题。
- `guardrails/README.md`：文件表顺序现在与阅读顺序一致。假检测器相关信息集中在第 68 行和「开发联调」一节，没有其他地方要同步。没有新的皱眉点。
- `checker_test.go`：测试名和失败信息已统一术语。局部变量 `e` 见 K10，不影响理解。
- 已删除的 `fake/README.md`：确认没有现行文档引用它，信息没有丢失。

**表外发现：** 已做开放式复读，没有表外发现。

**实际静态阅读范围与未执行的检查：**
- **读过的文件：**
  - 范围内：3 个现存文件全文，以及 1 个已删除文件的差量。
  - 相关文件：`fake/detector.go` 与 `app/bootstrap.go` 全文；`plugin/plugin.go`、`deny.go` 的相关行（grep）；ee 内 `fasthttp.Status*` 的用法（grep）。
  - 规则与裁决：见「事实来源」。
- **未运行的命令：**
  - `go vet`、`go test`、`go build`、预飞、冒烟脚本都没有运行：它们会写构建缓存或临时目录，本评审只读。
  - 编译和测试是否通过归代码评审。
- **未读的文件：**
  - `plugin/*` 的其余文件、`app/guardrails_test.go`、`guardrails-smoke.py` 不在本轮差量内，没有重读，只用 grep 核对了相关符号。
  - 差量中的 workbench 过程文档与生成物（`代码评审5.md`、交付摘要、开发联调证据、预飞、视图、导航）不是被评对象，没有读；方案v2 和 findings.md 只按要求读了裁决与登记部分。
  - 按要求没有读 `收敛评审5.md` 以外的旧评审。
- **上游核实：** 本轮的判断不依赖「上游会或不会做什么」，没有新增上游代码核实；`SyncLoadedPlugin` 的行为已在上一轮核实，本轮没有变化。
