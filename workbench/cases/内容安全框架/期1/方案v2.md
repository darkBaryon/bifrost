---
title: 内容安全框架 期1 方案 v2
type: 方案
case: 内容安全框架
phase: 1
version: 2
status: 已实施
relates: [内容安全框架]
created: 2026-09-16
---

## 给用户的摘要

2026-09-17 用户补充裁决：本期包含框架可读性整理，以及显式开启的开发用假检测器、app 注册和真实 HTTP / Playground 联调；默认关闭，不提供生产安全识别。README 按用户要求保留宏观流程与使用说明；命名使用 Checker、CheckResult、RuleEvaluation，类型随职责归位。以下早期“不装配 app”“不注入假检测器”的限制，仅继续约束默认和生产启用，不再排除显式开发联调。

当前只有竞品研究，完成后有可被 BF 调用的本地安全检测框架。参考 HG 的内容提取、阈值判定、处置职责，去掉云调用和 WASM 依赖。放行、拦截、仅记录与显式失败策略已获用户确认。通过测试检测器验证 BF Chat 文本的输入和非流式输出；实际检测器、生产注册、配置管理和其他协议后续接入。没有待拍板的新增成本、依赖或公开 API。

## 1. 起点

基线 `7c81459aa0e62750f22f57b57c3c4a4750a98cfc`，分支 `feat/content-safety`。起始未提交内容均为本会话内容安全研究文档，不覆盖其他代码。HG 固定提交 `faccaad586a3cdc9e85dc7fa39358ff31a6453b2`，出处见 [源码研究](../../../../product/需求/内容安全/Higress源码研究.md)。

## 2. 现状与约束盘点

- `ee/AGENTS.md`：根包只用标准库；插件依赖业务根包；中文注释，文件超过 300 行评估拆分、500 行必须拆。
- `core/schemas/plugin.go`：普通 Go error 不阻断，须使用短路或响应错误；本轮安全错误禁止 fallback 绕过。
- `core/schemas/context.go` 提供流式 gate API，但完整正文累计依赖其他插件，本轮不接 gate。
- `core/providers/utils/utils.go`：先向 trace 记录 chunk 计数，再执行 PostLLMHook，最后进入 gate；此流程不保证全文已累计。
- `framework/streaming/gate.go` 的 EndStream 与无 final Hook 的 CloseStream 会释放暂停缓存，本轮不使用该路径实现输出保护。
- `ee/internal/app/bootstrap.go` 当前没有护栏装配。框架不注入空策略或假检测器，不修改上游文件。

## 3. 目标 / 非目标

目标：新增 `ee/internal/guardrails` 业务根包和 `plugin` 入口，支持按阶段运行本地检测、合并风险、生成不含原文的事件。框架始终接收完整文本。
非目标：真实算法、外部服务、配置 CRUD/持久化、HTTP 管理 API、脱敏/代答、多模态/MCP/Responses/TextCompletion 协议、生产启用及发布。Chat 入口只接受当前实现可完整提取的纯文本消息，未覆盖结构明确报不支持，不冒充已检测。

## 4. 接口与数据设计

- `Detector.Detect(context.Context, string) ([]Finding, error)`：本地同步调用，实现必须响应取消、支持并发；不另建适配器层。
- `Rule`：ID、检测器 ID、检测类别、输入/输出阶段、风险阈值、命中动作 Block/Observe、失败动作 Allow/Block、超时；关键字段无默认值。规则按配置顺序运行，阻断短路，观察和允许失败继续后续规则。
- `New(rules, detectors, maxTextBytes)` 校验并复制规则/映射；`Check(ctx, stage, text)` 返回最终 CheckResult 与各规则 RuleEvaluation；空规则放行；空正文仍送检，避免将缺失与安全混同。
- 超限、非法输入、取消属于框架错误；检测器超时/错误/非法结果按该规则失败策略处理。检测事件不保留正文、命中原文或 detector error 文本。
- 插件 `New(checker, options, logger)`：选项明确拒答状态/文案；接 BF Chat 的 PreLLM/PostLLM。只在匹配阶段有规则时检查；输入包含全部历史纯文本消息，输出包含全部 choice 的纯文本。复杂 assistant/tool/multimodal 结构本轮拒绝处理。
- 流式调整（2026-09-16 用户选择 1）：本轮不接 BF 输出流缓存；匹配输出检查的 Chat 流式请求在 PreLLMHook 明确短路，不调用模型、不释放未经检查输出。只有输入规则时可正常检查输入并透传模型流。后续再修复输出流机制。
- Chat 输入/输出仅接受纯文本字符串或 text blocks；对多模态、tool、reasoning、refusal 等本轮未提取结构明确拒绝。拒绝 RawRequestBody、UseRawRequestBody、LargePayload passthrough、ExtraParams 和 ExtraContent 等绕过规范字段的路径；不将未知结构当空文本放行。
- 插件只提供可注入的构造与 Hook 实现，无新增公开 HTTP 契约或默认启用；后续由 app 在真实检测器和配置到位后装配。

## 5. 实现步骤

1. 完成产品方向和工程记录；独立方案复核。
2. 业务规则执行和失败策略，编写语义单测。
3. BF Chat Hook 和中文 README；用测试检测器验证，输出流按用户对收窄范围的裁决处理。
4. EE 全量检查、预飞、提交本地候选，启动独立代码与收敛评审；修复问题后交用户验收。

## 6. 测试策略

- 多规则：观察后继续；阻断停止；阈值边界；D 项、动作、失败策略缺失拒绝构造。
- 检测器：返回风险、失败、超时、非法严重度；配置快照不受调用方修改；并发无竞态。
- Hook：输入阻断返回 ShortCircuit，输出阻断替换结果，AllowFallbacks=false；无规则透明通过；不支持结构明确失败。
- 流：有输出规则时 PreLLMHook 明确短路，不以普通 Go error 冒充阻断；仅输入规则时继续，PostLLM 不对 chunk 假装做完整输出检测。
- 真实算法、网关部署、所有 provider 协议与其他插件改写顺序均未验证，不作为本轮结论。

## 7. 验收标准 + 检查清单 YAML

按 `checklist.yaml` 执行 EE vet/race/build、文档检查及格式检查；所有新增 Go 文件不超过 500 行。预飞绿后做独立评审，Gate 2 由用户验收。

## 8. 风险与禁止事项

框架不等于识别能力；禁止默认启用、伪造生产检测器、隐式失败放行、把原文写日志、在 ctx 存流内容、改动上游或引入云依赖。独立测试不等于生产实测。

## 9. 回滚口径

未发布、无数据迁移；可回退本次 guardrails 新包和相关说明，既有启动行为不变。

## Gate 决策结果

Gate 1：核心引擎沿用既有授权和本轮已确认处置范围，独立复核通过后可继续；2026-09-16 用户选择方案 1，确认完成普通请求接入、有输出规则的流式明确不支持；插件接入按此实施。该授权不代表上线或 Gate 2。

## v1 问题与调整依据

1. `CloseStream` 在未出现 terminal hook 时也会 End 并释放缓存，无法保证检测前零输出。
2. 首 chunk 在 GateSend 前 End 会丢错误。
3. GetAccumulatedResponse 依赖其他插件累计，且 chat accumulator 只取第一个 choice。
4. 为框架阶段修复这些问题会涉及上游与协议行为，已经单独请求用户确认范围；当前方案不将它们隐藏为“已支持流式”。

## 核心阶段历史进度

2026-09-16：核心规则和引擎已实现（rules.go、engine.go），单元与 race 测试通过。BF 插件未编写，等待用户对输出流范围收窄的明确裁决；不将部分进度标为已实施。已执行现有 app/host 回归，均通过。

核心阶段补充验证：`go vet ./internal/guardrails`、`go test -race ./internal/guardrails` 通过，方案复核者独立阅读核心代码和运行 race 测试，未发现确定性缺陷。此为阶段检查，不替代实施完成后的正式代码及收敛评审；未运行本期完整预飞，尚未形成提交候选。

## 实施记录

- 业务根包：规则与检测器接口、配置快照、阈值、观察/阻断、显式失败策略及取消处理。
- BF 插件：规范化 Chat 输入/非流式输出检查；输出规则拒绝流式；安全错误短路禁止 fallback；日志不输出原文。
- 范围与来源：按 HG 职责和处置流程参考实现；不复制云调用、WASM 或上游流 gate，不改上游文件，不注册生产空策略。
- 验证：核心及插件 race 测试、真实 BF 插件管线测试通过；EE 全量验证与预飞见本期事实表。候选 commit 在检查完成后记录。

## 实施偏差记录

v1 流式接入问题由独立方案复核发现，已形成 v2；2026-09-16 用户明确选择方案 1，确认普通请求接入、输出流检查不支持。其余无范围偏差。测试管线在 Hook 前仍读取 Provider 配置，测试账号提供本地不可用地址并拒绝取 key，模型替身短路，不调用外部服务。

## 蓝图验证备忘

不适用；本期是框架，不代表检测算法、生产装配或全部 Provider 验证已完成。

候选提交：`cf2d94083c98601149273d217712fb4e879484cb`；EE 全量 vet/race/build、格式和工作台校验及预飞均通过，进入正式独立代码与收敛评审。

## 第一轮评审修复记录

- 代码 R1：拒绝含词元概率的输出，覆盖正文安全但 content/top/refusal token 含危险文本的回归；同时拒绝消息名称及停止文本这些未送检载体。
- 代码 R2：引擎先检查取消和字节上限，再验证 UTF-8；文本拼接也先检查剩余预算。回归覆盖超限与非法编码并存，以及已取消请求。
- 收敛 F1–F4：README 改为问题说明及文件调用链，明确同步取消和装配前提；日志依赖校验与同类构造函数保持一致；统一错误码转换；说明并测试出错后保留已完成事件的约定。
- 采纳 S1/S2：纯模型文件改名为 model.go，配置测试并入 engine_test.go；新增上游 Chat 字段清单测试，结构变化时要求重新审阅提取边界。
- 转交项核对：正数大响应阈值可能在调用模型后切换为透传，所以当前明确拒绝，README 已说明此兼容限制；推理强度和缓存时效字段是控制参数，推理正文已拒绝。真实管线测试已覆盖安全插件排在短路模型替身之前，生产插件顺序仍是后续实际装配的验收前提。
- 超时分类沿用根契约：父请求 deadline 到期是请求取消，检测器报告 deadline 是检测失败中的超时；README 已明确。
- 竞品对照中的我方流式结论已同步到用户选择的方案 1。
- 第一轮独立评审额外运行的 branding-smoke 因本地未嵌入 UI 导致 SPA 断言失败；安全框架不涉及 UI。脚本失败后临时证据清理问题属于既有工具行为，本次不改该脚本，交给收敛复核判定是否挂账。不得据必过检查通过声称所有冒烟通过。

## 第二轮评审修复记录

- 代码评审 2 通过，候选 c503a4bb8 的 R1/R2 与五条转交项均已处置。
- 收敛 F5：接入范围拆为列表，解释宿主超大正文透传、响应阈值为何须提前拒绝；词元概率就地说明为生成词及候选词。
- 收敛 F6：在引擎与拼接处补充先限长、后 UTF-8 扫描的原因注释；没有运行行为变化。
- 采纳 S3：字段清单加入直接接入的 BifrostChatRequest，合并上游根字段变更时同样提醒复审。
- 新转交的 logprobs 空结构、主动请求 logprobs 及 Name 可用性，继续交代码复核判断：本轮按明确的纯文本契约拒绝整个未覆盖载体，无生产装配，未承诺全部可选 Chat 参数兼容；目前没有把这些请求当作已支持。
- 评审文件新增导致的生成视图漂移在本轮重新生成，后续每份评审落盘后均同步生成，不沿用旧预飞声称当前视图通过。

### 代码评审 2 转交原文

启动器只按无编号的二级标题匹配转交栏，本次评审使用「## 5. 转收敛评审 / 挂账」，无法自动提取。依照交付规范的转交闭环要求，原样附在本实施记录；没有改写评审原文或启动提示词。

**转收敛评审：**

- 转收敛评审：[branding-smoke.py:129](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/branding-smoke.py:129) 的失败现场保留方式，沿用上轮范围外转交。
- 转收敛评审：评审期间新增 `收敛评审2.md` 后，工作台四处生成视图出现漂移。

**挂账：无。**

## 第三轮评审修复记录

- 代码评审 3 通过，新增的四条转交均已处置：logprobs 空结构、请求词元概率及 Name 的兼容性按已声明限制撤回本期必修要求；品牌冒烟失败现场沿用 FIND-026。
- 收敛 F7：README 先说明只接受纯文本消息与已审阅控制参数，再明确拒绝清单仅为举例，补充预测内容、响应格式、联网搜索、文件及拒答正文，并链接实际提取判定；没有代码或测试行为变化。
- 本轮只修改上述 README 一行及工作台记录，不新增功能、测试或运行依赖。

## 最终复核与验收状态

- 功能候选：958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8；后续只提交工作台交付记录，不变更已复核功能。
- 代码评审 4「通过」，收敛评审 4「无碍」；本轮必修项全部关闭，无新增转交。六项必过检查及产物由代码复核亲核通过。
- 两侧转交项与既有工具 FIND-026/027 的处置见[交付摘要](../../../evidence/内容安全框架-期1-交付摘要.md)。品牌冒烟缺 UI 的失败如实保留，不计通过。
- 长期口径已写入模块 README、EE 入口及产品内容安全总结；没有新增 HTTP 契约、数据表或部署步骤需要迁移。实施与评审过程留在工作台。
- Gate 2 等待用户确认；当前维持「已实施 / 开发中」，不创建验收通过记录，不合并或发布。

## 用户阅读与开发联调后的补充记录（2026-09-17）

- 用户逐段阅读代码，要求将 Engine 改为 Checker、Result/Event 改为 CheckResult/RuleEvaluation，将 model.go 中定义放回所属职责文件，提取请求准入校验，并在 README 解释整体流程。按明确要求完成，文件粒度和 README 长度沿用本次用户裁决。
- 用户同意先做 fake Detector；9c8bc7fc5 增加显式开发开关、app 注册、标记检测器和隔离 HTTP 联调脚本，支持输入拦截、输入仅记录、输出拦截。默认关闭、非法模式拒绝启动、无持久化配置。
- Go vet/race/build、真实 HTTP 和 Playground 联调通过，详见[开发联调证据](../../../evidence/内容安全框架-期1-开发联调.md)。用户亲自测试输入拦截后回复“OK了”，随后要求提交代码。
- 用户确认本期独立合入自有 develop，不等全部 safety 实现；并明确 dev 是 develop 的口头简称，不操作上游 dev。约定已写入根 AGENTS.md。该确认授权在本轮评审及最新 develop 集成验证通过后合入；不代表真实算法或生产防护已交付。
- 本轮对 958fc9830 之后的实际改动补做差量代码与收敛评审。原第 4 轮通过结论只覆盖旧候选，不能替代新增装配与可读性调整的复核。
- 实施偏差：开发联调与可读性调整均来自上述用户明确要求；未引入外部安全服务、真实算法、管理 API、数据库迁移或默认防护。
- 蓝图验证：本期仍为基础框架交付，后续真实检测能力逐项实现与验证。

## 第五轮补审修复记录

- 代码评审 5 通过：完整检查本轮重命名、默认关闭的开发注册与 HTTP 验证，没有正确性必修项。
- 收敛 F8：测试名与失败信息改用 Checker / RuleEvaluation，注释同步。F9：检测器 ID 与开发拒绝状态使用命名常量。F10：按编码规范“子包按需”的详细规则删除重复的 fake 子包 README，保留父包文件表与联调说明。F11：README 将 deny.go 与其余插件文件放在一起。
- 代码评审转交的接口表述与旧文件链接已同步；历史评审原文保持不改，旧候选结论不冒充当前结论。
- 收敛转代码的三项保留原文，由第 6 轮代码复核处置；本轮 HTTP smoke 已在 checklist 列为必过命令，Makefile 聚合并非唯一检查入口。临时目录由 mkdtemp 以 0700 创建，browser.json 位于该私有目录，最终权限 0600；现场保留用于失败定位。部署文件与 origin/develop 一致，本轮不执行集群部署。
- 范围外建议 S1/S2 分别登记 FIND-028/029，留待相应维护工作处理，不修改定价或部署文件。

### 代码评审 5 转收敛原文

- 转收敛评审：[方案接口表述:40](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/cases/内容安全框架/期1/方案v2.md:40) 及旧交付摘要中的类型、文件链接与本轮重命名后的现状同步。
