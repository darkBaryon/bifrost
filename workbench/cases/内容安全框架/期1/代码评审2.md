---
title: 内容安全框架 期1 代码评审 #2
type: 代码评审
case: 内容安全框架
phase: 1
round: 2
verdict: 通过
created: 2026-09-16
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 cf2d94083c98601149273d217712fb4e879484cb 到候选 c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过（针对候选代码的本轮差量复核）。** 上轮两项必修已关闭，五条转交意见已逐条核实，未发现新增必修项。EE vet、全量 race、构建及定价、SQLite 身份 smoke 通过。品牌 smoke 因未嵌入 UI 失败；评审期间新增文档又造成末次视图检查失败，因此不能声称当前工作树预飞全部通过。这两项均与候选源码缺陷区分记录。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 本地检测、阈值、观察/阻断、显式失败策略 | 实现与验收口径一致，相关测试亲跑通过 |
| 全历史输入、全部非流式候选输出 | 保留原行为，补齐 logprobs、消息名称、停止文本的明确拒绝 |
| 取消、超限、非法编码 | 校验顺序已修正；错误仍阻断，保留此前完成的事件 |
| 输出规则拒绝流式、仅输入规则允许规范化流式 | 符合 Gate 决策，Hook 与真实 BF 管线测试通过 |
| 模型文件更名、配置测试迁移 | `model.go` 与原 `rules.go` 字节一致；删除的两组配置测试原样保留在 `engine_test.go` |
| 对外接口、依赖、生产装配 | 差量未新增公开符号、依赖、HTTP 接口或生产注册 |
| 三方不一致、多做/少做、未登记偏差 | 本轮未发现；声明修改均能对应到差量 |

版本与范围：

- 分支 `feat/content-safety`；HEAD 始终为 `c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe`。
- 已核实基线及上轮候选均为 HEAD 祖先；读取指定差量文件的完整现状，补读受影响调用链。
- 起始工作树干净；末次源码、索引仍无修改。
- 评审期间新增两个未跟踪文件：交付摘要已补读；`收敛评审2.md` 仅登记文件名，为保持独立性未读取意见。
- `rules_test.go` 当前已删除，已读取删除内容并核验迁移完整性。

## 3. 本轮修复项

**无新增必修项。** 上轮「本轮修复项」处置如下：

| 上轮意见 | 处置与证据 |
|---|---|
| R1：输出 logprobs 文本漏检 | **已修复。** [text.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:71) 拒绝任何非 nil `LogProbs`；[text_test.go:111](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:111) 覆盖正文安全、content/top/refusal token 含风险文本，断言不返回原响应且禁止 fallback。亲跑通过，无需追加修正。 |
| R2：UTF-8 扫描早于限长/取消 | **已修复。** [engine.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine.go:71) 先取消、再字节上限、后 UTF-8；[text.go:118](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:118) 先检查剩余预算。[engine_test.go:243](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine_test.go:243)、[text_test.go:162](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:162) 验证超限与非法编码并存、取消优先级及已完成事件保留。亲跑通过，无需追加修正。 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无。**

收敛评审五条转交意见逐项处置：

| 转交项 | 处置 | 依据 |
|---|---|---|
| 正数大响应阈值导致所有 Chat 检查被拒绝 | **撤回本期缺陷质疑，保留兼容限制事实** | [text.go:22](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:22) 确实如此，包括仅输入规则。上游 [largeresponse.go:218](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/providers/utils/largeresponse.go:218) 可在模型调用后切换 reader 透传；方案排除该路径，README 明确限制。本仓未注册该插件，不能推断现有企业部署已因此不可用。 |
| Name、Reasoning、CacheControl 等漏检 | **修复 Name；撤回控制参数漏检质疑** | [text.go:85](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:85) 已拒绝消息名称，输入和输出均有回归。推理正文仍拒绝；[ChatReasoning:329](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/schemas/chatcompletions.go:329) 与 [CacheControl:1395](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/schemas/chatcompletions.go:1395) 是推理控制及缓存参数，所查转换路径未将其当正文拼入。 |
| 前序短路跳过输出检查，装配前提没有测试 | **撤回本期必修要求** | 跳过机制属实：[bifrost.go:7904](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/bifrost.go:7904)、`:7975` 仅执行已运行 PreHook 的对应 PostHook。但 [integration_test.go:51](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/integration_test.go:51) 已测试 `{安全插件, 短路替身}` 的输出阻断，并亲跑通过。任意插件组合及错误装配顺序未获验证，属于明确后续生产装配范围。 |
| 父 DeadlineExceeded 被归为 CheckCanceled | **撤回** | [plugin.go:142](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin.go:142) 将父请求终止归为检查取消，与 [README:25](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:25) 一致；保持阻断且禁止 fallback。此处为分类语义静态核对，不声称新增测试专门覆盖父 deadline。 |
| 检测器自行返回 DeadlineExceeded 被记为 DetectorTimeout | **撤回** | [engine.go:108](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine.go:108) 先区分父请求终止，再将检测器报告的 deadline 归入检测超时；最终仍是 `Failed`，按显式 `OnError` 处置。分类并未限定只能由规则定时器触发。 |

另外，不将品牌 smoke 失败归因于本次 guardrails 回归：嵌入目录只有 `.keep`，上游 UI 处理器读取不到 `index.html` 会返回 404。

## 5. 转收敛评审 / 挂账

**转收敛评审：**

- 转收敛评审：[branding-smoke.py:129](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/branding-smoke.py:129) 的失败现场保留方式，沿用上轮范围外转交。
- 转收敛评审：评审期间新增 `收敛评审2.md` 后，工作台四处生成视图出现漂移。

**挂账：无。**

## 6. 开放式复读的表外发现

**已做开放式复读，无表外新增发现。**

复读确认的失败归宿：

| 失败路径 | 最终行为 |
|---|---|
| 非法配置、缺检测器 | 构造返回错误，不产生可用引擎 |
| 检测器错误、超时、非法结果 | 记录 `Failed` 事件，按显式策略允许或阻断；不输出异常原文 |
| 父请求取消、非法输入、超限 | 框架错误；插件阻断并记录安全错误码 |
| 未支持载体、输出流 | 短路或替换响应，不返回未检查输出，禁止 fallback |
| 后续规则取消 | 保留已完成事件，最终决定为 Block |
| 上游错误 | 原样保留，不伪装成正文检测通过 |

## 7. 起手表覆盖

以下结果限定本轮差量、转交意见及受影响区域。

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | **无**：上轮缺口已关闭 |
| 对照方案 | 方案、实施记录、diff 不一致 | **无** |
| 对照方案 | 多出功能、公共接口、抽象或依赖 | **无** |
| 对照方案 | 偏差未登记 | **无** |
| 行为正确性 | 失败被吞或假成功 | **无**：失败与通过分开记录 |
| 行为正确性 | 空值、空集合无分支 | **无**：缺失、空正文、空策略分别处理 |
| 行为正确性 | 取消/超时后仍有写入或副作用 | **无**：同步取消契约明确，无新增持久化 |
| 行为正确性 | 重发产生两份副作用 | **无**：未新增业务写入 |
| 行为正确性 | 事务外写后读、回滚不完整 | **不适用**：无事务变更 |
| 行为正确性 | 关闭、旧数据、旧客户端无处理 | **无**：空策略透传，生产装配未变 |
| 行为正确性 | 昂贵检查先于廉价检查、校验缺层 | **无**：R1、R2 已关闭 |
| 行为正确性 | 路由保护、认证关闭行为 | **不适用**：无路由或认证变更 |
| 行为正确性 | 敏感信息进日志或响应 | **无**：新增载体拒绝，正文与异常原文不入日志 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | **无**：差量未新增，超限拒绝顺序已修正 |
| 行为正确性 | 未登记配置、环境或部署步骤 | **无** |
| 失败可观测 | 服务端无法追溯原因 | **无**：规则事件及拒绝码保留 |
| 失败可观测 | 对外泄露内部错误原文 | **无** |
| 失败可观测 | 脚本失败现场被清理 | **发现**：既有品牌 smoke，已转交 |
| 测试可信 | 新规则或失败路径无测试 | **无**：新增拒绝与错误优先级有回归 |
| 测试可信 | 断言绑定实现而非行为 | **无**：字段清单测试明确用于升级复审，不替代行为测试 |
| 测试可信 | 失败信息无法定位条件 | **无**：新增输出载体用例已命名 |
| 测试可信 | 返回值、错误未检查 | **无**：修正构造夹具错误检查；迁移测试完整 |
| 测试可信 | fixture 依赖个人环境 | **无**：新增测试不依赖个人数据库或既有服务 |
| 测试可信 | mock 结果冒充真实环境结论 | **无**：文档明确测试替身边界 |
| 测试可信 | 整体替换被测模块的 mock | **无**：实际执行引擎、插件与 BF 管线 |
| 残留 | 调试输出、后门、本机配置 | **无** |
| 残留 | 未使用代码、无关改动 | **无**：生产未注册符合非目标 |
| 残留 | 生成物与源不一致 | **发现**：末次工作台视图漂移；候选初始检查通过 |

## 8. 验证记录

环境：`go1.27.1 darwin/arm64`。`TMPDIR` 为指定 scratch；Go 缓存和二进制均写入其中。

### 实际执行的命令与结果

| 命令 | 结果 |
|---|---|
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go vet ./...` | **通过** |
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go test -race -count=1 ./...` | **通过**；guardrails 1.091s，plugin 1.388s |
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | **通过** |
| `gofmt -l ee/internal/guardrails` | **通过**，输出为空 |
| `git diff --check` | **通过** |
| `git diff --check cf2d94083c98601149273d217712fb4e879484cb..c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe` | **通过** |
| `python3 -B workbench/tools/build_views.py --check` | 初次 **通过，139 篇**；新增收敛记录后复查 **失败，exit 1**：`views/all.md`、`SUMMARY.md`、`assets/nav.js`、案例 `index.md` 漂移 |
| 三项 `test -e`：README、engine.go、plugin.go | **全部存在** |
| `rg -n 'type Detector interface' ee/internal/guardrails` | 命中 `model.go:54` |
| 分别 `rg -n` 检查 `proxywasm`、`wasm-go`、`net/http`、`fasthttp` | **均无命中** |
| `python3 -B ee/scripts/pricing-smoke-test.py` | **6 项通过**；出现 SQLite 连接未关闭的 `ResourceWarning` |
| `python3 -B ee/scripts/pricing-smoke.py --binary "$TMPDIR/bifrost-http"` | **通过** |
| `python3 -B ee/scripts/branding-smoke.py --binary "$TMPDIR/bifrost-http"` | **失败**：第 67 行 SPA HTML 断言；未嵌入 UI |
| `python3 -B ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | **通过** |
| 内存 Python 对比模型文件及迁移测试 | 模型字节一致；删除的测试函数原样保留 |
| 内存 Python 检查两份范围内 Markdown 及新增交付摘要的本地链接 | 无缺失目标 |
| Git 状态、祖先关系、差量清单及索引检查 | 候选源码未变；索引为空；末次两个未跟踪文档如上 |
| `ps` 进程清单检查 | 环境拒绝，未提权或操作既有进程 |

所有验证命令均已退出；smoke 按自身 `finally/stop` 路径回收子进程。身份 smoke 现场保留于 scratch 的 `bifrost-identity-smoke-qpjpgaph`。

二进制 SHA-256：

```text
f3ea14b95eded44ca1780e6b65161056a76868435ea103212e5797366a02feae
```

### 实际阅读范围及未执行项

- 完整读取指定规则、需求验收、方案、checklist、findings、预飞证据、上轮代码评审，以及指定差量文件现状；补读原删除文件、集成测试、BF Hook/fallback、Chat 类型、相关 provider 转换、大响应路径和 smoke 夹具。
- 未读取收敛评审正文；仅处理用户原样提供的五条转交意见。
- checklist 六条命令及产物、符号、禁留项均亲核；只调整缓存/二进制路径、增加 `-count=1` 和 Python `-B`。未重复执行写报告的预飞包装器。
- 未运行整套 `make build/smoke`：其依赖会同步 workspace 和 UI 覆盖层；已直接使用 scratch 二进制执行三个自包含 smoke。未构建 UI，品牌 smoke 后续断言未覆盖。
- 未运行 PostgreSQL：两个测试 DSN 均未设置，且禁止连接已有实例；条件跳过部分不计入验证。
- 未调用真实模型、检测服务或复测竞品资料；未做性能压测、历史版本 red 测试或任意生产插件组合验证。
- 未修改源码、索引、报告或生成视图，未启动 dev server，未暂存、提交、推送。

verdict: 通过
