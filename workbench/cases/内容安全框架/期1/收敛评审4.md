---
title: 内容安全框架 期1 收敛评审 #4
type: 收敛评审
case: 内容安全框架
phase: 1
round: 4
verdict: 无碍
created: 2026-09-16
---

> 评审者与实施上下文隔离（不能是写代码的会话自评）。基线 `7c81459aa0e62750f22f57b57c3c4a4750a98cfc`。本轮是差量复核，范围从上一轮候选 `8fd978c7c76f718c38093f15e390f84d40fa4661` 到本轮候选 `958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8`。`git rev-parse HEAD` 的结果与本轮候选一致。
> 本轮风险等级为 L2，四维核查收成段落和表格。

## 结论

**总体判断：** 上一轮唯一的修复项 F7 已经关闭，本轮也没有新的修复项，可以交付。

- **改动规模：** 差量里只有 `ee/internal/guardrails/README.md` 第 33 行这一行（+1/−1），代码、测试和装配都没有改。
- **F7 已修复：** 新写法先讲原则「只接受纯文本消息与已审阅的控制参数，其余一律拒绝」，再用「以下仅举例」明说后面的清单不完整。
  - 上一轮点名漏写的预测内容、响应格式、联网搜索、文件、拒答正文都已补上。
  - 控制参数前加了「已允许的」，不会再被误读成「凡是没列出的参数都放行」。
  - 句末链接到 `plugin/text.go`，把最终判定交给代码。
- **逐项对照：** 清单每一项都能在 `text.go` 找到对应的拒绝判断；两个被放行的控制参数也核对了上游字段，确实不在拒绝条件里。
- **架构与依赖：** 与上一轮结论相同，仍符合 EE 后端架构 §4–§7。

## 事实来源

**执行的命令**（均为只读）：
- `git status --porcelain=v1 --untracked-files=all`：评审开始和结束各跑一次，都只有一个未跟踪文件 `workbench/evidence/内容安全框架-期1-交付摘要.md`。
  - 它是 workbench 过程文档，不在被评文件清单里，本轮没有读。
  - 提示词说「工作树干净」，实际多出这一个文件。它不影响被评的 `ee/` 内容，这里如实记录。
- `git rev-parse HEAD`：`958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8`。
- `git log --oneline 7c81459aa..HEAD`：共四个提交，依次是 `cf2d94083`、`c503a4bb8`、`8fd978c7c`、`958fc9830`。
- `git diff --stat 8fd978c7c..958fc9830`：共 9 个文件。其中 `ee/` 只有 README 一个（+1/−1），其余 8 个是 workbench 过程文档和生成物，不在范围内。
- `git diff --stat 8fd978c7c..958fc9830 -- ee/`：再次确认 `ee/` 下只有 `ee/internal/guardrails/README.md` 一个文件、改了一行。
- `git diff 8fd978c7c..958fc9830`：确认改动就是 README 第 33 行被整句替换。
- `git show 8fd978c7c:ee/internal/guardrails/README.md`：取出上一轮的原文，与新版逐项比对。
- `git grep -n "github.com/" -- ee/internal/guardrails/engine.go ee/internal/guardrails/model.go`：无输出，说明根包没有第三方 import。
- `git grep -n "guardrails" -- ee/internal/app ee/internal/host ee/cmd`：无输出，说明本包仍没有被装配。

**完整通读的文件（现状），及各自职责：**
- `ee/internal/guardrails/README.md`（41 行，被评文件）：讲本包解决什么问题、文件级调用链、检测契约、接入前提和验证入口。
- 为核对第 33 行的表述，另外通读了范围外的两个文件：
  - `ee/internal/guardrails/plugin/text.go`（127 行）：提取有界纯文本，拒绝不支持的载体。
  - `ee/internal/guardrails/plugin/schema_test.go`（41 行）：固定已审阅的上游 Chat 字段清单。

**规则与裁决：**
- 已读：根 AGENTS.md、workbench/AGENTS.md、收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md、收敛评审模板、EE后端架构.md 全文、findings.md 全文（到 FIND-027）。
- 方案v2 第 30–142 行已读，包括 §3–§9、「Gate 决策结果」「实施偏差记录」和第一至第三轮评审修复记录（含代码评审 2 的转交原文）。
- 已经裁决、本轮不重提的内容：
  - 输出规则开启时不支持流式请求。
  - 本期不装配到 `app`。
  - 按纯文本契约拒绝整个未覆盖载体（方案 §4 第 42 行）。
  - logprobs 空结构、主动请求词元概率、消息 Name 的兼容性，已按声明的限制撤回本期必修要求（第三轮修复记录第 139 行）。
  - 品牌冒烟失败现场的问题沿用 FIND-026。
- 上一轮记录 `收敛评审3.md` 已全文读过，用于逐条处置它的修复项，并沿用它的保留项 R1–R17。

**同类实现对照：**
- 参照 `ee/internal/pricing/README.md`（编码规范指定的样例），本轮重读了全文。
  - 它的写法是先讲原则、再给例子，细节交给代码或使用说明。例如第 45 行「具体到每类价目怎么挑……见使用说明的同名小节」。
  - 新的 README:33 用「具体判定见 text.go」把完整判定交给代码，与这种写法一致。
  - README 仍是「问题 → 调用链 → 约束」的结构，长度在一屏以内。

**上游假设核实**（`core/schemas/chatcompletions.go`）：
- `:197-227` 是 `ChatParameters` 的字段：
  - `Prediction`（`:207`）、`ResponseFormat`（`:213`）、`WebSearchOptions`（`:227`）、`Audio`（`:198`）都出现在 `text.go:29` 的拒绝条件里，与 README 例子中的「预测内容、响应格式、联网搜索、音频」对应。
  - `Reasoning`（`:212`，推理参数）和 `PromptCacheRetention`（`:210`，缓存时效）不在 `text.go:29` 里，属于 README 所说的「已允许的推理强度、缓存时效等控制参数」。
- `ChatReasoning` 的字段是 `Enabled Effort MaxTokens Display`（`schema_test.go:27`），只有控制值，不含正文。README 把它归为控制参数是准确的。
- 「已审阅」有测试兜底：`schema_test.go:19` 固定了 `ChatParameters` 的全部 38 个字段，上游一旦增删字段，测试就失败并提示复审 `text.go`（`:37`）。所以 `text.go:29` 虽然是逐项排除的写法，现有字段都已逐个审阅过。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无 | README:33 的例子是说明文字，句末把完整判定指向 `text.go`，拒绝规则只有代码一处实现（R18） |
| 职责与实现质量 | 无 | 差量不涉及代码；README 的职责仍是给接手者讲约束 |
| 架构与项目一致性 | 无。架构符合性、依赖关系都符合 | 见下 |
| 契约一致性 | 无。F7 已关闭 | README:33 逐项对照 `text.go:16,25,29,32-35,65,71,85,89,107` |

**README:33 逐项对照**（例子 → 代码中的拒绝位置）：
- 未经规范化的原始正文 → `text.go:16`（`UseRawRequestBody`）、`:25`（`RawRequestBody`）。
- 未知参数 → `:29`（`ExtraParams`）。
- 工具 → `:29`（`Tools`、`ToolChoice`）、`:85`（工具消息）、`:89`（`ToolCalls`）。
- 预测内容、响应格式、联网搜索 → `:29`（`Prediction`、`ResponseFormat`、`WebSearchOptions`）。
- 图像/音频/文件等非文本内容：
  - 请求侧 → `:107`（`ImageURLStruct`、`InputAudio`、`File`）、`:29`（`Audio`）。
  - 输出模态 → `:32-35`（非 `text` 模态）。
  - 响应侧 → `:65`（`Videos`）、`:89`（助手消息里的 `Audio`）。
- 推理或拒答正文 → `:89`（`Reasoning`、`ReasoningDetails`、`Refusal`）、`:107`（`block.Refusal`）。
- 消息名称 → `:85`（`Name`）。
- 停止文本 → `:71`（`StopString`）。
- 词元概率 → `:71`（`LogProbs`）。

没写进例子的拒绝项都已被「其余一律拒绝」「以下仅举例」覆盖，读者不会再把它们误当成放行：
- 参数里的 MCP 服务器、容器、上下文管理（`:29`）。
- 响应里的搜索结果、引用（`:65`）。
- 内容块里的引用、缓存点（`:107`）。
- 助手消息里的注释（`:89`）。

**架构符合性**（依据：EE 后端架构 §4 模块内部的职责、§6 插件与普通业务、§7 与 Bifrost 的协作边界、§8 实现细节维护在包 README）：
- 差量只改了包 README 的一行，模块划分和职责归属都没变。
  - 根包仍负责规则执行与处置（§4「模块根包：业务模型、服务、规则、错误及所需接口」）。
  - `plugin/text.go` 仍负责把 Chat 结构转成文本（§4「`plugin/` 接入 Bifrost 钩子，转换输入与处理结果」）。
- 把实现层面的拒绝约束写在包 README 里、完整判定指向代码，符合 §8「实现细节维护在包 README」。
- `git grep` 在 `ee/internal/app`、`ee/internal/host`、`ee/cmd` 下都查不到 `guardrails`，仍符合 Gate 1「不装配到 app」。

**依赖关系**（依据：§5 依赖与协作、§7「模块根包只依赖标准库」「宿主接入只声明窄接口，由 app 显式注入」）：
- 根包：`git grep` 查不到 `engine.go`、`model.go` 有任何 `github.com/` import，仍只依赖标准库，没有反向导入 `plugin`。
- `plugin/text.go:4-11` 的 import 是 `errors strings unicode/utf8`、根包 `guardrails`、`core/schemas`，方向仍是 plugin → 根包，Bifrost 类型留在适配层。
- 差量不涉及 import、构造函数、窄接口、注入方式或资源归属，这几项与上一轮的核对结果相同。

## 出口清单

### 本轮修复项

**上一轮本轮修复项的处置：**

| 编号 | 处置 | 证据 |
|---|---|---|
| F7 README 拒绝清单比代码窄，而且写得像完整列举 | **已修复** | 见下方三点 |

F7 的修复证据：
- **原则先行，明说只是举例。** `README.md:33` 现在写的是：「只接受纯文本消息与已审阅的控制参数，其余一律拒绝。以下仅举例：……」，读者不会再把它当完整清单。
- **上一轮点名的漏项都已补上。**
  - 预测内容、响应格式、联网搜索：对应 `text.go:29`。
  - 文件：「图像/音频/文件等非文本内容」，对应 `text.go:107` 的 `block.File`。
  - 拒答正文：「推理或拒答正文」，对应 `text.go:89`、`:107`。
- **消除了与控制参数的歧义。**
  - 下一句改成「已允许的推理强度、缓存时效等控制参数不作为正文送检」，已按上游 `chatcompletions.go:210,212` 和 `text.go:29` 核实。
  - 句末加了「具体判定见 [text.go](plugin/text.go)」，链接指向的文件存在。
- 逐项对照见「四维核查」。

**本轮新增：** 无

### 需用户裁决的 Blocking

- 无

### 立债（REF：立 `triggered_by: [<本case>]` 的需求进 reports/）

- 无

### 挂账（Finding：登记 findings.md，带四要件）

- 无

### 转代码评审（范围外发现，一行一条，不出结论）

- 无

## 台账对账

**涉及的已有 Finding：**
- **FIND-022**（README 违反规范明文却被评审放过）：本轮逐条对照了编码规范「模块根包要有 README」一节。
  - 同步更新：README:33 与 `text.go` 已经一致，见上方逐项对照。
  - 少提外部名词：新句子没有出现上游类型名，只链接了本包自己的文件。
  - 长度：41 行，在一屏以内。
- **FIND-024**（`internal/host` 目录归位）、**FIND-025**（认证扩展模型）：本轮没有涉及，未恶化。
- **FIND-026**（品牌冒烟失败现场被清理）、**FIND-027**（启动工具提取不到带编号的转交栏）：不在本轮 `ee/` 差量内，沿用已有登记。
- 其余条目与本案无关。

**看似可改但有理由保留（下一轮不再提）：**
- 上一轮 R1–R17 的保留理由不变，差量没有让它们恶化。
- **R18 README:33 的例子与 `text.go` 的拒绝条件部分重合，但不算同一规则写两份：**
  - README 用「以下仅举例」「具体判定见 text.go」明确了代码才是唯一依据，README 只让读者先知道默认拒绝和典型载体。
  - 编码规范要求 README 回答「改之前要知道什么」，这类兼容边界正属于此。只留一个链接，读者就得先读 127 行代码才知道哪些 Chat 请求能用。
  - 反证条件：如果后续 `text.go` 放开了某个例子里的载体而 README 没有同步，说明例子本身在制造维护负担，届时应把例子收缩到原则句加链接。
- **R19 例子里的「停止文本」没有注明是响应侧的 `StopString`，而请求参数 `Stop`（`chatcompletions.go:218`）不在 `text.go:29` 的拒绝条件里：**
  - 这几个字沿用上一版原文，不是本轮新引入的；它和紧挨着的「词元概率」都属于输出侧载体，与测试名 `text_test.go:110` 的 `"stop text"` 口径一致。
  - 读者即使误解，方向也是保守的：会以为请求的 `stop` 也被拒绝，从而少估兼容范围，不会把被拒绝的载体当成放行。句末的链接一跳就能核对。
  - 反证条件：如果装配方或使用文档因此把请求 `stop` 列为不兼容，就应改成「响应中的停止文本」。
- **R20 第 33 行一条列表项塞了 13 个例子，句子偏长：**
  - 这是上一轮 F7 给出的最小修正形状，「原则 → 举例 → 例外 → 依据」都放在同一个语义单元里。再拆成子列表会重新显得像完整清单，正好把 F7 修掉的误读带回来。

---

**起手表覆盖：**

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。差量没有引入数字 |
| 重复 | 同一规则多份实现且互不注明 | 无。README 的例子已写明「仅举例」，完整判定指向 `text.go`（R18） |
| 重复 | 重造已有 helper | 不适用。差量不涉及代码 |
| 重复 | 派生常量无推导 | 不适用。差量没有常量 |
| 职责与实现质量 | 魔数 | 不适用。差量不涉及代码 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用。差量没有触及状态码或错误码 |
| 职责与实现质量 | 无名的错误形状 | 不适用。差量没有新增返回路径 |
| 职责与实现质量 | 紧凑但绕的控制流 | 不适用。差量不涉及代码 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无。没有增删文件；README 41 行，在一屏以内 |
| 职责与实现质量 | 多余状态或守卫 | 不适用。差量不涉及代码 |
| 职责与实现质量 | 同一效果做两次 | 不适用。差量不涉及代码 |
| 职责与实现质量 | 注释与现状矛盾 | 无。README:33 与 `text.go` 已逐项核对一致，README 与代码的一致性在契约维度记录 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。没有改动接缝；README「原则 + 指向依据」的写法与 `pricing/README.md:45` 一致 |
| 架构与项目一致性 | **架构符合性** | 符合，见「四维核查」 |
| 架构与项目一致性 | **依赖关系** | 符合，见「四维核查」 |
| 架构与项目一致性 | 绕过既有出口 | 不适用。差量没有触及存储、迁移、响应或鉴权出口 |
| 架构与项目一致性 | 把上游现状当契约 | 无。README 不再列上游字段全集，改为「已审阅」加链接；已审阅的范围由 `schema_test.go:19` 固定，上游字段变化会触发复审 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无。F7 已关闭，README:33 与 `text.go` 一致；本期没有前端 |
| 契约一致性 | 状态码口径不一 | 不适用。差量没有触及状态码 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。空文本和缺失内容的表述（README:23）没有改动；「已允许的控制参数不作为正文送检」与 `text.go` 对这些字段既不提取也不拒绝的行为一致 |

**开放式复读：** 已放下起手表，以半年后要改这里的维护者的身份，把 README 全文重读一遍，重点看第 33 行，问「第一眼会让人皱眉的是什么」：
- 第 33 行句子偏长，但层次清楚：先原则，再例子，再例外，最后依据。保留理由见 R20。
- 「停止文本」没有写明是请求侧还是响应侧，保留理由见 R19。
- 第 32、34–36 行没有改动，与上一轮复读结论相同。第 38、40 行的装配前提和验证入口仍与现有文件对应（`plugin/integration_test.go`、`plugin/schema_test.go` 都存在）。

**已做开放式复读，无表外发现**（上面两个皱眉点已作为 R19、R20 写明保留理由，不构成修复项）。

**实际静态阅读范围与未执行的检查：**
- 未运行 `go vet`、`go test`、`go build` 和预飞：它们会写构建缓存，本评审只读；差量也不涉及代码。
- 未读 `plugin/plugin.go`、`model.go`、`engine.go`、`text_test.go`（只经 grep 看到第 110 行）、`plugin_test.go`、`integration_test.go`、`engine_test.go` 全文：不在本轮差量内。根包的依赖只用 `git grep` 核对了 import。
- 未读根目录未跟踪的 `workbench/evidence/内容安全框架-期1-交付摘要.md`，也没读 workbench 生成视图、`代码评审3.md`：都是过程文档或生成物，不是被评对象。
- 按要求没读旧代码评审、方案v1、方案评审和更早的收敛评审；只读了上一轮收敛评审记录 `收敛评审3.md`，用于逐条处置。
