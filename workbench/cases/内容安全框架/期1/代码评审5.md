---
title: 内容安全框架 期1 代码评审 #5
type: 代码评审
case: 内容安全框架
phase: 1
round: 5
verdict: 通过
created: 2026-09-17
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8 到候选 f6c9437e657af58e15204412fd2421ed98c5a81e；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 本结论限定候选 `f6c9437e657af58e15204412fd2421ed98c5a81e` 相对 `958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8` 的指定差量及受影响区域。重命名和拆分保留了既有检查、失败策略与阻断行为；显式开发假检测器、app 注册和 HTTP 联调符合补充裁决。七项 checklist 检查及追加的 SQLite 身份 smoke 均亲核通过，未发现本轮必须修复的正确性问题。结论不包含真实检测效果、生产防护、容器或集群运行验证。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 正常、命中、仅记录、失败放行/拦截、取消 | 检查器及 Hook 行为测试覆盖，亲跑通过 |
| Chat 输入、非流式输出 | 全部历史消息和全部候选回答的检查边界保留；不支持载体明确拒绝 |
| 输出流 | 有输出规则时调用模型前拒绝；仅输入规则允许流式，HTTP smoke 验证通过 |
| 可读性整理 | `Checker`、`CheckResult`、`RuleEvaluation` 及职责拆分已落地；原 `engine.go`、`model.go` 删除，调用方完成迁移 |
| 开发假检测器 | 仅显式场景启用，默认关闭；非法值拒绝启动；注册不持久化，关闭后复用数据库重启验证通过 |
| 接口及依赖 | 新增开发环境变量、假检测器和私有装配函数；无新增 HTTP 路由、数据库迁移或 Go 依赖 |
| 部署文件 | 与合入的 `53d83a9ee` 对比无差异；来源已在开发联调证据登记 |
| 多做、少做、未登记偏差 | 按补充裁决理解范围，未发现功能性偏差；文档接口表述同步转收敛评审 |

失败路径已追到最终出口：

| 路径 | 最终行为 |
|---|---|
| 配置非法或注册失败 | 返回 Bootstrap，入口记录错误并非零退出，不进入主 HTTP 监听 |
| 检测器错误、超时、非法结果 | 记录 `Failed` 分类，执行规则明确指定的失败策略 |
| 取消、超限、结构不支持 | 返回安全错误并记录拒绝码；不伪装成功 |
| 输入命中 | ShortCircuit 阻断；HTTP smoke 确认模型调用次数为零 |
| 输出命中 | 丢弃原回答，返回安全错误；HTTP smoke 确认没有 `choices` |
| 安全阻断后的 fallback | `AllowFallbacks=false`，宿主判断保留 |
| 上游错误 | 原样保留，符合已声明边界 |
| smoke 失败 | `finally` 回收自有进程，临时目录及日志保留 |

## 3. 本轮修复项

**无新增必修项。** 已读取上一轮 `代码评审4.md`，其中延续的修复项逐条复核如下：

| 上轮意见 | 处置及证据 |
|---|---|
| R1：输出 logprobs 文本漏检 | **已修复，保持关闭。** [text.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:71) 仍拒绝非 nil `LogProbs`；[text_test.go:111](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:111) 覆盖生成词、候选词和 refusal token，验证原响应不泄露且禁止 fallback。亲跑通过，无需追加修正。 |
| R2：UTF-8 扫描早于限长/取消 | **已修复，保持关闭。** [checker.go:68](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker.go:68) 保留取消→限长→UTF-8 顺序；[text.go:118](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:118) 先检查剩余预算。[checker_test.go:243](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker_test.go:243) 与 [text_test.go:162](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:162) 回归通过，无需追加修正。 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无新增事项。**

**有依据撤回：无新增撤回项。** 上轮已裁决的 logprobs 空结构、提前拒绝请求参数、消息名称兼容性等限制不重新提出。

本轮收到的收敛评审「转代码评审」原文为“无”，没有待处置转交项。FIND-026/027 属既有范围外工具问题，本轮未扩大为必修项。

## 5. 转收敛评审 / 挂账

- 转收敛评审：[方案接口表述:40](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/cases/内容安全框架/期1/方案v2.md:40) 及旧交付摘要中的类型、文件链接与本轮重命名后的现状同步。

**挂账：本轮范围内无。**

## 6. 开放式复读的表外发现

**已做开放式复读，无表外发现。**

按“失败如何发现、如何恢复”重新检查了配置入口、检查器、Hook、假检测器、smoke 及部署文件：启动配置错误有明确出口，检查失败有分类日志和响应，开发开关可通过取消设置并重启恢复。部署文档已明确当前集群兼容限制，本轮没有将其视为可直接部署的验证结论。

## 7. 起手表覆盖

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | 无：规定的框架行为及必过命令已验证 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | 发现：接口表述同步，已转收敛评审；功能范围一致 |
| 对照方案 | 多出的功能、公共接口、抽象或依赖 | 无：开发装配属于补充裁决 |
| 对照方案 | 偏差未登记 | 无：开发联调、整理及部署合并来源有记录 |
| 行为正确性 | 失败被吞或降级成假成功 | 无：失败出口已追踪 |
| 行为正确性 | 空值、空集合与非空无分支 | 无：空策略、空正文、缺失正文有区分 |
| 行为正确性 | 取消或超时后仍写入或产生副作用 | 无：父取消阻断；同步协作取消边界已说明 |
| 行为正确性 | 重发产生两份副作用 | 不适用：护栏无业务持久化副作用；每次检查日志符合语义 |
| 行为正确性 | 事务外写后读、部分回滚 | 不适用：本轮无业务事务变更 |
| 行为正确性 | 关闭、旧数据、旧客户端无处理 | 无：默认关闭及同库重启关闭亲测通过 |
| 行为正确性 | 昂贵检查先于廉价检查、校验缺层 | 无：限长顺序及载体拒绝保持 |
| 行为正确性 | 路由保护、认证关闭行为 | 无：没有新增认证旁路；身份 smoke 通过 |
| 行为正确性 | 敏感信息进日志或响应 | 无：检查日志不含正文、检测器异常原文；相关断言通过 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | 无：假检测器只做有界文本匹配，未新增外部 I/O |
| 行为正确性 | 配置、环境依赖、部署步骤未登记 | 无：环境变量和部署要求已有说明 |
| 失败可观测 | 服务端无法追溯原因 | 无：规则、阶段、动作、失败分类及拒绝码可见 |
| 失败可观测 | 对外泄露内部错误原文 | 无：错误映射使用固定文案 |
| 失败可观测 | 脚本失败现场被清理 | 无：本轮两个 smoke 保留现场；未重提 FIND-026 |
| 测试可信 | 新规则或失败路径无测试 | 无：配置拒绝、处置及流式边界有测试 |
| 测试可信 | 断言绑定实现而非行为 | 无：主要断言检查响应、模型调用次数、日志和重启行为 |
| 测试可信 | 失败信息无法定位条件 | 无：用例名称、调用栈、实际值及现场可定位 |
| 测试可信 | 返回值、错误未检查 | 无：关键返回值和错误出口已核对 |
| 测试可信 | fixture 依赖个人环境 | 无：本轮 smoke 使用临时 SQLite、随机端口、自有进程 |
| 测试可信 | mock 结果冒充真实环境结论 | 无：明确区分真实 HTTP 链路与模型/检测器替身 |
| 测试可信 | 整体替换被测模块的 mock | 无：实际执行 Checker、Hook、app 装配和 BF 管线 |
| 残留 | 调试输出、临时后门、本机配置 | 无：假检测器属于显式授权范围，默认关闭且有启动警示 |
| 残留 | 未使用代码、无关改动 | 无：旧实现删除，调用方迁移；部署改动来源明确 |
| 残留 | 生成物与源不一致 | 无：末次工作台视图检查通过 |

## 8. 验证记录

环境：`go1.27.1 darwin/arm64`。以下 `$TMPDIR` 为指定目录：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全框架-期1-代码评审5/scratch`

| 实际命令 / 检查 | 结果 |
|---|---|
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race -count=1 ./...` | 全部通过；禁用测试结果缓存 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过 |
| `gofmt -l ee/internal/guardrails ee/internal/app/guardrails.go ee/internal/app/guardrails_test.go` | 通过，无输出 |
| `git diff --check` | 通过，末次复查通过 |
| `git diff --check 958fc983..f6c9437e` | 通过 |
| `python3 -B workbench/tools/build_views.py --check` | 通过，145 篇文档；末次复查通过 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | 通过：两次 disabled 及三个启用模式全部 PASS |
| `python3 -B ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | 通过 |
| Python `Path.is_file()` 检查五项必存产物 | 全部存在 |
| `rg -n 'type Detector interface' ee/internal/guardrails` | 命中 `detector.go:23` |
| `rg -n 'proxywasm\|wasm-go\|net/http\|fasthttp' ee/internal/guardrails`，使用正则交替 | 无命中，退出码 1 为预期 |
| Python 检查 guardrails Go 文件行数 | 最大 271 行 |
| `git diff 53d83a9ee f6c9437e -- .dockerignore Dockerfile deploy` | 无差异，确认部署文件来源 |
| 两个基线的 `git merge-base --is-ancestor … HEAD` | 均通过 |
| 读取本轮 smoke 日志 | 命中阻断、观察、输出流拒绝及开发警示均存在 |
| `ps -axo pid=,args=` | 被沙箱拒绝，未获得全局进程清单；未尝试绕过 |

构建二进制 SHA-256：

```text
e45ed1a79246413b8872e67de95f068d6edced932fb52932c9d43c2ac0b9425d
```

两个 smoke 均正常退出，其 `finally` 执行进程停止和等待。现场保留于：

- `$TMPDIR/ee-guardrails-h0jde2b6`
- `$TMPDIR/bifrost-identity-smoke-_8cpkel0`

Git 绑定：初始工作树干净，无未跟踪文件；末次 HEAD 仍为指定候选，索引无变化，无未跟踪文件。评审期间外部仅追加以下证据文件 7 行，已重新读取：

```text
 M workbench/evidence/内容安全框架-期1-开发联调.md
```

实际阅读范围及未执行项：

- 完整读取指定规则、需求、方案、findings、checklist、三份证据及上一轮 `代码评审4.md`。
- 完整读取全部指定差量文件现状；删除的 `engine.go`、`model.go` 读取上一候选版本对照。补读护栏其余测试，以及宿主注册、排序、前后 Hook、fallback、启动错误出口、EE Makefile 和 Docker entrypoint 的相关实现。
- 未读取其他旧评审或收敛评审正文。上游运行代码及依赖在两候选间无差异，结合调用链核查，保持差量复核。
- **checklist 无跳过项。** 构建输出和 smoke 二进制参数改为 scratch，Python 增加 `-B`；未运行会写报告的预飞包装器，已逐项亲核。
- 未启动 `--serve`、dev server 或浏览器 Playground；受本轮禁止持续服务及操作已有实例的边界约束。
- 未运行 Docker 构建、容器或 Kubernetes 命令：不属于 checklist，且本轮禁止操作服务及已有实例；部署文件仅作静态复核。
- 未运行 PostgreSQL、真实模型/检测服务、生产插件组合及性能验证：不属于本期验收范围，也未据替身测试作此类结论。
- 未创建报告、修改源码或索引，未暂存、提交、推送；仅产生获准的构建与测试产物。

verdict: 通过
