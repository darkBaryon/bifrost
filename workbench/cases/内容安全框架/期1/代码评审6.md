---
title: 内容安全框架 期1 代码评审 #6
type: 代码评审
case: 内容安全框架
phase: 1
round: 6
verdict: 通过
created: 2026-09-17
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 f6c9437e657af58e15204412fd2421ed98c5a81e 到候选 cf1a35646b7abd6dec8dbe9a871ee42649fa267e；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 本结论限定候选 `cf1a35646b7abd6dec8dbe9a871ee42649fa267e` 相对上一候选 `f6c9437e657af58e15204412fd2421ed98c5a81e` 的四个指定文件及受影响区域。运行代码为等值常量替换，测试断言未改变，未发现新增正确性问题或未关闭必修项。EE vet、全量非缓存 race、构建及自包含 HTTP smoke 均亲核通过。候选初始工作树的视图检查通过；评审期间外部新增 `收敛评审6.md` 后出现生成视图漂移，单独转交，不将当前工作树描述为预飞全绿。结论不包含生产防护、浏览器或集群运行验证。

## 2. 对照方案与接口面对账

| 对账项 | 亲核结果 |
|---|---|
| app 装配改动 | 检测器注册键、规则 ID 均保持 `"dev-fake"`；实际替换依赖中的 `StatusBadRequest` 等于原值 `400`。默认关闭、三种模式及注册顺序不变 |
| 测试改动 | 两个测试名称、注释和失败文案调整；输入、执行步骤及断言不变 |
| README 改动 | 父包文件表移动 `deny.go` 行；删除的 fake 子包说明已读取上一候选版本，父包仍保留标记、模式、启动和能力边界说明 |
| 接口及依赖 | 无新增公开符号、HTTP 路由、数据迁移或模块依赖；`fasthttp` 已是 EE 直接依赖 |
| 验收行为 | 正常、命中、仅记录、失败放行/拦截、取消及输出流拒绝均有行为测试，本轮重新执行通过 |
| 三方不一致、多做/少做、未登记偏差 | 未发现本轮功能性偏差；声明修改与实际差量一致 |

命名、常量组织、文档组织和契约同步的质量判断归收敛评审，本轮只核其行为影响。宿主、框架、插件、模块依赖及 smoke 脚本在两候选间无变化，结合等值替换与调用链核查，维持差量复核。

失败路径已追到最终出口：

| 路径 | 最终行为 |
|---|---|
| 非法开发配置、构造或注册失败 | 逐层返回至入口，记录启动错误并非零退出，不进入主 HTTP 监听 |
| 检测器错误、超时、非法结果 | 记录失败分类，执行显式失败策略 |
| 父请求取消、超限、不支持载体 | 返回安全拒绝错误；取消不降级为失败放行 |
| 输入或输出命中 | 输入短路、输出丢弃原响应；禁止 fallback |
| smoke 成功或失败退出 | `finally` 停止自有应用和模型服务，保留现场 |

## 3. 本轮修复项

**无新增必修项。** 已读取 `代码评审5.md`，其“本轮修复项”没有新增问题；延续的两项逐条复核如下：

| 上轮意见 | 处置与证据 |
|---|---|
| R1：输出 logprobs 文本漏检 | **已修复，保持关闭。** [text.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:71) 继续拒绝非 nil `LogProbs`；[text_test.go:111](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:111) 起覆盖生成词、候选词和 refusal token，断言原响应不返回、安全错误禁止 fallback。本轮 race 通过，无需追加修正。 |
| R2：UTF-8 扫描早于限长/取消 | **已修复，保持关闭。** [checker.go:68](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker.go:68) 保持取消→长度→UTF-8 顺序；[text.go:118](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:118) 先检查剩余预算。[checker_test.go:243](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker_test.go:243) 的更名未削弱断言，相关回归本轮通过，无需追加修正。 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无新增事项。**

收敛评审转来的三项逐条处置：

| 转交项 | 处置及依据 |
|---|---|
| guardrails smoke 未加入 Makefile，是否属于必过验证 | **撤回“缺少本期必过验证”的疑虑；确认必须运行。** [checklist.yaml:17](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/cases/内容安全框架/期1/checklist.yaml:17) 已列 `guardrails-http`。本轮实际执行成功。Makefile 是否增加聚合入口属于收敛判断，不据此认定验收缺失。 |
| 临时目录不清理，browser.json 先写入后改权限 | **撤回本轮凭据暴露必修要求。** [guardrails-smoke.py:89](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/guardrails-smoke.py:89) 使用 `mkdtemp`；本机标准库实现以 0700 原子创建目录，本轮现场权限也实测为 0700。[第111行](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/guardrails-smoke.py:111) 的文件始终位于该私有目录中，随后设为 0600；没有证据证明其他用户可利用写入与 chmod 间隙读取。凭据为隔离实例随机生成，现场保留便于排错，进程由 `finally` 回收。未运行 `--serve`，故 browser.json 路径仅作静态核查，不冒充动态验证。 |
| develop 带入的镜像、用户、探针及旧集群兼容限制 | **撤回本轮安全框架必修要求，不撤回部署限制。** `git diff 53d83a9ee cf1a35646 -- .dockerignore Dockerfile deploy` 无差异；[K8s部署步骤.md:38](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/deploy/K8s部署步骤.md:38) 已明确要求适配清单或升级集群后再部署。部署不属于本轮差量和验收范围，未据本轮测试认定可部署。 |

已裁决的 logprobs 空结构、消息名称等兼容限制不重提；既有 FIND-026～029 不扩大为本轮必修项。

## 5. 转收敛评审 / 挂账

- 转收敛评审：外部新增 `workbench/cases/内容安全框架/期1/收敛评审6.md` 后，视图检查报告 `views/all.md`、`SUMMARY.md`、`assets/nav.js`、`cases/内容安全框架/index.md` 四处漂移。
- 转收敛评审：部署转交项的后续归属与已声明集群限制，留部署维护范围处理，本轮不作部署结论。

**挂账：本轮范围内无。**

## 6. 开放式复读的表外发现

**已做开放式复读，无表外发现。**

重新通读四个文件的现状或删除前版本，按“如何发现故障、如何恢复”检查：装配失败有启动日志和退出码；检查失败有分类日志与拒绝响应；测试失败仍保留实际结果和错误；开发模式可取消环境变量后重启关闭，HTTP smoke 已验证同库重启恢复默认行为。

## 7. 起手表覆盖

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | 无：本轮受影响行为及必过检查已亲核；浏览器验证不冒充重测 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | 无：声明修改与四文件差量一致 |
| 对照方案 | 多出的功能、公共接口、抽象或依赖 | 无：新增常量私有且等值，无新增模块依赖 |
| 对照方案 | 偏差未登记 | 无：整理及开发联调有用户裁决和补充记录 |
| 行为正确性 | 失败被吞或降级成假成功 | 无：追至启动退出或安全拒绝出口 |
| 行为正确性 | 空值、空集合与非空无分支 | 无：关闭配置、空策略、nil 实例和空正文均有对应行为 |
| 行为正确性 | 取消或超时后仍写入或产生副作用 | 无：相关路径未改，取消与失败策略回归通过 |
| 行为正确性 | 重发产生两份副作用 | 不适用：本轮无业务持久化副作用，逐请求日志符合语义 |
| 行为正确性 | 事务外写后读、部分回滚 | 不适用：本轮无事务或持久化变更 |
| 行为正确性 | 关闭、旧数据或旧客户端无处理 | 无：默认关闭、同库重启关闭通过，协议边界未变 |
| 行为正确性 | 昂贵检查先于廉价检查、校验缺层 | 无：取消及限长优先顺序保留 |
| 行为正确性 | 路由保护、认证关闭行为 | 无：未新增路由或修改鉴权，关闭分支直接返回 |
| 行为正确性 | 敏感信息进日志或响应 | 无：正文及检测器异常隐私断言通过；临时凭据见第4节 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | 无：运行差量仅等值常量替换 |
| 行为正确性 | 配置、环境依赖、部署步骤未登记 | 无：本轮无新增项，开发环境变量已有说明 |
| 失败可观测 | 服务端无法追溯原因 | 无：启动错误、规则执行分类及拒绝码可见 |
| 失败可观测 | 对外泄露内部错误原文 | 无：固定错误映射，隐私回归通过 |
| 失败可观测 | 脚本失败现场被清理 | 无：smoke 保留目录和日志，回收自有进程 |
| 测试可信 | 新规则或失败路径无测试 | 无：无新增路径，既有行为回归重新执行 |
| 测试可信 | 断言绑定实现而非行为 | 无：更名未改断言；检查动作、响应、调用次数和日志 |
| 测试可信 | 失败信息无法定位条件 | 无：用例名称及结果、错误输出保留 |
| 测试可信 | 返回值、错误未检查 | 无：装配错误逐层返回，关键测试返回值有断言 |
| 测试可信 | fixture 依赖个人环境 | 无：smoke 使用随机端口、临时 SQLite、自有进程 |
| 测试可信 | mock 结果冒充真实环境结论 | 无：区分真实 HTTP 链路与模型、检测器替身 |
| 测试可信 | 整体替换被测模块的 mock | 无：真实运行 Checker、Hook、app 注册及 BF 管线 |
| 残留 | 调试输出、临时后门、本机配置 | 无：开发假检测器属于已授权范围，默认关闭且有警示 |
| 残留 | 未使用代码、无关改动 | 无：新增常量均被使用，四文件差量对应声明修改 |
| 残留 | 生成物与源不一致 | 发现：外部新增评审后四处视图漂移，已转交 |

## 8. 验证记录

环境：`go1.27.1 darwin/arm64`。以下 `$TMPDIR` 均为：

```text
/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全框架-期1-代码评审6/scratch
```

| 实际命令 / 检查 | 结果 |
|---|---|
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race -count=1 ./...` | 全部通过，禁用测试结果缓存 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过 |
| `gofmt -l ee/internal/guardrails ee/internal/app/guardrails.go ee/internal/app/guardrails_test.go` | 通过，无输出 |
| `git diff --check` | 通过，结束时复查通过 |
| `git diff --check f6c9437e657af58e15204412fd2421ed98c5a81e..cf1a35646b7abd6dec8dbe9a871ee42649fa267e` | 通过 |
| `python3 -B workbench/tools/build_views.py --check` | 初始通过，147篇；外部新增评审后复查报告四处漂移 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | 通过：disabled → input-block → input-observe → output-block → disabled |
| Python `Path.is_file()` 逐项核查五个必存产物 | 全部存在 |
| `rg -n 'type Detector interface' ee/internal/guardrails` | 命中 `detector.go:23` |
| `rg -n 'proxywasm\|wasm-go\|net/http\|fasthttp' ee/internal/guardrails`（正则交替） | 无命中，符合禁留要求 |
| Python 统计 guardrails Go 文件行数 | 最大271行 |
| 两个基线分别执行 `git merge-base --is-ancestor … HEAD` | 均通过 |
| `git diff --exit-code f6c9437e..cf1a35646 -- core framework transports plugins ee/go.mod ee/go.sum ee/scripts` | 无差异 |
| `git diff 53d83a9ee cf1a35646b7abd6dec8dbe9a871ee42649fa267e -- .dockerignore Dockerfile deploy` | 无差异 |

构建产物 SHA-256：

```text
75af2dc7c1ee98ca7e9d148970b9a25a3c72b05d6a08526c0d8be11935087021
```

smoke 现场：`$TMPDIR/ee-guardrails-r3vga_1p`。亲核根目录权限 0700、配置文件 0600；日志含三次开发警示、observe/block 和输出流拒绝记录，末尾为清理完成及服务停止。脚本正常退出。

Git 范围绑定：

- 初始分支 `feat/content-safety`，HEAD 为指定候选，工作树及索引干净，无未跟踪文件。
- 已执行基线 `git diff --stat`、两候选间文件清单和完整 EE 差量核对。
- 结束时 HEAD 未变，已跟踪文件和索引仍无差异；唯一新增未跟踪文件为外部生成的 `workbench/cases/内容安全框架/期1/收敛评审6.md`，已纳入状态记录，未读取正文。

实际阅读范围及未执行项：

- 读取指定规则、验收口径、方案、findings、checklist、三份证据和允许读取的 `代码评审5.md`。
- 四个范围文件完整阅读；删除的 fake README 读取上一候选版本。补读 guardrails 实现和全部测试、app 装配测试、Bootstrap 与入口错误出口、宿主注册实现、smoke 全文及共享 Node 夹具、EE Makefile。
- 部署转交仅核查来源差异、Dockerfile、三个 YAML 及部署说明相关段落；未读取其他旧评审或本轮收敛正文。
- **checklist 无跳过项。** 构建和 smoke 使用 scratch 二进制；Python 加 `-B`。未运行会写报告的预飞包装器，已逐项亲核其检查。
- 未运行 `--serve`、dev server、浏览器 Playground：本轮禁止持续开发服务和操作已有实例，且差量没有 UI 改动。
- 未运行 Makefile 聚合构建、额外品牌/身份 smoke、Docker/Kubernetes、PostgreSQL或真实模型服务：不属于本轮 checklist；相关实现未发生差量，部署及已有实例操作也不在授权范围。
- 未创建报告、修改代码或索引，未暂存、提交、推送；仅生成获准的构建与测试产物。

verdict: 通过
