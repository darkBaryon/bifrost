---
title: 内容安全框架 期1 收敛评审 #3
type: 收敛评审
case: 内容安全框架
phase: 1
round: 3
verdict: 有Blocking
created: 2026-09-16
---

> 评审者与实施上下文隔离（不能是写代码的会话自评）。基线 `7c81459aa0e62750f22f57b57c3c4a4750a98cfc`；本轮为差量复核，范围是上一轮候选 `c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe` 到本轮候选 `8fd978c7c76f718c38093f15e390f84d40fa4661`。`git rev-parse HEAD` 与本轮候选一致。
> L2：四维收束为段落与表格。

## 结论

**总体判断：** 上一轮的 F5、F6 和取舍项 S3 都已落地，证据见「本轮修复项」开头的处置表。

- 差量只有四个文件，一共 +9/−2 行，都是注释、README 和测试清单，不涉及运行行为。
- 架构与依赖关系没有变化，仍符合 EE 后端架构 §4–§7：
  - 根包没有新增 import。
  - `plugin/` 没有新增 import。
  - `app` 没有改动。
- 新增的两行注释放在该放的位置，能挡住「把 UTF-8 校验合并回首个守卫」这种误改。
- README 拆成列表后，读起来明显更清楚。透传阈值的原因，已按上游 `largeresponse.go:154-218` 的实际时序改写成白话。

但 F5 重写拒绝清单时引入了一处新的不准确（F7），需要修：
- 「多模态」被收窄成「图像或音频」，而代码还拒绝文件内容块、视频和非文本输出模态。
- 这句写成冒号后逐项列举，读起来像完整清单，但漏掉了 `text.go:29` 明确拒绝的预测内容、响应格式、联网搜索等已知参数。
- 下一句又写着「控制参数不作为正文送检」，读者容易把这些漏列的参数当成放行。

F7 是一句话的局部修改，不需要用户裁决。关闭之前 verdict 维持「有Blocking」。

## 事实来源

**执行的命令**（均为只读）：
- `git status --porcelain --untracked-files=all`：输出一个未跟踪文件 `workbench/evidence/内容安全框架-期1-交付摘要.md`。
  - 它是 workbench 过程文档，不在本轮被评文件清单里。
  - 提示词说「工作树干净」，实际多这一个文件。它不影响被评代码，这里如实记录。
- `git rev-parse HEAD`：`8fd978c7c76f718c38093f15e390f84d40fa4661`。
- `git log --oneline 7c81459aa..HEAD`：共三个提交，即 `cf2d94083`、`c503a4bb8`、`8fd978c7c`。
- `git diff --stat c503a4bb8..8fd978c7c`：
  - 4 个 `ee/` 文件与提示词清单一致。
  - 其余 8 个是 workbench 过程文档与生成物，不在范围内。
- `git diff c503a4bb8..8fd978c7c -- ee/`：确认差量为 README 第 32 行拆成 5 条列表，`engine.go:77`、`text.go:121` 各加一行注释，`schema_test.go:18` 加一行清单。
- `git show c503a4bb8:ee/internal/guardrails/README.md`：取旧版第 32 行原文，与新版逐项比对。

**完整通读的被评文件（现状），及各自职责：**
- `guardrails/README.md`（40 行）：包的问题陈述、文件级调用链、检测契约、接入前提与验证入口。
- `engine.go`（153 行）：校验配置并保存快照，按阶段顺序执行规则，处理取消、超限、超时与失败。
- `plugin/text.go`（127 行）：提取有界纯文本，拒绝不支持的载体。
- `plugin/schema_test.go`（41 行）：固定已审阅的上游 Chat 字段清单，上游升级时触发复审。
- 为核对注释和 README 的表述，另读了范围外的内容：
  - `plugin/text_test.go` 第 55–176 行。
  - `engine_test.go` 第 242–247 行，经 grep 定位。

**规则与裁决：**
- 已读：
  - 根 AGENTS.md、workbench/AGENTS.md、收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md、收敛评审模板。
  - EE后端架构.md 全文、findings.md（含 FIND-026/027）。
- 方案v2 全文已读，包括「Gate 决策结果」「实施偏差记录」「第一轮评审修复记录」和「第二轮评审修复记录」（含代码评审 2 转交原文）。
- 已裁决、不重提的内容：
  - 输出流检查不支持，只有输入规则时放行流式请求。
  - 不装配到 app。
  - 按纯文本契约拒绝整个未覆盖载体。方案 §4 第 42 行写明，第二轮修复记录第 123 行再次确认。
  - logprobs、Name 的可用性问题已转代码复核。
- 上一轮记录 `收敛评审2.md` 已读，用于逐条处置修复项，并沿用其保留项 R1–R14。

**同类实现对照：**
- README 的列表写法：`ee/internal/pricing/README.md` 是规范指定的参照样例，本轮没有重读。上一轮已完成结构对照，本轮差量只把一段拆成列表，没有改变「问题 → 调用链 → 约束」的结构。
- 注释写法：编码规范「注释写清用途、约束和原因」要求内部函数的特殊处理解释原因。`engine.go:77` 与 `text.go:121` 各用一行写明原因，与 `text.go:21`、`:59` 已有的一行式原因注释风格一致。
- 字段清单：EE 内没有其他「固定上游字段清单」的先例，没有可对照的同类实现，与上一轮结论相同。

**上游假设核实：**
- `core/schemas/chatcompletions.go:14-21`：`BifrostChatRequest` 的 6 个字段依次是 `Provider Model Input Params Fallbacks RawRequestBody`，与 `schema_test.go:18` 逐字一致。
- `core/providers/utils/largeresponse.go`：
  - `:97-103` 的 `PrepareResponseStreaming` 在阈值 > 0 时，发请求前就把响应改为流式读取。
  - `:154-218` 的 `FinalizeResponseWithLargeDetection` 在拿到 Provider 响应之后，响应体超过阈值时才设置 `BifrostContextKeyLargeResponseMode = true`（`:218`）。
  - 这就证实了 README:34「宿主可能在调用模型之后才切换为透传」和 `text.go:21-24` 的判定。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无新增 | 两行新注释分别解释两处各自的判断顺序，防的对象不同（R15），不算同一规则写两份 |
| 职责与实现质量 | 无新增。F6 已关闭 | `engine.go:77`、`text.go:121` |
| 架构与项目一致性 | 无。S3 已采纳。架构符合性、依赖关系均符合 | 见下 |
| 契约一致性 | F7（README:33 拒绝清单比代码窄，而且写得像完整列举） | `README.md:33` 对照 `text.go:29,32-35,65,89,107` |

**架构符合性**（依据：EE 后端架构 §4 模块内部的职责、§6 插件与普通业务、§7 与 Bifrost 的协作边界）：
- 差量没有改变模块划分和职责归属：
  - 根包 `guardrails` 的 `engine.go` 只加了一行注释，仍承担规则执行与处置（§4「模块根包：业务模型、服务、规则、错误及所需接口」）。
  - `plugin/text.go` 只加了一行注释，仍只负责把 Chat 结构转成文本（§4「`plugin/` 接入 Bifrost 钩子，转换输入与处理结果」）。
  - `schema_test.go` 加的一行仍是 `plugin` 负责适配的上游类型，Bifrost 类型没有因此进入根包（§5「Bifrost 的具体类型留在适配实现中」）。
- README 的调整只影响文档表达。§8 要求「实现细节维护在包 README」，本轮改动与之一致。
- 差量没有触及 `ee/internal/app`、`ee/internal/host`（diff --stat 里没有），仍符合 Gate 1「不装配到 app」。

**依赖关系**（依据：§5 依赖与协作、§7「宿主接入只声明窄接口，由 app 显式注入」）：
- `engine.go:4-11` 的 import 为 `context errors fmt reflect strings unicode/utf8`，全是标准库，差量没有改动。
- `text.go:4-11` 的 import 为 `errors strings unicode/utf8`、根包 `guardrails`、`core/schemas`，方向仍是 plugin → 根包，差量没有改动。
- `schema_test.go:4-11` 是外部测试包，只 import 标准库与 `core/schemas`，新增的一行没有引入新依赖。
- 构造注入、logger 窄接口和资源归属都不在差量内，与上一轮核对结果相同。

## 出口清单

### 本轮修复项

**上一轮本轮修复项的处置：**

| 编号 | 处置 | 证据 |
|---|---|---|
| F5 README 上游名词未解释、多条规则压在一段 | **已修复** | 原第 32 行拆成 `README.md:32-36` 五条列表：输入与输出范围、拒绝载体、透传、流式准入、上游错误。透传一条（`:34`）不再出现上游符号，改写为「宿主为超大请求或响应开启的原样透传模式不支持。即使只配置了大响应阈值，也会拒绝：宿主可能在调用模型之后才切换为透传，安全检查来不及介入」，已用 `largeresponse.go:154-218` 核实。词元概率就地解释为「包含模型生成的词及候选词」（`:33`）。拆分后的拒绝清单另有不准确，见 F7，不算 F5 复发 |
| F6 输入校验拆成两处但没有原因注释 | **已修复** | `engine.go:77`「先判取消和长度，再做线性的 UTF-8 扫描，避免超限输入消耗扫描成本。」，紧贴 `:78` 的 `utf8.ValidString`。`text.go:121`「先判剩余预算，再做线性的 UTF-8 扫描，避免超限片段消耗扫描成本。」，紧贴 `:122`。两处注释与测试 `engine_test.go:242`、`text_test.go:162` 的意图一致，与实际顺序（`engine.go:71-80`、`text.go:118-124`）一致 |

上一轮取舍项 S3 已被采纳：`schema_test.go:18` 新增 `{schemas.BifrostChatRequest{}, "Provider Model Input Params Fallbacks RawRequestBody"}`，与上游 `chatcompletions.go:14-21` 逐字一致，放在清单首行，也就是提取入口结构的位置。

**本轮新增：**

**F7. README 的拒绝清单比代码窄，而且写成完整列举，与下一句「控制参数不送检」连读会误导**

- **位置：** `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:33`
- **代码事实：**
  - README:33 写的是「未覆盖的内容明确拒绝：未经规范化的原始正文、未知参数、工具、图像或音频、推理正文、消息名称、停止文本，以及词元概率……」。上一版第 32 行这里写的是「多模态」，本轮改成了「图像或音频」。
  - 代码实际还拒绝以下载体，README 都没有写：
    - `text.go:107`：文件内容块（`block.File`）、引用（`block.Citations`）、缓存点（`block.CachePoint`）、拒答文本（`block.Refusal`）。
    - `text.go:65`：响应里的视频（`chat.Videos`）、搜索结果、引用。
    - `text.go:32-35`：非 `text` 的输出模态。
    - `text.go:29`：已知参数中的预测内容（`Prediction`）、响应格式（`ResponseFormat`）、联网搜索（`WebSearchOptions`）、MCP 服务器、容器、上下文管理。
    - `text.go:89`：助手消息里的拒答文本和注释。
  - README:33 的下一句是「推理强度、缓存时效等控制参数不作为正文送检」。
- **维护影响：**
  - 这是接手者和装配方判断「哪些 Chat 请求能用」的唯一手写契约。清单用冒号逐项列举，读起来像完整清单。
  - 读者对照上游参数表，会认为 `ResponseFormat`、`Prediction` 这类不在拒绝清单里的参数属于「控制参数，不送检但放行」。而 `Prediction` 恰恰带有会发给模型的文本，代码实际是拒绝的。
  - 装配方据此预估兼容范围，结果会与实际行为不一致；维护者想放开某个参数时，也会先误以为它已经放开。
  - 「图像或音频」是本轮新引入的收窄。编码规范要求「包的功能、文件或约束发生变化时同步更新」，也要求注释与代码不一致视为缺陷。README 是这一约束的手写开发指南，同样适用。
- **最小修正：** 把 README:33 改成先讲原则、再举例。例如：「只接受纯文本消息与已审阅的控制参数，其余一律拒绝，例如未经规范化的原始正文、未知参数及工具、预测内容、响应格式等会携带文本的参数、图像/音频/文件等非文本内容、推理正文、消息名称、停止文本，以及词元概率（包含模型生成的词及候选词）。」不必逐项列全，只要让读者知道清单只是举例、默认拒绝即可。

### 建议取舍项（实施者决定采纳或撤回）

- 无

### 需用户裁决的 Blocking

- 无

### 立债（REF：立 `triggered_by: [<本case>]` 的需求进 reports/）

- 无

### 挂账（Finding：登记 findings.md，带四要件）

- 无

### 转代码评审（范围外发现，一行一条，不出结论）

- 无新增。上一轮转交的 logprobs 空结构、主动请求 logprobs、消息 `Name` 可用性三条，方案v2 第二轮修复记录第 123 行已写明继续交代码复核，本侧不重复。

## 台账对账

**涉及的已有 Finding：**
- **FIND-022**（README 规范明文违规被评审放过）：本轮逐条对照编码规范「模块根包要有 README」一节。
  - 外部名词：README:34 不再出现上游符号，:33 就地解释了词元概率，F5 关闭。
  - 同步更新：README:33 的清单与代码不一致，列为 F7，没有放过。
- **FIND-024**（`internal/host` 目录归位）：本轮没有触及，未恶化。
- **FIND-026**（branding-smoke 失败现场被清理）：代码评审 2 的转交原文（方案v2 第 132 行）要求收敛侧处置。该问题已按四要件登记为 FIND-026，脚本不在本轮范围，本评审未读。沿用登记，不重复立项。
- **FIND-027**（启动工具提取不到带编号的转交栏）：代码评审 2 的另一条转交，即评审落盘后生成视图漂移，属于 workbench 生成物与工具行为，不在本轮 `ee/` 被评范围。方案v2 第 124 行已记录重新生成，提取缺陷登记在 FIND-027。本侧不裁决生成物状态，漂移与否由预飞 `build_views --check` 证明。
- **FIND-012、FIND-025**：没有改 `app` 装配或认证链，未涉及。
- 其余条目与本案无关。

**看似可改但有理由保留（下一轮不再提）：**
- 上一轮 R1–R14 的保留理由不变，差量没有使其恶化。
- **R15** `engine.go:77` 与 `text.go:121` 两行注释措辞几乎相同：
  - 它们解释的是两个包里各自的判断顺序，防的对象不同。前者防根包 `Check` 的直接调用方，后者防拼接前的单段文本，返回的错误也不同（`ErrInvalidInput` 与 `errUnsupported`）。
  - 这是上一轮 F6 要求的「各加一行」，不是同一规则写两份。
  - 如果抽成共享函数，会让 plugin 为一行校验依赖根包的内部顺序，没有维护收益。
- **R16** `text.go:21` 的注释仍用「Provider」「reader 透传」等上游词汇：
  - 代码注释的读者是正在读这段上游适配代码的人，旁边就是 `BifrostContextKeyLargeResponseThreshold`，这些词就地可查。
  - 编码规范「少提外部名词」针对的是 README，面向没读过上游代码的人；README:34 已改成白话。
- **R17** `schema_test.go:18` 把 `BifrostChatRequest` 放在清单首行，而不是按上游文件顺序排：
  - 清单按「提取入口 → 参数 → 消息 → 响应」的阅读顺序排列，与 `text.go` 的调用顺序一致。
  - 顺序不影响断言，每个结构体各自一个子测试。

---

**起手表覆盖：**

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。差量没有引入数字；字节上限仍只从 `Engine.maxTextBytes` 读取 |
| 重复 | 同一规则多份实现且互不注明 | 无。两处 UTF-8 校验防的对象不同，且现在都有原因注释（R15） |
| 重复 | 重造已有 helper | 无。差量没有新增函数 |
| 重复 | 派生常量无推导 | 无。差量没有新增常量 |
| 职责与实现质量 | 魔数 | 无。`schema_test.go:18` 的字段名字符串是被固定的清单本身 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用。差量没有触及状态码或错误码 |
| 职责与实现质量 | 无名的错误形状 | 不适用。差量没有新增返回路径 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无。F6 已由注释关闭，控制流本身没有变化 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无。差量没有增删文件；四个文件都远低于 300 行 |
| 职责与实现质量 | 多余状态或守卫 | 无。差量没有新增守卫 |
| 职责与实现质量 | 同一效果做两次 | 无 |
| 职责与实现质量 | 注释与现状矛盾 | 无。`engine.go:77` 与 `:71-80` 的顺序一致，`text.go:121` 与 `:118-124` 一致，`schema_test.go:1` 的承诺现已覆盖请求根结构。README 与代码不一致的地方归入契约维度的 F7 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。差量没有改动接缝；logger、错误码、文件命名与同类的对照结论同上一轮 |
| 架构与项目一致性 | **架构符合性** | 符合，见「四维核查」 |
| 架构与项目一致性 | **依赖关系** | 符合，见「四维核查」 |
| 架构与项目一致性 | 绕过既有出口 | 不适用。差量没有触及存储、迁移、响应或鉴权出口 |
| 架构与项目一致性 | 把上游现状当契约 | 无。S3 采纳后，提取器读取的请求根、消息、参数、响应、候选都在字段清单测试里；README:34 关于透传时序的表述已用上游代码行核实 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **发现**：F7（README:33 拒绝清单比 `text.go` 窄，且写成完整列举）。本期没有前端 |
| 契约一致性 | 状态码口径不一 | 不适用。差量没有触及状态码 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。README:32–36 没有改动空文本与缺失内容的表述，空正文送检的说明仍在 `:23` |

**开放式复读：** 已放下起手表，以半年后接手的维护者的身份把四个范围内文件重读一遍，逐个问「第一眼会让人皱眉的是什么」：
- `README.md`：
  - 第 33 行冒号后的清单像是完整列举。对照 `text.go` 后发现漏项，并与下一句「控制参数不送检」形成歧义，这就是 F7，属于契约维度的表内发现。
  - 第 34 行把「不支持」和原因拆成两句，读得顺。
  - 第 32、35、36 行各一条规则，没有皱眉点。
- `engine.go`：第 68 行与第 78 行返回同一错误，第 77 行的注释已交代原因，没有新的皱眉点。
- `text.go`：第 121 行注释到位；第 21 行注释的上游词汇见 R16。没有新的皱眉点。
- `schema_test.go`：新增行的位置与内容清楚，见 R17。没有新的皱眉点。

上面的发现都已在起手表对应行记录；**已做开放式复读，无表外发现。**

**实际静态阅读范围与未执行的检查：**
- 未运行 `go vet`、`go test`、`go build`：它们会写构建缓存，本评审只读。`schema_test.go:18` 的清单与上游一致，已由人工逐字比对 `chatcompletions.go:14-21`；测试是否通过由预飞证明。
- 未读 `ee/internal/pricing/README.md`：本轮差量没有改变 README 结构，结构对照沿用上一轮结论。
- 未读 `plugin/plugin.go`、`model.go`、`plugin_test.go`、`integration_test.go`、`engine_test.go` 全文：不在本轮差量内。`engine_test.go` 只经 grep 读了第 242–247 行。
- 未读 `ee/scripts/branding-smoke.py` 和 workbench 生成视图：范围外，由 FIND-026/027 覆盖。
- 未读根目录未跟踪的 `workbench/evidence/内容安全框架-期1-交付摘要.md`：它是过程文档，不是被评对象。
- 按要求未读旧代码评审、方案v1 与方案评审；只读了上一轮收敛评审记录 `收敛评审2.md`，用于逐条处置，没有读 `收敛评审1.md`。
