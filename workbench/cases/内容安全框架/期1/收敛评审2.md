---
title: 内容安全框架 期1 收敛评审 #2
type: 收敛评审
case: 内容安全框架
phase: 1
round: 2
verdict: 有Blocking
created: 2026-09-16
---

> 评审者与实施上下文隔离（不能是写代码的会话自评）。基线 `7c81459aa0e62750f22f57b57c3c4a4750a98cfc`；本轮为差量复核，范围是上一轮候选 `cf2d94083c98601149273d217712fb4e879484cb` 到本轮候选 `c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe`。`git status --porcelain --untracked-files=all` 输出为空：工作树干净，没有未跟踪文件。
> L2：四维收束为段落与表格。

## 结论

**总体判断：** 上一轮的 F1–F4 四条修复项都已关闭，证据见「本轮修复项」开头的处置表。两条取舍项也都被采纳：
- S1：`rules.go` 改名为 `model.go`，配置测试并入 `engine_test.go`。
- S2：新增 `plugin/schema_test.go`，固定上游字段清单。

架构与依赖关系没有变化，仍符合 EE 后端架构 §4–§7：
- 根包只 import 标准库。
- `plugin` 删掉 `reflect` 后只依赖根包和 `core/schemas`。
- `app` 没有改动。

差量本身紧凑，错误码转换收成了一个函数，README 读起来也清楚多了。但本轮新增或调整的内容里有两处小问题需要修：
- **F5**：README 新写的「不支持的载体」一句用了上游名词，没有解释，而且把五六条独立规则压在一段里。
- **F6**：`Check` 和 `appendText` 把同一类输入校验拆成前后两处，是有意为之，但原因只写在测试里，代码里没有注释。

另有一条取舍建议（S3）：字段清单测试漏掉了提取器直接读取的请求根结构 `BifrostChatRequest`。

F5、F6 都是局部改动，不需要用户裁决。关闭之前 verdict 维持「有Blocking」。

## 事实来源

**执行的命令**（均为只读）：
- `git status --porcelain --untracked-files=all`：空。
- `git diff --stat cf2d94083..c503a4bb8`：其中 11 个被评文件与提示词清单一致。`rules.go → model.go` 是 100% 相似度的纯改名，`rules_test.go` 已删除。其余 7 个是 workbench 过程文档与生成物，不在评审范围内。
- `git diff cf2d94083..c503a4bb8 -- ee product workbench/cases/内容安全框架/期1/方案v2.md`
- `git grep -n "ChatRequest\.\|req\.(Input|Params|RawRequestBody|Fallbacks)" -- ee/internal/guardrails/plugin/text.go ee/internal/guardrails/plugin/plugin.go`

**完整通读的被评文件（现状），及各自职责：**
- `guardrails/README.md`（36 行）：包的问题陈述、文件调用链、检测契约、接入前提。
- `engine.go`（152 行）：配置校验与快照，按阶段顺序执行规则，处理超时与失败。
- `model.go`（125 行）：类型、枚举、检测器接口、事件、错误值与错误码。
- `engine_test.go`（271 行）：规则语义、配置校验、零值与 nil 守卫、错误优先级与事件保留、并发。
- `plugin/plugin.go`（157 行）：钩子、统一的错误码转换、短路与日志。
- `plugin/text.go`（126 行）：有界纯文本提取，拒绝不支持的载体。
- `plugin/plugin_test.go`（214 行）：钩子行为与构造校验。
- `plugin/text_test.go`（176 行）：提取与拒绝，以及「先判超限、后校验 UTF-8」的顺序。
- `plugin/schema_test.go`（40 行）：固定已审过的上游字段清单。
- `阿里云与Bifrost功能对照.md` 30–59 行：本轮只改了第 46 行。
- 另读了范围外的 `plugin/integration_test.go`（92 行），用于核对 README:34、:36 的表述。

**规则与裁决：**
- 已读：根 AGENTS.md、workbench/AGENTS.md、收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md、收敛评审模板、EE后端架构.md §4–§7、findings.md。
- 方案v2 全文已读，包括「Gate 决策结果」「实施偏差记录」和新增的「第一轮评审修复记录」。
- 已裁决、不重提的内容：输出流检查不支持、只有输入规则时放行流式请求、不装配到 app、生产错误格式后续再定。
- 上一轮记录 `收敛评审1.md` 已读，用于逐条处置修复项，并沿用其保留项 R1–R9。

**同类实现对照：**
- `ee/internal/pricing/README.md:14-31`：先讲解决什么问题，再画文件级调用链，`model.go` 在调用链外单独一行说明，测试用一句话带过。本案 README:7-18、:36 的写法与之一致。
- `ee/internal/pricing/model.go`、`ee/internal/identity/model.go`：类型放 `model.go`，测试与被测文件同名。本案改名后一致。
- `ee/internal/pricing/sync.go`：logger 只做 `== nil` 判定。本案 `plugin.go:37` 现已一致。
- 在 `ee/internal` 里 grep `reflect\.|IsNil\(\)`：只剩 `engine.go:146-149` 一处 typed-nil 判定，其余都是测试或同步代码里的 `DeepEqual`。typed-nil 判定已不存在第二份实现。
- 在 `ee` 里 grep `NumField\(\)`：只有 `schema_test.go:31`。EE 内没有「固定上游字段清单」的先例，也就没有可对照的同类实现。

**上游假设核实：**
- `core/schemas/chatcompletions.go:14-21`：`BifrostChatRequest` 有 6 个字段：`Provider Model Input Params Fallbacks RawRequestBody`。`text.go:25,28,42` 直接读其中 3 个，但它不在 `schema_test.go` 的清单里（见 S3）。
- `core/schemas/chatcompletions.go:1068-1077`（`ChatMessage`）、`:1740-1743`（`ChatNonStreamResponseChoice`）、`:329-334`（`ChatReasoning`）、`:1395-1399`（`CacheControl`）：字段名和顺序与 `schema_test.go:19,25,26,27` 逐一一致。其他结构体的字段清单未逐字段核对，见文末「未执行」。
- `core/schemas/bifrost.go:400`：`BifrostContextKeyLargeResponseThreshold` 是 int64，由企业版设置。`core/providers/utils/largeresponse.go:98,109,154` 和 `gemini/gemini.go:689` 按 `> 0` 判定是否启用。这与 `text.go:21-24` 的注释和判定一致。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无新增。F2、F3 的重复已消除 | `plugin.go:141-149` 是唯一的错误码转换，三处调用 `:76,:92,:111`；typed-nil 判定只剩 `engine.go:141-152` |
| 职责与实现质量 | F6（拆开的输入校验没有原因注释） | `engine.go:68-79`；`text.go:117-126` |
| 架构与项目一致性 | S3（字段清单漏了请求根结构）。架构符合性、依赖关系均符合 | 见下 |
| 契约一致性 | F5（README:32 未解释上游名词，多条规则压在一段） | `guardrails/README.md:32` |

**架构符合性**（依据：EE 后端架构 §4 模块内部的职责、§6 插件与普通业务、§7 与 Bifrost 的协作边界）：
- 本轮没有改变模块划分。根包 `guardrails` 仍承担规则与处置（§4「模块根包：业务模型、服务、规则、错误及所需接口」）。
- `plugin/` 仍只做两件事：把 Chat 结构转成文本，把结果转成短路（§4「接入 Bifrost 钩子，转换输入与处理结果」；§6「插件负责把业务结果转换为放行、修改或短路」）。
- 新增的 `schema_test.go` 放在 `plugin/`，因为它固定的正是 `plugin` 负责适配的上游类型。Bifrost 类型没有因此进入根包。
- `rules.go` 改名为 `model.go` 只动了文件名，没有改职责，与 §4「同一包内优先将需要一起理解的……放在一起」不冲突。
- 差量没有触及 `ee/internal/app` 或 `ee/internal/host`（diff --stat 里没有），仍符合 Gate 1「不装配到 app」和方案 §4「后续由 app 装配」。

**依赖关系**（依据：§5 依赖与协作、§7「宿主接入只声明窄接口，由 app 显式注入」）：
- 根包 import：`model.go:4-8` 为 `context errors time`，`engine.go:4-11` 为 `context errors fmt reflect strings unicode/utf8`，全是标准库。
- `plugin.go:4-12` 删掉了 `reflect`，现在只依赖标准库、根包 `guardrails` 和 `core/schemas`。方向仍是 plugin → 根包，没有反向或双向依赖。
- `schema_test.go:4-11` 是外部测试包，只 import `core/schemas`，不影响生产依赖。
- logger 窄接口不变（`plugin.go:15`），判定口径与 pricing 一致。检测器由 `New` 注入并复制注册表（`engine.go:21-47`）。
- 没有全局变量或容器查找。资源归属不变：`Cleanup` 不关闭检测器（`plugin.go:49-50`，README:26）。

## 出口清单

### 本轮修复项

**上一轮本轮修复项的处置：**

| 编号 | 处置 | 证据 |
|---|---|---|
| F1 README 三问与外部名词 | **已修复** | 问题陈述：`README.md:3`「自有网关推理链此前没有可注入的本地内容检查。本包让……成立」。调用链：文件表换成 `:9-18` 的文件级调用链，测试改为 `:36` 一行带过。外部名词：「HG」「BF」`schemas.LLMPlugin`、`AllowFallbacks=false` 在全文已不出现，Bifrost 在 `:5` 就地解释，回退改写为「禁止回退到其他模型重试」（`:16`）。原第 21 行改为 `:25`「检测在调用方同步执行，框架不另起 goroutine；不响应取消的检测器会在超时后继续阻塞调用方」。原第 30 行改为 `:34`「生产装配必须满足：……」。本轮新写的 `:32` 另有问题，见 F5，不算 F1 复发 |
| F2 typed-nil 反射判定两份 | **已修复** | `plugin.go:37` 只剩 `logger == nil`，`reflect` import 已删（`plugin.go:4-12`）；`plugin_test.go:207-209` 只保留 nil logger 用例，typed-nil 用例已删；`engine.go:141-152` 是唯一一份 |
| F3 错误码转换两份 | **已修复** | 合并为 `plugin.go:141-149` 的 `errorCode(err, fallback)`，处理取消/超时、超限和兜底三种情况；`check` 传 `CheckFailed`（`:111`），两处提取路径传 `UnsupportedContent`（`:76`、`:92`）；`extractionCode` 已删除 |
| F4 `Check` 特殊返回值未注释 | **已修复** | `engine.go:64`「返回 error 时 Action 恒为 Block，Events 保留出错前已完成规则的事件，调用方仍可记录」；`engine_test.go:254-270` 断言出错时 `Action == Block` 且保留了先前的 Observe 事件 |

上一轮取舍项 S1、S2 已被采纳：
- S1 按选项 (b) 落地：`model.go` 与 `rules.go` 内容 100% 相同，`rules_test.go` 的两个测试原样并入 `engine_test.go:184-240`。合并后 271 行，没有超过 300 行的评估线。
- S2 落地为 `plugin/schema_test.go`，覆盖面的缺口见 S3。

**本轮新增：**

**F5. README 第 32 行用了上游名词没有解释，还把多条独立规则压在一段里**

- **位置：** `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:32`
- **代码事实：**
  - 这一段在一句话里列出了原始正文、大载荷/大响应透传（包括已设置正数阈值）、未知参数、工具、多模态、推理正文、消息名称、停止文本、词元概率等 12 类拒绝载体。同一段还写了「控制参数不送检」、流式准入和上游错误保留三条独立规则。
  - 「大载荷/大响应透传」和「已设置正数阈值」指的是宿主的上游模式：`core/schemas/bifrost.go:393,400` 的上下文标志和 `BifrostContextKeyLargeResponseThreshold`。README 没有交代是谁的阈值，也没说为什么设了阈值就要拒绝。实际原因写在 `text.go:21` 的注释里：阈值可能在调用模型之后才切换成透传。
- **维护影响：**
  - 编码规范第 55 行要求：「确实绕不开的，第一次出现就地用一句话说明它是什么，不能假设读者读过上游代码」。按规范，读者是没读过上游代码的人，他看不懂「已设置正数阈值」在说什么。企业版部署时正是这条会让所有 Chat 检查被拒绝，这是改之前最需要知道的约束之一，现在却埋在长句中间。
  - 输入范围、输出范围、拒绝清单、流式准入、上游错误是五条互不相关的规则，挤在一段里，想确认「流式请求怎么处理」得从句子中间找。
  - FIND-022 记录过同类问题：README 明文违规被评审放过。
- **最小修正：** 把第 32 行拆成几条列表：输入范围与输出范围一条；拒绝的载体一条；流式准入一条；上游错误一条。把透传那一项改写为「宿主为超大请求或响应开启的原样透传模式（包括只配置了大响应阈值的情况：它可能在调用模型之后才切换为透传，检查来不及介入）」，不必出现上游符号。

**F6. `Check` 和 `appendText` 有意把输入校验拆成前后两处，代码里没有交代原因**

- **位置：**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine.go:68-79`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:117-126`
- **代码事实：**
  - `engine.go:68` 与 `:77` 分两处返回同一个 `ErrInvalidInput`，中间隔着取消检查和字节上限检查。
  - `text.go:118` 先判剩余预算，`:121` 再校验 UTF-8。
  - 两处的顺序都是本轮刻意调整的（代码评审 R2：先做廉价检查，再做线性扫描），但原因只写在测试注释 `engine_test.go:242` 和测试名 `text_test.go:162` 里，函数本身没有注释说明。
- **维护影响：**
  - 下一个维护者第一眼看到 `engine.go:68` 和 `:77` 返回同一个错误，很自然会把 `!utf8.ValidString(text)` 合并回第 68 行的守卫，当作简化。
  - 编码规范「注释写清用途、约束和原因」一节要求：复杂逻辑或特殊处理必须解释原因。现在测试能挡住回退，但维护者得先改坏、看到测试失败，才会知道这个约束。
- **最小修正：** 在 `engine.go:77` 前和 `text.go:121` 前各加一行注释，例如「先判取消和长度，再做线性的 UTF-8 扫描，避免超大非法输入耗费扫描成本」。

### 建议取舍项（实施者决定采纳或撤回）

**S3. 字段清单测试没有固定提取器直接读取的请求根结构 `BifrostChatRequest`**

- **位置：** `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/schema_test.go:17-28`；读取点在 `text.go:25`、`:28`、`:42`
- **代码事实：**
  - 清单固定了 `ChatParameters`、`ChatMessage`、`ChatContentBlock` 等 10 个结构体。
  - 提取器的输入入口是 `*schemas.BifrostChatRequest`，直接读取它的 `RawRequestBody`、`Params`、`Input`（上游 `chatcompletions.go:14-21` 共 6 个字段），但这个结构不在清单里。
  - 文件头注释（`schema_test.go:1`）的承诺是「固定已审阅的上游字段集合，让上游升级触发文本提取边界复审」。
- **维护影响：** 如果上游在请求根上新增一个会发给模型的文本字段（与 `Params` 并列，而不是放进 `Params`），字段清单测试照样通过，提取器会悄悄漏检。这正是 S2 想防的情况，只是出现在清单漏掉的那一层。补一行成本很低。
- **最小修正：** 在清单里加一行：`{schemas.BifrostChatRequest{}, "Provider Model Input Params Fallbacks RawRequestBody"}`。
- **反证条件：** 如果实施者确认上游约定请求级新字段一律进 `ChatParameters`，而不会加在 `BifrostChatRequest` 根上（例如上游有代码注释或贡献约定写明），并在 `schema_test.go` 注释里注明这一点，就可以撤回。

### 需用户裁决的 Blocking

- 无

### 立债（REF：立 `triggered_by: [<本case>]` 的需求进 reports/）

- 无

### 挂账（Finding：登记 findings.md，带四要件）

- 无（S3 如果未采纳，且反证条件也不成立，建议登记为 Finding，触发条件是第一次合并改动 `BifrostChatRequest` 的上游提交）

### 转代码评审（范围外发现，一行一条，不出结论）

- `text.go:71`：只要 `choice.LogProbs != nil` 就拒绝整个输出。如果有 Provider 在客户端没请求 logprobs 时也返回非 nil 的空结构，所有输出都会被拒，待核实。
- `text.go:29`：输入侧不拒绝 `ChatParameters.LogProbs/TopLogProbs`。请求了 logprobs 的调用会先调用模型，再在输出侧被必然拒绝，是否应在 PreLLM 就提前拒绝，待核实。
- `text.go:85`：输入消息带 `Name` 一律拒绝。常见 OpenAI 客户端会在消息上携带 `name`，对可用性的影响待核实。
- 方案v2 第 115 行：第一轮额外运行的 branding-smoke 在 SPA 断言失败后，临时证据清理的问题交由复核判定。该脚本不在本轮范围内，本评审未读，由代码评审判定是否挂账。

## 台账对账

**涉及的已有 Finding：**
- **FIND-022**（README 规范明文违规被评审放过）：本轮再次逐条对照编码规范「模块根包要有 README」一节。F1 已关闭，本轮新写的 `README.md:32` 仍有外部名词未解释，列为 F5，没有放过。
- **FIND-024**（`internal/host` 目录归位）：本轮没有触及，插件仍在 `guardrails/plugin/`，未恶化。
- **FIND-012、FIND-025**：本轮没有改 `app` 装配或认证链，未涉及。
- 其余条目与本案无关。

**看似可改但有理由保留（下一轮不再提）：**
- 上一轮 R1–R9 保留理由不变，差量没有使其恶化。R4 的 ctx 守卫（`plugin.go:103`、`:152`，`engine.go:68`）、R7 两份 `detectorFunc`、R8 零值 Engine 守卫的现状都与上一轮一致。
- **R10** `errorCode` 在提取路径上也会把取消映射为 `CheckCanceled`：提取函数实际不会返回 ctx 错误，这个分支在提取路径上用不到。但把三种情况合在一个函数里，正是 F3 要的「只有一份转换」，不为此再拆出一个变体。
- **R11** `schema_test.go:35` 用 `slices.Equal` 做有序比较，上游只调整字段顺序也会触发失败：误报的代价是复审一次后更新清单；改成无序比较则要多写集合转换。这个测试本来就是复审提醒，不是正确性断言，保持现状即可。
- **R12** `schema_test.go` 固定了 `ChatReasoning`、`CacheControl`，但 `text.go` 并不读这两个结构：README:32 声明「推理强度、缓存时效等控制参数不作为正文送检」，固定它们正是为了在这些控制结构新增文本字段时触发复审，与声明相符。
- **R13** `engine_test.go:1` 的文件头注释没有提到并入的配置校验测试：原文「验证规则顺序、风险阈值及失败和取消语义」里的「规则语义」可以涵盖规则配置校验，注释与内容不矛盾，不要求改。
- **R14** `schema_test.go` 与源文件不同名，不符合「测试与被测文件同名」：它的被测对象是上游结构，没有对应的本包源文件；README:36 已单独点名说明用途。

---

**起手表覆盖：**

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。400/599 只在 `plugin.go:33` 定义；字节上限只从 `Engine.maxTextBytes` 读取；512/1024 只在测试里出现 |
| 重复 | 同一规则多份实现且互不注明 | 无。F2、F3 已消除；`engine.go` 与 `text.go` 各有一次 UTF-8 校验，但它们防的对象不同：前者防根包的直接调用方，后者防拼接前的单段文本，而且错误码不同（`ErrInvalidInput` 与 `errUnsupported`） |
| 重复 | 重造已有 helper | 无。EE 里没有字段清单 helper；`schemas.Ptr` 复用了 |
| 重复 | 派生常量无推导 | 无 |
| 职责与实现质量 | 魔数 | 无。`schema_test.go` 的字段名字符串是被固定的清单本身，不是魔数 |
| 职责与实现质量 | 状态码、错误码裸数字 | 无。错误码是 `model.go:118-125` 的类型化常量；测试按契约写字面量 |
| 职责与实现质量 | 无名的错误形状 | 无。`errorCode` 返回类型化的 `ErrorCode`，兜底值通过参数显式传入 |
| 职责与实现质量 | 紧凑但绕的控制流 | **发现**：F6（同一类输入校验拆成两处，没有原因注释）。`reject` 闭包见 R1 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无。S1 采纳后 `model.go`、`engine.go` 名实相符，`engine_test.go` 271 行；`schema_test.go` 单独成文件的理由见 R14 |
| 职责与实现质量 | 多余状态或守卫 | 无。删掉 logger 反射守卫后，剩下的守卫都各自防一个入口（R4、R8） |
| 职责与实现质量 | 同一效果做两次 | 无 |
| 职责与实现质量 | 注释与现状矛盾 | 无矛盾。`plugin.go:35` 已同步为「调用方须提供可用的 logger 实例」；`engine.go:63-65` 与实现一致；README:25 的两种超时分类与 `engine.go:108-113`、`plugin.go:142` 一致。注释不完整的地方见 F6 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无，逐项一致：logger 判定口径同 pricing；错误码常量放根包，同 identity；类型文件名同 identity、pricing；README 结构同 pricing |
| 架构与项目一致性 | **架构符合性** | 符合，见「四维核查」 |
| 架构与项目一致性 | **依赖关系** | 符合，见「四维核查」 |
| 架构与项目一致性 | 绕过既有出口 | 无 |
| 架构与项目一致性 | 把上游现状当契约 | **发现**：S3。S2 已用字段清单测试把「上游现状」变成了会报警的检查，但漏掉了请求根结构 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **发现**：F5（README 这份手写开发指南有外部名词未解释）。语义上 README:32 的拒绝清单与 `text.go:15-39,65,71,85,89,107` 逐项一致；`阿里云与Bifrost功能对照.md:46` 与 Gate 1 裁决一致。本期没有前端 |
| 契约一致性 | 状态码口径不一 | 无。所有拒绝共用 `Options.StatusCode`（R5） |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。空正文照常送检（`engine_test.go:155`、`text_test.go:142`），`Content == nil` 拒绝（`text.go:94-96`），README:23 一致 |

**开放式复读：** 已放下起手表，以半年后接手的维护者的身份把范围内文件重读一遍，逐个问「第一眼会让人皱眉的是什么」：
- `engine.go`：第 68 行和第 77 行返回同一个错误，看上去像冗余。这就是 F6，属于表内发现。
- `text.go`：`appendText` 的判断顺序有同样的问题，并入 F6。
- `README.md`：第 32 行太长、太密。这就是 F5。
- `schema_test.go`：发现清单缺了请求根结构。这就是 S3。
- `model.go`、`plugin.go`、`plugin_test.go`、`text_test.go`、`engine_test.go`、对照文档第 46 行：没有新的皱眉点。`engine_test.go` 头注释的情况记为保留项 R13。

上面列出的发现都已在起手表对应行记录；复读没有产生表外发现。

**实际静态阅读范围之外、未执行的检查：**
- 未运行 `go vet`、`go test`、`go build`。它们会写构建缓存，本评审只读。
- `schema_test.go` 里 `ChatParameters`、`ChatContentBlock`、`ChatAssistantMessage`、`BifrostChatResponse`、`BifrostResponseChoice`、`ChatMessageContent` 这几项的字段清单，没有与上游逐字段比对（grep 输出过长，已保存但没有通读），只核对了另外 4 项。清单与上游是否一致，由该测试运行结果证明，归代码评审或预飞。
- 未读 branding-smoke 脚本（范围外，已转代码评审）。
- 按要求未读旧代码评审、方案v1 与方案评审；只读了上一轮收敛评审记录，用于逐条处置。
- `阿里云与Bifrost功能对照.md` 只读了 30–59 行，本轮 diff 只改了第 46 行。
