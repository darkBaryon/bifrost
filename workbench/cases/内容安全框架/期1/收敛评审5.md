---
title: 内容安全框架 期1 收敛评审 #5
type: 收敛评审
case: 内容安全框架
phase: 1
round: 5
verdict: 有Blocking
created: 2026-09-17
---

> 评审者与实施上下文隔离（不是写代码的会话自评）。基线为 `7c81459aa0e62750f22f57b57c3c4a4750a98cfc`。本轮是差量复核：上一轮候选 `958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8`，本轮候选 `f6c9437e657af58e15204412fd2421ed98c5a81e`，`git rev-parse HEAD` 与本轮候选一致。
> 风险等级 L2，四维核查写成段落加表格。

## 结论

**总体判断：** 本轮新增的开发护栏装配方式与现有结构一致，框架整理也符合要求，但还有 4 个小修复项未关闭，因此结论为有Blocking。修完后做定向复核即可，不需要用户另行裁决。

**做对了的部分：**
- **按职责归位：** 根包由 `engine.go`/`model.go` 拆成 `checker.go`、`evaluation.go`、`rule.go`、`detector.go`；面向客户端的错误码移到 `plugin/deny.go`。
- **装配方式对：** `app/guardrails.go` 通过宿主既有的 `SyncLoadedPlugin` 在内存中注册插件，排在内置插件之前；默认关闭，非法值拒绝启动。
- **依赖方向对：** 依赖只有 app → plugin/fake → 根包这一个方向，根包只用标准库。
- **没有另起一套：** 冒烟脚本复用 `identity-smoke.py` 的进程夹具，没有新建基础设施。

**待修的问题：**
- 改名没有收尾：测试名、注释里还留着 Engine/Event。
- 开发装配里有一个重复三次的检测器 ID，以及裸状态码 `400`。
- 假检测器子包的 README 全文都在复述父包内容。
- 父 README 的文件表把 `deny.go` 放在检测器之后，与它自己说的阅读顺序不符。

**上一轮修复项：** F7 仍然关闭。

**合并带入的文件：** 部署相关文件经 develop 合并进来，不是本 Change 写的。其中有过程叙述过时、个人路径入库的问题，列为挂账建议，不计入本 Change 的修复项。

## 事实来源

**执行的命令**（均为只读）：
- `git status --porcelain`：开始时无输出（工作树干净）。
- `git status --porcelain=v1 --untracked-files=all`：结束时多了一个已修改文件 `workbench/evidence/内容安全框架-期1-开发联调.md`。
  - 这不是本评审改的，是评审期间外部改动。
  - 它是 workbench 过程文档，不在被评范围内，本轮没有读。
- `git rev-parse HEAD origin/develop`：HEAD 为 `f6c9437e6…`；origin/develop 为 `53d83a9ee…`。
- `git log --oneline 958fc9830..f6c9437e6` 共 8 个提交：
  - 本 Change 的是 `9c8bc7fc5`、`3df5d9930` 和合并提交 `f6c9437e6`。
  - `aaf2bf6c3`、`b8b79f07f`、`95c9565c7`、`53d83a9ee` 来自 develop（Dockerfile/deploy）。
- `git diff --stat 958fc9830 f6c9437e6`：41 个文件，其中范围清单里的 30 个均在内，其余是 workbench 文件。
- `git diff --stat 958fc9830 3df5d9930 -- . ':!workbench'`：确认本 Change 自己改的是 22 个文件（AGENTS.md 与 ee/ 下文件）；`.dockerignore`、`Dockerfile`、`deploy/*` 来自合并。
- 另外执行了：
  - `git diff 958fc9830 f6c9437e6 -- ee/internal/app ee/README.md ee/scripts/identity-smoke.py ee/internal/guardrails/plugin`
  - `git diff … -- AGENTS.md .dockerignore Dockerfile deploy/Dockerfile.buildbase`
  - `git show 958fc9830:ee/internal/guardrails/engine.go`、`git show 958fc9830:ee/internal/guardrails/model.go`（与新文件逐段对照）
  - `git grep` 查了以下内容：
    - 被改名的旧符号、`guardrails.New(`/`mapCheckError`/`requestID(` 的调用方；
    - `app` 下的环境变量常量惯例；
    - 冒烟脚本对 `identity-smoke` 的复用；
    - ee 内 `fasthttp.StatusXxx` 的用法；
    - `deploy` 下的文件清单；
    - Dockerfile 的分布。

**完整通读的文件（现状），及各自职责：**
- `ee/internal/guardrails/README.md`：模块用途、整体流程、文件表、开发联调说明、接入约束。
- `checker.go`：Checker 快照、`New`、`HasRules`、`Check`。
- `evaluation.go`：单条规则执行，以及 Outcome/Failure/RuleEvaluation 的定义。
- `rule.go`：Stage/Category/Action/Rule 的定义与规则校验。
- `detector.go`：Level/Finding/Detector 契约与 typed-nil 判定。
- `checker_test.go`：规则顺序、阈值、失败与取消的语义测试。
- `detectors/fake/detector.go`、`detectors/fake/README.md`：假检测器及其说明。
- `plugin/plugin.go`：Hook 入口、请求准入、调用检查器并记日志。
- `plugin/deny.go`：错误码、拒绝响应、错误映射、requestID。
- `plugin/text.go`：支持范围判断与文本提取。
- `plugin/plugin_test.go`：Hook 行为测试。
- `ee/internal/app/bootstrap.go`、`guardrails.go`、`guardrails_test.go`、`README.md`：应用装配与开发护栏注册。
- `ee/README.md`：EE 模块总表。
- `ee/scripts/guardrails-smoke.py`：本地模型替身加真实 EE 进程的联调脚本。
- `ee/scripts/identity-smoke.py`（第 30–119 行）：`Node` 夹具；本轮给 `start` 增加了 `extra_env`。
- `AGENTS.md` 的差量段落：分支约定。
- `.dockerignore`（差量）、`Dockerfile`、`deploy/Dockerfile.buildbase`：EE 镜像构建。
- `deploy/ai-gateway-k8s.yaml`、`ai-gateway-storage.yaml`、`ai-gateway-ingress.yaml`：单副本 SQLite 的 K8s 清单。
- `deploy/K8s部署步骤.md`、`deploy/构建基础镜像.md`：部署与基础镜像说明。

**规则与裁决：**
- 已读：根 AGENTS.md、workbench/AGENTS.md、收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md、收敛评审模板、EE后端架构.md 全文、findings.md 全文（到 FIND-027）、方案v2 全文（含「Gate 决策结果」「实施偏差记录」「用户阅读与开发联调后的补充记录」）。
- 已经裁决、本轮不重提的内容：
  - 2026-09-17 的用户裁决：
    - 显式开启的开发假检测器、app 注册与 HTTP/Playground 联调在本期范围内；
    - 命名定为 Checker/CheckResult/RuleEvaluation，类型随职责归位；
    - README 保留宏观流程与使用说明，长度沿用用户裁决。
  - 输出规则开启时不支持流式请求。
  - 按纯文本契约拒绝整个未覆盖载体。
  - FIND-026、FIND-027。
- 上一轮记录 `收敛评审4.md` 已全文读过，用于处置其修复项。

**同类实现对照：**
- **插件注册：**
  - 上游 `transports/bifrost-http/server/server.go:2033-2063` 的 `SyncLoadedPlugin` 只在内存中注册，并完成排序和同步 core。
  - `product/docs-zh/04-开发指南/07-ee包壳.md:51` 把它列为 EE 进程内注册的既定接缝，并说明「纯内存，不落库」。
  - 本案用法与此一致。
- **插件顺序：**
  - `lib/config.go:6148-6170` 规定分组顺序为 pre_builtin(0) → builtin(1) → post_builtin(2)，组内按 order 升序。
  - `server/plugins.go:196` 内置插件使用 builtin 分组。
  - 所以 `PluginPlacementPreBuiltin` 加 `math.MinInt` 确实让本插件排在所有内置插件之前，README:86 与 `guardrails.go:27` 的说法成立。
- **环境变量装配：** `app/identity.go:24-28` 和 `app/pricing.go:18-25` 都把环境变量名放在 const 块里并加注释，`guardrails.go:18-28` 与之一致。
- **状态码写法：** ee 内状态码统一用 `fasthttp.StatusXxx`（`branding/http/handler.go:108`、`identity/http/protocol.go:112-122`）；`guardrails.go:57` 写的是裸 `400`。
- **错误码归属：** `identity` 在根包定义 `Error` 取值（`identity/model.go:21-24`），由 `http/` 映射状态码。本案错误码放在 plugin，理由见保留项 K2。
- **冒烟夹具复用：** `branding-smoke.py:38` 与 `pricing-smoke.py:24` 都继承 `identity_smoke.Node`，guardrails-smoke 沿用这一做法。
  - `pricing-smoke.py:50-66` 抄了一份 `Node.start`，注释写的理由是「方案 §8 禁止修改父脚本」；本轮父类已增加 `extra_env`，见挂账 S1。
- **EE 镜像构建：** 根 `Dockerfile` 通过 `make -C ee build`（`ee/Makefile:31-34`）复用唯一的 EE 构建入口，运行阶段与上游 `transports/Dockerfile:54-121` 同形（相同的运行库、`USER 1000:0`、entrypoint、HEALTHCHECK）。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | F9（检测器 ID 字面量重复三次）；S1（pricing 冒烟里抄的 start 失去了原有理由） | `app/guardrails.go:38,50`；`pricing-smoke.py:51` 与 `identity-smoke.py:58-62` |
| 职责与实现质量 | F8（改名残留）、F9（裸状态码）、F10（子包 README 复述父包）、F11（文件表顺序） | 见本轮修复项 |
| 架构与项目一致性 | 无新问题。架构符合性、依赖关系均符合 | 见下 |
| 契约一致性 | 无 | Category 取值从 `D1…` 改为 `harmful_content…`：`rule.go:24-27`、README:74、`plugin_test.go:93` 三处一致；错误码字符串 `deny.go:17-22` 与 README:84、`guardrails-smoke.py:123,127` 一致；本期无前端与 HTTP 管理契约 |

**架构符合性**（依据：EE 后端架构 §1、§4、§6、§7）：
- **根包：**
  - `checker.go`、`evaluation.go`、`rule.go`、`detector.go` 承载规则、执行与检测器契约，对应 §4「模块根包：业务模型、服务、规则、错误及所需接口」。
  - 原来放在根包的面向客户端错误码 `ErrorCode`，已随职责移到 `plugin/deny.go:13-23`，对应 §4「`plugin/` 转换输入与处理结果」和 §6「插件负责把业务结果转换为放行、修改或短路行为」。
- **plugin：** `plugin.go:51-125` 只做准入、提取、调用 `Checker.Check` 和结果映射，没有自己的规则判断。
- **检测器实现：** `detectors/fake` 实现根包的 `Detector` 接口，与 §7「`<模块>/<子包>` 是把根包定义的接口适配到某个库」的先例 `identity/hasher` 同形。§4 的子包表写明「不是必须照建的文件清单」，所以多一层 `detectors/` 不算偏离。
- **app：**
  - `app/guardrails.go:31-65` 只读环境变量、构造规则/检查器/插件，再调用宿主注册，没有运行期业务编排，符合 §5「app 只做装配」。
  - 它在 `attach` 里调用（`bootstrap.go:50-52`），位于上游 Bootstrap 之后、Start 之前，与品牌、定价的接入时机一致（`app/README.md:29`）。
  - 它触碰 `*BifrostHTTPServer` 仅为注册，与 `attach` 注册品牌路由（`bootstrap.go:58`）是同一种装配动作，不属于 §7 应进 `internal/host/` 的宿主适配（那里放的是实现宿主接口的代码，另见 FIND-024）。
- **内嵌 Bifrost：** 复用宿主插件链与排序，没有另建管线，也没有修改上游文件（`SyncLoadedPlugin` 是上游已导出的方法）。

**依赖关系**（依据：§5 依赖与协作、§7「模块根包只依赖标准库」「由 app 显式注入」）：
- **根包：** import 只有 `context errors fmt unicode/utf8`（`checker.go:5-10`）、`context errors`（`evaluation.go:4-7`）、`fmt strings time`（`rule.go:4-8`）、`context reflect`（`detector.go:4-7`），全部是标准库，没有反向导入 plugin 或 fake。
- **plugin：** import 根包与 `core/schemas`（`plugin.go:4-10`、`deny.go:4-10`、`text.go:4-11`），Bifrost 类型留在适配层。
- **fake：** 只 import 根包与标准库（`fake/detector.go:4-9`）。
- **app：** import 根包、fake、plugin、`core/schemas`、`server`（`guardrails.go:4-16`），方向为 app → 实现 → 根包，没有双向依赖。
- **注入：**
  - `guardrails.New(rules, detectors, maxTextBytes)` 从参数接收检测器注册表（`checker.go:32`）。
  - `safetyplugin.New(checker, options, log)` 从参数接收检查器与日志（`plugin.go:31`）。
  - 日志使用 plugin 自己声明的窄接口 `Logger{Info}`（`plugin.go:13`），由 app 传入宿主 logger（`guardrails.go:57`），没有全局查找。
- **资源所有权：**
  - 假检测器无状态（`fake/detector.go:15`）。
  - `Plugin.Cleanup` 不关闭检测器（`plugin.go:44-45`），检测器生命周期归装配方。
  - 插件注册进宿主的内存插件链，由宿主管理（`server.go:2035-2046`）。
  - 不写数据库、不持久化，与 README:78 一致。

## 出口清单

### 本轮修复项

**上一轮修复项的处置**（`收敛评审4.md` 的「本轮修复项」只有 F7，「本轮新增：无」）：

| 编号 | 处置 | 证据 |
|---|---|---|
| F7 README 拒绝清单比代码窄，而且写得像完整列举 | **已修复（维持关闭）** | 见下方说明 |

F7 的核对说明：
- 原来的第 33 行已改写为 `ee/internal/guardrails/README.md:100`：「未知参数、工具、多模态、推理或拒答正文等未支持内容会被拒绝；完整判断见 plugin/text.go」。
  - 「等」加上「完整判断见」仍然表明清单不完整，并把判定交给代码。
  - 链接的文件存在，`text.go:15-127` 的拒绝逻辑本轮只改了函数名。
- 上一轮的保留项 R19（「停止文本」口径）和 R20（长句）随原句删除，已不适用；R18（例子与代码部分重合）在新句中仍然成立。

**本轮新增：**

**F8 改名没有收尾：测试名和注释仍使用 Engine/Event**
- **位置：**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker_test.go:219`（`TestUninitializedEngineAndContext`）
  - `:242`（注释「出错不能丢失此前已完成的观察事件」）
  - `:243`（`TestCheckErrorPrecedenceAndEvents`）
  - `:269`（`"completed event lost"`）
  - 附带：同文件 `:28,:53` 等处，以及 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin_test.go:46,196`，Checker 实例仍命名为 `e`。
- **代码事实：** 本 Change 按用户要求把 `Engine` 改为 `Checker`、`Event`/`Events` 改为 `RuleEvaluation`/`RuleEvaluations`（`checker.go:13,22`、`evaluation.go:30`）。`git grep` 显示生产代码里旧名已清零，只剩上面这几处。
- **维护影响：** 维护者按测试名找对应概念时，会去找一个已不存在的 Engine 类型或「事件」概念，用户刚统一过的术语在测试里又分成两套。
- **最小修正：**
  - 改名为 `TestUninitializedCheckerAndContext` 和 `TestCheckErrorPrecedenceAndRuleEvaluations`。
  - 注释改为「出错不能丢失此前已完成的规则执行结果」，失败信息改为 `"completed rule evaluation lost"`。
  - 局部变量 `e` 顺带改为 `checker`（可选）。

**F9 开发装配里检测器 ID 重复三次，状态码写成裸数字**
- **位置：**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails.go:38`（`ID: "dev-fake", DetectorID: "dev-fake"`）
  - `:50`（注册表键 `"dev-fake"`）
  - `:57`（`StatusCode: 400`）
- **代码事实：**
  - 同一个 const 块已经给超时、文本上限、插件顺序都起了名（`:23-27`），唯独检测器 ID 和状态码没有。
  - `DetectorID` 与注册表键必须相同，`New` 才能通过校验（`rule.go:68-71`），这个约束现在靠两个手写字面量保证。
  - ee 其他地方的状态码都用 `fasthttp.StatusXxx`（`branding/http/handler.go:108`、`identity/http/protocol.go:112-122`）；README:86 对外写明「命中返回 HTTP 400」。
- **维护影响：** 以后改检测器 ID 时只改一处就会启动失败；裸 `400` 违反编码规范「业务限制要命名」和起手表「状态码裸数字」。
- **最小修正：**
  - 在 const 块加 `fakeDetectorID = "dev-fake"`，同时用于 `DetectorID` 和注册表键（规则 ID 可以复用它）。
  - 加 `fakeDenyStatus = fasthttp.StatusBadRequest`（或 `net/http` 的同名常量），并加一句「开发夹具的拒绝状态」注释。

**F10 假检测器子包 README 全文复述父包，应删除**
- **位置：** `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/fake/README.md:1-5`
- **代码事实：**
  - 第 3 行「包含 `[guardrails-test]` 时返回高风险……只用于联调」，与父 README:67（文件表行）和 :86 重复，也与包注释 `fake/detector.go:1`、类型注释 `:14` 重复。
  - 第 5 行「由 app/guardrails.go 在显式设置 EE_GUARDRAILS_FAKE 时装配」，复述父 README:78，并链回父 README。
- **维护影响：** 编码规范「模块根包要有 README」写明子包按需，「子包的 README 如果通篇在复述父包的上下文……说明它不该独立成文」。这份 README 没有独立的约束或读者，却多了一处要同步的描述：标记或装配方式一变，要改三处。
- **最小修正：** 删除该文件；父 README 的文件表第 67 行已经一行带过。

**F11 父 README 文件表的顺序与它自己说的阅读顺序不符**
- **位置：** `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:68`
- **代码事实：**
  - 第 57 行说「先读插件入口……再读检查器和单条规则执行；开发具体算法时看检测器契约」。
  - 表中 `plugin/plugin.go`、`plugin/text.go` 在第 61–62 行，而同属插件入口的 `plugin/deny.go` 却排在第 68 行，位于 `detector.go` 和 `detectors/fake` 之后。
- **维护影响：** 表格是「代码在哪」的路标，插件的三个文件被检测器隔开，读者按表读时会漏掉拒绝映射，或者误以为它跟检测器有关。
- **最小修正：** 把第 68 行移到第 62 行之后。

### 需用户裁决的 Blocking

- 无（上面的 F8–F11 都在授权范围内，无需裁决）。

### 立债（REF：立 `triggered_by: [<本case>]` 的需求进 reports/）

- 无

### 挂账（Finding：登记 findings.md，带四要件）

以下为建议取舍项，由实施者决定是否登记。

**S1 pricing 冒烟里抄的 `Node.start` 失去了原有理由**
- **位置：** `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/pricing-smoke.py:50-66`
- **代码事实：** 注释写「抄自 identity_smoke.Node.start：父类剥掉 EE_* 变量，方案 §8 禁止修改父脚本」；本轮 `identity-smoke.py:58-62` 已给父类加了 `extra_env`。
- **维护影响：** 两份 start 的环境隔离规则和就绪检查要手工同步，注释里的理由也已过时。
- **建议：** 下次修改 pricing 冒烟时改为调用 `super().start(extra_env=…)`，或者至少更新注释。本轮不改（该文件不在差量内）。
- **反证条件：** 如果 pricing 冒烟需要的 `-log-style json` 和 `log_offset` 无法通过父类表达，抄一份仍然成立，届时只需更新注释里的理由。

**S2 经合并带入的部署文档里有过时的过程叙述和个人路径**
- **位置：**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/deploy/构建基础镜像.md:40`（「本次修改未提交 Git；原有 deploy 文件的暂存状态保留」，但文件现已提交）
  - `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/deploy/K8s部署步骤.md:35`（`/Users/admin/Desktop/kubectl/config` 个人路径）
  - `:90-92`（「本次验证边界」过程记录）
- **来源：** develop 提交 `53d83a9ee`，不是本 Change 写的。
- **维护影响：** 长期文档里混入与现状矛盾的过程叙述，也混入只在某台机器上成立的路径。
- **建议：** 由部署文档维护方在 develop 清理，不作为本 Change 的修复出口。
- **反证条件：** 如果用户把 `deploy/` 定位为运维操作记录而不是长期文档，保留即可。

### 转代码评审（范围外发现，一行一条，不出结论）

- `ee/scripts/guardrails-smoke.py` 没有加入 `ee/Makefile:40-52` 的 smoke 目标，请判断它是否属于本期必过验证。
- `ee/scripts/guardrails-smoke.py:89,111-113` 的临时目录不清理；`browser.json` 含密码和会话，先写入、后改 0600。请从凭据暴露和现场保留角度判断。
- `Dockerfile` 和 `deploy/*.yaml`（来自 develop）的镜像、运行用户、探针，以及 `K8s部署步骤.md:38` 自述的 K8s 1.14.2 不兼容，属于部署与运维范围。

## 台账对账

**涉及的已有 Finding：**
- **FIND-022**（README 违反规范明文却被评审放过）：本轮逐条对照了编码规范「模块根包要有 README」一节。
  - 子包按需：发现 F10。
  - 代码在哪：发现 F11。
  - 少提外部名词：README:11 首次出现 Bifrost 时已就地解释。
  - 长度：111 行，超过一屏，但按 2026-09-17 用户裁决不提。
- **FIND-024**（`internal/host` 目录归位）：本轮的插件注册放在 app，没有新增宿主适配代码，未恶化。
- **FIND-025**（认证扩展模型）、**FIND-026**、**FIND-027**：本轮没有涉及，未恶化。
- **FIND-012**（attach 惯性点）：`attach` 新增 3 行调用，护栏装配放在独立文件里，attach 仍是薄装配，未恶化。

**看似可改但有理由保留（下一轮不再提）：**
- **K1 `plugin.go:96` 的 `validateRequest` 重新计算了 `HasRules(Output)`，而 PreLLMHook 在 `:52` 已经算过：**
  - 这样 `validateRequest` 的准入判断能独立读懂；`output` 在 `:53` 只用于「两阶段都没有规则时直接透传」。
  - 性能影响归代码评审。
- **K2 错误码 `errorCode` 从根包移到 `plugin/deny.go:13-23`，并改为不导出：**
  - 编码规范要求「出现在接口响应里的取值在业务根包定义为类型化常量」，但这组值有一半是插件自己的概念（`unsupportedContent`、`unsupportedOutputStream` 与 Chat 结构、流式有关），根包从不产生它们；另一半由插件从 `CheckResult`/根包错误映射得到。
  - 它们仍是类型化命名常量，没有裸字符串同义词；这次移动也是用户「类型随职责归位」裁决的直接结果。
  - identity 的 `Error` 取值由服务本身返回，与此不同。
  - 反证条件：出现第二个入口（管理 HTTP、Responses 插件等）需要同一组错误码时，应移回根包并导出。
- **K3 README:86 抄写了开发夹具的阈值、超时、上限、状态码和文案：**
  - 这是用户要求保留的使用说明，联调者需要这些值。F9 修完后，这些值在代码里都有名字，也都在同一个 const 块里。
  - 反证条件：以后修改夹具参数却没有同步 README。
- **K4 `guardrails.go:47` 的错误信息手写了三个模式名：**
  - 与 `:20-22` 的常量只隔几行，`guardrails_test.go:18` 也会断言错误信息里包含变量名。
  - 反证条件：新增第四个模式。
- **K5 `guardrails.go:40-49` 的 switch 有一个空的 `case fakeInputBlock:`：**
  - 规则字面量就是 input-block 的配置，其余模式在此基础上改一个字段，空分支用来明确列出合法值。这比每个分支重写整条规则更短，也更容易看出差异。
- **K6 `plugin.go:56-58` 的 `reject` 闭包捕获了 `req` 和 `ctx`：**
  - 它被用了三次，让提前返回保持扁平。本轮只改了被调函数名，没有让它更复杂。
- **K7 根包从 2 个文件拆成 4 个：**
  - 这是用户裁决。拆后每个文件 40–95 行，而且按「快照与编排 / 单条执行 / 规则 / 检测器契约」分组；读 `Check` 时只需跳到 `evaluation.go`。
- **K8 `deploy/ai-gateway-k8s.yaml:46-55` 重复声明了镜像 `ENV` 里已有的默认值：**
  - 在清单里显式写出运行参数是常见的运维写法，而且两边取值相同。
- **K9 `evaluation.go:74-76` 在一行里同时给三个变量赋值：**
  - 这是从旧 `engine.go` 原样搬来的，本轮没有改变。
- **R18（上一轮）：** 在 README:100 的新写法中仍然成立，保留理由不变。

---

**起手表覆盖：**

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 发现：F9（`"dev-fake"` 出现三次）。README:86 抄写夹具值，见保留项 K3 |
| 重复 | 同一规则多份实现且互不注明 | 发现：S1（两份 `Node.start`，范围外挂账）。guardrails 包内没有重复规则，`mapCheckError` 仍只有一份（`deny.go:53`） |
| 重复 | 重造已有 helper | 无。插件注册复用 `SyncLoadedPlugin`，冒烟复用 `identity_smoke.Node`，镜像构建复用 `make -C ee build` |
| 重复 | 派生常量无推导 | 无。`fakeMaxTextBytes = 64 * 1024` 写出了推导 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 发现：F9（检测器 ID）。`math.MinInt` 有注释（`guardrails.go:26-27`） |
| 职责与实现质量 | 状态码、错误码裸数字 | 发现：F9（`StatusCode: 400`）。错误码均为命名常量 |
| 职责与实现质量 | 无名的错误形状 | 无。`errorCode` 用空串表示「通过」，与旧实现相同，并在 `validateRequest`/`checkText` 两处一致使用 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无新增问题，`reject` 闭包见 K6，空 case 见 K5 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无。根包拆分见 K7；`deny.go` 69 行，集中了拒绝映射，拆分合理。所有文件都不超过 300 行 |
| 职责与实现质量 | 多余状态或守卫 | 无。`HasRules` 算了两次，见 K1；`checkText` 的 `ctx == nil` 守卫（`plugin.go:106`）在 PreLLM 路径上已由 `validateRequest` 拦截，但 PostLLM 路径没有，两处保护的对象不同 |
| 职责与实现质量 | 同一效果做两次 | 无（部署 ENV 与清单重复见 K8） |
| 职责与实现质量 | 注释与现状矛盾 | 发现：F8（测试注释残留「事件」）；S2（部署文档过程叙述，来自 develop）。`bootstrap.go:16`、`app/README.md:7`、`plugin.go:21,30` 已同步 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无，逐项比较如下。插件注册：一致（包壳指南 07:51）。插件排序：一致（`config.go:6148-6170`）。环境变量常量：一致（`identity.go:24`、`pricing.go:18`）。状态码写法：不一致，已列入 F9。错误码位置：不同于 identity，见 K2。冒烟夹具：一致（`branding-smoke.py:38`） |
| 架构与项目一致性 | **架构符合性** | 符合，见「四维核查」（§1、§4、§6、§7） |
| 架构与项目一致性 | **依赖关系** | 符合，见「四维核查」（§5、§7） |
| 架构与项目一致性 | 绕过既有出口 | 无。插件经宿主注册出口，拒绝经 `BifrostError` 短路（`deny.go:43-50`），不写存储 |
| 架构与项目一致性 | 把上游现状当契约 | 无。「插件排在内置之前」已由上游代码核实；README:104 明确说明生产插件组合仍需在实际装配时验证 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无。Category 新取值与错误码在代码、README、冒烟三处一致；本期没有前端 |
| 契约一致性 | 状态码口径不一 | 无。README:86「HTTP 400」、`guardrails.go:57`、`guardrails-smoke.py:72` 一致（写法问题见 F9） |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。`EE_GUARDRAILS_FAKE` 未设置和空值都表示关闭（`guardrails.go:32-35`，用 `Getenv` 不区分两者），与 README:78「未设置或空值时不注册」、`guardrails_test.go:11` 一致 |

**开放式复读：** 已放下起手表，以半年后要改这里的维护者身份，把范围内每个文件重读一遍，问「第一眼会让人皱眉的是什么」：
- `app/guardrails.go`：`"dev-fake"` 为什么写三遍、`400` 从哪来，已记为 F9；空 case 见 K5。
- `guardrails/README.md`：插件文件在表里被拆开，已记为 F11；长度按用户裁决不提。
- `detectors/fake/README.md`：看完才发现全是父 README 的内容，已记为 F10。
- `checker_test.go`：测试名还叫 Engine，已记为 F8。
- `plugin/deny.go`：`minErrorStatus/maxErrorStatus` 只在 `plugin.go:35` 构造时使用，却放在 deny.go；`requestID` 也同时被 `plugin.go:112` 的日志使用。两者都与「拒绝响应的状态码与请求标识」相关，放在一起读得通，不构成问题。
- `guardrails-smoke.py`：在 `for mode in ['', *MODES, '']` 里，首尾两个空串分别验证「默认关闭」和「重启后关闭」，意图靠 app README:33 的说明才能看懂。脚本只有 147 行，读者能对上，不作为发现。
- `pricing-smoke.py`：父类加了 `extra_env` 后，抄的那份 start 失去理由，已记为 S1。
- `deploy/构建基础镜像.md`、`deploy/K8s部署步骤.md`：有过程叙述和个人路径，已记为 S2。
- `AGENTS.md`、`ee/README.md`、`checker.go`、`evaluation.go`、`rule.go`、`detector.go`、`plugin.go`、`text.go`、`bootstrap.go`、`app/README.md`、`Dockerfile`、`Dockerfile.buildbase`、`.dockerignore`、三个 yaml：没有新的皱眉点。

**表外发现：** S1、S2（均已列入挂账建议）。除此之外无表外发现。

**实际静态阅读范围与未执行的检查：**
- **未运行的命令：**
  - `go vet`、`go test`、`go build`、预飞、冒烟脚本都没有运行：它们会写构建缓存或临时目录，本评审只读。
  - 没有运行 `docker build`/`kubectl`：属于外部操作。
- **未全文读的文件：**
  - `plugin/integration_test.go`、`plugin/schema_test.go`、`plugin/text_test.go` 不在本轮差量内，只用 `git grep` 确认其中没有旧符号。
  - `ee/Makefile` 只 grep 了 build 和 smoke 目标。
  - `pricing-smoke.py` 只读了第 20–69 行，`identity-smoke.py` 只读了第 30–119 行。
  - `transports/docker-entrypoint.sh` 和 `transports/Dockerfile` 已全文读，作为同类对照。
- **未读的过程文档：**
  - 没有读评审期间被外部修改的 `workbench/evidence/内容安全框架-期1-开发联调.md`，也没有读其他 workbench 生成物与过程文档。
  - 按要求没有读旧代码评审、方案v1、方案评审和更早的收敛评审，只读了 `收敛评审4.md`，用于逐条处置。
- **上游核实范围：** `server.go:2025-2074`、`lib/config.go:6039-6069,6120-6174`、`server/plugins.go:196`（grep），以及 `product/docs-zh/04-开发指南/07-ee包壳.md:51`（grep）。
