---
title: 内容安全框架 期1 代码评审 #3
type: 代码评审
case: 内容安全框架
phase: 1
round: 3
verdict: 通过
created: 2026-09-16
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe 到候选 8fd978c7c76f718c38093f15e390f84d40fa4661；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过，限候选 `8fd978c7c76f718c38093f15e390f84d40fa4661` 的本轮差量复核。** 本轮运行代码仅增加注释，新增字段清单测试及 README 说明与声明修改一致；上一轮两项修复保持关闭，四条转交已逐条处置，未发现新增必修项。checklist 六条命令及产物检查全部通过。定价、SQLite 身份 smoke 通过；品牌 smoke 因缺少嵌入 UI 失败，不能宣称全部冒烟通过。本结论不代表真实检测效果、生产装配或 Gate 2 已通过。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 正常、命中、仅记录、失败放行/拦截、取消 | 当前引擎及 Hook 测试覆盖，亲跑通过 |
| 全历史输入、全部非流式候选输出 | 提取与共享字节预算保持；相关行为测试通过 |
| 有输出规则拒绝流式；仅输入规则允许流式 | 保持已确认口径，Hook 和 BF 管线测试通过 |
| 本轮声明修改 | README 展开限制说明；引擎、拼接各增加一处校验顺序注释；字段清单增加 `BifrostChatRequest` |
| 公共符号、依赖、生产装配 | 差量没有新增接口签名、依赖或生产注册 |
| 三方不一致、多做/少做、未登记偏差 | 未发现 |

范围核验：

- 分支为 `feat/content-safety`；基线及上一轮候选均为 HEAD 祖先。
- 已读取四个指定差量文件的完整现状。内存比较确认 `engine.go`、`text.go` 去掉整行注释后与上一轮一致，因此保留差量复核范围。
- 初始存在未跟踪的 `workbench/evidence/内容安全框架-期1-交付摘要.md`，已完整读取。
- 评审期间外部新增 `收敛评审3.md`，并修改四个生成视图；仅登记该评审文件名，没有读取意见。末次视图检查通过。源码、索引、HEAD 均未变化。

## 3. 本轮修复项

**无新增必修项。** 上一轮记录「本轮修复项」中的两项逐条复核如下：

| 上轮意见 | 处置与证据 |
|---|---|
| R1：输出 logprobs 文本漏检 | **已修复，保持关闭。** [text.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:71) 拒绝非 nil `LogProbs`；[text_test.go:111](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:111) 覆盖生成词、候选词及 refusal token 的风险内容，验证不返回原响应并禁止 fallback。亲跑通过；无需追加修正。 |
| R2：UTF-8 扫描早于限长/取消 | **已修复，保持关闭。** [engine.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine.go:71) 先取消、后限长、再扫描；[text.go:118](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:118) 先检查剩余预算。[engine_test.go:243](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine_test.go:243) 与 [text_test.go:162](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:162) 验证错误优先级及已完成事件保留。亲跑通过；无需追加修正。 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无新增事项。**

收敛评审四条转交的处置：

| 转交项 | 处置 | 依据 |
|---|---|---|
| 空 logprobs 可能导致普通输出全部被拒 | **撤回本期必修要求** | [text.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:71) 确实连空结构一起拒绝，但只在启用输出检查时生效。Gemini 转换在空候选时返回 nil（[utils.go:3090](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/providers/gemini/utils.go:3090)）；OpenAI 路径按实际响应解码。未取得某 Provider 默认返回空结构、使普通请求全部失败的证据。整载体拒绝已登记为本期限制；不据此声称所有 Provider 兼容。 |
| 请求 LogProbs/TopLogProbs 应在 PreLLM 提前拒绝 | **撤回本期必修要求** | [text.go:29](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:29) 不检查这两个控制参数，模型调用后若返回 logprobs 且启用输出规则，会明确阻断。仅输入规则时 [plugin.go:86](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin.go:86) 直接透传输出，因此“请求后必然拒绝”不普遍成立。当前未发生漏检放行；方案也未承诺所有不支持的输出载体均提前拒绝。提前避免模型调用属于可优化行为。 |
| 消息 Name 拒绝影响客户端可用性 | **撤回本期必修要求，保留限制事实** | OpenAI 转换保留 Name（[utils.go:134](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/providers/openai/utils.go:134)），[text.go:85](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:85) 在受检消息上明确拒绝。输入、输出均有行为回归。本期已登记消息名称为未覆盖载体，没有承诺全部可选 Chat 字段兼容；未验证其实际客户端使用频率。 |
| branding-smoke 失败时清理现场 | **挂账，沿用 FIND-026** | 当前准确位置为 [branding-smoke.py:126](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/branding-smoke.py:126)：`TemporaryDirectory` 在异常退出时也清理目录，`finally` 回收自有进程。本轮再次亲跑于第 67 行失败，之后 scratch 中无品牌现场目录。既有台账已登记，脚本不在本轮修改范围；不新增或修改台账。 |

## 5. 转收敛评审 / 挂账

**转收敛评审：无新增。**

**本轮范围内挂账：无。** 转交的既有品牌脚本问题沿用 `FIND-026`，处置见上一节。

## 6. 开放式复读的表外发现

**已做开放式复读，无表外新增发现。**

四个文件的失败识别与恢复路径均已复核：

| 文件 | 失败如何识别、如何恢复 |
|---|---|
| README | 明确未支持载体、同步取消约束及装配前提，调用方可调整请求或配置 |
| engine.go | 配置错误在构造时返回；检测失败生成分类事件；框架错误阻断并保留此前事件 |
| text.go | 未支持结构、超限、非法文本返回错误，经 Hook 转为带安全错误码的拒绝，禁止 fallback |
| schema_test.go | 按类型名定位失败，打印实际字段并要求复审提取边界；不能仅更新清单冒充行为验证 |

## 7. 起手表覆盖

结果限定本轮差量、上一轮意见及转交涉及区域。

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | **无**：本地框架验收测试通过 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | **无** |
| 对照方案 | 多出功能、公共接口、抽象或依赖 | **无** |
| 对照方案 | 偏差未登记 | **无** |
| 行为正确性 | 失败被吞或降级成假成功 | **无**：Failed 与 Passed 分开记录 |
| 行为正确性 | 空值、空集合无分支 | **无**：缺失内容、空文本、空策略分别处理 |
| 行为正确性 | 取消或超时后仍有写入或副作用 | **无**：取消阻断；同步协作取消限制已明确 |
| 行为正确性 | 重发产生两份副作用 | **无**：未新增持久化或业务写入 |
| 行为正确性 | 事务外写后读、回滚不完整 | **不适用**：无事务变更 |
| 行为正确性 | 关闭、旧数据、旧客户端无处理 | **无**：空策略透传；兼容限制已核实 |
| 行为正确性 | 昂贵检查早于廉价检查、校验缺层 | **无**：限长/扫描顺序修复保持 |
| 行为正确性 | 路由保护、认证关闭行为 | **不适用**：无路由、认证或生产装配变更 |
| 行为正确性 | 敏感信息进日志或响应 | **无**：正文及检测器异常原文不输出 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | **无**：差量未改变运行行为 |
| 行为正确性 | 未登记配置、环境依赖或部署步骤 | **无** |
| 失败可观测 | 服务端无法追溯原因 | **无**：保留请求标识、规则事件及拒绝码 |
| 失败可观测 | 对外泄露内部错误原文 | **无** |
| 失败可观测 | 脚本失败现场被清理 | **发现**：既有品牌 smoke，沿用 FIND-026 |
| 测试可信 | 新规则或失败路径无测试 | **无**：新增字段清单已执行，历史修复回归通过 |
| 测试可信 | 断言绑定实现而非行为 | **无**：字段清单只承担升级提醒，另有行为测试 |
| 测试可信 | 失败信息无法定位条件 | **无**：类型及行为用例可定位 |
| 测试可信 | 返回值、错误未检查 | **无** |
| 测试可信 | fixture 依赖个人环境 | **无**：受审测试不依赖个人数据库或已有实例 |
| 测试可信 | mock 结果冒充真实环境结论 | **无**：明确测试检测器及模型替身边界 |
| 测试可信 | 整体替换被测模块的 mock | **无**：执行实际引擎、Hook 和 BF 管线 |
| 残留 | 调试输出、后门、本机配置 | **无** |
| 残留 | 未使用代码、无关改动 | **无**：生产未注册符合非目标 |
| 残留 | 生成物与源不一致 | **无**：末次工作台校验通过 |

## 8. 验证记录

环境：`go1.27.1 darwin/arm64`。以下 `$TMPDIR` 均为用户指定的：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全框架-期1-代码评审3/scratch`

### 实际执行命令与结果

| 命令 | 结果 |
|---|---|
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go vet ./...` | **通过** |
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go test -race -count=1 ./...` | **通过**；guardrails 1.085s，plugin 1.193s |
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | **通过** |
| `gofmt -l ee/internal/guardrails` | **通过**，无输出 |
| `git diff --check` | **通过**，末次复查也通过 |
| `git diff --check c503a4bb8c98ab49ef86ef4dd31daee2b4736ebe..8fd978c7c76f718c38093f15e390f84d40fa4661` | **通过** |
| `python3 -B workbench/tools/build_views.py --check` | **通过**：初次 141 篇，末次 142 篇 |
| Python `Path.exists()` 检查 checklist 三项必存产物 | README、engine.go、plugin.go **全部存在** |
| `rg -n 'type Detector interface' ee/internal/guardrails` | 命中 `model.go:54` |
| `rg -n 'proxywasm\|wasm-go\|net/http\|fasthttp' ee/internal/guardrails`（正则交替检查） | **均无命中** |
| `python3 -B ee/scripts/pricing-smoke-test.py` | **6 项通过**；出现 SQLite 连接未关闭的 `ResourceWarning` |
| `python3 -B ee/scripts/pricing-smoke.py --binary "$TMPDIR/bifrost-http"` | **通过** |
| `python3 -B ee/scripts/branding-smoke.py --binary "$TMPDIR/bifrost-http"` | **失败，exit 1**：第 67 行 SPA HTML 断言 |
| `python3 -B ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | **通过** |
| 内存 Python 比较两处运行文件、检查 README 与交付摘要本地链接 | 非注释行未变；无缺失链接目标 |
| `git status`、祖先关系、差量统计、源码及索引检查 | 候选源码、索引和 HEAD 未变 |
| `ps -axo pid,ppid,etime,pcpu,comm` | 环境拒绝；未提权或操作既有进程 |

品牌失败归因依据：EE 嵌入目录只有 `.keep`，UI handler 在找不到 `ui/index.html` 时返回 404。该 smoke 后续断言未执行。

所有测试命令均已退出；smoke 使用自身 `finally/stop` 回收子进程。身份 smoke 现场保留在 scratch 的 `bifrost-identity-smoke-62ob_3cy`。

二进制 SHA-256：

```text
adf84d3a25e11d562ff0601f757e8545c2e7d4005a1f58c589b28f73c7a55128
```

### 实际阅读范围及未执行项

- 完整读取指定规则、需求、方案、findings、checklist、两份交付证据、`代码评审2.md`，以及四个差量文件。
- 补读 guardrails 模型、插件及全部测试；定向追踪 BF 短路、PostHook、fallback、Chat 类型、OpenAI/Gemini 转换；读取三个 smoke、定价 smoke 自测及 UI 缺失处理。
- 未读取其他旧评审或本轮收敛评审正文，仅处置用户提供的转交原文。
- checklist 无跳过项；构建路径改为 scratch，race 增加 `-count=1`，Python 增加 `-B`。未执行会写报告的预飞包装器，其检查项已逐条亲核。
- 未构建 UI、运行整套 `make build/smoke`：该路径会同步 workspace/UI 文件；已直接执行允许的自包含 smoke。
- 未运行 PostgreSQL 检查：两个测试 DSN 均未设置，且禁止连接已有实例。
- 未调用真实模型或检测服务，未进行性能压测、历史版本 red 测试或任意生产插件组合验证。
- 末次工作树外部变化为四个生成视图，以及未跟踪的 `收敛评审3.md`、交付摘要；本会话未修改代码、索引、报告或视图，未启动 dev server，未暂存、提交、推送。

verdict: 通过
