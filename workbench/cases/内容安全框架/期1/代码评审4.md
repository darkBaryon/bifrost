---
title: 内容安全框架 期1 代码评审 #4
type: 代码评审
case: 内容安全框架
phase: 1
round: 4
verdict: 通过
created: 2026-09-16
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 8fd978c7c76f718c38093f15e390f84d40fa4661 到候选 958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过，限候选 `958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8` 的本轮差量复核。** 指定改动只有 README 第 33 行，未改变运行代码、测试或依赖；声明修改与现有行为一致，历史修复保持关闭，无新增必修项。checklist 六项命令及产物检查全部亲核通过。定价、SQLite 身份 smoke 通过；品牌 smoke 因缺少嵌入 UI 失败，不能宣称全部冒烟通过。本结论不代表真实检测效果、生产装配或 Gate 2 已通过。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 本轮声明修改 | README 明确拒绝清单仅为举例，补充预测内容、响应格式、联网搜索、文件、拒答正文，并链接提取实现；与方案第三轮修复记录一致 |
| 实际行为 | `text.go:29` 拒绝预测内容、响应格式和联网搜索；`:89`、`:107` 拒绝拒答正文及文件载体 |
| 验收口径 | 正常、命中、仅记录、失败放行/拦截、取消，以及输出流拒绝均有现有测试，本轮亲跑通过 |
| 接口面 | 差量无新增符号、签名、配置、依赖或生产装配 |
| 三方不一致、多做/少做、未登记偏差 | 本轮范围内未发现 |

失败路径定向复核结果：

| 路径 | 最终去向 |
|---|---|
| 不支持载体、超限、取消 | Hook 返回安全错误；记录请求标识及拒绝码，`AllowFallbacks=false` |
| 检测器错误、超时、非法结果 | 生成 `Failed` 分类事件，按显式失败策略放行或阻断 |
| 中途框架错误 | 返回阻断，保留此前已完成事件 |
| 上游错误 | 原样保留，不审核其正文；README 已说明此边界 |

范围证明：`git diff 8fd978c7..958fc983 -- ee ':!ee/internal/guardrails/README.md'` 无差异，因此保持差量复核，无需恢复全量。未跟踪交付摘要已完整读取；评审期间其候选及转交内容被外部更新，已重新核对。

## 3. 本轮修复项

**无新增必修项。** 已读取 `代码评审3.md`，其中「本轮修复项」逐条处置如下：

| 上轮意见 | 处置、证据与最小修正 |
|---|---|
| R1：输出 logprobs 文本漏检 | **已修复，保持关闭。** [text.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:71) 拒绝非 nil `LogProbs`；正文安全但生成词、候选词或 refusal token 含风险文本时，不返回原响应且禁止 fallback。[回归测试:111](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:111) 亲跑通过。无需追加修正。 |
| R2：UTF-8 扫描早于限长/取消 | **已修复，保持关闭。** [engine.go:71](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine.go:71) 先取消、后限长、再扫描；[text.go:118](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:118) 先检查剩余预算。超限与非法编码并存、已取消请求及事件保留的[引擎回归:243](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/engine_test.go:243)和[提取回归:162](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text_test.go:162)亲跑通过。无需追加修正。 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无新增事项。**

用户提供的收敛转交原文称“无新增”，其中延续的三条逐条处置：

| 转交项 | 处置 | 依据 |
|---|---|---|
| logprobs 空结构 | **撤回本期必修要求，保持原处置** | `text.go:71` 确实整载体拒绝，属于已登记限制；[Gemini 转换:3090](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/providers/gemini/utils.go:3090) 对无 chosen candidates 返回 nil。本轮没有“默认空结构导致普通请求全部失败”的新证据。 |
| 主动请求 logprobs 应提前拒绝 | **撤回本期必修要求，保持原处置** | 请求控制参数未提前拒绝；启用输出检查且返回 logprobs 时明确阻断。仅输入规则时 [plugin.go:87](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin.go:87) 透传输出。未发现受检内容漏检放行，方案未承诺全部提前拒绝。 |
| 消息 `Name` 可用性 | **撤回本期必修要求，保留限制事实** | [text.go:85](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/text.go:85) 拒绝受检消息名称，输入、输出回归通过；本期未承诺全部可选 Chat 字段兼容。 |

上一轮另列的品牌 smoke 失败现场清理，**沿用 FIND-026 挂账**：本轮再次在 `branding-smoke.py:67` 失败；`:126` 的 `TemporaryDirectory` 随异常清理现场。该既有脚本不在本轮修改范围，不重新提出必修要求。

## 5. 转收敛评审 / 挂账

**转收敛评审：无新增。**

**本轮范围内挂账：无。** 范围外品牌脚本问题沿用 FIND-026，见上一节。

## 6. 开放式复读的表外发现

**已做开放式复读，无表外发现。**

重新通读 README，并按“失败如何发现、如何恢复”检查：拒绝码与日志可识别检查失败；调用方可调整载体、长度、流式方式或检测器实现。同步协作取消、上游错误透传和生产插件排序前提均已明示，没有将框架能力写成已启用生产防护。

## 7. 起手表覆盖

以下结果限定本轮差量、历史意见及相关验证区域。

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | **无**：框架验收测试通过 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | **无** |
| 对照方案 | 多出的功能、公共接口、抽象或依赖 | **无** |
| 对照方案 | 偏差未登记 | **无** |
| 行为正确性 | 失败被吞或降级成假成功 | **无**：失败分类及阻断出口已追踪 |
| 行为正确性 | 空值、空集合无分支 | **无**：缺失正文、空文本、空策略有区分 |
| 行为正确性 | 取消或超时后仍有写入或副作用 | **无**：本轮无行为变化；协作取消限制明确 |
| 行为正确性 | 重发产生两份副作用 | **不适用**：本轮仅文档修改，无新增业务写入 |
| 行为正确性 | 事务外写后读、回滚不完整 | **不适用**：无事务变更 |
| 行为正确性 | 关闭、旧数据、旧客户端无处理 | **无**：空策略透传，兼容限制已登记 |
| 行为正确性 | 昂贵检查早于廉价检查、校验缺层 | **无**：R2 修复保持，回归通过 |
| 行为正确性 | 路由保护、认证关闭行为 | **不适用**：无路由、认证或装配变更 |
| 行为正确性 | 敏感信息进日志或响应 | **无**：受审路径不输出正文或检测器异常原文 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | **无**：差量未修改运行代码 |
| 行为正确性 | 未登记配置、环境依赖或部署步骤 | **无** |
| 失败可观测 | 服务端无法追溯失败原因 | **无**：请求标识、规则事件和错误分类可追溯 |
| 失败可观测 | 对外泄露内部错误原文 | **无** |
| 失败可观测 | 脚本失败现场被清理 | **发现**：品牌 smoke，沿用 FIND-026 |
| 测试可信 | 新规则或失败路径无测试 | **无**：本轮没有新规则；历史修复回归通过 |
| 测试可信 | 断言绑定实现而非行为 | **无**：行为断言与字段升级提醒分工明确 |
| 测试可信 | 失败信息无法定位条件 | **无**：受审用例报告条件、结果或错误码 |
| 测试可信 | 返回值、错误未检查 | **无**：相关 Hook、引擎断言已核对 |
| 测试可信 | fixture 依赖个人环境 | **无**：受审测试及所跑 smoke 使用隔离数据 |
| 测试可信 | mock 结果冒充真实环境结论 | **无**：测试检测器、模型替身边界明确 |
| 测试可信 | 整体替换被测模块的 mock | **无**：实际执行引擎、Hook 和 BF 管线 |
| 残留 | 调试输出、后门、本机配置 | **无** |
| 残留 | 未使用代码、无关改动 | **无**：指定差量仅 README；未注册符合非目标 |
| 残留 | 生成物与源不一致 | **无**：末次视图检查通过 |

## 8. 验证记录

环境：`go1.27.1 darwin/arm64`。下列 `$TMPDIR` 为：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全框架-期1-代码评审4/scratch`

### 实际执行命令与结果

| 命令 / 检查 | 结果 |
|---|---|
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go vet ./...` | **通过** |
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go test -race -count=1 ./...` | **通过**；guardrails 1.071s，plugin 1.259s |
| `cd ee && GOWORK=off GOCACHE="$TMPDIR/go-cache" go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | **通过** |
| `gofmt -l ee/internal/guardrails` | **通过**，无输出 |
| `git diff --check` | **通过**，末次复查通过 |
| `git diff --check 8fd978c7c76f718c38093f15e390f84d40fa4661..958fc983076d0b7cfe83aad1be0ee3f31a1aa5a8` | **通过** |
| `python3 -B workbench/tools/build_views.py --check` | **通过**；初次 143 篇，末次 144 篇 |
| Python `Path.is_file()` 检查三项必存产物 | README、engine.go、plugin.go **全部存在** |
| `rg -n 'type Detector interface' ee/internal/guardrails` | 命中 `model.go:54` |
| `rg -n 'proxywasm\|wasm-go\|net/http\|fasthttp' ee/internal/guardrails`（实际使用正则交替） | **均无命中** |
| Python 检查 Go 文件行数及 README、摘要本地链接 | 最大 271 行；链接目标均存在 |
| `python3 -B ee/scripts/pricing-smoke-test.py` | **6 项通过**；出现 SQLite 连接未关闭的 `ResourceWarning` |
| `python3 -B ee/scripts/pricing-smoke.py --binary "$TMPDIR/bifrost-http"` | **通过** |
| `python3 -B ee/scripts/branding-smoke.py --binary "$TMPDIR/bifrost-http"` | **失败，exit 1**：第 67 行 SPA HTML 断言 |
| `python3 -B ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | **通过** |
| Git 状态、基线祖先关系、差量、索引及 EE 源码核验 | 两个基线均为祖先；HEAD、索引和 EE 源码未变 |

品牌失败归因依据：`ee/cmd/bifrost-http/ui` 只有 `.keep`；UI handler 找不到 `ui/index.html` 时返回 404。失败后的品牌后续断言未执行，现场目录已被脚本删除。

全部测试命令均已退出；smoke 的 `finally/stop` 回收自有子进程。身份 smoke 保留目录为 `$TMPDIR/bifrost-identity-smoke-hfrruau0`。

构建二进制 SHA-256：

```text
6e5826ca740eca7602577fa3b4db3c9845c66ee17afc0c844f429ef2cf98f773
```

### Git 状态绑定

初始状态仅有未跟踪交付摘要。末次状态为外部新增评审及生成视图更新：

```text
 M workbench/SUMMARY.md
 M workbench/assets/nav.js
 M workbench/cases/内容安全框架/index.md
 M workbench/views/all.md
?? workbench/cases/内容安全框架/期1/收敛评审4.md
?? workbench/evidence/内容安全框架-期1-交付摘要.md
```

末次跟踪文件差异为上述四个视图，4 行新增、1 行删除；EE 与 HEAD 无差异，暂存区无差异，HEAD 仍为本轮候选。

### 实际阅读范围及未执行项

- 完整读取指定规则、需求、方案、findings、checklist、两份证据、`代码评审3.md` 和 README。
- 补读 guardrails 四个实现文件及全部测试、四个 smoke 脚本、定价 smoke 自测、EE Makefile；定向追踪 BF 短路/PostHook/fallback、OpenAI 名称转换、Gemini logprobs 转换及 UI 缺失处理。
- 未读取其他旧评审或任何收敛评审正文；`收敛评审4.md` 仅登记文件状态。
- **checklist 无跳过项。** 构建输出改到 scratch，race 增加 `-count=1`，Python 增加 `-B`。未运行写报告的预飞包装器，已逐条执行其检查。
- 未运行 `make build/smoke`：会同步 workspace/UI；改为直接运行三个自包含 smoke。未构建 UI。
- 未运行 PostgreSQL 检查：`BRANDING_TEST_POSTGRES_DSN`、`IDENTITY_TEST_POSTGRES_DSN` 均未设置，且禁止操作已有实例。
- 未运行身份升级 smoke：需要明确提供历史二进制，本轮没有迁移改动，也非 checklist 必过项。
- 未调用真实模型/检测服务，未做生产插件组合、性能压测或历史版本 red 测试。
- 本会话未创建或修改代码、报告、索引或跟踪文件，未启动 dev server，未暂存、提交、推送；仅产生获准的构建与测试产物。

verdict: 通过
