---
title: workbench外壳重设计 期1 收敛评审 #1
type: 收敛评审
case: workbench外壳重设计
phase: 1
round: 1
verdict: 仅立债挂账
created: 2026-09-02
---

> 评审者与实施上下文隔离(干净会话,独立于并行代码评审)。基线 `b00f19b..d7cb8da`(4 commit)。级别 L2,六维一次通读 + 嫌疑取证。

## 结论

本次 Change 让 workbench 净变好——收敛评审从"规范里有、外壳里无"的暂借状态转为 schema/校验器/模板/流程锚点四层原生,同时砍掉两份冗余视图与两份零引用模板,操作页由三页收敛为一页;代价是新引入两处小额未清偿项(执行序双页同步义务出生即带一处分叉、新增台账与 `triggered_by` 字段游离于机械校验之外),均为挂账量级,不阻塞 Gate 2。

## 事实来源

- `git log/diff/stat b00f19b..HEAD -- workbench/`(4 commit 与方案 §4 步骤一一对应;+813/−263);
- 通读:AGENTS/SCHEMA/规范.md/ADOPTION/findings.md/templates/收敛评审.md/占位页/Harness 横幅/build_views.py 全文/tests/方案v3 含实施记录/checklist/REF 需求;
- 旧态回源:`git show b00f19b:` 旧 AGENTS、本仓操作规范(60 行)、convergence-review 模板(英文 frontmatter + CHG 编号制,与"case 名即主键"冲突——重命名+对齐 SCHEMA 是修正而非美化);
- 独立实跑:--check OK(21 篇)、pytest 5 passed、build_site exit 0、preflight 对本案 checklist 重放全绿;
- 残留引用扫描:仅剩 规范/change 两页 vendor 自带既有死链(ADOPTION 第 11 条已声明,非本案引入)。

## 六维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | **净减一、新增一**。删:本仓操作规范占位化(约 70% 重叠)、active.md(同函数产物)、by-case.md(与 cases/*/index.md 重复)、两份零引用模板。增:「执行序」在 AGENTS 流程锚点与 规范.md 单线图以同等粒度各写一遍,且出生即分叉(`/convergence-review skill` 仅 规范.md:37 提及)。见挂账 1。状态机 AGENTS↔SCHEMA 双写为既有格局,本次同步无遗漏 | diff d7cb8da;规范.md:37 vs AGENTS.md:54 |
| 膨胀与职责 | 无碍。入口页职责清晰化(README 门牌/AGENTS 唯一操作页/规范.md 地图+理念/SCHEMA 数据字典);Harness 16KB 保留但横幅降级为考古层;templates 8 份份份有消费方 | AGENTS.md:3 |
| 架构边界 | 无碍。"SCHEMA 定义 → build_views 强制 → templates 起稿 → preflight 验收"单向链未破坏,收敛评审沿同链落位;占位页策略成本≈0;暗礁:占位页物理上住在只读副本 规范/ 内,re-vendor 整目录替换会连带清掉它(届时无入链,断链面为零,不入账) | 占位页:1-6;ADOPTION:37-41 |
| 契约一致性 | 基本无碍,一处缺口入账:SCHEMA 把 `triggered_by` 立为一等血缘字段,但 validate() 只查 relates 悬空,`triggered_by` 悬空静默通过——与铁律3"悬空即拦"承诺不对称。见挂账 2。三方一致性(SCHEMA↔REQUIRED/VERDICTS↔模板)核对无误;旧 verdict「通过」零残留 | build_views.py:112-114;SCHEMA.md:22 |
| 测试健康 | 变好。1→5 测试,4 条新断言精确对应 S2;S3 用真实数据当了一次集成测试;phase_flat 900/950/999 魔数有测试钉序+行内注释,碰撞域不现实,不入账 | tests:48-88 |
| 非功能 | 无碍。零第三方依赖保持;门禁独立重放全绿;死链 grep 锚定 `href="` 防自匹配实测成立 | 事实来源第 4 条 |

## 出口清单

### Blocking(阻塞 Gate 2)

- 无。

### 立债(REF)

- 无。两条挂账均 15-30 分钟量级,不值得立案排期。

### 挂账(Finding → findings.md FIND-010/011)

1. **执行序双页同步义务,出生即带一处分叉**(AGENTS 流程锚点 vs 规范.md 单线图;skill 提及只在一边)。理想结构:AGENTS 为正典,另一页降为链接或粗粒度示意。反证条件:用户裁定两页服务不同读者而刻意双写则转豁免,但 skill 提及仍应对齐。规模 ±15 行。
2. **findings.md 台账与 `triggered_by` 无机械校验**(id 唯一性/status 枚举/指向域均不查;findings-count 仅本案 checklist 非常驻)。理想结构:build_views 加约 25 行解析校验 + validate() 域检查 + 2 条测试。反证条件:用户确认"9 条不值得上机器"的比例判断则转豁免,台账过 30 条重估。规模 ±30 行。

### 如果由评审者来做(不入账)

phase_flat 用显式 `(stage, n)` 元组替代单数轴(现实收益≈0,有测试钉序);Harness 横幅可注明"本文不再随流程演进更新"把归档语义说满。

## 台账对账

FIND-001~009 全部关于 ui/ 中文化层,与本 Change 无交集,无恶化;FIND-009 所指 REF 需求已由本 Change 顺手立案,账实相符。本页挂账 2 条登记为 FIND-010/011(`triggered_by: [workbench外壳重设计]`)。
