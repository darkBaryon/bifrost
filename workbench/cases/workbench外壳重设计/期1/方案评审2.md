---
title: workbench外壳重设计 期1 方案评审 #2
type: 方案评审
case: workbench外壳重设计
phase: 1
round: 2
verdict: 需修改
created: 2026-09-02
---

> 同一独立 agent 复核第 1 轮 5 条阻断 + 3 条建议在方案v2 的闭合情况,不重审已通过部分。

## 结论

**需修改**,残余 1 条(修复面 = checklist 1 行 + §5 一句)。B1/B2/B3/B5 与建议 1-3 全部闭合(逐条判定见评审记录表,依据均落到 v2 行号);B4 的 9 条新命令实跑语法全部有效、红项均为"待实施"性质,唯 no-dead-links 存在自匹配缺陷。

## 残余阻断

**B4-r|no-dead-links 裸文件名 grep 会被方案自身及评审1 的渲染页自匹配,实施正确后仍永久红。**
实测证据:方案v2 §5 引用了命令原文,渲染后 `site/.../方案v2.html` 正文含裸串 `active.html`/`by-case.html`(行内代码经 html.escape,引号转义但文件名原样);`方案评审1.html` 同(处方文本 2 处)。视图与 nav 删干净后该项不可能变绿,预飞永久卡红。
处方(已实测自安全):锚定链接属性 `! grep -rq 'href="[^"]*\(active\|by-case\)\.html' workbench/site --include='*.html'`——渲染器把行内代码的引号转义为 `&quot;`/`&#x27;`,被引用命令文本永不含裸 `href="`;真实死链必为 `href="...by-case.html"` 形态。实测:锚定式对引用文本 0 命中,对当前基线侧栏真实链接命中正常(判别力正确)。

## 备注(非阻断)

- Gate 口径变化留用户知悉:v2 起 §3 取舍不再单独请 Gate 1,随 Gate 2 复核(取舍 2-4 系既有裁决推导、取舍 1 系一致性必然)。
- `site/.pytest_cache/README.html` 渲染噪音系既有问题,不扩范围。

## 验证命令记录

preflight 实跑(9 命令+2 产物+2 符号+1 禁留全执行,无语法错);`grep -rl active.html site` 全站命中含方案v2.html(自匹配证据);锚定式对引用文本 0 命中验证;回源 reports 非目标豁免条款。
