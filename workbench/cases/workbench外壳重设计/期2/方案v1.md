---
title: workbench外壳重设计 期2 方案 v1
type: 方案
case: workbench外壳重设计
phase: 2
version: 1
status: 已实施
relates: [workbench外壳重设计]
created: 2026-09-02
---

> 期2 = 本 case 的真正主诉:**规范页面内容重排**(期1 只动了外壳工具,页面内容一字未动,用户明确指出这不是要的)。用户已裁决:playbook 上游仓是历史中间态,vendor 只读约束废除,本地自由演化(已记 ADOPTION);布局方向(共享机制+类型差异,18→约 10 页)已在会话中展示并获继续指令。status 直接「已通过」依据:布局即方案,已获用户方向确认;内容合并的正确性由实施后的独立「内容无损审计」兜底(见 §4),比事前评审更能防真实风险(丢规则)。

## 1. 诊断(已核实)

同一信息多处重复:流程总览 7 处、风险分级 5 处、职责边界 4 处、偏差表/预飞/方案复核/代码复核 各 2 处(development ↔ refactoring 四步页系镜像克隆,同表同规则只换领域名词)、状态参考 3+1 处。另有 vendor 自带死链:testing/README 引用的 03/04 两页不存在、规范/change 两页指向不存在的 ../templates/。根因:playbook 按"每套自包含"演化,Change 模型后来罩顶但未抽走公共机制。

## 2. 终态布局(每条规则只活在一处)

```
规范.md              # 主页:理念 + 指向流程.md 的一行 + 页面地图(不再自画流程图)
规范/流程.md          # 唯一权威:Change 中心思想/类型/执行序图/通用职责四分/
                     #   Gate 条件触发(拍板项清单)/交付摘要/知识沉淀/项目落地边界与冲突顺序
规范/风险分级.md      # 唯一分级页:L1-L3 判据+必需产物+两根保险丝+定档程序+tier 映射+测试组合表
规范/机制/方案与复核.md # 方案必备内容(合并开发9项/重构10项,差异标注)/方案复核/Gate1/蓝图与分期/敏感信息
规范/机制/实施纪律.md   # 固定起点/标准执行方式/偏差即停(一张表)/实施记录三章节/预飞/并行与共享资源
规范/机制/交付.md      # 自测-推送-独立测试三事实/代码复核(合并清单)/Gate2/集成清理
规范/机制/收敛评审.md   # 原 change/convergence-review.md 原文迁移
规范/机制/finding台账.md # 原 change/findings.md 原文迁移
规范/差异/开发.md      # 仅开发独有:任务判别/立案记录要件/蓝图级任务
规范/差异/重构.md      # 仅重构独有:先证明问题/现状盘点/行为保护/接口白名单/碰巧同形/阶段终扫/结构问题状态
规范/差异/测试.md      # 测试套整页:职责表/交测包/GateT0/分层执行/用例/证据/测试状态图(合并 README+01+02)
```

删除:规范/change|development|refactoring|testing 四目录、规范/本仓操作规范.md 占位页(git 史保留)。AGENTS.md 流程锚点降为"指向 流程.md + 本仓特有三点(preflight 命令/实施记录信号/台账登记)"——顺手关 FIND-010。活页链接全部更新(SCHEMA/findings/README/规范.md/nav);历史 case 文档内旧路径不改(记录当时事实,死链可接受)。

## 3. 合并纪律(防丢规则)

- 尽量保留原句,合并处取两版更强表述;开发/重构差异用「重构另须:」行内标注,不做第三次改写;
- 规范性条目(表格行/清单项/禁令)只许迁移或显式声明放弃并给理由,不许静默消失;
- 实施后跑独立**内容无损审计**:审计者对照 git 基线的旧 18 页,逐条规范性条目在新树中定位,输出"迁移/合并/放弃+理由"三态清单,任何静默丢失即阻断。

## 4. 验收(checklist 同源)

- build_views --check / pytest / build_site 全绿;站点无 href 死链指向已删目录(grep 锚定式);
- 旧四目录与占位页不存在;新 11 页存在;
- 内容无损审计通过(独立 agent);收敛评审(期2 增量)无 Blocking;
- FIND-010 关账。

## 实施记录

单 commit `2c97f22`(基线 `16ca696`,含期1 全部产物):

1. 新树 11 页:流程.md / 风险分级.md / 机制×5(收敛评审与 finding台账 用 `git mv` 迁移保历史,CHG 编号例句改为 case 名口径)/ 差异×3;合并纪律按 §3 执行(保留原句、「重构另须」行内标注、规范性条目不静默丢弃)。
2. 删除:规范/{change,development,refactoring,testing} 四目录 + 本仓操作规范.md 占位页(git rm,史中可考)。
3. 活页更新:规范.md 重写(理念 + 页面地图,不再自画流程图);AGENTS.md 首段与流程锚点降级(执行序唯一权威 → 流程.md,FIND-010 关账);SCHEMA/findings/README/build_views 注释与 nav 全部指向新路径;ADOPTION 头部记 vendor 约束废除。
4. 预飞全绿(7 命令 + 2 产物 + 2 符号);站点 49 页,无指向已删目录的死链(历史 case/evidence 页除外,方案 §2 既定口径)。

**实施记录补充(审计后)**:内容无损审计(独立 agent,证据:`evidence/workbench外壳重设计-期2-内容无损审计.md`)核对约 249 条规范性条目——迁移 ≈168、合并 ≈74、显式放弃 2(合规)、**丢失 5**(均轻微,无核心纪律):先护栏后动刀、复核"是否对症"项、固定起点含生成物状态、"或第三方"判据、"已尝试的安全检查"。5 条已全部补回;并按审计建议在 AGENTS 铁律1 补注约束对象(过程文档),消除与占位页删除的字面张力。

## 实施偏差记录

1. 差异/测试.md 把原 testing/README 状态图与 01/02 两页合并成一页时,顺带以一句话补了 vendor 即缺失的 03(缺陷复测)/04(发布准入)两页的要点(「三个事实」与「证据与历史」节)——原文不存在故无从"迁移",属新增笔墨,已在该页开头注明。
2. 机制/收敛评审.md 与 finding台账.md 的 CHG-xxx 编号例句改写为 case 名口径——超出"原文迁移"字面,但系期1 已定契约(case 名即主键)的必然同步,不改会自相矛盾。
3. 无其他偏差。

## 蓝图验证备忘

不适用(标准级)。

## Gate 决策结果

- 2026-09-02 用户裁决(会话):"不用管 plbk,那个本来就是我自己的仓库"——vendor 约束废除;"我想整理的是这个(页面布局)"——期2 方向确认。
