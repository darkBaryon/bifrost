---
title: 身份模块整理 期1 收敛评审 #2
type: 收敛评审
case: 身份模块整理
phase: 1
round: 2
verdict: 有Blocking
created: 2026-09-13
---

> 评审者与实施上下文隔离（独立会话，未读旧收敛评审以外的评审意见；上一轮记录与代码评审 1 的「转收敛评审」栏按差量复核规则读取）。差量复核：上一轮候选 `cde781a93acca87a8dbf6f5b70329f659a845ece`，本轮候选 `e9075c51b118e08e2309bf588cb1d4273ee4b9e8`（`git status --porcelain=v1 --untracked-files=all` 为空，工作树干净，无未跟踪文件）。L3：逐维留痕。

## 结论

这一轮是纯修复轮，做得干净：上一轮六条修复项 R1–R6 全部关闭且每条都修在点上，三条取舍项 S1（撤回并挂账 FIND-021）/S2（注释版）/S3（采纳）各有落点，没有一条是改个字了事——R5 抽出 `transaction.sweepExpired` 后，`ee/` 内「到期」这条规则从两份实现收敛成一份，边界 `<=` 与 `core.go:139` 的 `!ExpiresAt.After(at)`、`store.go:207` 的 `expires_at > ?` 三处同口径，`git grep expires_at -- ee` 已无第四种写法；R3 把播种改成直接落行，让登录/签票真正成为唯一触发者，用例从「数行数」升级为「未到期行整行留底」；S3 去掉 `page` 恒等于入参的第二返回值后，两条分页线读起来终于一致。依赖方向、模块职责、事务边界本轮一处未动，`identity` 根包仍只依赖标准库，清理仍在既有 state 写锁事务内，对外契约零变化（`<=` 的可观察差异只在 `expires_at` 恰等于清理时刻的行，且 `verify` 与 `ConsumeTicket` 早已把它判为过期）。

挡在交付前的只有一条，且来自代码评审的转交而非差量文件本身：`identity/http/README.md` 改短版时，把基线里指向 `TestIdentityRouteContract` 的那一句整段删掉了，而该测试在另一个包，本包的 `go test` 跑不到它——这是一处文档回归，一行可修。代码评审转交的另外四条，三条有依据撤回（两条是方案 §4.1/§6 明文规定的形态，一条已挂账 FIND-021），一条转为建议取舍项 S4。修完 R7 即可转无碍。

## 事实来源

**跑过的命令**（全部只读，无写操作）：`git status --porcelain=v1 --untracked-files=all`、`git rev-parse HEAD`、`git diff --stat cde781a93 e9075c51b`、`git diff --stat 7284ad939 e9075c51b`、`git diff cde781a93 e9075c51b -- ee/internal/identity`、`git diff cde781a93 e9075c51b -- product`、`git diff cde781a93 e9075c51b -- workbench/cases/身份模块整理/期1/方案v4.md`、`git show e9075c51b --stat`、`git show e9075c51b:product/架构/EE后端架构.md`、`git show 7284ad939:ee/internal/identity/http/README.md`、`git grep -n 端点 -- ee/internal/identity`、`git grep -n "page(" -- ee/internal`、`git grep -rn internal/bifrost -- ee product workbench/规范`、`git grep -n TestIdentityRouteContract`、`git grep -n darkBaryon/bifrost/ee/internal -- ee/internal/identity ee/internal/host ee/internal/app`、`git grep -n "time.Now()" -- ee/internal`、`git grep -n expires_at -- ee`。本轮逐 hunk 差量取得完整（上一轮受工具限制未能取得，本轮无此缺口）。

**范围核对**：`git diff --stat cde781a93..e9075c51b` = 20 文件 / +556 / −71，其中 `ee/` 与 `product/` 下恰为提示词列出的 9 个文件（`ee/internal/identity/{accounts.go, core.go, model.go, passwords.go}`、`ee/internal/identity/http/protocol.go`、`ee/internal/identity/persistence/{cleanup_test.go, store.go}`、`product/上游改动/README.md`、`product/架构/EE后端架构.md`），其余 11 个为 workbench 过程文档与生成视图，按提示词不作为被评对象。工作树干净，无需追加未跟踪文件。

**完整读过的范围内文件现状**（绝对路径省略前缀 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/`）：`ee/internal/identity/{core.go(236 行全文), accounts.go(159 行全文), passwords.go(203 行全文), model.go(294 行全文)}`、`ee/internal/identity/http/protocol.go:1`、`ee/internal/identity/persistence/{store.go(323 行全文), cleanup_test.go(85 行全文)}`、`product/上游改动/README.md(124 行全文)`、`product/架构/EE后端架构.md(全文)`。

**范围外但为判断而读的既有实现与输入**：`ee/internal/identity/persistence/rows.go`（核 `sessionRow`/`ticketRow` 字段与 `expires_at` 列名）、`ee/internal/identity/persistence/service_test.go:1-120,335-374`（核 `fixture`/`services` 夹具与 `TestExpiredCredentialsAndDatabaseFailure` 是否受 `<=` 影响）、`ee/internal/identity/http/dto.go:30-160`（核 `Items` 非 nil 保证在 HTTP 层是否被依赖）、`ee/internal/host/auth_test.go:337`（核 `TestIdentityRouteContract` 位置）、六份包 README（`identity`、`identity/http`、`identity/persistence`、`host` 全文）、`ee/docs/账号认证接口.md:23`、`ee/scripts/branding-smoke.py:87`。**裁决输入**：`workbench/findings.md` 全文（FIND-017~021）、`workbench/cases/身份模块整理/期1/方案v4.md` §4.6、§5、§6、§9「Gate 决策结果」、「实施偏差记录」1–8、「评审处置」表。**上一轮记录**：`workbench/cases/身份模块整理/期1/收敛评审1.md` 全文（差量复核规则要求）。**规则**：`AGENTS.md`、`workbench/AGENTS.md`、`workbench/规范/流程.md`、`规范/流程/收敛评审.md`、`规范/流程/收敛评审审查方向.md`、`规范/项目/编码规范.md`、`product/架构/EE后端架构.md`、`workbench/templates/收敛评审.md`。

**关键假设的证实**：「`<=` 与业务判定同口径」不是据方案推断，是读 `core.go:139`、`store.go:207` 原文比对；「`ee/` 内再无第二种到期写法」是 `git grep expires_at -- ee` 全量结果（9 处，逐条归类）；「`Items` 恒非 nil 的保证是真的而非装饰」是读两个生产者（`accounts.go:141,157`、`passwords.go:167,201`）与唯一消费路径（`dto.go:96-102,145-151`）确认；「基线 README 原本指向路由契约测试」是 `git show 7284ad939:ee/internal/identity/http/README.md` 原文第 12 行确认，不依赖任何历史评审记录。

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | **无**。本轮未新增任何数字；到期判定现三处同口径（`store.go:158` `expires_at <= ?`、`core.go:139` `!ExpiresAt.After(at)`、`store.go:207` `expires_at > ?` 为存活），`git grep expires_at -- ee` 的 9 处命中无第四种口径 |
| | 同一规则多份实现且互不注明 | **无**（R5 关闭）。两份 DELETE 合并为 `store.go:157` `sweepExpired`，`InsertSession:164` 与 `InsertTicket:198` 各调一次，规则只剩一处 |
| | 重造已有 helper | **无**。`sweepExpired` 为新抽的包内私有方法，与 `notFound:30`/`conflict:41`/`beforeCursor:290` 职责不重叠；`cleanup_test.go:37,44` 两个查询闭包见有理由保留 P2 |
| | 派生常量无推导 | **不适用**。本轮未新增或修改任何常量（`model.go` 仅改一行注释） |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | **无**。`sweepExpired` 不含阈值；`cleanup_test.go` 的 `-time.Hour`/`+time.Minute` 是用例数据而非业务限制 |
| | 状态码、错误码裸数字 | **不适用**。本轮无 HTTP 层代码改动，`protocol.go` 只改文件注释，`:107-125` 的七错七码映射未动 |
| | 无名的错误形状（以元组表示失败） | **无**（S3 关闭）。`core.go:215` `page` 现返回 `(Cursor, error)`，不再有恒等回传入参的第二返回值 |
| | 紧凑但绕的控制流 | **无**。`sweepExpired` 一行；两条分页线 `accounts.go:145-158`、`passwords.go:189-202` 线性可读；`cleanup_test.go` 的两个闭包只做查询 |
| | 文件集中或拆分是否妨碍理解 | **无**。`store.go` 323 行（净增 6，仍在 300–500 评估区间，见有理由保留 P3）；`sweepExpired` 与两个调用点相邻（`:157-203`），读一次清理不跨文件；`cleanup_test.go` 85 行独立文件 |
| | 多余状态或守卫 | **无**（R4 关闭）。两条分页线的 `out.Items` 初始化不再被覆盖，`accounts.go:157` 与 `passwords.go:201` 同为 `append` |
| | 同一效果做两次（响应头、注册、初始化） | **无**。清理只在 `sweepExpired` 一处；`t.stage` 由两个调用点各设一次且值不同（`session.sweep`/`ticket.sweep`），不是重复 |
| | 注释与现状矛盾 | **发现 R7**（`ee/internal/identity/http/README.md:25`，代码评审转交项 4，非差量文件但经转交入范围）。差量文件内：R2/R3/R5 的三处矛盾注释均已改对，逐条复核见「上一轮修复项处置」；`store.go:155-156`、`:161`、`:195` 与 `cleanup_test.go:1,16,66` 的说法与代码逐句对得上 |
| 架构与项目一致性 | 接缝形状与同类不同 | **无**。本轮未动任何跨包接缝：`sweepExpired` 是 `transaction` 的未导出方法，未进 `identity.Tx`（`model.go:230-247` 方法集与上一轮一致）；`page` 是 `identity` 包内未导出函数 |
| | **架构符合性** | **无**（代码侧与文档侧均符合）。逐章见下 |
| | **依赖关系** | **无**。全量 import 图见下，本轮反而少一条测试边 |
| | 绕过既有出口 | **无**。清理仍在 `Store.transaction`（`store.go:225-256`）的 state 写锁事务内执行，未新开连接、未加索引、未动 `ee_identity_v1` 迁移 |
| | 把上游现状当契约 | **不适用**。本轮无任何上游接触点改动（`host/`、`transports/` 零变更） |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **无**。`page` / `Items` / `sweepExpired` 全为内部形状；`ee/docs/账号认证接口.md:23` 的 `expires_at` 指登录响应字段，与表列无关；DTO 键集合未动 |
| | 状态码口径不一 | **不适用**。本轮无错误映射改动 |
| | 省略、空、值三态语义不对应 | **无**（R4 关闭）。`model.go:191` 写明 `Items` 恒非 nil（空页为空切片），两个生产者都维持；`dto.go:97,146` 另建 DTO 切片（`make(...,0,len)`），不依赖该保证但与之不冲突 |

**架构符合性逐章核对**（对照 `product/架构/EE后端架构.md`）：

- **§3 总体布局 / §8 适用与维护**：R1 已回填，`:46` 现为 `│   └── host/              宿主与上游能力集成`，与 `:53`、`:113` 及实际包名 `ee/internal/host` 三处一致；`git grep -rn internal/bifrost -- ee product workbench/规范` 命中 0。本轮未新增或移动任何包。
- **§4 模块内部的职责**：`sweepExpired`（`store.go:157`）是表级删除，落在 `persistence/`；`page`（`core.go:215`）是分页参数的业务校验（limit 范围、游标解码），落在模块根包。职责归位正确，无「Handler 承载业务」或「存储承载规则」的越界。
- **§5 依赖与协作 / 事务**：清理与插入在同一 `t.db` 上执行（`store.go:164,169`；`:198,202`），事务范围仍由业务服务经 `Repository.Transaction` 决定（`session.go` 调用侧未改），`*gorm.DB` 未进业务层。业务根包 `core.go:4-15` 的 import 仍是纯标准库。
- **§6 插件与普通业务**：本期无插件入口，**不适用**。
- **§7 与 Bifrost 的协作边界**：`host/` 本轮零改动；「生命周期统一协调」不受影响（`store.go:19` 的「连接由宿主持有并关闭」仍成立，清理不涉及连接）。

**依赖关系逐项核对**（`git grep -n darkBaryon/bifrost/ee/internal -- ee/internal/{identity,host,app}` 全量结果）：

- `identity` 根包：`core.go`/`accounts.go`/`passwords.go`/`model.go` 在全量 import 图中一次都未出现，即不 import 任何 ee 包。无反向依赖。
- `identity/http → identity`、`identity/persistence → identity`、`identity/hasher → identity`；三个适配子包互不依赖。
- `host → identity + identity/http`（生产代码不依赖 `persistence`/`hasher`）；`app → host, identity, identity/hasher, identity/http, identity/persistence`。全图单向无环，与方案 §4.7 终态一致。
- **本轮唯一的依赖图变化**：`persistence/cleanup_test.go` 删去了 `identity` 的 import（播种改用同包 `sessionRow`/`ticketRow`），测试边减少一条。方向收窄，不是新增耦合。
- 使用方窄接口、显式注入、共享资源所有权：本轮零改动，沿用上一轮已核对的形状。

### 开放式复读

已做开放式复读（放下起手表，按「半年后要改这里的人」把 9 个文件各再读一遍）。**表外发现：无。** 复读中被我停下来掂量过、最终判定不构成问题的三处（`page` 在 S3 后名实关系、`sweepExpired(model any)` 的无类型参数、`cleanup_test.go` 两个查询闭包的近似重复），理由写在「看似可改、但有理由保留」P1–P3，供下一轮不再重提。本轮唯一的修复项 R7 来自代码评审的转交条目，我按本侧规则独立取证后确认，不是开放式复读的产物——如实记录来源。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无。上一轮 R5 的两份到期清理已收敛为一份，`ee/` 内到期口径唯一 | `ee/internal/identity/persistence/store.go:157-159,164,198`；`git grep expires_at -- ee` 全量 9 处逐条归类 |
| 职责与实现质量 | 差量文件内无。**R7**（转交项）：`http/README.md` 的文件表让读者以为本包测试覆盖路由契约，实际测试在 `host` 包 | `ee/internal/identity/http/README.md:25` vs `ee/internal/host/auth_test.go:337`；基线版 README 原有指向（`git show 7284ad939:ee/internal/identity/http/README.md` 第 12 行） |
| 架构与项目一致性 | 无。模块边界、职责、事务边界、依赖方向本轮零变化；架构文档布局图已回填 | `product/架构/EE后端架构.md:46,53,113` 三处一致；import 图见上 |
| 契约一致性 | 无。对外行为、DTO 键集合、状态码映射、迁移 ID 均未触及；`<=` 的可观察差异仅在边界瞬时且已被业务判定覆盖 | `core.go:139`、`store.go:207`、`ee/docs/账号认证接口.md:23`；偏差 7 已登记并转 T3 定向复核 |

## 出口清单

### 上一轮修复项逐条处置（差量复核必答）

| 项 | 处置 | 证据 |
|---|---|---|
| **R1** 架构文档布局图仍写旧包名 | **已修复** | `product/架构/EE后端架构.md:46` 现为 `│   └── host/              宿主与上游能力集成`，缩进与同块 `app/`/`identity/` 各行对齐；与 `:53`「已落在 …及`internal/host/`」、`:113`「`internal/host/` 负责宿主…」三处一致。`git grep -rn internal/bifrost -- ee product workbench/规范` 命中 0 |
| **R2** `protocol.go` 文件注释指向已拆走的端点位置 | **已修复** | `ee/internal/identity/http/protocol.go:1` 结尾改为「端点按线见 session.go、accounts.go、passwords.go」，与同包 `handler.go:2` 的写法逐字一致（`git grep -n 端点 -- ee/internal/identity` 全量比对，包内无第三种说法） |
| **R3** 清理用例注释与控制流不符 | **已修复**（采纳最小修正 (b)） | `cleanup_test.go:17-36` 播种改为 `db.Create(&r)` 直接落行，绕开 `Tx.InsertSession`/`InsertTicket` 自带的清理；`:16` 新增一句说明这就是绕开的目的；`:66` 原注释「一次登录写入新会话，触发会话清理；一次签票触发票据清理」现在成立——`:56-64` 的触发前断言证实四条到期行仍在库，`:67,70` 的 `Login`/`IssueTicket` 是唯一触发者。用例同时从「数行数」升级为「整行比较」（`:79,82` 的 `reflect.DeepEqual`），并补齐「已消费票据」两条（`:29,31`），覆盖了 §4.6 口径里「含已撤销/已消费」这一半 |
| **R4** 两条分页线 `Items` 写法不一且初始化被覆盖 | **已修复** | `passwords.go:201` 由 `out.Items = rows` 改为 `out.Items = append(out.Items, rows...)`，与 `accounts.go:157` 同形；`model.go:191` 注释补「`Items` 恒非 nil（空页为空切片）」。核实该保证是真的：两个生产者是 `AccountPage`/`EventPage` 在 `ee/` 内的全部构造点（`accounts.go:141`、`passwords.go:167` 各一处初始化，其后只 `append`），错误路径同样返回已初始化的 `out`；HTTP 层 `dto.go:97,146` 另建 DTO 切片，不依赖也不抵触该保证 |
| **R5** 到期清理两份实现、边界与业务判定差一个等号 | **已修复** | `store.go:157-159` 抽出 `func (t *transaction) sweepExpired(model any) error`，`InsertSession:164` 与 `InsertTicket:198` 各调一次；边界由 `<` 改为 `<=`。三处口径现已对齐：`sweepExpired` 的 `expires_at <= now`、`core.go:139` 的 `!v.ExpiresAt.After(at)`（即 `<= at` 为过期）、`store.go:207` 的 `expires_at > ?` 为存活。`git grep expires_at -- ee` 确认无残留的 `<` 写法。`:155-156` 的方法注释交代了 FIND-020 来源、含已撤销/已消费、口径对齐理由与无索引全表扫描，`:161`、`:195` 两处调用点注释相应改短且不与之矛盾。边界变化已按纪律登记为实施偏差 7 并转 T3 定向复核 |
| **R6** 上游改动登记列了不存在的根 `docs-zh/` | **已修复** | `product/上游改动/README.md:5` 现为「`ee/`、`product/`、`workbench/` 是我们自己的目录」，`docs-zh/` 已删。同文件其余抽查仍成立：`:41` 的 `ui/app/globals.css` 一行 `@source`、`:102-108` 的 7 个新增文件路径未受本轮影响 |
| **S1** 清理时钟由存储层自取 | **有依据撤回**（按反证条件） | 反证条件成立：改 `Tx.InsertSession`/`InsertTicket` 签名超出方案 §6「公共接口白名单」（`方案v4.md:249-257` 逐项列举，两者不在其中）。已按四要件登记 `workbench/findings.md:194-199` FIND-021（`status: 观察中`、`triggered_by: [身份模块整理]`、evidence 带路径、`convert_when` 可判定）。止损已达成：`git grep "time.Now()" -- ee/internal` 显示 `persistence` 生产代码的时钟读取由两处收敛为一处（`store.go:158`），`identity` 侧仍只有 `core.go:81` |
| **S2** `State`/`RequireAccountManager` 提升到三条线无惯例 | **采纳（注释版）** | `core.go:195`「…经嵌入提升到三条线，外部从任意一条调用等价」、`core.go:201`「…同 State，三条线上等价」。`http/session.go` 的 `svc.Session.State` 按方案 §4.2 的会话线端点形态保留，处置理由写在方案「评审处置」表，属反证条件中「RBAC 期再统一」的一支 |
| **S3** `page` 第二返回值恒等于入参 | **采纳** | `core.go:215` 签名改为 `page(cursor string, limit int) (Cursor, error)`，四处 `return` 相应去掉 `0`；`:214` 注释补「不改写 limit」。两处调用点 `accounts.go:145,149,153-155` 与 `passwords.go:189,193,197-199` 直接用 `limit`，`git grep "page(" -- ee/internal` 确认无第三个调用点遗漏 |

### 代码评审 1「转收敛评审」栏逐条处置

| 转交条目 | 处置 | 依据 |
|---|---|---|
| `store.go:158,192` 清理用 `time.Now().UTC()`，与同包由业务层传 `at` 的方法时钟来源不一致 | **挂账（不重复处理）** | 与上一轮 S1 同一问题。R5 合并后现仅 `store.go:158` 一处；已登记 `findings.md:194-199` FIND-021，四要件齐全，`convert_when` 明确（下次需要可注入时钟，或 `Tx` 因其他原因改签名时一并收口）。按「不重提已裁决问题」不再出意见 |
| `core.State`/`core.RequireAccountManager` 经嵌入提升到三条线，`AccountService`/`PasswordService` 也暴露 `State` | **有依据撤回** | 方案 §4.1 的预期形态（结构方案 B「同包三类型 + core」由用户在方案评审 2 后裁决），转交栏本身也标「仅记录」。上一轮 S2 已按注释版关闭，`core.go:195,201` 现已写明三条线等价。核实三条线内部确实都用到 `State`（`accounts.go:32`、`passwords.go:175`、`core.go:207`），属共享判定，不宜下沉到单条线 |
| 测试内组合类型 `services`（`service_test.go:60-71`）与 `identity.Services` 同形 | **有依据撤回** | 方案 §6（`方案v4.md:232-237`）逐行规定了该夹具的代码形状与两个转发方法，目的是让 61+2+2 处既有 `s.X(` 调用与全部断言零改动，从而让本次拆分保持可机械核对。实读 `service_test.go:60-71` 与方案示例一字不差。它是表征测试的手段，不是第二套业务规则——业务判定仍只在 `identity` 包内一份 |
| `http/README.md` 短版不再指向 `host/auth_test.go` 的 `TestIdentityRouteContract` | **修复（本轮修复项 R7）** | 独立取证后确认成立，见下 |
| `persistence/README.md` 的「正常应在两位数以内」是运维口径，无来源引用 | **建议取舍项 S4** | 有反证条件，交实施者决定，见下 |

### 本轮修复项

> 一条，在授权范围内、无需用户裁决、不触及契约。本轮未关闭，故 verdict 为有Blocking；修复后定向复核即可转无碍。

**R7 · `identity/http` 的包 README 不再指向钉住其路由表的契约测试，而该测试在另一个包**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/README.md:25`（关联同文件 `:7`、`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/handler.go:52`、`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/host/auth_test.go:337`）
- **代码事实**：README 的文件表只写「`handler_test.go` / `dto_test.go` — 错误映射与 JSON 限制契约；账号响应键集合」，这与两个文件的实际内容相符。但钉住路由归属的测试是 `TestIdentityRouteContract`，位于 `ee/internal/host/auth_test.go:337`，属另一个包。`git grep -n TestIdentityRouteContract` 显示 `ee/` 下只命中该测试函数本身，无任何包 README 指向它——`host/README.md:23` 仅把 `auth_test.go` 描述为「真实路由链上的鉴权边界测试」，不提路由表归属。基线版本的同一份 README 曾有这句指向：`git show 7284ad939:ee/internal/identity/http/README.md` 第 12 行为「精确路由归属、所有 POST 的来源拒绝及匿名/会话边界由 `../../bifrost/auth_test.go` 的 `TestIdentityRouteContract` 验证」，本期步骤 8 改短版时随整段删除。
- **维护影响**：同一份 README 的 `:7` 把 `handler.go` 的路由表声明为「唯一来源」，`handler.go:52` 也这么写；改这张表的人自然在本包内找回归测试，而 `go test ./ee/internal/identity/http/...` 跑不到 `TestIdentityRouteContract`（它在 `host` 包），README 里也没有线索。代价具体：改错一条路由的匿名标记或 method，本包测试全绿，要等 `host` 包的测试或冒烟才暴露；而编码规范对包 README 的要求正是「方便新人找到要看的代码」。这句指向在基线里存在、本次重写把它删掉了，属回归而非既有欠账。
- **最小修正**：在 `http/README.md` 的「做什么」第一条句尾或文件表 `handler.go` 行补一句，例如「路由归属与来源拒绝的契约测试在 [`../../host/auth_test.go`](../../host/auth_test.go) 的 `TestIdentityRouteContract`，改路由表要一并跑」。一句话，不动短版结构。

### 建议取舍项（附反证条件）

**S4 · `persistence/README.md` 的行数期望值是与部署配置相关的估算，但写成了无条件口径**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/README.md:13`
- **代码事实**：该行为「运维自查行数：`SELECT COUNT(*) FROM ee_identity_sessions; SELECT COUNT(*) FROM ee_identity_ws_tickets;`——正常应在两位数以内，超过万行再评估加索引」。会话表的行数上界由 `SessionTTL` 窗口内的登录次数决定（`store.go:157` 的清理把它封顶），而 `SessionTTL` 是部署可配的：`identity/model.go:74-76` 允许 `MinSessionTTL = time.Hour` 到 `MaxSessionTTL = 7 * 24 * time.Hour`，默认 24 小时。README 未写这个推导，也未标明「两位数」是按默认 TTL 的内部控制台估算还是实测。
- **维护影响**：运维照这一句判断异常时，把 TTL 配到 7 天或账号数增长后出现的三四位数是正常值，却会被当成故障排查；反向上，读者无法按自己的部署重算期望值，只能把这个数当常量记住。改动成本是一句话。
- **最小修正**：把口径补成推导，例如「会话行数 ≈ `SessionTTL` 窗口内的登录次数（默认 24 小时的内部控制台为两位数），票据行数 ≈ 30 秒内的 WS 建连次数；超过万行再评估加索引」。
- **反证条件**：若认为这句的作用只是给出「万行」这个可执行阈值、「两位数」仅为量级提示，且本产品的部署 TTL 实际固定在默认值不做调整，则保持现状不改——此时应反过来把可变的那一半（`SessionTTL` 可配）从读者的心智负担里移除，而不是维持一个半精确的数字。

### 需用户裁决的 Blocking

- 无。R7 与 S4 均在授权范围内，不触及范围、契约与风险取舍。

### 立债(REF)

- 无。

### 挂账(Finding)

- 无新增。上一轮 S1 已由实施者登记为 `findings.md:194-199` FIND-021，本轮核对四要件齐全、evidence 与代码现状一致（`store.go` 的 `sweepExpired` 自取 `time.Now().UTC()`，同文件 `ReserveLogin` 与 `Tx` 的 `RevokeSession`/`ConsumeTicket` 由业务层传 `at`），不重复登记。

### 转代码评审（范围外发现，一行一条，不出结论）

- T4：`ee/internal/identity/persistence/cleanup_test.go:79,82` 用 `reflect.DeepEqual` 比较含 `time.Time` 与 `*time.Time` 字段的行结构，请核在 SQLite 与 `IDENTITY_TEST_POSTGRES_DSN` 两条驱动路径下时间值的往返表示是否稳定到可用 `DeepEqual` 断言。
- T5：`ee/internal/identity/persistence/cleanup_test.go:30,31` 的「未到期票据」余量为 1 分钟（`now.Add(time.Minute)`），而播种到 `:70` `IssueTicket` 之间隔着一次含 bcrypt 的 `Login`，请核该余量在慢速 CI 上是否足够。
- T6：上一轮 T1（用例证明力）随 R3 的播种改写而变化，`cleanup_test.go:56-64` 现已断言触发前状态，请在定向复核时按新形态重核，上一轮 T2（全表 DELETE 与 `state` 写锁叠加的容量）与 T3（`<=` 的可观察差异）按原文仍待复核。

## 台账对账

- **FIND-017**（busy 重试两份）：本轮未触及 `branding/persistence/migration.go` 与 `identity/persistence/diagnostics.go`，未恶化，继续观察。
- **FIND-018**（`ReserveLogin` 抢 `state` 写锁）：未关闭。本轮把清理边界由 `<` 放宽到 `<=`，删除行数最多多一行，写锁持有时间的变化可忽略；容量与并发判断属代码评审范围，按 T6 沿用上一轮 T2 的转交，本评审不下结论。
- **FIND-019**（哈希槽耗尽与登录限流共用 `ErrLimited`）：已接受/豁免，本轮未触及 `protocol.go:107-125` 的映射，不重提。
- **FIND-020**（会话/票据无删除路径）：已转 Case 并在本期实现。本轮复核实现口径仍与方案 §4.6 拍板一致——只按 `expires_at` 过滤、不看 `revoked_at`/`consumed_at`（`store.go:158`），未加索引（`migration.go` 未在差量内，索引集合与基线一致），迁移 ID 未动。台账 `findings.md:190` 已注明「随期 1 交付关账」，R5 关闭后无遗留条件，可在 Gate 2 一并关账。
- **FIND-021**（清理时钟由存储层自取）：本轮由实施者按上一轮 S1 的四要件登记（`findings.md:194-199`），本轮复核 evidence 与代码现状一致，未恶化，继续观察。

## 看似可改、但有理由保留（写明理由，下一轮不再重提）

- **P1 · `page` 在 S3 之后名实关系变松**（`core.go:215`）：函数名是 `page`，但现在只返回 `Cursor`，`limit` 纯粹进去被校验、不带出来；调用方 `accounts.go:149` 的 `limit+1` 依赖「`page` 已保证 `limit` 在 1..`MaxPageSize`」这一不在签名里的约定。改名（如 `parsePageParams`）是纯 churn，`:214` 的注释已把两件事都说明（「校验分页参数并解码游标…不改写 limit」），且上一轮 S3 的反证条件已明确记录了「若日后要把 `pageLimit` 的默认值兜底下沉进来，第二返回值才该回来」这条反向路径。保留。
- **P2 · `cleanup_test.go:37-50` 两个查询闭包近似重复**：`sessions` 与 `tickets` 结构相同，只差行类型与列名（`token_hash` / `hash`）。合成一个泛型 helper 要把 `t`、`db`、列名都传进去，节省约 6 行却让每个调用点多一个参数；而两个闭包各自的列名恰是「会话按 token 摘要、票据按 hash 主键」这条领域差异的体现。四维要求测试代码按组织与重复看，此处重复量小且各自表达了不同的表事实。保留。
- **P3 · `store.go` 323 行，超过「300 行须评估」线**：本轮净增 6 行（`sweepExpired` 及其注释）。评估结果仍是不拆——读取、事务、限流、分页共用 `notFound`/`conflict`/`beforeCursor`/`authRecord`，行结构已在 `rows.go`，`sweepExpired` 与它的两个调用点相隔不到 50 行，按「读/写」再切会让一次事务的完整路径跨两个文件。距 500 行硬线仍有余量。保留。
- **P4 · `sweepExpired(model any)` 的参数无类型约束**（`store.go:157`）：传入一个没有 `expires_at` 列的模型会在运行时报 SQL 错误而非编译期拦下。GORM 的 `Delete` 签名本身就是 `interface{}`，改成泛型需要为两个行类型引入一个只为此存在的类型约束，收益不抵复杂度；包内仅两个调用点且相邻可见。保留。
- **P5 · `t.stage` 由调用点设置而非 `sweepExpired` 内部设置**（`store.go:163,197`）：两处 stage 值不同（`session.sweep` / `ticket.sweep`），移进方法里就要再传一个字符串参数，等于把同样的信息换个位置写。诊断日志的阶段定位不受影响。保留。
- **P6 · `model.go:191` 的文档注释同时描述 `AccountPage` 与 `EventPage`，后者（`:197`）因此没有自己的 godoc**：这是基线既有形状，本轮只在该句里补了 `Items` 的非 nil 语义，未改变结构；两个类型形状相同、注释显式点名两者，分开写反而制造重复。保留。

## 实际阅读范围与未执行的检查

**读了什么**：见「事实来源」三段清单——9 个差量文件全部读完整现状（不只读 diff），另读了 `rows.go`、`service_test.go` 的夹具段与受影响用例、`dto.go` 的分页转换段、`host/auth_test.go:337`、四份包 README、`ee/docs/账号认证接口.md:23`、`branding-smoke.py:87`。本轮逐 hunk 差量完整取得（`git diff cde781a93 e9075c51b -- ee/internal/identity` 与 `-- product` 两条命令的全文），新旧对照不依赖推断。**同类实现对照**：同包既有时间条件（`RevokeSession`/`RevokeSessions`/`ConsumeTicket`/`ReserveLogin`）作为 `sweepExpired` 时钟与边界的对照；`accounts.go` 与 `passwords.go` 两条分页线互为对照；基线版 `http/README.md` 作为 R7 的对照。**关键假设的证实**：三处到期口径一致是读原文比对不是据方案推断；`Items` 非 nil 保证的真实性是读全部生产者与唯一消费路径确认；R7 的「基线曾有指向」是 `git show` 原文确认，不引用任何历史评审记录作为依据。

**未执行的检查**（无一条被当成通过）：

1. **没有编译、`go vet`、`go test`、`gofmt -l`、预飞**（只读评审，按提示词不运行会写文件的测试或构建）。`<=` 边界变化、`reflect.DeepEqual` 断言、`page` 签名变化是否真的通过测试，本轮未验证；测试是否可信属代码评审范围，已按 T4/T5/T6 转交。
2. **未读 `migration.go` 正文**。FIND-020「不加索引」本轮的核对依据是该文件不在差量清单内（`git diff --stat cde781a93..e9075c51b` 无此文件），因此索引集合必然与上一轮相同；上一轮已读原文确认无 `expires_at` 索引。本轮未重新打开该文件。
3. **未读 `ee/internal/identity/{session.go, hasher/bcrypt.go}` 与 `ee/internal/identity/http/{handler.go, session.go, accounts.go, passwords.go}` 正文**（均不在差量内，且本轮改动不触及其行为）。`handler.go:2` 与 `:52` 仅经 `git grep` 核对与 R2/R7 相关的两句表述。
4. **未跑 `dupl`/`deadcode` 等机械工具**；重复判断来自逐文件阅读与定向 grep。
5. **`product/上游改动/README.md` 的「7 个修改文件、增删行数」未与上游实际差量核对**（需 `git diff $(git merge-base develop upstream/dev) develop`，超出只读评审范围且本轮该文件只改了第 5 行的一处措辞）。本轮只核实了 R6 那一行本身。
6. **workbench 侧的 11 个差量文件（`findings.md`、`checklist.yaml`、`方案v4.md`、两份评审记录、预飞事实表、生成视图）不作为被评对象**，按提示词只读取其中的裁决输入（Gate 决策结果、实施偏差记录、评审处置表、findings 台账）。`checklist.yaml` 必含符号是否随 `sweepExpired`/`expires_at <= ?` 改对，本轮未核。
7. **按规则未读**：旧代码评审与旧收敛评审的意见正文（差量复核例外：已读 `收敛评审1.md` 全文以逐条处置其修复项，已按提示词原样接收代码评审 1 的「转收敛评审」栏五条）。`git grep TestIdentityRouteContract` 的输出中偶然命中了账号认证期次的历史评审文档，未据其内容形成任何判断，R7 的证据链独立于它们。
