---
title: 身份模块整理 期1 收敛评审 #3
type: 收敛评审
case: 身份模块整理
phase: 1
round: 3
verdict: 无碍
created: 2026-09-13
---

> 评审者与实施上下文隔离（独立会话；按差量复核规则读取上一轮记录 `收敛评审2.md` 与代码评审 1 的「转收敛评审」栏原文，未读其他评审意见）。差量复核：上一轮候选 `e9075c51b118e08e2309bf588cb1d4273ee4b9e8`，本轮候选 `beb05c0c1cc4c27eab45ad9da2d14ea035bf0cd4`（`git status` 工作树干净，`git status --porcelain --untracked-files=all` 为空，无未跟踪文件）。L3：逐维留痕。

## 结论

本轮是三行文档/用例数据的收尾轮，方向正确、落点准确，可以交付。上一轮唯一的 Blocking 项 R7 已按最小修正关闭，且我独立核实了它补回的那句话的每个事实：链接 `../../host/auth_test.go` 从 `ee/internal/identity/http/` 解析得到 `ee/internal/host/auth_test.go`（文件存在，`TestIdentityRouteContract` 在 `:337`）；该测试断言的三件事与 README 的措辞一一对应——`:362` 的 `OwnsRoute` 双向方法归属、`:365-368` 对表中每条 POST 用 `https://evil.invalid` 断 403、`:369-372` 的匿名期望（200/401/409）；「所有 POST」也不是修辞，测试表的 15 条路径与 `handler.go:53-70` 的 15 条 POST 路由逐条相等，剩下的一条 GET 由 `:380-383` 单独覆盖。全仓只有这一处 README 指向该测试（`git grep TestIdentityRouteContract -- ee` 两处命中：测试本身与这句），没有造出第二份要维护的说法；链到另一个包的 `.go` 文件也有既有先例（`ee/ui/app/enterprise/lib/schemas/README.md:13` 链 `internal/branding/image.go`）。取舍项 S4 采纳得比我预期的更彻底：行数期望值从一个无条件的「两位数」改成了以 `SessionTTL`/`WSTicketTTL` 两个命名常量为源的推导，两个常量确实存在（`model.go:74-77`）、范围与 `core.go:29` 的校验及 `ee/docs/账号认证升级与恢复.md:11` 的「1–168」一致，票据那一半用常量名替代了原方案里手抄的「30 秒」——这正是编码规范「派生值要写推导」想要的形状。`cleanup_test.go` 顺手把未到期票据的余量从 1 分钟放宽到 1 小时（对代码评审 T5 的回应），未到期会话与票据的余量因此对称，用例的四态结构、播种绕开清理的手法、触发前后的整行比较都没动。

架构与依赖本轮零变化：差量不含任何生产代码，`cleanup_test.go` 的 import 仍只有 `context`/`reflect`/`testing`/`time`，两份 README 的文件表与 `git ls-files` 的实际文件一一对应，两处文档改动都落在架构 §8「实现细节维护在包 README」指定的位置。对外行为、DTO、状态码、迁移 ID 全未触及。

没有本轮修复项。留下三条建议取舍项，都在文档与用例措辞层面、都附反证条件、都不阻塞：S7 指出新补的那句「改路由表要一并跑」弱于实际需要（那张期望表是手维护的，加路由只跑不加用例会静默失去覆盖，而同一句话又对外宣称「所有 POST」）；S6 指出「1 小时到 7 天」把一个可配上界第二次抄进散文，且抄成了 grep 不到的单位；S5 指出放宽后的 1 小时余量没有留下「为什么是 1 小时」的线索。三条实施者都可以带理由撤回，撤回后无遗留。

## 事实来源

**跑过的命令**（全部只读）：`git status`、`git status --porcelain --untracked-files=all`、`git rev-parse HEAD`、`git diff --stat e9075c51b..beb05c0c1`、`git diff e9075c51b..beb05c0c1 -- ee/`、`git ls-files ee/internal/identity`、`git grep -n "WSTicketTTL\|SessionTTL" -- ee`、`git grep -n TestIdentityRouteContract -- ee`、`git grep -n "](\.\./\.\./" -- ee`、`git grep -n "_test.go" -- <五份包 README>`、`git grep -n "两位数\|万行\|ee_identity_ws_tickets" -- ee product workbench`、`git grep -n EE_SESSION_TTL_HOURS -- ee product workbench`、`git grep -n "168\|7 天\|七天" -- ee`、`git grep -n "24 小时\|24小时\|两位数\|个位数" -- ee`、`git grep -n "^## " -- product/架构/EE后端架构.md`、`git grep -n "Gate 决策结果\|实施偏差记录\|评审处置" -- workbench/cases/身份模块整理/期1/方案v4.md`。逐 hunk 差量完整取得。

**范围核对**：`git diff --stat e9075c51b..beb05c0c1` = 12 文件 / +347 / −10；其中 `ee/` 下恰为提示词列出的 3 个文件（`ee/internal/identity/http/README.md` +1−1、`ee/internal/identity/persistence/README.md` +1−1、`ee/internal/identity/persistence/cleanup_test.go` +2−2），其余 9 个为 workbench 过程文档与生成视图（`SUMMARY.md`、`assets/nav.js`、`views/all.md`、`cases/*/index.md`、`代码评审1.md`、`收敛评审2.md`、`方案v4.md`、`findings.md`、预飞 5 事实表），按提示词不作为被评对象。工作树干净、无未跟踪文件，故实际交付物 = `beb05c0c1`。

**完整读过的范围内文件现状**（绝对路径省略前缀 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/`）：`ee/internal/identity/http/README.md`（26 行全文）、`ee/internal/identity/persistence/README.md`（31 行全文）、`ee/internal/identity/persistence/cleanup_test.go`（85 行全文）。每个文件一句话职责：第一份是 `identity/http` 包的入口说明（路由表/JSON/Cookie/错误码五条 + 文件表）；第二份是 `identity/persistence` 包的入口说明（六表映射、state 写锁、清理、驱动差异 + 运维自查一句 + 文件表）；第三份是 FIND-020 的行为用例（四态播种 → 一次登录 + 一次签票 → 过期行清空、未到期行整行留底）。

**范围外但为判断而读的既有实现与输入**：`ee/internal/host/auth_test.go:320-388`（`TestIdentityRouteContract` 全文，核 README 的三项断言与「所有 POST」）、`ee/internal/identity/http/handler.go:1-80`（路由表 15 POST + 1 GET，核测试表是否覆盖全部 POST；`:52` 的「唯一来源」措辞）、`ee/internal/identity/persistence/store.go:140-219`（`sweepExpired` 及两个调用点、`ConsumeTicket`，核 README 第 10 行与文件表）、`ee/internal/identity/persistence/rows.go:60-104`（`ticketRow` 无 `CreatedAt`，核用例余量是否与行内字段自相矛盾）、`ee/internal/identity/persistence/service_test.go:55-103`（`services` 夹具与 `fixture` 的 Initialize/Login/Authenticate，核用例时钟起点与转交项 3）、`ee/internal/identity/model.go:74-77`（`DefaultSessionTTL`/`MinSessionTTL`/`MaxSessionTTL`/`WSTicketTTL`）、`ee/internal/identity/core.go:21,29`（`Options.SessionTTL` 与范围校验）、`ee/internal/app/identity.go:26,32-41`（`EE_SESSION_TTL_HOURS` 与范围注释）、`ee/internal/identity/README.md`、`ee/internal/host/README.md`（两份全文，核跨包指向是否互相矛盾）、`ee/docs/账号认证升级与恢复.md:11`、`ee/docs/账号认证接口.md:9`、`product/架构/EE后端架构.md` §3/§4/§5/§7/§8 全文。**裁决输入**：`workbench/findings.md:155-199`（FIND-017~021 全文）、`workbench/cases/身份模块整理/期1/方案v4.md` §6（`:227-257`）、§9「Gate 决策结果」（`:275-277`）、「实施记录」（`:279-300`）、「实施偏差记录」1–8（`:302-318`）、「评审处置」（`:320-340`）、观察项（`:271`）。**上一轮记录**：`workbench/cases/身份模块整理/期1/收敛评审2.md` 全文（差量复核规则要求）。**规则**：`AGENTS.md`、`workbench/AGENTS.md`、`workbench/规范/流程/收敛评审.md`、`规范/流程/收敛评审审查方向.md`、`规范/项目/编码规范.md`、`workbench/templates/收敛评审.md`。

**关键假设的证实**（结论不依赖「上游会/不会做 X」的推断）：

- 「README 指向的测试真的覆盖它宣称的三件事」→ 读 `host/auth_test.go:341-383` 原文，逐项对照 README 措辞，不据方案或历史记录。
- 「所有 POST 都被覆盖」→ 把测试表的 15 条路径与 `handler.go:54-68` 的 15 条 POST 路由逐条比对（`/api/identity/{status,initialize,login,logout,me,change-password,ws-ticket,password-events}`、`/api/accounts/{create,list,set-status,reset-password}`、`/api/session/{login,logout,ws-ticket}`），一一对应无遗漏；GET 那条由 `:380-383` 单独覆盖。
- 「那张期望表不会随路由表自动扩张」→ `host/auth_test.go:336` 的注释原文「路径/匿名状态取自批准的HTTP契约，**不依赖生产路由表构造预期**」，这是 S7 的依据，不是推测。
- 「README 引用的两个 TTL 常量存在且范围属实」→ `model.go:74-77` + `core.go:29` 的 `o.SessionTTL < MinSessionTTL || > MaxSessionTTL` 校验 + `app/identity.go:36-41` 的环境变量换算。
- 「`7 天` 是这个上界在仓库里的第二种写法」→ `git grep -n "168\|7 天\|七天" -- ee` 全量两处命中：`ee/docs/账号认证升级与恢复.md:11` 的「1–168整数」与本次新增的 README「1 小时到 7 天」，代码侧写作 `7 * 24 * time.Hour`。
- 「1 小时余量与票据行内字段不矛盾」→ `rows.go:93-98` 的 `ticketRow` 只有 `Hash/SessionID/ExpiresAt/ConsumedAt`，无 `CreatedAt`，故该行不像会话行（`cleanup_test.go:18` 的 `CreatedAt: now-25h` 与 `ExpiresAt: now-1h` 自洽于 24 小时 TTL）那样自带矛盾，只与 `WSTicketTTL = 30 * time.Second` 在领域层面不符。
- 「链接形式有先例」→ `git grep -n "](\.\./\.\./" -- ee` 全量结果中 `ee/ui/app/enterprise/lib/schemas/README.md:13` 已链向另一包的 `.go` 文件。

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | **发现（S6）**。`persistence/README.md:13` 新增的「1 小时到 7 天」是 `MaxSessionTTL`（`model.go:76` 的 `7 * 24 * time.Hour`）在仓库里的第二种写法，与 `ee/docs/账号认证升级与恢复.md:11` 的「1–168整数」单位不同；「默认 24 小时」是第四份副本（`model.go:74`、`ee/docs/账号认证接口.md:9`、`升级与恢复.md:11`、本行） |
| | 同一规则多份实现且互不注明 | **无**。`http/README.md:7` 是全仓唯一指向 `TestIdentityRouteContract` 的说明（`git grep` 仅两处命中：测试本身 + 该行）；`host/README.md:23` 只把 `auth_test.go` 描述为「真实路由链上的鉴权边界测试」，不重复路由归属的口径，两句不冲突 |
| | 重造已有 helper | **不适用**。差量未新增任何函数或 helper（`cleanup_test.go` 只改两个时间字面量） |
| | 派生常量无推导 | **发现（S6）**。向好的一半已做到：票据那一半用 `WSTicketTTL` 替代原方案 §4.6 手抄的「30 秒」；未做到的一半是范围仍手抄成「1 小时到 7 天」，而同仓 `app/identity.go:26` 对同一上界的既有写法恰是「范围见 identity.MinSessionTTL/MaxSessionTTL」 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | **发现（S5）**。`cleanup_test.go:30,31` 的未到期余量由 `time.Minute` 改为 `time.Hour`，是为覆盖中间一次含 bcrypt 的 `Login` 的墙钟耗时，但文件里没有任何一句交代这一点；它不是业务阈值，故不触硬规则 |
| | 状态码、错误码裸数字 | **不适用**。差量无 HTTP 代码；`http/README.md:10` 的「7 个业务错误映射 7 个状态码」本轮未动 |
| | 无名的错误形状（以元组表示失败） | **不适用**。差量无函数签名变化 |
| | 紧凑但绕的控制流（闭包、多层三元、一次调用做两件事） | **无**。`cleanup_test.go` 的两个查询闭包（`:37-50`）与播种循环结构未变，只换了两个字面量 |
| | 文件集中或拆分是否妨碍理解 | **无**。`cleanup_test.go` 仍 85 行、独立文件（方案 §6「独立文件，避免 service_test.go 越过 500 行」）；两份 README 为短版，`git ls-files ee/internal/identity` 的 8 个 http 文件与 8 个 persistence 文件与两张文件表逐条对应，无漏列、无幻列 |
| | 多余状态或守卫 | **无**。`:56-64` 的触发前断言是证明播种有效的必要前提，不是重复防护 |
| | 同一效果做两次（响应头、注册、初始化） | **无**。差量未新增注册或初始化路径 |
| | 注释与现状矛盾（尤其鉴权、边界、装配处） | **发现（S7）**。`http/README.md:7` 的「改路由表要一并跑」与 `host/auth_test.go:336`「不依赖生产路由表构造预期」不匹配：只跑不加用例时，同句宣称的「所有 POST」会静默变成「除新路由外的所有 POST」。逐句核对的其余部分一致：README `:10` 的清理描述对得上 `store.go:155-159,164,198`；`:24` 的「store.go 含过期清理」成立；`cleanup_test.go:1,16,66` 三句与控制流一致 |
| 架构与项目一致性 | 接缝形状与同类不同 | **不适用**。差量不含生产代码，无任何跨包接缝、构造函数或接口改动 |
| | **架构符合性** | **无**。逐章见下 |
| | **依赖关系** | **无**。逐项见下 |
| | 绕过既有出口（存储、迁移、响应、鉴权） | **不适用**。无代码路径改动；未新开连接、未加索引、未动 `ee_identity_v1` |
| | 把上游现状当契约 | **不适用**。`host/`、`transports/`、`ui/` 零变更；README 新增的跨包指向是 ee 内文件，不涉上游 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **无**。README 新引入的三个事实逐项对账：默认 24 小时 = `model.go:74` = `ee/docs/账号认证接口.md:9` = `升级与恢复.md:11`；范围 1 小时–7 天 = `model.go:75-76` = `core.go:29` 校验 = `升级与恢复.md:11` 的「1–168」；票据 TTL 以 `WSTicketTTL`（`model.go:77` = 30s）指代，`http/session.go:78` 的 `expires_in` 由同一常量推导。三方一致，重复副本的代价另记 S6 |
| | 状态码口径不一 | **不适用**。无错误映射改动；README `:7` 引用的 403/200/401/409 与 `auth_test.go:341-372` 原文一致 |
| | 省略、空、值三态语义不对应 | **不适用**。差量不含 DTO 或可空字段 |

**架构符合性逐章核对**（对照 `product/架构/EE后端架构.md`）：

- **§3 总体布局（`:34-53`）**：布局图 `:46` 为 `│   └── host/              宿主与上游能力集成`，`:53` 的「已落在 …及`internal/host/`」与实际包名一致（上一轮 R1 的关账状态本轮未退化）；本轮未新增或移动任何包与文件。
- **§4 模块内部的职责（`:55-72`，`:61-68` 职责表）**：两份被改 README 各自描述的正是表里 `http/`「解析请求、调用服务、映射 HTTP 响应」与 `persistence/`「存储、表映射、迁移与事务实现」；新增的运维自查一句描述的是表行数，落在 `persistence/`，不是业务规则外移。
- **§5 依赖与协作（`:74-99`）**：`cleanup_test.go` 属 `persistence` 包，经同包夹具持有 `identity` 服务、直接持 `*gorm.DB` 播种，符合「GORM 具体类型留在适配实现中」；业务根包未被反向导入。
- **§6 插件与普通业务（`:101-108`）**：**不适用**，本期无插件入口。
- **§7 与 Bifrost 的协作边界（`:111-117`）**：`:113`「`internal/host/` 负责宿主、管理路由、会话兼容等跨功能接入」——`http/README.md:7` 把路由契约测试指到 `host` 包，与这条归属一致，不是把身份 HTTP 的职责外移；共享连接的所有权（`persistence/README.md:3`「连接由宿主持有并关闭」）本轮未动。
- **§8 适用与维护（`:119-123`）**：`:121`「目录迁移本身不改变 API、表名、迁移 ID、认证方式或配置契约」——本轮零对外变化；`:123`「实现细节维护在包 README」——两处文档改动正落在这里，位置正确。

**依赖关系逐项核对**：

- **import 实际变化：零**。`cleanup_test.go:4-9` 的 import 仍为 `context`、`reflect`、`testing`、`time`，不含任何 ee 包（上一轮收窄后的形状保持）；两份 README 非代码。
- **构造函数与注入**：差量不含构造函数。用例经同包 `fixture`（`service_test.go:81-97`）取得服务与 `*gorm.DB`，`*gorm.DB` 未进业务层。
- **窄接口与共享资源所有权**：本轮零改动，沿用既有形状（`host` 三个未导出窄接口、`app` 单向注入）。
- **跨模块数据访问**：用例只读写 `ee_identity_*` 自有表（`sessionRow`/`ticketRow`，`rows.go:100`），不碰其他模块的表。
- **新增文档指向的依赖含义**：`identity/http` 的 README 指向 `host` 包的测试，是文档层面的引用，不产生 Go 依赖边（`identity/http` 的 import 未变），不构成反向依赖。

### 开放式复读

已做开放式复读（放下起手表，按「半年后要改这里的人」把 3 个文件各再读一遍，回答「第一眼会让我皱眉的是什么」）。

- `http/README.md`：皱眉点就是 S7——第一眼读到「所有 POST 的来源拒绝」会以为这层覆盖是自动成立的，而打开测试才发现期望表是手抄的。**这是表外发现**（起手表「注释与现状矛盾」那一行把它抓住了，但真正让我停下的是通读这句话时的读者视角，如实记录来源为复读）。
- `persistence/README.md`：皱眉点是 S6 的「1 小时到 7 天」——同一行里前半句已经用常量名 `SessionTTL`/`WSTicketTTL` 立了好规矩，后半句又把范围抄成散文数字，半新半旧。
- `cleanup_test.go`：皱眉点是 S5——「票据一小时后到期」这件事在生产里不可能发生（`WSTicketTTL` 是 30 秒），读者要自己想明白这只是播种余量。

**表外发现：S7 一条**（另两条由表内信号同时命中）。复读中掂量后判定不构成问题的三处写在「看似可改、但有理由保留」P7–P9。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | **S6**（取舍）：可配上界「7 天」成为该值的第二种写法，且是 grep 不到的单位；同仓对同一上界已有「范围见 `identity.MinSessionTTL`/`MaxSessionTTL`」的既有写法 | `ee/internal/identity/persistence/README.md:13`；`ee/internal/identity/model.go:75-76`；`ee/internal/app/identity.go:26`；`ee/docs/账号认证升级与恢复.md:11`；`git grep "168\|7 天" -- ee` 全量两处 |
| 职责与实现质量 | **S7**（取舍）：新补的指向句给出的维护动作弱于它自己宣称的覆盖面。**S5**（取舍）：放宽后的用例余量无来源说明 | `ee/internal/identity/http/README.md:7` vs `ee/internal/host/auth_test.go:336,341-360`、`ee/internal/identity/http/handler.go:53-70`；`ee/internal/identity/persistence/cleanup_test.go:30,31` vs `ee/internal/identity/model.go:77` |
| 架构与项目一致性 | 无。差量不含生产代码；两处文档落在架构 §8:123 指定的位置，包归属与 §4 职责表、§7:113 一致；import 图零变化 | `product/架构/EE后端架构.md:46,61-68,113,121-123`；`cleanup_test.go:4-9` |
| 契约一致性 | 无。README 新引入的默认值、范围、票据 TTL 与 `model.go`、`core.go` 校验、`ee/docs` 两份接口文档四方一致（重复副本的代价见 S6）；对外 API、DTO、状态码、迁移 ID 未触及 | `model.go:74-77`、`core.go:29`、`app/identity.go:36-41`、`ee/docs/账号认证升级与恢复.md:11`、`ee/docs/账号认证接口.md:9` |

## 出口清单

### 上一轮修复项逐条处置（差量复核必答）

| 项 | 处置 | 证据 |
|---|---|---|
| **R7** `identity/http` 的包 README 不再指向钉住其路由表的契约测试，而该测试在另一个包 | **已修复** | `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/README.md:7` 句尾补「路由归属、所有 POST 的来源拒绝与匿名/会话边界的契约测试在 [host/auth_test.go](../../host/auth_test.go) 的 `TestIdentityRouteContract`（另一个包），改路由表要一并跑」。独立取证：①链接从 `ee/internal/identity/http/` 解析为 `ee/internal/host/auth_test.go`，文件存在且 `TestIdentityRouteContract` 在 `:337`；②三项宣称逐条对得上测试原文（`:362` 方法归属、`:365-368` 逐条 POST 的 403、`:369-372` 匿名状态码）；③「所有 POST」属实——测试表 15 条路径 = `handler.go:54-68` 的 15 条 POST 路由，GET 那条由 `:380-383` 另测；④`git grep TestIdentityRouteContract -- ee` 仅两处命中（测试 + 本行），未造第二份说法；⑤链向另一包 `.go` 文件的写法有先例（`ee/ui/app/enterprise/lib/schemas/README.md:13`）。修复落在「做什么」第一条句尾，未动短版结构，与上一轮给出的最小修正一致。**遗留的措辞精度另开为 S7，不影响本项关闭。** |
| **S4** `persistence/README.md` 的行数期望值是与部署配置相关的估算，却写成无条件口径 | **已采纳（超出最小修正）** | `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/README.md:13` 现为「会话行数 ≈ `SessionTTL` 窗口内的登录次数（默认 24 小时、内部控制台约为两位数；TTL 可配 1 小时到 7 天，按部署重算），票据行数 ≈ `WSTicketTTL` 内的 WS 建连次数；超过万行再评估加索引」。逐项核实：`SessionTTL` 存在（`core.go:21` 字段、`model.go:74-76` 三个常量）、默认 24 小时（`model.go:74`）、范围 1 小时–7 天（`model.go:75-76` + `core.go:29` 校验 + `app/identity.go:36-41` 的 `EE_SESSION_TTL_HOURS` 换算 + `ee/docs/账号认证升级与恢复.md:11` 的「1–168」）、`WSTicketTTL` 存在且为 30 秒（`model.go:77`，`http/session.go:78` 的 `expires_in` 由它推导）。比最小修正多做了一步：票据那一半从方案 §4.6 的手抄「30 秒」改为常量名，符合硬规则「派生值要写推导」。「万行」阈值与 §4.6 的不加索引拍板一致，未承诺索引。**新引入的数字副本问题另开为 S6，不影响本项关闭。** |

### 代码评审 1「转收敛评审」栏逐条处置

| 转交条目 | 处置 | 依据 |
|---|---|---|
| `store.go:158,192` 清理用 `time.Now().UTC()`，同包其余时间条件（`RevokeSession`/`ConsumeTicket`/`ReserveLogin`）由服务层传入 `at`，时钟来源不一致 | **挂账，不重复处理** | 已裁决项。`workbench/findings.md:193-199` FIND-021（`status: 观察中`、`triggered_by: [身份模块整理]`、evidence 带路径、`convert_when` 可判定）。本轮核对 evidence 与代码现状仍一致：`store.go:158` 的 `sweepExpired` 自取 `time.Now().UTC()`，同文件 `:172,:177,:207` 的 `RevokeSession`/`RevokeSessions`/`ConsumeTicket` 由业务层传 `at`；转交栏所写的 `:192` 在合并后已不存在（现仅 `:158` 一处）。本轮差量未触及该文件，未恶化。按「不重提已裁决问题」不出意见 |
| `core.State`/`core.RequireAccountManager` 经嵌入提升到三条线，`AccountService`/`PasswordService` 也暴露 `State`（方案 §4.1 预期，仅记录） | **有依据撤回** | 转交栏自标「仅记录」，且属 Gate 1 已裁决的结构方案 B（`方案v4.md:275-277`「结构方案 B 与 DTO 单文件、端点三文件在评审 2 后已裁决」）。本轮差量不含 `core.go`/`accounts.go`/`passwords.go`，形态未变，无恶化依据 |
| 测试内组合类型 `services`（`service_test.go:60-71`）与 `identity.Services` 同形（方案 §6 规定，仅记录） | **有依据撤回** | 读 `service_test.go:60-71` 原文与 `方案v4.md:232-237` 的代码示例，形状一字不差（组合类型 + 两个转发方法），目的是让 61+2+2 处既有 `s.X(` 调用与全部断言零改动（`:231,238`），并由 `tools/test_guard.sh` 机械核对；gofmt 展开多行一事已登记为实施偏差 3（`:308`）。它是表征测试的手段，不是第二套业务规则。本轮差量不含该文件 |
| `http/README.md` 短版不再指向 `host/auth_test.go` 的 `TestIdentityRouteContract`，路由契约测试的位置只剩代码可查 | **已修复** | 即上一轮 R7，本轮关闭，证据见上表。措辞精度的余项另开 S7（取舍，不阻塞） |
| `persistence/README.md` 的「正常应在两位数以内」是运维口径，无来源引用 | **已修复** | 即上一轮 S4，本轮采纳并写成以 `SessionTTL`/`WSTicketTTL` 为源的推导，证据见上表。新引入的数字副本另开 S6（取舍，不阻塞） |

### 本轮修复项

- 无。上一轮 R7 已关闭、S4 已采纳；本轮差量内未发现必须修且无需裁决的问题。三条新意见均带反证条件，列入下节由实施者决定。

### 建议取舍项（附反证条件，均不阻塞 Gate 2）

**S7 · 新补的指向句要求「一并跑」，但那张期望表是手维护的，只跑不加用例会让同句宣称的「所有 POST」静默失真**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/README.md:7`（关联 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/host/auth_test.go:336,341-360`、`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/handler.go:52-70`）
- **代码事实**：该行宣称「路由归属、**所有 POST** 的来源拒绝与匿名/会话边界的契约测试在 host/auth_test.go 的 `TestIdentityRouteContract`…改路由表要一并跑」。`TestIdentityRouteContract` 的期望来自一张手写的 15 行表（`auth_test.go:341-360`），其上方 `:336` 的注释明写「路径/匿名状态取自批准的HTTP契约，**不依赖生产路由表构造预期**」；测试不遍历 `handler.go:53` 的 `routes`，也没有「表长度 == 路由表 POST 条数」之类的断言（`:375-379` 只反向检查三条不属于本包的路径）。今天两边恰好都是 15 条，所以宣称成立；新增第 16 条 POST 路由后，`TestIdentityRouteContract` 依旧全绿，而这句话仍然宣称覆盖「所有 POST」。
- **维护影响**：这句话的读者正是改路由表的人，也是唯一被告知去哪找回归测试的人。按它的字面指示「一并跑」而不加用例，新路由的来源拒绝与匿名/会话边界零覆盖，README 却继续对下一位读者背书「所有 POST」已被钉住——错误从「没有线索」升级为「有一条让人放心的假线索」。代价是一句话的措辞。
- **最小修正**：把动作说全，例如「…改路由表要在那张表里同步加一行并一并跑」；或在「所有 POST」后加限定「（表内 15 条，与路由表逐条对应，加路由须同步加用例）」。
- **反证条件**：若新增身份路由必然走「批准 HTTP 契约 → 改 `ee/docs/账号认证接口.md` → 补契约表」的既定流程（`auth_test.go:336` 的措辞正暗示期望来自批准契约而非代码），则改路由表的人不会只跑不加，这句「一并跑」已够，保持现状——此时更该做的是把「期望表以批准契约为源」这半句也带进 README，而不是加强动作词。

**S6 · 行数推导把可配上界第二次抄进散文，且抄成了 grep 不到的单位**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/README.md:13`
- **代码事实**：该行写「TTL 可配 1 小时到 7 天，按部署重算」。同一上界在仓库里已有两种表示：`ee/internal/identity/model.go:76` 的 `MaxSessionTTL = 7 * 24 * time.Hour`，与 `ee/docs/账号认证升级与恢复.md:11` 的「默认24，1–168整数」。`git grep -n "168\|7 天\|七天" -- ee` 全量命中只有后两者，即新增的「7 天」既匹配不到代码常量的写法，也匹配不到已批准文档的「168」。同仓对同一上界已有不抄数字的既有写法：`ee/internal/app/identity.go:26` 的注释「范围见 identity.MinSessionTTL/MaxSessionTTL」。「默认 24 小时」同理成为第四份副本（`model.go:74`、`ee/docs/账号认证接口.md:9`、`升级与恢复.md:11`、本行）。
- **维护影响**：上界一旦调整，改的人会 grep `168`、`MaxSessionTTL` 或 `7 * 24`，三种检索都找不到这行中文散文，于是留下一句看起来精确、实则过期的运维口径——而这行的全部价值就在「按部署重算」，读者信它才有用。代价是半句话。
- **最小修正**：沿用同仓既有写法，把范围换成常量指向，例如「`SessionTTL` 可配（范围见 `identity.MinSessionTTL`/`MaxSessionTTL`），按部署重算」；默认值那半句已由 `SessionTTL` 指名，可保留也可同样改为常量指向。
- **反证条件**：若认为这行是给运维读的、必须在不打开 Go 源码的前提下自成一体，则保留具体数字是对的——但此时应补上运维真正握有的那个旋钮名 `EE_SESSION_TTL_HOURS`（`app/identity.go:26`，`ee/docs/账号认证升级与恢复.md:11` 已载其 1–168 口径），而不是给运维一个内部 Go 字段名 `SessionTTL` 加一对手抄的边界。

**S5 · 未到期票据余量放宽到 1 小时，但没有留下「为什么是 1 小时」的线索**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/cleanup_test.go:30,31`（对照 `:20,21`、`:16`、`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/model.go:77`）
- **代码事实**：两条「未到期票据」的 `ExpiresAt` 由 `now.Add(time.Minute)` 改为 `now.Add(time.Hour)`，而 `identity.WSTicketTTL = 30 * time.Second`（`model.go:77`）——生产路径（`identity/session.go:107` 的 `now().Add(WSTicketTTL)`）永远签不出一小时后到期的票据。余量真正要覆盖的是播种（`:14` 取 `now`）到清理触发（`:70` 的 `IssueTicket`）之间那次含 bcrypt 的 `s.Login`（`:67`）的墙钟耗时。同文件的会话行反而是领域自洽的（`:18` 的 `ExpiresAt: now-1h` 配 `CreatedAt: now-25h`，正好是 24 小时 TTL 的过期会话），因此这处不一致读起来像笔误而非取舍；`:16` 的注释只解释了「为什么直接写行」，没解释「为什么是这个余量」。
- **维护影响**：下一个读到这里的人有两条歧路——要么以为票据可以活一小时（与 `WSTicketTTL` 矛盾），要么出于「让用例像真的」把它收回 30 秒附近，把刚被评审提出、本轮特意放宽的慢 CI 抖动重新引回来。代价是半句注释。
- **最小修正**：在 `:16` 的注释后或票据循环上方补一句来源，例如「未到期行的余量取 1 小时，只为覆盖中间那次含 bcrypt 的登录在慢 CI 上的耗时，与 `WSTicketTTL` 无关」。
- **反证条件**：若认为这四行本就是手写的合成行（同循环已有 `"ticket-expired-consumed"` 这类不可能由生产代码产生的 `Hash`，`:16` 也已声明是直接写行绕过清理），读者不会把余量当领域事实，则保持现状不改——此时也不必收紧余量，它没有语义负担。

### 需用户裁决的 Blocking

- 无。三条取舍项均在授权范围内，不触及范围、契约与风险接受；两处文档措辞与一处用例注释，不改任何对外行为。

### 立债（REF）

- 无。

### 挂账（Finding）

- 无新增。FIND-021 已由实施者按上一轮 S1 登记（`workbench/findings.md:193-199`），本轮复核四要件与代码现状一致，不重复登记。

### 转代码评审（范围外发现，一行一条，不出结论）

- T7：`ee/internal/identity/persistence/cleanup_test.go:30,31` 的未到期票据余量已由 1 分钟放宽到 1 小时（对上一轮 T5 的回应），请在定向复核时按新值确认 T5 可关闭，并确认 T4（`:79,82` 的 `reflect.DeepEqual` 对含 `time.Time`/`*time.Time` 字段的行在 SQLite 与 `IDENTITY_TEST_POSTGRES_DSN` 两条驱动下的稳定性）在余量改变后仍按原口径核对。

## 台账对账

- **FIND-017**（busy 重试两份）：本轮未触及 `branding/persistence/migration.go` 与 `identity/persistence/diagnostics.go`，未恶化，继续观察。
- **FIND-018**（`ReserveLogin` 抢 `state` 写锁）：未关闭，未恶化。本轮差量不含生产代码，容量与并发不受影响；`findings.md:171` 已载上一轮定向复核的实测数字，本评审不重复判断（属代码评审范围）。
- **FIND-019**（哈希槽耗尽与登录限流共用 `ErrLimited`）：已接受/豁免，本轮未触及错误映射，不重提。
- **FIND-020**（会话/票据无删除路径）：已转 Case 并在本期实现。本轮复核实现口径与 Gate 1 拍板 (c) 仍一致——只按 `expires_at` 过滤、含已撤销/已消费（`store.go:155-159`），未加索引（`migration.go` 不在差量内，`persistence/README.md:13` 亦只写「超过万行再评估加索引」，未承诺索引），迁移 ID 未动。`cleanup_test.go` 的本轮改动只放宽了未到期行的余量，四态覆盖与断言强度不变。`findings.md:190` 的「随期 1 交付关账」条件本轮无新增遗留，可在 Gate 2 一并关账。
- **FIND-021**（清理时钟由存储层自取）：`status: 观察中`，本轮 evidence 与代码现状一致（`store.go:158` 自取时钟，`:172,:177,:207` 由业务层传 `at`），未恶化；代码评审转交的同一条按此挂账处置，不重提。

## 看似可改、但有理由保留（写明理由，下一轮不再重提）

- **P7 · `http/README.md:7` 这条 bullet 现在承载两件事**（路由表是唯一来源 + 契约测试在哪）：短版 README 的 bullet 本就是「一条讲清一个面」的粒度，这两件事是同一个读者在同一时刻需要的（改表的人要知道去哪回归），拆成第三条 bullet 或塞进文件表的 `handler.go` 行都会让指向离「唯一来源」这句话更远。上一轮的最小修正也明确给了这两个落点任选。保留。
- **P8 · `host/README.md:23` 没有反向注明 `auth_test.go` 同时钉着 `identity/http` 的路由表**：两份 README 的方向不对称是刻意的——需要被提醒的是改路由表的人（在 `identity/http`），不是改 `host` 的人；补上反向一句就制造了第二处要同步维护的说法，而 `git grep TestIdentityRouteContract -- ee` 现在只有一处指向正是 S7 之外的另一半价值。保留。
- **P9 · `cleanup_test.go` 的过期行余量仍不对称**（会话 `-time.Hour`、票据 `-time.Minute`，而未到期行两者都已是 `+time.Hour`）：过期方向没有竞态——无论 1 分钟还是 1 小时前过期，清理都必删，余量大小不影响判定；且 `-time.Minute` 与票据 30 秒 TTL 的领域尺度更贴（`past` 这个共用变量也是 `-time.Minute`，用于 `RevokedAt`/`ConsumedAt`）。统一成同一个数只是观感一致，反而抹掉会话行 `-1h`/`-25h` 与 24 小时 TTL 自洽的那点用心。保留。
- **P2/P3/P4/P5/P6**（上一轮已写明理由的查询闭包近似重复、`store.go` 323 行不拆、`sweepExpired(model any)` 无类型约束、`t.stage` 由调用点设置、`model.go:191` 合并 godoc）：本轮差量未触及这些位置，我独立复看 `store.go:140-219`、`cleanup_test.go:37-50` 现状后判断相同，不重提。

## 实际阅读范围与未执行的检查

**读了什么**：见「事实来源」四段清单——3 个差量文件全部读完整现状（不只读 diff 行），另完整读了 `host/auth_test.go:320-388`、`http/handler.go:1-80`、`store.go:140-219`、`rows.go:60-104`、`service_test.go:55-103`、`identity/README.md`、`host/README.md`、`model.go:74-77`、`core.go:21,29`、`app/identity.go:26,32-41`、`ee/docs` 两份文档的相关行、EE 后端架构 §3/§4/§5/§7/§8。**同类实现对照**：`app/identity.go:26` 的「范围见 identity.MinSessionTTL/MaxSessionTTL」作为 S6 的既有写法对照；`ee/ui/app/enterprise/lib/schemas/README.md:13` 作为跨包链 `.go` 文件的先例；`host/README.md:23` 与 `identity/README.md` 作为包 README 指向测试的行文对照；同文件的会话播种行作为票据播种余量的对照。**关键假设的证实**：README 宣称的三项覆盖、「所有 POST」的完备性、期望表不随路由表扩张、两个 TTL 常量与范围、「7 天」在仓库里的唯一性、`ticketRow` 无 `CreatedAt`——六项均以原文行号取证，未据方案、历史评审或印象推断。

**未执行的检查**（无一条被当成通过）：

1. **没有编译、`go vet`、`go test`、`gofmt -l`、预飞**（只读评审，按提示词不运行会写文件的测试或构建）。放宽后的 1 小时余量是否真的消除了 T5 的抖动、`reflect.DeepEqual` 在两种驱动下是否稳定，本轮未验证；测试是否可信属代码评审范围，已按 T7 转交。
2. **未渲染 Markdown、未跑链接检查器**。`[host/auth_test.go](../../host/auth_test.go)` 的可解析性是按路径逐段推算并用 `git grep` 确认目标文件与函数存在，未实际点击或用工具批量验链；`ee/` 下的 README 不在 `workbench/tools/build_views.py` 的校验范围内。
3. **未读 `migration.go` 正文**。FIND-020「不加索引」本轮依据是该文件不在差量清单内（`git diff --stat e9075c51b..beb05c0c1` 无此文件），索引集合必然与上一轮相同；本轮未重新打开。
4. **未读 `identity/http/{protocol.go, dto.go, session.go, accounts.go, passwords.go, handler_test.go, dto_test.go}` 与 `persistence/{diagnostics.go, accounts_page_test.go, diagnostics_test.go}` 正文**（均不在差量内）。两份 README 的文件表完整性是用 `git ls-files ee/internal/identity` 与表行逐条比对，未逐个打开核对每行描述是否仍贴合文件内容——只核对了与本轮改动相关的 `handler.go`、`store.go`、`cleanup_test.go` 三行。
5. **未核对 `ee/docs/账号认证接口.md` 全文与 HTTP 层的三方同步**（本轮差量不含代码与 DTO）；只核对了与 README 新增数字相关的 `:9`（默认 24 小时）与 `升级与恢复.md:11`（1–168）两行。
6. **未跑 `dupl`/`deadcode` 等机械工具**；重复判断来自逐文件阅读与定向 `git grep`（`168`/`7 天`/`24 小时`/`两位数`/`TestIdentityRouteContract`/`WSTicketTTL` 六组全量结果）。
7. **workbench 侧 9 个差量文件不作为被评对象**，按提示词只读取其中的裁决输入（Gate 决策结果、实施偏差记录、评审处置表、方案 §6、findings 台账）。本期 `checklist.yaml` 的必存产物与必含符号是否随本轮文档改动仍成立，未核（上一轮亦未核，属预飞范围）。
8. **按规则未读**：`代码评审1.md` 正文与更早的评审记录（差量复核例外：已读 `收敛评审2.md` 全文以逐条处置其修复项，已按提示词原样接收代码评审 1 的「转收敛评审」栏五条并独立取证）。`git grep` 输出中偶然命中的历史评审文档未据其内容形成任何判断，S5/S6/S7 与 R7/S4 的处置证据链均独立于它们。
