---
title: 身份模块整理 期1 收敛评审 #1
type: 收敛评审
case: 身份模块整理
phase: 1
round: 1
verdict: 有Blocking
created: 2026-09-12
---

> 评审者与实施上下文隔离（独立会话，未读旧代码评审／旧收敛评审）。基线 `7284ad939462e4b78b6518826f6c537e13aecb46` 到 `cde781a93acca87a8dbf6f5b70329f659a845ece`（工作树干净，`git status --porcelain=v1` 为空，`git ls-files --others --exclude-standard` 为空，无未跟踪文件）。L3：逐维留痕。

## 结论

这次整理达到了它自己设定的目标：一个 18 方法的大 `Service` 被切成三条业务线加一个共享 `core`，切口落在真实的业务边界上（三条线互不调用，跨线动作经 `Tx` 或 `core` 判定），端点文件与路由表一一对应，`host` 只通过三个使用方自定义的窄接口触及身份能力，依赖图单向无环、业务根包仍只依赖标准库，`AccountRecord` 改名与账号列表去哈希把"业务类型/DTO/行结构分开"这条硬规则落实得比整理前更干净，FIND-020 的实现口径与 §4.6 的拍板（到期即删、含已撤销、不加索引、迁移 ID 不变）逐条对得上。表结构、迁移 ID、路由表、DTO 键集合、冒烟脚本都没有被顺手改动，"只整理结构不改对外行为"的约束守住了。挡在交付前的是 6 条小而确定的收尾问题：架构文档的布局图还写着旧包名（同一文档内自相矛盾）、两处文件注释在拆分后失效、清理用例的注释与它实际的控制流不符、同一条"删到期行"规则被写成两份且边界口径与业务层判定差一个等号、两条分页线对 `Items` 空值给出两种写法、新写的上游改动登记列了一个已不存在的目录。没有一条需要用户裁决，也没有一条要动已批准的契约；修完即可转无碍。

## 事实来源

**跑过的命令**（Bash 在本会话中途起被权限模式拒绝，见"未执行的检查"）：`git status --porcelain=v1`、`git rev-parse --abbrev-ref HEAD`/`HEAD`、`git ls-files --others --exclude-standard`、`git diff --stat 7284ad939462e4b78b6518826f6c537e13aecb46 HEAD -- ee product`、`find ee/internal`。其后 `git diff <路径>`、`git show`、`git log` 均被拒，因此**逐 hunk 的新旧对照不可得**；新旧定位改用三条互相独立的线索交叉：上面那份 `--stat`（35 文件、+1217/-766，含 `bifrost => host` 的重命名行）、方案 v4 §5 的逐步骤实施记录、以及对每个范围内文件的完整现状阅读。

**完整读过的范围内文件**（绝对路径省略前缀 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/`）：`ee/internal/identity/{model.go, core.go, session.go, accounts.go, passwords.go}`、`ee/internal/identity/http/{handler.go, session.go, accounts.go, passwords.go, dto.go, dto_test.go}`、`ee/internal/identity/hasher/bcrypt.go`、`ee/internal/identity/persistence/{store.go, rows.go, service_test.go(497 行全文), accounts_page_test.go, cleanup_test.go}`、`ee/internal/host/{auth.go, config.go, auth_test.go(388 行全文)}`、`ee/internal/app/{bootstrap.go, identity.go, recovery.go}`、六份包 README（`ee/internal/{app, host, identity, identity/hasher, identity/http, identity/persistence}/README.md`）、`ee/README.md`、`product/README.md`、`product/上游改动/README.md`、`product/架构/EE后端架构.md`。

**范围外但为判断而读的既有实现与输入**：`ee/internal/identity/http/protocol.go`（未改，但拆分把它的文件注释打穿，见 R2）、`ee/internal/identity/persistence/migration.go`（核 FIND-020 是否偷加索引、迁移 ID 是否动过）、`ee/docs/账号认证接口.md:13,27,28`（契约三方对账）、`ee/scripts/identity-smoke.py:169-267` 与 `identity-upgrade-smoke.py:37,133`（冒烟侧契约）、`ui/app/globals.css:10`（核 `product/上游改动/README.md` 的一条事实）、`workbench/findings.md` 全文、`workbench/cases/身份模块整理/期1/方案v4.md:96-329`（含 §9 Gate 决策结果、实施记录、实施偏差记录 1–6、重构阶段终扫）、`workbench/规范/流程/收敛评审.md`、`收敛评审审查方向.md`、`workbench/规范/项目/编码规范.md`、`AGENTS.md`、`workbench/AGENTS.md`、`workbench/templates/收敛评审.md`。**同类实现对照**：`ee/internal/branding/*`（经 import 清单核依赖形状）、`ee/internal/identity/http/handler_test.go`（经 grep 核测试职责与 README 说法是否一致）。

**执行过的 grep（结果即证据）**：`internal/bifrost`（ee/、product/、workbench/规范 命中 0，仅历史 case 文档与期1 checklist 顶部注释命中，后者按方案 §8 为有意保留）；`identity\.Service\b|NewService|Credential`（ee/ 下命中 2 处，均为无关词：`service_test.go:341` 的测试函数名、`auth_test.go:243` 的 `temptoken.NewService`）；`darkBaryon/bifrost/ee/internal`（全量 import 图，见"依赖关系"）；`time\.Now\(\)`（ee/ 全量，9 处）；`framework/encrypt|golang.org/x/crypto/bcrypt`（生产代码仅 `hasher/bcrypt.go:8,9`）；`^`（行数统计，用于 300/500 行规则）。

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。业务限制集中在 `identity/model.go:68-96`，本轮未新增任何数字；`hasher/bcrypt.go:20` 的正则按方案原文照搬，`persistence` 侧已无第二份 |
| | 同一规则多份实现且互不注明 | **发现 R5**（两处到期清理） |
| | 重造已有 helper | 无。`page`/`encodeCursor` 上移 `core.go:215,232` 后两条线共用；`beforeCursor`/`notFound`/`conflict`/`authRecord` 仍各一份；`hasher` 转调上游 `encrypt`，未自建 bcrypt |
| | 派生常量无推导 | 无。`model.go:99` `tokenLength = base64.RawURLEncoding.EncodedLen(tokenBytes)`、`http/session.go:78` `int(identity.WSTicketTTL/time.Second)`、`app/recovery.go:24` `identity.MaxPasswordBytes + len("\r\n")` 都是表达式 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无。新增代码无裸值；`stateRow` 单例 `id = 1`（`store.go:54,117,225,233`）是既有写法且 `rows.go:39` 已注明，不属本次改动 |
| | 状态码、错误码裸数字 | 无。生产代码一律 `fasthttp.Status*`（`http/session.go:28,43,56,67,78` 等）；测试里的 200/401/403 属编码规范明示豁免的"按契约独立书写字面量" |
| | 无名的错误形状（以元组表示失败） | **发现 S3**（`core.go:215` `page` 的第二返回值原样回传入参） |
| | 紧凑但绕的控制流 | 无。`core.go:158` `current` 把"查会话"抽成函数参数供事务内外共用，是收敛不是绕；`http/handler.go:90` `serve` 的前置规则线性可读 |
| | 文件集中或拆分是否妨碍理解 | 无。端点三文件与路由表一一对应，读一条线不用跨文件；`dto.go` 单文件四段（`:10,48,77,125`）保住了"响应形状一起看"；详见有理由保留 P1–P5 |
| | 多余状态或守卫 | **发现 R4**（`accounts.go:141` 的 `Items` 初始化在 `:157` 被 `append` 复写、`passwords.go:167` 的同一初始化在 `:201` 被直接覆盖） |
| | 同一效果做两次（响应头、注册、初始化） | 无（`Cache-Control`、路由注册、Cookie 写入各一处）；`Items` 的重复初始化已计入 R4 |
| | 注释与现状矛盾 | **发现 R2、R3**。反向核对：`model.go:139`（撤销/清理口径）与 `model.go:249`（bcrypt 位置）本轮已改对 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。`host.AuthAdapter` 仍以 `var _ server.ConsoleAuthProvider = (*AuthAdapter)(nil)`（`auth.go:290`）钉住上游接口；`RegisterRoutes(*router.Router, ...schemas.BifrostHTTPMiddleware)` 与 branding 的注册形状一致；`hasher` 以 `var _ identity.PasswordHasher = Bcrypt{}`（`bcrypt.go:28`）钉住本包接口 |
| | **架构符合性** | **发现 R1**（文档侧）。代码侧符合，逐章见下 |
| | **依赖关系** | 无。全量 import 图见下 |
| | 绕过既有出口 | 无。迁移仍走 `host.Config.ConfigStore.RunMigration`（`app/identity.go:71`）、表结构与 `ee_identity_v1` 未动（`migration.go:14,41-53`）、响应仍经 `identityhttp.JSON/Error`、鉴权仍经 `APIMiddleware` |
| | 把上游现状当契约 | 无。`host` 对上游的依赖点（`ConsoleAuthProvider`、`WebSocketAuthorizeContextKey`、`IsLocalAdminContextKey`、`temptoken` 六组路由）本轮一处未动，且都登记在 `product/上游改动/README.md` |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无。`ee/docs/账号认证接口.md:13` 的字段集合 == `dto.go:80-90` `accountDTO` == `dto_test.go:20` 断言；`Account` 新增的 `CreatedAt`（`model.go:122`）未泄进 DTO；前端暂无消费方（`grep api/accounts` 仅命中两个冒烟脚本，均未改） |
| | 状态码口径不一 | 无。`protocol.go:107-125` 七错七码单点映射，本轮未动；`host/auth_test.go:341-374` 的匿名契约表仍逐路径钉住 |
| | 省略、空、值三态语义不对应 | **发现 R4**（页 `Items` 的空值语义两条线不一致）。`listRequest.Limit`/`eventsRequest.Limit` 的指针三态（`dto.go:117,171` + `pageLimit`）正确 |

### 开放式复读

已做开放式复读（放下表，按"半年后要改这里的人"逐文件再读一遍）。**表外发现 2 条**：R6（`product/上游改动/README.md:5` 把已不存在的根 `docs-zh/` 列为自有目录——起手表没有覆盖"文档列了不存在的路径"这类信号）、S2（`State`/`RequireAccountManager` 经嵌入提升到三条线后，外部调用点各选一条、无惯例——起手表的"接缝形状"只管跨包接缝，不管同包内能力提升后的调用口径）。其余文件第一眼会让维护者皱眉的地方均已落入表内条目。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | R5：`expires_at` 到期清理写成两份，且与业务层判定差一个等号 | `ee/internal/identity/persistence/store.go:158`、`:192` vs 同文件 `:256` 与 `ee/internal/identity/core.go:139` |
| 职责与实现质量 | R2、R3 注释与现状矛盾；R4 多余/互相矛盾的状态初始化；S3 多余返回值 | `http/protocol.go:1`、`persistence/cleanup_test.go:41`、`identity/accounts.go:141,157` 与 `identity/passwords.go:167,201`、`identity/core.go:215` |
| 架构与项目一致性 | 代码侧符合；R1 架构文档布局图未随改名回填，同文档自相矛盾 | `product/架构/EE后端架构.md:46` vs `:53`、`:113`；代码侧证据见下两节 |
| 契约一致性 | 对外契约三方一致；仅页 `Items` 的空值语义未定（R4） | `ee/docs/账号认证接口.md:13` / `http/dto.go:80-90` / `http/dto_test.go:20`；`identity/model.go:192-200` |

**架构符合性逐章核对**（对照 `product/架构/EE后端架构.md`）：

- §3 总体布局 / §8 适用与维护：代码已落在 `ee/internal/{app,host,identity,branding}`，新增 `identity/hasher` 属"模块内按需要选择适配实现"，不是新模块。**但文档 `:46` 的布局图仍写 `└── bifrost/ 宿主与上游能力集成`**，与 `:53`、`:113` 和实际包名冲突——R1。
- §4 模块内部的职责：模块根包只有业务（`core.go`/`session.go`/`accounts.go`/`passwords.go` 的 import 全是标准库）；`http/` 只做"解码→调服务→装响应"（`http/accounts.go:9-55` 每个端点三步、无业务判断）；`persistence/` 只做表映射与事务。§4 明示"不是每个模块必须照建的文件清单"，故端点三文件、`dto.go` 单文件都不按示例清单判。
- §5 依赖与协作：`app` 只做构造与连线（`app/identity.go:55-86` 全是校验、迁移、New、注入，无运行期编排）；事务范围由业务服务决定（`accounts.go:111` `SetAccountStatus` 把改状态与撤销会话包在一个 `Transaction` 内），`*gorm.DB` 未进业务层。
- §6 插件与普通业务：本期无插件入口，不适用。
- §7 与 Bifrost 的协作边界："集成归位"到 `internal/host/`（`host/auth.go:1` 包注释与 `:290` 的接口断言）；"按缺口扩展"——`hasher` 只包了 `encrypt.Hash/CompareHash` 两个调用点，没有重复包装整套 SDK；"生命周期统一协调"——`persistence/store.go:19` 注明"连接由宿主持有并关闭"，`Store` 不 Close 共享连接，`app/recovery.go:68` 的 `defer store.Close(ctx)` 关的是恢复命令自己打开的那个。

**依赖关系逐项核对**（`grep -rn darkBaryon/bifrost/ee/internal ee/internal` 全量结果）：

- `identity`（根包）：`model.go`/`core.go`/`session.go`/`accounts.go`/`passwords.go` 在全量 import 图中**一次都没出现**，即不 import 任何 ee 包；其 import 只有 `context/crypto/encoding/errors/fmt/regexp/time/unicode`。无反向依赖。
- `identity/http → identity`（`handler.go:6`、`session.go:8`、`accounts.go:5`、`dto.go:7`、`protocol.go:16`）；`identity/persistence → identity`（`store.go:11`、`rows.go:7`、`diagnostics.go:11`）；`identity/hasher → identity`（`bcrypt.go:7`，仅为 `:28` 的接口断言）。三个适配子包互不依赖。
- `host → identity + identity/http`（`auth.go:13,14`、`config.go:10,11`），生产代码**不**依赖 `persistence`/`hasher`；测试边 `auth_test.go:13,15` → `hasher`/`persistence`，`service_test.go:17` → `hasher`（兄弟包），无任何测试边指向 `host`。
- `app → host, identity, identity/hasher, identity/http, identity/persistence`（`identity.go:13-17`、`bootstrap.go:9-11`、`recovery.go:16`）。全图单向无环，与方案 §4.7 的终态一致。
- 使用方窄接口与显式注入：`host/auth.go:27,36,42` 三个未导出接口全部由 host 自己声明，构造函数 `NewAuthAdapter(host, sessions, handler)`（`:56`）、`ImportLegacyAdministrator(ctx, host, accounts, log)`（`:67`）显式收依赖；`app/identity.go:78,85` 分别只把 `svc.Account`、`svc.Session` 递进去，建号/重置/恢复对 `host` 不可达。日志同样是窄接口（`host/auth.go:61` `Logger`、`persistence` 的 `Logger`），`ee/` 内无标准库 `log`。跨模块无直接改表，共享资源所有权明确（见上）。

## 出口清单

### 本轮修复项

> 六条均在授权范围内、无需用户裁决。本轮未关闭，故 verdict 为有Blocking；修复后定向复核即可转无碍。

**R1 · 架构文档的布局图仍写旧包名，同一文档内自相矛盾**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/product/架构/EE后端架构.md:46`
- 代码事实：总体布局代码块第 46 行为 `│   └── bifrost/           宿主与上游能力集成`；同文档 `:53` 写"现有代码已落在 …及`internal/host/`"，`:113` 写"`internal/host/` 负责宿主、管理路由、会话兼容等跨功能接入"；实际包为 `ee/internal/host`（`host/auth.go:2` `package host`）。
- 维护影响：这份文档是编码规范指定的架构与依赖唯一依据，新功能作者按布局图建目录会建回 `internal/bifrost`；同文档三处说法不一致时，读者无法判断哪一处是现行约定。
- 最小修正：`:46` 改为 `│   └── host/              宿主与上游能力集成`，一词替换，不动其余章节。

**R2 · 端点拆分后 `protocol.go` 的文件注释指向已失效的位置**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/http/protocol.go:1`
- 代码事实：首行注释结尾为"端点见 handler.go"；`handler.go` 现在只剩常量、`Handler`、路由表、`OwnsRoute`/`RegisterRoutes`/`serve`（`handler.go:13-117`），十一个端点已分别在 `session.go:13,33,48,59,70`、`accounts.go:9,21,33,45`、`passwords.go:9,21,33`。同包的 `handler.go:2` 已按新布局写对。
- 维护影响：`protocol.go` 是协议管道，改错误映射或 Cookie 的人正是最需要顺藤找到端点的人；指错一次要多翻一遍包。
- 最小修正：与 `handler.go:2` 对齐，改为"端点按线见 session.go、accounts.go、passwords.go"。

**R3 · 清理用例的注释与它实际的控制流不符**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/cleanup_test.go:41`（关联 `:17-37` 的 seed 循环、`store.go:158,192`）
- 代码事实：注释写"一次登录写入新会话，触发会话清理；一次签票触发票据清理"。但 seed 用 `tx.InsertSession` 逐条插入，而 `InsertSession` 本身就先删到期行（`store.go:158`）：插入 `e002` 时删掉 `e001`，插入 `a001` 时删掉 `e002`；票据同理，插入 `ticket-alive` 时删掉 `ticket-expired`（`store.go:192`）。到 `:42` 的 `Login` 与 `:45` 的 `IssueTicket` 执行时，四条到期行已不存在。
- 维护影响：注释描述的因果链在代码里不成立，改清理触发点的人会据此判断"登录路径已被覆盖"；这类注释比没有注释更贵。
- 最小修正：二选一——(a) 改注释，说明 seed 的插入本身即触发清理，`Login`/`IssueTicket` 是第二次触发；(b) 到期行改用 `db.Create(&sessionRow{...})`/`db.Create(&ticketRow{...})` 直接落库绕过 `Tx`，让 `Login`/`IssueTicket` 成为唯一触发点，注释即可原样保留。（用例的证明力属正确性，另见"转代码评审" T1。）

**R4 · 两条分页线对 `Items` 的空值给出两种写法，且各自的初始化都被随后覆盖**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/accounts.go:141,157` 与 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/passwords.go:167,201`；类型定义在 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/model.go:192-200`
- 代码事实：两者都以 `out := AccountPage{Items: []Account{}}` / `EventPage{Items: []PasswordEvent{}}` 开头（为错误路径保证非 nil），但成功路径一个写 `out.Items = append(out.Items, rows...)`（`accounts.go:157`），另一个写 `out.Items = rows`（`passwords.go:201`）——后者在 `rows` 为 nil 时把初始化的非 nil 保证又丢掉。`accounts.go:157` 的 `append` 是本轮 `Repository.Accounts` 改返回 `[]Account`（`model.go:224`）后残留的形状：`rows` 已是目标类型，`append` 到空切片只是多一次拷贝。`AccountPage`/`EventPage` 的注释只说明了 `NextCursor` 为空的含义，没有交代 `Items` 是否可能为 nil。
- 维护影响：两个相邻同类函数用两种写法表达同一件事，读者必须逐行确认差异是否有意；`Items` 的 nil 语义未定，下一个消费方（RBAC/账号页）要么多写一次判空，要么踩到 `null` 与 `[]` 的序列化差别——当前只是因为 HTTP 层 `toAccountPage`/`toEventPage`（`http/dto.go:96-102,145-151`）都用 `make(...,0,len)` 兜住了，业务类型本身并无这个保证。
- 最小修正：统一成一种写法并在 `model.go:191` 的注释里加一句 `Items` 恒非 nil（推荐把 `passwords.go:201` 改为 `out.Items = append(out.Items, rows...)`，两行都保住非 nil 保证，不依赖某个 `Repository` 实现的返回习惯）。

**R5 · "删到期行"被写成两份实现，且到期边界与业务层判定差一个等号**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:158` 与 `:192`（关联同文件 `:201`、`:256` 与 `/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/core.go:139`）
- 代码事实：`InsertSession` 与 `InsertTicket` 各写一行 `t.db.Where("expires_at < ?", time.Now().UTC()).Delete(&sessionRow{}/&ticketRow{})`，除模型类型外完全相同，互不注明。到期口径三处不一致：业务层 `verify` 用 `!v.ExpiresAt.After(at)`（即 `<= at` 为过期，`core.go:139`），`ConsumeTicket` 用 `expires_at > ?` 为存活（即 `<= at` 为过期，`store.go:201`），而两处清理用 `<`——`expires_at` 恰等于当前时刻的行已被判定为过期，却不会被清理。
- 维护影响：编码规范硬规则要求"同一条规则只在一处实现，多处需要时抽成包内函数并互相引用"。改保留策略（例如加宽限期、改成按批删）要同时找到两行；边界的一个等号之差无注释说明是否有意，下一个读者要自己回推是笔误还是刻意。
- 最小修正：在 `transaction` 上抽一个私有方法并让两处调用它，同时把比较改成与判定口径一致的 `<=`：

  ```go
  // sweepExpired 删除已到期的行：写入新会话/票据时顺带执行（FIND-020）。
  // 口径与 identity 的过期判定一致：expires_at <= 当前时刻即为过期，含已撤销/已消费的行。
  func (t *transaction) sweepExpired(model any) error {
      return t.db.Where("expires_at <= ?", time.Now().UTC()).Delete(model).Error
  }
  ```
  （清理所用时钟的来源另见"建议取舍项 S1"，那一条要动接口签名，与本条分开处置。）

**R6 · 新增的上游改动登记把已不存在的根 `docs-zh/` 列为自有目录**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/product/上游改动/README.md:5`
- 代码事实：该行写"`ee/`、`product/`、`workbench/`、`docs-zh/` 是我们自己的目录，不属于登记范围"。仓库根不存在 `docs-zh/`（`glob docs-zh/*.md` 无命中），其内容已在 `product/docs-zh/`（`product/README.md:47` 记录了这次搬迁，根 `AGENTS.md:1038` 明确"不要重新创建根目录 `docs-zh/`"）。同行已经列了 `product/`，该项既过时又冗余。
- 维护影响：这份文件是"同步上游前必读"的唯一清单（`product/README.md:14`），开篇就给出一个不存在的路径，会让读者以为根目录还该有 `docs-zh/`，与 AGENTS.md 的禁令直接冲突。
- 最小修正：删掉 `、docs-zh/` 三个字符。（同文件的其余事实抽查通过：`:41` 声称的 `ui/app/globals.css` 一行 `@source` 实存于 `ui/app/globals.css:10`；`:102-108` 的 7 个新增文件全部存在。）

### 建议取舍项（附反证条件）

**S1 · 清理用的时钟由存储层自取，与同模块"时间由业务层传入"的口径不一致 → 挂账**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/persistence/store.go:158,192`
- 代码事实：两处清理用 `time.Now().UTC()` 自取时钟。同一文件里性质完全相同的既有清理——`ReserveLogin` 顺带删过期限流桶（`store.go:256`）——用的是调用方传入的 `at`；`Tx` 的其他时间相关方法（`RevokeSession`、`RevokeSessions`、`ConsumeTicket`，`identity/model.go:240,241,246`）同样由业务层传时间，业务层只有 `core.go:81` 一个 `now()`。整理后 `persistence` 生产代码出现了第二个时钟源（`grep time.Now() ee/` 佐证：生产代码仅 `store.go:158,192` 与 `branding/persistence/store.go:55`）。
- 维护影响：同一事务内两个时钟；将来要做可注入时钟（测试/多节点时钟漂移排查）时，这两行是唯一不受控的点。
- 最小修正：给 `Tx.InsertSession`/`InsertTicket` 各加一个 `at time.Time` 参数，由 `session.go:72,107` 传 `now()`。
- **反证条件**：方案 §6"公共接口白名单"逐项列了 `Tx` 的签名变化范围，`InsertSession`/`InsertTicket` 不在其中——改签名属范围变化，需另行报备。若认定白名单在本期冻结，本条不在本轮做，按下面的四要件登记台账（R5 的抽函数已把时钟读取收敛到一处，先行止损）。
- 台账条目（建议登记 `workbench/findings.md`，编号取台账当前最大 FIND-020 之后）：
  ```yaml
  id: FIND-021
  status: 观察中
  triggered_by: [身份模块整理]
  evidence: "ee/internal/identity/persistence/store.go:158,192 的到期清理自取 time.Now().UTC()，
    而同文件 ReserveLogin(:256) 与 Tx 的 RevokeSession/ConsumeTicket 均由业务层传 at；
    identity 侧只有 core.go:81 一个时钟。改 Tx 签名超出方案 §6 白名单，本期不做；来源:收敛评审1 取舍项 S1"
  convert_when: "下次需要可注入时钟（测试或时钟漂移排查），或 Tx 因其他原因要改签名时一并收口"
  ```

**S2 · `State`/`RequireAccountManager` 提升到三条线后，外部调用点各选一条、无惯例 → 采纳即当轮改，撤回则本记录留证、不立账**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/core.go:195,201`
- 代码事实：两个方法挂在 `*core`，经嵌入提升到三个服务，于是 `SessionService.State`、`AccountService.State`、`PasswordService.State` 全部可调且语义相同。外部调用点各按各的：`http/session.go:19` 的 `status` 端点用 `h.svc.Session.State(r.ctx)`（读的其实是身份初始化状态，不是会话状态），`host` 的 `legacyImporter` 把 `State` 声明在账号线上并由 `app/identity.go:78` 注入 `svc.Account`，测试夹具 `persistence/service_test.go:66-68` 又把 `State` 转发给 `SessionService`。
- 维护影响：`Services` 的三条线读起来像互斥的能力划分，实际有两个方法横跨三条；新增调用点时没有惯例可循，`svc.Session.State(...)` 这种写法还会误导读者以为返回的是会话状态。
- 最小修正：在 `core.go:195`、`:201` 的方法注释各补一句"经嵌入提升到三条线，外部从任意一条调用等价"；或统一外部读 `State` 的入口到账号线（`http/session.go:19` 改 `h.svc.Account.State`）。
- **反证条件**：若 RBAC 期计划把 `State` 读取收敛到独立的策略/状态服务（`model.go:256` 的 `AccountPolicy` 已预留替换点），则现在统一口径属白做，只补注释即可；三条线内部都确实用到 `State`（`accounts.go:32`、`passwords.go:175`、`core.go:206`），说明它本就是共享判定，不宜下沉到单条线。

**S3 · `page` 的第二个返回值原样回传入参 → 采纳即当轮改，撤回则本记录留证、不立账**
- 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-auth/ee/internal/identity/core.go:215-230`
- 代码事实：`func page(cursor string, limit int) (Cursor, int, error)` 校验 `limit` 范围后 `return c, limit, nil`，第二个返回值与入参恒等；两个调用点 `accounts.go:145`、`passwords.go:189` 都写成 `c, n, err := page(cursor, limit)` 然后用 `n`。
- 维护影响：多出来的返回值暗示这里会做钳制或默认值替换（实际没有），读者要跳进函数体才能确认 `n == limit`；本轮刚把它提升为两条线共用的公共规则，形状问题会被复制到后续每条分页线。
- 最小修正：改成 `func page(cursor string, limit int) (Cursor, error)`，两个调用点直接用 `limit`。
- **反证条件**：若打算把 HTTP 层的默认值兜底（`http/dto.go:41-46` 的 `pageLimit`）下沉进 `page`，让它真正承担"解析并归一化分页参数"，那么第二个返回值是预留位，保留更省事——此时应反过来把 `pageLimit` 合并进来，而不是维持现状。

### 需用户裁决的 Blocking

- 无。六条修复项均在授权范围内，不触及范围、契约与风险取舍。

### 立债(REF)

- 无。

### 挂账(Finding)

- 见 S1 的 `FIND-021` 候选条目（四要件已齐）。是否登记取决于 S1 的取舍结果：当轮采纳则不登记。

### 转代码评审(范围外发现，一行一条，不出结论)

- T1：`ee/internal/identity/persistence/cleanup_test.go:42-66` 的断言在 seed 循环结束时即已成立（四条到期行在 seed 内被逐次清掉），请核该用例对"登录/签票触发清理"的证明力是否达到预期。
- T2：`ee/internal/identity/persistence/store.go:158,192` 把一次无索引全表 DELETE 放进持有 `state` 写锁的事务（`store.go:225`），与 FIND-018 记录的"每次登录抢全局写锁"叠加，请核容量与并发影响是否仍在接受范围。
- T3：到期边界 `expires_at < now`（`store.go:158,192`）与业务判定 `<=`（`core.go:139`、`store.go:201`）相差一个等号，请核是否存在可观察的行为差异（预期无）。

## 台账对账

- **FIND-017**（busy 重试两份）：本轮未触及 `branding/persistence/migration.go` 与 `identity/persistence/diagnostics.go`，未恶化，继续观察。
- **FIND-018**（`ReserveLogin` 抢 `state` 写锁）：未关闭。本轮 FIND-020 在同一写事务内新增一次无索引全表 DELETE（`store.go:158`），理论上延长写锁持有时间；容量与并发判断属代码评审范围，已按 T2 转交，本评审不下结论。
- **FIND-019**（哈希槽耗尽与登录限流共用 `ErrLimited`）：已接受/豁免，本轮未触及 `protocol.go:121-123` 的映射，不重提。
- **FIND-020**（会话/票据无删除路径）：已转 Case，本轮实现。核对结果与 §4.6 拍板一致——到期即删且含已撤销（`store.go:158,192` 只按 `expires_at` 过滤，不看 `revoked_at`/`consumed_at`）、未加索引（`migration.go:46-53` 的七条索引与基线一致，无 `expires_at` 索引）、迁移 ID 仍为 `ee_identity_v1`（`migration.go:14`）、`model.go:139` 的注释已同步。实现口径正确，R5/S1 针对的是它的写法而非口径；R5 关闭后此条可由实施者在台账关账。
- **无新增 Finding**（S1 的 `FIND-021` 视取舍结果而定）。

### 看似可改、但有理由保留（写明理由，下一轮不再重提）

- **P1 · `identityhttp.Handler` 持具体的 `*identity.Services` 而非窄接口**（`http/handler.go:23,29`）：与 `host` 刚做的窄接口收窄形成对比，但 Handler 的使用面几乎等于三条线的全部导出方法（11 个端点覆盖 16 个方法），窄接口只是把方法表抄一遍；且 `identityhttp` 与 `identity` 同属一个业务模块，架构 §5 只要求适配子包单向依赖业务根包，已满足。保留。
- **P2 · 两条分页线的尾部四行结构重复**（`accounts.go:153-157` 与 `passwords.go:197-201`）：元素类型与游标字段都不同，泛型化需要额外传一个取键函数，收益不抵可读性损失；共享部分（`page`、`encodeCursor`、`beforeCursor`）本轮已经收敛到单份。保留（其中 `Items` 赋值的不一致按 R4 单独处理）。
- **P3 · 响应文案常量留在 `handler.go:16-18`**（`messagePasswordChanged` 只被 `passwords.go:18` 使用）：三条文案集中一处便于统一口径，分散到三个端点文件反而要三处找；`handler.go` 承载"端点共同"的部分，与文件注释不冲突。保留。
- **P4 · `store.go` 317 行，超过"300 行须评估"线**：本轮净增约 8 行。评估结果是不拆——读取、事务、限流、分页共用 `notFound`/`conflict`/`beforeCursor`/`authRecord`，行结构已经在 `rows.go`，再按"读/写"切会让一次事务的完整路径跨两个文件。保留。
- **P5 · `service_test.go` 497 行**：方案 §3 已把它的拆分列为本期非目标，不重提。仅记录事实：距 500 行硬线 3 行，下一次往该文件加用例时必须先拆（本轮新用例已按此放进 `cleanup_test.go`、`accounts_page_test.go`，做法正确）。
- **P6 · `core` 的方法接收者仍名 `s`**（`core.go:110,120,125,131,166,179,187,196,202`）：`service.go → core.go` 改名后的残留，改成 `c` 更贴类型名，但要动 9 处、零功能收益，且 `s` 在整个包里指代一致，不会误读。保留。
- **P7 · `OwnsRoute` 对 16 条路由线性扫描**（`http/handler.go:73-80`）：每个管理请求都会走一次（`host/auth.go:221`），但表小且是编译期常量数组，换 map 反而要多维护一份键构造。保留。

## 实际阅读范围与未执行的检查

**读了什么**：见"事实来源"的三段清单（范围内文件全部读完整现状，不只读 diff；另读了 protocol.go、migration.go、账号认证接口文档、冒烟脚本相关段、globals.css 一行作为对照与证实）。**同类实现对照**：`branding` 模块的分层与注册形状（经全量 import 图与 `bootstrap.go:55` 的 `RegisterRoutes` 调用核对），`identity/http/handler_test.go` 的测试职责（经函数名与文件注释核对 README 说法）。**关键假设的证实**：FIND-020 未加索引、未改迁移 ID，是读 `migration.go:14,41-53` 原文确认的，不是据方案推断；"根 `docs-zh/` 不存在"是 glob 实测；"`globals.css` 的 `@source` 行仍在"是读原文第 10 行确认。

**未执行的检查**（无一条被当成通过）：

1. **逐 hunk 的 diff 未取得**。本会话 Bash 在第 4 条命令后被权限模式拒绝（`git diff <路径>`、`git show <rev>:<path>`、`git log` 均返回 denied），因此没有读到任何一行 `-`/`+` 形式的差量。新旧定位改由"`git diff --stat` 的文件级增删 + 方案 v4 §5/实施记录的逐步骤说明 + 完整现状阅读"三者交叉完成；涉及"这一行是不是本轮新增"的判断（R4 的 `append` 残留、R5 的两处清理、R2/R3 的注释）都另有代码内自洽证据支撑，不依赖差量。
2. **没有编译、`go vet`、`go test`、`gofmt -l`、预飞**（只读评审，且 Bash 不可用）。测试是否通过、覆盖是否可信不属本评审结论，测试代码只按四维看了组织与重复。
3. **没有跑 `dupl`/`deadcode` 等机械工具**；重复判断来自逐文件阅读与定向 grep。
4. **`product/上游改动/README.md` 的"7 个修改文件、增删行数"未与上游实际差量核对**（需 `git diff $(git merge-base develop upstream/dev) develop`，Bash 不可用）。只证实了两件事：`ui/app/globals.css:10` 的 `@source` 行确实存在；表中 7 个"新增文件"全部存在于工作树。这份登记是否**遗漏**了其他被改的上游文件，本轮未能验证。
5. **未读的相关文件**：`ee/internal/identity/persistence/diagnostics.go`（未改，仅经 `logDatabaseFailure`/`retryBusy` 的调用点判断其接口）、`ee/internal/branding/**` 的正文（仅经 import 图与 README 对照形状）、`ee/internal/identity/http/handler_test.go` 正文（仅 grep 函数名）、`workbench/cases/身份模块整理/期1/checklist.yaml` 与预飞事实表（评审不依赖其结论）。
6. **按规则未读**：旧代码评审、旧收敛评审、先前会话意见（首轮，无"转交"栏输入）。已读且已遵守的裁决输入：`workbench/findings.md` 全文、方案 v4 §9「Gate 决策结果」与「实施偏差记录」1–6。
