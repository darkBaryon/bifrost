---
title: UI完整中文化 期1 验收记录
type: 验收记录
case: UI完整中文化
phase: 1
created: 2026-09-02
---

## 验收结论

**通过**。Gate 2 于 2026-09-02 由用户拍板放行(第二次请求;第一次 2026-08-28 不放行,裁决 B 补齐阶段C 后重新请求)。放行后验收核对中又发现并当场修复一处扫描盲区(见「交付物核对」第 4 项与方案实施记录第 7 小节),修复后 preflight 重跑全绿,一并计入本期交付。

## 交付物核对

对照需求「验收口径」逐项:

1. **覆盖率脚本可执行,清单为空(白名单除外)** —— ✅ `node scripts/zh-coverage.mjs --check` exit 0,清单 0 条。scanner 含 8+ 提取通道(①/①c/①d/②/②b/③/④/⑤/⑤b/⑤c/⑥/⑦/⑦b/⑧),self-test **54** 项全绿(fixture + 对全部 34 条生产 RULES 的固定点/互证/恰中机械遍历)。
2. **动态句样例翻译正确** —— ✅(带口径说明)。需求点名的 4 个样例句式(`N keys found`/`N items`/`N minutes ago`/`Showing X of Y`)经全仓 grep 核实**在 UI 源码中并不存在**,属立案时对句式的猜测;实际存在的动态句(`3 min ago`→`3 分钟前`、`in 5 minutes`→`5 分钟后`、`3 model budgets`→`3 个模型预算`、`HTTP error! status: 500` 等 34 条规则)全部经 `makeTranslate` 程序化验证命中,且 RULES 四字段契约(re/out/skeleton/sample)由 self-test 机械互证。
3. **tsc 全绿 + build 成功** —— ✅ preflight 2026-09-02 重跑:ui-typecheck / ui-build / scanner-selftest / coverage-check / 产物·符号·禁留全部通过(`预飞事实表.txt`)。
4. **人工巡检无用户可见英文残留(白名单除外)** —— ✅(带诚实边界)。浏览器巡检两轮:第一轮(阶段B 后)发现 5 类机制盲区触发 Gate 2 驳回;第二轮(阶段C 后,30 页 = 20 盲区实例页 + 原 10 页,三轮收敛)终态残留全部属方案 §3 既定非目标(证据:`evidence/UI完整中文化-期1-浏览器复巡检2-阶段C终态.md`)。本次验收核对再抽查发现**语义后缀键**盲区(`xxxLabel:`/`emptyMessage=` 等 52 条,含 mcpClientForm OAuth 表单整页 label/tooltip),当场按裁决 B 先例修复(⑤c/②b 通道 + 49 词条 + 3 白名单)。已知遗留(记 Finding,非目标或翻译层不可达):115 条跨节点碎片、`alt=` 属性(运行时 ATTRS 不含 alt)、代码示例 `content:` 键、启发式非 AST 的固有枚举不完备。

最终规模:DICT **3047** 词条(独立数据文件 `ui/lib/zhDict.json`,一键一行按键排序)、RULES **34** 条、白名单 **528** 行(全部带理由标签)、self-test **54** 项、coverage 清单 **0**。

## 验收方式

**实测 + 抽查**:preflight 全套命令本会话实际重放(非引用历史结论);动态句样例用生产 `makeTranslate` 逐条求值;验收抽查用反证探针(任意键位英文串全量扫描 → 同口径求值)主动找"清零但不可见"形态,并当场消化其产出;浏览器人工巡检由用户在 localhost:3000 亲验后拍板。

## 度量(标准级以上)

| 阶段 | 模型 | 起止时刻 | token |
|---|---|---|---|
| 计划 | Claude(会话) | 2026-08-18 | 缺失 |
| 评审 | 方案评审×3 + 代码评审×2:Claude 独立代理;代码评审3:Codex(跨厂商,用户驱动) | 2026-08-18 ~ 2026-09-02 | 缺失 |
| 开发 | Claude(会话)+ 用户在 Codex 并行(评审3 阻断修复) | 2026-08-18 ~ 2026-09-02 | 缺失 |
| Gate | 用户拍板 | Gate1 2026-08-18;Gate2 驳回 2026-08-28 / 通过 2026-09-02 | 缺失 |

## 归档动作

- 方案v4 status → 已归档 ✅
- 需求 status → 已交付 ✅
- 长期口径回填:无(zhLocale 维护口径已写入 `zhLocale.ts`/`zhDict.json` 文件头注释与方案 §4,scanner 用法写入脚本头注释)
