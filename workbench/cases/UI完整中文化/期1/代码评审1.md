---
title: UI完整中文化 期1 代码评审 #1
type: 代码评审
case: UI完整中文化
phase: 1
round: 1
verdict: 需修改
created: 2026-08-18
---

> 覆盖 commit 范围:`57a9c5a`…`2ed92d9`(阶段A,12个commit)。独立复核代理执行,未参与实施。

## 结论

**需修改**,阻断 2 条。机制设计与方案 v4 §4 接口一致、1988 词条抽样质量扎实,但 scanner 存在系统性提取盲区,且实施记录里的一条"已验证"结论经不起复验。

## 对照方案逐 commit 核查

四类提取 + 三重围栏结构与方案接口逐条对应;RULES 四字段契约、收敛契约①②、attributes 观察均按 §4 落地。问题不在架构,在两处实现细节。

## 可证伪质疑(阻断)

1. **channel①(原①b)只处理"同行文本"与"紧邻单行独立文本",漏掉"多行连续纯文本被闭合标签终止"这一常见 JSX 写法**——真实 DOM 折叠多行为单个文本节点,scanner 只取第一行当字典 key,系统性截断。实测 5 处直接验证(如 `observabilityView.tsx:68-69`)+ 71 处高度疑似,覆盖面广。
2. **实施记录声称"旗舰样例(providerConfigCard.tsx:186)已验证运行时正确"不成立**——用 `parseZhLocale`/`makeTranslate` 直接对生产 32 条 RULES 求值 `"3 model budgets"` 返回 `null`,RULES 里根本没有覆盖该骨架的规则。附带发现 self-test 的固定点/互证断言只测孤立硬编码样例,从未触达生产数据,安全网空转。

## 验证记录

独立复跑 tsc/build/self-test/coverage-check,机械层面通过;但对 diff 与实施记录的语义核对(非机械)发现上述两处。详见方案v4实施记录"独立代码复核"章节全文转录。
