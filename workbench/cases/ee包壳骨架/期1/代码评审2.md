---
title: ee包壳骨架 期1 代码评审 #2
type: 代码评审
case: ee包壳骨架
phase: 1
round: 2
verdict: 通过
created: 2026-09-04
---

## 结论

**通过**(差量复核)。范围:提交 `5ccc64d79`(`1e8c2f75d` 之后唯一一个提交),只核 代码评审1 六条建议的落地与是否引入新问题;未修改区域不受影响(改动限于注释、文档、一行 replace 删除与冒烟脚本前置守卫,不触及任何 Go 逻辑与路由)。六条建议全部落实;发现 **1 处新引入的小问题**(cp 守卫在 `trap cleanup EXIT` 之前退出,空临时目录不清理)和 1 处遗漏的注释(smoke.sh 第 2 行仍写 T1-T7),均非阻塞。

## 对照方案逐 commit 核查

`5ccc64d79` 非实施步骤,是评审后修正。对照 代码评审1 建议逐条:

| 建议 | diff 实际 | 落实 |
|---|---|---|
| 1 订正 Branding/License「点保存报错」 | 07 文档「企业模式差异」改为「渲染的仍是上游 `_fallbacks` 的占位页(无表单,不会调用 PUT/DELETE;update/reset branding 的 mutation 无调用方)」,与 `ui/app/workspace/config/branding/page.tsx:2`、`_fallbacks/enterprise/components/branding/brandingView.tsx` 事实一致;方案 §3/§8 正文按规矩不改,改为偏差记录第 7 条说明 | 是 |
| 2 smoke.sh cp 守卫 + Makefile 注释 | `smoke.sh:12-13` 两条 cp 分别 `\|\| { echo "missing …"; exit 1; }` / `\|\| exit 1`;`Makefile:42` 注释改「9 条断言 T1-T8」 | 是(见质疑 1、2) |
| 3 07 文档 placement/order 含义 | `SyncLoadedPlugin` 行补「传 nil = `post_builtin` 组、order 0(lib/config.go:6121-6128)」,与上游 `SetPluginOrderInfo` 代码一致 | 是 |
| 4 删 jsonparser replace | `ee/go.mod` 删 `plugins/jsonparser => ../plugins/jsonparser` 一行;`grep -c jsonparser go.mod` = 1(剩 `buger/jsonparser` 间接依赖,应保留) | 是 |
| 5 sync.sh 删 `n=0` | 已删 | 是 |
| 6 smoke.sh 前提补上游 ui 目录 | 头注释第 3-4 行补「上游 transports/bifrost-http/ui/ 目录存在」;07 文档踩坑节补一条 | 是 |

实施记录第 6 行「评审后修正」、偏差记录第 7、8 条与 diff 三方一致;第 5 行 commit 由「本提交」补为 `1e8c2f75d`,正确。第 6 行的 commit 列写「评审后修正」而非 SHA(提交时无法自引),可在下一次动方案文件时补 `5ccc64d79`。

## 接口面对账

本提交无新增/删除导出符号、路由、表、插件;go.mod 依赖图不变(`GOWORK=off go mod tidy -diff` 无输出)。

## 预飞事实表

差量轮不重跑完整预飞;亲核与本提交相关的机械项:`ee-build/vet/test`、`GOWORK=off` 构建、`worktree-clean`(见验证记录)。上轮全绿项未受影响的依据:改动文件仅 07 文档、Makefile 注释、go.mod 一行 replace、smoke.sh 前 13 行、sync.sh 一行删除、workbench。

## 可证伪质疑

1. **cp 守卫在 trap 之前**(新引入,非阻塞):`smoke.sh:11` `mktemp -d` 后 `:12-13` 守卫 `exit 1`,而 `trap cleanup EXIT` 在 `:21` 才注册,守卫路径不会 `rm -rf "$TMP"`。实测 `APP_SRC=/nonexistent/config.db bash ee/scripts/smoke.sh` → 退出码 1(建议 2 的目标达成),端口无占用,但 `$TMPDIR/tmp.YsB6CbeFUb/{ee,oss}` 两个空目录残留(mtime 与运行时刻一致,评审后已手工删除)。修法二选一:把 `trap cleanup EXIT` 提到 `mktemp` 之后 cp 之前(cleanup 对空 PIDS 与未监听端口都是安全的),或守卫里加 `rm -rf "$TMP"`。
2. **smoke.sh 第 2 行注释仍写「跑 T1-T7」**:Makefile 注释已改为 T1-T8,脚本自身头注释漏改(cosmetic)。
3. **删 jsonparser replace 是否影响构建**:`cd ee && go build/vet/test ./...` 与 `GOWORK=off go build ./...` 均通过,`tidy -diff` 零漂移 —— 不影响。
4. **07 文档新增描述是否准确**:「update/reset branding 的 mutation 无调用方」—— 上轮已核 `grep -rln useUpdateBrandingMutation\|useResetBrandingMutation ui` 只命中 `brandingApi.ts` 自身;「placement/order 传 nil = post_builtin、0」—— `lib/config.go:6121-6128` 逐字对应。准确。
5. **方案 v3 正文是否被改**:diff 只动「实施记录」与「实施偏差记录」两节,§3/§8 原文未改,符合「旧版冻结」规矩。

## 建议(非阻塞)

1. `smoke.sh`:`trap cleanup EXIT` 上移到 `TMP="$(mktemp -d)"` 之后、cp 之前;或守卫里 `rm -rf "$TMP"`。
2. `smoke.sh:2` 注释「T1-T7」→「T1-T8」。
3. 方案 v3 实施记录第 6 行 commit 列补 `5ccc64d79`(下次动该文件时顺手)。

## 验证记录

全部在 `/Users/xinyue/VSCode/ws_2026/bifrost`(develop,HEAD `5ccc64d79`)亲自执行,2026-09-04:

| 命令 | 结果 |
|---|---|
| `git show 5ccc64d79 --stat` | 13 文件,+260/-9;ee 与 docs-zh 侧仅 07 文档、Makefile、go.mod、smoke.sh、sync.sh |
| `git show 5ccc64d79 -- ee docs-zh 方案v3.md` | 逐行读,内容如上表 |
| `cd ee && go build ./... && go vet ./... && go test ./...` | 全部通过(handlers、lib ok) |
| `cd ee && GOWORK=off go build ./...` | 通过 |
| `cd ee && GOWORK=off go mod tidy -diff` | 无输出,exit 0 |
| `grep -c jsonparser ee/go.mod` | 1(仅 `buger/jsonparser` 间接依赖) |
| `lsof -nP -iTCP:18080 -iTCP:18081`(实测前) | 空 |
| `APP_SRC=/nonexistent/config.db bash ee/scripts/smoke.sh` | 输出 `cp: … No such file or directory` + `missing /nonexistent/config.db`,退出码 1;未起任何服务 |
| `lsof -nP -iTCP:18080 -iTCP:18081`(实测后);`ls -d $TMPDIR/tmp.*` | 空;残留 1 个空目录 `tmp.YsB6CbeFUb/{ee,oss}`(质疑 1),已删除 |
| `grep -n "^cp \|^trap \|mktemp" ee/scripts/smoke.sh` | mktemp :11、cp :12-13、trap :21 |
| `git status --porcelain --untracked-files=no` | 空(无工具链漂移) |
