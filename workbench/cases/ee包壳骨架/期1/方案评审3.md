---
title: ee包壳骨架 期1 方案评审 #3
type: 方案评审
case: ee包壳骨架
phase: 1
round: 3
verdict: 通过
created: 2026-09-04
---

## 结论

**通过**。v3 相对 v2 的实际差异只有 4 处(§4.3 :96、§6 :131、§6 :136-138、§7 :144,`diff` 逐行核对),与「相对 v2 的变化」声明一致;评审 2 的两个阻塞项按实测结果改正,四条建议全部落实;checklist.yaml 未变(md5 d8e761c2…,47 行,与评审 2 核过的版本相同);需求文件验收口径已同步。未引入新问题。

### Gate 1(供用户拍板)

- **放行对象**:ee包壳骨架 期1,方案 v3,基线 `d4ee8f175`(develop,合并 upstream/dev c05bc0e21 之后);checklist.yaml 为预飞合同。
- **已确认的目标**:需求目标 1-5——`ee/` 独立模块(镜像上游路径、纳入 go.work、14 行 replace 可独立构建);三个插座各打通一次(`ConfigStore.DB()` 建 `ee_probe`、`SyncLoadedPlugin` 注册 no-op `ee-probe`、Router 上加 `GET /api/ee/ping`)外加 Bootstrap 前注入的 ShellRewriter 与 `GET /api/branding` 默认值;UI 覆盖层替换 audit-logs 占位组件;一条命令出带 UI 的二进制、`make -C ee dev`;插座可用性按「实测/静态确认/不可用」三档写进 docs-zh/04-开发指南。
- **已确认的范围/非目标**:不改上游任何文件;不设 `IsEnterprise` ctx 标记;不碰权限;无业务功能;Branding 只读默认值(PUT/DELETE 不做);不做集群/license/打包分发/热重载。
- **需用户决定的事项**:无(企业模式 UI 已于 2026-09-04 裁决接受;其余为技术取舍,方案内已定)。
- **抽查入口**(1-3 个):
  1. `ee/transports/bifrost-http/server/bootstrap.go`——prepare 是否只设 `ShellRewriter`、attach 四步顺序是否与 §4.2 一致、ConfigStore 为 nil 是否报错退出;
  2. `make -C ee smoke` 的 T1/T6/T7 输出——providers 集合相等、`/api/branding` 为 JSON 而 OSS 对照为 text/html、`GET /` 含 `x-bifrost-ee` meta;
  3. 浏览器打开 `/workspace/audit-logs` 看到 ee 占位文案,并对照 §2 差异清单点一遍 Security 页。
- **最大剩余风险**:控制台进入企业模式后,License/Branding 页与 Security 页新增区块背后的 API 在 ee 里尚不存在,页面上点保存会报错——已接受并写进差异清单,靠人工核对与文档兜底,不是机械检查能拦的;其次是上游 `make dev`/`make install-ui` 分别抹掉 go.work 里的 ee 与覆盖层,靠幂等目标与文档提醒。
- **通过 / 不通过分别发生什么**:通过→ 按 §5 五步实施,每步一个 commit,完成后跑 preflight 得事实表,再进代码评审与收敛评审(Gate 2);不通过→ 方案回 v4,`ee/` 不落一行代码,现有仓库零变化。

## 逐章核查

差量评审,只核 v3 相对 v2 的 4 处改动与两份关联文件。

| 位置 | v3 改动 | 核对 |
|---|---|---|
| §4.3 :96 | workspace 目标改为 `cd .. && (grep -qxE '\s*\./ee' go.work \|\| go work use ./ee)`,注明 `make -C ee` 的 cwd 是 ee/ | ✓ 关闭评审 2 B1。在临时工作区从 ee/ 下按原文执行两次:第一次加入 `./ee`,第二次不变,rc=0。 |
| §6 :131 | 冒烟总则「OSS 对照一律不比状态码」,引用 ui.go:64,139-145 | ✓ 与评审 2 实测(所有未知 `/api/*` 200 text/html)一致;行号正确(:64 catch-all,:139-145 回退 index.html)。 |
| §6 :136 T5 | `sqlite3 <app-dir>/config.db "select count(*) from config_plugins where name='ee-probe'"` 期望 0 | ✓ 关闭评审 2 B2 之表名项。对本机 config.db 只读执行:返回 0,rc=0(该表现有 0 行,列 `name` 存在)。 |
| §6 :137 T6 | ee 返回 `application/json` 且 `enabled=false`;OSS 对照 Content-Type `text/html` | ✓ 关闭评审 2 B2 之 T6 项。OSS 实测 `text/html; charset=utf-8`,子串匹配成立。 |
| §6 :138 T7 | 注明须用 build 产物验,dev 构建走 vite 代理绕过 ShellRewriter(ui.go:69-71) | ✓ 落实建议;行号正确。 |
| §7 :144 | 预飞前置补「先跑 build_views.py 并把生成的视图随实施提交」 | ✓ 落实建议。 |
| 需求文件 验收口径 | 改为 `bifrost-http`、覆盖层定义、`/api/branding` 与 ShellRewriter 标记、diff 口径改为「本仓既有差异 + 新增 ee/(checklist upstream-untouched 为空)」;处置历史加第 4 条 | ✓ 落实建议,与 v3 §4.1/§6/§7 一致。 |
| checklist.yaml | 未变 | ✓ 头注仍写「方案v2 §7」,仅标签,不影响执行(见建议)。 |
| 其余章节 | `diff` 无差异 | ✓ v2 评审已核部分不重复。 |

方案状态机:v2 已置 `需修改` 冻结,v3 `待评审`,符合「旧版冻结不覆盖」。

## 可证伪质疑

1. [已关闭] v3 的 workspace 命令从 ee/ 执行能否幂等地加入 `./ee`?——能(两次执行结果一致)。
2. [已关闭] T5 的 sqlite 命令在真实 config.db 上能否执行、表与列是否存在?——能,返回 0。
3. [已关闭] v3 是否只改了声明的地方?——是,`diff` 仅 4 处。
4. [未关闭,不阻塞] 评审 1 的 S11(vite dev 对逐文件符号链接的热更新)仍是实施第 3 步的确认项,未有实测。

## 建议栏(不立案)

- checklist.yaml 首行注释「方案v2 §7」可改为 v3(纯标签)。
- 实施第 1 步起服务前先执行 `make -C ee workspace`,并把 `plugins/routing/go.mod` 的工具链漂移 `git checkout` 还原,避免 preflight 首条就红。

## 验证记录

在 `/Users/xinyue/VSCode/ws_2026/bifrost/workbench`(HEAD d4ee8f175)执行:

| 命令 / 查看点 | 结果摘要 |
|---|---|
| `sed -n 1,30p cases/ee包壳骨架/期1/方案v3.md` | frontmatter version 3、status 待评审;变化声明 6 条 |
| `diff <(sed -n '18,$p' 方案v2.md) <(sed -n '/^## 给用户的摘要/,$p' 方案v3.md)` | 仅 4 处差异::96、:131、:136-138、:144 |
| `grep -n '^status' 方案v2.md 方案v3.md` | v2 需修改 / v3 待评审 |
| `md5 -q checklist.yaml`;`wc -l`;grep 四条关键项 | d8e761c2f3f814edd3e637dbe4141872,47 行,内容与评审 2 所核一致 |
| `sed -n '/## 验收口径/,/## 处置历史/p' reports/ee包壳骨架.md`;处置历史尾 3 行 | 验收口径 5 条已同步;处置历史含「方案评审2 需修改…出方案 v3」 |
| scratchpad `wstest`:go.work 重置为 `use ./core`,在 ee/ 下执行 v3 原文命令两次 | 第一次加入 `./ee`,第二次不变,rc=0 |
| `sqlite3 ~/.config/bifrost/config.db "select count(*) from config_plugins where name='ee-probe'"`(只读) | 0,rc=0;`select count(*) from config_plugins` 亦为 0 |
| `git status --porcelain --untracked-files=no \| wc -l` | 7(与评审 2 相同的已修改文件,本轮未新增) |
