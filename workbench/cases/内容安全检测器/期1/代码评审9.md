---
title: 内容安全检测器 期1 代码评审 #9
type: 代码评审
case: 内容安全检测器
phase: 1
round: 9
verdict: 通过
created: 2026-09-19
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 2b7e0e699 到候选 a1c75dcbdf5d5b12d66fb7898a63e8d2cf35aa77；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 已完成 `2b7e0e699..a1c75dcbdf5d5b12d66fb7898a63e8d2cf35aa77` 的独立差量复核：本轮仅修改 README、注释及私有函数形参名，执行逻辑未变，无需恢复全量评审。未发现必须修改的正确性问题。checklist 九条命令全部亲核通过，另完成内容安全包无缓存 race 和五组隔离 HTTP 故障验证。评审前后分支、HEAD、工作树和暂存区一致，无未跟踪文件。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 方案与实施声明 | 「第八轮评审修复记录」声明调整错误分流措辞、注释及形参名；diff 包含对应修改 |
| 实际代码差量 | README 增删各 1 行；`judge.go` 增 8、删 7 行，均为注释和 `code` → `status` 的一致替换 |
| 公共接口、配置、数据库、依赖 | 本轮无新增或变更 |
| 多做、少做、未登记偏差 | 未发现行为层面的新增偏差；契约措辞同步按第 5 节转交 |
| 验收标准 | EE vet/race/build、自包含 smoke、其余清单检查通过；真实 DeepSeek 为非必过项，本轮未重跑 |

受影响失败路径已追到最终消费方：

| 失败路径 | 最终结果 |
|---|---|
| provider 返回错误 | `judgeError` 保留安全诊断，检测器有界重试；耗尽后记为失败并执行 `on_error` |
| provider 伪造合成错误类型 | 只附加白名单类型名，不复制 code/message；本轮 HTTP 验证未泄露回显 |
| 网关自身错误或无状态码错误 | 保留网关诊断；既有回归检查无可用 key、取消与 UTF-8 截断 |
| 取消、超时 | context 错误向上传递，检测器停止重试，框架区分取消与检测超时 |
| 响应、候选或文本缺失 | 返回错误；空字符串在等级解析时失败，不作为无风险 |
| 客户端拒绝响应 | 插件构造固定安全错误，禁止 fallback，不透传判官错误原文 |

## 3. 本轮修复项

**无新增必修项。**

上一轮《代码评审8》「本轮修复项」逐项处置：

| 上轮条目 | 处置与证据 |
|---|---|
| 无新增必修项 | 无待关闭事项 |
| 延续 R1：provider 伪造类型导致 code/message 泄露 | **已修复，保持关闭。** [judge.go:131](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:131) 仅输出状态类别和白名单类型；[host_test.go:136](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:136) 检查三种伪造类型不泄露。本轮无缓存 race 和对应三组 HTTP 故障验证均通过 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无。**

收敛评审8转交的三条意见分别处置如下，均为撤回必修要求，不记作本轮修改了代码：

| 转交项 | 处置与依据 |
|---|---|
| 502/502/503 同属一档，503 没有可区分断言 | **撤回。** 503 没增加分类分支覆盖属实，但三者按 [judge.go:148](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:148) 本就同属 `http_unavailable`。循环还逐项断言不同的 `known_type` 和不泄露，并非只有类别断言。本轮进一步分别验证准确状态码、类型名和安全响应，全部通过 |
| 只有 429 场景断言状态码，删除拼接后另外五个场景仍绿 | **撤回。** 该局部覆盖事实成立；但状态码共用 [judge.go:113](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:113) 的拼接路径，[host_test.go:117](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:117) 已约束该行为，删除拼接会使整个测试失败。本轮另亲核 499/502/503 的准确状态输出。未修改源码做变异测试 |
| 非流式调用无法到达 `http_cancelled` | **撤回。** 内部取消构造点不是全部入口。provider 直接返回 HTTP 499 时，经 [openai.go:912](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/core/providers/openai/openai.go:912) 的非 200 响应处理进入错误解析，再到 [judge.go:146](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:146)。本轮真实候选二进制配本地 provider 实测得到 `status=499 category=http_cancelled`，共调用 4 次，最终返回 `content_safety_check_failed`。仓内未单列这一 HTTP 场景属实，“不可达且无法覆盖”不成立 |

## 5. 转收敛评审 / 挂账

- 转收敛评审：[README.md:101](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:101) 与 [judge.go:104](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:104) 的错误分流措辞、注释同步，沿用上轮转交，由收敛侧核对闭环，本侧不作结论。

挂账：无。

## 6. 开放式复读的表外发现

**已做开放式复读，无表外发现。**

逐文件重新核对故障发现与恢复：`judge.go` 保留状态及安全诊断，错误继续进入重试和失败策略；README 提供配置更新及启动失败恢复入口。本轮未引入新的恢复障碍。

## 7. 起手表覆盖

以下结果限定于本轮差量、上轮意见和受影响路径。

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | 无：必过项全部亲核；非必过真机项未重跑 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | 无行为差异；契约措辞转收敛 |
| 对照方案 | 多出的功能、公共接口、抽象或依赖 | 无 |
| 对照方案 | 偏差未登记 | 无新增 |
| 行为正确性 | 失败被吞或降级成假成功 | 无：五组故障均返回检查失败 |
| 行为正确性 | 空值、空集合与非空无分支 | 无：状态码、错误字段、响应和正文均有处理 |
| 行为正确性 | 取消或超时后仍写入或产生副作用 | 无新增：取消和重试逻辑未变；provider 继续执行属已声明限制 |
| 行为正确性 | 同一请求重发产生两份副作用 | 不适用：差量未新增业务写入或调用 |
| 行为正确性 | 事务外写后读、多方仅回滚一方 | 不适用：差量不涉及事务和配置发布 |
| 行为正确性 | 关闭、旧数据、旧客户端路径无处理 | 无新增：未配置、reset、重启及 FAKE 冲突 smoke 通过 |
| 行为正确性 | 昂贵检查先于廉价检查、校验缺层 | 无新增：执行顺序未变 |
| 行为正确性 | 路由保护、认证关闭时的行为 | 不适用：差量不涉及路由或鉴权 |
| 行为正确性 | 敏感信息进日志或响应 | 无：伪造类型及普通错误的回显均未泄露 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | 无 |
| 行为正确性 | 配置、环境依赖或部署步骤未登记 | 无 |
| 失败可观测 | 服务端无法追溯失败原因 | 无：状态、类别及白名单类型保留 |
| 失败可观测 | 对外泄露内部错误原文 | 无：故障验证返回固定安全错误 |
| 失败可观测 | 脚本失败现场被清理 | 无：smoke 的 finally 回收进程但保留现场 |
| 测试可信 | 新规则或失败路径无测试 | 无新增；既有覆盖粒度与 499 可达性已逐项处置 |
| 测试可信 | 断言绑定实现而非行为 | 无：检查诊断内容、脱敏、重试次数和拒绝结果 |
| 测试可信 | 失败信息无法定位条件 | 无：循环输出类型，独立场景有对应失败信息 |
| 测试可信 | 返回值、错误未检查 | 无差量发现 |
| 测试可信 | fixture 依赖个人环境 | 无：临时 SQLite、随机端口、本地替身 |
| 测试可信 | mock 结果写成真实环境结论 | 无：本轮替身验证与历史 DeepSeek 记录分开 |
| 测试可信 | 整体替换模块的 mock | 无不当替换：补充验证运行真实 EE，仅替换 provider |
| 残留 | 调试输出、临时后门、本机配置 | 无：范围内检索无命中 |
| 残留 | 未使用代码、无关改动 | 无：499 分支已证明可达，形参替换完整 |
| 残留 | 生成物与源不一致 | 无：视图校验通过；规则生成源本轮未改 |

## 8. 验证记录

评审产物目录：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全检测器-期1-代码评审9/scratch`

以下 `$TMPDIR` 均指该目录。

| 实际命令 | 结果 |
|---|---|
| `git status --porcelain=v1 --untracked-files=all`、`git branch --show-current`、`git rev-parse HEAD` | 前后干净；分支及候选正确，无未跟踪文件 |
| `git -c core.quotePath=false diff --stat b9e8752ea` | 核对完整基线清单，共 87 文件 |
| `git diff 2b7e0e699..HEAD -- ee`、`git diff --numstat 2b7e0e699..HEAD -- ee` | EE 差量仅指定两文件，增 9、删 8 行 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race ./...` | 通过，部分包命中缓存 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/guardrails/...` | 无缓存通过 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过；按要求调整产物位置 |
| `gofmt -l ee/internal/guardrails ee/internal/app` | 空输出 |
| `find ee/internal/guardrails ee/internal/app -name '*.go' -exec awk 'END{if(NR>500)print FILENAME}' {} \;` | 空输出 |
| `find ee/internal/guardrails -maxdepth 1 -name '*.go' -exec grep -l 'fasthttp\|core/schemas\|net/http' {} \;` | 空输出 |
| `git diff --check`、`git diff --check 2b7e0e699..HEAD` | 通过 |
| `python3 -B workbench/tools/build_views.py --check` | 203 篇文档校验通过 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | production、fake-conflict、三个 fake 模式全部 PASS |
| `python3 -B -` 内联读取 checklist，检查路径并执行其 grep | 12 项必存产物、9 项必含符号、9 项禁留标记全部通过 |
| `rg` 检索两范围文件的 TODO/FIXME、调试输出及本机路径 | 无命中 |
| `git diff --stat`、`git diff --cached --stat` | 最终均为空 |
| `git check-ignore "$TMPDIR/bifrost-http"`、`test -x "$TMPDIR/bifrost-http"` | 二进制存在、可执行且处于忽略目录 |

补充执行 `python3 -B - <<'PY' … PY` 内联 HTTP 故障探针，未保存探针源码。五组分别为 provider 499、三个伪造合成类型的 502/502/503、普通 provider 502。每组均断言：

- 准确状态码、分类及应有或不应有的 `known_type`；
- 日志和响应不含错误回显标记；
- 初次调用加三次重试，共 4 次；
- 最终 HTTP 400、错误码 `content_safety_check_failed`。

五组全部通过，退出码 0，自有 EE 子进程退出码 0。现场保留于 scratch 下：

| 验证 | 子目录 |
|---|---|
| 自包含 smoke | `ee-guardrails-aip004wn` |
| 五组 HTTP 故障探针 | `review9-http-errors-htqy7yej` |

**实际阅读范围：** 根、workbench、ee 的 AGENTS；指定三份流程文档；需求验收口径、方案 v5、checklist、findings；真机及预飞至预飞9共十份证据；《代码评审8》和用户提供的三条转交原文；两范围文件全文及差量。追读 `host_test.go`、判官 detector/parse 及测试、evaluation、插件检查和拒绝路径、smoke 全文、Node 进程夹具，以及上游非流式错误解析和取消构造路径。未读取其他旧评审全文。

**未执行及原因：**

- checklist 必过命令无跳过；未运行会生成报告的预飞包装器，已逐项亲核。
- 未重跑真实 DeepSeek：验收明确为非必过，历史结果未作为本候选亲测结论。
- 三个 PostgreSQL 测试 DSN 均未设置，可选 PostgreSQL 路径未执行；未连接已有实例。
- 未修改源码做变异测试，未重新生成本轮未改动的规则数据。
- 未启动 dev server 或 `--serve`，未修改代码、受跟踪文件或暂存内容，未提交、推送。

verdict: 通过
