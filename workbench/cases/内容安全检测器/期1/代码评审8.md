---
title: 内容安全检测器 期1 代码评审 #8
type: 代码评审
case: 内容安全检测器
phase: 1
round: 8
verdict: 通过
created: 2026-09-19
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 5b0311cea 到候选 2b7e0e69923f77d092d9c64e6ead3a33e2072369；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 已完成候选 `2b7e0e69923f77d092d9c64e6ead3a33e2072369` 相对 `5b0311cea` 的差量复核。指定三个文件全文、错误消费路径、上一轮修复项及两条转交意见均已核对；未发现必须修改的正确性问题。checklist 九条命令全部通过，另完成无缓存 race 和十组 HTTP 故障验证。评审前后分支、HEAD、工作树及暂存区一致，无未跟踪文件；仅在指定 scratch 生成验证产物。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 本轮声明修改 | 与方案「第七轮评审修复记录」一致：调整 README、错误分类改为 `http_*`、简化测试夹具 |
| 公共接口、配置、数据库、依赖 | 差量没有新增或变更 |
| 错误脱敏与诊断 | 状态码、固定类别、白名单类型仍保留；provider 自由文本未重新进入错误链 |
| 多做、少做、未登记偏差 | 未发现行为层面的新增偏差；文档措辞交第 5 节处理 |
| 验收 | EE vet/race/build、预飞各项、自包含 smoke 亲核通过；真实 DeepSeek 属非必过项，本轮未重跑 |

生产差量只改变错误类别文本及私有函数名。检索未发现按旧类别字符串分支的 EE 消费方；重试、失败策略、配置发布和请求快照逻辑未变，无需恢复全量评审。

失败路径追踪结果：

| 路径 | 最终去向 |
|---|---|
| provider 错误、伪造合成类型 | 适配器记录安全诊断，检测器重试；耗尽后记为失败并执行 `on_error`，HTTP 探针得到 `content_safety_check_failed` |
| 无支持模型的 key | 保留 `gateway_internal` 与配置诊断，错误继续进入失败策略 |
| 父取消、超时 | 返回 context 错误、停止重试；框架区分取消与检测超时 |
| 缺失响应、候选或正文 | 返回错误；空字符串也会在等级解析时失败，不作为无风险 |
| 对外拒绝 | 插件构造固定安全错误，不透传判官错误原文，禁止 fallback |

## 3. 本轮修复项

**无新增必修项。**

上一轮《代码评审7》「本轮修复项」逐项处置：

| 上轮条目 | 处置与证据 |
|---|---|
| 无新增必修项 | 无待关闭事项 |
| 延续 R1：provider 伪造类型导致 code/message 泄露 | **已修复，保持关闭。** [judge.go:130](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:130) 仅输出状态分类和精确匹配的 `known_type`；[host_test.go:136](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:136) 检查回显不进入错误及日志。无缓存 race、三个同名类型的 HTTP 故障验证均通过 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无。**

收敛评审7两条转交意见分别处置如下，均为撤回必修要求，未记作代码已修复：

| 转交项 | 处置与依据 |
|---|---|
| 502/502/503 全落同一分类，新增 503 没有可区分断言 | **撤回。** 同属 `http_unavailable` 符合 [judge.go:147](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:147) 的既定行为，不要求二者类别不同。循环也并非只检查类别：[host_test.go:139](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:139) 逐项检查不同的 `known_type` 和不泄露，普通 502 对照明确禁止 `known_type`。503 未增加分类分支覆盖这一事实成立，但不足以构成本轮必修问题 |
| 未逐项断言状态码；删除状态码拼接后全函数仍绿 | **撤回。** 502/503 未逐项断言状态码属实；但“全函数仍绿”不成立：[host_test.go:117](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:117) 明确要求 `status=429`，它与其他状态共用 [judge.go:112](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:112) 的拼接路径，删除该路径会触发失败。这是静态控制流结论，未修改源码做变异测试。本轮 HTTP 探针另亲核了 502/503 的准确状态输出 |

## 5. 转收敛评审 / 挂账

- 转收敛评审：[README.md:101](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:101) 的“凡带 HTTP 状态码……只留”与后句“网关标记为自身错误……额外保留”存在条件重叠，关联 `judge.go:115` 的分支优先级，属文档契约措辞同步。

挂账：无。

## 6. 开放式复读的表外发现

**已做开放式复读，无新增表外发现。**

逐文件核对了故障发现与恢复：`judge.go` 留下状态及安全诊断；测试失败输出具体类型或分支；README 提供配置恢复入口。文档措辞已转交第 5 节。

## 7. 起手表覆盖

以下结论限定于差量及其受影响路径。

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | 无：必过项亲核通过；非必过真机项未重跑 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | 无行为差异；契约措辞转收敛 |
| 对照方案 | 多出的功能、公共接口、抽象或依赖 | 无 |
| 对照方案 | 偏差未登记 | 无新增 |
| 行为正确性 | 失败被吞或降级成假成功 | 无：故障探针均返回检查失败 |
| 行为正确性 | 空值、空集合无分支 | 无：状态码、错误字段、响应及正文均有处理 |
| 行为正确性 | 取消或超时后仍写入或产生副作用 | 无新增：取消与重试逻辑未变；provider 侧继续执行为已声明限制 |
| 行为正确性 | 重发产生两份副作用 | 不适用：差量未增加业务写入 |
| 行为正确性 | 事务外写后读、多方仅回滚一方 | 不适用：差量不涉及事务及配置发布 |
| 行为正确性 | 关闭、旧数据、旧客户端路径无处理 | 无新增：未配置、reset、重启及 FAKE 冲突 smoke 通过 |
| 行为正确性 | 昂贵检查先行、校验缺层 | 无新增：差量仅固定分类及测试调整 |
| 行为正确性 | 路由保护、认证关闭行为 | 不适用：差量不涉及路由或鉴权 |
| 行为正确性 | 敏感信息进日志或响应 | 无：同名伪造类型及普通错误均未泄露探针回显 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | 无 |
| 行为正确性 | 配置、环境依赖或部署步骤未登记 | 无 |
| 失败可观测 | 服务端无法追溯失败原因 | 无：状态、类别和已知类型保留 |
| 失败可观测 | 对外泄露内部错误原文 | 无：HTTP 返回固定安全错误 |
| 失败可观测 | 脚本失败现场被清理 | 无：smoke 保留现场；本轮探针误报现场亦保留 |
| 测试可信 | 新规则或失败路径无测试 | 无：既有回归与本轮十组故障验证覆盖受影响行为 |
| 测试可信 | 断言绑定实现而非行为 | 无：断言诊断内容、脱敏及失败结果；转交项已逐条处置 |
| 测试可信 | 失败信息无法定位条件 | 无：循环输出 kind，其余分支有独立失败信息 |
| 测试可信 | 返回值、错误未检查 | 无差量发现 |
| 测试可信 | fixture 依赖个人环境 | 无：替身模型、临时 SQLite、随机端口 |
| 测试可信 | mock 结果冒充真实环境结论 | 无：历史真机记录与本轮替身验证明确区分 |
| 测试可信 | 整体替换模块的 mock | 无不当替换：HTTP 验证运行真实 EE，仅替换 provider |
| 残留 | 调试输出、后门、本机配置 | 无：范围内残留检索无命中 |
| 残留 | 未使用代码、无关改动 | 无行为类发现；旧私有函数名及旧类别无残留 |
| 残留 | 生成物与源不一致 | 无：视图校验通过，规则生成源本轮未改 |

## 8. 验证记录

`$TMPDIR` 为指定目录：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全检测器-期1-代码评审8/scratch`

| 实际命令 | 结果 |
|---|---|
| `git status --porcelain=v1 --untracked-files=all`、`git branch --show-current`、`git rev-parse HEAD` | 前后干净；分支、候选正确，无未跟踪文件 |
| `git diff --stat b9e8752ea` | 核对完整基线清单：84 文件 |
| `git diff --stat 5b0311cea..HEAD`、`git diff 5b0311cea..HEAD -- ee` | EE 差量恰为指定三文件，新增 18 行、删除 19 行 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race ./...` | 通过；部分包命中缓存 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/guardrails/...` | 无缓存通过 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过；按要求调整产物位置 |
| `gofmt -l ee/internal/guardrails ee/internal/app` | 空输出 |
| `find ee/internal/guardrails ee/internal/app -name '*.go' -exec awk 'END{if(NR>500)print FILENAME}' {} \;` | 空输出 |
| `find ee/internal/guardrails -maxdepth 1 -name '*.go' -exec grep -l 'fasthttp\|core/schemas\|net/http' {} \;` | 空输出 |
| `git diff --check`、`git diff --check 5b0311cea..HEAD` | 通过 |
| `python3 -B workbench/tools/build_views.py --check` | 201 篇文档校验通过 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | production、fake-conflict、三个 fake 模式全部 PASS |
| `python3 -B -` 内联读取 checklist，检查路径并执行其 grep | 12 项必存产物、9 项必含符号、9 项禁留标记全部通过 |
| `rg` 检索范围内残留及旧分类名 | 无命中 |
| `git diff --stat`、`git diff --cached --stat` | 最终均为空 |

补充 HTTP 验证使用 `python3 -B - <<'PY' … PY` 内联探针，未保存源码。临时 SQLite、随机端口、真实候选二进制及本地错误 provider 验证了：

- 401、403、429、499、普通 502、418、399 的状态及分类；
- 三个伪造合成类型的 502/502/503、对应 `known_type`；
- 每组均返回 `content_safety_check_failed`，日志及响应均无探针回显。

首次探针退出 1：评审探针将日志字节偏移用于字符切片，导致误报缺少 401 分类；读取原始日志确认分类已正确输出。改用字节切片后十组全部通过，退出 0。两次探针均回收自有子进程，`CHILD_EXIT 0`。

现场保留于 scratch：

| 验证 | 子目录 |
|---|---|
| 自包含 smoke | `ee-guardrails-4g7kh1ds` |
| 首次探针误报 | `review8-errors-_krtbei9` |
| 修正后十组探针 | `review8-errors-a9cp2vzh` |

**实际阅读范围：** 根、workbench、ee 的 AGENTS；三份指定流程文档；需求验收口径、方案 v5 全文、checklist、findings；九份指定证据；《代码评审7》及用户提供的转交原文；三个范围文件全文和差量。追读了 Builder、判官 detector/parse 及测试、checker/evaluation、插件错误消费与拒绝路径、两个 guardrails 脚本、Node 进程夹具，以及上游相关错误构造和解析路径。未读取其他旧评审全文。

**未执行及原因：**

- checklist 必过命令无跳过；未运行会生成报告的预飞包装器，已逐项亲核。
- 未重跑真实 DeepSeek：验收明确非必过，历史记录未作为本候选亲测结论。
- 三个 PostgreSQL 测试 DSN 均未设置，可选 PostgreSQL 路径未执行；未连接已有实例。
- 未修改源码做变异测试，未重新生成未改动的规则数据。
- 未启动 dev server 或 `--serve`，未修改受跟踪文件、暂存、提交或推送。

verdict: 通过
