---
title: 内容安全检测器 期1 方案 v2
type: 方案
case: 内容安全检测器
phase: 1
version: 2
status: 需修改
relates: [内容安全检测器]
created: 2026-09-19
---

## 给用户的摘要

现状：框架已合入 develop，只有匹配 `[guardrails-test]` 的假检测器。做完后：网关用规则查密钥（D3），用 DeepSeek 判官查有害内容、提示词攻击和管理员写的业务规则（D1/D4/D5）；同阶段规则并行，一次请求在输入、输出两个阶段各多等最多一次判官调用。配置来自 JSON 文件，默认不启用。

v2 相对 v1 的变化：判官调用改用上游提供的内部子请求方式（v1 的写法会让判官取不到 key）；适配器从 `app` 移到 `guardrails/host`；并行执行的取消语义和插件错误码判定重新定义；送检文本设上限；转换脚本入库。

拍板项：
1. **期 1 配置用 JSON 文件**，管理 API 与数据库留期 2。利：不引入表和接口；弊：改配置要重启。
2. **判官调用不进上游日志与计费**（内部子请求跳过插件管线）。判官 token 用量本期只写 EE 日志，期 2 再落库。
3. **判官理由不写日志**：理由可能复述原文。
4. **送检判官的文本上限 16KB**（约 8 千汉字，全部历史消息拼接后计），超过按检测失败处理（即拦截）。利：延迟与费用可控；弊：长对话会被拒。也可选「只送最近 N 轮」，本期不做。
5. **成本口径**：每个请求每阶段的判官调用数 = 启用的项目数（示例配置：输入 3 次、输出 2 次），单次 ≤16KB 文本；按实验单价估算每次 ≤0.02 元。
6. **密钥等级映射**：厂商专用规则 High、通用规则 Medium。

## 1. 起点

`起点=c05dba215`（develop `05db7cea2` + 本案立案），分支 `feat/content-safety-detectors`。

## 2. 现状与约束盘点

- `ee/internal/guardrails/detector.go:22`：`Detect(ctx, text) ([]Finding, error)`，`Finding{Level}`。本期不改。
- `checker.go:66-93`：串行、遇 Block 停；`checker.go:19-23` 注释与 README「遇拦截即停」描述串行语义，**本期随并行一起改**。`checker_test.go:61-80 TestOrderShortCircuitAndStages` 断言后续规则不执行且用无锁 slice 记录调用，**必须重写**。
- `evaluation.go:44-79`：先查 `ctx.Err()`，再派生 `Timeout` 子 ctx；`checker.go:60-62` 约定返回 error 时保留已完成评估。并行后要区分「父 ctx 取消」与「因 Block 取消」。
- `plugin/plugin.go:118-122`：拦截时看**最后一条**评估是否 `Failed` 决定 `check_failed` 还是 `content_safety_blocked`。并行后结果顺序与完成顺序无关，**该判定改为读 `CheckResult` 新字段**（§4.3）。
- 判官调用：`core/utils.go:486 bifrost.PrepareContextForInternalRequest` 是上游给插件发内部子请求的入口（设跳过管线并清掉调用方的 key 路由与传输状态）；先例 `plugins/routing/llmclassify.go:129-141`：`schemas.NewBifrostContext(ctx, deadline)` + `defer Cancel()` + `PrepareContextForInternalRequest`。直接在调用方 ctx 上设 `SkipPluginPipeline` 会让当前用户请求的后续管线整体跳过（`core/bifrost.go:7977/8071`），且继承治理层 key 过滤导致取不到 key（评审 1 核实）。
- `schemas.BifrostChatRequest.Params`：`Temperature *float64`（`chatcompletions.go:220`）、`MaxCompletionTokens *int`（:202）、`ResponseFormat *interface{}`（:213）。
- 出口：`server.go:230 host.Client *bifrost.Bifrost`，`core/bifrost.go:801 ChatCompletionRequest`。判官 provider 自身的 `MaxRetries`（`provider.go:65`）与检测器重试叠加，装配时若该 provider `MaxRetries>0` 记 Warn 日志提示。
- 先例：`ee/internal/rbac/host/` 是「模块内接宿主」的位置；架构 §7 与编码规范要求 `app` 只装配。判官适配器放 `guardrails/host/`。
- `pricing` 的「嵌入数据 + 环境变量指外部文件」做法（`app/pricing.go:16-24`）供密钥规则数据参照。
- 约束：根包只用标准库；`guardrails` 根包及 `detectors/` 禁止 `net/http`、`fasthttp`、`core/schemas`；不改上游；日志不含原文、命中值、判官理由；文件 ≤500 行。`ee/go.mod` 无 gitleaks、无 TOML 库（已核）。

## 3. 目标 / 非目标

**目标**：

1. `detectors/secrets`：密钥规则检测器 + 规则数据 + 入库的转换脚本。
2. `detectors/judge`：判官检测器，内置 D1/D4 提示词与 D5 模板，重试。
3. `guardrails/host`：判官模型适配器（内部子请求）。
4. `Checker` 并行 + `CheckResult` 增加拦截来源；`plugin` 错误码改读该字段。
5. `app` 按 JSON 配置装配；冒烟脚本；README。

**非目标**：管理 API、数据库、UI；脱敏、代答；输出流；MCP、多模态、Responses；判官调用落库计费；只送最近 N 轮；提示词效果调优；上游文件改动。

## 4. 接口与数据设计

### 4.1 密钥检测器 `detectors/secrets`

```go
type Options struct{ IgnoredKeywords []string } // 管理员忽略词：小写、去重，命中值含任一子串则丢弃；与 Gitleaks StopWords 同义
func New(opts Options) (*Detector, error)
func (d *Detector) Detect(ctx, text) ([]guardrails.Finding, error)
```

规则数据 `data/rules.json`（字段：id、description、regex、keywords、entropy、secret_group、allow{regexes,stop_words}、level）与 `data/NOTICE`（Gitleaks MIT）。转换脚本 `ee/scripts/secrets-rules-convert.py`：输入 Gitleaks `gitleaks.toml` 路径 + 本仓 `data/rules.extra.json`（修正与新增），输出 `rules.json`；脚本去掉纯路径规则与 `path` 条件，`generic-*` 规则 level=medium、其余 high，`data/README.md` 记录来源版本与命令，可复现。

扫描：小写 → 关键词命中集合（逐词 `strings.Contains`）→ 只跑命中规则（无关键词规则总是跑）→ `FindAllStringSubmatchIndex` → secret 取 `secret_group` 或第一个非空组 → 熵 ≤ 阈值丢弃 → 内置白名单（`sk-bf-` 前缀，不可关闭）→ 全局与规则 allow → 管理员忽略词 → 同一 secret 已有专用规则命中则丢 generic。每 32 条规则查一次 `ctx.Err()`。返回每命中一条 `Finding{Level}`。

规则内容：Gitleaks v8.30.1 去路径规则；修正 Gemini `AQ.`、Anthropic 通配、OpenAI 不定长；新增可写专用规则的厂商（[密钥格式核实](../../../../product/需求/内容安全/密钥格式核实.md)第二节）；`generic-api-key` 关键词补 密钥、秘钥、密码、令牌、口令、apikey。长度写「前缀 + 最小长度」。

### 4.2 判官检测器 `detectors/judge`

```go
type Model interface { Complete(ctx context.Context, system, user string) (string, error) }
type Kind string // Harmful, PromptAttack, BusinessRule
type Options struct {
    Model    Model
    Kind     Kind
    Rule     string // Kind=BusinessRule 时必填
    Stage    guardrails.Stage
    Retries  int    // 3–5，默认 3
    MaxBytes int    // 送检上限，默认 16384；超过返回 ErrTextTooLarge
}
func New(opts Options) (*Detector, error)
```

请求：system=提示词（`prompts/harmful.md`、`prompt_attack.md`、`business_rule.md`，`go:embed`，沿用实验初稿，业务规则模板以 `{{rule}}` 替换），user=`{"stage":…,"text":…}`。解析 `{"risk_level":"none|low|medium|high"}`：none→空结果，其余→`Finding{Level}`；非法 JSON、未知等级、空响应各算一次失败。失败按 200ms×2ⁿ 退避重试至 `Retries` 次；每次尝试都用传入 ctx，规则 `Timeout` 到期即停并返回 `context.DeadlineExceeded`，由框架记 `DetectorTimeout`。理由字段忽略。

### 4.3 并行执行与拦截来源

```go
type CheckResult struct {
    Action          Action
    Blocking        *RuleEvaluation // Action==Block 时指向决定拦截的评估；框架错误时为 nil
    RuleEvaluations []RuleEvaluation
}
```

`Check`：先做取消/长度/UTF-8 检查 → 派生 `blockCtx, cancel` → 同阶段规则各起 goroutine 调 `evaluateRule(parent, blockCtx, rule)` → 收集。评估函数：detect 用 `blockCtx` 派生的 `Timeout` 子 ctx；detect 返回后**先查 `parent.Err()`**（父取消 → 返回 error，`Check` 返回该 error 与已完成评估）；再查 `blockCtx.Err()`（因 Block 取消且本条未完成 → 丢弃，不写入结果）；否则按现有失败/阈值逻辑记录。第一条得到 `Action==Block` 的评估设为 `Blocking` 并 `cancel()`；多条同时 Block 时取配置顺序最靠前的。`RuleEvaluations` 按配置顺序，仅含完成的评估。

`plugin.checkText`：`Blocking.Outcome==Failed` → `check_failed`，否则 `content_safety_blocked`；`err!=nil` 路径不变。

### 4.4 判官适配器 `guardrails/host`

```go
type Client interface { ChatCompletionRequest(*schemas.BifrostContext, *schemas.BifrostChatRequest) (*schemas.BifrostChatResponse, *schemas.BifrostError) }
func NewJudgeModel(client Client, provider, model string, log Logger) judge.Model
```

`Complete`：`schemas.NewBifrostContext(ctx, deadline)`（deadline 取 ctx 的）+ `defer Cancel()` + `bifrost.PrepareContextForInternalRequest`；请求 `Temperature=0`、`MaxCompletionTokens=200`、`ResponseFormat={"type":"json_object"}`；取 `choices[0]` 文本；`BifrostError` 转普通 error（不含响应正文）。每次调用 Info 日志：provider、model、token 用量、耗时、成功与否。

### 4.5 配置与装配（`app/guardrails.go`）

`EE_GUARDRAILS_CONFIG` 指向 JSON，未设置不注册；解析用 `DisallowUnknownFields`。

```json
{
  "deny": {"status": 400, "message": "内容未通过安全检查"},
  "max_text_bytes": 65536,
  "judge": {"provider": "deepseek", "model": "deepseek-v4-flash", "retries": 3, "timeout_ms": 10000, "max_text_bytes": 16384},
  "secrets":       {"enabled": true, "stages": ["input"], "threshold": "medium", "on_match": "block", "on_error": "block", "timeout_ms": 500, "ignored_keywords": []},
  "harmful":       {"enabled": true, "stages": ["input","output"], "threshold": "medium", "on_match": "block", "on_error": "block"},
  "prompt_attack": {"enabled": true, "stages": ["input"], "threshold": "medium", "on_match": "block", "on_error": "block"},
  "business_rules": [{"id": "pricing", "rule": "……", "stages": ["input","output"], "threshold": "medium", "on_match": "block", "on_error": "block"}]
}
```

判官类项目的 `Timeout` 统一取 `judge.timeout_ms`；密钥取自身 `timeout_ms`。拒启条件：未知字段、`deny.status` 不在 400–599、阈值/动作/阶段非法、`judge` 缺 provider/model 或 provider 未在网关配置、`retries` 不在 3–5、`business_rules` ID 重复或 rule 空、所有项目都未启用。规则 ID=`<项>:<阶段>`，判官实例 ID=`judge:<项>:<阶段>`（Stage 是检测器构造参数）。`EE_GUARDRAILS_FAKE` 与本配置互斥，同时设置拒启。

## 5. 实现步骤

1. `Checker` 并行、`Blocking` 字段、`plugin` 错误码改读；重写 `TestOrderShortCircuitAndStages`，新增父取消/Block 取消/多 Block 顺序测试；README 与注释同步。
2. 密钥检测器：转换脚本 + 数据 + NOTICE + 扫描 + 真假样本单测 + 40KB 基准。
3. 判官检测器：提示词、解析、重试、上限；替身 `Model` 测四种等级、非法、错误、慢响应、取消。
4. `guardrails/host` 适配器 + 单测（替身 `Client` 断言 ctx 已设跳过管线、参数正确）；配置解析与装配 + 拒启用例；`guardrails-smoke.py` 扩展（密钥拦截、判官替身 provider）；README。
5. 真实 DeepSeek 跑实验样本，写独立验证记录（延迟分布、token）；预飞；候选提交。

每步一个 commit，前缀 `feat(guardrails)` / `feat(ee)`。

## 6. 测试策略

- 密钥：每类新增规则一真一假；中文字段名召回；`sk-bf-` 不命中；忽略词生效；generic 去重；取消响应；基准。
- 判官：解析矩阵；重试次数与退避总时长；超时后不再重试；超上限返回错误；`Retries`/`MaxBytes` 越界拒绝构造。
- 并行：一条 Block 时其余被取消且不在结果；Observe 与 Block 并发时 Observe 若已完成则保留；无 Block 时顺序与配置一致；父取消返回 error 且保留已完成评估；`-race`。
- 插件：`Blocking.Outcome` 两种取值对应两种错误码；既有集成测试加密钥规则用例。
- 装配：拒启条件逐项；示例配置生成的规则集断言。
- 盲区：真实准确率与延迟只在独立记录中；提示词质量不断言；provider 自身重试叠加只做 Warn 提示不测。

## 7. 验收标准 + 检查清单 YAML

见同目录 `checklist.yaml`（v2 同步：必含符号改为 `guardrails/host`、`Blocking`；禁留标记 `core/schemas` 限于 `detectors/` 与根包；新增 `secrets-rules-convert.py` 必存）。所有 Go 文件 ≤500 行。

## 8. 风险与禁止事项

- 判官延迟：每阶段最多一次调用时长（并行），两阶段叠加；`timeout_ms` 到期即拦截。真实分布在独立验证中记录，超预期由用户决定是否调整阈值或阶段。
- 密钥规则长度多来自第三方；`ignored_keywords` 止血。
- 判官 provider 若自带重试，实际调用次数 = (1+MaxRetries)×(1+retries)；装配时 Warn。
- 禁止：日志含原文、命中值、判官理由；判官失败当通过；改上游；根包与 detectors 引网络库或 `core/schemas`；默认启用。

## 9. 回滚口径

未合并前弃分支。无配置文件时行为与 develop 一致；无数据迁移。

---

## 实施记录

待实施。

## 实施偏差记录

待实施。

## 蓝图验证备忘

不适用。

## Gate 决策结果

待方案复核。

## v1 问题与调整依据

评审 1 六条阻塞逐条对应：①判官 ctx → §2、§4.4 改用 `PrepareContextForInternalRequest`；②插件错误码 → §4.3 `Blocking`；③取消区分 → §4.3 双 ctx；④并行为行为变更 → §2、§5 步骤 1 列出测试与文档改动；⑤适配器位置 → §4.4 `guardrails/host`；⑥延迟与成本 → 摘要拍板 4、5，§4.2 `MaxBytes`。建议项采纳：`Timeout` 来源、`retries` 3–5、`DisallowUnknownFields`、`IgnoredKeywords` 命名、Level 映射列为拍板、转换脚本入库、`MaxCompletionTokens`。未采纳：Observe 统计偏低——并行下 Observe 规则若在 Block 前完成即保留，与用户裁决的「遇拦截取消其余」一致，接受偏低。
