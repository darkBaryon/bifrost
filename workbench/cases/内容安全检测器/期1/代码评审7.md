---
title: 内容安全检测器 期1 代码评审 #7
type: 代码评审
case: 内容安全检测器
phase: 1
round: 7
verdict: 通过
created: 2026-09-19
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 02d5d74a8 到候选 5b0311cea6c1fec3185d4065a14965ddf1c2ff5a；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 已完成 `02d5d74a8..5b0311cea6c1fec3185d4065a14965ddf1c2ff5a` 三个指定文件的差量复核、全文阅读、失败路径追踪和开放式复读。本轮错误分类调整保留了诊断能力，未重新引入 provider 自由文本泄露；测试断言的两项转交问题已修复，脚本输出转交项有依据撤回。checklist 九条命令及产物、符号检查全部亲核通过。评审前后分支、HEAD、工作树与索引一致，没有未跟踪文件；仅生成指定 scratch 内的验证产物，未操作已有实例。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 方案要求判官错误不含正文，同时可以诊断失败 | 符合。带状态码的非内部错误仅输出固定类别及白名单类型；内部错误保留截断诊断 |
| 本轮声明调整分类、补真实 503 夹具、修正文档引用 | 与 diff 一致，已登记于方案「第六轮评审修复记录」 |
| 公共接口、依赖、配置、数据库结构 | 本轮没有新增或变更 |
| 多做、少做、未登记偏差 | 未发现 |

生产差量仅影响错误格式化；调用参数、重试控制、失败策略、配置持久化和请求快照均未改变。已追读错误的消费路径，未发现要求恢复全量评审的影响。

| 失败路径 | 最终去向及核验 |
|---|---|
| provider 返回错误，包括伪造合成类型 | 记录安全诊断，进入检测器重试；耗尽后按规则失败策略处理。HTTP 探针验证客户端返回 `content_safety_check_failed` |
| 真实连接中断、凭据耗尽 | 保留对应 `known_type`；日志及客户端响应无测试回显原文 |
| 没有支持判官模型的 key | `gateway_internal`，保留可定位配置问题的消息 |
| 父取消、超时 | 适配器优先返回 `ctx.Err()`；检测器停止重试，框架区分取消和检测超时 |
| 缺失响应、候选或正文 | 返回错误，不作为无风险；继续进入既有重试及失败策略 |

## 3. 本轮修复项

**无新增必修项。**

上一轮《代码评审6》「本轮修复项」记录逐项处置：

| 上轮记录 | 本轮处置与证据 |
|---|---|
| 无新增必修项 | 无待关闭事项 |
| 延续复核的 R1：provider 伪造类型使 code/message 泄露 | **已修复，保持关闭。** [judge.go:130](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:130) 按状态码分类，仅在类型精确命中白名单时附加 `known_type`，不复制 code/message。[host_test.go:136](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:136) 检查返回错误及日志；本轮 HTTP 探针另验证了三个伪造类型、相似类型及普通错误，均无回显泄露 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无。**

收敛评审6转交项逐条处置：

| 转交项 | 处置与依据 |
|---|---|
| `category=gateway` 子串断言不能区分分支 | **修复。** [host_test.go:123](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:123)、[host_test.go:131](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:131) 改为检查 `category=gateway_internal`；另一分支使用 `provider_*`，不再发生此前的前缀混淆。无缓存 race 测试通过 |
| 三种合成类型共用 502，不能防止按状态码误判 | **修复。** [host_test.go:136](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:136) 使用 502/502/503，并逐项检查对应 `known_type`；[host_test.go:148](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:148) 对普通 502 明确禁止 `known_type`。本轮 HTTP 探针还验证了“已知类型＋429”和相似但不匹配的类型，状态分类与类型提示相互独立 |
| 中止原因只写 stderr，stdout 单独捕获时缺少原因 | **撤回必修要求，未记作已修复。** 此事实仍成立，但没有要求 stdout 单独构成完整报告的契约或实际调用方证据。[guardrails-judge-e2e.py:112](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/guardrails-judge-e2e.py:112) 输出中止原因及调用栈，随后退出 1；结果文件保存部分结果和错误，stdout 提供其路径。异常注入亲测上述出口均有效，不能据只丢弃 stderr 的假设判定脚本吞错 |

## 5. 转收敛评审 / 挂账

转收敛评审：无新增。

挂账：无。

## 6. 开放式复读的表外发现

**已做开放式复读，无表外发现。**

逐文件复读结果：`judge.go` 的失败可通过类别及白名单类型定位；`host_test.go` 的失败信息能指出对应类型或检查条件；README 的判别函数链接已指向现存实现，保留了配置恢复说明。

## 7. 起手表覆盖

以下结果限定于本轮差量及受影响路径。

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准有未覆盖项 | 无：必过检查通过；真实 DeepSeek 非必过，本轮未重跑 |
| 对照方案 | 方案、实施记录、diff 三方不一致 | 无：分类及夹具调整与修复记录一致 |
| 对照方案 | 多出的功能、公共接口、抽象或依赖 | 无 |
| 对照方案 | 偏差未登记 | 无 |
| 行为正确性 | 失败路径被吞或降级成假成功 | 无：故障探针均返回检查失败；脚本异常退出 1 |
| 行为正确性 | 空值、空集合与非空无分支 | 无：状态码、错误字段、响应、候选及正文均有检查 |
| 行为正确性 | 取消或超时后仍产生写入或副作用 | 无新增：取消路径未改；provider 侧继续执行属于已声明限制 |
| 行为正确性 | 重发请求产生两份副作用 | 不适用：差量仅格式化错误，不新增业务写入 |
| 行为正确性 | 事务外写后读、多方只回滚一方 | 不适用：差量不涉及事务和配置发布 |
| 行为正确性 | 关闭、旧数据、旧客户端路径无处理 | 无：未配置、reset、重启及 FAKE 冲突 smoke 通过 |
| 行为正确性 | 昂贵检查先于廉价检查、校验缺层 | 无新增：新增处理为状态判断及固定白名单查询 |
| 行为正确性 | 路由保护、认证关闭时行为 | 无新增：未改鉴权；隔离实例匿名读取返回 401，原鉴权测试通过 |
| 行为正确性 | 敏感信息进日志或响应 | 无：伪造类型、相似类型及真实故障探针均未泄露回显 |
| 行为正确性 | 热路径新增 I/O、无界缓冲或循环 | 无 |
| 行为正确性 | 新配置、环境依赖或部署步骤未登记 | 无 |
| 失败可观测 | 服务端无法追溯失败原因 | 无：连接、凭据、模型 key 配置问题均保留诊断 |
| 失败可观测 | 对外泄露内部错误原文 | 无：客户端仅收到固定安全错误 |
| 失败可观测 | 脚本失败现场被清理 | 无：异常注入确认部分结果和调用栈保留 |
| 测试可信 | 新规则或失败路径无测试 | 无：分类与白名单分支有回归及 HTTP 补充验证 |
| 测试可信 | 断言绑定实现而非行为 | 无：检查诊断输出、无泄露及分支差异；两项转交已关闭 |
| 测试可信 | 失败信息无法定位条件 | 无：类型循环输出具体 kind，其他分支有独立失败信息 |
| 测试可信 | 返回值、错误未检查 | 无差量发现：调用错误进入断言，测试及脚本退出码已核对 |
| 测试可信 | fixture 依赖个人环境 | 无新增：替身、临时 SQLite、随机端口 |
| 测试可信 | mock 结果冒充真实环境结论 | 无：本轮替身验证与历史 DeepSeek 记录明确区分 |
| 测试可信 | 整体替换模块的 mock | 无不当使用：HTTP 探针使用真实 EE；脚本替身仅核异常出口 |
| 残留 | 调试输出、临时后门、本机配置 | 无：范围文件残留检索无命中 |
| 残留 | 未使用代码、无关改动 | 无行为类发现 |
| 残留 | 生成物与源不一致 | 无：视图校验通过；本轮未改规则生成源 |

## 8. 验证记录

产物目录：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全检测器-期1-代码评审7/scratch`，下称 `$TMPDIR`。

| 实际命令或检查 | 结果 |
|---|---|
| `git status --porcelain=v1 --untracked-files=all`、`git branch --show-current`、`git rev-parse HEAD` | 前后干净；分支及候选正确，无未跟踪文件 |
| `git diff --stat b9e8752ea` | 已核对完整基线清单：81 文件 |
| `git diff --stat 02d5d74a8..HEAD -- ee` 及完整差量 | 恰为指定三个文件，新增 25 行、删除 27 行 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race ./...` | 通过；部分包使用缓存 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/guardrails/...` | 无缓存重跑，通过 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过，仅调整 checklist 产物位置 |
| `gofmt -l ee/internal/guardrails ee/internal/app` | 空输出 |
| `find ee/internal/guardrails ee/internal/app -name '*.go' -exec awk 'END{if(NR>500)print FILENAME}' {} \;` | 空输出 |
| `find ee/internal/guardrails -maxdepth 1 -name '*.go' -exec grep -l 'fasthttp\|core/schemas\|net/http' {} \;` | 空输出 |
| `git diff --check`、`git diff --check 02d5d74a8..HEAD` | 通过 |
| `python3 -B workbench/tools/build_views.py --check` | 199 篇文档校验通过 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | production、fake-conflict、三个 fake 模式全部 PASS |
| `python3 -B -` 内联读取 checklist，检查路径并执行其 grep | 12 项产物、9 项必含符号、9 项禁留标记全部通过 |
| `rg` 检索范围文件的 TODO/FIXME/HACK/DEBUG、本机路径 | 无命中 |
| 最终 `git diff --stat`、`git diff --cached --stat` | 均为空 |

补充验证均通过 `python3 -B - <<'PY' … PY` 内联执行，未保存探针源码：

- **HTTP 错误探针**：三个伪造类型、普通 502、相似类型、已知类型＋429、真实连接中断通过，每场景四次判官失败均无回显。第一次凭据耗尽断言失败：夹具 `max_retries=0`，实际正确返回 `provider_auth`。核对上游重试逻辑后，以 `max_retries=1` 的新隔离夹具补测，真实凭据耗尽及不支持模型两项通过。两次探针均确认自有子进程退出。
- **脚本异常注入**：第二次 completion 抛出超时；确认退出 1、保留一条结果、stderr 打印一次调用栈、`results.json` 保存错误、清理入口执行。此项只验证脚本异常处理，不代表真实模型验证。

现场均保留在 scratch：

| 验证 | 子目录 |
|---|---|
| 自包含 smoke | `ee-guardrails-y3f24z97` |
| 首次 HTTP 探针，包括夹具预期失败 | `review7-errors-o9h0m2z2` |
| 凭据耗尽及模型配置补测 | `review7-credentials-771wpc2f` |
| 脚本异常注入 | `ee-judge-e2e-33p1ehqr` |

**实际阅读范围**：根、workbench、ee 的 AGENTS；指定三份流程文档；需求验收口径、方案 v5 全文、checklist、findings；八份指定证据；允许读取的《代码评审6》及用户原样提供的转交栏；三个范围文件全文与差量。另追读判官 detector/parse、Builder、evaluation、插件日志与拒绝路径及相关测试，两个 guardrails 脚本、Node 启停夹具，以及上游错误解析、连接错误、凭据耗尽和 DeepSeek 调用路径。未读取其他旧评审全文。

**未执行及原因**：

- checklist 必过命令无跳过；未运行生成报告的预飞包装器，已逐项亲核其检查。
- 未重跑真实 DeepSeek：验收明确为非必过项，历史真机记录未充当当前候选亲测结果。
- 三个 PostgreSQL 测试 DSN 均未设置，相关可选路径未运行；未连接或启动已有数据库实例。
- 未修改源码做变异测试，未重新生成未改变的规则数据。
- 未启动 dev server 或 `--serve`，未修改被跟踪文件、索引，未暂存、提交或推送。

verdict: 通过
