---
title: 内容安全检测器 期1 代码评审 #2
type: 代码评审
case: 内容安全检测器
phase: 1
round: 2
verdict: 需修改
created: 2026-09-19
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 93e6a1272 到候选 fde843b5b659943d76b6cc19517afccf417d23c3；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：需修改。** 对候选 `fde843b5b659943d76b6cc19517afccf417d23c3` 完成差量复核：必过命令全部通过，热更新、零行更新检查、启动拒启及并行测试稳定性已有修复；但判官错误仍可泄露送检原文，验证脚本仍存在误判和丢失部分结果的问题，部分承诺的回归覆盖尚未完成。评审期间 HEAD 被其他操作推进到 `1579d7c1`，仅追加真机验证文档；34 个评审范围文件与指定候选完全一致。本人未修改源码、索引或报告，未暂存、提交、推送。

## 2. 对照方案与接口面对账

| 对账项 | 本轮结果 |
|---|---|
| D3、D1/D4/D5、等级解析及同阶段并行 | 差量未发现行为回归；相关测试亲跑通过 |
| 默认无配置不检测、配置读写/reset、权限 | 自包含 smoke 通过；匿名读取实测 401，权限矩阵测试通过 |
| 检查器与拒绝策略一起热更新 | 实现符合；HTTP 实测自定义 451 和文案立即生效，重启保留 |
| 条件更新、持久化与发布顺序 | 零行检查和共同锁已加入；并发回归证据仍有缺口，见 N4 |
| 坏配置拒启 | 已补坏 JSON、缺失 provider 单测，亲跑通过 |
| 失败可观测且不泄露原文 | 存储原因已有日志；判官错误脱敏未完成，见 N1 |
| 验证脚本及每类规则真假样本 | 未完全满足，见 N2、N3 |

未发现新增业务功能或模块依赖。`Swap(checker, options)`、Logger 注入及公共等级解析等变化已记入修复记录。**未登记为未完成项的缺口**是 N1–N4；实施记录不能据此宣称上一轮意见全部关闭。

上一轮「本轮修复项」逐条处置：

| 上轮项 | 处置 | 证据 |
|---|---|---|
| R1 拒绝策略未热更新 | **已修复** | `plugin.go:23、63` 整体状态原子发布；smoke 自定义 451 热更新、重启断言通过；快照测试缺口另见 N4 |
| R2 零行更新假成功 | **已修复** | `store.go:66、81` 检查插入/更新影响行数；`TestZeroRowUpdateIsConflict` 通过 |
| R3 写库与发布乱序 | **仍未关闭：实现已修，要求的确定性交错回归未完成** | `handler.go:118、139` 共用锁；测试问题见 N4 |
| R4 判官日志泄露 | **仍未关闭** | Message 已省略，type/code 仍可泄露，见 N1 |
| R5 存储故障原因丢失 | **已修复** | `handler.go:153` 记录操作和原因；`builder.go:68` 区分查询失败；单测及隔离查询故障验证通过 |
| R6 并行测试随机失败 | **已修复** | `checker_test.go:119` 加屏障；三个相关用例 `-race -count=100` 通过；未使用检测器已删除 |
| R7 真机脚本误判、正常退出 | **仍未关闭，部分修复** | 43 个 HTTP 500 现全部记 failure、退出 1；非法回答及中途异常仍有问题，见 N2 |
| R8 关键回归缺失 | **仍未关闭，部分修复** | 启动拒启和新增正例已补；每类新增规则反例仍未补齐，见 N3 |

## 3. 本轮修复项

**N1｜P1：provider 的 type/code 仍未经白名单处理进入日志**

位置：[judge.go:102](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:102)、[judge.go:76](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:76)。

- **代码事实**：直接拼接 `Error.Type`、`Error.Code` 并记录；上游 OpenAI 解析器直接复制 provider 返回的这两个字符串。
- **触发与后果**：隔离 provider 分别在 type、code 回显送检文本。两次请求均正确返回 `content_safety_check_failed`，但两个测试原文标记各进入日志 **4 次**。省略 Message 未满足“日志不含原文”。
- **最小修正**：仅记录本地白名单分类、状态码及关联标识；未知 type/code 使用固定类别。补 Message、Type、Code 各自回显原文的隐私断言。

**N2｜P2：真实判官脚本仍会误判非法回答，异常中断时不保存部分结果**

位置：[guardrails-judge-e2e.py:44](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/guardrails-judge-e2e.py:44)、[guardrails-judge-e2e.py:75](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/guardrails-judge-e2e.py:75)、[guardrails-judge-e2e.py:86](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/scripts/guardrails-judge-e2e.py:86)。

- **代码事实**：200 且 `choices` 为真值就算 pass；结果文件写在 `try/finally` 之后。
- **触发与后果**：内存验证 `classify(200, {"choices":[{}]})` 返回 pass，实际没有合法回答。另注入“第一条完成、第二条 TimeoutError”，脚本抛异常后**没有 results.json**，已完成结果未保存。实际复用夹具的 HTTP 超时为 12 秒，而此脚本配置判官超时 20 秒，中途超时有现实入口。
- **最小修正**：校验回答结构和有效内容；将逐条结果及异常信息持久化放入异常也会执行的路径，始终打印现场位置、非零退出。补非法 200、HTTP 故障、运输异常和部分结果保留回归。

**N3｜P2：新增密钥规则仍未达到“每类一真一假”**

位置：[detector_test.go:134](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/secrets/detector_test.go:134)。

- **代码事实**：新增了 12 个边界反例，但 Cerebras、xAI、百炼套餐、MiniMax、百度千帆、百度 ALTAK、阿里 CAIS、飞书/Lark 等新增规则仍没有对应边界反例。
- **触发与后果**：这些规则的最小长度、字符集或 URL 边界放宽时，现有正例仍可能全绿，无法发现误拦。属于方案 §6 和上轮 R8 尚未完成的覆盖要求。
- **最小修正**：按新增规则逐项补配对反例，明确证明各自边界被执行；通用规则仍可能命中时，应断言专用等级/规则边界，而非机械要求零命中。

**N4｜P2：热更新回归没有验证其声称的关键保证**

位置：[handler_test.go:198](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler_test.go:198)、[swap_test.go:125](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/swap_test.go:125)。

- **代码事实**：并发测试不控制提交到发布的交错，不检查 update/reset 状态，只比较最后“是否为 nil”；快照测试创建 `inflight` 后直接丢弃，没有断言旧请求的拒绝选项。
- **触发与后果（静态分析）**：并发更新全部失败、只有 reset 成功时，前者仍可能通过；旧请求错误采用新拒绝文案时，后者也不能发现。这些测试不能完整证明修复记录宣称的顺序及旧请求快照保证。
- **最小修正**：用屏障固定 update/reset 两种交错，检查状态、持久化版本和最后发布内容；对更新前已准入的输出请求断言仍返回旧状态码/文案，对新请求断言新值。

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无。** N1–N4 均属既定行为和验证要求，不需要扩大产品范围。

收敛评审转交项逐条处置：

| 转交项 | 处置及依据 |
|---|---|
| 坏配置拒启缺单测 | **修复**：`TestStoredConfigurationFailuresRejectStartup` 覆盖坏 JSON、缺失 provider 及恢复提示，亲跑通过 |
| 未配置装配只有间接覆盖 | **撤回**：生产 smoke 明确验证初始 `{config:null, version:0}`、正常推理及判官调用数为 0，已亲跑 |
| guardrails 未进入 `make smoke` | **撤回其作为本轮正确性阻塞的要求**：本期 checklist 已把该自包含 smoke 列为必过且亲跑通过；默认集合组织转收敛 |
| 四个重试用例真实等待 | **撤回**：测试明确验证 200+400+800ms 退避及调用次数，符合方案；本轮无缓存判官包测试通过，约 7 秒 |
| 取消错误使用直接比较 | **撤回**：检测器契约为返回 `ctx.Err()`，当前路径直接返回该错误；该断言未造成错误判断 |
| `bErr` 与 `ctx.Err()` 交互 | **撤回**：实测 70ms 超时，block/allow 均只发一次判官请求，记录 timeout；兄弟规则拦截时慢判官取消、评估丢弃，无额外应用层重试 |

另撤回“provider 查询故障原文泄露给客户端”的初步质疑：隔离库查询失败虽然经 Handler 返回原文，但生产 RBAC 响应处理会脱敏；实测客户端只收到 `invalid_input`，服务端保留查询原因。

## 5. 转收敛评审 / 挂账

- 转收敛评审：`ee/Makefile:42` 默认 smoke 集合与本期独立 guardrails smoke 的组织关系。
- 转收敛评审：`ee/internal/guardrails/README.md:92`“数值字段 0 均取默认值”与 `deny.status` 必须显式合法的契约表述。
- 转收敛评审：`host/builder.go`、`config/rules.go` 注释仍引用已改为未导出的 `Validate`。

**挂账：无。**

## 6. 开放式复读的表外发现

已对 34 个范围文件按“失败如何发现、如何恢复”完成开放式复读，**无新增表外发现**。确认的问题已归入 N1–N4。

## 7. 起手表覆盖

| 信号 | 结果 |
|---|---|
| 验收标准有未覆盖项 | **发现**：N2–N4；真实 DeepSeek 本轮未重跑 |
| 方案、实施记录、diff 三方不一致 | **发现**：修复完成声明超过实际覆盖，N1–N4 |
| 多出的功能、公共接口、抽象或依赖 | **无**：本轮接口变化有修复记录，无新增业务功能、依赖 |
| 偏差未登记 | **发现**：上述未完成项未明确登记 |
| 失败被吞或降级成假成功 | **发现**：N2 非法回答算 pass |
| 空值、空集合与非空无分支 | **无**：nil 快照、未配置、空结果均有处理 |
| 取消或超时后仍产生写入或副作用 | **无新增发现**：已实测取消；provider 侧继续执行属已接受限制 |
| 同一请求重发产生两份副作用 | **无**：版本检查、零行冲突和 reset 幂等已核 |
| 事务外写后读、多方写入仅一方回滚 | **无行为缺陷发现**：写库至发布共用锁；测试缺口见 N4 |
| 开关关闭、旧数据、旧客户端无处理 | **无**：无配置、reset、fake 互斥与重启加载已核 |
| 昂贵检查先于廉价检查、校验缺层 | **无新增发现**：体积、取消检查先于解析/扫描 |
| 路由保护、认证关闭时行为 | **无**：EE 强制认证链无旧开关放行路径；匿名 401、权限矩阵通过 |
| 敏感信息进日志或响应 | **发现**：N1 |
| 热路径新增 I/O、无界缓冲或循环 | **无新增发现**：判官 I/O 为批准范围；有文本、超时及重试边界 |
| 配置、环境依赖、部署步骤未登记 | **无** |
| 服务端无法追溯失败原因 | **无新增发现**：R5 已补日志并亲核 |
| 对外泄露内部错误原文 | **无**：生产外层脱敏实测有效 |
| 脚本失败现场被清理 | **发现**：N2 异常时部分结果未落盘；日志目录仍保留 |
| 新规则或失败路径无测试 | **发现**：N3、N4 |
| 断言绑定实现而非行为 | **无新增发现**：原并行数量断言已加屏障；其他证据不足见 N4 |
| 失败信息无法定位条件 | **发现**：N2 中途异常缺结构化部分结果 |
| 返回值、错误未检查 | **发现**：N4 并发测试忽略操作状态 |
| fixture 依赖个人环境 | **无新增发现**：隔离 SQLite、随机端口；真实脚本的 key 要求已注明 |
| mock 结果被写成真实环境结论 | **无**：本轮替身验证明确限定；不以旧真机记录代替亲测 |
| 整体替换模块的 mock | **无**：HTTP smoke 使用真实 EE；脚本替身仅验证脚本行为 |
| 调试输出、临时后门、本机配置 | **无新增发现**：fake 为明确批准的开发模式 |
| 未使用代码、无关改动 | **发现**：N4 的 `inflight` 未参与断言；原 `blockingDetector` 已删除 |
| 生成物与源不一致 | **无差量发现**：规则数据本轮未改，清单产物与视图检查通过 |

## 8. 验证记录

所有二进制、隔离数据库及测试现场均位于指定 `$TMPDIR`：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全检测器-期1-代码评审2/scratch`

| 实际命令 | 结果 |
|---|---|
| `git status --porcelain=v1 --untracked-files=all`、`git branch --show-current`、`git rev-parse HEAD` | 开始分支/候选正确，工作树干净，无未跟踪文件 |
| `git diff --stat b9e8752ea`；`git diff --name-only/--numstat 93e6a1272..fde843b5 -- ee product` | 差量范围为 33 个 EE 文件及 1 个架构文件，与指定范围一致 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race ./...` | 全部通过，部分包使用缓存 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/guardrails/... ./internal/app ./internal/rbac/host` | 禁用缓存重跑，通过 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过；仅调整产物位置 |
| `gofmt -l ee/internal/guardrails ee/internal/app` | 空输出 |
| `find ee/internal/guardrails ee/internal/app -name '*.go' -exec awk 'END{if(NR>500)print FILENAME}' {} \;` | 空输出 |
| `find ee/internal/guardrails -maxdepth 1 -name '*.go' -exec grep -l 'fasthttp\|core/schemas\|net/http' {} \;` | 空输出 |
| `git diff --check`；`git diff --check 93e6a1272..fde843b5` | 通过 |
| `python3 workbench/tools/build_views.py --check` | 189 篇文档校验通过 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | production、fake-conflict、三个 fake 模式全部 PASS |
| `cd ee && GOWORK=off go test -race -count=100 ./internal/guardrails -run 'TestAllowFailureContinuesToBlockingRule\|TestCompletedObserveKeptAndFirstBlockWins\|TestParentCancelDuringParallelRun'` | 通过 |
| `python3 -B -` 逐条核验 checklist 产物、符号、禁留项 | 12 项产物、9 项必含、9 项禁留全部通过 |
| 最终 `git status --porcelain=v1 --untracked-files=all`、`git diff --cached --stat` | 均为空；无源码或索引改动 |
| `git diff fde843b5..HEAD -- <34个范围文件>` | 空；外部新增提交仅改变真机验证文档 |

补充执行的内联验证均使用 `python3 -B - <<'PY' … PY`，没有保存测试源码：

- **错误回显、匿名认证、查询故障**：复现 type/code 原文各入日志 4 次；匿名 401；查询失败有服务端原因、客户端脱敏。现场：`review2-probe-55uou78j`。
- **判官超时**：延迟合法响应，70ms 配置下 block 返回 400、allow 返回 200，分别约 73ms/76ms，各一次判官请求，均记录 timeout。现场：`review2-deadline-_11u0qqe`。
- **兄弟规则取消**：两路判官，一路拦截；约 5ms 返回，慢规则被取消且不进入评估结果，总调用数 2。现场：`review2-cancel-d78xl15z`。
- **脚本分类与异常保留**：43 个 HTTP 500 全部 failure、退出 1；非法 200 被误算 pass；第二条抛异常后无结果文件。现场：`ee-judge-e2e-984y6z4w`、`ee-judge-e2e-9c8_vlbe`。
- 清单 smoke 现场：`ee-guardrails-zth7m9b4`。自包含实例均执行自身停止、等待及回收逻辑。

**实际阅读范围**：指定规则、验收口径、方案 v5、findings、三份指定证据及允许读取的代码评审1；34 个差量文件完整现状及改动。另追读判官/config 测试、规则补充数据、smoke 进程夹具、认证/RBAC 响应出口、provider 错误转换及取消路径。未读取收敛评审1全文或其他旧评审。

**未执行及原因**：

- checklist 必过命令无跳过；未运行会生成报告的预飞包装器，已逐条亲核同一清单。
- 未执行真实 DeepSeek 付费调用：不在必过命令，本轮以替身核查修改与失败路径，不把历史真机结论当成本轮亲测。
- PostgreSQL 测试 DSN 均未设置，未连接或启动 PostgreSQL；本轮存储验证为隔离 SQLite。
- 未重新生成本轮未变动的全量规则数据；已核新增规则与测试对应关系，并运行规则加载和检测测试。
- 未启动 dev server、`--serve` 或连接任何已有实例。

verdict: 需修改
