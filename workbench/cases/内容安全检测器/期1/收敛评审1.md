---
title: 内容安全检测器 期1 收敛评审 #1
type: 收敛评审
case: 内容安全检测器
phase: 1
round: 1
verdict: 有Blocking
created: 2026-09-19
---

> 评审者与实施上下文隔离（独立会话，只读仓库）。基线 `b9e8752ea` 到 `93e6a12727b9180f00e6b1c6e8cf24b9754bfd4f`（工作树干净，`git status --porcelain` 无输出，无未跟踪文件）。
> L2：四维收束为段落，附起手表逐行结果与开放式复读结论。

## 结论

模块划分、依赖方向与装配形状与 [EE 后端架构](../../../../product/架构/EE后端架构.md) 和仓内同类实现（branding / rbac）一致：根包只用标准库、`config`/`detectors`/`persistence`/`http`/`host`/`plugin` 各守其职、窄接口由使用方定义并经构造函数注入、没有反向依赖或跨模块改表，数据（4290 行规则）与代码分离且带转换脚本与 NOTICE，文件粒度合理（最大 261 行，无 300 行以上文件）。但有三处必须本轮处理：`deny` 配置在热更新路径上根本不生效（配置对象被拆成"热"的检查器和"冷"的插件选项两种生命周期）、PostgreSQL 迁移顾问锁键与 rbac 完全相同而注释恰好声明"不相同"、等级字符串到 `guardrails.Level` 的映射在三个包里各写一份（其中两份逐字相同）。另有七处小修（文档失效、切片别名依赖未写明的不变量、map 遍历导致错误信息不确定、routes.txt 破序等）。修复项未关闭，verdict 取 `有Blocking`；无需用户裁决的事项。

## 事实来源

**跑过的命令**（均为只读）：`git status --porcelain`、`git rev-parse HEAD`、`git diff --stat b9e8752ea HEAD`、`git diff b9e8752ea HEAD -- <文件>`（bootstrap.go / detector.go / rbac 三件 / app 两个测试 / plugin 两个测试）、`git ls-files`（ee/internal/app、ee/scripts、ee/docs、各模块 http 包）、`git grep`（`RetrySQLiteBusy|sqliteBusy|busyRetries`、`834276|advisory`、`开发护栏`、`RuleCount`、`"sk-bf-"`、`SyncLoadedPlugin`、`maximhq/bifrost` 于根包与 detectors、导出符号使用方、`^@ managed Settings`、`smoke` 于 ee/Makefile、`api/branding` 于 docs/ui）、`git grep -c ""` 统计行数。

**完整读过的范围内文件**：`guardrails/README.md`、`checker.go`、`evaluation.go`、`detector.go`、`config/config.go`、`config/rules.go`、`config/config_test.go`、`detectors/judge/{detector.go,parse.go,detector_test.go}`、三份 `prompts/*.md`、`detectors/secrets/{detector.go,rules.go,scan.go,detector_test.go}`、`data/{README.md,NOTICE,rules.extra.json}`、`host/{builder.go,judge.go,host_test.go}`、`http/{handler.go,handler_test.go}`、`persistence/{migration.go,store.go,store_test.go}`、`plugin/plugin.go`、`plugin/swap_test.go`、`checker_test.go`、`parallel_test.go`、`app/{guardrails.go,bootstrap.go,guardrails_test.go}`、`rbac/host/routes.txt` 相关段、`ee/scripts/{guardrails-smoke.py,guardrails-judge-e2e.py,secrets-rules-convert.py}`；`plugin/{plugin_test.go,text_test.go}`、`app/pricing_test.go`、`rbac/host` 三件按 diff 读。范围外但作为现状上下文完整读过：`plugin/{deny.go,text.go}`、`rule.go`。`data/rules.json` 只读结构与抽样（4290 行，判断依据是生成脚本与 extra 文件）。

**对照的同类实现**：`branding/http/{handler.go,dto.go}`（POST 三件套、`parseUpdateRequest` 的 `dec.Decode(&struct{}{}) != io.EOF` 惯例、`no-store`、`upstream.SendJSON/SendError`）、`branding/persistence/migration.go`（迁移 ID + 顾问锁 + busy 重试的整体形状）、`identity/persistence/{diagnostics.go:51-77,scope.go:122-123}` 与 `rbac/persistence/migration.go:29`（已导出的 `RetrySQLiteBusy` 复用先例）、`rbac/host/routes.txt`（分组与组内排序惯例）、`app/{bootstrap.go,README.md}`、`ee/README.md`。

**核实过的上游事实**（结论依赖它们）：`core/utils.go:486-489` `PrepareContextForInternalRequest` 确实 `SetValue(BifrostContextKeySkipPluginPipeline, true)`，README"判官子请求不会再经过本插件"成立；`transports/bifrost-http/server/server.go:2033` `SyncLoadedPlugin(ctx, name, plugin, placement, order)` 签名与调用一致；`plugins/governance/main.go:30` `VirtualKeyPrefix = "sk-bf-"` 存在于插件模块（详见有理由保留项）。

**未执行**：任何测试、构建、冒烟、服务启动与真机判官调用（只读评审）；性能/并发/安全实测；`rules.json` 逐条校对；历史评审记录与旧收敛评审（按规则不读）。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 等级字符串映射三份（R3）；SQLite busy 重试第三份、未用已导出的复用出口（S1）；`400–599` 与 `64KB` 各两到三处独立字面量（S2）；e2e 脚本复制冒烟脚本的节点配置（S3） | `config/config.go:210`、`secrets/rules.go:142`（逐字相同）、`judge/parse.go:40`；`persistence/migration.go:22-47` vs `identity/persistence/scope.go:122`＋`rbac/persistence/migration.go:29`；`config/config.go:18-19` vs `plugin/deny.go:26`、`judge/detector.go:40`、`app/guardrails.go:32`；`guardrails-judge-e2e.py:51-60` vs `guardrails-smoke.py:181-189` |
| 职责与实现质量 | 顾问锁键与 rbac 撞号且注释自称不撞（R1）；`slots` 别名依赖未写明的 cap 不变量（R5）；`Validate` 用 map 遍历（R6）；switch 后孤立的 `err` 兜底（R7）；错误消息按字节截断（R9）；三处注释与现状不符（R10）；`no-store` 三处重复（S5） | `persistence/migration.go:19-20`；`checker.go:91,97,105`；`config/config.go:130`；`app/guardrails.go:69-88`；`host/judge.go:106-109`；`evaluation.go:43`、`secrets/detector.go:38`、`app/guardrails_test.go:1`；`http/handler.go:65,80,114` |
| 架构与项目一致性 | 符合（详见下方两项专门核对），无另起基础设施、无反向依赖、无绕过既有出口 | 见「架构符合性」「依赖关系」 |
| 契约一致性 | `deny` 只在启动期生效，热更新只换检查器（R2）；模块 README 与架构文档仍写"真实检测器尚未接入"（R4）；显式 `0` 与省略不可区分（S6） | `app/guardrails.go:70,84` vs `plugin/plugin.go:49`；`app/README.md:5,12,16,29,31`、`ee/README.md:18`、`product/架构/EE后端架构.md:54`；`config/config.go:97-113` vs `:126` |

**架构符合性（EE 后端架构 §3/§4/§6/§7）**：模块落在 `ee/internal/guardrails/`，与 §3 布局一致；模块内部按 §4 表分层——根包 `checker.go`/`evaluation.go`/`rule.go`/`detector.go` 承载规则与检测器契约，`http/handler.go` 只做解析→调用→映射响应（`handler.go:79-111` 的 update 是"校验→构建→写库→热替换"四步，业务判断在 `config`/`host`），`plugin/plugin.go` 只把检查结果翻译成放行/短路，`persistence/` 只管表与事务，`host/` 按 §7 第二条"只服务某项业务的宿主检查放在 `<模块>/host/`"承载判官子请求适配与 Builder（对照先例 `rbac/host/`）。§6"护栏的管理接口放在 `guardrails/http/`、实时检测入口放在 `guardrails/plugin/`，共同调用模块内的业务服务"被逐字满足。`app/guardrails.go` 只做迁移→读库→构建→注册（`:43-112`），没有把业务编排搬进 `app`：唯一的业务判断（配置解析与检查器构建）都委托给 `config.Parse` 与 `host.Builder`，与 `app/bootstrap.go:51-71` 装配品牌/定价的既有形状一致。内嵌 Bifrost 的边界保留：共享 `host.Router`、`host.Config.ConfigStore`、`host.Client`，插件经上游 `SyncLoadedPlugin`（`server.go:2033`）注册，表经上游 `ConfigStore.RunMigration` 建（`app/guardrails.go:53`），没有另起网关、另开数据库连接或另建迁移体系。§7"复用完整操作"方面，判官走 `bifrost.PrepareContextForInternalRequest`（已核实语义）而不是自己拼 HTTP 客户端。

**依赖关系（EE 后端架构 §5/§7）**：实际 import 核对——根包四个文件只 import `context/errors/fmt/sync/reflect/strings/time/unicode/utf8`；`git grep "maximhq/bifrost" -- 根包四文件 config detectors` 无输出，即 `config` 与两个检测器包都不含上游类型，满足方案 §2 的自定约束；`host`（`core/schemas`、`framework/configstore`、`core`）、`http`（`fasthttp`、`router`、上游 handlers）、`persistence`（`gorm`、`sqlite3`、上游 `configstore`/`migrator`）、`plugin`（`core/schemas`）各自把具体类型关在适配层，业务根包不反向导入任何适配子包。接口由使用方定义且都窄：`host.Client`（1 方法，`judge.go:18-20`）、`host.ProviderLookup`（1 方法，`builder.go:17-19`）、`host.Logger`（Info/Warn）、`plugin.Logger`（只 Info）、`http.Builder`/`Swapper`/`Locked`（`handler.go:24-32`）、`judge.Model`（`detector.go:19-21`）；全部经构造函数显式注入（`NewBuilder`/`NewJudgeModel`/`NewHandler`/`New`），没有从全局 Server/Config/容器取依赖，也没有万能 Repository。跨模块：不读写其他模块的表；唯一跨模块产物是在 `rbac/host/routes.txt` 登记两组路由，这是 branding/identity 共用的既有登记处，且由 `app/bootstrap.go:43` 的 `VerifyRoutes` 强制。资源所有权明确：`persistence.NewStore` 注释声明"复用传入连接，不负责关闭"（`store.go:39`），`plugin.Cleanup` 声明不关闭检测器（`plugin.go:54`），与 §7"生命周期统一协调"一致。唯一的依赖异味是 S1（重复实现而非复用 `identity/persistence.RetrySQLiteBusy`），属复用取舍而非方向错误。

## 出口清单

### 本轮修复项

**R1 · PostgreSQL 顾问锁键与 rbac 撞号，注释恰好声明不撞**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/persistence/migration.go:19-20`
代码事实：`guardrailsAdvisoryLockKey = 8342761903`，注释写"必须稳定，且不与上游及其他 EE 模块的锁键相同"；`ee/internal/rbac/persistence/migration.go:20` 的 `migrationLock int64 = 8342761903` 是同一个值（branding `...901`、identity `...902`）。
维护影响：注释与代码直接矛盾，下一个模块继续按注释"取下一个未用值"时无从判断已用集合；PostgreSQL 上 rbac 与 guardrails 的迁移事务互相串行，排查启动期迁移阻塞时会指向错误的模块。
最小修正：改为 `8342761904`，并照 `identity/persistence/migration.go:19-21` 的写法在注释里列出已占用的 `...901/902/903`。

**R2 · `deny` 配置只在启动期生效，热更新只换检查器**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails.go:70,84` 与 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin.go:17-20,43-49`
代码事实：`safetyplugin.Options{StatusCode, DenyMessage}` 在启动时由 `cfg.Deny` 构造（`guardrails.go:84`）或取占位值（`:70`，`400` + `placeholderDenyMessage`），之后存进 `Plugin.options` 且再无写入路径；`Swap(checker *guardrails.Checker)` 只替换检查器指针。`http/handler.go:109` 的 update 成功路径也只调用 `Swap(checker)`。因此 `POST /api/guardrails/update` 修改 `deny.status` / `deny.message` 后，运行中进程仍按启动时的状态码与文案拒绝，直到重启；未配置启动的实例永远返回占位文案。
维护影响：同一个配置对象被拆成两种生命周期，一半热、一半冷，而接口与 README（`guardrails/README.md:94`"成功后新配置只对之后到达的请求生效"）都表述为整份配置生效；冒烟脚本用的 `deny` 恰好等于占位值（`guardrails-smoke.py:105`：`status 400` + `'内容未通过安全检查'`），所以这条缝隙在现有验证里不可见，后续改 `deny` 的人要靠重启才能发现。
最小修正：把拒绝策略和检查器放进同一个快照一起替换——例如 `Swap(checker *guardrails.Checker, options Options)`（或一个内含两者的小结构），`buildDenialError` 从请求级快照取值；若确定口径就是"`deny` 仅启动期生效"，则须在 `guardrails/README.md` 配置与接口一节和方案 §4.6 明写，并让 update 响应或日志提示需要重启。

**R3 · 等级字符串到 `Level` 的映射三份，两份逐字相同**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/config/config.go:210-220`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/secrets/rules.go:142-152`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/judge/parse.go:40-50`
代码事实：前两处是同名同签名同实现的 `parseLevel(s string) (guardrails.Level, bool)`；第三处是其超集（多 `none`、做大小写折叠）。根包 `detector.go:13-17` 只定义了 `Low/Medium/High` 三个数值常量，没有对应的字符串名。`"low"/"medium"/"high"` 这三个字面量同时出现在管理员配置 JSON、`data/rules.json` 的 `level` 字段和判官的输出契约里。
维护影响：违反编码规范硬规则"同一条规则只在一处实现"与"对外枚举必须是命名常量"；新增一个等级或改写法要同时改三处，漏一处的表现是某一路默认落到"非法等级"（`evaluation.go:87` 记 `InvalidResult`）而不是编译错误。
最小修正：在根包 `detector.go` 定义等级名常量与一个 `ParseLevel(string) (Level, bool)`，`config` 与 `secrets` 直接调用；`judge/parse.go` 只保留 `none→0` 与大小写折叠后再委托。

**R4 · 模块 README 与架构文档仍写"开发护栏 / 真实检测器尚未接入"**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/README.md:5,12,16,29,31`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/README.md:18`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/product/架构/EE后端架构.md:54`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/bootstrap.go:17,50`
代码事实：`app/README.md:16` 把 `guardrails.go` 的职责写成"开发假检测器的显式启用、场景装配和 BF 插件注册"，`:31` 写"默认关闭且不持久化"；`ee/README.md:18` 写"真实检测器尚未接入"；架构 §3 结尾写"`guardrails` 已有本地规则框架和 Chat Hook，真实检测器尚未接入"；`bootstrap.go:17,50` 两处注释写"接入开发护栏"。本次改动后 `assembleGuardrails` 会建表、读库、构建生产检查器、失败拒启并注册三条管理路由（`app/guardrails.go:53-102`）。
维护影响：编码规范要求"包的功能、文件或约束发生变化时同步更新"README，且判断现状只看文档的人会得出"这只是联调夹具"的相反结论，进而漏掉启动期拒启、配置表和权限登记这些真实约束。
最小修正：改这四处措辞（`app/README.md` 另需补一行"配置表与三条接口、坏配置拒启"）；架构 §3 那句按流程"知识沉淀"在 Gate 2 前一并更新。

**R5 · `slots` 取址依赖未写明的 cap 不变量**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/checker.go:91,97,105`
代码事实：`slots := make([]slot, 0, len(c.rules))`，循环里 `slots = append(slots, slot{})` 后把 `&slots[len(slots)-1]` 交给 goroutine 写入。当前安全仅因为 cap 恰好等于规则总数、append 次数必然不超过它；没有任何注释说明这一点。
维护影响：这是"改一个看似无关的初始化就静默出错"的形状——把 cap 去掉或改小，append 触发扩容后 goroutine 写进被丢弃的底层数组，表现是评估结果（包括一条 Block）凭空消失且无报错，`-race` 也未必报。
最小修正：先收集本阶段规则再定长分配——`staged := 本阶段规则切片; slots := make([]slot, len(staged))`，循环按下标传 `&slots[i]`，取消对 append 的依赖。

**R6 · `Validate` 用 map 遍历，报错信息不确定**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/config/config.go:130`
代码事实：`for name, item := range map[string]Item{"secrets": ..., "harmful": ..., "prompt_attack": ...}`，Go 的 map 遍历顺序随机；同一份含多个非法项目的配置每次返回的错误字段名不同。
维护影响：同一个坏配置在接口上得到随机的 400 文案，用户与定位问题的人无法复现；同包 `Expand()`（`rules.go:40-70`）用固定顺序，两者口径不一致。
最小修正：改为遍历固定顺序的切片（`[]struct{name string; item Item}`，顺序与 `Expand` 一致）。

**R7 · switch 之后孤立的 `err` 兜底只服务一个分支**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails.go:69-88`
代码事实：`case configured:` 分支内部已对 `builder.Build` 的错误直接 `return`（`:81-83`），只有 `case mode != "":` 的 `checker, err = fakeChecker(mode)` 把错误留给 switch 之后的 `if err != nil { return err }`（`:86`）；而这个 `err` 还是 `:65` `NewBuilder` 那个已检查过的变量。
维护影响：读者必须回溯三十行才能确定这个兜底在防谁，且复用已消费的 `err` 变量，以后在 switch 里加分支很容易误以为错误已被统一处理。
最小修正：在 `case mode != "":` 内部就地判错返回，删掉 `:86-88` 的兜底。

**R8 · routes.txt 新增两行破坏组内按路径排序**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/rbac/host/routes.txt:673-677`
代码事实：`@ managed Settings.Manage none ordinary` 组内现为 `POST /api/guardrails/reset`、`POST /api/guardrails/update`、`DELETE /api/cache/clear-by-key/{cacheKey}`、`DELETE /api/cache/clear/{cacheId}`；文件其余各组（如 `:659-667`、`:679-684`、`:698-703`）一律按路径升序排列。
维护影响：这是一份手维护的 820 行权限登记表，靠有序才能快速查一条路径是否已登记、合并时才不会产生假冲突；一处破序会被后续登记照抄。
最小修正：把两行移到两条 `/api/cache/...` 之后。

**R9 · 判官错误消息按字节截断会切断多字节字符**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:106-109`
代码事实：`if len(msg) > maxErrorMessageBytes { msg = msg[:maxErrorMessageBytes] + "…" }`，按字节切片。
维护影响：这段消息正是偏差记录 1 专门为排障保留的（鉴权、限流、模型不存在），国内 provider 的错误文案多为中文，超过 200 字节时尾部会变成乱码字节并进日志与错误链；日志消费方（grep、结构化采集）遇到非法 UTF-8 行为不确定。
最小修正：截断后用 `strings.ToValidUTF8(msg, "")`，或回退到最后一个完整 rune 边界再拼 `…`。

**R10 · 三处注释与现状不符**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/evaluation.go:43`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/secrets/detector.go:38`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails_test.go:1`
代码事实：① `evaluationState` 注释写"区分规则已完成、因其他规则拦截被取消，以及父请求取消"三态，但常量只有 `completed`/`discarded` 两个（`:46-49`），父取消靠第三个返回值 error 表达；② `RuleCount` 注释写"供装配与测试核对数据文件"，`git grep RuleCount` 显示唯一使用方是 `detector_test.go:55`，装配侧没有调用；③ `app/guardrails_test.go:1` 写"关闭或错误配置不会访问宿主插件链"，而本次已删除 `mode==""` 那条断言，文件里只剩 `validateFakeMode`/`fakeChecker` 的检查。
维护影响：编码规范把"与代码不一致的注释视为缺陷"；①会让人以为还有第三个状态常量待补，③会让人以为装配路径仍有守卫覆盖。
最小修正：①改为两态并点明父取消由 error 传出；②删掉"装配"或说明它是数据文件的回归锚点；③按文件现有断言重写首行。

### 需用户裁决的 Blocking

- 无。

### 立债（REF）

- 无。

### 挂账（Finding，建议取舍项；由实施者决定采纳或撤回，未采纳的按四要件登记）

**S1 · SQLite busy 重试的第三份实现，未复用已导出的出口**
位置：`ee/internal/guardrails/persistence/migration.go:22-47`（`busyRetries`/`busyRetryStep`/`sqliteBusy`/重试循环）。仓内现状：`identity/persistence/scope.go:122-123` 专门导出了 `RetrySQLiteBusy(ctx, attempt)`，注释写"供共用配置库的迁移复用相同重试规则"，`rbac/persistence/migration.go:29` 已按此复用；branding 是历史上的第二份。新代码的循环体与 `identity/persistence/diagnostics.go:64-77` 行为逐行等价。
维护影响：驱动兼容策略现在有三份，注释互相声明"改动时须同步"，但没有机械护栏；**FIND-017 的转 Case 条件"第三个模块需要同样的 busy 重试"已经满足**。
最小修正：`Migrate` 改为 `authstore.RetrySQLiteBusy(ctx, func() error { return migrateOnce(ctx, db) })`，与 rbac 一致；或按 FIND-017 的另一条出路抽成 EE 内共享小包，把 branding 一并收编。
反证条件：若判定 `guardrails` 不应依赖 `identity/persistence`（跨模块包依赖比 15 行复制更贵），则保留复制，但须把 FIND-017 从"观察中"改为已裁决的豁免并写明理由，不能让触发条件悬空。

**S2 · 同一业务限制的多份字面量**
位置：`config/config.go:18`（`MinDenyStatus, MaxDenyStatus = 400, 599`）与 `plugin/deny.go:26`（`minErrorStatus, maxErrorStatus = 400, 599`）校验的是同一个值（`app/guardrails.go:84` 把 `cfg.Deny.Status` 直接传成 `Options.StatusCode`）；`config/config.go:19`（`DefaultMaxTextBytes = 64*1024`）、`judge/detector.go:40`（`DefaultMaxBytes = 64*1024`，注释自称"与框架默认总上限一致"）、`app/guardrails.go:32`（`fakeMaxTextBytes = 64*1024`）是同一个 64KB 的三次手抄。
维护影响：编码规范"业务限制要命名，派生值要写推导"要求派生值用表达式算出；下调默认上限时后两处会静默留在 64KB。
最小修正：状态码上下界只在一处定义（可放根包，两侧引用）；`judge.DefaultMaxBytes` 与 `fakeMaxTextBytes` 改为从单一来源推导或直接注释"仅在未注入时生效，数值须与 config.DefaultMaxTextBytes 同步"。
反证条件：`judge` 与 `plugin` 都被要求不依赖 `config`（`config` 是序列化边界），若确认无法在不破坏分层的前提下共享常量，则保留三份但补上互相引用的注释。

**S3 · e2e 脚本复制冒烟脚本的节点配置**
位置：`ee/scripts/guardrails-judge-e2e.py:51-60` 与 `ee/scripts/guardrails-smoke.py:181-189` 的 `cfg/config` 字典（client / framework.pricing 四个 URL / config_store / governance / providers.openai）几乎逐字相同；e2e 已经 `load('guard_smoke', ...)` 并复用了 `MODEL`、`ModelHandler`、`completion`。
维护影响：与 FIND-028（pricing-smoke 复制 identity-smoke 启动逻辑）同一类；上游配置结构一变要改两处，漏改的表现是 e2e 脚本起不来而冒烟能跑。
最小修正：在 `guardrails-smoke.py` 抽一个 `node_config(root, base, password, username)`，两边共用。
反证条件：若判官 e2e 是一次性实验工具、验证记录出具后不再维护，则保留复制，但在文件头写明"一次性实验脚本，不随冒烟演进"。

**S4 · 包外无使用方的导出符号**
位置：`config/config.go:18`（`MinDenyStatus`/`MaxDenyStatus`）、`:21-22`（`DefaultJudgeTimeoutMS`/`DefaultSecretsTimeoutMS`）、`:116`（`Validate`，仅被同包 `Parse` 调用）、`config/rules.go:12,19`（`ItemSecrets`、`JudgeDetectorPrefix` 只在本文件内使用）、`judge/detector.go:35-36,40`（`MinRetries`/`MaxRetries`/`DefaultMaxBytes`）。`git grep` 确认这些符号在各自包外（含外部测试包）没有使用方。
维护影响：编码规范"包外没有使用方的符号不导出"；导出面越大，以后改默认值或边界越要当成对外契约处理。
最小修正：降为未导出，保留确有外部使用方的 `DefaultMaxTextBytes`、`DefaultJudgeRetries`、`SecretsDetectorID`、`ItemHarmful`/`ItemPromptAttack`/`ItemBusinessRule`、`ErrInvalid`、`ErrInvalidVerdict`。
反证条件：期 2 的管理页面要用同一套边界做前端表单校验、或接口文档要把它们列为契约，则保留导出并在注释里点明"对外契约"。

**S5 · `Cache-Control: no-store` 在三个处理函数各写一遍**
位置：`ee/internal/guardrails/http/handler.go:65,80,114`。同类实现 `branding/http/handler.go:95-96` 把它收在 `brandingJSON` 一处（只有 `asset` 因缓存策略不同单独设置）。
维护影响：同一响应头效果做三次，新增端点容易漏写。
最小修正：收进一个统一的响应辅助函数，或在 `RegisterRoutes` 包一层。
反证条件：若后续 `get` 要改成可缓存（例如带 ETag），分散声明反而更直白，则保留现状。

**S6 · 显式 `0` 与省略不可区分**
位置：`ee/internal/guardrails/config/config.go:97-113` 的 `applyDefaults` 对 `max_text_bytes`、`judge.retries`、`judge.timeout_ms`、`judge.max_text_bytes`、`secrets.timeout_ms` 一律把 `0` 当作"未提供"填默认值；`:126` 的 `retries` 必须落在 3–5 的校验因此对显式 `"retries": 0` 永远不会触发（静默变成 3）。
维护影响：管理员写 `"retries": 0` 期望"不重试"，得到的是 3 次重试且没有任何提示；接口的三态语义（省略 / 显式 0 / 有效值）与校验文案对不上。
最小修正：README 配置一节明写"0 或省略均取默认值，重试次数不可低于 3"；或把这几个字段改成指针以区分省略与显式 0。
反证条件：若产品口径就是"0 即默认"，只补文档即可，不改结构。

**S7 · 冒烟脚本靠提示词正文识别判官调用**
位置：`ee/scripts/guardrails-smoke.py:19`（`JUDGE_PROMPT_HINT = '审核器'`）与三份 `prompts/*.md` 的首句；两侧都没有注明这条耦合。
维护影响：改写提示词开头时，替身模型会把判官调用当成业务模型调用，判官计数断言随之全错，排查要先怀疑网关。
最小修正：在 `prompts/` 目录或三份提示词里加一行"'审核器'字样被冒烟替身用于识别判官请求"，或改用更稳定的判据（如 system 里出现 `risk_level`）。
反证条件：计数断言失败是显式且立即可见的，不会静默放过；若认为这足够，保留现状即可。

**S8 · `ee/docs/` 缺内容安全接口文档**
位置：`ee/docs/` 现有 `账号认证接口.md`、`角色权限接口.md`、`国内定价.md`，`ee/README.md:49-55` 按功能给出入口；本次新增的三条公开管理接口只在 `ee/internal/guardrails/README.md:91-95` 描述。
维护影响：接口契约（版本号语义、409/400 口径、权限要求）现在只存在于包内 README，与其他 EE 对外接口的存放惯例不一致，前端接入时找不到对应页面。
最小修正：补一页 `ee/docs/内容安全接口.md`，或在 `ee/README.md` 明确指向包内 README。
反证条件：期 2 交付管理页面时一并写接口文档更省事（方案已把 UI 列为非目标），则本期只在 `ee/README.md` 加一行指引。

### 转代码评审（范围外发现，一行一条，不出结论）

- 方案 §6 列的"启动时坏配置拒启（单测）"未见对应单测：`app/guardrails_test.go` 只覆盖 `validateFakeMode`/`fakeChecker`，拒启路径（`app/guardrails.go:76-83`）无直接断言。
- `app/guardrails_test.go` 删除了原先 `mode==""` 时 `assembleGuardrails` 返回 nil 的断言，未配置装配路径现仅被 `pricing_test.go` 的 attach 用例间接覆盖。
- `ee/scripts/guardrails-smoke.py` 未接入 `ee/Makefile:42-46` 的 `make smoke`（现含 pricing/branding/identity/rbac），生产配置链路不在默认冒烟集合内。
- `detectors/judge/detector_test.go:102-125` 的四个子用例各自真实等待约 1.4s 退避，单测总耗时约 6s；退避基数 `retryBackoff` 不可注入。
- `detectors/secrets/detector_test.go:164` 用 `err != context.Canceled` 而非 `errors.Is` 断言取消错误。
- `host/judge.go:76-79`：`bErr` 非空且 `ctx.Err()` 非空时返回 `ctx.Err()`，判官重试与框架取消的交互边界建议实测复核。

### 有理由保留（写出理由，下一轮不再重提）

- **三份提示词共用的框架句（stage/text 说明、"只输出 JSON"）不抽模板**：提示词按类别独立调优，模板化会把三者绑死；方案已把"提示词效果调优"列为非目标。
- **`secrets` 自定义 `gatewayKeyPrefix = "sk-bf-"`（detector.go:14）不复用 `plugins/governance/main.go:30` 的 `VirtualKeyPrefix`**：detectors 包被方案 §2 约束为不依赖上游与网络库，为一个字符串引入插件模块依赖代价更大；上游自己也在三处写了该字面量。
- **`evaluation.go:58-76` 的多重取消判定**：父取消/拦截取消/超时三条路径各有注释，且偏差记录 3 说明了为什么不能只看 `parent.Err()`，`parallel_test.go:83-105` 有确定性用例守着；不要求简化。
- **`http/handler_test.go` 用包内测试（`package guardrailshttp`）而其余包用外部测试包**：该测试需要访问未导出的 `maxBodyBytes`（`:120`），是有理由的例外。
- **`attach`（bootstrap.go:52-58）与 `assembleGuardrails`（guardrails.go:50）对 `Config`/`ConfigStore` 的重复 nil 守卫**：核实两处防的是同一对象，但后者还额外校验 `Client`/`Router`，作为函数自身的入口自检可接受，删掉收益不抵风险。
- **`rules.extra.json` 中约 20 条规则重复的终止符片段 `(?:[\x60'"\s;]|\\[nr]|$)`**：`_note` 已说明"终止符沿用 Gitleaks 写法"，这是数据文件对上游格式的贴合，模板化会让规则不能再直接与 Gitleaks 对照。

## 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 发现：S2（400–599 两处、64KB 三处）、R1（锁键撞号） |
| | 同一规则多份实现且互不注明 | 发现：R3（parseLevel 三份，两份逐字相同且互不注明） |
| | 重造已有 helper | 发现：S1（未用 `identity/persistence.RetrySQLiteBusy`，rbac 已有复用先例） |
| | 派生常量无推导 | 发现：S2（`judge.DefaultMaxBytes`、`fakeMaxTextBytes` 手抄 64KB） |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 发现：R1；其余阈值均有命名常量（`config/config.go:17-25`、`judge/detector.go:33-43`、`scan.go:11`） |
| | 状态码、错误码裸数字 | 无：Go 侧统一用 `fasthttp.Status*` 与命名常量（`http/handler.go`、`plugin/deny.go:16-23`）；脚本按编码规范可独立书写字面量 |
| | 无名的错误形状 | 无：`evaluateRule` 的三元返回由命名类型 `evaluationState` 承载（`evaluation.go:43-49`） |
| | 紧凑但绕的控制流 | 发现：R7（switch 后孤立兜底）、R5（append + 取址）；`PreLLMHook` 的 `reject` 闭包（plugin.go:70-72）核实为 3 行局部收敛，不计 |
| | 文件集中或拆分是否妨碍理解 | 无：最大文件 261 行（checker_test.go），Go 源文件均 ≤185 行；`secrets` 的 rules/scan、`judge` 的 detector/parse 各成完整一段，读一个操作不需反复跨文件 |
| | 多余状态或守卫 | 无：核实 `attach` 与 `assembleGuardrails` 的重复 nil 检查防同一对象但各有入口自检职责（见有理由保留） |
| | 同一效果做两次 | 发现：S5（`no-store` 三处） |
| | 注释与现状矛盾 | 发现：R1、R4、R10（共六处） |
| 架构与项目一致性 | 接缝形状与同类不同 | 无：逐项对照 branding（POST 三件套 + `RegisterRoutes(r, auth)` + `upstream.SendJSON/SendError` + `parseUpdateRequest` 的单对象校验）与 rbac（routes.txt 登记、host 子包）均一致 |
| | 架构符合性（EE 后端架构） | 无：见「四维核查」专段，§3 布局、§4 职责表、§6 双入口、§7 集成归位逐条核对一致 |
| | 依赖关系 | 无：见「四维核查」专段，import 实测、窄接口、显式注入、资源所有权均符合 §5/§7；唯一异味是 S1 的复用取舍 |
| | 绕过既有出口 | 无：建表经 `ConfigStore.RunMigration`、响应经 `upstream.SendJSON/SendError`、鉴权经注入的 `auth` 中间件并在 routes.txt 登记、插件经 `SyncLoadedPlugin` |
| | 把上游现状当契约 | 无：`PrepareContextForInternalRequest`（core/utils.go:486-489）与 `SyncLoadedPlugin`（server.go:2033）已读源码核实，非凭印象 |
| 契约一致性 | 后端类型/前端类型/开发指南三方不一致 | 发现：R4（三份文档失效）、S8（无 ee/docs 接口页）；本期无 UI，无 OpenAPI / config.schema.json 影响（EE 接口本就不登记在上游契约中，已用 `git grep api/branding -- docs ui` 核实惯例） |
| | 状态码口径不一 | 发现：R2（`deny.status` 热更新不生效，运行时状态码与库中配置不符）；接口侧 400/409/500 口径与方案 §4.6 一致 |
| | 省略、空、值三态语义不对应 | 发现：S6（显式 `0` 与省略同解）；`version` 的 0/未配置/`config:null` 三态在 `store.go:23-28`、`handler.go:68,120`、README:94 三处一致，无问题 |

## 开放式复读

已做开放式复读（放下表，按"半年后接手要改这里的人"逐文件再读一遍）。表外发现三条，与表内同等对待，均已写入本轮修复项：

- **R2**（`deny` 只在启动期生效）——读 `app/guardrails.go` 与 `plugin.Swap` 时问"这份配置里有几样东西会随热更新变"，才发现拒绝策略与检查器被拆成两种生命周期，且冒烟用例的取值恰好掩盖了它。
- **R5**（`slots` 取址）——读 `runStage` 时第一眼在意的是"goroutine 写进的地址由 append 决定"。
- **R9**（字节截断）——读判官错误路径时注意到国内 provider 的中文错误文案会被切断。

其余表外观察（提示词框架句重复、终止符重复、退避不可注入等）经权衡未构成问题，理由见「有理由保留」与「转代码评审」。

## 台账对账

- **FIND-017（SQLite busy 重试多份）：触发条件已满足并恶化**。`convert_when` 写的是"第三个模块需要同样的 busy 重试"；`guardrails/persistence/migration.go:22-47` 正是第三份（branding、identity 之外），且此次是在 `identity/persistence.RetrySQLiteBusy` 已导出、rbac 已复用的前提下另写的。处置见 S1，未自行裁决。
- **FIND-028（冒烟脚本复制启动逻辑）：同类再现**。`guardrails-judge-e2e.py` 复制 `guardrails-smoke.py` 的节点配置（S3），与该条描述的模式一致，可在同一次收口中一并处理。
- **FIND-024 / FIND-025（`internal/host` 归属与认证扩展点）：未恶化**。本次新增的宿主适配放在 `guardrails/host/`，与 FIND-024 认定的正确方向一致，没有往 `internal/host/` 添文件，也没有在认证包上新开构造选项。
- **FIND-026 / FIND-027、FIND-012～016、FIND-018～023、FIND-030：本次未触及**。
- 无新增 Finding 条目建议（本轮取舍项 S1–S8 先交实施者处置，未采纳者再按四要件登记）。
