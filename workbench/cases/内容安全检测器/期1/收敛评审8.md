---
title: 内容安全检测器 期1 收敛评审 #8
type: 收敛评审
case: 内容安全检测器
phase: 1
round: 8
verdict: 有Blocking
created: 2026-09-19
---

> 评审者与实施上下文隔离（独立只读会话）。差量复核：上一轮候选 `5b0311cea` → 本轮候选 `2b7e0e69923f77d092d9c64e6ead3a33e2072369`（`git status --porcelain=v1 --untracked-files=all` 无输出，工作树干净，无未跟踪文件）。
> L2：四维收束为段落，附上一轮修复项逐条处置、起手表逐行结果与开放式复读结论。

## 结论

上一轮三条（必修 R1、R2，取舍 S1）都动了，其中 R2、S1 完整关闭，R1 的旧错被删除但改写后的句子引入了同一类新错，因此未能关闭。本轮生产侧只有一处实质改动：状态码派生类别由 `provider_*` 改名 `http_*`，函数由 `providerFailureCategory` 改名 `statusCategory`，注释改为「状态码可能来自 provider 响应，也可能是网关合成的 502/503」。这个方向是对的，而且顺带消掉了一处此前无人提到的巧合——上游 `docs/openapi/schemas/management/governance.yaml:2388` 的治理失败枚举里就有 `provider_unavailable`，本模块此前用同名字符串表达完全不同的判定，改名后不再撞。测试侧同步改断言、删掉一次无作用的取址拷贝、把 `bad := 502` 挪到它对应的注释之下。零包移动、零文件增删、零 import 变化，`git grep -c "darkBaryon/bifrost/ee/internal" -- ee/internal/guardrails` 逐文件合计仍为 40，与上一轮逐条相同，模块边界与依赖方向未变。

必修一条：`README.md:101` 这次按「带状态码 / 不带状态码」两分法重写，但代码的分流第一判据是 `IsBifrostError`（`judge.go:115`），不是状态码有无。上游的取消是 499、超时是 504，两者都带状态码且 `IsBifrostError` 为 true——正是 README 自己在第二个分句里举的两个例子。于是同一句话的前后两半对同一种错误给出相反结论，而本包的回归（`host_test.go:126-131`）钉的是后半句那个行为。方案 v5 第七轮修复记录第一条把「两分法」当成修好了的口径记了下来，所以这个错还进了实施记录。另有两条取舍项交实施者裁量（`judge.go:104` 首句「不断言来源」与第一分支输出的 `gateway_internal` 仍相互拉扯；`statusCategory` 的形参名 `code` 与错误链里表示 `Error.Code` 的 `code=` 撞名，与上一轮已采纳的 S1 同一类）。必修项未关闭，verdict 取 `有Blocking`；无需用户裁决的事项。

## 事实来源

**跑过的命令**（均为只读）：`git status --porcelain=v1 --untracked-files=all`、`git rev-parse HEAD`、`git log --oneline -3 2b7e0e699`、`git diff --stat 5b0311cea..2b7e0e699`、`git diff 5b0311cea..2b7e0e699`、`git diff 5b0311cea..2b7e0e699 -- ee/`、`git diff --stat b9e8752ea 2b7e0e699 -- ee/`、`git grep -n "^## \|^### " -- workbench/cases/内容安全检测器/期1/方案v5.md`、`git grep -n "^## \|^### " -- product/架构/EE后端架构.md`、`git grep -n "499" -- core/bifrost.go core/inference.go core/utils.go core/schemas/bifrost.go core/providers/utils/utils.go`、`git grep -n "IsBifrostError" -- core/schemas/`、`git grep -n "newBifrostQueueFullError\|newBifrostCtxDoneError" -- core/`、`git grep -n "^func (bifrost \*Bifrost)" -- core/bifrost.go`、`git grep -n "func HandleProviderAPIError\|func NewBifrostOperationError\|func NewBifrostUpstreamConnectionError\|func NewBifrostTimeoutError" -- core/providers/utils/utils.go`、`git grep -n "provider_rate_limited\|provider_unavailable\|provider_auth\|provider_rejected\|provider_cancelled\|provider_error\|providerFailureCategory" -- ee/ workbench/ product/ docs/`、`git grep -n "http_unavailable\|http_rate_limited\|http_auth\|http_rejected\|http_cancelled\|http_other\|statusCategory\|gateway_internal\|known_type" -- ee/ workbench/ product/`、`git grep -n "provider_*\|category=" -- ee/scripts ee/internal ee/docs ee/README.md`、`git grep -n -A 30 "func FromStatus" -- ee/internal/rbac/http/protocol.go`、`git grep -n "fasthttp\|net/http" -- workbench/cases/内容安全检测器/期1/checklist.yaml`、`git grep -n "^go \|^module " -- ee/go.mod go.work`、`git grep -c "darkBaryon/bifrost/ee/internal" -- ee/internal/guardrails`、`git show <候选>:…judge.go | wc -l`（186）、`…host_test.go | wc -l`（219）。

**完整读过的范围内文件现状**：`ee/internal/guardrails/README.md`（138 行）、`ee/internal/guardrails/host/judge.go`（186 行）、`ee/internal/guardrails/host/host_test.go`（219 行）。

**读过的规则与裁决**：根 `AGENTS.md`、`workbench/AGENTS.md`、`规范/流程/收敛评审.md`、`规范/流程/收敛评审审查方向.md`、`规范/项目/编码规范.md`、`templates/收敛评审.md`、`product/架构/EE后端架构.md`（全文）、`workbench/findings.md`（全文）、方案 v5 的「实施记录」「实施偏差记录」「第一～七轮评审修复记录」「Gate 决策结果」（:184-301）、上一轮记录 `收敛评审7.md`（差量复核要求与其「本轮修复项」）。

**核实过的上游事实**（结论依赖它们，逐条读到源码）：
- `core/utils.go:335-359` `newBifrostCtxDoneError`：超时 → **504** + `IsBifrostError: true`，取消 → **499** + `IsBifrostError: true`，两者都填 `Message`；`core/providers/utils/utils.go:386-411` 同形状（504 / 499，均 `IsBifrostError: true`）；`core/providers/utils/utils.go:2644-2656` `NewBifrostTimeoutError` → **504** + `IsBifrostError: true`。即「取消、超时」在上游一律带状态码，落进 `judge.go:115` 的第一分支并保留消息。
- `core/utils.go:364-374` `newBifrostQueueFullError` → **503** + `IsBifrostError: true` + 消息 `request dropped: queue is full`，调用点 `core/bifrost.go:5627` 位于 `tryRequest`（`core/bifrost.go:5499`，非流式路径），判官的 `ChatCompletionRequest`（`judge.go:73`）可达。这是一个「带状态码但保留消息」的现实形状，且父 ctx 未取消，`judge.go:76` 的日志与 `:80` 的返回值都会带上消息。
- `core/providers/utils/utils.go:2063-2089` `HandleProviderAPIError` 无条件把 `resp.StatusCode()` 填进 `StatusCode`——provider 正文来源的错误必定带状态码，因此 `judge.go:115` 的「无状态码 ⇒ 文本由网关生成」在这条路径上成立，第一分支保留消息不构成回显缺口。
- 499 的三个产出点：`core/utils.go:345-352` 与 `core/providers/utils/utils.go:400-411` 都置 `IsBifrostError: true`；`core/providers/utils/utils.go:3410-3416` `HandleStreamCancellation` 未置该标记，但只服务流式路径，而判官只发非流式子请求（`judge.go:63-73`）。故 `judge.go:145` 的 `code == 499` 分支对本调用方当前不可达。
- `ee/go.mod:3` `go 1.27.0` → Go 1.22 起每轮新建循环变量，`host_test.go:137` 直接 `&status` 安全，上一轮 S1 的判据在本轮落地后依然成立。
- `docs/openapi/schemas/management/governance.yaml:2388` 上游治理失败枚举含 `provider_unavailable`；本轮改名后 ee 内不再出现同名字符串（`git grep -n "provider_unavailable" -- ee/internal ee/scripts ee/docs` 零命中）。

**对照的同类实现与既有约定**：`ee/internal/rbac/http/protocol.go:147-157` 的 `FromStatus`（ee 内另一处「状态码 → 固定名字」的映射）——它用查表 + 类型化常量，因为取值进接口响应体（`:164-169` 的 `errorDetail.Code`）；判官侧只进服务端日志，用 switch + 字面量，两者差异有依据（`workbench/cases/内容安全检测器/期1/checklist.yaml:52` 禁 `net/http`、`:56` 禁 `fasthttp` 于 `guardrails/host`，rbac 则允许 import fasthttp 并用其常量）。另对照 `ee/internal/guardrails/plugin/plugin.go:176` 同模块另一处 `category=` 的既有用法（检测项目类别），确认本轮改值未引入新的口径冲突。

**未执行**：任何测试、构建、`go vet`、`-race`、冒烟、服务启动与真机判官调用（只读评审）；性能 / 并发 / 安全实测；本轮未改动的文件（`checker.go`、`evaluation.go`、`rule.go`、`detector.go`、`config/*`、`persistence/*`、`plugin/*`、`detectors/*`、`host/builder.go`、`app/guardrails.go`、`ee/scripts/*`）只在引用处查证，未重读全文；`detectors/secrets/data/rules.json` 未逐条校对。

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无（有依据）：本轮未新增数字；`host_test.go:136` 的 502/502/503 与 `:144` 的 `bad := 502` 是上一轮按上游真实形状补的刻意对照（`:143` 注释「同样是 502」即其存在理由） |
| | 同一规则多份实现且互不注明 | 无：错误分类只有 `judge.go:110-135` 一份；`README.md:101` 是其文档面（本轮口径不符记 R1，不是第二份实现） |
| | 重造已有 helper | 无：改名未引入新工具；`schemas.ProviderConnectionFailed` 仍直接复用上游导出常量（`judge.go:99`） |
| | 派生常量无推导 | 无：本轮未新增派生值 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无：本轮未新增阈值或标识常量 |
| | 状态码、错误码裸数字 | 无（有依据）：`judge.go:141-152` 的字面量由 checklist 禁 `net/http`/`fasthttp` 决定，评审 3 已按反证条件撤回，`:138` 就地注明数字是 HTTP 状态码 |
| | 无名的错误形状 | 无：`judgeError` 仍返回普通 `error`，两分支各自命名类别，未以元组表示失败 |
| | 紧凑但绕的控制流 | 无：上一轮 S1 指出的 `code := status` 已删（`host_test.go:137` 直接 `&status`）；`judgeError` 仍是两段早返回，每段自足 |
| | 文件集中或拆分是否妨碍理解 | 无：186 / 219 / 138 行，均未触编码规范 300 行评估线，本轮增量 ≤2 行 |
| | 多余状态或守卫 | 发现（已判定保留）：`judge.go:145` 的 `code == 499` 对判官当前不可达（证据见「事实来源」）；理由见「有理由保留」第一条。`:117`、`:131` 的 nil 守卫分属不同分支，不是重复 |
| | 同一效果做两次 | 无：`known_type` 只在 `:132` 追加一次；日志只在 `judge.go:76` 输出一次 |
| | 注释与现状矛盾 | 发现：S1（`judge.go:104` 首句「不断言来源」与 `:116` 输出的 `gateway_internal` 相互拉扯）。`:137-138` 本轮改写后与实际输入相符；`:94-97`、`:105-109` 我逐条对上游核实属实 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无：`judgeError`、`statusCategory`、`synthesizedGatewayTypes` 均为包内未导出符号，`host` 对外面貌（`Client`/`Logger`/`NewJudgeModel`/`NewBuilder`）未变；与 `rbac/http` 的状态码映射差异有依据（见「事实来源」） |
| | **架构符合性**（EE 后端架构） | 无：见下方专段，§4 职责表、§5 适配类型归位、§6 双入口、§7 适配归位、§8 契约不变逐条一致 |
| | **依赖关系** | 无：见下方专段，零 import 变化，40 条内部依赖边与上一轮逐条相同 |
| | 绕过既有出口 | 无：建表仍经 `persistence.Migrate`，响应仍经上游 `SendJSON/SendError`，发布仍经 `Swapper.Swap`，判官仍经 `Client.ChatCompletionRequest` 内部子请求；本轮未新增出口 |
| | 把上游现状当契约 | 无（本轮进一步收紧）：`judge.go:107-109` 明写状态码可能来自 provider 也可能由网关合成、`Error.Type` 不可信；改名后不再与上游治理枚举 `provider_unavailable` 同名 |
| 契约一致性 | 后端类型/前端类型/开发指南三方不一致 | 发现：R1（`README.md:101` 的两分法与 `judge.go:115` 的分流判据不同）。ee 内无残留的旧类别名（`git grep` 零命中）；本期无 UI / OpenAPI / `config.schema.json` 影响，`routes.txt` 未因本轮失效 |
| | 状态码口径不一 | 无：对外状态码未改；`MinDenyStatus/MaxDenyStatus` 未动。判官侧「类别按状态码派生」只进日志，不对外 |
| | 省略、空、值三态语义不对应 | 无：`judgeError` 对 `StatusCode` 的 nil/有值（`:112`、`:115`）、`Error` 与 `Error.Type` 的 nil/有值（`:117`、`:131`）各有分支且不混淆；`get` 的 `config: null` + `version: 0` 三态未变 |

### 开放式复读

已做开放式复读（放下表，按「半年后接手要改这里的人」把范围内 3 个文件逐个再读一遍）。**表外发现：无。** 三处停顿点均落回表内行：

- `README.md`：第一眼皱眉在「凡带 HTTP 状态码的错误只留状态码…」与下半句「（取消、超时…）额外保留…消息」并列——取消和超时恰恰都带状态码。落回契约行，记 R1。
- `judge.go`：第一眼皱眉在 `:104` 的「不断言来源」紧跟 `:106` 的 `category=gateway_internal`。落回「注释与现状矛盾」行，记 S1。另注意到 `:145` 的 499 分支对本调用方不可达，落回「多余状态或守卫」行，按「有理由保留」处置并把覆盖问题转代码评审。
- `host_test.go`：第一眼皱眉在 `:136` 的循环变量 `status` 遮蔽了 `:112` 的 `status`（429），而 `:112` 那个夹具在 `:152` 还要用。核实后判定可接受，写入「有理由保留」，不提修改。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无新增。生产侧类型清单仍只在 `judge.go:98-102` 一处；测试侧同名字面量属「测试按契约独立书写」明文豁免 | `judge.go:98-102`；`host_test.go:136` |
| 职责与实现质量 | 发现：S1（`judge.go:104` 首句与 `:116` 的输出）、S2（`statusCategory` 形参 `code` 与错误链 `code=` 撞名）。上一轮 S1 指出的测试侧绕点已消除 | `judge.go:104,116,122,139`；`host_test.go:137,144` |
| 架构与项目一致性 | 无。见下方专段 | 依赖边逐文件合计 40，与上一轮逐条相同 |
| 契约一致性 | 发现：R1（`README.md:101` 的分流口径与 `judge.go:115` 不一致，且与本包回归 `host_test.go:126-131` 相反） | `README.md:101` vs `judge.go:115`、`core/utils.go:335-359,364-374`、`core/bifrost.go:5627`、`core/providers/utils/utils.go:2644-2656` |

**架构符合性（EE 后端架构 §4/§5/§6/§7/§8）**：本轮未新增、删除或移动任何包与文件。按 §4 职责表复核受影响文件：`host/judge.go` 的 `judgeError`/`statusCategory` 仍只做「把上游 `BifrostError` 翻译成本模块可用的中性错误」，不含业务判断，属 §7「适配归位判定」中「只服务某项业务的宿主检查放在 `<模块>/host/`」；改名把 HTTP 状态码这件事挑明后，业务根包依旧不认识状态码与上游错误类型，符合 §5「Bifrost 及外部 SDK 的具体类型留在适配实现中」。§6「管理接口放 `guardrails/http/`、实时检测入口放 `guardrails/plugin/`」继续成立；本轮唯一的测试改动落在 `host` 包自己的外部测试包（`package host_test`，`host_test.go:2`）内。README 是包内实现说明，不触及 §8 的 API / 表名 / 迁移 ID 契约（`ee_guardrails`、`ee_guardrails_v1` 未动）。

**依赖关系（EE 后端架构 §5/§7）**：`git diff 5b0311cea..2b7e0e699 -- ee/` 中两个 Go 文件的 import 块均未被 diff 触及，零 import 变化。逐文件依赖边计数合计 40，与上一轮逐条相同；`host/judge.go` 对 ee 内部包的依赖仍为 0，只依赖 `core` 与 `core/schemas`。①业务根包 `guardrails` 只作为被 import 方出现；②`host` 未反向 import `http`/`plugin`；③接口仍窄且经构造函数注入（`host.Client` 1 方法、`host.Logger` 2 方法，`judge.go:19-27,44-46`）；④本轮未新增跨模块写入、未新增全局容器查找；⑤共享资源所有权未变。

## 上一轮（收敛评审7）修复项处置

| 上一轮条目 | 处置 | 证据 |
|---|---|---|
| R1 `README.md:101` 把「没有可用 key」整类许诺成保留消息，三种形状里两种其实没有消息；且未提新增的第三个 token | **仍未关闭**（旧错已消，改写引入同类新错） | 现 `README.md:101` 不再整类许诺：括号收窄为「取消、超时、key 池没有支持判官模型的 key」，并补上「类型命中网关合成清单时附加该常量名」，两个旧缺口都按上一轮的最小修正堵住了，段落也未重新点名三个类型（保住了评审 6 S2 的简化）。但改写采用的「凡带 HTTP 状态码 / 没有状态码」两分法与代码的第一判据 `IsBifrostError`（`judge.go:115`）不同：`core/utils.go:335-359` 证实取消带 499、超时带 504 且 `IsBifrostError` 为 true，`core/utils.go:364-374` + `core/bifrost.go:5627` 证实队列满带 503 且保留消息——这三种都进第一分支。README 自己举的两个例子被前半句否定，本包回归 `host_test.go:126-131` 钉的正是后半句。详见本轮 R1 |
| R2 `providerFailureCategory` 注释称状态码来自 provider；`judgeError` 首句「不断言来源」与其输出的 `provider_*` 自相矛盾 | **已修复（主体）** | 函数改名 `statusCategory`（`judge.go:139`），注释改为「只按 HTTP 状态码给出固定类别，不看状态码由谁产生」（`:137`），"provider 返回的" 这一限定词已删除；类别名改为 `http_*`（`:142-152`），不再断言来源，`:107` 明写「状态码可能来自 provider 响应，也可能是网关合成的 502/503」。上一轮 R2 的两个直接矛盾点都消失。附带收益：ee 内不再有与上游 `docs/openapi/schemas/management/governance.yaml:2388` 治理枚举同名的 `provider_unavailable`。残留的是 `:104` 首句与第一分支 `gateway_internal` 的口径（上一轮建议改写该首句、实施方选择了改名路线），按取舍项另记 S1，不再算 R2 未关闭 |
| S1（取舍）测试夹具多做一次取址拷贝并命名 `code`，与同行 `Error.Code` 撞名；`bad := 502` 与其解释性注释被隔开 | **已采纳，两半均关闭** | `host_test.go:137` 现为 `StatusCode: &status`，`code := status` 已删，撞名随之消失（`ee/go.mod:3` 为 `go 1.27.0`，`&status` 在 Go 1.22+ 语义下安全）；`:143-144` 注释「同样是 502，但类型来自 provider 正文」现位于 `bad := 502` 之上、紧邻它要解释的夹具 |
| 上一轮转代码评审两条 | 按流程归代码评审，本侧不重复处置 | 两条指向的代码本轮未变（`host_test.go:136-142` 三个 502/502/503 仍同落 `code >= 500` 一档且只断言一个类别；全函数仍只有 `:117` 断言 `status=` 出现在错误链）。本轮再次以原样转交，不出结论 |

## 出口清单

### 本轮修复项

**R1 · README 的分流口径按「有无状态码」写，代码按 `IsBifrostError` 先判；README 自己举的取消与超时都带状态码，前后两半互相否定**

位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:101`（对应实现 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:110-135`，判据在 `:115`）。

代码事实：README 这一句写「判官失败时……**凡带 HTTP 状态码的错误只留状态码和按状态码派生的固定类别**，类型命中网关合成清单时附加该常量名；**没有状态码或网关标记为自身错误的（取消、超时、key 池没有支持判官模型的 key）额外保留类型、代码与前 200 字节消息**」。`judge.go:115` 的分流是 `if bErr.IsBifrostError || bErr.StatusCode == nil`——网关标记在前，状态码有无只是它的或项。因此「带状态码」和「保留消息」并不互斥，上游至少三种形状同时满足两个分句的前提，实际行为一律是后半句：

- 取消 → `core/utils.go:345-352`（`statusCode = 499`、`IsBifrostError: true`、`Message` 非空）、`core/providers/utils/utils.go:400-411` 同形状；
- 超时 → `core/utils.go:341-343`（504）、`core/providers/utils/utils.go:2644-2656` `NewBifrostTimeoutError`（504、`IsBifrostError: true`）；
- 判官子请求被队列丢弃 → `core/utils.go:364-374` `newBifrostQueueFullError`（503、`IsBifrostError: true`、消息 `request dropped: queue is full`），调用点 `core/bifrost.go:5627` 在 `tryRequest`（`core/bifrost.go:5499`）这条非流式路径上，判官的 `ChatCompletionRequest`（`judge.go:73`）可达，且此时父 ctx 未取消，消息经 `judge.go:76` 进日志、经 `:80` 进错误链。

本包的回归钉的就是后半句：`host_test.go:126-131` 用「`IsBifrostError` 为 true 且带 499」的真实取消形状断言 `category=gateway_internal`、`type=request_cancelled` 与截断后的消息都在，即测试与 README 的前半句直接相反。方案 v5 第七轮修复记录第一条把这个两分法当成已修好的口径记入了实施记录（`workbench/cases/内容安全检测器/期1/方案v5.md:282`），所以口径错误目前有两处留痕。

维护影响：编码规范把包 README 的读者设定为「第一次接手、准备改它的人」，这段是本模块唯一的脱敏与可诊断性承诺，而「判官失败时能不能看到消息」正是实施偏差记录 1（`方案v5.md:199`）为真机排障专门争回来的行为。按现在的写法，接手的人会得到「带状态码 ⇒ 无消息」的规则：据此写排障手册，手册在超时、取消、队列满这三种最常见的网关侧故障上都是错的；据此改代码，`:115` 那个「或」看起来像是多余的宽松分支，删掉就把偏差记录 1 争回来的行为又丢掉了。编码规范两条明文同时被触及：「修改代码时必须同步检查并更新相关注释，已失效的说明及时删除」「包的功能、文件或约束发生变化时同步更新」。

最小修正：只调换两个分句的先后并把第一判据写成网关标记，例如「网关标记为自身错误的、或不带状态码的（取消、超时、key 池没有支持判官模型的 key）保留类型、代码与前 200 字节消息；其余带状态码的错误只留状态码和按状态码派生的固定类别，类型命中网关合成清单时附加该常量名」。保持两个分句、不要再展开成分支条件的逐句转写——评审 6 的 S2 刚把这段从实现细节压回「承诺 + 后果」，本轮已比那时长了一截。只改文字，不涉及行为。同时建议把方案 v5 第七轮修复记录那一行的「两分法」表述一并订正，避免下一轮再以它为准。

### 需用户裁决的 Blocking

- 无。

### 立债（REF）

- 无。

### 挂账（Finding，建议取舍项；由实施者决定采纳或撤回，未采纳的按四要件登记）

**S1 · `judgeError` 首句「不断言来源」是全量承诺，而它的第一分支输出的正是一个来源名**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:104`（对应输出 `:116`）。代码事实：`:104` 写「类别名只陈述可验证的事实，**不断言来源**」，`:116` 输出 `category=gateway_internal`——它断言来源是网关。这个断言本身站得住：分支条件用的是网关自己的标记 `IsBifrostError`，provider 正文无法置位；而 `core/providers/utils/utils.go:2063-2064` 证实 provider 正文来源的错误一定带状态码，所以「无状态码」这一路也不会把 provider 文本带进来（这两点 `:105-106` 已写明）。问题只在首句是全量句式，与紧随其后的第一条项目符号打架。上一轮 R2 给出的最小修正正是改写这一句，实施方改走了改名路线，这半句因此留了下来。
维护影响：`judgeError` 是本模块被改得最多的函数（七轮里四次），每次都是「哪些字段可信」判断错了；它的注释是下一个人唯一的判断依据。一个自我否认的首句会让人不确定该信首句还是信项目符号，而这正是最不该犹豫的地方。成本在阅读侧，行为无影响。
最小修正：把首句收窄为实际口径，例如「类别名只陈述可验证的事实：来源只在能从网关自己的标记推出时才写（`gateway_internal`），按状态码派生的类别不断言来源」。只改注释。
反证条件：若团队把这句的「来源」读作「provider 来源」（上一轮 R2 提出时就是这个语境），则首句已经准确、无需改动——此时明确撤回本条，下一轮不再重提。

**S2 · `statusCategory` 的形参叫 `code`，与同文件错误链里表示 `Error.Code` 的 `code=` 撞名**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:139`（另见 `:141-150` 的比较、`:122` 的 `code=`）。代码事实：`judgeError` 往错误链里拼四个 token：`status=`（状态码，`:113`）、`category=`（`:116`/`:130`）、`code=`（`Error.Code`，`:122`）、`known_type=`（`:132`）。本轮新命名的 `statusCategory(code int)` 用 `code` 表示的是 `status=` 那个值，与 `code=` 表示的错误码不是一回事。上一轮 S1 已就同一类撞名（测试里 `StatusCode: &code` 与 `Code: schemas.Ptr(...)` 并排）给出意见并被采纳，生产侧这一处未同步。
维护影响：读 `judgeError` 的输出与读 `statusCategory` 的入参时，同一个词指两个字段，核对「哪个字段进了错误链」这件本函数唯一要核对的事情要多停一次。一次形参改名即可消除，无行为影响。
最小修正：`judge.go:139` 形参改名 `status`（函数名已含 status，`:137-138` 的注释不必改）。
反证条件：若团队认为 15 行函数内、注释已写明「数字是 HTTP 状态码」的前提下不存在歧义，则保留并明确关闭本条，下一轮不再重提。

### 转代码评审（范围外发现，一行一条，不出结论）

- `ee/internal/guardrails/host/host_test.go:136-142`：三个合成夹具的 502/502/503 全部落进 `judge.go:147` 的 `code >= 500` 同一档，循环只断言 `category=http_unavailable`，503 相对 502 仍未产生可区分的断言（与上一轮转交同一处，本轮未变）。
- `ee/internal/guardrails/host/host_test.go:117,139,147`：全函数只有 `:117` 断言 `status=<夹具状态码>` 出现在错误链里，若 `judge.go:112-114` 的状态码拼接被删除，另外五个场景仍会全绿（与上一轮转交同一处，本轮未变）。
- `ee/internal/guardrails/host/judge.go:145`：`code == 499` 分支对判官的非流式调用不可达（两个 499 产出点 `core/utils.go:345-352`、`core/providers/utils/utils.go:400-411` 均置 `IsBifrostError: true`，第三个 `core/providers/utils/utils.go:3410-3416` 只服务流式），`http_cancelled` 没有也无法有回归覆盖。

### 有理由保留（写出理由，下一轮不再重提）

- **`judge.go:145` 的 499 分支不删**：`statusCategory` 是一张「状态码 → 固定名字」的完整映射，留着这一行的收益是上游若把某条非流式路径改成不置 `IsBifrostError` 的 499，诊断里出现的是 `http_cancelled` 而不是 `http_other`；删掉只省一行、且要推翻评审 3 S1 已采纳的 499 命名决定（`方案v5.md:250`）。覆盖问题已单独转代码评审。
- **`host_test.go:136` 的循环变量 `status` 遮蔽 `:112` 的 `status`（429）**：遮蔽范围只有 `:137-141` 六行，循环体内不需要外层值；外层夹具 `provider` 在 `:152` 复用时取的仍是 `:112` 那个 429，不受影响。再造第三个名字只为消除一次视觉重名，收益低于上一轮 S1 换来的「直接 `&status`」的简洁。
- **`judge.go:141-152` 的 HTTP 状态码字面量**：`checklist.yaml:52` 禁 `net/http` 于整个 `ee/internal/guardrails`、`:56` 禁 `fasthttp` 于 `guardrails/host`，评审 3 已按反证条件撤回（`方案v5.md:250`），`:138` 就地注明数字是 HTTP 状态码。同模块的 `rbac/http/protocol.go:148` 用 `fasthttp.StatusMethodNotAllowed` 是因为那个包允许 import fasthttp，不构成本处的反例。
- **`category=`/`known_type=`/`http_*` 系列用字面量而非类型化常量**：编码规范「对外枚举必须是命名常量」约束会持久化或出现在接口响应里的取值；这些只进服务端日志文本，不跨包、不落库、不进响应。对照 `rbac/http/protocol.go:164-169`——那边用常量正是因为值进 `error.code` 响应字段。
- **`synthesizedGatewayTypes` 保持 `map[string]bool`**：`:94-97` 的出处注释加三行键值构成一份可直接与上游对照的清单，加一项只改一行；`:131` 处零值即 false，不需两值断言。未导出、只读、无并发写入。
- **`judgeError` 两处相同的 `return errors.New(strings.Join(parts, " "))`**：两个分支各自拼完自己的部分立即返回；合并单出口会让 `parts` 的拼装跨越分支，读者要回头确认「到这里 parts 里已经有什么」。
- **`category=` 这个 key 在本模块有两种含义**（`plugin/plugin.go:176` 指检测项目类别 `harmful_content`/`business_rule` 等，`judge.go:116,130` 指判官失败类别）：两者本轮之前就并存，本轮只换了判官侧取值；判官侧嵌在 `content safety judge: … failure=%q` 的引号内（`judge.go:76`），与插件行前缀不同，按前缀过滤仍可区分。属差量范围外的既有口径。
- **`host_test.go:136` 与 `judge.go:98-101` 各写一份类型串**：编码规范明文「测试和脚本按契约独立书写字面量，不受此限」；漂移可检测性已由评审 6 转代码评审并以「清单集中一处 + 注明出处」处置（`方案v5.md:276`）。
- **`TestJudgeModelErrorPolicy`（`host_test.go:111-155`）一个函数承载六种错误形状**：六种形状共用 `log`（`:115`）并依赖累积断言，每个场景上方各有一行中文说明其上游形状，`t.Fatalf` 各自可辨；改表驱动要为六组互不相同的期望抽一个谓词，读者反而要先读谓词。本轮长度 46→45 行。
- **三个文件的行数**（`judge.go` 186、`host_test.go` 219、`README.md` 138）：均未达编码规范 300 行评估线，本轮增量 ±2 行内，无拆分动作需要。

## 台账对账

- **FIND-022（评审放过规范明文违规）：本轮按该条指向逐条对照了编码规范硬规则**（对外枚举命名常量、业务限制命名与派生推导、同一规则一处实现、业务/DTO/行类型分工、导出符号用途注释、依赖显式注入、日志经注入 logger、文件行数 300/500、文件顶部职责说明、注释与现状一致、包 README 三问与「少提外部名词」），产出 R1、S1、S2；其中「修改代码时必须同步检查并更新相关注释」「包的功能、文件或约束发生变化时同步更新」是 R1 的直接依据，「与代码不一致的注释视为缺陷」是 S1 的依据。该条本身未恶化。
- **FIND-030（上游错误转换口径）：相关但未恶化**。本轮改动的仍是只进服务端日志的失败类别，不进接口响应，与该条所指 `rbac/http` 对外 `error.code` 不是同一出口；本轮把类别名从 `provider_*` 改成 `http_*` 顺带消除了与上游治理枚举 `provider_unavailable`（`docs/openapi/schemas/management/governance.yaml:2388`）的同名，若将来要把判官失败类别对外暴露，与该条统一口径时少一处歧义。
- **FIND-026（品牌冒烟失败即清理现场）、FIND-027（转交栏提取）、FIND-028（冒烟脚本复制启动逻辑）、FIND-029（部署文档遗留）：本轮未触及相关文件。**
- **FIND-024 / FIND-025（`internal/host` 归属与认证扩展点）：未恶化**。本轮改动全部落在 `guardrails/host/judge.go` 与同包测试、包 README，未往 `internal/host/` 加文件，也未在认证链上开新缝。
- **FIND-017（SQLite busy 重试多份）、FIND-021（时钟自取）：未恶化**，`persistence/` 本轮未触及。
- **FIND-001～016、FIND-018～020、FIND-023：本次未触及。**
- 无新增 Finding 条目建议（本轮取舍项 S1、S2 先交实施者处置，未采纳者再按四要件登记）。
