---
title: 内容安全检测器 期1 收敛评审 #2
type: 收敛评审
case: 内容安全检测器
phase: 1
round: 2
verdict: 有Blocking
created: 2026-09-19
---

> 评审者与实施上下文隔离（独立会话，只读仓库）。差量复核：上一轮候选 `93e6a1272` → 本轮候选 `fde843b5b659943d76b6cc19517afccf417d23c3`（`git status --porcelain` 无输出，工作树干净，无未跟踪文件）。
> L2：四维收束为段落，附上一轮修复项逐条处置、起手表逐行结果与开放式复读结论。

## 结论

上一轮的 10 条本轮修复项（R1–R10）全部关闭，8 条取舍项（S1–S8）全部采纳，逐条核对见下节；其中 R2（`deny` 只在启动期生效）的修法是把检查器与拒绝策略合成插件的一份 `state` 快照原子替换，方向正确，接口、README、冒烟（451 热更新与重启断言）三处口径已对齐。新增改动没有引入新的架构偏离：`http → plugin` 这条新的适配层横向依赖与仓内 `branding/http → branding/persistence`、`rbac/host → rbac/http` 同型，不构成反向依赖或环；等级名与状态码边界收进根包后，`config`/`secrets`/`judge` 三处映射确实只剩一份。本轮新发现四处需要处理的小问题，都是这次修复顺手留下的痕迹：未配置时的占位拒绝策略在 `app` 与 `http` 两个包各写了一份逐字相同的副本（根因是 `newState` 在 checker 为 nil 时仍强制校验 options）、`reset` 丢弃了 `update` 明确要报 500 的同一个 `Swap` 错误、两处测试用 `_ =` 空白赋值掩盖了本该参与断言的变量（其中一处的注释因此承诺了没有断言的行为）、三处注释指向已被降级或已搬走的符号。另有两条取舍项（新导出的等级名常量无包外使用方、判官重试 3–5 与默认 3 在两个包各一份且互不注明）交实施者裁量。修复项未关闭，verdict 取 `有Blocking`；无需用户裁决的事项。

## 事实来源

**跑过的命令**（均为只读）：`git status --porcelain`、`git rev-parse HEAD`、`git diff --stat 93e6a1272 fde843b5b`、`git diff 93e6a1272 fde843b5b -- <各文件>`（README 三件 / routes.txt / bootstrap.go / 架构 / plugin+deny+handler / config.go+detector.go / 四个测试文件 / host_test.go+store_test.go）、`git show develop:ee/internal/rbac/persistence/migration.go`、`git ls-files ee/internal/guardrails`、`git grep -c ""`（行数）、`git grep`（`Validate`、`LevelName`、`DefaultJudgeRetries|DefaultRetries|DefaultMaxTextBytes|MinDenyStatus|…|UnsetVersion`、`RuneStart|ToValidUTF8|func truncate`、`Cache-Control`、`IsBifrostError`、`darkBaryon/bifrost/ee/internal` 于四个同类 http/host 包）。

**完整读过的范围内文件现状**：`ee/README.md`、`app/README.md`、`app/bootstrap.go`、`app/guardrails.go`、`app/guardrails_test.go`、`guardrails/README.md`、`checker.go`、`evaluation.go`、`detector.go`、`config/config.go`、`config/rules.go`、`detectors/judge/detector.go`、`detectors/judge/parse.go`、`detectors/secrets/detector.go`、`detectors/secrets/rules.go`、`detectors/secrets/detector_test.go`、`host/builder.go`、`host/judge.go`、`host/host_test.go`、`http/handler.go`、`http/handler_test.go`、`persistence/migration.go`、`persistence/store.go`、`plugin/plugin.go`、`plugin/deny.go`、`plugin/swap_test.go`、`checker_test.go`、`parallel_test.go`、`rbac/host/routes.txt`（:640-719 段）、`ee/scripts/guardrails-smoke.py`、`ee/scripts/guardrails-judge-e2e.py`、`product/架构/EE后端架构.md`。`pricing_test.go`、`store_test.go` 按 diff 读。范围外但作为现状上下文读过：`rule.go`、`plugin/plugin_test.go:30-90`（测试夹具）。

**对照的同类实现**：`identity/http/handler.go:84-94`（`serve` 包装里统一设 `no-store` 的先例，与本次 `noStore` 中间件同型）、`branding/http/handler.go:48,60,83,96`、`rbac/host/response.go:24,29`；`git show develop:ee/internal/rbac/persistence/migration.go`（`authstore.RetrySQLiteBusy` + 顾问锁 + `RunSingleMigration` 的整体形状，与本案 `migration.go:23-45` 逐项一致）；`rbac/host/routes.txt:648-703`（组内按路径升序、方法不参与排序）；`branding/http → branding/persistence`、`rbac/host → rbac/http`、`rbac/http → identity/http`（适配层横向依赖的既有先例）。

**核实过的上游事实**（结论依赖它们）：`core/providers/utils/utils.go:2063-2089` `HandleProviderAPIError` 对 provider 响应体错误置 `IsBifrostError: false`，而 `:2620-2674` 的 `NewBifrostOperationError`/`NewBifrostTimeoutError`/`NewBifrostBadRequestError` 与 `:386-411` 的取消/超时错误置 `true` 且消息为网关自造文本——`host/judge.go:108-114` 用 `IsBifrostError` 区分"网关自身消息可留、provider 消息必弃"在主路径上成立，README:101 的表述与代码一致。

**未执行**：任何测试、构建、冒烟、服务启动与真机判官调用（只读评审）；性能/并发/安全实测；`data/rules.json`（4290 行）逐条校对；`plugin/text.go`、`scan.go` 等非差量文件的重读（上一轮已覆盖，本轮只在引用处查证）。

## 上一轮（收敛评审1）修复项处置

| 上一轮条目 | 处置 | 证据 |
|---|---|---|
| R1 顾问锁键与 rbac 撞号 | **已修复** | `persistence/migration.go:19` 改为 `8342761904`，`:17-18` 注释列出品牌 901 / 身份 902 / 角色权限 903 |
| R2 `deny` 只在启动期生效 | **已修复** | `plugin/plugin.go:22-26` 新增 `state{checker, options}`，`:65-72` `Swap(checker, options)` 原子替换；`http/handler.go:117,129` 更新时一并发布；`swap_test.go:123-141`、`handler_test.go:114-116` 有断言；`guardrails-smoke.py:145-152` 实测 451 热更新不重启即生效 |
| R3 `parseLevel` 三份 | **已修复** | `detector.go:19-37` 收为根包 `ParseLevel`；`config/config.go:180`、`secrets/rules.go:93` 直接调用；`judge/parse.go:43-49` 只处理 `none` 与大小写后委托 |
| R4 文档写"真实检测器尚未接入" | **已修复** | `ee/README.md:18`、`app/README.md:5,12,16,19,29,31`、`product/架构/EE后端架构.md:54`、`bootstrap.go:17,50` 四处均改为现状 |
| R5 `slots` 依赖 cap 不变量 | **已修复** | `checker.go:91-98` 先收集 `staged` 再 `make([]slot, len(staged))`，`:100-108` 按下标取址，`:97` 注释写明理由 |
| R6 `Validate` map 遍历 | **已修复** | `config/config.go:129-141` 改为固定顺序切片，`:129` 注释写明与 `Expand` 一致 |
| R7 switch 后孤立 `err` 兜底 | **已修复** | `app/guardrails.go:72-76` 在 `case mode != ""` 内就地返回，switch 之后已无兜底 |
| R8 routes.txt 破序 | **已修复** | `rbac/host/routes.txt:673-677` 两条 `guardrails` 已移到 `/api/cache/...` 之后；对照 `:648-657` 确认全文按路径升序、方法不参与排序 |
| R9 错误消息按字节截断 | **已修复** | `host/judge.go:119-129` `truncateUTF8` 回退到 rune 边界；`host_test.go:119-124` 断言不切断多字节且 `utf8.ValidString` |
| R10 三处注释与现状不符 | **已修复** | `evaluation.go:43` 改为两态并点明父取消由 error 表达；`secrets/detector.go:38` 改为"测试用它锚定数据文件版本"；`app/guardrails_test.go:1` 按现有断言重写 |
| S1 busy 重试第三份 | **已采纳** | `persistence/migration.go:24` 改用 `authstore.RetrySQLiteBusy`，与 develop 的 rbac 写法一致 |
| S2 多份字面量 | **已采纳** | `MinDenyStatus/MaxDenyStatus` 收进 `detector.go:40`，`config`（:116）与 `plugin`（:58）共用；`app/guardrails.go:32` 改为 `config.DefaultMaxTextBytes`；`judge/detector.go:39` 注明同步要求（重试范围未同办，见本轮 S2） |
| S3 e2e 复制节点配置 | **已采纳** | `guardrails-smoke.py:97-107` 抽出 `node_config`，`guardrails-judge-e2e.py:61-63` 复用并只追加 deepseek |
| S4 包外无使用方的导出符号 | **已采纳** | `config.go:20-23`、`rules.go:12,19` 降为未导出，`Validate → validate`；`judge` 的 `minRetries/maxRetries/defaultMaxBytes` 同办（新引入的等级名常量见本轮 S1） |
| S5 `no-store` 三处 | **已采纳** | `http/handler.go:74-80` `noStore` 中间件，三条路由统一包裹 |
| S6 显式 `0` 与省略同解 | **已采纳（文档口径）** | `guardrails/README.md:92` 写明"写 `0` 与省略等价；`judge.retries` 写 0 得到默认 3" |
| S7 冒烟靠提示词正文识别判官 | **已采纳** | `judge/detector.go:64` 与 `guardrails-smoke.py:18-19` 互相注明耦合 |
| S8 缺 `ee/docs` 接口页 | **已采纳（最小形式）** | `ee/README.md` 新增「内容安全」节指向包内 `#配置与接口`，`docs/` 页面留期 2 |

无"有依据撤回"的条目；无未关闭的上一轮条目。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 未配置占位拒绝策略两份（R1）；判官重试范围与默认值两份且互不注明（S2） | `app/guardrails.go:38-39,70` vs `http/handler.go:149-150`；`config/config.go:19,22-23` vs `judge/detector.go:35-37` |
| 职责与实现质量 | `reset` 丢弃与 `update` 同一调用的错误（R2）；两处 `_ =` 空白赋值掩盖未使用变量（R3）；新导出常量无包外使用方（S1） | `http/handler.go:145` vs `:129-134`；`plugin/swap_test.go:125,129`、`http/handler_test.go:166,181`；`detector.go:20-24` |
| 架构与项目一致性 | 符合（详见下方两项专门核对），无另起基础设施、无反向依赖、无绕过既有出口 | 见「架构符合性」「依赖关系」 |
| 契约一致性 | 三处注释指向已降级或已搬走的符号（R4） | `config/rules.go:39`、`host/builder.go:37`、`detector.go:1` vs `:39-40` |

**架构符合性（EE 后端架构 §3/§4/§6/§7）**：本轮改动没有移动任何模块边界。按 §4 的职责表逐项复核受影响文件：`http/handler.go` 仍只做"解析 → 调用 → 映射响应"，新增的 `mu`（`:49`）串行的是"写库 + 发布"这一对存储与运行状态的一致性动作，属于该入口自身的职责，没有把业务判断搬进 Handler（校验仍在 `config.Parse`、构建仍在 `host.Builder`）；`plugin/plugin.go` 新增的 `state` 是"把同一份配置的两个产物绑在一起"的插件内部表示，仍只负责把检查结果翻译成放行/短路，未承载规则判定；`app/guardrails.go:43-109` 仍是"建表 → 读库 → 构建 → 注册插件 → 注册路由"的薄装配，`:71-86` 的 switch 只在三种启动形态间选择检查器与拒绝策略，没有运行期编排。§6"管理接口放 `guardrails/http/`、实时检测入口放 `guardrails/plugin/`，共同调用模块内的业务服务"继续成立。§7"复用完整操作"与"生命周期统一协调"未变：建表仍经 `ConfigStore.RunMigration`（`app/guardrails.go:53`），插件仍经上游 `SyncLoadedPlugin`（`:91`），`persistence.NewStore` 仍声明不关闭连接（`store.go:40`）。§8"目录迁移不改变表名与迁移 ID"：`guardrailsMigrationID = "ee_guardrails_v1"` 与表名 `ee_guardrails` 未动。

**依赖关系（EE 后端架构 §5/§7）**：本轮唯一的依赖变化是 `guardrails/http` 新增 import `guardrails/plugin`（`handler.go:15`），用于让 `Swapper` 接口的签名带上 `plugin.Options`。逐项核对后判定不构成问题：①方向上不成环——`plugin` 只 import 根包与 `core/schemas`（`plugin.go:4-11`、`deny.go:4-10`），不反向 import `http`；②业务根包仍不 import 任何适配子包（`checker.go`/`evaluation.go`/`detector.go`/`rule.go` 只用 `context/errors/fmt/sync/reflect/strings/time/unicode/utf8`）；③适配层之间的横向依赖在仓内已有先例且形状相同——`branding/http → branding/persistence`（`branding/http/handler.go:9`）、`rbac/host → rbac/http`（`rbac/host/response.go:11`）、`rbac/http → identity/http`（`rbac/http/handler.go:9`），不是本案首创的新架构；④接口仍由使用方定义且窄——`http.Swapper`（1 方法，`handler.go:31-33`）、`http.Logger`（只 `Error`，`:36`）、`host.Client`/`ProviderLookup`（各 1 方法），全部经构造函数注入，`NewHandler` 的 5 个依赖都是显式参数（`:53`）；⑤跨模块没有新增写入，唯一跨模块产物仍是 `rbac/host/routes.txt` 的两组登记，由 `bootstrap.go:43` 的 `VerifyRoutes` 强制；⑥`config` 与 `detectors/` 的"只用标准库、不碰上游类型"约束仍成立（`config` 只 import stdlib + 根包；`judge`/`secrets` 只 import stdlib + 根包）。唯一的依赖异味是本轮 S2（同一条重试边界在 `config` 与 `judge` 各一份而不互相引用），属复用取舍而非方向错误。

## 出口清单

### 本轮修复项

**R1 · 未配置时的占位拒绝策略在两个包各写一份，互不引用**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/app/guardrails.go:38-39,70` 与 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler.go:149-150`；根因在 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin.go:54-62`
代码事实：`app` 用 `placeholderDenyMessage = "内容未通过安全检查"` + `fasthttp.StatusBadRequest` 构造未配置启动时的 `Options`（`:70`）；`http` 用 `disabledOptions = safetyplugin.Options{StatusCode: fasthttp.StatusBadRequest, DenyMessage: "内容未通过安全检查"}`（`:150`）构造 `reset` 后的 `Options`。两者取值逐字相同、语义相同（"检查器为 nil，这份策略用不到"），彼此没有任何引用。两份都存在是因为 `newState` 在 `checker == nil` 时仍然强制校验 `options`（`plugin.go:58`）。
维护影响：同一条"未配置"约定有两份表达，改文案或状态码要同时改两个包；漏改不会报错（值本来就到不了 `buildDenialError`），只有在按日志或代码判断"当前拒绝策略是什么"时才发现两处不一致。另外这句占位文案与 README:83 的示例配置、冒烟配置（`guardrails-smoke.py:118`）的 `deny.message` 逐字相同，读代码的人分不清它是占位还是管理员真的配了这句。
最小修正：让插件自己表达"未配置"——`newState` 在 `checker == nil` 时跳过 `options` 校验并存入零值（或在 `plugin` 包导出一份 `DisabledOptions`），`app` 与 `http` 两处都引用同一来源，删掉 `placeholderDenyMessage` 与 `disabledOptions` 其中之一。

**R2 · `reset` 丢弃 `Swap` 的错误，与同文件 `update` 对同一调用的处置相反**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler.go:145`（对照 `:129-134`）
代码事实：`reset` 写作 `_ = h.swapper.Swap(nil, disabledOptions)`；同一文件的 `update` 把同一个方法的错误记 `log.Error` 并返回 500，注释明写"不让库与运行状态静默错位"（`:130`）。
维护影响：现在不会失败，只因为 `disabledOptions` 恰好合法——而这份合法性由 R1 里那份副本保证。一旦插件端的合法性检查变化（例如要求 `Swap` 的 checker 非 nil、或要求 options 成对来自配置），`reset` 会删掉库中行却仍按旧配置拦截请求，且没有任何日志，正是 `update` 那条注释要防的状态。
最小修正：与 `update` 同样处理（记 `log.Error` + 返回 500，文案沿用"saved but could not be activated"的同类口径）；若确信它恒不失败，就在这一行写明"`disabledOptions` 恒合法，`Swap` 不会失败"的理由，不要裸 `_ =`。

**R3 · 两处测试用 `_ =` 空白赋值掩盖未使用的变量，其中一处的注释因此承诺了没有断言的行为**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/swap_test.go:122,125,129` 与 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler_test.go:166,181`
代码事实：①`swap_test.go:125` `inflight := primed(t, p, testContext())` 之后没有任何使用，靠 `:129` 的 `_, _, _ = inflight, p, t` 消掉编译错误；而 `:122` 的用例注释写"Swap 后新请求用新状态码与文案，**进行中的请求仍用旧快照**"。②`handler_test.go:166` `r, swapper, log := setup(...)` 取到的 `r` 在 `:181` 被 `_ = r` 丢弃——`setup` 会建临时 SQLite 库、跑一次迁移、造 Handler 和 Router，这里只为拿一个新的 `swapper` 和 `log`。
维护影响：`_ =` 是"这行本来该做点什么"的痕迹。①的注释让读者以为"拒绝策略的快照保持"已被覆盖，实际本文件没有这条断言（`TestSwapAfterPreKeepsSnapshot` 只覆盖检查器，不覆盖 options）；下一个改 `Swap` 的人会误判保护范围。②每跑一次多建一个库、多跑一次迁移，且让人以为 `r` 与后面的 `broken` 路由有关系。
最小修正：①把插件改成带 Output 规则构造（`newPlugin(t, []Rule{testRule(guardrails.Output)}, matchingDetector)`），再用 `inflight` 断言 Swap 到 451 之后它的 `PostLLMHook` 仍返回旧的 400 与旧文案；做不到就删掉 `inflight` 并把注释收敛到实际断言。②直接 `swapper, log := &fakeSwapper{}, &testLogger{}`，删掉这次 `setup` 调用与 `_ = r`。

**R4 · 三处注释指向已降级或已搬走的符号**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/config/rules.go:39`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/builder.go:37`、`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detector.go:1` 与 `:39-40`
代码事实：①`rules.go:39` 写"调用前须先通过 Validate"、②`builder.go:37` 写"配置须已通过 `config.Validate`"，但上一轮 S4 已把它降为未导出的 `validate`（`config.go:115`），包外唯一入口是 `config.Parse`；`git grep Validate -- ee/internal/guardrails` 只剩这两条注释指向它。③`detector.go:1` 的文件头写"本文件定义本地检测器契约、风险结果及其有效性检查"，而 S2 把 `MinDenyStatus, MaxDenyStatus`（拦截响应的 HTTP 状态码范围，`:39-40`）搬进了同一文件，不属于该职责。
维护影响：编码规范把与代码不一致的注释视为缺陷，并要求每个文件顶部一句话说明职责、改动时同步。①②让读者去找一个不存在的导出函数，也没交代真正的前置条件是"经 `Parse` 返回的 Config"；③按文件头找不到这对常量，下一个需要它的人容易在 `rule.go` 或 `plugin` 再造一份，正是 S2 刚消除的形态。
最小修正：①②改成"须是 `config.Parse` 返回的配置"；③文件头补一句（或把这对常量移到 `rule.go`，与 `Action`/`Stage` 这些处置侧的业务约束同处）。

### 需用户裁决的 Blocking

- 无。

### 立债（REF）

- 无。

### 挂账（Finding，建议取舍项；由实施者决定采纳或撤回，未采纳的按四要件登记）

**S1 · 新导出的等级名常量在包外没有使用方**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detector.go:20-24`。代码事实：R3 的修复新增了 `LevelNameLow/LevelNameMedium/LevelNameHigh` 三个导出常量，`git grep LevelName -- ee` 只有本文件 6 处命中（3 处定义 + `ParseLevel` 的 3 个 case），`config`、`secrets`、`judge` 都只调用 `ParseLevel`；`config/config.go:181` 的错误文案还是自己写的字面量 "low, medium or high"。
维护影响：编码规范"包外没有使用方的符号不导出"；与本轮已采纳的 S4 处置（`config`、`judge` 的同类符号统统降为未导出）不一致，同一次提交里两套标准。
最小修正：降为 `levelNameLow` 等；`ParseLevel` 与 `MinDenyStatus/MaxDenyStatus` 保持导出。
反证条件：若期 2 的管理页面或接口文档要把这三个名字作为对外契约列出（前端表单枚举、接口说明的取值表），则保留导出，并在注释里写明"对外契约，改名即破坏接口"。

**S2 · 判官重试范围 3–5 与默认值 3 在两个包各一份，互不注明**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/config/config.go:19,22-23`（`DefaultJudgeRetries=3`、`minJudgeRetries=3`、`maxJudgeRetries=5`）与 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/detectors/judge/detector.go:35-37`（`minRetries=3`、`maxRetries=5`、`DefaultRetries=3`）。代码事实：两处是同一条用户裁决（方案 v5 §4.2「`Retries` 3–5，默认 3」）的两份实现，各自独立校验（`config.go:125`、`judge/detector.go:85`）；同一个 const 块里紧邻的 `defaultMaxBytes` 已按上一轮 S2 写了"须与 `config.DefaultMaxTextBytes` 同步"（`judge/detector.go:39`），这三个没有任何互指。
维护影响：编码规范"同一条规则只在一处实现……多处需要时抽成包内函数并互相引用"。裁决范围一旦调整（例如放宽到 2–6），只改 `config` 侧会让配置通过校验后在 `Builder.Build` 里被 `judge.New` 拒绝，表现为 `update` 返回 400 且错误文本来自另一个包；只改 `judge` 侧则配置层先拒绝，`judge` 放宽的边界永远走不到。
最小修正：照 `defaultMaxBytes` 的既有写法，在两侧各加一行互相注明；或让 `config` 直接引用 `judge` 导出的边界（`judge` 不 import `config`，不成环）。
反证条件：若判定 `config` 不应依赖具体检测器包、且"两层各自守自己的契约"就是有意的防御（`judge` 可被 `config` 之外的装配方直接使用），则保留两份，但按 `defaultMaxBytes` 的先例补上互指注释，不能两边都不提对方。

### 转代码评审（范围外发现，一行一条，不出结论）

- `plugin/swap_test.go:122-129`：注释声明的"进行中的请求仍用旧拒绝策略"没有对应断言（`inflight` 被丢弃），该行为目前无用例守护。
- `http/handler_test.go:86-102` 的 `call` 内含 `t.Fatalf`，而 `:206-211` 在 goroutine 里调用它；从非测试 goroutine 调 `t.Fatal*` 是 testing 包的误用。
- `persistence/store.go:79,91`：事务回滚（`ErrVersionConflict`）后仍返回已填好的 `result` Row，调用方若不先判 err 会拿到一个从未写入的版本号。
- `guardrails-smoke.py:83` 的 `expected_status = 400 if code else 200` 写死 400，自定义 451 的拦截断言只能绕开该辅助函数手写（`:151-152`、`:159-160`）。
- `host/judge.go:108-114` 用 `IsBifrostError` 区分网关自造消息与 provider 回显消息：主路径已核实成立，流式/回退等非主路径的取值建议实测复核。

### 有理由保留（写出理由，下一轮不再重提）

- **`http/handler.go:15` 新增的 `guardrails/http → guardrails/plugin` 横向依赖**：仓内已有同形先例（`branding/http → branding/persistence`、`rbac/host → rbac/http`、`rbac/http → identity/http`），`plugin` 不反向 import `http`、不成环，`Swapper` 仍是使用方定义的 1 方法窄接口；把 `Options` 上提到根包会把"客户端状态码与文案"这一协议概念塞进只谈检测的业务根包，代价更大。
- **`secrets/detector_test.go` 的 `fake` 种子在 `TestNoFalsePositives`（70、71）与 `TestIgnoredKeywords`（:169 的 70、71）重号**：两处的长度与字符集不同，生成的串实际不同，也没有任何断言依赖种子唯一；改号收益不抵一次无谓改动。
- **`app/guardrails.go:70` 先给 `options` 一个占位再在 switch 两个分支里覆盖**：三种启动形态（假检测器 / 库中配置 / 未配置）里只有第三种落到占位，写成三分支各自赋值反而更长；问题只在占位值的唯一来源（见本轮 R1），不在这个写法。
- **`judge/parse.go:18` 的 `noRiskName = "none"` 留在 judge 包而不进根包**：`none` 不是 `Level` 的取值（根包 `Level` 零值无效、无风险用空结果表达），把它放进根包的等级名会引入一个不存在的等级。
- **`checker_test.go` 与 `parallel_test.go` 共用 `rule`/`mustChecker`/`detectorFunc` 而分成两个文件**：前者管阈值与失败语义、后者管并行与取消，各自读得完整，夹具靠同包共享没有重复。

## 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 发现：R1（400 + 占位文案两份）、S2（3–5 与默认 3 两份）；上一轮的 400–599 与 64KB 已收口 |
| | 同一规则多份实现且互不注明 | 发现：S2（两处独立校验、互不引用）；R3 的三份 `parseLevel` 已消除 |
| | 重造已有 helper | 无：`RetrySQLiteBusy` 已复用（`migration.go:24`）；`noStore` 与 identity/rbac 的同类是各包自己的一份而非重造；`truncateUTF8` 在 ee 内无同类（`git grep RuneStart\|ToValidUTF8\|func truncate -- ee` 仅本处） |
| | 派生常量无推导 | 发现：S2（默认 3 手抄两处）；`fakeMaxTextBytes` 已改为 `config.DefaultMaxTextBytes` |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无：锁键已命名并列出已占用值（`migration.go:17-19`），状态码走 `fasthttp.Status*` 与 `guardrails.MinDenyStatus` |
| | 状态码、错误码裸数字 | 无：Go 侧统一命名常量；测试与脚本按编码规范可独立书写字面量 |
| | 无名的错误形状 | 无：`evaluateRule` 的三元返回仍由命名类型 `evaluationState` 承载（`evaluation.go:43-49`） |
| | 紧凑但绕的控制流 | 发现：R3（两处 `_ =` 空白赋值）；`app/guardrails.go:71-86` 的 switch 已按上一轮 R7 就地返回，读得顺 |
| | 文件集中或拆分是否妨碍理解 | 无：范围内最大文件 `checker_test.go` 267 行，Go 源文件均 ≤212 行；`state` 与 `Swap` 和两个 Hook 同处一文件，读一次热替换不跨文件 |
| | 多余状态或守卫 | 发现：R1 的根因（`newState` 在 checker 为 nil 时仍强制校验 options，逼出两份占位）；`attach` 与 `assembleGuardrails` 的重复 nil 守卫核实防同一对象但各有入口自检职责，未恶化 |
| | 同一效果做两次 | 无：`no-store` 已收进 `noStore` 中间件（`handler.go:74-80`），三条路由各包一次 |
| | 注释与现状矛盾 | 发现：R4（三处）、R3①（注释承诺未断言的行为） |
| 架构与项目一致性 | 接缝形状与同类不同 | 无：`noStore` 包装与 `identity/http/handler.go:91-94` 同型；迁移与 develop 的 `rbac/persistence/migration.go` 逐项一致；routes.txt 排序与全文惯例一致 |
| | **架构符合性**（EE 后端架构） | 无：见「四维核查」专段，§3 布局、§4 职责表、§6 双入口、§7 集成与生命周期逐条核对一致，本轮未移动任何边界 |
| | **依赖关系** | 无：见「四维核查」专段；唯一新增边 `http → plugin` 有仓内同形先例、不成环、接口仍窄且显式注入，唯一异味是 S2 的复用取舍 |
| | 绕过既有出口 | 无：建表经 `ConfigStore.RunMigration`，响应经 `upstream.SendJSON/SendError`，鉴权经注入的 `auth` 并在 routes.txt 登记，插件经 `SyncLoadedPlugin` |
| | 把上游现状当契约 | 无：`IsBifrostError` 的判别依据已读上游源码核实（`core/providers/utils/utils.go:2080` 置 false、`:2633/2648/2667` 与 `:390/403` 置 true），非凭印象 |
| 契约一致性 | 后端类型/前端类型/开发指南三方不一致 | 发现：R4（三处注释）；README、`app/README`、`ee/README`、架构 §3 与代码现状一致，本期无 UI / OpenAPI / config.schema.json 影响 |
| | 状态码口径不一 | 无：`deny.status` 热更新已生效，`handler_test.go:114` 断言随检查器一起发布，冒烟 `:146-152` 实测 451 |
| | 省略、空、值三态语义不对应 | 无：`0` 与省略等价的口径已写进 README:92；`version` 的 0 / 未配置 / `config:null` 三态在 `store.go:24-29`、`handler.go:85,146`、README:95 一致 |

## 开放式复读

已做开放式复读（放下表，按"半年后接手要改这里的人"逐文件再读一遍范围内文件）。表外发现两条，与表内同等对待，均已写入本轮修复项或有理由保留：

- **R1**（未配置占位两份）——读 `app/guardrails.go` 与 `http/handler.go` 时问"这次把 checker 和 options 绑在一起之后，还有谁在自己造 options"，才发现"未配置"这一种状态被两个包各写了一份，且根因是插件对 nil 检查器仍校验 options 的那个守卫。
- **R3**（`_ =` 空白赋值）——读 `swap_test.go` 最后一个用例时第一眼在意的是 `_, _, _ = inflight, p, t` 这行在做什么，顺着发现注释比断言多说了一句。

另有一条经核实后判为无问题、写入「有理由保留」：`http → plugin` 的新依赖（先 grep 四个同类 http/host 包的 import 再下结论，仓内已有同形先例）。

## 台账对账

- **FIND-017（SQLite busy 重试多份）：未恶化，触发条件已由本案消化**。`persistence/migration.go:24` 改用 `authstore.RetrySQLiteBusy`，本案不再是第三份；branding 的历史副本仍在，条目按原 `convert_when` 继续观察。
- **FIND-028（冒烟脚本复制启动逻辑）：本案侧已消除**。`guardrails-judge-e2e.py:61` 复用 `guardrails-smoke.py:97` 的 `node_config`，不再复制节点配置；`pricing-smoke.py` 本轮未触及，条目保留。
- **FIND-022（评审放过规范明文违规）：本轮按该条指向逐条核对了编码规范硬规则**（导出面、注释一致性、同一规则一处实现、文件行数），产出 R4 与 S1、S2；这些是本轮新引入的，不构成该条恶化，但说明"逐条对照规范明文"这一动作确有产出。
- **FIND-024 / FIND-025（`internal/host` 归属与认证扩展点）：未恶化**。本轮新增宿主适配仍全部在 `guardrails/host/`，没有往 `internal/host/` 加文件，也没有在认证包上新开构造选项。
- **FIND-026 / FIND-027、FIND-012～016、FIND-018～021、FIND-023、FIND-029、FIND-030：本次未触及。**
- 无新增 Finding 条目建议（本轮取舍项 S1–S2 先交实施者处置，未采纳者再按四要件登记）。
