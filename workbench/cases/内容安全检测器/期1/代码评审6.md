---
title: 内容安全检测器 期1 代码评审 #6
type: 代码评审
case: 内容安全检测器
phase: 1
round: 6
verdict: 通过
created: 2026-09-19
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 3a4e87dcb 到候选 02d5d74a85a9b51a9f669f8476fcd5c6685ca933；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 已完成 `3a4e87dcb..02d5d74a85a9b51a9f669f8476fcd5c6685ca933` 五个指定文件的差量复核、全文阅读和开放式复读。上一轮 R1 日志泄露已关闭；checklist 九条命令及产物、符号检查全部亲核通过，未发现新的必修问题。评审前后分支、HEAD、工作树和索引均未变化，无未跟踪文件；仅生成指定 scratch 内的测试产物，未操作已有实例。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| provider 错误不泄露送检内容 | 符合。三个同名伪造类型及普通 502 对照，实际 HTTP 验证均无回显泄露 |
| 连接失败、凭据耗尽可诊断 | 符合。真实触发网关合成错误后，分别保留固定类别 |
| 判官失败进入重试及失败策略 | 保持。六种故障场景各产生四条判官失败日志，客户端均为 `content_safety_check_failed` |
| 脚本调用栈去重、保留部分结果 | 与声明一致。异常注入验证退出 1、保留首条结果、stderr 仅打印一次调用栈 |
| HTTP 测试夹具置位调整 | 等价改写；两种并发交错在 race 下重复 30 次通过 |
| 新增公共接口、依赖或业务功能 | 无；增加 `no_eligible_keys` 固定诊断类别已在第五轮修复记录登记 |
| 多做、少做、未登记偏差 | 本轮无行为层面发现；文档符号引用转收敛评审 |

生产代码差量仅改变判官错误格式化；已追读其日志出口、检测器重试、规则失败分类和客户端拒绝映射。未修改配置、检测算法、持久化与插件快照逻辑，维持差量复核范围。

## 3. 本轮修复项

**无新增必修项。**

上一轮《代码评审5》唯一修复项逐条处置：

| 上轮项 | 处置与证据 |
|---|---|
| R1：provider 伪造网关错误类型后，code/message 进入日志 | **已修复**。[judge.go:130](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:130) 仅从本地映射取固定类别，不复制自由文本；[host_test.go:137](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:137) 同时断言错误与日志无回显。隔离 HTTP 探针验证三个伪造类型及普通 502 均无泄露，真实连接中断、凭据耗尽的诊断仍存在 |

## 4. 需用户裁决 / 有依据撤回

**需用户裁决：无。**

收敛评审转交四项处置：

| 转交项 | 处置与依据 |
|---|---|
| 类型可伪造，是否还需校验哨兵消息 | **修复**。现在只输出固定类别，不再依靠消息可信性保留正文；无需追加消息匹配。HTTP 探针还在响应 JSON 中伪造 `is_bifrost_error=true`，仍未绕过过滤 |
| `upstream_credentials_exhausted` 在上游、生产代码、测试重复，未来改名无信号 | **撤回本侧当前行为缺陷结论，转收敛评审**。当前字面量与上游一致，真实凭据耗尽路径已亲测正确；未来契约同步与常量组织不在本侧裁判范围 |
| 普通 provider 502 用例未断言日志 | **撤回必修要求，未将其记作已补断言**。[host_test.go:147](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:147) 确实仍只检查返回错误；但 [judge.go:76](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:76) 输出同一错误对象，已有其他类别的日志断言，本轮普通 502 HTTP 探针也检查全部新增日志，回显为零 |
| `fail`、`entered`、`gate` 锁外访问是否存在竞态 | **撤回**。`fail` 在同步调用前赋值（[handler_test.go:227](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler_test.go:227)）；两个 channel 在启动 goroutine 前初始化（[handler_test.go:259](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler_test.go:259)），之后不重新赋值。并发修改的 `gated` 已受锁保护；全量及重复 race 验证通过 |

## 5. 转收敛评审 / 挂账

- 转收敛评审：[README.md:101](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:101) 仍指向已删除的 `fromProviderResponse`，当前判别逻辑位于 `judgeError`。
- 转收敛评审：[judge.go:100](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:100)、[host_test.go:137](/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/host_test.go:137) 与上游错误类型字面量的同步及回归护栏。

**挂账：无。**

## 6. 开放式复读的表外发现

已对五个文件按“出问题时如何发现、如何恢复”完成开放式复读，**无行为类表外发现**。

判官故障有固定分类并进入失败策略；脚本异常保留部分结果与调用栈；并发测试检查中间库态和最终发布状态。文档符号引用已转收敛评审。

## 7. 起手表覆盖

以下结果限定于本轮差量及受影响区域。

| 信号 | 结果 |
|---|---|
| 验收标准有未覆盖项 | 无：必过检查通过；真实 DeepSeek 属非必过项，本轮未重跑 |
| 方案、实施记录、diff 三方不一致 | 无行为差异；文档符号引用转收敛 |
| 多出的功能、公共接口、抽象或依赖 | 无 |
| 偏差未登记 | 无：新增固定类别及脚本调整已有记录 |
| 失败被吞或降级为假成功 | 无：故障请求返回检查失败，脚本异常退出 1 |
| 空值、空集合无分支 | 无：状态码、错误字段、响应、候选消息均有相应处理 |
| 取消或超时后仍产生副作用 | 无差量发现：取消返回路径保留；provider 侧继续执行属已声明限制 |
| 重发请求产生重复副作用 | 无：版本冲突测试与 smoke 通过 |
| 事务外写后读、多方只回滚一方 | 无：写库到发布的锁未改，两种交错验证通过 |
| 关闭、旧数据、旧客户端路径无处理 | 无：未配置、reset、重启加载、FAKE 冲突 smoke 通过 |
| 昂贵检查先于廉价检查、校验缺层 | 无差量发现：请求体先限长再解析 |
| 路由保护、认证关闭时行为 | 无差量发现：鉴权链未改；隔离实例匿名读取返回 401，默认认证夹具测试通过 |
| 敏感信息进日志或响应 | 无：上一轮缺陷关闭，HTTP 回显探针通过 |
| 热路径新增 I/O、无界缓冲或循环 | 无：本轮仅调整有界错误格式化 |
| 新配置、环境依赖、部署步骤未登记 | 无 |
| 服务端无法追溯失败原因 | 无：连接失败及凭据耗尽保留固定诊断 |
| 对外泄露内部错误原文 | 无：故障探针均返回固定检查失败响应 |
| 脚本失败现场被清理 | 无：部分结果、调用栈和现场保留 |
| 新规则或失败路径无测试 | 无：新增类型过滤有回显回归，并亲核真实解析路径 |
| 断言绑定实现而非行为 | 无本侧缺陷；类型字符串同步转收敛 |
| 失败信息无法定位条件 | 无：测试含类别标识，脚本保留第 86 行调用栈 |
| 返回值、错误未检查 | 无差量发现：工作 goroutine 结果经 channel 回主测试断言 |
| fixture 依赖个人环境 | 无新增发现：临时 SQLite、随机端口；真实 key 为已声明依赖 |
| mock 结果冒充真实环境结论 | 无：本轮替身验证与历史 DeepSeek 证据分别记述 |
| 整体替换模块的 mock | 无不当使用：行为验证使用真实 EE；脚本替身只核异常处理 |
| 调试输出、临时后门、本机配置 | 无：范围文件残留检索无命中 |
| 未使用代码、无关改动 | 无行为类发现 |
| 生成物与源不一致 | 无：视图校验通过；未改动规则生成源 |

## 8. 验证记录

产物目录为：

`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/workbench/tmp/codereview/内容安全检测器-期1-代码评审6/scratch`

| 实际命令或检查 | 结果 |
|---|---|
| `git status --porcelain=v1 --untracked-files=all`、`git branch --show-current`、`git rev-parse HEAD` | 分支及候选匹配；前后干净，无未跟踪文件 |
| `git diff --stat b9e8752ea`、`git diff --stat 3a4e87dcb..HEAD -- ee` | 完整基线范围已核对；EE 差量恰为指定五文件，48 行新增、47 行删除 |
| `cd ee && GOWORK=off go vet ./...` | 通过 |
| `cd ee && GOWORK=off go test -race ./...` | 通过，部分包使用缓存 |
| `cd ee && GOWORK=off go test -race -count=1 ./internal/guardrails/... ./internal/app ./internal/rbac/host` | 禁用缓存重跑，通过 |
| `cd ee && GOWORK=off go test -race -count=30 -run '^TestUpdateAndResetAreSerialized$' ./internal/guardrails/http` | 30 次通过 |
| `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 通过；仅调整 checklist 产物位置 |
| `gofmt -l ee/internal/guardrails ee/internal/app` | 空输出 |
| `find ee/internal/guardrails ee/internal/app -name '*.go' -exec awk 'END{if(NR>500)print FILENAME}' {} \;` | 空输出 |
| checklist 的 `guardrails-root-stdlib` 原命令 | 空输出 |
| `git diff --check`、`git diff --check 3a4e87dcb..HEAD` | 通过 |
| `python3 -B workbench/tools/build_views.py --check` | 197 篇文档校验通过 |
| `python3 -B ee/scripts/guardrails-smoke.py --binary "$TMPDIR/bifrost-http"` | production、fake-conflict、三个 fake 模式全部 PASS |
| `python3 -B -` 内联读取 checklist，逐项检查路径并执行其 grep | 12 项产物、9 项必含符号、9 项禁留标记全部通过 |
| `rg` 检索五个文件的 TODO/FIXME/HACK/DEBUG、本机路径 | 无命中 |
| 最终 `git diff --stat`、`git diff --cached --stat` | 均为空 |

补充验证使用 `python3 -B - <<'PY' … PY` 内联执行，未保存探针源码：

- **HTTP 错误探针**：三个伪造类型、普通 502、真实连接中断、真实凭据耗尽，六项全部通过；每项四条判官失败日志，类别正确，客户端及新增服务端日志均无回显。现场：`scratch/review6-errors-2lnnbdeh`。
- **脚本异常注入**：第二次 completion 抛出超时；退出 1、保留一条结果，stderr 调用栈仅一次，`results.json` 保存同一调用栈，清理入口均调用。现场：`scratch/ee-judge-e2e-d32xkrim`。
- **smoke 现场**：`scratch/ee-guardrails-yc0vpb0y`。
- **进程表补查受限**：`ps -axo pid=,command=` 被沙箱拒绝。HTTP 探针自身已断言所启动 EE 子进程退出；smoke 正常退出并执行其 `finally` 清理。

**实际阅读范围**：根、workbench、ee 的 AGENTS；指定三份流程文件；需求验收口径、方案 v5 全文、checklist、findings；七份指定证据；允许读取的《代码评审5》；五个范围文件全文和差量。另追读 handler、判官 detector、evaluation、插件拒绝与日志路径、smoke、Node 进程夹具，以及上游 provider 错误解析、连接失败、凭据耗尽和 DeepSeek 调用路径。未读取其他旧评审全文。

**未执行及原因**：

- checklist 必过命令无跳过；未运行会生成报告的预飞包装器，已逐项亲核其检查。
- 未重跑真实 DeepSeek：非必过项，历史真机结果不作为当前候选亲测结果。
- 三个 PostgreSQL 测试 DSN 均未设置；未连接或启动已有数据库实例。
- 未修改源码做变异测试，未重新生成未改动的规则数据。
- 未启动 dev server、`--serve`，未暂存、提交或推送。

verdict: 通过
