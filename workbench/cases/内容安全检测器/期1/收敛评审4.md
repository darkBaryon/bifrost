---
title: 内容安全检测器 期1 收敛评审 #4
type: 收敛评审
case: 内容安全检测器
phase: 1
round: 4
verdict: 有Blocking
created: 2026-09-19
---

> 评审者与实施上下文隔离（独立会话，只读仓库）。差量复核：上一轮候选 `60852b139` → 本轮候选 `4f70c02d92f1c0186e592aa5c0c8bb3451a7b0a8`（`git status --porcelain` 无输出，工作树干净，无未跟踪文件）。
> L2：四维收束为段落，附上一轮修复项逐条处置、起手表逐行结果与开放式复读结论。

## 结论

上一轮的 2 条本轮修复项（R1、R2）已关闭，4 条取舍项中 S1、S3、S4 采纳、S2 有依据撤回（撤回依据是 checklist.yaml:52 对 `ee/internal/guardrails` 的 `net/http` 禁留项，不是口头理由），逐条核对见下节。本轮改动没有移动任何包与文件、没有新增依赖边，模块边界与上一轮逐条相同；判官错误判别从"`IsBifrostError` 是否为真"换成"是否来自 provider 的 HTTP 响应"的方向是对的——它把上一轮暴露的"网关自造消息被丢弃"这一类问题从判别式层面解决，`host_test.go:120-123` 也按上游实际形状（`IsBifrostError` 为 false、无状态码）补了回归。

但新判别式对上游契约的陈述仍然不成立，这是本轮唯一的必修项：`judge.go:91-94` 与 `README.md:101` 都把"`IsBifrostError` 为 false 且带状态码"等同于"provider 的 HTTP 响应"，而上游在**一元请求路径**上至少有两处以这个形状发出网关/传输层自造的错误——`core/providers/utils/utils.go:443-446` 的连接与 DNS 失败（合成 502，消息是 Bifrost 常量），以及 `core/bifrost.go:6220-6231` 的判官 key 全部失效（502 `upstream_credentials_exhausted`）。这两种正是文档明说要保留消息的情形，实际却走了丢弃分支。缺陷类别与上一轮 R1 相同（起手表"把上游现状当契约"），只是判别式换了一侧；因此不视为上一轮问题未关闭，而是本轮新提。另有四条取舍项（策略注释挂在常量上、`testOptions` 声明位置、串行化用例的两段同条件分支、屏障超时一名两裸）交实施者裁量。修复项未关闭，verdict 取 `有Blocking`；无需用户裁决的事项。

## 事实来源

**跑过的命令**（均为只读）：`git status --porcelain`、`git diff --stat 60852b139 4f70c02d9`、`git diff 60852b139 4f70c02d9 -- ee/`、`git grep -n "darkBaryon/bifrost/ee/internal" -- ee/internal/guardrails`（依赖边逐条对照）、`git grep -n "IsBifrostError: *true" -- core/ framework/ transports/ plugins/`、`git grep -n -A4 "IsBifrostError: *false" -- core/bifrost.go core/inference.go core/utils.go | grep StatusCode`（定位"false + 带状态码"的全部产地）、`git grep -n "errAllKeysFiltered\|errAllKeysDead" -- core/`、`git grep -n "executeRequestWithRetries" -- core/`、`git grep -n "NewBifrostUpstreamConnectionError" -- core/providers/utils/utils.go core/providers/openai/openai.go`、`git grep -n "ErrProviderDoRequest\s*=\|ProviderConnectionFailed\s*=" -- core/schemas/`、`git grep -n "ErrProviderResponseHTML\|ErrProviderResponseUnmarshal\|ErrProviderResponseEmpty" -- core/schemas/`、`git grep -n "InvalidRequestErrorf(" -- core/`、`git grep -n "net/http\|fasthttp" -- workbench/cases/内容安全检测器/期1/checklist.yaml`、`git grep -n "FIND-0" -- workbench/findings.md`、`git grep -n "Gate 决策结果\|实施偏差记录" -- .../方案v5.md`；`git show 4f70c02d9:<file> | sed -n` 读上游片段；Grep 工具定位 `core/bifrost.go` 的函数边界（`executeRequestWithRetries` 在 :6081）与 `core/providers/utils/utils.go` 的 `makeRequestWithDoFunc`（:366）。

**完整读过的范围内文件现状**：`ee/internal/guardrails/README.md`、`host/judge.go`、`host/host_test.go`、`http/handler_test.go`、`plugin/swap_test.go`、`plugin/plugin_test.go:1-110`（夹具区，变更行在 :51）、`ee/scripts/guardrails-judge-e2e.py`。范围外但本轮必须读的现状：`http/handler.go`（全文，为核实串行化用例断言的对象是真实锁边界）。

**对照的同类实现**：`plugin/plugin_test.go:18-89`（本包共享夹具的实际归属地：`detectorFunc`、`testLogger`、`testRule`、`newPlugin`、`testContext`、`primed`、`message`、`request`、`response`、`requireCode` 全在此文件），用于判断 `testOptions` 的声明位置；`http/handler.go:118-119` 与 `:139-140` 的两处 `h.mu.Lock()`（串行化用例声称验证的对象）；`http/handler.go:82-93` 的 `get` 不取 `h.mu`（用例在第一方持锁期间直接读库的前提）。

**核实过的上游事实**（结论依赖它们，逐条读到源码）：
- `core/providers/utils/utils.go:2723-2735` `NewBifrostUpstreamConnectionError` 置 `IsBifrostError: false` + `StatusCode: 502` + `Error.Type = "provider_connection_failed"`（`core/schemas/bifrost.go:1903`）+ `Error.Message` 为常量（`core/schemas/provider.go:41`）。其调用点 `core/providers/utils/utils.go:443`（DNS / `net.OpError`）与 `:446`（连接失败、fasthttp 超时）位于 `makeRequestWithDoFunc`（:366）→ `MakeRequestWithContext`（:459），即**一元**请求路径，判官走的就是它。
- `core/bifrost.go:6207-6218`（`errAllKeysFiltered` → 503 `no_eligible_keys`）与 `:6220-6231`（`errAllKeysDead` → 502 `upstream_credentials_exhausted`）同样置 `IsBifrostError: false` + `StatusCode`，产地在 `executeRequestWithRetries`（:6081），由非流式分发 `:7231` 调用；哨兵产生处见 `:7030`、`:7076-7078`。`keyPoolFilter` 在本仓 transports/ee 未装配（`git grep "KeyPoolFilter:"` 无命中），故 503 分支当前不可达；502 分支在判官 provider 的 `max_retries ≥ 1` 且其 key 全部返回 401/402/403 时可达。
- `core/bifrost.go:6999-7017`：key 池构建失败为 `IsBifrostError: false` + **无**状态码——本轮新代码正确地把它留在 gateway 分支，`host_test.go:121` 的回归形状与之一致。
- `core/utils.go:335-360` `newBifrostCtxDoneError`：取消 499 / 超时 504 均为 `IsBifrostError: true` **且带状态码**，落入 gateway 分支，与注释一致。
- `core/providers/utils/utils.go:2063-2089` `HandleProviderAPIError` 与 `core/providers/openai/errors.go:30`：provider 响应错误确为 `IsBifrostError: false` + 原样状态码，判别式的这一侧成立。
- `core/providers/utils/utils.go:2635-2680`：`HandleProviderResponse` 的 HTML / 反序列化失败把原始正文放进 `Error.Error`（`judgeError` 不打印该字段），`Message` 为常量，无回显风险。

**未执行**：任何测试、构建、`-race` 运行、冒烟、服务启动与真机判官调用（只读评审）；性能 / 并发 / 安全实测；`data/rules.json` 逐条校对；本轮未改动的文件（`checker.go`、`evaluation.go`、`rule.go`、`detector.go`、`config/*`、`persistence/*`、`plugin/plugin.go`、`plugin/text.go`、`plugin/deny.go`、`detectors/*`、`app/guardrails.go`）只在引用处查证，未重读全文。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无新增。上一轮的三份拒绝策略字面量已收敛为一处 `testOptions` | `plugin_test.go:51`、`swap_test.go:15,131` |
| 职责与实现质量 | 策略注释挂在无关常量上（S1）；串行化用例同一条件分两段判断（S3）；屏障超时一处命名两处裸值（S4） | `judge.go:91-95`；`handler_test.go:241-252`；`handler_test.go:236,264,285` |
| 架构与项目一致性 | 发现：`judge.go:91-94` 把"`IsBifrostError` 为 false + 带状态码"当作 provider 响应的等价契约，上游一元路径有反例（R1） | `core/providers/utils/utils.go:443-446,2723-2735`；`core/bifrost.go:6220-6231` |
| 契约一致性 | 发现：包 README 的判官错误策略与上一条同源（R1 的第二处位置）；共享夹具声明在非夹具文件（S2） | `README.md:101`；`swap_test.go:14-15` vs `plugin_test.go:51` |

**架构符合性（EE 后端架构 §4/§6/§7/§8）**：本轮未新增、删除或移动任何包与文件，边界与上一轮完全一致。按 §4 职责表复核受影响文件：`host/judge.go` 新增的 `fromProviderResponse` 仍是"把上游 `BifrostError` 翻译成本模块可用的中性错误"，属 §7「适配归位判定」中"只服务某项业务的宿主检查放在 `<模块>/host/`"，没有上提到业务根包，根包也没有因此知道 HTTP 状态；README 是包内实现说明，不触及 §8 的 API / 表名 / 迁移 ID 契约（`ee_guardrails`、`ee_guardrails_v1` 未动）；§6「管理接口放 `guardrails/http/`、实时检测入口放 `guardrails/plugin/`」继续成立，三个改动的测试文件都在各自包内。`handler_test.go` 改的是夹具与断言，未把业务判断搬进 Handler 或测试。

**依赖关系（EE 后端架构 §5/§7）**：`git grep -n "darkBaryon/bifrost/ee/internal" -- ee/internal/guardrails` 的 40 行结果与上一轮逐条相同，本轮零新增边、零删除边。①业务根包 `guardrails` 只作为被 import 方出现；②`host` 只 import 根包、`config`、两个检测器包与上游 `core`/`core/schemas`，未反向 import `http`/`plugin`，`judge.go` 本轮未增减 import；③`http → plugin`（`handler.go:15`）是上一轮已核实的既有横向边，`handler_test.go:18` 沿用同一别名，未扩大；④接口仍窄且经构造函数注入（`host.Client` 1 方法、`host.Logger` 2 方法、`http.Swapper` 1 方法、`http.Logger` 只 `Error`）；⑤本轮未新增跨模块写入，`persistence/migration.go:8` 对 `identity/persistence` 的复用未触及；⑥共享资源所有权未变（`handler_test.go:81-82` 的 `sql.Close()` 仍由建库方 `t.Cleanup` 负责）。

## 上一轮（收敛评审3）修复项处置

| 上一轮条目 | 处置 | 证据 |
|---|---|---|
| R1 `judgeError` 的策略注释举的例子走的是它排除的分支 | **已修复** | 判别式改为 `fromProviderResponse`（`judge.go:122-125`），"key 池没有支持判官模型的 key"（`core/bifrost.go:6999-7017`，无状态码）现落在 gateway 分支并保留消息；`judge.go:91-94` 注释按新判别式重写，不再把该例子归入 `IsBifrostError`；`host_test.go:120-125` 新增"无状态码 + no keys found"用例，断言 `category=gateway` 且消息保留。原注释所述与代码行为的矛盾已消除（新注释另有对上游契约的陈述问题，见本轮 R1，属新提） |
| R2 包 README 的判官错误策略停留在改动前口径 | **已修复** | `README.md:101` 改为两分支表述："来自 provider HTTP 响应的错误（type、code、message 都可能回显送检内容）一律不进日志，只留状态码与按状态码派生的固定类别；网关或传输层自造的错误……保留类型、代码与前 200 字节消息"，与 `judge.go:103-119` 的两分支逐字对应；`type`/`code` 不再被写成 provider 错误的保留项 |
| S1 `cancelled` 类别不带 `provider_` 前缀 | **已采纳** | `judge.go:136` 改为 `provider_cancelled`；六个取值现全部带 `provider_` 前缀，gateway 侧另用 `category=gateway`（`:107`），两侧可区分 |
| S2 新增状态码判别用裸数字 | **有依据撤回** | 按上一轮给出的反证条件保留字面量，并在 `judge.go:128` 补"数字是 HTTP 状态码；本包不引入 HTTP 框架，故不用命名常量"。撤回依据经核实成立且强于注释所述：`checklist.yaml:52` 有 `{grep: 'net/http', in: ee/internal/guardrails}` 禁留项，方案 v5:250 登记了这一取舍。不再重提（注释措辞见「有理由保留」） |
| S3 拒绝策略字面量在测试同包出现第三份 | **已采纳** | `plugin_test.go:51` 的 `newPlugin` 改用 `testOptions`；`swap_test.go:131` 断言改用 `testOptions.StatusCode` / `testOptions.DenyMessage`；`swap_test.go:14` 补"newPlugin 与断言共用"。三份收敛为一处（声明位置见本轮 S2） |
| S4 脚本用 dict 当闭包单元格 | **已采纳** | `guardrails-judge-e2e.py:70` 改为 `error = None`，`:74`、`:110-112` 直接读；`:73` 注释写明"error 由 main 的 except 写入，闭包读到最新值"。`error` 是 `main` 的局部变量、`persist` 是其闭包，赋值发生在 `main` 自身作用域（`:93`），读取语义正确 |

无未关闭的上一轮条目。上一轮「转代码评审」栏的前两条（网关自造 `no keys found` 消息被丢弃、`host_test.go` 用错形状构造该错误）实际在本轮一并处置，见 R1 行证据；后三条属代码评审判断，本侧不出结论。

## 出口清单

### 本轮修复项

**R1 · 判官错误策略的注释与 README 把"带状态码且非 `IsBifrostError`"当成 provider 响应的等价契约，上游一元路径有反例**

位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:91-94`（对应判别式 `:122-125`、分支 `:103-106`）与 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/README.md:101`。

代码事实：注释写「provider 的 HTTP 响应（`IsBifrostError` 为 false 且带 provider 状态码……）」与「网关或传输层自造的错误（`IsBifrostError` 为 true 的取消/超时，或**没有 provider 状态码**的错误）」，README 同口径。`fromProviderResponse` 按 `!bErr.IsBifrostError && bErr.StatusCode != nil` 判定。上游在判官所用的一元 Chat 路径上，至少两处以这一形状发出**非** provider 响应的错误：

- `core/providers/utils/utils.go:2723-2735` `NewBifrostUpstreamConnectionError` 置 `IsBifrostError: false`、`StatusCode: 502`、`Error.Type = "provider_connection_failed"`、`Error.Message` 为 Bifrost 常量（`core/schemas/provider.go:41`）；调用点 `core/providers/utils/utils.go:443`（DNS / `net.OpError`）与 `:446`（连接失败、fasthttp 超时）在 `makeRequestWithDoFunc`（`:366`）内，即"网关根本没连上 provider、没有任何响应正文"。
- `core/bifrost.go:6220-6231` 在 `executeRequestWithRetries`（`:6081`，非流式分发 `:7231` 调用）把 `errAllKeysDead` 转成 `IsBifrostError: false`、`StatusCode: 502`、`Error.Type = "upstream_credentials_exhausted"`、`Error.Message` 为哨兵文本（`:6061`），触发条件是判官 provider 的 key 全部返回 401/402/403 且 `max_retries ≥ 1`。

这两类错误经 `:103` 判定为 provider 响应，type / code / message 全部丢弃，只剩 `status=502 category=provider_unavailable`。

维护影响：两处文本都明说这类"网关或传输层自造的错误"会保留类型、代码与截断消息，实际不会——编码规范把与实际行为不一致的注释视为缺陷，包 README 又是本模块对接手人唯一的约束说明。代价是双向的：读文档的人以为 502 的日志里有诊断信息，去找却只有一个 `provider_unavailable`；而这两种恰是"判官全线失败"最常见的两个原因（连不上判官 provider、判官 key 失效），它们的消息是 Bifrost 常量、不含送检内容，丢弃它们不换来任何脱敏收益。上一轮 R1 是同一类错误的镜像（当时把 `IsBifrostError` 当成"网关自身错误"的等价式），判别式换了一侧后该类错误仍未被真正核实。

最小修正（本评审范围内，只动文本）：把 `judge.go:91-94` 与 `README.md:101` 改成陈述**实际实现的规则**——"凡带状态码且 `IsBifrostError` 为 false 的错误一律按可能回显处理，只留状态码与派生类别；这包含网关为连接失败、key 耗尽合成的 502/503，其消息虽不含送检内容也一并丢弃"，并把举例中的"取消、超时"保留、删掉会被误导的"传输层"字样。若倾向于改判别式让这两类回到 gateway 分支（例如按 `Error.Type` 识别 `provider_connection_failed` / `upstream_credentials_exhausted`），那是行为变更，其取舍与回归归代码评审，本轮已在「转代码评审」记一行；两条路径任选其一都能关闭本项，但不能两处文本都不动。

### 需用户裁决的 Blocking

- 无。

### 立债（REF）

- 无。

### 挂账（Finding，建议取舍项；由实施者决定采纳或撤回，未采纳的按四要件登记）

**S1 · 判官错误的两分支策略注释挂在 `maxErrorMessageBytes` 常量上**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/host/judge.go:91-95`。代码事实：`:91-94` 的四行注释描述 `judgeError` 的两类来源与各自保留策略，紧接其后的 `:95` 是 `const maxErrorMessageBytes = 200`，因此它是该常量的 doc 注释；真正的函数 `judgeError`（`:98`）自己只有一行 `:97` 「把网关错误转成不含送检内容的普通 error」。本轮该段从三行增至四行并承载了完整的两分支分类。
维护影响：`go doc` 里这段策略挂在一个数字常量名下，而常量自身"200 字节是消息截断上限"的含义反被埋在段落中间；改 `judgeError` 的人从 `:97` 往下读，策略说明在六行之外的上方。编码规范要求"对外类型和函数说明用途""关键字段和常量补充单位与业务限制"，当前是两者互换了位置。
最小修正：把 `:91-94` 整段移到 `judgeError`（`:98`）之上并与 `:97` 合并，`:95` 常量处留一行"错误消息保留上限，按 UTF-8 边界截断"。
反证条件：若认为策略描述应与它引用的唯一数字相邻、移走后 `maxErrorMessageBytes` 会失去上下文，则保留现状——此时把 `:97` 那行改成"策略见上方常量注释"，不让两处各说一半。

**S2 · 本包共享测试夹具 `testOptions` 声明在 `swap_test.go`，而夹具的归属文件是 `plugin_test.go`**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/swap_test.go:14-15`（使用方在 `/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/plugin/plugin_test.go:51`）。代码事实：`plugin_test.go:18-89` 集中了本包全部共享夹具（`detectorFunc`、`testLogger`、`testRule`、`matchingDetector`、`newPlugin`、`testContext`、`primed`、`message`、`request`、`response`、`requireCode`）；上一轮 S3 采纳后 `newPlugin` 开始引用 `testOptions`，但该变量仍声明在 `swap_test.go`，而 `swap_test.go:1` 的文件头写明本文件职责是"验证检查器热替换与请求级快照"。
维护影响：打开 `plugin_test.go` 看 `newPlugin` 的人要跨文件才知道插件被构造成什么拒绝策略；`swap_test.go` 的文件头一句话与它实际承担的"声明全包夹具"不再对应，而编码规范要求每个文件顶部一句话说明职责、并把需要一起理解的代码放在一起。
最小修正：把 `:14-15` 连同注释移到 `plugin_test.go` 的夹具区（`:44` 的 `newPlugin` 之前）。
反证条件：若认为夹具应声明在第一个引入它的用例文件、且 `:14` 的注释已足以指路，则保留——此时把 `swap_test.go:1` 的文件头补上"并声明本包共用的拒绝策略夹具"，让文件职责与内容一致。

**S3 · 串行化用例把同一个 `first == "reset"` 判断拆成两段，中间隔着无关语句**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler_test.go:241-252`。代码事实：`:241-247` 的 `if first == "reset"` 负责播种数据并设定第二方的 `second/secondPath/secondBody`；`:248` 设置 `swapper.entered/gate`；`:250-252` 又是一个 `if first == "reset"`，只为设定 `firstPath/firstBody`。同一条件在本用例里共出现四次（`:242`、`:250`、`:275`、`:293`）。
维护影响：`:248` 夹在两个同条件分支之间，读的人必须确认它是否必须在两者之间（它只需要在播种之后，与第二段分支无关）才敢合并或移动；改交错顺序时要记住角色赋值分布在两处。
最小修正：把 `:250-252` 并入 `:242` 的分支，先给 `firstPath/firstBody/secondPath/secondBody/second` 一组默认值（update 先行），分支内整体对调，`:248` 留在分支之后。
反证条件：若认为"播种"与"角色对调"是两件事、合并后分支体承担两个目的反而更难读，则保留现状——此时在 `:248` 上方补一行说明它必须晚于播种、早于并发启动，消除读者的顺序疑问。

**S4 · 同一用例里的等待时长一处命名、两处裸值**
位置：`/Users/xinyue/VSCode/ws_2026/bifrost-content-safety/ee/internal/guardrails/http/handler_test.go:236`（`const wait = 200 * time.Millisecond`）与 `:264`、`:285`（两处 `time.After(5 * time.Second)`）。代码事实：三处时长语义不同——`wait` 是"观察第二方是否错误地完成"的窗口，两处 5s 是"合法阻塞的请求最长允许多久返回"的兜底上限；只有前者有名字。
维护影响：调稳定性时（负载机器上 200ms 是否够）要先分辨哪些数字可以一起调、哪些不能，而命名只覆盖了三者之一；`wait` 这个名字本身也没说明它是"预期不发生"的窗口而非"预期完成"的等待。
最小修正：三处都命名，例如 `holdWindow = 200 * time.Millisecond`（预期无事发生）与 `barrierTimeout = 5 * time.Second`（屏障兜底），各带一句用途。
反证条件：若认为测试内的超时兜底属一次性数值、命名不增加可读性，则保留，但把 `wait` 也改回字面量或就地注明它与 5s 的区别，不保留"一名两裸"的中间状态。

### 转代码评审（范围外发现，一行一条，不出结论）

- `ee/internal/guardrails/host/judge.go:103-106` 与 `core/providers/utils/utils.go:443-446`、`core/bifrost.go:6220-6231`：网关连不上判官 provider（合成 502 `provider_connection_failed`）与判官 key 全部失效（502 `upstream_credentials_exhausted`）时，其 Bifrost 常量消息与类型被丢弃，日志只剩 `status=502 category=provider_unavailable`，两种最常见的判官故障失去可诊断信息。
- `ee/internal/guardrails/host/host_test.go:111-137`：三个用例覆盖了 `false+有状态码`、`false+无状态码`、`true+无状态码`，缺 `IsBifrostError: true` **且带状态码**（`core/utils.go:345-356` 的取消 499 / 超时 504 的真实形状）；把 `fromProviderResponse` 简化成 `StatusCode != nil` 时现有用例仍全绿。
- `ee/internal/guardrails/host/host_test.go:126-129`：`cancelled` 替身不带 `StatusCode`，与上游真实取消错误（499）的形状不符。
- `ee/internal/guardrails/http/handler_test.go:42,47-48`：`fakeSwapper.gated` 在 `s.mu` 之外读写，而同结构的 `swaps`/`options` 受该锁保护；`Swap` 正是用例要证明"未被串行化即失败"的入口。
- `ee/internal/guardrails/http/handler_test.go:268-272`：串行化仍有一路依赖 200ms 时间窗的断言，负载机器上的稳定性需评估（另一路 `:274-277` 的读库断言不依赖时间）。

### 有理由保留（写出理由，下一轮不再重提）

- **`judge.go:129-143` 的状态码字面量**：上一轮 S2 的撤回依据经核实成立——`checklist.yaml:52` 对 `ee/internal/guardrails` 整目录禁留 `net/http`，方案 v5:250 已登记该取舍，本条不再提。仅记录一处措辞出入供下次改注释时顺手处理：`:128` 写"本包不引入 HTTP 框架"，而被排除的 `net/http` 是标准库、真正的依据是上述禁留项；这不构成注释与行为不一致，不单列条目。
- **`providerFailureCategory` 与 `category=gateway` 用字面量而非类型化常量**：编码规范"对外枚举必须是命名常量"约束的是"会持久化或出现在接口响应里的取值"，这些值只进服务端日志文本，不跨包、不落库、不进响应；与 `rbac/http/protocol.go` 的 `FromStatus` 用途不同（那边绑定对外 `error.code`），抽公共表会把 rbac 的错误域拖进 `guardrails/host`。
- **`handler_test.go` 304 行未拆**：按编码规范"超过 300 行须评估拆分"评估——八个用例共用 `setup`、`call`/`tryCall`、`fakeBuilder`、`fakeSwapper`、`testLogger`、`validConfig` 六个夹具，拆成两个文件会逼出第三个夹具文件，理解一个用例反而要跨文件跳转；离 500 行强制线尚远，保持集中。
- **`outcome` 结构体（`handler_test.go:228-231`）只服务一个用例却定义在包级**：Go 测试文件无法在函数内定义带方法集的具名类型以外的更好归属，且它就近定义在唯一使用者之上，符合"错误和参数类型可以就近定义"。
- **`guardrails-judge-e2e.py:72-74` 的 `persist()` 闭包读 `error`**：上一轮 S4 的采纳形态正确——`error` 是 `main` 的局部变量，`:93` 的赋值在同一作用域，闭包按最新值读取，无需 `nonlocal`；`:73` 的注释已把这一点写明，不必再改成显式传参。
- **`judgeError` 在 gateway 分支同时输出 `status=` 与 `category=gateway`**：两者不重复——`status` 来自上游（可能为空），`category` 标明来源侧；取消的 499 与 provider 返回的 499 因此可区分（`category=gateway` vs `provider_cancelled`）。

## 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无：上一轮的三份 `400`+"请求已拦截" 已收敛为 `testOptions` 一处；本轮新增的 `5 * time.Second` 两处属等待兜底，另记为 S4 的取舍项而非重复规则 |
| | 同一规则多份实现且互不注明 | 无：错误分类规则只在 `judge.go:98-143` 一份；README:101 是它的文档面，本轮两者口径一致（其对上游的陈述问题见 R1，不是第二份实现） |
| | 重造已有 helper | 无：`fromProviderResponse` 是本判别式的具名化，ee 内无同类；`tryCall`/`call` 是一次抽取加一层薄包装，不是两份实现 |
| | 派生常量无推导 | 无：本轮未新增派生值；`wait` 与两处 5s 相互独立，不存在应推导却手抄的关系 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 发现（轻微）：S4（`handler_test.go` 的 5s 屏障两处裸值）；`judge.go` 的状态码字面量按上一轮 S2 有依据保留，不重提 |
| | 状态码、错误码裸数字 | 无（有依据）：见「有理由保留」第一条，checklist.yaml:52 禁留 `net/http`，已就地注明数字是 HTTP 状态码 |
| | 无名的错误形状 | 无：`judgeError` 返回普通 `error`；`tryCall` 的第三返回值是具名 `error`；`outcome{status, err}` 已把用例的二元结果具名化（上一轮该处是裸 `chan int`） |
| | 紧凑但绕的控制流 | 发现：S3（同条件两段分支被 `:248` 隔开）；`:275` 的 `configured != (first == "update")` 虽是异或写法，但紧邻的 `:273` 注释已说明预期，不单列 |
| | 文件集中或拆分是否妨碍理解 | 发现：S2（共享夹具与其归属文件分离）；另附评估：`handler_test.go` 304 行按硬规则评估后保持集中（见「有理由保留」），范围内 Go 源文件其余均 ≤ 202 行 |
| | 多余状态或守卫 | 无：`fakeSwapper.gated` 是新增状态但有用——它让第二方的 `Swap` 不阻塞，从而把"第二方是否已写库"变成可直接观测的断言（其并发保护归代码评审，已转交） |
| | 同一效果做两次 | 无：`persist()` 的循环内调用与 `finally` 调用覆盖不同时刻；`:274` 与 `:289` 的两次 `get` 分别断言"持锁期间"与"放行之后"，不是重复 |
| | 注释与现状矛盾 | 发现：R1（`judge.go:91-94` 与 `README.md:101` 对上游契约的陈述）；上一轮 R1/R2 指出的两处已修正；`host_test.go:110`、`handler_test.go:39,233-234`、`swap_test.go:14`、脚本 `:73` 的新注释与代码逐条相符 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无：本轮未新增接缝；`tryCall`/`call` 的分层与本包既有夹具风格一致 |
| | **架构符合性**（EE 后端架构） | 无：见「四维核查」专段，§4 职责表、§6 双入口、§7 适配归位、§8 契约不变逐条核对一致，本轮未移动任何边界 |
| | **依赖关系** | 无：`git grep` 核实 40 条包依赖边与上一轮逐条相同，零新增；根包不反向 import，接口仍窄且显式注入，共享资源所有权未变 |
| | 绕过既有出口 | 无：建表仍经 `persistence.Migrate`，响应仍经 `upstream.SendJSON/SendError`，发布仍经 `Swapper.Swap`；本轮未新增出口 |
| | 把上游现状当契约 | 发现（即 R1）：`IsBifrostError` 为 false + 带状态码被当作"provider 响应"的等价判别式，上游一元路径有两处反例；已读源码核实，非凭印象 |
| 契约一致性 | 后端类型/前端类型/开发指南三方不一致 | 发现：R1 的第二处位置（`README.md:101`）；本期无 UI / OpenAPI / `config.schema.json` 影响，`ee/README.md` 只指向包内说明、未随本轮改动失效 |
| | 状态码口径不一 | 无：对外状态码本轮未改；`update`/`reset` 的失败仍同为 500，`MinDenyStatus/MaxDenyStatus` 未动 |
| | 省略、空、值三态语义不对应 | 无：`bErr.StatusCode` 的 nil / 有值、`bErr.Error` 的 nil / 有值在 `judgeError` 中各有分支且不混淆；`get` 的 `config: null` + `version: 0` 三态未变 |

## 开放式复读

已做开放式复读（放下表，按"半年后接手要改这里的人"把范围内 7 个文件逐个再读一遍）。表外发现一条，与表内同等对待：

- **R1 是复读的产物，不是起手表的产物。** 第一眼让人停住的不是注释与代码矛盾（它们是一致的），而是 `judge.go:122-125` 那个只有一行的谓词加一句"只有这种错误可能带回显文本"——一个把"错误从哪来"压缩成两个布尔条件的断言，值得去上游验一遍。顺着 `IsBifrostError: false` 且带 `StatusCode` 的全部产地查下去（`git grep -A4 ... | grep StatusCode`），才碰到 `NewBifrostUpstreamConnectionError` 和 `errAllKeysDead` 这两处网关自造的 502。起手表的"把上游现状当契约"一行只能提示去查，查到哪一条取决于是否沿着"这两个条件真的等价于那句话吗"往下走。

另有两条经核实后判为无问题、写入「有理由保留」：`persist()` 闭包读 `error` 的 Python 语义（先读作用域与赋值点，确认无需 `nonlocal`）；gateway 分支同时输出 `status=` 与 `category=`（先确认 499 的两种来源可区分）。

## 台账对账

- **FIND-022（评审放过规范明文违规）：本轮按该条指向逐条对照了编码规范硬规则**（导出面、注释一致性、同一规则一处实现、裸数字、文件行数、文件顶部职责说明），产出 R1 与 S1、S2、S4；其中"改代码时必须同步检查并更新相关注释"正是 R1 的依据。该条本身未恶化。
- **FIND-017（SQLite busy 重试多份）：未恶化**。`persistence/` 本轮未触及。
- **FIND-028（冒烟脚本复制启动逻辑）：未恶化**。`guardrails-judge-e2e.py` 本轮只改闭包变量形态，`:65` 仍复用 `guard_smoke.node_config`，未新增复制。
- **FIND-024 / FIND-025（`internal/host` 归属与认证扩展点）：未恶化**。本轮改动全部落在 `guardrails/host/judge.go` 与测试，未往 `internal/host/` 加文件。
- **FIND-030（上游错误转换口径）：相关但未恶化**。本轮新增/改写的仍是只进服务端日志的失败类别，不进接口响应，与 FIND-030 所指的 `rbac/http` 对外 `error.code` 不是同一出口；若将来把判官失败类别对外暴露，应与该条一并统一。
- **FIND-012～016、FIND-018～021、FIND-023、FIND-026 / FIND-027、FIND-029：本次未触及。**
- 无新增 Finding 条目建议（本轮取舍项 S1–S4 先交实施者处置，未采纳者再按四要件登记）。
