---
title: 角色权限后端 期4 收敛评审 #4
type: 收敛评审
case: 角色权限后端
phase: 4
round: 4
verdict: 有Blocking
created: 2026-09-17
---

> 本轮评审由启动脚本在新会话中执行，与实施上下文隔离。
> - 基线：`f371548b5444693d7e259033bec9f4a467e3748b`
> - 交付物：`4c62a615e10a2e6774efde48e8fc471fdd738ffa`
> - 本轮是差量复核。上一轮候选为 `38baff8fd970f4d0064651b5e919b8a8958d9f8b`，范围是 `git diff 38baff8fd..4c62a615e`。
> - 工作树状态：`git status --porcelain --untracked-files=all` 输出为空，没有未暂存或未跟踪的文件；`git rev-parse HEAD` 与交付物一致。
> - 差量中的 ee 与 product 文件共 18 个，与指定清单一致。workbench 下的改动属于过程文档，只作为裁决依据读取。
> - 风险等级 L3，逐维留痕。

## 结论

**总体判断**
- 上一轮的 M1–M8 全部已修复，证据见下表。
- 取舍项的处理：
  - S1、S3 已采纳；
  - S2 按反证条件保留，理由成立，见 K4。
- 本轮差量没有改变模块边界和依赖方向，符合 EE 后端架构：
  - 迁移改为复用上游的加列 helper；
  - 删除相关的注释、README、接口指南和产品方案已并入正文；
  - 凭据测试已按功能归组。
- 仍有一处小问题需要在本轮修复（N1）：
  - `dto_test.go` 新增状态响应键集合测试后，文件头注释没有同步；
  - 两个键集合测试是同一段断言的两份写法。
- 按规则，修复项未关闭时结论为"有Blocking"。该修复在授权范围内，不需要用户另行批准。关闭后即可按"无碍"交付。

## 事实来源

**执行的命令**
- `git status --porcelain --untracked-files=all`、`git rev-parse HEAD`
- `git diff --stat 38baff8fd..4c62a615e`
- `git diff 38baff8fd..4c62a615e -- ee product`
- `git diff 38baff8fd..4c62a615e -- workbench/cases/角色权限后端/期4/方案v1.md workbench/findings.md`
- `git ls-files ee/internal/rbac/persistence ee/internal/rbac/identity ee/internal/rbac/http`
- `git grep`：
  - 旧测试函数名、`lastLoginPrecision`、`LastLoginAt`；
  - identity 根包中的常量与变量声明位置；
  - `AddColumnIfNotExists` 在上游 configstore、logstore 中的用法；
  - `credentials_test.go` 中的 `403` 与状态码写法；
  - 产品方案中的实现名词和"本轮"；
  - `Security` 在 permissions.go 和角色权限接口指南中的出现位置。

**先读的规则与裁决**
- 根 AGENTS.md、workbench/AGENTS.md
- 收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md
- templates/收敛评审.md、product/架构/EE后端架构.md（全文）、workbench/findings.md（全文）
- 期4 方案v1 全文，包括：
  - 「Gate 决策结果」「实施偏差记录」
  - 两节「Gate 决策追加」
  - 「第二轮复核修正」
  - 本轮新增的「第三轮评审收尾」
- 上一轮记录 收敛评审3.md（全文）

**完整阅读的范围内文件（当前现状）**
- identity：`accounts.go`、`session.go`、`model.go`、`README.md`
- identity/http：`dto.go`、`dto_test.go`、`README.md`
- identity/persistence：`migration.go`、`README.md`
- rbac：`assignments.go`、`README.md`
- rbac/host：`providers.go`、`credentials_test.go`、`README.md`
- rbac/persistence：`accounts_test.go`
- ee/docs：`账号认证接口.md`、`账号认证升级与恢复.md`
- product：`需求/角色权限/产品方案.md`

**对照的同类实现与上游证据**
- `framework/migrator/addcolumn.go:11-63`：
  - helper 自带 HasColumn 检查；
  - PG 下执行 `ADD COLUMN IF NOT EXISTS`；
  - 注释要求调用方不要再包一层检查。
- `framework/configstore/migrations.go:30`、`:512`、`:539`，以及 `framework/logstore/migrations.go:26`：上游通过包内别名调用该 helper，并传入 logger。
- `ee/internal/identity/passwords.go:71-74`：ResetPassword 在事务外校验 UUID 格式，是 S1 调整的对照。
- `ee/internal/identity/access.go:12`：根包在 model.go 之外已有就近定义的常量块，是 K1 的依据。
- `ee/internal/identity/persistence/store.go:255-273`：每个身份事务（包括登录）都会调用 `policyFactory` 构造策略，是 K4 的依据。
- `ee/internal/identity/persistence/store.go:160-178`：DeleteAccount 的存储实现，用于核对 README 描述。
- `ee/internal/rbac/permissions.go:19`、`:89-90`、`:150` 与 `ee/docs/角色权限接口.md:48`：核对产品方案中"安全"模块的敏感权限与实现是否一致。
- `ee/internal/rbac/host/routes.txt:684-686`：核对升级文档中 `/api/dev/pprof` 需要 `Settings.Manage,Logs.RevealContent`。

**起手表覆盖、开放式复读**：见文末两节。

## 上一轮「本轮修复项」处置

| 编号 | 处置 | 证据 |
|---|---|---|
| M1 status 响应里从不赋值的 `last_login_at` | 已修复 | ① `dto.go:51-58` 中的 `statusResponse` 已没有该字段；`accountDTO.LastLoginAt` 保留（`:86`、`:90`）。<br>② `账号认证接口.md:21` 的 status 行没有该键。<br>③ `dto_test.go:32-50` 新增了状态响应键集合断言。 |
| M2 `deleteAccountRequest` 放错分段 | 已修复 | 已移到 `dto.go:126-129`，位于 `setStatusRequest` 之后、"密码线"（`:131`）之前。 |
| M3 import 分组 | 已修复 | ① `rbac/persistence/accounts_test.go:4-14`：标准库一组，其他包按路径排序为一组。<br>② `rbac/host/credentials_test.go:4-18`：`github.com/valyala/fasthttp` 已移到第三方分组末尾。 |
| M4 加列迁移没有复用 helper，也没有说明 | 已修复 | ① `migration.go:66-69` 改为 `migrator.AddColumnIfNotExists(tx, nil, &accountRow{}, "LastLoginAt")`，并在 `:67` 加注释"新库的v1已按当前行结构建好该列"。<br>② 文件头 `:1`、`MigrateIdentity` 注释 `:23` 已同步。<br>③ `persistence/README.md:7`、`:28`，以及 `账号认证升级与恢复.md:23` 已同步。 |
| M5 删除相关注释 | 已修复 | ① `providers.go:1` 已加上"检查厂商及Key是否携带旧凭据更换发送目标"。<br>② `assignments.go:1` 加上"删除账号时清理角色"；`:30` 加上"删除"。<br>③ `BeforeDelete` 已移到 `BeforeDisable`（`:104-113`）之后（`:115-127`）。<br>④ `session.go:66` 加上"删除"；`model.go:168` 改为"停用或删除"。 |
| M6 README 与接口指南以补充段追加 | 已修复 | ① `identity/README.md`：`:16-17` 表格行已更新，`:27-28` 并入"改之前要知道"，末尾补充段已删除。<br>② `identity/http/README.md:22-25` 表格行已更新，追加句已删除。<br>③ `persistence/README.md:7-8`、`:28` 已并入正文。<br>④ `rbac/README.md`：`:21`、`:28` 表格行已更新，`:45-46` 已补上删除，末尾两段重复说明已删除。<br>⑤ `host/README.md:28` 已更新。<br>⑥ `账号认证接口.md`：`:13` 并入字段语义，`:30` 并入状态码，`:35` 删除说明改为表后段落，`:41` 改为"停用或删除"，`:53` 加上"删除"，"本轮补充"一节已删除。<br>⑦ `账号认证升级与恢复.md:23` 已补上 v2。 |
| M7 产品方案前后矛盾、混入实现细节 | 已修复 | ① `:11` 新增带日期的补充确认。<br>② `:87`"共八项"与 `:163`"最初确认七项，2026-09-17 增加……共八项"口径一致。<br>③ `:106` 首版清单补上"安全"模块。<br>④ `:141-143`、`:152` 已把规则并入第五节。<br>⑤ 末尾的实现补充节已删除，`git grep` 在该文件中找不到 `last_login_at`、`Security.`、`available=` 或"票据"。 |
| M8 凭据测试按评审轮次组织 | 已修复 | ① 两个 `…Boundaries` 函数已删除，`git grep` 无残留。<br>② 用例已按功能并入对应测试：<br>　- 别名与推理配置 → `TestProviderKeyCredentialDestination:110-142`；<br>　- 代理 TLS 与 URL 认证 → `TestProxyCredentialDestination:174-203`；<br>　- OTEL 请求头 → `TestPluginCredentialDestination:281-297`；<br>　- 首次 PUT → `TestPluginCredentialDestinationUpdate:342-357`。<br>③ 拒绝状态统一写作 `fasthttp.StatusForbidden`（`:161`、`:180`、`:197`、`:216`），文件中已没有裸写的 `403`。 |

**上一轮取舍项的后续**
- **S1**：已采纳。`accounts.go:170-172` 在进入事务前执行 `ValidAccountID`，与 `passwords.go:72` 一致。
- **S2**：有依据保留，见 K4。
- **S3**：已采纳。`session.go:10-11` 定义了命名常量 `lastLoginPrecision` 并说明用途，`:86` 引用该常量，业务代码中不再出现驱动名。

## 代码评审「转收敛评审」处置

原文为"无新增。本轮仅核实其转交的九项正确性问题；不替代正在进行的收敛评审。"没有需要处置的条目，也就没有修复、撤回或挂账的事项。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | `dto_test.go` 中的两个键集合测试是同一段断言的两份写法（并入 N1）。其余无：加列已复用上游 helper；精度只有一个命名常量。 | `dto_test.go:11-29`、`:31-50`；`migration.go:68`；`session.go:11` |
| 职责与实现质量 | `dto_test.go` 文件头没有跟上新增的测试（N1）。其余改动（事务前校验、常量、方法换位、测试归组）职责清楚。 | 见出口清单 |
| 架构与项目一致性 | 符合。逐项核对见下文两段。 | 见下文 |
| 契约一致性 | 无新增问题：<br>- 状态响应、账号响应、接口指南、产品方案四处一致；<br>- 删除接口的状态码 404/403/409 与 `accounts.go:170-187` 一致，格式错误按通用 400 处理（`账号认证接口.md:47`）。 | `账号认证接口.md:13`、`:21`、`:30`；`产品方案.md:87`、`:106`、`:141-143` |

### 架构符合性（对照 EE 后端架构）

- **§4 模块内职责：符合。**
  - 格式校验、判权、保护规则和删除编排仍在根包 `identity/accounts.go:169-189`。
  - HTTP 层只调整了 DTO 的位置（`dto.go:126-129`）。
  - 存储层的迁移仍放在 `persistence/migration.go`。
- **§5 模块之间与事务：符合。**
  - `rbac/assignments.go:115-127` 只是换了位置，内容没有变化。
  - 它仍在身份事务中通过 `s.write` 执行，并且只修改 rbac 自己的表（`tx.ReplaceAccountRoles`）。
- **§5 根包只依赖标准库：符合。** `session.go:11` 的新常量只用到 `time`，import 没有变化（`session.go:4-8`）。
- **§7 按缺口扩展、复用完整操作：符合。** 加列改用上游的 `migrator.AddColumnIfNotExists`（`migration.go:68`），迁移仍经 `upstream.RunSingleMigration`（`:66`），没有另起迁移机制。
- **§3 app 只做装配、§6 插件：不适用。** 本轮差量没有涉及 app 和插件。

### 依赖关系

- **import 的变化**：
  - 生产代码的 import 没有变化，`migration.go:7-10` 原本就导入了 `framework/migrator`。
  - 两个测试文件只调整了分组顺序，没有新增依赖。
- **反向依赖**：identity 各包仍不导入 rbac。本轮 identity 侧的 import 没有变化；rbac 通过 identity 定义的 `AccountPolicy` 接缝接入，关系不变。
- **窄接口与显式注入**：没有新增接口或构造参数。`policyFactory` 的注入方式不变（`persistence/store.go:265-273`）。
- **资源所有权**：没有新增共享资源，迁移使用宿主传入的连接。

## 出口清单

### 本轮修复项

**N1 `dto_test.go` 文件头没有跟上新增的状态响应测试，两个键集合测试重复**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/http/dto_test.go:1`、`:11-29`、`:31-50`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/http/README.md:25`
- **代码事实**：
  - 文件头只写了"钉住账号响应的 JSON 键集合"，而本轮新增的 `TestStatusResponseKeys` 钉住的是状态响应；README 第 25 行已改为"账号与状态响应键集合"。
  - 两个测试执行的是同一组步骤：序列化 → 解码成 map → 比较键数 → 逐键检查。
  - 两份写法的变量名和失败信息不同：一份用 `b/got/k` 加 `Fatalf`，另一份用 `data/fields/name` 加 `Fatal`。
- **维护影响**：
  - 文件头与 README 不一致，而编码规范把与现状不符的注释视为缺陷；
  - 以后再钉第三个响应形状时，大概率会再复制一份。
- **最小修正**：
  - 文件头改为"钉住账号与状态响应的 JSON 键集合"；
  - 两个用例改为一个表驱动测试（元素为 `{名称, 值, 期望键}`），或抽出包内 helper，共用一套断言。

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [角色权限后端]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- `accounts.go:170-172`：格式校验移到判权之前后，已登录但没有 Users.Manage 的调用方传入非法 ID 会得到 400 而不是 403，需核实是否符合预期的信息暴露口径。
- `dto_test.go:32-50`：该测试只序列化 `statusResponse{}` 的零值，没有经过 `/api/identity/status` 及旧 `/api/session/is-auth-enabled` 的实际处理，需核实能否防止回归。

### 看似可改但有理由保留

- **K1 `lastLoginPrecision` 定义在 `session.go:11`，没有放进 `model.go` 的常量块**：该常量只在 `session.go:86` 使用；根包已有就近定义常量的先例（`access.go:12`）；接口契约"精度为微秒"已写在 `账号认证接口.md:13`。
- **K2 `AddColumnIfNotExists` 传入 nil logger（`migration.go:68`）**：身份存储约定 SQL 日志静默，故障只记诊断字段（`persistence/README.md:3`）。上游传 logger 是 configstore 包自己的约定（`configstore/migrations.go:512`），不是 helper 的要求（`addcolumn.go:30-32` 明确允许传 nil）。
- **K3 `rbac/README.md:34` 只列出 `persistence/accounts_test.go` 一个存储测试**：`identity/persistence/README.md:8` 跨模块指向这个文件，需要在表中能找到；其余存储测试由 `rbac/README.md:50` 的通配说明覆盖。
- **K4 `TestLastLoginAtomicity` 保留在 rbac/persistence（上一轮 S2）**：
  - `persistence/store.go:265-273` 表明每个身份事务（包括登录）都会调用 `policyFactory` 构造 rbac 策略；
  - "装配了 rbac 事务策略后，登录仍原子提交"只有在装配后的夹具中才能验证；
  - 测试注释（`accounts_test.go:90`）已写明这一目的，满足上一轮给出的反证条件；
  - `identity/persistence/README.md:8` 也有指引。
- **K5 产品方案的新文字使用全角标点，并保留"本轮"**：
  - 新增内容都带有日期（`:11`、`:129`、`:163`）；
  - 原文多处以同样方式使用"本轮"（`:3`、`:135`、`:145`、`:165`）；
  - 标点混用不影响规则理解，没有维护代价。
- **K6 "新库的 v1 已含该列"在三处陈述**（`migration.go:67`、`persistence/README.md:7`、`账号认证升级与恢复.md:23`）：三处分别面向改代码的人、包维护者和运维，都是说明文字，不是规则的多份实现。
- **K7 `credentials_test.go` 仍有 358 行**：方案v1 第 94 行和第 98 行已做评估；按功能归组后，文件内都是同一类配置授权测试，没有超过 500 行的强制拆分线。
- **K8 "alias inference profile"子用例（`credentials_test.go:128-142`）没有并入上面的字段循环（`:99-109`）**：前者断言"允许"与"拒绝"两个分支，后者只断言"拒绝"，两者断言形状不同。
- 上一轮的 K-a 至 K-g 仍然成立，更早的 K1–K11 也仍然成立。

## 台账对账

- **FIND-017**：本轮没有新增 busy 重试的副本，未恶化。
- **FIND-018**：本轮差量没有改变登录事务内的写入次数，`session.go:86-90` 只是把字面量换成常量，未恶化。
- **FIND-021**：`lastLoginPrecision` 只做截断，不读取时钟；登录时间仍取自 `v.CreatedAt`，未恶化。
- **FIND-022**：本轮没有涉及 rbac/host 的错误转换，未恶化。
- **FIND-012**：本轮 app 没有改动。
- 其余 Finding 与本轮无关。

---

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无：微秒精度只在 `session.go:11` 命名，接口指南中的是契约陈述；拒绝状态码已统一为 fasthttp 常量 |
| 重复 | 同一规则多份实现且互不注明 | 无：删除保护按模块归属分两处，`assignments.go:30` 的注释已包含删除 |
| 重复 | 重造已有 helper | 无：`migration.go:68` 已改用上游 helper（M4 已关闭）；测试断言重复并入 N1 |
| 重复 | 派生常量无推导 | 无 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无：精度已命名为 `lastLoginPrecision` |
| 职责与实现质量 | 状态码、错误码裸数字 | 无：`credentials_test.go` 已统一 |
| 职责与实现质量 | 无名的错误形状（以元组表示失败） | 无 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无：`alias names` 中的 `decode` 闭包只在当次循环内使用 `name`，没有跨迭代的状态 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无：凭据测试已按功能归组（K7）；dto 两个键集合测试放在同一文件是合理的 |
| 职责与实现质量 | 多余状态或守卫 | 无：手写的 HasColumn 守卫已移除；DeleteAccount 的格式校验只在事务前执行一次（事务内那次已删除） |
| 职责与实现质量 | 同一效果做两次 | 无 |
| 职责与实现质量 | 注释与现状矛盾 | 发现：N1（`dto_test.go:1`） |
| 架构与项目一致性 | 接缝形状与同类不同 | 一致：`BeforeDelete` 与 `BeforeDisable` 相邻且同形；DeleteAccount 在事务前校验格式，与 ResetPassword（`passwords.go:72`）一致 |
| 架构与项目一致性 | 架构符合性 | 符合：§4、§5、§7 见正文；§3、§6 不适用（本轮没有 app 和插件改动） |
| 架构与项目一致性 | 依赖关系 | 符合：生产代码的 import 没有变化；identity 不导入 rbac；注入方式不变 |
| 架构与项目一致性 | 绕过既有出口 | 无：迁移经 `RunSingleMigration` 和上游加列 helper |
| 架构与项目一致性 | 把上游现状当契约 | 无：helper 的幂等行为已对照 `addcolumn.go:33-62` 核实 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无：DTO、接口指南、产品方案已对齐；前端不在范围 |
| 契约一致性 | 状态码口径不一 | 无：删除接口的 404/403/409 已写入表格行，400 按通用规则处理 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无：`last_login_at` 的 null 与时间值语义写在 `账号认证接口.md:13`；状态响应不再包含该键 |

### 实际静态阅读范围与未执行项

- **已读**：见「事实来源」。
  - 差量内 18 个 ee/product 文件，全部读了当前完整现状；
  - `persistence/store.go` 读第 150–279 行，`passwords.go` 读第 60–89 行，`routes.txt` 读第 670–689 行，`addcolumn.go` 读全文。
- **开放式复读**：已完成，以"半年后要改这里的人"的视角逐文件复读。
  - 表外发现：N1 中"两个键集合测试写法不同"一项，已并入 N1；
  - 另外复读了产品方案的标点和"本轮"用词、rbac README 的测试行、凭据测试的子用例形状，结论分别记为 K5、K3、K8，不构成问题。
- **未执行**：
  - 没有运行任何测试、构建、vet、goimports 或冒烟（只读约束）；M3 的结论基于阅读 import 块和路径排序得出。
  - 没有读旧代码评审的正文，只使用了提示中转交的一条原文。
  - 没有读 `workbench/evidence/` 下的本轮验证记录。
  - 没有通读差量以外的 `identity/http/accounts.go`、`identity/access.go`、`rbac/identity/policy.go`：本轮这些文件没有改动，上一轮已核对。
  - 没有核对 `TestStatusResponseKeys` 以外的状态接口测试（属代码评审范围，已转交）。
