---
title: "角色权限后端 期4 代码评审 #9"
type: 代码评审
case: 角色权限后端
phase: 4
round: 9
verdict: 通过
created: 2026-09-18
---

> 独立正式差量复核。候选 `4e5b835a6c0fd5eb5e0897f24852bf750845c83b`，上一候选 `de704b4c0575b88aace6f90aee6cc63cefa01801`，总基线 `f371548b5444693d7e259033bec9f4a467e3748b`。开始及测试完成时树干净；本报告随后新增。未修改生产代码或提交。
> 核对全部差量、收敛评审8第一条转交及其引用的前轮两条。未读取本轮收敛评审9；仅进行静态防御核查和普通合成配置测试。

## 结论

**通过本轮代码差量复核。** 删除独立Username在Password为空时的凭据分支符合宿主行为；URL内用户名认证、独立密码、引用和环境身份仍受原保护，R9保持关闭。未发现新增本轮修复项。

定向race、独立URL用户名回归、host vet及期4预飞通过。结论限于本轮代码复核，不替代独立收敛结果或整体验收Gate。

## 对照方案核查

- 差量11文件、427行增加、3行删除，主要为前轮报告与过程文档。唯一生产变化是`providers.go`删除独立用户名else分支并说明原因；测试增加`unused independent username`。
- 方案「第八轮评审整理」与实际差量一致，没有新增业务、接口、依赖、迁移或部署要求。
- 本轮不是完全行为等价：旧配置只有独立用户名、没有密码，且新URL认证恰好与之同值时，不再因该未使用的旧字段要求敏感权限。此变化是明确登记的小修。
- HTTP和SOCKS5宿主路径均只在独立用户名/密码同时非空时才使用这组认证；WebSocket路径也是相同条件。全局代理的独立字段处理同样如此。独立用户名与URL的userinfo字段不能混为一种实际发送来源。
- `proxyURLCredential`未改，仍在URL没有非空密码时提取用户名；本轮删除分支不影响它。厂商已保存URL认证、独立密码、引用及环境模式标记的收集顺序和拒绝条件保持。

## 接口面对账

- 无新增对外符号、端点、字段或状态码。只减少一个不对应宿主实际认证的保守匹配来源。
- 厂商模块管理权限、保存前敏感权限回调、原值恢复及错误映射均未改。新测试走原纯函数，并同时核对有权限允许分支。

## 预飞事实表

独立执行`python3 workbench/tools/preflight.py --config workbench/cases/角色权限后端/期4/checklist.yaml --repo . --out /tmp/rbac-review9-preflight.md`，范围、空白、gofmt、source-route-contract、生成一致性、产物及必含检查**全部通过**；`git diff --check de704b4c0..4e5b835a6`通过。

结果绑定固定候选；报告新增后的导航生成留实施方统一处理。

## 可证伪质疑

### 收敛评审8转交处置

| 条目 | 结论与证据 |
|---|---|
| 独立Username无Password仍被计为凭据，跨来源同值导致额外判权 | 已关闭。候选[providers.go:146](/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:146)仅收集独立密码，URL认证继续另行处理。上游`core/providers/utils/utils.go:665-676`、`:692-703`、`:767-771`的独立字段均要求用户名和密码同时有值。新增`unused independent username`精确覆盖原转交组合，定向race通过。另在scratch补标准/无scheme URL仅用户名、改变CA的普通配置断言：缺少敏感权限仍拒绝，授权后允许，确认未误删真正的URL认证保护。 |
| 引用收敛7第1条：全局URL由旧侧决定比较变为逐侧提取 | 已在[代码评审8](代码评审8.md)「收敛评审7三项转交」第1行处置。本轮`credentials.go`和`settings.go`无变化。精确证据绑定`de704b4c0575b88aace6f90aee6cc63cefa01801`：overlay的`username to new password`允许、`password reused as username`拒绝，均有权允许；见下文引用路径。本轮不重跑、不写成新实测。 |
| 引用收敛7第3条：厂商代理跨来源匹配 | 已在代码评审8同表第3行处置。旧独立密码与新URL密码同值仍按保留旧认证值处理；本轮独立Password、URL提取及retainedValues未改，删除的只是无Password时的独立Username。前轮`provider cross-source match`合成断言仍作为该未变路径证据，不将其泛化为外部密钥后端测试。 |

引用的第八轮scratch：`/var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/rbac-review8-up8e8fae/overlay.json`，执行结果1.095s。收敛7第2条“两来源从严口径”在第八轮已经统一，本轮没有再次分叉。

### R9及其余边界

- R9保持关闭。定向`TestProviderProxyCredentialDestination`重新验证被覆盖URL、无scheme完整独立字段、Env暂存URL、引用/CA、坏旧配置恢复及真正删除/替换旧认证后的允许分支。
- 本轮未新增失败路径：解析失败、原值恢复错误和授权拒绝继续走原出口，未发现吞错或保存后才判断。
- 无新增需用户裁决项；无未关闭的本轮代码修复项。

## 挂账(Finding:范围内、裁决本轮不修、需登记 findings.md)

- 无新增。既有登录容量、时钟、诊断台账均未受本差量影响。

## 转收敛评审

- 无新增。N1删除分支的实际行为已核对，组织与注释结论由收敛侧独立给出。

## 验证记录

| 本轮实际命令 | 结果 |
|---|---|
| status、HEAD、stat、全部差量及差量diff --check | 测试前后树干净，HEAD为4e5b835a6；11文件纳入 |
| 上述期4预飞 | 全绿；`/tmp/rbac-review9-preflight.md` |
| EE：`GOWORK=off go test -race -count=1 ./internal/rbac/host -run '^TestProviderProxyCredentialDestination$'` | 通过，1.104s |
| EE：`GOWORK=off go vet ./internal/rbac/host` | 退出0，无诊断 |
| EE：`GOWORK=off go test -race -count=1 -overlay /var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/rbac-review9-z2a700lq/overlay.json ./internal/rbac/host -run '^TestReview9URLUsernameStillProtected$'` | 通过，1.119s |

overlay只在scratch追加标准/无scheme URL用户名认证的纯配置断言，未修改仓库测试或生产代码。所有测试已完成，没有后台待跑项；没有网络请求或真实凭据读取。

### 起手表覆盖

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准未覆盖 | 无新增；独立用户名与URL用户名分别验证 |
| 对照方案 | 方案/实施/diff不一致 | 无 |
| 对照方案 | 多出功能/接口/依赖 | 无 |
| 对照方案 | 未登记偏差 | 无；行为放宽已登记 |
| 行为正确 | 吞错/假成功 | 无新增 |
| 行为正确 | 空/非空同路 | 无；删除无密码独立用户名分支，保留URL分支 |
| 行为正确 | 超时取消仍副作用 | 无新增；纯计算 |
| 行为正确 | 重发双份副作用 | 不适用：未新增副作用 |
| 行为正确 | 事务外写读/多方回滚 | 不适用：未改持久化 |
| 行为正确 | 开关/旧数据/旧客户端 | 无；Env暂存URL与旧配置回归保持 |
| 行为正确 | 校验次序/缺层 | 无；原保存前检查未变 |
| 行为正确 | 路由保护 | 无；模块与敏感权限接入保持 |
| 行为正确 | 敏感日志/响应 | 无新增输出 |
| 行为正确 | 热路径I/O/无界增长 | 无 |
| 行为正确 | 配置/部署依赖未登记 | 无 |
| 失败可观测 | 无法追溯原因 | 无新增错误路径 |
| 失败可观测 | 泄露内部错误 | 无新增错误内容 |
| 失败可观测 | 清理失败现场 | 无；scratch与预飞保留 |
| 测试可信 | 新规则无测试 | 无；新增允许与独立保留保护断言通过 |
| 测试可信 | 断言只跟实现 | 无；已核宿主不发送独立用户名的事实 |
| 测试可信 | 失败难定位 | 无；新增用例按行为命名 |
| 测试可信 | 返回值未检查 | 无新增 |
| 测试可信 | 个人环境fixture | 无新增；合成配置不依赖服务 |
| 测试可信 | 替代结果冒充真实 | 无；明确本轮/前轮、纯配置/宿主静态边界 |
| 测试可信 | 整体替换模块mock | 无新增 |
| 残留 | 调试/后门/本机配置 | 无 |
| 残留 | 未使用代码/无关改动 | 无 |
| 残留 | 生成物漂移 | 固定候选预飞通过 |

### 阅读范围、未执行项与开放式复读

- 完整读取当前providers.go和受影响凭据测试，核对全部差量；复读`proxyURLCredential`及上游HTTP/SOCKS5/WebSocket独立字段条件、全局buildProxyURLWithAuth。方案与验证增量、收敛8原文及代码8精确证据均已对账；生成视图按diff及预飞核实。
- 未重跑host全包race、全EE、全上游handlers、构建、完整HTTP/WS、数据库升级回退：本轮仅删除一个局部来源，已按要求执行相关代理测试和预飞；其他路径无生产变化。实施方de704b4c0的SQLite冒烟不列为本轮亲测。
- 未访问已有实例，未进行攻击、凭据提取、外部代理/厂商投递、前端或容量测试。没有把静态行为判断写成网络实验结论。
- 已做开放式复读，重点确认删除独立用户名后不会误删URL用户名保护、R9两种保存来源仍存在且普通新配置可用；无表外新增发现。
