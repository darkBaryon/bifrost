---
title: 角色权限后端 期4 收敛评审 #2
type: 收敛评审
case: 角色权限后端
phase: 4
round: 2
verdict: 有Blocking
created: 2026-09-17
---

> 本轮评审由启动脚本在新会话中执行，与实施上下文隔离。基线 `f371548b5444693d7e259033bec9f4a467e3748b`，交付物为 `983d1ecb48dd250438e54ffbd4950f6649c687b6`。
> 本轮是差量复核，上一轮候选为 `cbc94b031`，范围是 `git diff cbc94b031..983d1ecb4`。
> 工作树状态：`git status --porcelain=v1 --untracked-files=all` 输出为空，没有未暂存或未跟踪的文件。
> 差量中的 ee、transports、product 文件与指定清单一致。workbench 下的文件属于过程文档，不在评审范围内，只作为裁决依据读取。
> 风险等级 L3，逐维留痕。

## 结论

**上一轮的修复情况**
- R1–R12 均已修复或有依据撤回，R8 上一轮已撤回。
- S1、S2、S3、S5 已采纳。
- 本轮新增的敏感权限 `Security.ChangeCredentialDestination` 在架构上符合要求：
  - 规则留在 `rbac/host`；
  - 上游只增加可选回调；
  - 依赖方向没有变化。

**仍不能按"无碍"交付，原因有两类**

1. 新增的凭据目标检查代码留下了一些局部问题，需要本轮修复（N1–N9）：
   - 同一集合与自身比较（N1）；
   - 已经失效的跳过判权表（N2）；
   - 一个函数做两件事，注释没有跟上（N3、N4）；
   - 大小写不敏感比较没有交代原因（N5）；
   - 文件名带下划线（N6）；
   - 新权限没有前端开关映射，契约也没有说明（N7）；
   - 上游接缝文件内中英文注释混用（N8）；
   - 手抄的上限数值（N9）。
2. 代码评审转来的范围问题需要用户裁决（B1）：
   - 账号认证案把"账号删除"明确转给了角色权限案，但本案没有交付，也没有裁决记录；
   - 产品方案中的三项敏感权限（查看厂商密钥完整值、修改登录认证设置、导入配置）在目录里存在，但标为不可用，同样没有裁决记录。

## 事实来源

**执行的命令**
- `git status --porcelain=v1 --untracked-files=all`
- `git diff --stat cbc94b031..983d1ecb4`
- 分别对以下路径执行 `git diff cbc94b031..983d1ecb4`：
  - `ee/internal/host`、`ee/internal/app`、`ee/internal/identity`、`ee/Makefile`、`ee/README.md`
  - `ee/internal/rbac`
  - `transports`、`product/上游改动/README.md`、`product/架构/EE后端架构.md`
  - `workbench/findings.md`
  - `ee/scripts/rbac-sensitive-smoke.py`、`ee/scripts/rbac-smoke.py`
- `git log -3 -- ee/internal/rbac/host/plugins.go`
- `git show --stat 1d89df7d4`、`git show --stat 983d1ecb4`
- 以下文件的 `git grep`：reports、findings、期4方案、期3方案、期1方案v7

**先读的规则与裁决**
- 根 AGENTS.md、workbench/AGENTS.md
- 收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md
- product/架构/EE后端架构.md、templates/收敛评审.md
- workbench/findings.md
- 期4方案v1 全文，包括「Gate 决策结果」「实施偏差记录」「首轮评审后的修正范围」「Gate 决策追加」
- 开发蓝图
- 上一轮记录：收敛评审1

**完整阅读的范围内文件**
- ee/internal/host：auth.go、rbac_test.go、README.md
- ee/internal/identity：README.md、access.go 前 75 行；model.go、http/protocol.go、persistence/store.go 按 diff 阅读，并用 Grep 核对调用点
- ee/internal/rbac/host：credentials.go、plugin_credentials.go、credentials_test.go、providers.go、settings.go、webhooks.go、plugins.go、request.go、routes.go、notifications.go、authorization.go、README.md
- ee/internal/rbac/http：handler.go、protocol.go、matrix.go（matrix.go 不在差量内，但受新权限影响）
- ee/internal/rbac：permissions.go、README.md（diff）、rbac/identity/policy.go 第 55–104 行
- ee/docs：角色权限接口.md、账号认证接口.md
- 上游：transports/bifrost-http/handlers/consolepolicy.go 全文、providers.go 第 420–569 行、product/上游改动/README.md 全文
- 以下文件按 diff 阅读：authorization_test.go、routes_test.go、roles_test.go、service_test.go、consolepolicy_test.go、mcp.go、notifications.go、provider_keys.go、两份冒烟脚本、Makefile、ee/README.md、bootstrap.go、EE后端架构.md

**对照的同类实现与上游证据**
- `core/schemas/secretvar.go:237-243`、`:293-300`：`FullyRedacted` 输出 `<REDACTED>`，`IsRedacted` 按大小写不敏感方式识别占位文字（N5 的依据）。
- `core/schemas/account.go`：Key 配置中与目标有关的 JSON 字段，如 `use_anthropic_endpoints`、`use_deployments_endpoint`、`force_single_region`、`endpoint`、`region`、`url`、`workspace_url`、`github_domain`。
- `transports/.../providers.go:440-442`：`oldConfigRaw` 在调用策略前一定不是 nil。
- `plugins_projection_test.go:62`：插件的 SecretVar 占位文字是大写形式。
- `settings_contract_test.go:93`：设置可选字段与上游核对的先例。
- `permissions_test.go:13`：`failingConsoleAccess` 夹具。
- `identity/passwords.go:72`、`core.go:200`：身份模块内部直接使用 `uuidPattern`。
- 产品方案 `product/需求/角色权限/产品方案.md` 第 60–166 行。
- 账号认证方案 `workbench/cases/账号认证/期1/方案v2.md:65`。

## 上一轮「本轮修复项」处置

| 编号 | 处置 | 证据 |
|---|---|---|
| R1 恢复锚点兜底不可达 | 已修复 | `requireHostAdministrator` 已删除。`auth.go:302`、`:331` 在未注入 access 时改走 `RequireAccountManager`。`WithRecoveryAnchor` 注释已收窄为"供 /api/config…不参与管理接口授权"（`auth.go:74-75`），`a.anchor` 只剩 `config.go:50` 一个使用方。`rbac_test.go` 改为测试"存储失败不回退临时令牌"，使用 `WithConsoleAccess(failingConsoleAccess…)`。 |
| R2 身份 README 的 WS 描述 | 已修复 | `identity/README.md:7` 现在写 Notifications.View 并排除强制改密，与 `policy.go:69-70`、`core.go:172` 一致。 |
| R3 接口指南 | 已修复 | `账号认证接口.md:27-32`、`:54` 改为按权限描述。新增 `ee/docs/角色权限接口.md`，覆盖字段、三态、405、`account_count`，并说明 FIND-022。`rbac/README.md:50` 已加入指向链接。 |
| R4 架构文档同步 | 已修复 | `EE后端架构.md` 已同步：`:5`、`:22` 交付状态；`:53` 现状；`:69` 表格新增 `host/` 行；`:78` 依赖图；`:115` 归位判定写入 `<模块>/host/` 规则，并注明依据蓝图。 |
| R5 设置权限表矛盾 | 已修复（矛盾消除） | `settings.go:108-109` 改为 `{}` 并加注释。但另一张表 `clientIdentityFields` 已变成无效状态，本轮另列 N2。 |
| R6 跨模块重复规则 | 已修复 | 三条身份侧规则已改为引用身份模块的导出项：<br>• 唯一键冲突：`identity/persistence/store.go:53-59` 导出 `IsUniqueConflict`，`rbac/persistence/store.go:80` 引用；<br>• 请求体上限：`identity/http/protocol.go:26-27` 导出 `MaxBodyBytes`，`rbac/http/protocol.go:22` 引用；<br>• 账号 ID 格式：`identity/model.go:104-105` 导出 `ValidAccountID`，`rbac/http/protocol.go:82` 引用。<br>残留问题是注释里仍手写"16 KiB"，见 N9。 |
| R7 `Prepare` 重写 helper | 已修复 | `authorization.go:53-55` 改为调用 `requirePermissions`。 |
| R8 | 上一轮已撤回，维持撤回 | — |
| R9 `ConsolePolicyError` 字段与裸状态码 | 已修复 | `consolepolicy.go:79-81` 只保留 `Status` 字段，`:85-98` 改用 `fasthttp.Status*` 常量。 |
| R10 不可达守卫与过期注释 | 已修复 | `notifications.go` 的 `present && policy == nil` 分支已删除；`create` 注释（`:152-154`）已改为"注入策略时由策略判定"。 |
| R11 导出符号注释 | 已修复 | `access.go:31-38`、`:44`、`:62-71`，`policy.go:58`、`:77`、`:88`、`:96`。 |
| R12 import 分组 | 已修复 | `request.go:4-18`、`plugins.go:4-23`、`routes_test.go:4-15` 已按 goimports 分组；`consolepolicy.go`、`consolepolicy_test.go` 也一并调整。 |

**上一轮取舍项的后续**
- S1：已采纳，两份阶段脚本已删除。
- S2：已采纳，`GuardMethods` 已删除；`RootGuard` 列出全部允许的方法，并补上 `Cache-Control`（`routes.go:234-245`）。
- S3：已采纳，`notifications.go:47-54`，上游接口注释说明了前置校验（`consolepolicy.go:31`）。
- S5：已采纳，改用 `RBAC_TEST_POSTGRES_DSN`，并接入 Makefile 与 README。
- S4：只部分处理。`rbac/host/README.md:41` 补充了"清单只选策略，分支在功能文件"的说明；枚举仍在两处重复列出，没有采纳也没有立 REF，见本轮 S4'。

## 代码评审「转收敛评审」处置

1. **`identity/README.md:9` 的 WS 票据描述**：有依据撤回。R2 已在 `:7` 修正为 Notifications.View 并排除强制改密，与 `rbac/identity/policy.go:69-70`、`identity/core.go:172` 一致。当前 `:9` 是标题"## 代码在哪"，没有过期描述。
2. **账号删除与三个 `Available=false` 项的验收口径**：转为需用户裁决的 Blocking（B1），理由与证据见下文。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | ① 插件名单在三处出现：`plugins.go:309` 另写 `otel/telemetry` 名单，与 `plugin_credentials.go` 的两个 switch 对应（N3）。<br>② 设置字段的"不判权"在两张表各表达一次（N2）。<br>③ 权限总数 30 在 Go 测试和脚本中多处手写（S-a）。<br>④ 路由清单的三组枚举仍列两遍（S4'）。<br>⑤ `secretText` 是已有 helper 的一行包装（S-c）。 | 见出口清单 |
| 职责与实现质量 | ① 同一集合与自身比较（N1）。<br>② `restorePlugin`、`proxyUpdate`、`webhookUpdate` 新增了目标检查，注释和函数名没有同步（N3）。<br>③ `configSecretText` 注释与实际调用方不符；包 README 声称"测试会发现遗漏"，但 Key 字段靠名称列表匹配，这个说法不成立（N4）。<br>④ 大小写不敏感比较没有交代原因（N5）。<br>⑤ 文件名带下划线（N6）。<br>⑥ 注释中手抄的上限数值（N9）。 | 见出口清单 |
| 架构与项目一致性 | 符合。架构符合性和依赖关系的逐项核对见下文两段。<br>① 上游接缝文件内中英文注释混用，其中一句与 typed-nil 失败即拒绝的行为相矛盾（N8）。<br>② `keyConnection` 把上游 Key 的 JSON 字段名当作不受校验的契约（并入 N4，正确性部分已转代码评审）。 | 见下文 |
| 契约一致性 | ① 新权限在 `matrix` 中没有映射，接口指南也没有说明（N7）。<br>② 产品方案与交付声明在账号删除、三项预留敏感权限上缺少裁决记录（B1）。 | 见出口清单 |

### 架构符合性（对照 EE 后端架构）

- **§4、§7 适配归位：符合。**
  - 新增的目标比较集中在 `rbac/host/credentials.go` 和 `plugin_credentials.go`。
  - 这两个文件使用 `schemas`、`configstore`、`tables`、otel/telemetry 插件类型，都是宿主对象，放在 `<模块>/host/` 与 `EE后端架构.md:115` 的规定一致。
  - rbac 根包只新增了常量：`permissions.go:19`、`:89-91`、`:149-150`，仍只依赖标准库。
- **§7 复用完整操作、按缺口扩展：符合。**
  - 上游新增了三个可选回调（`consolepolicy.go:42-52`），调用点都在"完整合并之后、副作用之前"：
    - `providers.go:552-559`
    - `provider_keys.go` 的 create 与 update
    - `mcp.go:2664-2679`
  - 没有注入回调时维持原流程：`mcp.go:2645` 仅在没有策略时保留原来的"必须重填 secret"限制。
  - 这些改动已登记在 `product/上游改动/README.md:157-159`，方案 `期4/方案v1.md:64` 列出了白名单。
- **§5 业务与入口分工：符合。** 判权规则留在 EE，上游只负责传递快照和输出固定错误（`consolepolicy.go:84-102`）。rbac 根包没有引入 HTTP 或 GORM。
- **§3 app 只做装配：符合。** `bootstrap.go:46` 只剩 `RootGuard` 一层包装，比上一轮少一层。
- **§1 共享 Server、Handler：符合。** 仍只有一个外层包装，没有另起一套基础设施。
- **§6 插件：不适用。** 本轮没有新增插件入口，只是在管理接口中读取插件配置类型。

### 依赖关系

- **import 的变化**
  - `rbac/http/protocol.go:13` 新增对 identity 根包的依赖，用于 `ValidAccountID`；`handler.go:8` 本来已导入 identity 根包。
  - `rbac/persistence/store.go:9` 新增对 identity/persistence 的依赖（别名 `authstore`）；`migration.go:9` 本来已使用同一别名。
  - `rbac/host` 新增对 `plugins/otel`、`plugins/telemetry` 的依赖，`plugins.go` 本来已导入这两个包。
  - 以上都没有新增依赖方向，也没有形成反向或双向依赖。identity 各包仍不导入 rbac（Grep 核对）。
- **窄接口与显式注入**
  - `internal/host` 仍只通过 `ConsoleAccess` 使用角色能力（`auth.go:50-57`）。
  - rbac/host 的新回调通过请求上下文键注入（`request.go:144-159`），与已有的 `ConsoleSettingsUpdatePolicy` 等形状相同。
  - 没有使用全局容器。
- **导出面的层次正确**
  - 驱动错误识别放在 persistence：`IsUniqueConflict`。
  - 格式规则放在根包：`ValidAccountID`。
  - HTTP 上限放在 http：`MaxBodyBytes`。
- **跨模块数据与资源**
  - 本轮没有新增跨模块写表。
  - 上游策略收到的配置快照在接口注释中约定为只读（`consolepolicy.go:43`、`:51`）；是否真的没有别名共享，已转代码评审。

## 出口清单

### 本轮修复项

**N1 厂商级请求头判定写成同一集合与自身比较**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:148-150`
- **代码事实**：`retainedValues(headerValues(provider.NetworkConfig.ExtraHeaders), headerValues(provider.NetworkConfig.ExtraHeaders))` 把同一份值与自身比较，实际含义只是"存在非空请求头值"。
- **维护影响**：读者会以为这里比较的是新旧两份配置，需要推演才能看出是存在性判断；以后如果改成真正的新旧比较，也容易误以为原来就是这样。
- **最小修正**：
  - 写成直接的存在性判断，例如 `slices.ContainsFunc(headerValues(...), func(v string) bool { return v != "" })`，或在 `credentials.go` 增加 `hasValue`；
  - 注释写明"继承的厂商级请求头会随新 Key 发送，视为保留的凭据"。

**N2 `clientIdentityFields` 已成为无效状态**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/settings.go:19-20`、`:64-66`
- **代码事实**：这张表里的三个字段（`enforce_governance_header`、`enforce_scim_auth`、`whitelisted_routes`）在 `clientPermissions` 中都是 `{}`（`:108-109`、`:124`），所以第 64 行的 `continue` 跳过的是一个空循环，对行为没有影响；这张表也没有其他使用方（Grep 核对）。
- **维护影响**：两张表仍在描述同一组字段。以后给其中某个字段加权限时，改了 `clientPermissions` 也会被 `clientIdentityFields` 悄悄跳过，这正是上一轮 R5 指出的风险。
- **最小修正**：
  - 删除 `clientIdentityFields` 及其 `continue`；
  - 把第 19 行"由认证适配负责"的说明并入 `:108-109`、`:124` 的行尾注释。

**N3 目标检查并入三个函数后，函数名、注释和插件名单没有跟上**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/plugins.go:289`、`:308-311`、`:349-351`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/settings.go:1`、`:144`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/webhooks.go:50`、`:84-89`
- **代码事实**：
  - `restorePlugin` 的注释和函数名只说"恢复隐藏字段"。实际上它还负责凭据目标判权（`:349`）；对 otel/telemetry，即使请求里没有隐藏标记，也会读取旧配置（`:309`）。
  - `:309` 手写的 `"otel"`、`"telemetry"` 名单，与 `plugin_credentials.go:20-35`、`:55-107` 两个 switch 所覆盖的插件是同一份知识，但彼此没有注明。
  - `proxyUpdate`（`settings.go:144`）和 `webhookUpdate`（`webhooks.go:50`）的注释只写了恢复隐藏地址，没有提到目标判权；`settings.go:1` 的文件说明也是如此。
  - `webhooks.go:84-89` 用 if 包一层再单独 `return nil`，中间还有空行；同一轮的 `settings.go:173` 直接写 `return consoleError(credentialDestination(...))`，两处写法不一致。
- **维护影响**：
  - 以后给 maxim 等插件增加目标检查时，只改 `plugin_credentials.go` 会被 `:309` 的提前返回挡住，而且没有任何提示；
  - 看到 `restorePlugin` 这个名字的人，不会想到它会拒绝敏感操作。
- **最小修正**：
  - `plugin_credentials.go` 提供 `hasDestinationCheck(name)`（或包级名单），供 `:309` 与两个 switch 共用；
  - `restorePlugin` 改名（例如 `preparePluginUpdate`），或至少在注释写明"恢复隐藏字段，并检查携带旧凭据更换上报目标"；
  - 同步更新 `settings.go:1`、`:144` 和 `webhooks.go:50` 的注释；
  - `webhooks.go:84-89` 与 `settings.go:173` 统一写法。

**N4 `credentials.go` 的注释与现状不符；包 README 的"测试会发现遗漏"对 Key 字段不成立**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials.go:115`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials.go:53-54`、`:75`、`:83`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/README.md:47`
- **代码事实**：
  - `configSecretText` 的注释写"用于插件的存储JSON和API中的SecretVar"，但唯一的调用方是 `keyConnection`（`:76`、`:84`，Grep 核对），插件路径没有使用它。
  - `keyConnection` 按两份字段名列表识别目标和凭据，不在列表里的字段一律当作普通字段忽略。
  - README 第 47 行写"新内置插件或新配置字段需要补上权限规则；对应检查和测试会发现遗漏"。设置字段有 `clientPermissions` 查表并在未知字段时失败即拒绝（`settings.go:56-59`），插件有目录核对（`routes.go:178-186`）；Key 字段没有同类核对。
- **维护影响**：上游给 Key 新增目标类字段后，维护者会按 README 以为测试能发现，实际不会，因为上游 Key 的字段名被当成了不受校验的契约。
- **最小修正**：
  - 更正 `:115` 的注释；
  - README 第 47 行写明 Key 字段靠 `keyConnection` 的名称列表识别，上游升级时需要人工核对；
  - 或者仿照 `settings_contract_test.go` 增加 `schemas.Key` 的 JSON 字段清单核对，让这句话成立。
  - 名单本身是否完整，已转代码评审。

**N5 插件占位文字按大小写不敏感比较，没有交代原因**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/plugins.go:226-231`、`:249`、`:258`
- **代码事实**：
  - 这三处改为 `strings.EqualFold(…, redacted)`，而同包的 `providers.go:39`、`:50`，`settings.go:156`、`:166`、`:178`，`webhooks.go:66`、`:78` 仍用 `==`。
  - 原因在上游：插件的 SecretVar 经 `FullyRedacted` 输出的是 `<REDACTED>`（`core/schemas/secretvar.go:300`），而 EE 自己的占位文字是小写 `<redacted>`（`response.go:135`）。
- **维护影响**：同一包里"是否为占位文字"有两种判法却没有说明原因。维护者可能把某一处"统一"成另一种，导致插件写回或厂商写回出错。
- **最小修正**：在 `hasMarker` 的注释中说明"插件 SecretVar 由上游 `FullyRedacted` 输出大写占位文字，因此不区分大小写；其他功能使用本包的小写占位文字"。也可以抽出 `isPluginMarker` 供这三处共用。

**N6 新文件名带下划线**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/plugin_credentials.go`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/README.md:34`
- **代码事实**：根 AGENTS.md 的 Code Style 规定"Go filenames: No underscores. The only permitted underscore is the `_test.go` suffix"。ee 下这是唯一带下划线的非测试 Go 文件（Glob 核对）。
- **维护影响**：打破仓库统一的命名约定，后续新文件可能跟着这样命名。
- **最小修正**：改名为 `plugincredentials.go`，并同步 README 与方案中的引用。

**N7 新权限在前端开关矩阵中没有映射，契约也没有说明**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/http/matrix.go:114-146`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/docs/角色权限接口.md:28`、`:46-52`
- **代码事实**：
  - `matrixGrants` 覆盖了其余 29 项权限，唯独没有 `SecurityChangeCredentialDestination`，查表时得到零值，不产生任何开关。
  - 第 115 行注释写的是"逐项列出权限对应关系"。
  - 接口指南把 `matrix` 描述为前端显示入口和按钮的依据，新权限一节却没有说明它不产生开关。
- **维护影响**：
  - 前端接入时无法区分"有意不映射"和"遗漏"；
  - 以后给矩阵加"每项权限都有映射"的核对时会失败；
  - 手工维护的契约（接口指南）与后端不一致。
- **最小修正**：
  - 在 `matrixGrants` 中显式写出该项为空映射，并注明"前端没有对应操作；是否允许由后端在保存时判定"；
  - 或者在第 115 行注释写明例外；
  - 同时在 `角色权限接口.md` 第 48 行后补一句"该权限不产生 matrix 开关"。

**N8 上游接缝文件内中英文注释混用，一句注释与行为矛盾**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/transports/bifrost-http/handlers/consolepolicy.go:31`、`:42-52`
- **代码事实**：
  - 这个文件是 EE 新增到上游目录的，原有注释都是中文（`:1`、`:29`、`:36`、`:39`、`:54-78`），本轮新增的五段注释是英文。
  - `:43` 写"a nil policy preserves the original upstream behavior"，但 `consolePolicy` 对 typed-nil 会返回 503 并拒绝（`:111-114`）。只有"未注入"才保持原行为，文件头 `:1` 的表述是准确的。
- **维护影响**：
  - 同步上游时，读者要在两种语言之间切换；
  - "nil policy"这个说法可能让人误以为注入一个空回调等于关闭检查。
- **最小修正**：统一为文件已有的中文，并把 `:43` 改为"未注入时保持上游原行为；类型错误或空回调返回 503"。

**N9 注释中手抄了引用的上限数值**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/http/protocol.go:21-22`
- **代码事实**：`const maxBodyBytes = authhttp.MaxBodyBytes` 的注释仍写"将请求正文限制为16 KiB"。
- **维护影响**：R6 的目的就是避免身份侧调整后 rbac 侧悄悄分叉；身份侧改动上限时，这条注释会先过期。
- **最小修正**：注释改为"沿用身份接口的正文上限"，或者删除这个别名，在 `:29` 直接使用 `authhttp.MaxBodyBytes`。

### 需用户裁决的 Blocking

**B1 账号删除与三项预留敏感权限的交付口径没有裁决记录**（由代码评审转交）

- **账号删除**
  - 产品方案把"删除"列为账号操作（`product/需求/角色权限/产品方案.md:65`），并要求"不能停用或删除最后一个主管理员"（`:143`）。
  - 同一文档第 136 行又写"账号管理范围限于：创建、设置初始密码、停用/启用、重置密码"，文档内部前后不一致。
  - 账号认证案明确写了"账号删除与多角色管理由角色权限案完善"（`workbench/cases/账号认证/期1/方案v2.md:65`）。
  - 本案没有删除接口，接口指南写的是"账号支持停用，没有删除账号接口"（`ee/docs/角色权限接口.md:36`）。但开发蓝图 §2、期4「Gate 决策结果」和需求页都没有排除这一项的裁决。
- **三项预留敏感权限**
  - 产品方案第四节把查看厂商密钥完整值、修改登录认证设置、导入配置列为已确认的敏感操作（`:89`、`:93-94`、`:154`）。
  - 目录中这三项都是 `Available: false`（`permissions.go:45`、`:82-83`），接口指南称其为"预留项"（`:36`）。
  - 就代码事实而言，这三项都没有可接入的入口：
    - 817 条路由清单里没有导入配置或密钥明文读取的接口（Grep `routes.txt` 核对）；
    - 认证设置已由账号认证案改为使用部署环境变量，`PUT /api/config` 拒绝修改认证字段（`账号认证接口.md:52`）。
  - 但"本案不交付这三项入口"本身没有裁决记录。
- **其他缺口**：产品方案 3.2 还要求"创建时选角色"，列表显示"已分配角色、最近登录时间"（`:62-63`）。当前 `/accounts/create` 只接收 `{username,display_name?}`，账号字段也不含最近登录时间（`账号认证接口.md:13`、`:27`）。角色可以通过 `/accounts/set-roles` 另行设置，但列表字段的缺口同样没有裁决记录。
- **为什么需要用户裁决**：这些问题涉及范围与契约，实施者不能自行判定；不能以"目录共 30 项"代替功能覆盖。
- **建议**：请用户逐项选择"本案交付"或"移出本案（记入后续需求）"，结论写入期4「Gate 决策结果」，并同步产品方案第 65/136 行的矛盾表述和需求页的范围说明。如果选择移出，现有代码与接口指南无需改动。

### 立债(REF:立 `triggered_by: [角色权限后端]` 的需求进 reports/)

以下为取舍项，由实施者决定采纳或撤回；不采纳时按"事后治理不贵"立债。

**S-a 权限总数 30 多处手写**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/persistence/service_test.go:128`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/roles_test.go:23`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/scripts/rbac-smoke.py:38`、`:40`、`:45`
- **事实**：
  - 本轮新增一项权限，就要同时改 2 个 Go 测试和脚本里的 3 行。
  - `roles_test.go` 已经钉住了目录大小。存储测试想确认的是"主管理员拥有整个目录"，可以写成 `len(rbac.Permissions())`；脚本内三处可以收成一个局部常量。
- **反证条件**：存储测试有意独立钉住目录大小，作为第二道防线。

**S-b `keyConnection` 与唯一调用方分处两个文件**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials.go:53-129`
- **事实**：`keyConnection` 和 `configSecretText` 只被 `providers.go:139`、`:143` 使用，却放在"共享比较"文件里。阅读 `providerKeyDestination` 时需要跨文件跳转。
- **最小修正**：移到 `providers.go` 中 `providerKeyDestination` 旁边。移动后 providers.go 约 230 行，未超过 300 行。
- **反证条件**：短期内有其他功能（例如 MCP 或插件）会复用 `keyConnection`。

**S-c `secretText` 是已有 helper 的一行包装**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials.go:24-26`
- **事实**：函数体只有 `return schemas.SecretVarAsString(v)`。
- **最小修正**：直接调用上游函数，或在注释说明保留包装的理由。
- **反证条件**：计划在这里加入规范化逻辑（例如 env 引用与字面值的统一表示）。

**S4' 路由清单的枚举仍列两遍**（接续上一轮 S4）
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/routes.go:21-71`、`:119-121`
- **事实**：
  - 上一轮 S4 中"按路径分支加说明"的部分已由 `rbac/host/README.md:41` 处理；
  - 三组合法取值仍在常量块和校验列表中各写一遍，没有采纳也没有立 REF。
- **最小修正**：每类枚举收成一个切片，常量块与校验共用。
- **反证条件**：与上一轮相同，即要求保持与 `b38493b07` 的规则摘要一致时只做枚举去重。只做去重不会改变摘要。

### 挂账(Finding:登记 findings.md,带四要件)

- 无新增。FIND-022 已登记，接口指南 `角色权限接口.md:44` 已说明。

### 转代码评审(范围外发现,一行一条,不出结论)

- `credentials.go:75`：`keyConnection` 只按名称匹配字符串类字段，`use_anthropic_endpoints`、`use_deployments_endpoint`、`force_single_region` 等会改变发送目标的布尔字段不在比较范围内，需核实覆盖是否足够。
- `settings.go:165-170`：代理的"保留凭据"只看 Password，不看 Username；`current == nil` 时直接放行，需核实。
- `webhooks.go:65-83`：原先对非 Update 的未知 op 返回 invalid，现在会落入更新路径，需核实上游只会传三种 op。
- `mcp.go:2665`：`desired := *existingConfig` 是浅拷贝，与现有配置共享 map 和指针，需核实策略只读，且后续上游代码不受影响。
- `credentials.go:165-176`：只要现有配置带 TokenExchange，`desired.TokenExchange` 就不是 nil，需核实比较语义符合预期。
- `plugins.go:309-328`：otel/telemetry 的 PUT 即使没有隐藏标记也会读取已存配置；没有已存行时返回 400，需对照上游首次配置插件的行为。
- `plugin_credentials.go:101-104`：telemetry 在任一侧 BasicAuth 为 nil 时直接放行，需核实推送网关是否还有其他凭据形式。
- `mcp.go:2645`：注入策略后跳过上游"更换 OAuth 地址必须重填 secret"的校验，需核实 EE 策略的覆盖是否等价。
- `providers.go:148-150`：只要厂商级配置有任意非空请求头，新建或更新 Key 改目标就需要敏感权限，需核实这是否是预期口径。

## 台账对账

- **FIND-017**：本轮复用了 `IsUniqueConflict`，没有新增复制，未恶化。
- **FIND-021**：本轮没有新增自取时钟，未恶化。
- **FIND-022**：已登记，口径已写入接口指南，未恶化。
- **FIND-012**：`bootstrap.go` 少了一层包装，改善。
- 其余 Finding 与本轮改动无关。

### 看似可改但有理由保留

- **K-a `plugin_credentials.go` 的 otel 分支自写循环，没有调用 `credentialDestination`（`:72-95`）**：多个 profile 共用同一凭据时，需要按"凭据是否已与相同目标配对"判断，单次比较表达不了。注释（`:72`）已说明原因，测试覆盖了多 profile 原样保存的情况。
- **K-b 厂商有两个上游回调（`ConsoleProviderUpdatePolicy` 与 `ConsoleProviderConfigPolicy`）**：恢复占位文字必须在合并之前，判定目标必须在合并之后（`providers.go:444-451`、`:552-559`），时点不同，合并成一个会丢失其中一个时点。
- **K-c `mcp.go:2645` 在注入策略时跳过上游校验**：未注入时行为不变，这种有条件的改动已登记在 `product/上游改动/README.md:159`，并列入方案白名单（`期4/方案v1.md:64`）。
- **K-d `TestRouteSourceDigest` 对 `/metrics` 单独还原 projection（`routes_test.go:117-124`）**：方案 `期4/方案v1.md:54` 已记录这个做法；测试同时断言当前值，不会让错误的旧值回退。
- **K-e `/metrics` 使用 `protocol-scoped`**：`response.go:21` 对它与 `protocol` 的处理相同。`routes.txt` 中的 managed 分组一律不用 `protocol`（Grep 结果 0 条），沿用了既有约定。
- **K-f Makefile 目标名 `smoke-rbac-postgres` 与 `smoke-identity` 风格不同**：新名称更明确；给旧目标改名会改变已写入文档的命令，收益不足。
- **K-g identity 内部仍直接使用 `uuidPattern`（`passwords.go:72`、`core.go:200`）**：这个正则还用于校验 operation_id，`ValidAccountID` 只是给外部使用的具名入口，不构成第二份规则。
- 上一轮 K1–K11 仍然成立。其中 K7 所说 `plugins.go` 超过 300 行（现为 358 行），已在方案 `期4/方案v1.md:70` 评估并保留。

---

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 发现：权限总数 30（S-a）；注释中的 16 KiB（N9） |
| 重复 | 同一规则多份实现且互不注明 | 发现：插件目标检查名单（N3）；占位文字两种判法（N5）；设置"不判权"两张表（N2） |
| 重复 | 重造已有 helper | 发现：`secretText` 包装（S-c）。`requirePermissions`、`IsUniqueConflict` 等已复用 |
| 重复 | 派生常量无推导 | 无：`maxBodyBytes` 直接引用源常量；没有新增派生数值 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无：本轮没有新增阈值或 ID；Makefile 和脚本使用环境变量 |
| 职责与实现质量 | 状态码、错误码裸数字 | 无：`consolepolicy.go` 已改用常量；`webhooks_test.go`、`consolepolicy_test.go:170` 中的 403 属于测试字面量，不计 |
| 职责与实现质量 | 无名的错误形状（以元组表示失败） | 无：`keyConnection` 返回 `(targets, credentials, error)`，错误走 error；`consolePolicy` 三元组见 K10 |
| 职责与实现质量 | 紧凑但绕的控制流 | 发现：自我比较（N1）；`restorePlugin` 一次调用做两件事（N3）。`credentials.go` 的 `walk` 闭包递归属于常规写法 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 发现：`keyConnection` 与调用方分处两个文件（S-b）。凭据比较单独成文件，减少了功能文件间的重复，不计 |
| 职责与实现质量 | 多余状态或守卫 | 发现：`clientIdentityFields`（N2），已核实两张表防的是同一组字段。`providerDestination` 的 nil 守卫对应接缝契约的失败即拒绝，上游 `providers.go:440-442` 保证不为 nil，保留 |
| 职责与实现质量 | 同一效果做两次 | 无：405 与 `Cache-Control` 已收为一处（`routes.go:240-244`）；通知受众校验已去重 |
| 职责与实现质量 | 注释与现状矛盾 | 发现：N3、N4、N8；`host/auth.go` 与 `identity/README.md` 的矛盾已修复 |
| 架构与项目一致性 | 接缝形状与同类不同 | 一致：三个新回调与 `ConsoleProviderUpdatePolicy` 等同类的形状相同（上下文键、可选、typed-nil 返回 503、`sendConsolePolicyError`）；调用块写法与 `providers.go:444-451` 一致；`mcp.go` 在入口处取策略，因为两处要用，合理 |
| 架构与项目一致性 | 架构符合性 | 符合：§4、§7 归位，§5 分工，§3 装配，§1 共享 Handler，见正文；§6 不适用 |
| 架构与项目一致性 | 依赖关系 | 符合：没有新依赖方向，使用窄接口与上下文注入，导出面分层正确，见正文 |
| 架构与项目一致性 | 绕过既有出口 | 无：错误经 `consoleError` / `rbachttp.Error`，存储读取经 `ConfigStore` / `lib.Config`，判权经 `requirePermissions` |
| 架构与项目一致性 | 把上游现状当契约 | 发现：Key 字段名列表没有核对机制（N4），正确性部分已转代码评审。插件类型经 `MarshalForStorage` 规范化，并有目录核对 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 发现：`matrix` 与指南（N7）；产品方案与交付声明（B1）。前端不在本期范围 |
| 契约一致性 | 状态码口径不一 | 无新增：405 统一带 Allow 和 `no-store`；上游透传口径见 FIND-022 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无：新权限不引入新字段；`proxyUpdate` 对 nil 返回 400，与 `webhookUpdate` 一致 |

### 实际静态阅读范围与未执行项

- **已读**：见「事实来源」。差量内每个 ee、transports、product 文件都读了完整现状，或读了 diff 加足够的上下文：
  - `identity/model.go`、`identity/http/protocol.go`、`identity/persistence/store.go` 只读了 diff 和 Grep 调用点，没有通读全文；
  - `mcp.go`、`provider_keys.go`、上游 `notifications.go` 只读了 diff 片段，`providers.go` 读了第 420–569 行；
  - 两份冒烟脚本只读了 diff；
  - 测试文件中 `credentials_test.go` 通读，其余按 diff 阅读。
- **开放式复读**：已完成，以"半年后要改这里的人"的视角逐文件复读。表外发现三条，已并入上文：
  - N6：文件名下划线；
  - N7：新权限没有 matrix 映射；
  - N8：上游接缝文件注释语言混用及 nil 表述。
- **未执行**：
  - 没有运行任何测试、构建、vet、goimports 或冒烟（只读约束）；
  - 没有读旧代码评审正文，只使用提示中转交的两条；
  - 没有逐项核对 `schemas.Key` 的全部 JSON 字段，只 Grep 了 `account.go` 中与目标相关的字段名；
  - 没有核对上游 `createProviderKey`、`updateProviderKey` 中 `providerConfig`、`oldRawKey` 的来源行；
  - 没有读 `workbench/evidence/` 下的本轮验证记录。
