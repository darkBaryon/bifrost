---
title: 角色权限后端 期4 收敛评审 #3
type: 收敛评审
case: 角色权限后端
phase: 4
round: 3
verdict: 有Blocking
created: 2026-09-17
---

> 本轮评审由启动脚本在新会话中执行，与实施上下文隔离。
> - 基线：`f371548b5444693d7e259033bec9f4a467e3748b`
> - 交付物：`38baff8fd970f4d0064651b5e919b8a8958d9f8b`
> - 本轮是差量复核。上一轮候选为 `983d1ecb4`，范围是 `git diff 983d1ecb4..38baff8fd`，共两个提交：`86d5f7db4`、`38baff8fd`。
> - 工作树状态：`git status --short` 输出为空，没有未暂存或未跟踪的文件。
> - 差量中的 ee、transports、product 文件与指定清单一致。workbench 下的文件属于过程文档，只作为裁决依据读取。
> - 风险等级 L3，逐维留痕。

## 结论

**上一轮问题的处置**
- 收敛评审2 的 N1–N9 全部已修复。
- 取舍项 S-a、S-b、S-c 已采纳；S4' 的保留理由成立。
- B1 已由用户裁决，并在本期补齐：
  - 账号删除；
  - 最近登录时间；
  - 三项预留敏感权限写明"不交付入口"。

**本轮新增代码的架构判断：符合 EE 后端架构**
- 删除规则放在 identity 根包。
- 最后主管理员检查和角色解绑由 rbac 在自有表上完成，通过身份模块定义的 `AccountPolicy.BeforeDelete` 接缝，在同一事务内执行。
- 存储层只删除身份自有的三张表。
- 依赖方向没有变化。

**仍不能按"无碍"交付**：新增部分留下了几处需要本轮修复的局部问题（M1–M8）：
- status 响应中新增的 `last_login_at` 字段从不赋值（M1）；
- DTO 放错分段（M2）；
- 测试 import 分组不符合 goimports（M3）；
- 迁移没有复用上游的加列 helper，也没有说明 v2 在新库上为何不执行任何操作（M4）；
- 多处注释没有跟上删除语义（M5）；
- 包 README 和接口指南以"补充段"追加，没有并入正文，部分表格行已过期（M6）；
- 产品方案出现前后矛盾，并混入实现细节（M7）；
- 凭据测试按评审轮次命名和组织（M8）。

这些修复都在授权范围内，不需要用户另行批准。

## 事实来源

**执行的命令**
- `git status --short`
- `git diff --stat 983d1ecb4..38baff8fd`、`git log --oneline 983d1ecb4..38baff8fd`、`git show 38baff8fd --stat`
- 分别对以下范围执行 `git diff 983d1ecb4..38baff8fd`：
  - `ee/internal/identity`、`transports`
  - rbac 各文件、`ee/scripts`、`ee/docs`、`product`
  - `workbench/findings.md`、`期4/方案v1.md`
  - `credentials_test.go`
- `git diff --stat f371548b5..38baff8fd -- ee/internal/app ee/internal/host`
- `git ls-files ee/internal/rbac/{host,http,persistence}`
- `git show 38baff8fd:product/AGENTS.md`
- `git grep`：`last_login_at`、`ee_identity_v1|v2`、`internal/rbac`（限 identity 目录）、残留旧名（`plugin_credentials`、`restorePlugin`、`clientIdentityFields`、`secretText(`）、权限总数 30

**先读的规则与裁决**
- 根 AGENTS.md、workbench/AGENTS.md
- 收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md
- templates/收敛评审.md、product/架构/EE后端架构.md、workbench/findings.md
- 期4 方案v1 全文，包括：
  - 「Gate 决策结果」「实施偏差记录」
  - 「Gate 决策追加」两节
  - 「第二轮复核修正」「B1 范围核对」
- 上一轮记录 收敛评审2.md

**完整阅读的范围内文件**
- identity 根包：accounts.go、access.go、session.go；model.go 第 110–269 行
- identity/http：accounts.go、dto.go、session.go 第 1–45 行、README.md
- identity/persistence：store.go、migration.go、rows.go、README.md；identity/README.md
- rbac 根包与 identity 适配：assignments.go、rbac/identity/policy.go、rbac/README.md
- rbac/host：
  - 完整阅读：credentials.go、providers.go、plugincredentials.go、credentials_test.go、settings.go、webhooks.go、README.md
  - 部分阅读：plugins.go 第 200–374 行、request.go 第 95–164 行、routes.go 第 1–135 行、routes_test.go 第 100–150 行
- rbac/http：matrix.go 第 105–164 行；rbac/persistence：accounts_test.go、concurrency_test.go
- 其他：transports/.../consolepolicy.go、ee/scripts/rbac-smoke.py 前 40 行及 diff、两份 ee/docs 接口指南、产品方案第 1–30 行与第 55–189 行、ee/docs/账号认证升级与恢复.md、app/rbac.go

**对照的同类实现与上游证据**
- `transports/bifrost-http/handlers/plugins.go:468-480`：上游 `updatePlugin` 先按顶层浅合并，再恢复占位文字。这证实 `preparePluginUpdate` 中"与宿主保持一致"的说法成立。
- `framework/migrator/addcolumn.go:11-33`：`AddColumnIfNotExists` 的注释明确说"调用方不要再包一层 HasColumn"。
- `framework/configstore/migrations.go:30`：包内别名 `addColumnIfNotExists`，全文件共 185 处引用，是上游加列的主流写法（M4 的依据）。
- 身份模块同类实现：
  - `identity/passwords.go:72`：ResetPassword 在事务外先校验 ID 格式；
  - `accounts.go:109-143`：SetAccountStatus 不校验 ID 格式（S1 的依据）。
- rbac 同类实现：
  - `rbac/assignments.go:104-113`：BeforeDisable 是 BeforeDelete 的先例；
  - `rbac/http/api.go:69-79`：删除角色返回 `{"message"}`。
- 测试夹具：
  - `rbac/persistence/concurrency_test.go:17-31`：`pausedTransaction` 夹具；
  - `identity/persistence/service_test.go:81`、`:191`：身份存储已有的登录夹具与失败回调（S2 的依据）。
- `app/rbac.go:19-21`：policyFactory 让角色服务绑定身份事务。

## 上一轮「本轮修复项」处置

| 编号 | 处置 | 证据 |
|---|---|---|
| N1 自我比较 | 已修复 | `providers.go:151-154` 改为 `slices.ContainsFunc(... v != "")`，并加注释"继承的厂商请求头仍随新Key发送"。 |
| N2 `clientIdentityFields` 无效 | 已修复 | 表和 `continue` 已删除（Grep 无残留）。`settings.go:103-104`、`:119` 的行尾注释写明了原因。 |
| N3 函数名、注释、插件名单 | 已修复 | ① `plugins.go:291-292` 改名为 `preparePluginUpdate`，注释写明合并、恢复与目标检查；`request.go:116` 同步。<br>② `plugincredentials.go:90-93` 新增 `hasDestinationCheck`，注释说明它与两处 switch 共同登记；`plugins.go:311` 改为引用它。<br>③ `settings.go:1`、`:139` 和 `webhooks.go:50-51` 的注释已更新。<br>④ `webhooks.go:86` 与 `settings.go:179` 的写法已统一。 |
| N4 注释与 README | 已修复 | `providers.go:232` 的注释改为只针对 Key 字段。`host/README.md:47` 写明"Key字段由providers.go的keyConnection显式识别，上游升级时还须人工核对"。 |
| N5 大小写不敏感的原因 | 已修复 | `plugins.go:228` 已说明：插件 SecretVar 使用上游的大写占位文字。 |
| N6 文件名下划线 | 已修复 | 文件已改名为 `plugincredentials.go`；`host/README.md:34` 与方案已同步。`git grep plugin_credentials` 无结果。 |
| N7 matrix 没有映射 | 已修复 | `matrix.go:147-148` 显式写出空映射并加注释；`角色权限接口.md:48` 补充"该权限不产生matrix开关"。 |
| N8 上游接缝注释 | 已修复 | `consolepolicy.go:31`、`:42-51` 已统一为中文；`:43` 写明"未注入时保持上游行为；注入类型错误或空回调时返回503"，与 `:108-114` 的行为一致。 |
| N9 手抄上限 | 已修复 | `rbac/http/protocol.go:21` 改为"沿用身份接口的正文上限"。 |

**上一轮其他事项的后续**
- **B1**：已由用户裁决（方案v1「Gate决策追加：账号删除与最近登录时间」及「B1范围核对」），本轮已实现，不再作为 Blocking。
- **S-a**：已采纳。
  - `service_test.go:128` 改为 `len(rbac.Permissions())`；
  - 冒烟脚本收成常量 `EXPECTED_PERMISSION_COUNT`（`rbac-smoke.py:18`）；
  - 目录总数只在 `roles_test.go:23` 钉住（Grep 核对）。
- **S-b**：已采纳，`keyConnection` 和 `configSecretText` 已移到 `providers.go:158-246`。
- **S-c**：已采纳，`secretText(` 已无残留，调用处直接使用 `schemas.SecretVarAsString`。
- **S4'**：有依据保留。方案v1「第二轮复核修正」给出的理由核对属实：
  - `routes.go:21-71` 的常量块与 `:119-121` 的合法值列表都必须引用同一组命名常量；
  - 另建切片仍然是两份列举，不能消除"加了常量忘记加入集合"的风险。
  - 不再重提。

## 代码评审「转收敛评审」处置

原文为"无新增条目。已有目录分配、路由匹配分配、脚本夹具组织建议仍由收敛评审处理"。没有新增条目需要处理。所提三类既有建议，按本侧的理解处置如下：

1. **目录分配**（文件和测试放在哪个目录）
   - `keyConnection` 已移到 providers.go（S-b），已关闭。
   - 本轮新增的 `TestLastLoginAtomicity` 放在 rbac 包，作为取舍项 S2 提出。
2. **路由匹配分配**
   - 路由清单的枚举：S4' 有依据保留。
   - `TestRouteSourceDigest` 对删除路由另行断言后跳过：列入"看似可改但有理由保留"。
3. **脚本夹具组织**
   - S-a 已采纳。常量 `EXPECTED_PERMISSION_COUNT` 位于两次 `runpy` 加载之间（`rbac-smoke.py:16-20`），位置略显随意，但没有维护代价，撤回。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 加列迁移重写了上游 helper（M4）。新增的删除保护按模块归属分两处（身份：自己和固定恢复账号；rbac：最后主管理员），双方注释互相说明，不算重复。 | 见出口清单 |
| 职责与实现质量 | ① status 响应中的死字段（M1）。<br>② DTO 放错分段（M2）。<br>③ import 分组（M3）。<br>④ 注释跟不上删除语义（M5）。<br>⑤ 测试按评审轮次组织（M8）。<br>⑥ 业务根包里写了 PG 时间精度（S3，取舍项）。 | 见出口清单 |
| 架构与项目一致性 | 符合，逐项核对见下文两段。取舍项：身份行为的测试放在 rbac 包（S2）。 | 见下文 |
| 契约一致性 | ① status 响应键与指南不一致（M1）。<br>② 接口指南与 README 以"本轮补充"方式追加，正文有过期内容（M6）。<br>③ 产品方案内部"七项"与"八项"矛盾，并混入实现细节（M7）。<br>④ 三个账号操作对格式错误 ID 的处理顺序不一（S1）。 | 见出口清单 |

### 架构符合性（对照 EE 后端架构）

- **§4 模块内职责：符合。**
  - 删除规则在根包 `identity/accounts.go:167-189`：判权、自己和固定恢复账号保护、调用策略、执行 `tx.DeleteAccount`。
  - HTTP 层 `identity/http/accounts.go:57-66` 只做"解码 → 调用 → 响应"。
  - 存储层 `persistence/store.go:160-178` 执行三张表的删除。
  - 这与 §4 第 73 行"停用账号"的示例分工相同。
- **§5 模块之间：符合。**
  - 角色解绑由 rbac 在自有表上执行：`rbac/assignments.go:136-146` 调用 `tx.ReplaceAccountRoles`。
  - 身份模块只通过自己定义的窄接口调用 rbac（`access.go:23-30` 的 `BeforeDelete`），存储层只删 `ee_identity_*` 三张表（`store.go:163-170`），没有直接修改对方的表。
- **§5 事务：符合。**
  - `app/rbac.go:19-21` 通过 policyFactory 为每个身份事务绑定角色服务；
  - `accounts.go:170`、`:184` 经 `accessPolicy(tx)` 取得绑定当前事务的策略；
  - 失败回滚由 `rbac/persistence/accounts_test.go:69-88` 覆盖。
- **§3 app 只做装配：符合。** 本轮 app 没有改动。
- **§7 集成归位与复用完整操作：基本符合。**
  - 新路由登记在身份路由表（`identity/http/handler.go:61`）和 `rbac/host/routes.txt:6` 的 identity-service 组，没有另起入口；
  - 迁移经 `upstream.RunSingleMigration` 执行（`migration.go:66`）；
  - 但加列没有复用上游的 `migrator.AddColumnIfNotExists`，见 M4。
- **§5 根包只依赖标准库：符合。**
  - `session.go:7` 只新增了 `time`；
  - 但 `session.go:83-84` 在根包写入了 PostgreSQL 精度知识，见 S3。
- **§6 插件：不适用。** 本轮没有插件入口的变化。

### 依赖关系

- **import 的变化**
  - identity 根包只新增标准库 `time`。
  - rbac 根包和 rbac/identity 的 import 没有变化。
  - rbac/host 只新增标准库 `net/url`（`settings.go:7`）和 `maps`（`plugins.go:7`）。
  - `rbac/persistence/accounts_test.go` 引入 `identity/hasher`，与既有的 `concurrency_test.go:11` 相同，仅用于测试。
- **反向依赖**：`git grep "internal/rbac" -- ee/internal/identity` 无结果，identity 各包仍不导入 rbac，没有形成反向或双向依赖。
- **窄接口与显式注入**
  - `AccountPolicy` 接口由使用方（身份模块）定义；
  - rbac 通过 `rbac/identity/policy.go:106-112` 实现，并由 app 注入；
  - 没有使用全局容器。
- **资源所有权**：没有新增共享资源，数据库连接仍由宿主持有。

## 出口清单

### 本轮修复项

**M1 status 响应新增了从不赋值的 `last_login_at` 字段**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/http/dto.go:58`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/http/session.go:28-29`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/docs/账号认证接口.md:21`
- **代码事实**：
  - `statusResponse` 新增了 `LastLoginAt *time.Time`，但唯一的构造点 `session.go:28-29` 没有给它赋值，Grep 也没有其他赋值；
  - 因此 `/api/identity/status`（匿名也可调用）恒定输出 `"last_login_at": null`；
  - 接口指南第 21 行的 status 字段列表没有这个键；
  - `status` 的注释写的是"没有会话时不暴露账号信息"。
- **维护影响**：
  - 契约中多了一个永远为 null 的键，前端可能误以为它会返回值；
  - 后端代码、DTO 和指南三方不一致；
  - 以后如果有人"补上赋值"，就会在匿名接口上暴露账号信息。
- **最小修正**：删除 `statusResponse.LastLoginAt`。`accountDTO` 里的字段保留。

**M2 `deleteAccountRequest` 放在了"密码线"分段**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/http/dto.go:176-179`
  - 对照同文件 `:1`、`:78`、`:122-127`，以及 `ee/internal/identity/http/README.md:24`
- **代码事实**：
  - 文件头和 README 都约定 dto.go "按 共同/会话/账号/密码 四段排列"；
  - 新增的账号请求结构却放在文件末尾的 `eventsRequest` 之后，也就是密码线分段内。
- **维护影响**：按分段查找账号请求的人找不到它；后续新增结构容易照此继续往文件末尾堆。
- **最小修正**：移到 `setStatusRequest`（`:122-125`）之后。

**M3 两个测试文件的 import 分组不符合 goimports**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/persistence/accounts_test.go:6`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials_test.go:8`（本轮新增的一行）
- **代码事实**：`github.com/darkBaryon/.../identity/hasher` 和 `github.com/valyala/fasthttp` 夹在标准库分组里。同目录的 `concurrency_test.go:4-15` 按三组排列。
- **维护影响**：
  - 与仓库 Code Style 规定的 `goimports` 不一致，也与上一轮 R12 已统一的写法不一致；
  - 下次有人运行 goimports 时会产生无关 diff。
- **最小修正**：对这两个文件运行 goimports，把两行移到第三方分组。

**M4 加列迁移没有复用上游 helper，也没有说明 v2 在新库上为何不执行任何操作**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/persistence/migration.go:66-71`
  - 同文件 `:1`、`:23-24`、`:44`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/persistence/README.md:26`
- **代码事实**：
  - v2 迁移手写了 `if HasColumn { return nil }; AddColumn`；
  - 上游 `framework/migrator/addcolumn.go:33` 提供了 `AddColumnIfNotExists`，注释（`:28-29`）明确要求调用方不要再包一层 HasColumn；
  - `configstore/migrations.go:30` 为它建了别名，全文件引用 185 处；
  - 本文件第 8 行已经导入 `migrator`。
  - 这个守卫之所以必要，是因为 v1 在 `:44` 用当前的 `accountRow` 建表：新库上 v1 已经带有 `last_login_at` 列，v2 实际不执行任何操作。但代码没有交代这一点。
  - 文件头（`:1`）、`MigrateIdentity` 的注释（`:23-24`）和 README 第 26 行都只描述了 v1。
- **维护影响**：
  - 手写守卫与上游约定的写法不同，而上游写法同时处理了多节点重复 ALTER 的问题；
  - 读者看到 HasColumn 时要自己推理出"v1 已按新结构建表"，否则可能把它当作多余守卫删掉；
  - 下一个加列迁移也会照抄这种写法。
- **最小修正**：
  - 改为 `return migrator.AddColumnIfNotExists(tx, nil, &accountRow{}, "LastLoginAt")`；
  - 加一行注释："新库由 v1 按当前行结构建表时已含该列，仅旧库需要补列"；
  - 同步 `:1`、`:23-24` 与 README 第 26 行，写明包含 v2 增量迁移。

**M5 删除语义加入后，相关注释没有同步**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:1`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/assignments.go:1`、`:30`、`:134-146`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/session.go:63`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/model.go:168`
- **代码事实**：
  - `providers.go:1` 仍写"隐藏厂商凭据，并在更新配置时用原值替换隐藏占位文字"，但文件现在还包含 `providerDestination`、`providerKeyDestination`、`keyConnection`，约占全文一半（S-b 移入后）。
  - `assignments.go:30` 中 `remainingChief` 的注释仍写"不允许停用它或移除它的主管理员角色"，现在删除也会调用它（`:141`）。文件头 `:1` 也没有提到删除时清理角色。
  - `BeforeDelete` 放在 `bindChief`、`RestoreChief` 之后，与同类的 `BeforeDisable`（`:104-113`）隔开。
  - `session.go:63` 的注释仍写"可能发生重置或停用"，而 `:65-67` 新增了账号在比较密码期间被删除时的处理。
  - `model.go:168` 写"记录不随账号停用删除"；现在账号删除后事件同样保留。
- **维护影响**：
  - 按编码规范，与实际行为不一致的注释视为缺陷；
  - 维护者按文件头找不到 Key 目标检查；
  - 修改 `remainingChief` 时可能忽略删除场景。
- **最小修正**：
  - 更新上述注释：providers.go 文件头加上"并检查携带旧凭据更换目标"；`remainingChief` 的注释加上"或删除它"；`session.go:63` 加上"删除"；`model.go:168` 改为"停用或删除"。
  - 把 `BeforeDelete` 移到 `BeforeDisable` 之后。

**M6 包 README 与接口指南以"补充段"追加，正文和表格行没有同步**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/README.md:17`、`:32-34`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/http/README.md:27`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/persistence/README.md:26`、`:33`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/README.md:21`、`:28`、`:44`、`:53-55`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/README.md:28`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/docs/账号认证接口.md:21`、`:30`、`:39`、`:58-62`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/docs/账号认证升级与恢复.md:23`
- **代码事实**：
  - **identity/README.md**：文件表第 17 行 accounts.go 仍是"初始化、旧账号导入、创建和启停账号"，另在末尾加了"## 账号补充能力"一节。
  - **identity 子包 README**：http 与 persistence 的 README 都在文件表之后追加了一句"增加……"，表格行本身（http `:24`、persistence `:26`）没有更新。
  - **rbac/README.md**：
    - 第 53 行与第 55 行两段是对同一件事的重复描述；
    - 表格行 `:21`（assignments.go）、`:28`（policy.go）没有提到删除；
    - "改之前要知道"第 44 行仍只写"移除角色或停用账号时"。
  - **host/README.md**：第 28 行 providers.go 仍是"隐藏厂商凭据，更新时保留原值"，而同文件第 47 行已经指向 providers.go 里的 keyConnection。
  - **账号认证接口.md**：
    - 第 58 行起是"## 本轮补充"一节，与第 13 行的字段列表、第 30 行的表格行重复，状态码只写在补充段里；
    - 第 39 行仍写"记录不会因账号停用删除"；
    - "本轮"是会过期的相对时间表述。
  - **账号认证升级与恢复.md**：第 23 行的升级步骤只列了 ee_identity_v1。该文件不在本次差量内，但直接受本次迁移影响。
- **维护影响**：
  - 包 README 应描述现状和逐文件职责，按变更日志追加会让同一事实分散在两处，读者看表会得到过期的职责描述；
  - 接口指南是手工维护的契约，同一接口分写两处，以后很容易只改一处。
- **最小修正**：
  - 把补充段内容并入对应的表格行和"改之前要知道"条目，然后删除补充段；
  - 接口指南把状态码并入第 30 行，第 39 行加上"或删除"，删除"本轮补充"一节；
  - 升级文档第 23 行补一句"v2 增量迁移为账号表补可空列"。

**M7 产品方案出现前后矛盾，并混入实现细节**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/product/需求/角色权限/产品方案.md` 第 3–9 行、`:85`、`:102-114`、`:155`、`:183-189`
- **代码事实**：
  - 第 85 行改成"原七项（本轮追加凭据目标变更后共八项）"，第 155 行的决策记录仍是"第四节所列七项"；
  - 第 102–114 行的首版清单没有列出新增的敏感操作；
  - 文首的确认记录（第 3–9 行）没有 2026-09-17 的追加确认；
  - 第 9 行声明"本文只描述产品行为与规则,不涉及实现"，而第 183–189 行写入了 `Security.ChangeCredentialDestination`、`last_login_at`、`available=false`、"票据"等实现名词；
  - `product/AGENTS.md` 也要求"产品方案描述业务行为"；
  - "本轮"是会过期的相对时间表述。
- **维护影响**：
  - 产品文档内部对敏感操作的数量说法不一致；
  - 实现名词进入产品层，接口改名时需要同步修改产品文档；
  - 这与 ee/docs/角色权限接口.md 已有的内容重复。
- **最小修正**：
  - 在文首追加一条带日期的确认记录；
  - 第 85 行和第 155 行改为同一数量，并注明追加日期；
  - 在第四节清单中补上该敏感操作所属的模块；
  - 末尾一节只保留业务行为描述，编码和字段名留在 ee/docs。

**M8 凭据测试按评审轮次命名和组织，同一功能的用例分散在多个函数**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials_test.go:220`、`:319`，对照 `:79`、`:120`、`:194`、`:287`
- **代码事实**：
  - `TestCredentialDestinationReviewBoundaries` 和 `TestAdditionalCredentialDestinationBoundaries` 两个函数名表达的是"评审补充"，而不是被测行为；
  - 两个函数都同时混合了代理、Key 别名和插件用例，而这三类各自已有专门的测试（`TestProxyCredentialDestination`、`TestProviderKeyCredentialDestination`、`TestPluginCredentialDestination`、`TestPluginPartialCredentialDestinationUpdate`）；
  - 同一文件内，拒绝状态的断言一半写 `403`（`:128`、`:153`），一半写 `fasthttp.StatusForbidden`（`:227`、`:325`）。
- **维护影响**：
  - 修改代理规则的人要在三个函数里找用例；
  - 函数名在评审语境过去后就失去了含义；
  - 文件已有 358 行，超过 300 行的评估线，这种分散写法也是行数偏多的原因之一。
- **最小修正**：
  - 把两个"Boundaries"函数中的用例按功能并入对应测试，作为 `t.Run` 子用例，然后删除这两个函数；
  - 同一文件内的状态码写法统一为一种。

### 需用户裁决的 Blocking

- 无。B1 已由用户裁决并实施。

### 立债(REF:立 `triggered_by: [角色权限后端]` 的需求进 reports/)

以下为取舍项，由实施者决定采纳或撤回；不采纳时写明理由。

**S1 三个账号操作对格式错误 ID 的处理顺序不一致**
- **位置**：
  - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/accounts.go:170-176`
  - 对照 `passwords.go:72-74`、`accounts.go:114-124`
- **事实**：
  - DeleteAccount 先开写事务并判权，再校验 ID 格式：无权调用方传入非法 ID 得到 403，有权调用方得到 400；
  - ResetPassword 在事务外先校验格式，一律返回 400；
  - SetAccountStatus 不校验格式，非法 ID 返回 404。
- **最小修正**：DeleteAccount 改为与 ResetPassword 一致，在进入事务前执行 `ValidAccountID`。这样也不会为格式错误的请求占用 state 写锁。
- **反证条件**：删除接口有意让无权调用方对任何输入都只得到 403，以免暴露校验规则。若如此，应在注释中写明，并说明 ResetPassword 为何不同。

**S2 纯身份行为的测试放在 rbac 包**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/persistence/accounts_test.go:90-134`
- **事实**：
  - `TestLastLoginAtomicity` 只调用身份服务和身份存储，不涉及角色规则；
  - `identity/persistence/service_test.go` 已有登录夹具（`:81`）和 GORM 失败回调的写法（`:191`）；
  - identity 维护者按包查找测试时找不到它，只能依靠 persistence README 第 33 行的指引。
- **最小修正**：迁到 identity/persistence 的独立测试文件。service_test.go 已接近 500 行，因此新建文件，不宜追加。
- **反证条件**：该测试的目的就是验证"装配了 rbac policyFactory 后，登录事务仍然原子"。若如此，在测试注释中写明。

**S3 业务根包写入了 PostgreSQL 时间精度**
- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/identity/session.go:83-84`
- **事实**：
  - 根包按"PostgreSQL 精度为微秒"截断 `LastLoginAt`，这是存储细节；
  - 身份模块只有一个时钟，即 `core.go:81` 的 `now()`；
  - 这是第一个同时从内存和数据库返回给前端的时间戳。
- **最小修正**：把"持久化精度"收成一个命名常量，或放进 `now()` 并注明来源，不在具体业务点写死驱动名。
- **反证条件**：在 `now()` 中截断会影响会话到期或分页游标的比较；或者后续不会再有其他需要"响应值等于存储值"的时间戳。满足任一条件时，保持现状即可。

### 挂账(Finding:登记 findings.md,带四要件)

- 无新增。

### 转代码评审(范围外发现,一行一条,不出结论)

- `session.go:83-90`：每次成功登录新增一次整行 `SaveAccount`，并在 state 写锁事务内执行，需核实对 FIND-018 所述登录串行化的实际影响。
- `store.go:163-170`：删除票据使用 `session_id IN (子查询)`，需核实两种驱动的行为，以及会话删除后仍在进行的请求、已建立的 WS 连接是否都按 401 或关闭处理，而不是 503。
- `settings.go:167-171`：`url.Parse` 失败时静默视为"URL 不带凭据"，需核实非标准代理 URL 的判定是否符合预期。
- `migration.go:66-71`：v2 与 v1 共用同一事务和咨询锁，需核实从旧库升级、以及回滚到旧二进制读取带新列的表时的行为。
- `assignments.go:144`：对没有任何角色的账号调用 `ReplaceAccountRoles(target, nil)`，需核实不会报错。
- `accounts.go:177`：固定恢复账号返回 403，而最后主管理员返回 409，需核实与前端提示口径一致。

## 台账对账

- **FIND-017**：本轮没有新增 busy 重试的副本，未恶化。
- **FIND-018**：登录事务在 state 锁内新增一次账号写入，属轻微增加，量化已转代码评审。
- **FIND-021**：`LastLoginAt` 取自 `v.CreatedAt`，也就是 `now()`；删除路径不读取时钟。没有新增自取时钟，未恶化。
- **FIND-022**：删除路由走身份错误映射，与 rbac/host 的错误转换无关，未恶化。
- **FIND-012**：app 本轮没有改动。
- 其余 Finding 与本轮无关。

### 看似可改但有理由保留

- **K-a 账号删除成功返回 `{}`，而角色删除返回 `{"message"}`**（`identity/http/accounts.go:65` 对照 `rbac/http/api.go:78`）：`{}` 是方案v1「Gate决策追加」中用户确认的契约，不因形状统一而改动。
- **K-b `rbac.Service.BeforeDelete` 在同一事务内再次检查 Users.Manage**（`assignments.go:138`）：与既有的 `BeforeDisable`（`:108`）同形。rbac 服务不依赖调用方已经判过权，这层检查防的是直接调用 rbac 的使用方，与身份侧的判权不是同一个对象。
- **K-c `preparePluginUpdate` 自行做一次顶层浅合并**（`plugins.go:343-352`）：EE 策略运行在上游合并之前，必须看到与上游相同的生效配置。上游 `plugins.go:470-480` 核对属实，注释已写明对应关系。
- **K-d `TestRouteSourceDigest` 对删除路由单独断言后跳过**（`routes_test.go:113-118`）：这样固定候选的摘要保持不变，新增路由的规则另有独立断言，方案v1 已登记。
- **K-e `ChiefPolicy.BeforeDelete` 不做任何操作**（`access.go:70-71`）：未装配 rbac 时只有固定恢复账号能执行管理操作，而它不能删除自己，因此不存在"最后主管理员"的场景，注释已说明。
- **K-f `mcpDestination` 仍在 credentials.go，没有随 S-b 移到功能文件**：rbac/host 没有 mcp.go，唯一调用方是 `request.go:145`。README 第 33 行已写明"含MCP的TLS/OAuth边界"，单独建文件收益不足。
- **K-g `DeleteAccount` 使用导出的 `ValidAccountID`，而 passwords.go 使用 `uuidPattern`**：上一轮 K-g 的理由仍然成立。
- 以下文件超过 300 行，均已在方案v1 中评估并保留：plugins.go（374 行）、store.go（354 行）、credentials_test.go（358 行）。其中 credentials_test.go 的组织问题见 M8。
- 上一轮 K-a 至 K-g，以及更早的 K1–K11，仍然成立。

---

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无：权限总数只在 `roles_test.go:23` 钉住，脚本收成常量；818、817 在 README、方案、测试中口径一致 |
| 重复 | 同一规则多份实现且互不注明 | 无：删除保护按模块归属分两处并互相注明；插件浅合并注明对应上游 |
| 重复 | 重造已有 helper | 发现：M4（`AddColumnIfNotExists`） |
| 重复 | 派生常量无推导 | 无 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无：迁移 ID 已命名；时间精度见 S3 |
| 职责与实现质量 | 状态码、错误码裸数字 | 无（生产代码）；测试文件同文件混用两种写法，并入 M8 |
| 职责与实现质量 | 无名的错误形状（以元组表示失败） | 无 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无：`proxyUpdate` 的嵌套判断和 `target` 闭包没有捕获可变外部状态 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 发现：M8（测试按轮次分散）；S2（跨包放置） |
| 职责与实现质量 | 多余状态或守卫 | 发现：M1（从不赋值的字段）。HasColumn 守卫核实为必要，但没有说明，并入 M4 |
| 职责与实现质量 | 同一效果做两次 | 无：两次 Users.Manage 检查防的是不同调用方，见 K-b |
| 职责与实现质量 | 注释与现状矛盾 | 发现：M5 |
| 架构与项目一致性 | 接缝形状与同类不同 | 一致：`AccountPolicy.BeforeDelete` 与 `BeforeStatusChange` 同形；`Service.BeforeDelete` 与 `BeforeDisable` 同形；`Tx.DeleteAccount` 与其他 Tx 方法同形；新路由沿用身份路由表和 routes.txt 分组 |
| 架构与项目一致性 | 架构符合性 | 符合：§4、§5、§3、§7 见正文；§6 不适用；根包存储细节见 S3 |
| 架构与项目一致性 | 依赖关系 | 符合：只新增标准库 import；identity 不导入 rbac；窄接口由 app 注入 |
| 架构与项目一致性 | 绕过既有出口 | 无：迁移经 `RunSingleMigration`，错误经 `SafeError`、`IdentityError`，响应经 `serve` |
| 架构与项目一致性 | 把上游现状当契约 | 无新增：插件合并已对照上游行核实 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 发现：M1、M6、M7；前端不在范围 |
| 契约一致性 | 状态码口径不一 | 发现：S1。`{}` 与 `{message}` 的差异由 Gate 裁决（K-a） |
| 契约一致性 | 省略、空、值三态语义不对应 | 无：`last_login_at` 的 null 与时间值已写明语义；status 响应中的恒 null 并入 M1 |

### 实际静态阅读范围与未执行项

- **已读**：见「事实来源」。
  - 差量内的 ee、transports、product 文件都读了完整现状，或读了与 diff 相关的完整函数及上下文；
  - `plugins.go` 读第 200–374 行，`request.go` 读第 95–164 行，`routes.go` 读第 1–135 行，`model.go` 读第 110–269 行，`matrix.go` 读第 105–164 行；
  - `rbac-smoke.py` 读前 40 行和完整 diff；
  - 产品方案读第 1–30 行和第 55–189 行。
- **开放式复读**：已完成，以"半年后要改这里的人"的视角逐文件复读。表外发现如下，已并入上文：
  - M2：DTO 分段；
  - M7：产品方案内部矛盾；
  - M8：测试按轮次命名；
  - M5 中 `providers.go:1` 文件头过期；
  - M6 中升级文档漏列 v2 迁移（该文件在差量外，但直接受影响）。
- **未执行**：
  - 没有运行任何测试、构建、vet、goimports 或冒烟（只读约束）；M3 的结论基于阅读 import 块得出。
  - 没有读旧代码评审正文，只使用提示中转交的一条。
  - 没有核对 `identity/http/handler_test.go` 和 `host/auth_test.go` 是否覆盖新路由（属代码评审范围）。
  - 没有读 `workbench/evidence/` 下的本轮验证记录。
  - 没有通读 `rbac/host/plugins.go` 第 1–199 行（本轮没有改动）。
  - 没有核对上游 `providers.go`、`provider_keys.go`、`mcp.go` 在本轮的变化：差量中没有这些文件的改动。
