---
title: "角色权限后端 期4 代码评审 #8"
type: 代码评审
case: 角色权限后端
phase: 4
round: 8
verdict: 通过
created: 2026-09-18
---

> 独立正式差量复核。候选 `de704b4c0575b88aace6f90aee6cc63cefa01801`，上一候选 `45fb2abc909407914b3b8cdffc4ea851560f5122`，总基线 `f371548b5444693d7e259033bec9f4a467e3748b`。开始及本轮测试完成时工作树干净；本报告随后新增。未修改生产代码或提交。
> 本轮核对完整差量、R9剩余项及收敛评审7原文三项转交。未读取本轮收敛评审8；不采信实施方测试通过声明作为亲核结果。

## 结论

**通过本轮代码差量复核，R9关闭。** 厂商代理现在同时记录仍保存的独立字段和URL认证，完整独立字段不再使URL来源被提前排除。无scheme/Opaque组合、被覆盖旧认证、环境模式暂存URL均纳入保护；真正删除或替换旧认证后，使用新凭据仍可正常修改目标。

host全包race、vet、期4预飞及独立转交项合成检查全部通过；未发现新增本轮修复项。此结论是代码复核结果，不代替独立收敛结论和整体验收Gate。

## 对照方案核查

- 差量15文件、481行增加、21行删除。生产变化在`providers.go`、`settings.go`及`credentials.go`注释；另有凭据测试、接口指南、README、方案收尾、上轮报告归档、验证记录与生成视图。
- 方案「第七轮复核收口」明确保护保存后仍存在的两种来源，不再把当前覆盖等同于删除旧凭据；接口指南和实际判断对应。未增加权限、公开入口、依赖、迁移或部署步骤。
- `proxyCredentials`移除完整独立字段提前返回；独立密码非空取密码，否则取用户名，然后继续读取URL认证或引用。Env模式增加环境身份标记，仍收集配置中的其他认证，避免切换类型时遗漏。
- 新增的删除旧URL认证、两份认证全部替换均允许；同地址普通保存不因凭据列表非空而拒绝，仍需目标发生变化才要求敏感权限。厂商Key及ExtraHeaders原有检查保留。
- S1解析出错返回nil已落实，调用方仍将新配置错误映射400，旧配置错误保守要求权限；S2两次独立比较改用`retainedValues`，与原非空相等表达等价，没有在全局代理引入跨字段合并。

## 接口面对账

- 无新增HTTP请求/响应字段或公开Go符号。角色权限目录、授权接入和错误出口保持。
- 已合并配置仍在宿主恢复占位符后、保存前检查；两种来源的提取不写回配置，不改变上游代理网络行为，不调用外部密钥解析服务。
- 全局及厂商代理都不再将“旧URL认证被覆盖”作为忽略它的理由。新接口说明只解释既有敏感权限边界，不开放明文读取。

## 预飞事实表

独立执行`python3 workbench/tools/preflight.py --config workbench/cases/角色权限后端/期4/checklist.yaml --repo . --out /tmp/rbac-review8-preflight.md`，范围、空白、gofmt、source-route-contract、生成一致性、产物及必含检查**全部通过**；`git diff --check 45fb2abc9..de704b4c0`通过。

结果绑定固定候选；本报告新增后的生成导航由实施方统一处理。

## 可证伪质疑

### R9 — 修复已关闭

- **位置**：[providers.go:136](/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:136)、[providers.go:114](/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:114)及[credentials_test.go:79](/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials_test.go:79)。
- **静态事实**：独立认证与URL认证都进入列表；不再依据GetValue非空推断覆盖已生效。第七轮的Opaque覆盖问题不再影响是否识别URL中保存的旧认证。环境模式不提前返回，配置中暂时未生效的URL认证仍被保留检查。
- **实际回归**：无Key/无ExtraHeaders夹具中的`overridden URL remains stored`、`schemeless override`和`inactive environment URL`无敏感权限时拒绝、有权限时允许；`remove stored URL authentication`、`replace both stored credentials`允许普通管理路径；坏旧配置有权修复、引用/CA及不变配置用例继续通过。
- **边界**：以上是纯配置授权结果，不是代理连接或外部投递实测。旧URL中的认证确实替换/删除才属于对应来源已更换；仅加入覆盖字段不能证明原值已删除。无需继续以真实凭据或外部网络复现来关闭本项。

### 收敛评审7三项转交逐条处置

| 原条目 | 事实与处置 |
|---|---|
| 全局URL比较改为两边各自提取：用户名认证→含新密码，与旧密码→同值用户名的行为变化 | 行为变化属实，不能称完全等价。本轮独立合成检查确认：旧仅用户名认证改为同用户名加一个不同的新密码并换目标，当前允许；旧密码值被新地址用作用户名则拒绝。前者按新的非空密码作为新认证值，与已批准的新凭据允许分支相容；后者仍重复使用旧认证值，保守要求权限。helper注释现在明确逐侧提取规则，不再声称由旧地址决定两侧比较字段。仓库主测试未单列这两个精确组合，本轮通过scratch overlay亲测补证；未声称已测试外部代理是否接受新认证。 |
| 全局代理把被独立字段覆盖的URL认证仍计入，与厂商此前口径不同 | 差异已收敛。厂商也检查两种保存来源，接口指南与方案明确原因；全局原从严路径保留。本轮独立合成检查确认全局URL旧认证未删除而填入新独立密码时，改目标仍拒绝；厂商对应回归同样拒绝。该结论是“两来源都保护”，不声称两类数据结构或全部内部比较细节完全相同。 |
| 厂商列表跨来源匹配，例如旧独立密码等于新URL密码 | 保守匹配符合保留旧认证值需额外权限的规则，不因值移到另一字段就认定为全新。本轮独立合成检查确认该组合无权限拒绝、有权限允许；没有把这一拒绝当作故障。相同引用/相同文本可能造成从严结果，判断不尝试证明不同引用最终解析值相异，也不做外部解析；测试结果没有泛化成所有密钥后端语义已经实测。 |

需用户裁决：无新增；没有未关闭的本轮代码修复项。

## 挂账(Finding:范围内、裁决本轮不修、需登记 findings.md)

- 无新增挂账。R9本轮关闭；既有登录容量、时钟与诊断台账范围未改变。

## 转收敛评审

- 无新增。接口文字、注释及文件职责同步由收敛侧判断；本侧已核对其描述与实际行为不冲突。

## 验证记录

| 本轮实际命令 | 结果 |
|---|---|
| status、HEAD、stat及差量diff --check | 测试前后树干净，HEAD为de704b4c0；15文件差量纳入 |
| 上述期4预飞 | 全绿；`/tmp/rbac-review8-preflight.md` |
| EE：`GOWORK=off go test -race -count=1 ./internal/rbac/host` | 通过，1.460s |
| EE：`GOWORK=off go vet ./internal/rbac/host` | 退出0，无诊断 |
| EE：`GOWORK=off go test -race -count=1 -overlay /var/folders/m3/56qrlvk11xs7nc4cxjnw1b700000gn/T/rbac-review8-up8e8fae/overlay.json ./internal/rbac/host -run '^TestReview8TransferredSemantics$'` | 通过，1.095s；上述三项转交的精确合成组合，以及解析失败返回nil检查 |

overlay仅在scratch追加测试，没有修改仓库文件或生产源码。测试只构造合成配置并断言纯函数/策略结果；没有网络连接、已有服务访问或真实凭据读取。所有测试已结束。

### 起手表覆盖

| 方面 | 信号 | 结果 |
|---|---|---|
| 对照方案 | 验收标准未覆盖 | 无新增；R9组合及新认证允许分支已核对 |
| 对照方案 | 方案/实施/diff不一致 | 无；保存来源规则已登记 |
| 对照方案 | 多出功能/接口/依赖 | 无 |
| 对照方案 | 未登记偏差 | 无 |
| 行为正确 | 吞错/假成功 | 无；新地址解析错拒绝，旧地址解析错保守授权 |
| 行为正确 | 空/非空分支 | 无；nil配置、空凭据、独立字段、引用与URL分别核对 |
| 行为正确 | 超时取消后副作用 | 无新增；提取与比较均为纯计算 |
| 行为正确 | 重发双份副作用 | 不适用：未新增持久化步骤 |
| 行为正确 | 事务外写读/多方回滚 | 不适用：未修改事务与存储 |
| 行为正确 | 开关/旧数据/旧客户端 | 无；Env暂存URL启用、坏旧配置恢复已有回归 |
| 行为正确 | 校验次序/缺层 | R9已关闭；判定仍在保存前 |
| 行为正确 | 路由实际保护 | 无新增；敏感权限仍与模块权限组合，不写死chief |
| 行为正确 | 敏感日志/响应 | 无新增输出；不记录凭据或解析错误原文 |
| 行为正确 | 热路径I/O/无界增长 | 无；固定少量来源，无外部解析 |
| 行为正确 | 配置/部署依赖未登记 | 无 |
| 失败可观测 | 无法追溯失败 | 无新增错误出口；原固定错误类别保持 |
| 失败可观测 | 内部错误原文 | 无；解析错误转ErrInvalid |
| 失败可观测 | 清理失败现场 | 无；scratch与预飞保留 |
| 测试可信 | 新规则/失败无测试 | 无新增；主回归与独立转交检查分别记录 |
| 测试可信 | 断言仅跟实现 | 无新增；目标变化授权/拒绝、真正替换允许均有行为断言 |
| 测试可信 | 失败无法定位 | 无；子测试名称区分场景 |
| 测试可信 | 返回值未检查 | 无新增；S1额外检查返回nil与error |
| 测试可信 | 个人环境fixture | 无新增服务依赖；使用合成配置 |
| 测试可信 | 替代结果冒充真实 | 无；纯配置检查不表述为外部投递结果 |
| 测试可信 | 整体替换模块mock | 无新增；局部store夹具不用于宣称端到端通过 |
| 残留 | 调试/后门/本机配置 | 无 |
| 残留 | 未使用代码/无关改动 | 无 |
| 残留 | 生成物漂移 | 固定候选预飞通过 |

### 阅读范围、未执行项与开放式复读

- 完整阅读固定候选`credentials.go`、`providers.go`、`settings.go`、`credentials_test.go`，核对全部生产与测试差量；对照README、接口指南、方案收尾与验证增量；逐条读取收敛7三项转交及S1/S2原文。过程归档与生成变化按diff和预飞核实。
- 前轮已追到宿主代理字段保存/恢复、HTTP/SOCKS实际消费、SecretVar与纯URL解析，本轮这些宿主路径未改。重点复读当前提取、目标比较、策略错误映射：不再因暂时覆盖或Env类型提前丢弃保存来源；没有把提取结果写回配置。
- 未重跑全EE、全上游handlers、全构建、完整HTTP/WS及数据库升级回退：本轮范围是代理配置判定，已执行host全包与转交定向验证；身份和数据库代码无变化，前轮闭合证据不重复运行。实施方构建和端到端证据不列为本评审亲测。
- 未访问已有实例，未做攻击、凭据提取、真实代理/厂商请求或外部投递，未做前端、密钥后端或容量测试。未把静态疑虑直接标成实测缺陷。
- 已做开放式复读，检查保存来源是否遗漏、错误是否安全拒绝、有权者能否修复旧配置、全部新凭据是否仍允许；无表外新增发现。
