---
title: 角色权限后端 期4 收敛评审 #7
type: 收敛评审
case: 角色权限后端
phase: 4
round: 7
verdict: 有Blocking
created: 2026-09-17
---

> 本轮评审由启动脚本在新会话中执行，与实施上下文隔离。
> - 基线：`f371548b5444693d7e259033bec9f4a467e3748b`
> - 交付物：`45fb2abc909407914b3b8cdffc4ea851560f5122`，`git rev-parse HEAD` 的结果与此一致
> - 本轮是差量复核。上一轮候选是 `f1e39630ea7d52942525c0cce8db80fedb9157b1`，范围是 `git diff f1e39630e..45fb2abc9`
> - 工作树：`git status --porcelain --untracked-files=all` 输出为空，没有未暂存或未跟踪的文件
> - 差量中有 5 个 ee 文件，与指定清单一致；workbench 下的改动是过程文档，只作为裁决依据读取
> - 风险等级 L3，逐维留痕

## 结论

**总体判断：有Blocking，需要先完成 3 项小修，之后可以交付。**

本轮修复了代码评审 6 的 R9：厂商代理在更新时也要识别地址里的认证信息。
- **组织方向正确**：代理地址解析 `parseProxyURL` 和认证提取 `proxyURLCredential` 已移到 `credentials.go`，全局代理和厂商代理共用这一份。
- **边界未变**：模块边界、依赖方向和对外契约都没有变化。

以下 3 处会误导以后的维护者，属于本轮修复项：
- **N1 环境代理规则拆成两处**：`proxyCredentials` 对环境代理返回"没有凭据"，调用方再用一个 `ambient` 布尔值补回来。同一文件里的 `keyConnection` 已有 `ambient:*` 哨兵写法，这里没有沿用。
- **N2 注释描述旧算法**：`proxyURLCredential` 的注释还在描述旧的"由旧地址决定比较密码还是用户名"，实际代码是两边各自提取后再比较。
- **N3 README 过时**：`settings.go` 一行仍写"解析代理地址"，解析函数已经移走。

三项都是局部修改，不涉及用户裁决。

## 事实来源

**执行的命令**
- `git status --porcelain --untracked-files=all`（输出为空）
- `git rev-parse HEAD`
- `git diff --stat f1e39630e..45fb2abc9`
- `git diff f1e39630e..45fb2abc9 -- ee workbench/cases`
- `git grep -n -e parseProxyURL -e proxyURLCredential -e proxyCredentials -e ambient -- ee`
- `git grep -n -e providerDestination -e 代理 -- ee/internal ee/docs/角色权限接口.md`
- `git grep` 定位上游：`ConfigureProxy`、`ProxyConfig`、`SecretVarAsString`、`SecretVar` 方法、`buildProxyURLWithAuth`、`GlobalProxyConfig`

**先读的规则与裁决**
- 根 AGENTS.md、workbench/AGENTS.md
- 收敛评审.md、收敛评审审查方向.md、编码规范.md、流程.md、templates/收敛评审.md
- product/架构/EE后端架构.md（全文）
- workbench/findings.md（全文）
- 期4 方案v1 全文，包括「Gate 决策结果」「实施偏差记录」和第 108–110 行的「第六轮复核修正」
- 上一轮记录 收敛评审6.md（全文）
- 差量中的 代码评审6.md（只用来确认 R9 的来源和转交原文）

**完整阅读的范围内文件（当前现状）**

| 文件 | 行数 | 职责 |
|---|---|---|
| `ee/internal/rbac/host/credentials.go` | 124 | 统一比较规则（旧凭据用到新目标时判权），放代理地址解析和认证提取，以及 MCP 的 TLS/OAuth 边界 |
| `ee/internal/rbac/host/providers.go` | 271 | 隐藏、恢复厂商字段；检查厂商、代理和 Key 的凭据目标 |
| `ee/internal/rbac/host/settings.go` | 222 | 设置字段判权，隐藏设置字段，恢复并检查全局代理 |
| `ee/internal/rbac/host/credentials_test.go` | 461 | 按功能分组回归凭据目标的授权组合 |
| `ee/internal/rbac/host/README.md` | 51 | 包的文件分工和修改须知 |

**对照的同类实现与上游证据**
- **宿主厂商代理**（`core/providers/utils/utils.go`）：
  - `:665-676`（HTTP）和 `:692-703`（SOCKS5）只在 `Username.GetValue()` 与 `Password.GetValue()` 都非空时，才用它们覆盖 URL 内认证；
  - `:705-707` 的环境代理不读取配置里的认证；
  - `:717` 起，所有类型都会消费 `CACertPEM`。
  - 结论：`proxyCredentials`（`providers.go:136-157`）的覆盖顺序与上游一致；"环境代理两边都在用时，CA 变化也要判权"有依据。
- **宿主全局代理**：`core/network/http.go:333-343` 使用同样的覆盖规则，只是字段为普通字符串（`framework/configstore/tables/config.go:84-85`）。
- **SecretVar 语义**：
  - `SecretVarAsString` 对引用返回引用本身（`core/schemas/utils.go:39-47`）；
  - `GetValue` 返回已解析的值（`core/schemas/secretvar.go:511-516`）；
  - `IsFromSecret` 覆盖 env 和 vault 两种来源（`:174-179`）。
  - 结论：`providers.go:146`、`:152` 的判断方式与这些语义一致。
- **同文件已有写法**：
  - `keyConnection` 用 `"ambient:bedrock"` 等哨兵表示没有显式密钥的服务器身份（`providers.go:242-252`）；
  - `mcpDestination` 用 `retainedValues` 比较单个值（`credentials.go:104`）。
- **接入点**：`request.go:149` 用 `consoleError` 包装 `providerDestination` 的返回，所以函数内直接返回 `rbac.ErrInvalid` 与同函数其他出口一致。

## 上一轮「本轮修复项」处置

上一轮（收敛评审6）的「本轮修复项」为"无"，本轮没有需要处置的条目。上一轮的 K2 写的是"`parseProxyURL` 放在 `settings.go`"。本轮已按 R9 修复把它移到 `credentials.go`，K2 由本轮 K1 取代，见「看似可改但有理由保留」。

## 代码评审「转收敛评审」处置

原文为："无新增。共享隐藏规则和测试组织的正确性已核对，不替代收敛侧的组织结论。"

- 没有需要修复、撤回或挂账的转交条目。
- 共享隐藏规则和测试组织的收敛结论见下文：本轮 N1–N3 与 K1–K4。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | **有一项**：环境代理规则拆在两处（N1）。另有一条建议：`settings.go:173` 内联写了 `retainedValues` 的非空相等判断（S2）。<br>代理解析和认证提取已合为一份，两个调用方共用。 | `providers.go:119-122`、`:139`；`settings.go:173`；`credentials.go:24-31`、`:51-75` |
| 职责与实现质量 | **有两项**：注释与实际算法不符（N2）；出错时仍返回部分切片（S1）。 | `credentials.go:61-62`；`providers.go:155-156` |
| 架构与项目一致性 | 符合，逐项核对见下文两段。 | 见下文 |
| 契约一致性 | **无契约问题**。README 有一处文件说明过时（N3）。 | `角色权限接口.md:50`；`README.md:32` |

### 架构符合性（对照 EE 后端架构）

- **§4「模块内部的职责」、§7「适配归位判定」：符合。**
  - 代理地址解析和认证提取只服务于 rbac 的宿主判权，所以放在 `rbac/host/credentials.go`；
  - 没有进入 rbac 根包，也没有放进跨功能的 `internal/host`；
  - 厂商侧和全局侧仍分别在 `providers.go`、`settings.go` 接入，与"各功能文件负责接入、credentials.go 集中比较规则"的已确认组织一致（方案v1:64、:70）。
- **§7「复用完整操作」「核实接入影响」：符合。**
  - 判断顺序按上游 `ConfigureProxy` 和 `buildProxyURLWithAuth` 的实际覆盖顺序来写，没有另写一套代理实现；
  - 授权时不解析外部密钥（`providers.go:137` 已说明），没有新增网络调用。
- **§7「按缺口扩展」：符合。** 没有新增依赖或通用框架，只把包内函数搬到共享文件。
- **§3「总体布局」中 app 只做装配、§5 模块之间与事务、§6 插件：不适用。** 本轮差量没有涉及 app、跨模块数据、事务或插件。

### 依赖关系

- **import**：
  - `credentials.go:5` 新增 `net/url`；
  - `settings.go` 相应删除了 `net/url`（`:4-17`）；
  - `providers.go:4-15` 没有变化，新代码只用了原本就导入的 `schemas`。
  - 本轮没有新增第三方或上游包。
- **go.mod**：没有改动。
- **方向**：rbac/host 仍只依赖 rbac 根包，以及上游的 core、framework、transports；rbac 根包没有反向依赖。
- **窄接口与注入**：没有新增接口或构造参数。
- **资源所有权**：没有新增共享资源。

## 出口清单

### 本轮修复项

- **N1 环境代理的"保留凭据"规则拆在两处，没有沿用同文件的 `ambient:*` 哨兵写法**
  - **位置**：
    - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:139`
    - `/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:119-122`
  - **代码事实**：
    - `proxyCredentials` 遇到 `EnvProxy` 时返回 `nil, nil`，看起来表示"没有凭据"；
    - `providerDestination` 另外计算 `ambient`（两边都是 `EnvProxy`），再用 `oldErr != nil || ambient || retainedValues(...)` 合并；
    - 同一文件的 `keyConnection`（`:244-252`）表达同类"服务器身份"时，是在凭据列表里追加 `"ambient:bedrock"` 等哨兵。
  - **维护影响**：
    - 只读 `proxyCredentials` 的人会得出"环境代理没有凭据"，与 `:119` 的注释相反；
    - 以后若有第二个调用方（例如 Key 更新也要考虑厂商代理），很容易漏掉 `ambient` 这条规则；
    - 同一文件对同一概念用了两种写法。
  - **最小修正**：
    1. 在 `proxyCredentials` 中把 `EnvProxy` 分支改为返回 `[]string{"ambient:proxy"}, nil`，并在该处注明"环境代理可能携带部署凭据"；
    2. 删除 `:119-120` 和 `:122` 中的 `ambient ||`。
  - **行为不变的依据**：
    - 两边都是 env：哨兵互相匹配，`retainedValues` 为真；
    - 只有一边是 env：列表中没有共同的非空值，结果为假；
    - 任一边为 nil：两种写法结果相同。
  - **回归**：现有 `credentials_test.go:121` 的 "environment proxy" 子测试可直接覆盖。

- **N2 `proxyURLCredential` 的注释描述的是旧的不对称比较，与现在的逐侧提取不符**
  - **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/credentials.go:62`
  - **代码事实**：
    - 注释写的是"旧密码非空时比较密码……空密码时……改为比较用户名"，也就是由旧地址决定比较方式，这正是上一候选 `settings.go` 的算法；
    - 现在的函数只处理一个地址：有非空密码就返回密码，否则返回用户名（`:71-74`）；
    - 调用方对新旧两边各调用一次，再比较结果（`settings.go:167-173`、`providers.go:155`），并不存在"由旧地址决定"的步骤。
  - **维护影响**：
    - 注释与实现不一致，编码规范把这种情况视为缺陷；
    - 读者会按注释推断"旧地址无密码、新地址加了密码"的判定结果，实际结果恰好相反（见「转代码评审」第 1 条）。
  - **最小修正**：把 `:62` 改为说明逐侧提取的规则，例如："有非空密码时返回密码，因此只改用户名不算换凭据；否则返回用户名，因为宿主仍会发送只含用户名的认证。"不改代码。

- **N3 README 仍把代理地址解析写在 `settings.go` 名下**
  - **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/README.md:32`
  - **代码事实**：
    - 该行写的是"按宿主规则解析代理地址并隐藏认证信息"；
    - `parseProxyURL` 已移到 `credentials.go:51`，`settings.go` 只在 `:134` 调用它隐藏字段、在 `:167`/`:171` 调用 `proxyURLCredential`；
    - 同表 `:33` 已写明 `credentials.go` 负责"共用代理地址及认证解析"，两行说法重叠。
  - **维护影响**：想修改代理解析规则的人会先打开 `settings.go` 找，找不到。
  - **最小修正**：把 `:32` 的后半句改为"隐藏代理地址中的认证信息，并检查全局代理的凭据目标"，解析归属只保留在 `:33`。

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [角色权限后端]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- 全局代理的比较从"由旧地址决定比较密码还是用户名"改为"两边各自提取再比较"（`credentials.go:63-75`、`settings.go:167-173`）。例如：旧 `u@` 改为新 `u:x@` 以前算保留，现在不算；旧 `u:p@` 改为新 `p@` 现在算保留。请确认这一行为变化是否有意、测试是否覆盖。
- 全局代理没有按宿主覆盖顺序处理（宿主见 `core/network/http.go:335-340`）。独立的用户名和密码都已填写时，URL 内的旧认证仍计为保留，与厂商侧（`providers.go:144-148`）口径不同。请确认这种从严处理是否有意。
- `proxyCredentials` 把独立密码、URL 内认证和 URL 引用放进同一个列表（`providers.go:143-156`），`retainedValues` 会跨来源匹配，例如旧的独立密码等于新 URL 内的密码。请确认这种保守判定是否可以接受。

### 建议取舍项

- **S1 `proxyCredentials` 出错时仍返回部分切片**
  - **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/providers.go:155-156`
  - **代码事实**：`return append(credentials, credential), err` 在 `err != nil` 时也返回非空切片。现有调用方在 `:116` 和 `:122` 遇错时都会短路，不会用到这个值。
  - **维护影响**：Go 惯例是出错时返回零值，新的调用方可能误用这个部分结果。
  - **最小修正**：改为 `if err != nil { return nil, err }`，然后再 append。
  - **反证条件**：如果有调用方确实需要在解析失败时拿到已识别的独立密码，就保留现状，并在注释中写明。

- **S2 `settings.go:173` 内联写了 `retainedValues` 的非空相等判断**
  - **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-permissions/ee/internal/rbac/host/settings.go:173`
  - **代码事实**：`oldCredential != "" && oldCredential == nextCredential` 与 `credentials.go:24-31` 的 `retainedValues` 语义相同；`mcpDestination`（`credentials.go:104`）比较单个值时用的是 `retainedValues`。
  - **维护影响**：以后若要调整"空值不算凭据"这条规则，需要改两处。
  - **最小修正**：改为 `retainedValues([]string{oldCredential}, []string{nextCredential})`。不要与 `:165` 的独立密码合并，否则会引入跨来源匹配，改变行为。
  - **反证条件**：如果实施者认为 `:165` 与 `:173` 两行使用相同的内联写法更便于对照阅读（`:165` 是既有代码），可以撤回。

### 看似可改但有理由保留

- **K1 代理解析和认证提取放在 `credentials.go`，没有留在 `settings.go` 或放进 `response.go`**
  - 理由：两个使用方是 `settings.go`（全局代理）和 `providers.go`（厂商代理），`credentials.go` 本来就是凭据比较的共享文件；隐藏字段只借用这个解析函数，共用的判断仍在 `response.go`。README `:33` 已写明。本项取代上一轮的 K2。
- **K2 全局代理和厂商代理仍分开计算"是否保留凭据"，没有合成一个函数**
  - 理由：两边的数据形状不同。全局代理是 `tables.GlobalProxyConfig` 的普通字符串，只有一个类型字段；厂商代理是可以引用 env/vault 的 `SecretVar`，需要区分"已解析"和"只有引用"（`providers.go:146`、`:152`）。只共用提取函数就足够，强行合并需要额外的适配层。两边口径差异是否有意，已转代码评审。
- **K3 `credentials_test.go` 为 461 行**
  - 理由：超过 300 行，但未达 500 行的强制拆分线；方案v1:110 已记录评估。新增的 `TestProviderProxyCredentialDestination` 与厂商、全局代理用例同属"凭据换目标"这一授权主题，放在同一文件便于对照阅读。
- **K4 新测试在子测试里补默认 `Type`（`:98-103`），没有在表中逐行写出**
  - 理由：8 个用例中有 7 个的类型都是 HTTP，只有 "proxy type" 需要区分。在循环里补默认值减少了重复，表中各行仍能看清差异。
- **K5 `providerDestination` 对新配置解析失败时直接返回 `rbac.ErrInvalid`（`:117`），没有调用 `consoleError`**
  - 理由：同一函数的其他出口也直接返回 rbac 错误，统一由 `request.go:149` 包装；`settings.go` 的 `proxyUpdate` 自带 `consoleError`，是因为它是宿主直接调用的回调。两处写法不同，是因为接缝不同。
- 上一轮的 K1、K3、K4，以及更早的保留项，仍然成立。

## 台账对账

- **FIND-022**：本轮没有改动错误转换。厂商路径仍经 `request.go:149` 的 `consoleError` 映射，全局路径仍在 `proxyUpdate` 内映射，未恶化。
- **FIND-017、FIND-018、FIND-021**：本轮没有涉及存储、登录事务和时钟，未恶化。
- 其余 Finding 与本轮无关。

---

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无：本轮没有新增数字或限制；测试中的 `8080` 是夹具 |
| 重复 | 同一规则多份实现且互不注明 | **发现 N1**：环境代理规则分在 `providers.go:139` 和 `:120`。代理解析与认证提取已合为一份 |
| 重复 | 重造已有 helper | **建议 S2**：`settings.go:173` 内联了 `retainedValues` 的语义 |
| 重复 | 派生常量无推导 | 无 |
| 职责与实现质量 | 魔数（锁键、端口、ID、阈值） | 无 |
| 职责与实现质量 | 状态码、错误码裸数字 | 无：使用 `rbac.ErrInvalid`、`rbac.ErrForbidden` |
| 职责与实现质量 | 无名的错误形状（以元组表示失败） | **建议 S1**：出错时仍返回部分切片；其余函数返回标准的 `(T, error)` |
| 职责与实现质量 | 紧凑但绕的控制流 | **发现 N1**：`ambient` 布尔值与凭据列表并行，都是为了表达"保留凭据"；`settings.go:173` 的 `\|\|`/`&&` 混写按 Go 优先级读法正确 |
| 职责与实现质量 | 文件集中或拆分是否妨碍理解 | 无：见 K1、K3 |
| 职责与实现质量 | 多余状态或守卫 | **发现 N1**：`ambient` 是可以并入凭据列表的多余状态；`providers.go:146` 的"独立字段已解析"守卫防的是宿主覆盖条件，不是重复守卫 |
| 职责与实现质量 | 同一效果做两次 | 无：每一侧只调用一次 `proxyCredentials` 或 `proxyURLCredential` |
| 职责与实现质量 | 注释与现状矛盾 | **发现 N2**（`credentials.go:62`）、**N3**（`README.md:32`）；`providers.go:136-137`、`:149`、`:121`、`settings.go:166`、`:172` 与代码一致，`:136` 已对照上游 `utils.go:665-676` 核实 |
| 架构与项目一致性 | 接缝形状与同类不同 | 一致：厂商检查仍是 `request.go:149` 调用的纯函数，全局代理仍是宿主可选回调；两者错误映射各自沿用原接缝（K5） |
| 架构与项目一致性 | 架构符合性 | 符合：§4、§7 见正文；§3 的 app 装配、§5 跨模块与事务、§6 不适用（本轮没有 app、跨模块、事务和插件改动） |
| 架构与项目一致性 | 依赖关系 | 符合：`net/url` 在包内挪动，没有新增依赖，go.mod 未变，方向不变，没有新增注入或共享资源 |
| 架构与项目一致性 | 绕过既有出口 | 无：仍经 `providerDestination`/`proxyUpdate` 接入，经 `credentialDestination` 统一判权 |
| 架构与项目一致性 | 把上游现状当契约 | 无：覆盖顺序、环境代理、CA 消费和 SecretVar 引用语义都已在上游源码逐行核实；全局与厂商口径差异已转代码评审 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无契约问题：`角色权限接口.md:50` 已涵盖"保留旧凭据改代理或 TLS 信任需要该权限"，环境代理属于其中的服务器身份，和云厂商的服务器身份一样没有单列；前端不在范围。README 过时见 N3 |
| 契约一致性 | 状态码口径不一 | 无：解析失败仍映射为 invalid，拒绝仍为 forbidden |
| 契约一致性 | 省略、空、值三态语义不对应 | 无：本轮没有字段变化；空 URL 和空独立字段按"没有凭据"处理，与上游 `GetValue() != ""` 的判断一致 |

### 实际静态阅读范围与未执行项

**已读**
- 见「事实来源」。差量内的 5 个 ee 文件都读了当前完整内容。
- 上游读了以下代码段：
  - `core/providers/utils/utils.go:640-719`
  - `core/schemas/provider.go:262-291`
  - `core/schemas/utils.go:30-47`
  - `core/schemas/secretvar.go:165-219`、`:505-519`
  - `core/network/http.go:325-354`
  - `framework/configstore/tables/config.go:78-93`
- 用 `git grep` 核对了 `request.go:149` 的调用，以及接口指南 `:50` 的原文。

**开放式复读：已完成，有一项表外判断，已并入 N2 和转代码评审第 1 条。** 以"半年后要改这里的人"的视角逐个文件复读：
- `credentials.go`：第一眼会皱眉的是 `:62` 的注释讲的是新旧两边的比较，而函数只接收一个地址。由此追查出行为口径的变化，这就是 N2 和转代码评审第 1 条。
- `providers.go`：第一眼会皱眉的是 `:139` 说环境代理"没有凭据"，`:119` 又说"可能携带部署凭据"，即 N1；另一处是 `:155-156` 出错时仍返回部分值，即 S1。
- `settings.go`：`:166-173` 读起来通顺；`:174` 多了一个空行，不影响理解，不提。
- `credentials_test.go`：新测试的两张表分别覆盖"改目标"和"只改 CA"，子测试名能定位用例，没有表外发现。
- `README.md`：`:32` 的内容过时，即 N3。

**未执行**
- 没有运行任何测试、构建、vet 或 gofmt（只读约束），行为等价性只做了静态推理（N1）。
- 没有读 `workbench/evidence/` 下的验证记录。
- 代码评审6只用来确认 R9 的来源和转交原文，没有采信其中的正确性结论。
- 没有通读差量以外的 `request.go`、`response.go`：本轮这两个文件没有改动，只用 `git grep` 核对了调用行。
- 没有读 fasthttpproxy 和 x/net/proxy 的源码：本轮结论不依赖它们，上一轮已核实。
