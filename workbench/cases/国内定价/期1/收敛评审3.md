---
title: 国内定价 期1 收敛评审 #3
type: 收敛评审
case: 国内定价
phase: 1
round: 3
verdict: 有Blocking
created: 2026-09-13
---

> 评审者与实施上下文隔离。本轮是差量复核：上一轮候选 `c7f8ed235f8cc295a06d06905a7464a7746014a8`，本轮候选 `f6311fd4bbffec6b59adc5fa03f343a3053f6448`（工作树干净，没有未跟踪文件）。基线 `f371548b5444693d7e259033bec9f4a467e3748b`，风险等级 L2。

## 结论

上一轮的 F1、F2、F3 都已修复。

本轮代码只有一处实质改动，是代码评审 R2 的修复：新增 `normalizeHost`，解析出的主机和白名单主机都经它转小写，并去掉一个末尾根域点。实现有以下特点：

- 只在 `vendors.go` 里实现一份，两个调用点共用；
- 注释写明了为什么只去掉一个点；
- 测试把两种白名单写法和各类地址两两组合；
- 冒烟把原来的两次重启改成用两种等价地址重启，没有增加启动次数。

结构和依赖方向没有变化。

修复同时留下一处小的说明不同步：

- 使用说明 `ee/docs/国内定价.md` 的匹配规则没有补上"忽略大小写和单个末尾根域点"，而包 README 已经写了；
- `ee/Makefile` 里 `smoke-pricing` 的帮助文字没有提到它先运行的失败现场回归脚本。

这项修复并经定向复核之前，verdict 保持有Blocking。它在授权范围内，不需要用户裁决。

## 事实来源

**执行的命令**
- `git status --porcelain --untracked-files=all`：输出为空。
- `git rev-parse HEAD`：`f6311fd4bbffec6b59adc5fa03f343a3053f6448`。
- `git diff --stat c7f8ed235..f6311fd4b`：共 18 个文件。其中 9 个是本轮评审范围内的 ee 文件，与任务清单一致；另外 9 个是 workbench 的记录、生成物和 `tools/code_review.py`，不在评审范围。
- `git diff c7f8ed235..f6311fd4b -- ee`：逐个 hunk 对照。
- `git log --oneline c7f8ed235..f6311fd4b`：3 个提交。
- `git ls-files ee/scripts`：`pricing-smoke-test.py` 已被跟踪。

**读过的规则与裁决**
- 根 `AGENTS.md`、`workbench/AGENTS.md`。
- 规范：`规范/流程.md`、`流程/收敛评审.md`、`流程/收敛评审审查方向.md`、`项目/编码规范.md`。
- `templates/收敛评审.md`。
- `product/架构/EE后端架构.md` 的 §4、§5、§7。
- `workbench/findings.md` 全文。
- `方案v2.md` 第 115–414 行：§4.3–§9、「Gate 决策结果」、「实施偏差记录」第 1–6 条。
- 上一轮记录 `收敛评审2.md` 全文。

**同类实现**
- `ee/scripts/identity-smoke.py:36-93`：`Node.__init__` 写 `config.json`，以及 `start`、`stop`。
- `ee/scripts/identity-upgrade-smoke.py:16`：直接改写 `config.json` 的先例。
- `ee/internal/pricing/pricefile.go` 全文：`endpoint_hosts` 的校验在 :127-134。
- `pricefile_test.go:11`：`validPrices` 的第一个厂商是 dashscope，这是 `vendors_test.go:51` 能直接改写 `f.Vendors[0]` 的前提。
- `ee/docs/国内定价.md:15-77`。
- `checklist.yaml`：用 grep 核对冒烟相关行。

**关键假设核实**
- **主机归一只有一份实现**：对 `ee/` 做 grep `normalizeHost|endpointHost\(|ToLower\(allowed`，只命中 `vendors.go:87,91,116,127,131` 和测试 :69。旧的 `strings.ToLower(allowed)` 已经没有残留。
- **ee 内没有其他主机末尾点处理**：Go 代码里 `TrimSuffix(..., ".")` 只命中上游 `core/providers/githubcopilot/token.go:462`，与本模块无关。所以本案没有重造 ee 已有的 helper。
- 本轮结论不依赖上游行为；`url.Parse` 和 `Hostname()` 是标准库。

### 上一轮本轮修复项的处置

| 项 | 处置 | 证据 |
|---|---|---|
| F1 改名残留 | 已修复 | `sync.go:118-121` 的循环变量改为 `hit`，不再遮蔽 `vendors.go:19` 的类型 `match`。`persistence/store.go:27,40,45,50` 的四个接收者统一为 `o`，与同文件的 `c`（:63,72）、`p`（:83）规则一致。diff 只有改名 |
| F2 回归脚本无入口；`ee/README.md` 未同步 | 已修复 | ① `ee/Makefile:51` 在 HTTP 冒烟（:52）之前运行 `pricing-smoke-test.py`，这是上一轮最小修正给出的二选一中的一个。② `pricing-smoke-test.py:8` 把 `sqlite3` 移到文件头，顺序与 `pricing-smoke.py:4-14` 一致。③ `ee/README.md:17` 模块表加了 `internal/pricing`，:35 更新了 `smoke` 的说明，:36 补了 `smoke-pricing`，:44 说明了定价冒烟和回归脚本。附带的帮助文字遗漏见本轮 R1 |
| F3 包 README 写入过程叙述 | 已修复 | `pricing/README.md:35` 改为长期约束："共享内存夹具……场景分组增长明显时按职责评估拆分，并遵守编码规范的文件行数上限"，不再有"本轮"和具体行数 |

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。"只去一个末尾点"只在 `vendors.go:132` 表达；测试和冒烟里的地址是契约字面量 |
| 重复 | 同一规则多份实现且互不注明 | 无。解析主机（:127）和白名单（:91）共用 `normalizeHost`，替代了原来各自 `ToLower` 的写法 |
| 重复 | 重造已有 helper | 无。ee 内没有既有的主机归一函数，见关键假设 |
| 重复 | 派生常量无推导 | 不适用。本轮没有新增常量 |
| 职责与实现质量 | 魔数 | 无 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用。本轮没有接口出口改动 |
| 职责与实现质量 | 无名的错误形状 | 无 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无。`pricing-smoke.py:152-159` 用重启地址做循环变量，把"重启两次"和"换等价地址"合成一个循环，注释 :151 写明了意图；首跑发现多加重启会触发登录限流，这样合并正是修复办法（方案v2 第 407 行） |
| 职责与实现质量 | 文件集中或拆分 | 无。`normalizeHost` 与唯一的使用方同在 `vendors.go`（133 行）；`vendors_test.go` 为 103 行 |
| 职责与实现质量 | 多余状态或守卫 | 无 |
| 职责与实现质量 | 同一效果做两次 | 无。`pricing-smoke.py:155` 改写 `config.json`，与父类 `identity-smoke.py:50-52` 的首次写入不是重复，保留理由见台账对账 |
| 职责与实现质量 | 注释与现状矛盾 | **发现** R1（b）：`ee/Makefile:50` 的帮助文字只写 HTTP 冒烟的内容，没有反映 :51 先运行的回归脚本。`vendors.go:130`、`pricing-smoke.py:151`、`pricing/README.md:11,35` 都与代码一致 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。归一化留在厂商识别的纯函数里，与 `ParseVendorMap` 在边界处做键小写（`vendors.go:32`）是同一类处理 |
| 架构与项目一致性 | 架构符合性 | 无。详见四维核查 |
| 架构与项目一致性 | 依赖关系 | 无。详见四维核查 |
| 架构与项目一致性 | 绕过既有出口 | 无。`store.go` 只改了接收者名，仍走上游 `Create/Update/DeletePricingOverride` 与 `GetPricingOverrides` |
| 架构与项目一致性 | 把上游现状当契约 | 无。本轮没有新增对上游行为的依赖 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **发现** R1（a）：`ee/docs/国内定价.md:22` 的匹配规则没有列出大小写和末尾根域点的归一，与 `pricing/README.md:11` 及 `vendors.go:127,131-133` 不一致 |
| 契约一致性 | 状态码口径不一 | 不适用。没有接口改动 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。空 `BaseURL` 仍在 `vendors.go:117` 返回空串，:92 的 `host != ""` 守卫不变；`normalizeHost("")` 不会产生非空值 |

### 开放式复读

已按"半年后要改这里的人"的视角，把 9 个范围内文件的完整现状重读一遍：

- **`vendors.go`**：读者第一眼会问"为什么不在加载价格文件时一次性归一白名单"。现在的写法让规则和唯一使用点在同一屏里，代价只是每次匹配重复做小写和去点，可读性上可以接受；启动一次的性能不在本评审范围。是否应在 `validate()` 拒绝多个末尾点的白名单，属于校验正确性，已转代码评审。
- **`vendors_test.go`**：14 个用例乘 2 种白名单写法，子测试名形如 `root dot/DASHSCOPE.ALIYUNCS.COM.`，失败时能直接定位。它依赖 `validPrices` 的第一个厂商是 dashscope，这个前提在同包 `pricefile_test.go:11`，不跨包。
- **`pricing-smoke.py`**：在循环中改写 `node.config` 并重写配置文件，读起来直接。两条 PASS 输出（:160-161）与断言对应。
- **`ee/README.md`、`pricing/README.md`、`store.go`、`sync.go`、`pricing-smoke-test.py`**：没有让人皱眉的地方。

表外发现：无。R1（b）的 Makefile 帮助文字已在起手表"注释与现状矛盾"一行记录。

### 实际阅读范围与未执行项

**完整读过**
- `ee/internal/pricing/{vendors.go,vendors_test.go,sync.go,pricefile.go,README.md}`
- `ee/internal/pricing/persistence/store.go`
- `ee/Makefile`、`ee/README.md`
- `ee/scripts/pricing-smoke.py`、`ee/scripts/pricing-smoke-test.py`

**部分读过**
- `ee/docs/国内定价.md` 第 15–77 行。
- `ee/scripts/identity-smoke.py` 第 30–100 行。
- `product/架构/EE后端架构.md` 的 §4、§5、§7。
- `方案v2.md` 第 115–414 行。
- 用 grep 读取：`checklist.yaml`、`pricefile_test.go:11-68`、`identity-upgrade-smoke.py` 中的 `config.json` 行。

**未执行**
- 按只读约束，没跑 go build、vet、test，没跑 Python 测试、冒烟和 preflight。
- 没读 `app/pricing.go`、`overrides.go`、`model.go`、`sync_test.go`、`persistence` 下的测试与 README；本轮没有改动它们，上一轮已完整阅读。
- 没读 `cn-prices.json`、价格核对记录、预飞事实表。
- 按要求没读代码评审记录和方案评审记录。
- 没评审 workbench 的 `tools/code_review.py` 及生成物。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无。主机归一收成一个函数 | `vendors.go:91,127,131-133`；grep 没有其他实现 |
| 职责与实现质量 | 无代码问题；Makefile 帮助文字没跟上执行步骤（R1b） | `ee/Makefile:50-52` |
| 架构与项目一致性 | 无变化 | 见下文 |
| 契约一致性 | 使用说明的匹配规则没有同步归一化行为（R1a） | `ee/docs/国内定价.md:22` 对照 `pricing/README.md:11` |

**架构符合性（对照 EE 后端架构 §4、§7）**
- **§4 根包承载规则**：主机归一属于厂商识别规则，放在根包 `vendors.go` 的非导出函数里，没有下沉到 `persistence` 适配层，也没有让 app 预处理 `BaseURL`。`persistence/store.go:94-96` 仍然原样传递 `NetworkConfig.BaseURL`，不做判断。
- **§7 适配归位**：`store.go` 本轮只改接收者名，三个适配器依然只包库类型，不接触宿主对象。
- **app 职责**：`app/pricing.go` 本轮没有改动，装配形状与上一轮一致。

**依赖关系（对照架构 §5、编码规范「依赖关系」）**
- **import 不变**：`vendors.go:4-9` 仍只用标准库（`fmt`、`net/url`、`sort`、`strings`）；`store.go:4-14` 的 import 不变；`vendors_test.go:4-8` 只用标准库。
- **导出面**：新增的 `normalizeHost` 不导出，只有同包使用。没有新增导出符号，也没有改动接口。
- **注入与资源所有权**：`sync.go:19-49` 的四个窄接口和 `Deps` 不变；没有新增共享资源，也不涉及其他 EE 模块。

## 出口清单

### 本轮修复项

**R1 归一化规则与 `smoke-pricing` 新步骤没有同步到使用说明和帮助文字**

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/docs/国内定价.md:22`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/Makefile:50`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:127,131-133`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/README.md:11`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/README.md:36,44`
- **代码事实**
  - （a）使用说明逐条列了匹配规则："支持等于或真实子域；无 scheme 时先补 `https://`；`dashscope-intl` 不匹配北京域名"。但没有写：`base_url` 主机和 `endpoint_hosts` 都忽略大小写，并去掉一个末尾根域点，多个末尾点不算等价。包 README :11 已经写了这条。
  - （b）`smoke-pricing` 的帮助文字是"隔离 SQLite 国内定价同步、重启幂等与汇率冒烟"，而该目标 :51 先运行 `pricing-smoke-test.py` 失败现场回归，再运行 :52 的 HTTP 冒烟。`ee/README.md:36` 的说明是"国内定价失败现场回归与 HTTP 冒烟"，与帮助文字不一致。
- **维护影响**
  - 使用说明是运维配置 `base_url` 和维护外部 `EE_PRICING_FILE` 中 `endpoint_hosts` 的依据（:35-37）。按现有文字，读者判断不出 `DASHSCOPE.ALIYUNCS.COM.` 或末尾带点的白名单能不能匹配。
  - 规则现在分成两份说明，一份完整、一份缺项，以后调整归一规则时容易只改一处。
  - `make -C ee help` 是找目标的入口，帮助文字漏掉一个实际执行的步骤。回归失败时，读者无法从帮助文字知道失败来自哪一步。上一轮 R6(a) 修的就是同一行文字与执行顺序不一致的问题。
- **最小修正**
  - （a）在 `ee/docs/国内定价.md:22` 的匹配规则里补一句："主机名与白名单均忽略大小写，并各去掉一个末尾根域点（`dashscope.aliyuncs.com.` 等价）；多个末尾点不视为等价。"
  - （b）把 `ee/Makefile:50` 的帮助文字改为与 `ee/README.md:36` 同口径，例如"定价失败现场回归，再做隔离 SQLite 同步、重启幂等与汇率冒烟"。
  - 只改文字，不改行为。

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [国内定价]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- `ee/internal/pricing/pricefile.go:130-133`：`validate()` 不拒绝含多个末尾点（如 `a.com..`）或只有一个点（`.`）的 `endpoint_hosts`。前者经 `normalizeHost` 后永远匹配不上，而且不报错；后者归一为空串，`vendors.go:92` 的 `strings.HasSuffix(host, "."+"")` 可能匹配任意以点结尾的主机，需核实。
- `ee/scripts/pricing-smoke.py:152-159`：两次重启都换了地址，原地址 `dashscope.aliyuncs.com` 下"未改配置直接重启"的零变更路径不再单独覆盖，需核实这是否削弱方案 §6 冒烟步 3 的幂等证明。

## 台账对账

**涉及的已有 Finding**
- **FIND-012**（attach 惯性点）：本轮没有改动 `bootstrap.go` 和 `app/pricing.go`，未恶化。
- FIND-013 至 FIND-021：本轮未触及。
- 本轮不新增 Finding。

**看似可改但有理由保留**

| 事项 | 保留理由 |
|---|---|
| `normalizeHost` 在每次匹配时对白名单调用（`vendors.go:91`），而不是加载文件时一次性归一 | 规则与唯一使用点在同一文件相邻处，一眼能看全。若在 `parsePriceFile` 里改写 `Vendor.EndpointHosts`，业务模型里保存的就不再是文件原值，还会把识别规则分散到两个文件。启动期只执行一次，没有维护收益值得换 |
| `pricing-smoke.py:155` 直接改写 `config.json`，没有抽夹具方法 | 方案 §8 禁止修改 `identity-smoke.py`。同目录 `identity-upgrade-smoke.py:16` 也是直接改写。文件已存在，改写会保留父类 `chmod 0o600` 设置的权限。只有一处使用，抽方法不会减少阅读成本 |
| `vendors_test.go:48` 让所有用例在两种白名单写法下各跑一遍 | 归一函数同时作用于两侧（:91 与 :127），交叉覆盖正是要证明的对称性；子测试名带白名单值，失败时可以直接定位 |
| `smoke-pricing` 在 HTTP 冒烟前运行回归脚本，但 `smoke` 目标（`Makefile:42-45`）和 `checklist.yaml` 没有运行 | 上一轮 F2 的最小修正允许二选一，实施选了 Makefile 入口（方案v2 第 405 行）。`smoke` 定位是聚合各功能的 HTTP 冒烟，不承担脚本自测 |
| 上一轮"看似可改但有理由保留"表中各项 | 本轮未恶化，不再重提 |
