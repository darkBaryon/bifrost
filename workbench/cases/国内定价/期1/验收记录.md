---
title: 国内定价 期1 验收记录
type: 验收记录
case: 国内定价
phase: 1
created: 2026-09-15
---

## 验收结论

**通过。** 2026-09-15 用户在本机实测后拍板 Gate 2。验收对象 `670ce3fd0`（代码最后改动 `3a00bf7f6`），基线 `f371548b5444693d7e259033bec9f4a467e3748b`，分支 `feat/domestic-pricing`。

## 交付物核对

对照[需求验收口径](../../../reports/国内定价.md)与产品方案第六节七条：

| 验收条 | 结果与证据 |
|---|---|
| 1 自定义厂商调用收录模型，费用等于单价乘 token 数 | **实测通过**。本机 `qwen` 厂商真实调用 `qwen-plus`，11 输入 + 3 输出，日志费用 `2.0555555555556e-06` 美元；手算 11×1.1111e-7 + 3×2.7778e-7 = 2.0556e-6 一致。同库中改动前的 `qwen-turbo` 调用费用为 `0.0`，对照成立 |
| 2 与控制台自定义定价页显示一致 | **实测通过**。模型目录详情显示 `$0.11 / 1M tokens` 并标「已应用」，定价覆盖页同行 patch 值与之对应 |
| 3 重启两次行数不变 | **实测通过**。第二次启动 `created=0 updated=0 deleted=0 unchanged=65`；冒烟另含一次不改配置的重启断言 |
| 4 手工覆盖不受影响 | 冒烟覆盖；所有权同时核验保留前缀与 UUID，前缀伪装只告警不接管 |
| 5 价格文件非法时正常启动、日志明确、旧行仍在 | 冒烟覆盖 |
| 6 未收录模型行为不变 | 冒烟反查 + 精确匹配源码 + 费用引擎测试 |
| 7 未知域名不写入并提示 | 冒烟覆盖；实测 `unrecognized=[mystery]` |

方案验证策略的机械项见[预飞事实表](../../../evidence/国内定价-期1-预飞事实表.md)，14 条全绿。

额外实测（超出验收口径，用户现场要求）：新增「智谱」自定义厂商后重启，`created=19 updated=0 deleted=0 unchanged=65`，中文厂商名照常按接入主机名识别；价格文件置空后重启 `deleted=65`，回滚路径成立。

## 验收方式

**实测。** 用户在本机真实部署（`~/.config/bifrost`）启动候选二进制，登录控制台查看定价覆盖页与模型目录，发起真实计费请求核对日志费用，并对照一个「导入前」状态的隔离实例（数据库副本 + 空价格文件）。数据库改动前已备份。

## 度量（标准级以上）

| 阶段 | 模型 | 起止时刻 | token |
|---|---|---|---|
| 计划 | claude-fable-5-1（产品方案与工程方案 v1/v2）、gpt-6-astra（上游链路排查） | 2026-09-13 01:54–06:00 | 缺失 |
| 评审 | 方案复核 ×2 claude-fable-5-1；代码评审 ×4 gpt-6-astra/high；收敛评审 ×5 claude-opus-5/high | 2026-09-13 05:58–07:30 | 缺失 |
| 开发 | gpt-6-astra/high（步骤 1–6 与三轮评审修复） | 2026-09-13 06:07–07:10 | 缺失 |
| Gate | 用户 | 2026-09-13 Gate 1；2026-09-15 Gate 2 | 缺失 |

token 用量未逐会话采集，标缺失；不估编。

## 归档动作

- 方案 status → 已归档
- 需求 status → 已交付
- 长期口径回填：
  - [EE 后端架构](../../../../product/架构/EE后端架构.md)第 3 节模块树新增 `internal/pricing/`，并说明它只有启动期入口、写上游既有表。
  - `ee/README.md` 模块表、冒烟命令与说明已在实施期同步。
  - 业务规则的长期形态是单元测试与三个冒烟脚本；使用说明与回滚见 `ee/docs/国内定价.md`。
  - 遗留风险登记 [FIND-022](../../../findings.md)（评审漏掉规范明文违规）、[FIND-023](../../../findings.md)（上游覆盖匹配区分大小写）。
  - Kimi 0 条收录属已知缺口，记在方案实施偏差记录，不单独立债。

## 集成与收口（2026-09-15）

合入 develop 的合并提交 `e8b9e4765`（`--no-ff`），合入对象即验收对象 `670ce3fd0`。合入后在 develop 上跑最小冒烟：`go vet`、`go test ./...`、`make -C ee build`、定价冒烟、品牌与身份回归、`build_views --check`，六项全绿；上游五目录与基线 `f371548b5` 仍为零差异。
