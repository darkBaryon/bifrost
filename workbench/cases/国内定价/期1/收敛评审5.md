---
title: 国内定价 期1 收敛评审 #5
type: 收敛评审
case: 国内定价
phase: 1
round: 5
verdict: 无碍
created: 2026-09-13
---

> 评审者与实施上下文隔离。本轮是差量复核：上一轮候选 `1089ea233e80a7dd919ca3f85ea9d5e65a7a6bf4`，本轮候选 `82aa575dade9b74b47336c6ad955af019adfb3bd`。工作树干净，没有未跟踪文件。基线 `f371548b5444693d7e259033bec9f4a467e3748b`，风险等级 L2。

## 结论

本轮适合交付，verdict 为无碍。

**上一轮 R1 已修复。** `ee/docs/国内定价.md` 原来的第 24 行被拆成两段：
- 第 24 行只讲 `base_url` 主机名与白名单的归一方式，并写明 `base_url` 带多个末尾点时按未识别处理。
- 第 26 行单独讲白名单校验：归一后为空、以点开头或以点结尾时，整份价格文件无效，按第 56 行的「价格文件解析/校验失败」处理。

两段都与代码一致：
- 归一只由 `vendors.go:131-133` 实现。
- 匹配逻辑在 `vendors.go:87-104`。
- 校验逻辑在 `pricefile.go:134-137`。
- 文件无效时 `pricefile.go:97-99` 返回空目录。
- 测试为 `vendors_test.go:40` 与 `pricefile_test.go:17-19`。

包 README 第 8 行写了"主机归一后须非空且首尾无点"，使用说明现在与它口径一致。本轮只改了文档文字，代码、依赖方向和模块边界都没有变化。

**阅读深度：** 本轮范围只有一个文档文件，按 L2 差量复核读完整现状，并逐句对照代码核实，没有重做整包评审。

## 事实来源

**执行的命令**
- `git status --porcelain --untracked-files=all`：输出为空，工作树干净，没有未跟踪文件。
- `git rev-parse HEAD`：`82aa575dade9b74b47336c6ad955af019adfb3bd`。
- `git log --oneline 1089ea233..82aa575da`：共 2 个提交。
  - `0e848f9e6` 是收敛评审 #4 的记录。
  - `82aa575da` 是使用说明修复。
- `git diff --stat 1089ea233..82aa575da`：共 7 个文件。
  - 评审范围内只有 `ee/docs/国内定价.md`（+3/-1）。
  - 其余是 workbench 的评审记录、生成物（`SUMMARY.md`、`assets/nav.js`、`index.md`、`views/all.md`），以及方案v2 新增的「第四轮评审修复」段落，不在评审范围内。
- `git diff 1089ea233..82aa575da -- ee workbench/cases/国内定价/期1/方案v2.md`：逐个 hunk 对照。ee 下只有 `ee/docs/国内定价.md` 一个 hunk。

**读过的规则与裁决**
- 根 `AGENTS.md`、`workbench/AGENTS.md`。
- 规范：`规范/流程.md`、`规范/流程/收敛评审.md`、`规范/流程/收敛评审审查方向.md`、`规范/项目/编码规范.md`。
- `templates/收敛评审.md`。
- `product/架构/EE后端架构.md` §4–§8。
- `workbench/findings.md` 全文。
- `方案v2.md` §9「Gate 决策结果」（:277-298）、「实施记录」「实施偏差记录」第 1–7 条，以及「第四轮评审修复」（:372-420）。
- 上一轮记录 `收敛评审4.md` 全文。

**同类实现与对照**

这次是纯文档改动，没有可比的新代码。对照对象是被描述行为的实际实现，以及同一规则的另一份说明：
- `ee/internal/pricing/vendors.go` 全文：归一在 :130-133，匹配和未识别日志在 :87-104，URL 主机提取在 :116-128。
- `ee/internal/pricing/pricefile.go` 全文：校验在 :127-138，文件无效时返回空目录在 :97-99。
- `ee/internal/pricing/README.md` 全文：:8 写了校验规则，:11 写了匹配规则。
- 测试（用 grep 定位）：`pricefile_test.go:17-19` 拒绝 `.`、`a.com..`、`.a.com`；`vendors_test.go:40` 断言 `https://dashscope.aliyuncs.com..` 不匹配。

**关键假设核实**

本轮结论不依赖上游行为，逐句核对如下：

1. **"`base_url` 带多个末尾点时不匹配该厂商，按未识别处理"（使用说明 :24）**
   - `normalizeHost` 只去掉一个点，`dashscope.aliyuncs.com..` 归一后仍是 `dashscope.aliyuncs.com.`。
   - 这个值既不等于白名单，也不以 `.dashscope.aliyuncs.com` 结尾（`vendors.go:92`）。
   - 所以 `matched==""`，进入 :101-103：记入 unknown，并输出 info 日志。
   - `vendors_test.go:40` 断言了这一点。
   - 手工映射优先的前提由使用说明 :38 交代，不矛盾。
2. **"归一后为空、以点开头，或去掉一个末尾点后仍以点结尾……整份价格文件判为无效"（使用说明 :26）**
   - 与 `pricefile.go:135` 的三个条件一一对应。
   - 三个例子都成立：`.` 归一后为空，`a.com..` 归一后以点结尾，`.a.com` 以点开头。测试为 `pricefile_test.go:17-19`。
3. **"即本次所有厂商都不同步"**
   - `validate()` 出错时，`parsePriceFile` 返回 `PriceFile{}`（`pricefile.go:97-99`）。
   - 使用说明 :56 规定：记 error，不同步，不删除原行。
   - 与 :26 的描述一致。

### 上一轮本轮修复项的处置

| 项 | 处置 | 证据 |
|---|---|---|
| R1 使用说明对白名单的归一描述与文件校验不一致 | 已修复 | ① 原 :24 已拆开。现 :24 限定为"`base_url` 带多个末尾点时不匹配……按未识别处理"，不再暗示白名单写多个点"只是匹配不上"。<br>② 新增 :26，写明白名单的三种非法形状和例子，失败范围为"整份价格文件判为无效……本次所有厂商都不同步"，并指向 :56。<br>③ 与 `pricefile.go:134-137`、`vendors.go:87-104,131-133`、包 README :8 一致。<br>④ 修正方式与上一轮给出的最小修正一致，只改文字；代码没有改动（ee 下 diff 只有这一个文件） |

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。"一个末尾根域点"在使用说明 :24/:26、包 README :8/:11 与 `vendors.go:130` 的注释中都出现。这些是面向运维与面向开发两类读者的说明，实现只有 `vendors.go:131-133` 一处；本轮新增的说明与实现一致 |
| 重复 | 同一规则多份实现且互不注明 | 不适用。本轮没有代码改动；归一仍只有 `normalizeHost` 一份实现（grep 只命中 `pricefile.go:134` 和 `vendors.go:91,127,131`） |
| 重复 | 重造已有 helper | 不适用。纯文档改动 |
| 重复 | 派生常量无推导 | 不适用。没有新增常量 |
| 职责与实现质量 | 魔数 | 不适用。纯文档改动 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用 |
| 职责与实现质量 | 无名的错误形状 | 不适用 |
| 职责与实现质量 | 紧凑但绕的控制流 | 不适用 |
| 职责与实现质量 | 文件集中或拆分 | 无。新增内容放在使用说明原有的「配置与使用」一节，紧挨匹配规则，读者不需要跳到别处 |
| 职责与实现质量 | 多余状态或守卫 | 不适用 |
| 职责与实现质量 | 同一效果做两次 | 不适用 |
| 职责与实现质量 | 注释与现状矛盾 | 无。使用说明 :24、:26 已逐句对照代码与测试，见「关键假设核实」 |
| 架构与项目一致性 | 接缝形状与同类不同 | 不适用。没有接入代码改动 |
| 架构与项目一致性 | 架构符合性 | 无，见四维核查 |
| 架构与项目一致性 | 依赖关系 | 无，见四维核查 |
| 架构与项目一致性 | 绕过既有出口 | 不适用。没有存储、迁移、响应或鉴权改动 |
| 架构与项目一致性 | 把上游现状当契约 | 无。新增文字只描述 EE 根包行为，不涉及上游 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无。使用说明、包 README :8 与 `pricefile.go:134-137` 口径已一致；没有前端 |
| 契约一致性 | 状态码口径不一 | 不适用 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。使用说明 :26 的"归一后为空"对应 `pricefile.go:135` 的 `normalized == ""`；原始值为空白由 :131 拒绝，属于第 26 行未列举的其他校验，保留理由见台账对账 |

### 开放式复读

已按"半年后要改这里的人"的视角，把 `ee/docs/国内定价.md` 的完整现状重读了一遍。

- **匹配规则段（:22-26）**
  - 先讲匹配哪些厂商、按什么匹配（:22）。
  - 再讲 `base_url` 的归一与匹配失败（:24）。
  - 最后讲白名单非法时的后果（:26）。
  - 三段递进，读者能区分"匹配不上"和"整份文件无效"这两种后果。
  - :26 的"或去掉一个末尾点后仍以点结尾"与"归一后"在语义上有重叠，但它正好点出 `a.com..` 被拒的原因，读者不必去翻 `normalizeHost`。没有让人皱眉的地方。
- **其余各节**：本轮未改动，没有与新文字冲突之处。:56 的失败处理与 :26 的引用一致。

表外发现：无。

### 实际阅读范围与未执行项

**完整读过**
- `ee/docs/国内定价.md`
- `ee/internal/pricing/{pricefile.go,vendors.go,README.md}`
- `workbench/findings.md`
- `workbench/cases/国内定价/期1/收敛评审4.md`
- 上文列出的规则页与模板

**部分读过**
- `product/架构/EE后端架构.md` §4–§8
- `方案v2.md` :277-298 与 :372-424
- 用 grep 读取：`pricefile_test.go:17-19`、`vendors_test.go:38-42`

**未执行**
- 按只读约束，没跑 go build、vet、test，没跑冒烟、回归脚本和 preflight。
- 没读 `sync.go`、`overrides.go`、`model.go`、`persistence/`、`app/`、`cn-prices.json` 与冒烟脚本；本轮没有改动它们。
- 按要求没读代码评审记录和方案评审记录。
- 没评审 workbench 生成物、评审记录和方案v2 新增段落。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无。规则仍只有一份实现，文档口径已统一 | `vendors.go:131-133`；使用说明 :24/:26 与包 README :8/:11 同口径 |
| 职责与实现质量 | 无 | 纯文档改动，放在原章节内 |
| 架构与项目一致性 | 无变化 | 见下文 |
| 契约一致性 | 无。上一轮 R1 已关闭 | 使用说明 :24/:26 对照 `pricefile.go:97-99,134-137`、`vendors.go:87-104` |

**架构符合性（对照 EE 后端架构 §4、§7、§8）**
- 本轮只改了 `ee/docs/` 下的运维使用说明，没有触碰模块根包、适配子包（`persistence/`）、`app` 装配或宿主接入。
- 按 §8「实现细节维护在包 README」，包 README :8 已记录校验规则；使用说明 :26 面向外部价格文件的维护者，写的是失败后果。两份文档的分工符合 §8，没有让 product 架构文档承载实现细节。
- 根包仍只依赖标准库：`pricefile.go:4-11`、`vendors.go:4-9`，符合 §7「适配归位判定」。

**依赖关系（对照架构 §5、编码规范「依赖关系」）**
- 本轮 diff 不涉及任何 import、构造函数、接口或调用链。
- `normalizeHost` 仍不导出，只在同包内被 `pricefile.go` 与 `vendors.go` 调用，不形成包依赖。
- 没有新增共享资源，所有权不变。

## 出口清单

### 本轮修复项

- 无（上一轮 R1 已修复，处置见上表）

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [国内定价]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- 无

## 台账对账

**涉及的已有 Finding**
- FIND-012：本轮没有改动 `bootstrap.go` 与 `app/`，未恶化。
- FIND-013 至 FIND-021：本轮未触及。
- 本轮不新增 Finding。

**看似可改但有理由保留**

| 事项 | 保留理由 |
|---|---|
| 使用说明 :26 只列举"首尾点、归一后为空"这类白名单非法形状，没有列出 `pricefile.go:127-133` 的其他校验（白名单为空列表、原始值为空白、含 `/`、`:`、空格或 `@`） | 使用说明本来就不穷举价格文件的校验规则：单位、币种、重复模型、非负价格等也都没列，统一由 :56「价格文件解析/校验失败」兜底。:26 只澄清紧挨 :24 归一规则、容易被误读成"只是匹配不上"的那一类。完整列举会把代码校验表抄进运维文档，形成第二份需要同步的规则。**反证条件**：外部价格文件维护者实际写出带 scheme 或端口的白名单，并因为使用说明没写而误判故障 |
| :26 的"或去掉一个末尾点后仍以点结尾"与前半句的"归一后"在语义上有重叠 | 这半句正好说明 `a.com..` 为什么被拒，读者不用去读 `normalizeHost` 的实现；删掉后要靠 :24 推导，理解成本反而更高 |
| 上一轮「看似可改但有理由保留」表中各项 | 本轮没有改动相关代码，未恶化，不再重提 |
