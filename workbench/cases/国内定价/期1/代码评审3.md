---
title: 国内定价 期1 代码评审 #3
type: 代码评审
case: 国内定价
phase: 1
round: 3
verdict: 通过
created: 2026-09-13
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 c7f8ed235f8cc295a06d06905a7464a7746014a8 到候选 f6311fd4bbffec6b59adc5fa03f343a3053f6448；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 对候选 `f6311fd4bbffec6b59adc5fa03f343a3053f6448` 的差量复核完成：上一轮 R2 已修复，三条收敛侧转交项均有依据撤回为必修缺陷；未发现新增行为问题。checklist 全部检查已亲核通过，包括全量 race、构建和三项自包含 HTTP smoke。真实 key 请求、日志费用与页面单价人工核对仍待 Gate 2，本结论不代替产品验收。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 实际范围 | `c7f8ed235..f6311fd4` 的 EE 差量恰为指定九个文件，均通读全文；起止工作树干净，无未跟踪文件 |
| 方案、实施记录、diff | 末尾点归一、等价域名重启回归、Makefile 增加失败现场测试，与实施偏差第 6 条一致 |
| 接口面 | 本轮仅新增私有 `normalizeHost`；无新增公共接口、HTTP 路由、表、依赖或环境变量 |
| 多做／少做／未登记偏差 | 未发现 |
| 未修改区域影响 | 价格文件、换算、覆盖身份、app 装配和既有身份／品牌脚本均无差量；同步与适配器仅局部变量改名。识别的受影响区域已追到覆盖增删及真实重启，无需恢复全量评审 |
| 验收口径 | 自动识别、覆盖金额、两次重启幂等、手工行保护、非法文件继续启动并保留旧行、未知域名提示均通过 smoke；费用引擎及未收录模型通过测试。真实请求日志与页面人工对照未执行 |

失败路径核对结果：

| 路径 | 最终处理 |
|---|---|
| 汇率非法、映射非法、厂商多命中 | 返回配置错误，入口记录原因并退出 |
| 文件读取、解析或校验失败 | app 记录 error，跳过同步与映射校验，继续启动 |
| 厂商读取、覆盖 CRUD 失败 | 返回阶段错误；app 记录 error，已提交行保留，下次启动继续收敛 |
| 创建冲突／删除缺行 | 分别转 Update／按删除成功处理 |
| 内存目录 Upsert 失败 | 记录 error，保留已提交表数据，符合 §9 裁定 |
| smoke 启动或断言失败 | finally 回收自有进程，保留现场路径并重新抛出异常 |

已裁决的 A1 取价、收录限制、逐行部分提交及多节点运行期内存差异不重提。

## 3. 本轮修复项

**无新增修复项。**

上一轮「本轮修复项」逐条处置：

| 上轮项 | 处置与证据 |
|---|---|
| R2：末尾点号误判并删除已有覆盖 | **已修复。** [vendors.go:91](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:91)、[vendors.go:127](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:127) 对白名单与接入主机使用同一归一函数；[vendors.go:132](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:132) 转小写且仅去掉一个末尾点。[vendors_test.go:36](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors_test.go:36) 起覆盖末尾点、端口、无 scheme、子域及拒配场景，全量 race 通过。[pricing-smoke.py:152](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke.py:152) 的两次真实重启通过：UUID、金额、手工行不变，均为 `created=0 updated=0 deleted=0 unchanged=2` |

上一轮已关闭的 R1 未回归：六项失败现场回归及本轮补充的真实 CLI 失败检查通过。

## 4. 需用户裁决／有依据撤回

**需用户裁决：无新增事项。**

收敛评审转交逐条处置：

| 转交项 | 处置与依据 |
|---|---|
| `app/pricing.go:66`：所有 `New` 错误拒启 | **撤回为本轮必修缺陷。** [pricing.go:44](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/app/pricing.go:44) 已在调用 `New` 前加载并完整校验文件，失败在第 46 行记录后返回 nil；目录缺失在第 53 行跳过，生产依赖由入口及 attach 装配。未找到非法文件落入第 66 行拒启的可达路径。配置顺序、attach 错误分类测试及非法文件真实 smoke 均通过；不据此宣称任意错误注入均有测试 |
| `sync.go:87,96`：不再归一映射键 | **撤回为本轮必修缺陷。** [sync.go:37](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:37) 明确要求映射来自 `ParseVendorMap`；唯一生产调用方在 [pricing.go:49](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/app/pricing.go:49) 遵循该约束。大小写归一、重复键拒绝及解析后同步测试通过。直接传大写键属于违反已有调用前置条件，当前无生产触发依据 |
| `pricing-smoke-test.py:30`：内部 patch 是否可信 | **撤回“因此不能证明失败现场行为”的质疑。** patch 隔离端口、HTTP 与故障触发，不替换被核对的 `main/finally/stop` 生命周期；测试实际创建子进程、SQLite 文件及完整日志，并断言退出与保留／清理结果。本轮六项通过，另以未经 mock 的 CLI 验证 `/usr/bin/false` 和不存在的二进制均退出 1、保留原异常及现场。实际 HTTP 行为另由真实 smoke 验证；这些测试不证明真实模型调用或全部超时情形 |

## 5. 转收敛评审／挂账

转收敛评审：无新增项。  
挂账：无。

## 6. 开放式复读的表外发现

已重新通读全部九个差量文件，逐文件核对“出问题如何发现、如何恢复”，重点复查识别失败后的删除、错误分流、部分提交恢复、重启日志区间及脚本进程回收。

**已做开放式复读，无表外发现。**

## 7. 起手表覆盖

以下结果限本轮差量及受影响区域。

| 方面／信号 | 结果 |
|---|---|
| 对照方案：验收标准有未覆盖项 | **发现：** 真实请求日志费用与页面人工核对尚待 Gate 2；R2 自动化缺口已关闭 |
| 方案、实施记录、diff 三方不一致 | 无 |
| 多出的功能、公共接口、抽象或依赖 | 无 |
| 偏差未登记 | 无 |
| 行为：失败被吞或降级成假成功 | 无；批准的降级保留 error 日志 |
| 空值、空集合与非空无分支 | 无；空主机、空厂商、nil 网络配置、空目录均有处理 |
| 取消或超时后仍写入或产生副作用 | 无新增缺陷；CRUD 传递 context，脚本超时进入回收路径 |
| 重发产生两份副作用 | 无；稳定 UUID 与两次真实重启通过 |
| 事务外写后读／多方只有一方回滚 | 无未批准问题；部分提交符合 §9，恢复测试通过 |
| 开关关闭、旧数据、旧客户端无处理 | 无；空目录清理、手工行保护、内置厂商跳过已核 |
| 昂贵检查在前／校验缺层 | 无新增缺口；主机归一修复及拒配测试通过 |
| 路由保护、认证关闭行为 | 不适用本轮路由改动；未新增或改变路由，身份／品牌回归通过 |
| 敏感信息进日志或响应 | 无新增问题；识别日志使用主机，不输出完整凭据 URL |
| 热路径新增 I/O、无界缓冲或循环 | 无；生产变化限启动期识别 |
| 新配置、环境依赖、部署步骤未登记 | 无 |
| 可观测：服务端无法追溯失败原因 | 无；错误阶段、行 ID 或厂商主机可追溯 |
| 对外泄露内部错误原文 | 无新增对外错误出口 |
| 脚本失败现场被清理 | 无；回归及真实 CLI 失败检查通过 |
| 测试：新规则或失败路径无测试 | 无本轮新增缺口；根域点正反例及覆盖保留已覆盖 |
| 断言绑定实现而非行为 | 无影响结论的问题；转交项已核并限定证明范围 |
| 失败信息无法定位条件 | 无新增问题；子测试名、域名、金额诊断及现场路径可定位 |
| 返回值、错误未检查 | 无本轮新增遗漏 |
| fixture 依赖个人环境 | 无；隔离目录、SQLite、随机端口、虚假 key |
| mock 结果写成真实环境结论 | 无；生命周期回归、真实 HTTP 与 Gate 2 明确区分 |
| 整体替换模块的 mock | 无影响结论的问题；故障触发替换未作为 HTTP 验证证据 |
| 残留：调试输出、临时后门、本机配置 | 无新增 |
| 未使用代码、无关改动 | 无范围内确认问题 |
| 生成物与源不一致 | 无；543 个 UI 文件与 embed 副本逐字节一致 |

## 8. 验证记录

实际 `TMPDIR`：

`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/workbench/tmp/codereview/国内定价-期1-代码评审3/scratch`

Go 版本为 `go1.27.1 darwin/arm64`。默认缓存权限失败后，以 `cp -cR /tmp/bifrost-pricing-go-cache.jEGuAC "$TMPDIR/go-cache"` 创建缓存副本，后续 Go 命令使用该 scratch 缓存。

| 检查 | 实际命令 | 结果 |
|---|---|---|
| 格式 | `cd ee && test -z "$(gofmt -l ./internal ./cmd)"` | 0 |
| Vet 首跑 | `cd ee && GOWORK=off go vet ./...` | 1；默认 Go 缓存目录 `operation not permitted` |
| Vet 重跑 | `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go vet ./...` | 0 |
| 全量 race | `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go test -race -count=1 -p 1 ./...` | 0；全部通过 |
| UI 依赖 | `cd ui && npm ci` | 0 |
| UI 构建 | `make -C ee build-ui` | 0 |
| 二进制 | `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go build -p 1 -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 0 |
| 定价 smoke | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/pricing-smoke.py --binary "$TMPDIR/bifrost-http"` | 0；全部 PASS |
| 品牌回归 | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/branding-smoke.py --binary "$TMPDIR/bifrost-http"` | 0 |
| 身份回归 | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | 0 |
| JSON | `python3 -c "import json,sys;json.load(open('ee/internal/pricing/data/cn-prices.json'))"` | 0 |
| 根包依赖 | `! grep -l 'gorm.io\|maximhq/bifrost' ee/internal/pricing/*.go` | 0 |
| 上游零差异 | `git diff --quiet f371548b5444693d7e259033bec9f4a467e3748b -- core framework plugins transports ui` | 0 |
| Workbench | `python3 workbench/tools/build_views.py --check` | 0；首跑 126 篇，最终 127 篇 |
| 空白 | `git diff --check`；另检查 `c7f8ed235..f6311fd4 -- ee` | 均为 0 |
| 脚本回归 | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/pricing-smoke-test.py -v` | 0；六项通过 |
| Makefile 顺序 | `make -n -C ee smoke-pricing` | 0；先失败现场回归，再 HTTP smoke |

补充亲核：

- 通过内存 Python 调度逐项执行 checklist 原始命令：**13 个 `test -e`、14 项必含 grep、5 项禁留 grep 全部符合预期**；未生成报告。
- 通过内存 Python 逐字节比较 `ui/out` 与 embed：543 个文件一致。
- 无 mock 的 CLI 失败检查：分别运行 `pricing-smoke.py --binary /usr/bin/false` 和 `--binary "$TMPDIR/intentionally-missing-binary"`；均按预期退出 1，保留原异常、配置、价格文件及日志。现场分别位于 scratch 下 `ee-pricing-smoke-w8swlod8`、`ee-pricing-smoke-5f0scikr`。
- npm 输出 14 项依赖漏洞提示；脚本回归出现 SQLite 连接 `ResourceWarning`。均未导致检查失败，本轮未改依赖或该连接管理逻辑。
- 因可用磁盘约 1.5 GB，Go 测试与构建使用 `-p 1`；测试加 `-count=1`，未复用测试结果。

版本绑定：

- 起始：`feat/domestic-pricing`，HEAD 为指定候选 `f6311fd4bbffec6b59adc5fa03f343a3053f6448`，工作树及索引干净。
- 结束：外部提交使 HEAD 为 `70a478bdf5638316d5cfa7c9a95d9112fde574e2`；仅新增收敛评审记录与视图，未读取该评审。最终工作树、索引干净，无未跟踪文件；EE 及上游代码与指定候选零差异。
- 二进制 SHA-256：`9e3c1b585f5d0b2bca02c196877a6e3f8bb24961c978b95e12ff00c743134d1e`。

实际阅读：指定规则、验收口径、方案与实施记录、findings、checklist、两份证据、代码评审2；九个差量文件全文并复读；另追读 pricing 模型、解析、覆盖生成及相关测试，app 装配与测试，共用 smoke 夹具和回收路径，构建配置，以及上游覆盖 CRUD、目录更新和入口错误处理。

未执行：**checklist 无跳过项。** 未运行 preflight 总入口及会执行 workspace 写入的 `make smoke-pricing`，其检查和脚本已分别亲跑。未执行真实 key 调用、页面人工核对、PostgreSQL 实例验证或官网价格重新核对；这些不属于本轮差量亲核范围。未启动 dev server、操作已有实例、修改跟踪文件或索引，未暂存、提交、推送。

verdict: 通过
