---
title: 国内定价 期1 收敛评审 #2
type: 收敛评审
case: 国内定价
phase: 1
round: 2
verdict: 有Blocking
created: 2026-09-13
---

> 评审者与实施上下文隔离。本轮是差量复核：上一轮候选 `380b87f1a3ea70606a0f8cbbdbd65fdd996c05cd`，本轮候选 `c7f8ed235f8cc295a06d06905a7464a7746014a8`（工作树干净，无未跟踪文件）。基线 `f371548b5444693d7e259033bec9f4a467e3748b`，风险等级 L2。

## 结论

上一轮 R1–R6 都已修复，建议项 S1、S2 也都采纳了，不再阻塞。

- 汇率判定现在只有 `ValidRate` 一份。
- 映射的归一化和查重只在 `ParseVendorMap` 里做。
- `match` 已改为不导出，四个注入接口集中到 `sync.go`。
- 字面量改为按字段名书写。
- 三个环境变量都补了注释。
- 说明文字与代码现状一致了。
- `persistence` 拆成三个小适配器，由 app 分别注入，依赖方向和资源所有权更清楚。

本轮修复本身留下三处小问题，都在授权范围内，也不需要用户裁决：

- **F1 改名残留**：`sync.go` 里的循环变量 `match` 遮蔽了同名类型；`Overrides` 的接收者名仍是旧名 `Store` 的 `s`。
- **F2 新增脚本没有入口**：新增的 `pricing-smoke-test.py` 在 Makefile、checklist 和各 README 里都没有入口；同时 `ee/README.md` 没有随本案同步，模块表和冒烟命令里都缺国内定价。
- **F3 README 写了过程记录**：包 README 写入了“本轮……增至 305 行”这种实施过程叙述。

这三项修复并经定向复核之前，verdict 保持有Blocking。

## 事实来源

**执行的命令**
- `git status --short --untracked-files=all`：输出为空。
- `git rev-parse HEAD`：`c7f8ed235f8cc295a06d06905a7464a7746014a8`。
- `git diff --stat 380b87f1a..c7f8ed235`：共 30 个文件。评审范围内的 16 个 ee 文件都在其中，另外 14 个是 workbench 过程文档、工具与生成物，不在评审范围。
- `git diff 380b87f1a..c7f8ed235 -- ee`：逐个 hunk 对照新旧改动。
- `git diff 380b87f1a..c7f8ed235 -- workbench/cases/国内定价/期1/方案v2.md`：只读新增的实施偏差记录第 5 条。
- `git log 380b87f1a..c7f8ed235`：共 6 个提交。
- `git ls-files -s ee/scripts`：列出脚本文件。

**读过的规则与裁决**
- 根 `AGENTS.md`、`workbench/AGENTS.md`
- `规范/流程.md`、`流程/收敛评审.md`、`流程/收敛评审审查方向.md`、`项目/编码规范.md`
- `templates/收敛评审.md`、`product/架构/EE后端架构.md` 全文
- `workbench/findings.md`
- `方案v2.md` 第 200–407 行，含 §4.6、§6、§8、§9「Gate 决策结果」「实施偏差记录」
- 上一轮记录 `收敛评审1.md` 全文

**同类实现**
- `ee/internal/app/identity.go:23-28`：环境变量常量注释的惯例。
- `ee/scripts/identity-smoke.py:36-126`：`Node` 的 `start`、`stop`、`call`。
- 核对 R6(c)：`pricing-smoke.py:50-77` 与 `identity-smoke.py:58-81` 除去 `default_home` 分支和定价变量注入外逐行一致，所以“抄自”这一注释属实。
- `ee/Makefile` 全文、`ee/README.md` 全文，`checklist.yaml` 全文。
- `ee/docs/国内定价.md:33-35,74`：用 grep 读取。

**关键假设核实**
- **删掉 `matchVendors` 开头的映射校验没有影响**：`s.mapping` 只在 `sync.go:96` 赋值，赋值前已在 `sync.go:87` 校验。生产代码里 `matchVendors` 只有 `sync.go:108` 一个调用点。
- **`Match` 包外无引用**：对 `ee/` 全量 grep `\bMatch\b`，Go 代码中没有命中。
- **不再有 `NewStore`、`catalogAdapter`、`providerAdapter` 残留**：grep 结果只剩 identity 模块自己的 `NewStore`，与本案无关。
- 本轮结论不新增对上游行为的依赖。

### 上一轮本轮修复项的处置

| 项 | 处置 | 证据 |
|---|---|---|
| R1 汇率规则四份实现，且对同一对象重复校验 | 已修复 | 判定函数只在 `pricefile.go:104-106` 定义一份 `ValidRate`，四个使用点 `app/pricing.go:33`、`sync.go:82`、`overrides.go:59`、`pricefile.go:117` 都引用它。`New` 只在 `sync.go:70` 校验一次文件。app 里不可达的非 `ErrConfig` 分支已删，见 `app/pricing.go:66-68` |
| R2 映射的归一化、查重、校验重复三处 | 已修复 | 小写和查重只在 `vendors.go:32-37`。`New` 在 `sync.go:87` 做一次边界校验。`matchVendors`（`vendors.go:57`）不再校验。`Options` 的注释在 `sync.go:37` 写明映射须来自 `ParseVendorMap` |
| R3 `Match` 多余导出；`logger` 与其余依赖接口分处两个文件 | 已修复 | `vendors.go:19` 改为 `match`；`logger` 移到 `sync.go:32-35`，与另外三个接口相邻。附带问题见 F1 |
| R4 位置式或超长单行字面量 | 已修复 | `sync.go:90-97`、`overrides.go:87-96`、`pricefile.go:62-93` 都改为按字段名书写。`store.go:129-142` 与 `store.go:149-162` 两个方向的字段顺序一致 |
| R5 环境变量常量没有注释 | 已修复 | `app/pricing.go:18-25` 三条都有注释，写明含义、单位和未设置或空值时的行为，与 `ee/docs/国内定价.md:33-35`、`pricingRate` 的 `LookupEnv` 语义一致 |
| R6(a) Makefile 帮助文字与执行顺序相反 | 已修复 | `ee/Makefile:42` 的文字与 :43-45 的执行顺序一致 |
| R6(b) app README 的 Bootstrap 条目过时 | 已修复 | `ee/internal/app/README.md:7` 已补“及国内定价同步” |
| R6(c) 冒烟 `start` 抄写父类但未注明 | 已修复 | `pricing-smoke.py:51-52` 注明来源、禁止修改父脚本的依据和需要同步的条件。类说明（:25）与之不矛盾 |
| S1 深拷贝范围过宽（建议项） | 已采纳 | `sync.go:76-86` 只复制 `Rates`，只读约束写在 `sync.go:67-68` 和 `pricing/README.md:31` |
| S2 一个 `Store` 承担三种适配角色（建议项） | 已采纳 | `persistence/store.go:16-80` 拆成 `Overrides`、`Catalog`、`Providers`，每个只持有自己需要的依赖；由 `app/pricing.go:61-63` 分别注入 |

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无。汇率判定已收成一处；`sync_test.go:249` 和 `pricing_test.go:34` 里的边界值是测试字面量 |
| 重复 | 同一规则多份实现且互不注明 | 无。`ParseVendorMap`（`vendors.go:38`）与 `New`（`sync.go:87`）调用的是同一个 `validateVendorMap`，不是两份实现，保留理由见台账对账。冒烟 `start` 已注明来源 |
| 重复 | 重造已有 helper | 无 |
| 重复 | 派生常量无推导 | 无。本轮没有新增常量 |
| 职责与实现质量 | 魔数 | 无 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用。本轮没有新增接口出口；脚本里的 503 是测试桩的字面量 |
| 职责与实现质量 | 无名的错误形状 | 无 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无。`pricing-smoke.py:190-202` 是 try/finally 嵌在 try/except/else 里。这样嵌套是必要的：进程必须先回收，才能决定删目录还是保留现场，外层合并会让 `else` 先于 `finally` 执行 |
| 职责与实现质量 | 文件集中或拆分 | 无。S2 拆出的三个适配器仍在同一个 `store.go`（164 行），一次读完就能看全，没有增加跳转。`sync_test.go` 为 305 行，已评估过，不要求拆；评估结论写的位置有问题，见 F3 |
| 职责与实现质量 | 多余状态或守卫 | 无。`New` 仍对 `Load` 已校验过的文件再做一次 `validate()`，保留理由见台账对账 |
| 职责与实现质量 | 同一效果做两次 | 无 |
| 职责与实现质量 | 注释与现状矛盾 | **发现** F3：`pricing/README.md:35` 用“本轮”记录行数增长，这是过程叙述，行数一变就与现状不符 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。环境变量注释已与 `identity.go:23-28` 对齐，只是换成独立注释行，信息量相同。app 仍按 §9 的错误类别分流 |
| 架构与项目一致性 | 架构符合性 | 无。详见四维核查 |
| 架构与项目一致性 | 依赖关系 | 无。详见四维核查 |
| 架构与项目一致性 | 绕过既有出口 | 无。`Catalog.Upsert/Delete`（`store.go:63-72`）仍走上游的 `UpsertPricingOverrides/DeletePricingOverride`，没有全量替换 |
| 架构与项目一致性 | 把上游现状当契约 | 无。本轮没有新增对上游行为的依赖 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **发现** F2：`ee/README.md:34,40` 的冒烟说明与 `ee/Makefile:42-51` 不一致，缺国内定价和 `smoke-pricing`；模块表 :11-19 缺 `internal/pricing`。三个环境变量的注释与开发指南一致 |
| 契约一致性 | 状态码口径不一 | 不适用。没有新增接口 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。`EE_PRICING_USD_CNY` 未设置时用文件汇率，空串拒启，见 `app/pricing.go:28-35`；注释（:19）和测试（`pricing_test.go:34` 含 `""`）的口径一致 |

### 开放式复读

已按“半年后要改这里的人”的视角，把范围内 16 个文件的完整现状重读一遍。表外发现两条：

- **F1**：R3、S2 改名后留下的命名残留，包括循环变量遮蔽类型名、接收者名沿用旧类型名。
- **F2 的一部分**：新增的 `ee/scripts/pricing-smoke-test.py` 在仓库里没有任何运行入口或说明；它在函数体内局部导入 `sqlite3`，与同目录脚本在文件头导入的写法不同。

其余文件第一眼没有让人皱眉的地方。

### 实际阅读范围与未执行项

**完整读过**
- `ee/internal/pricing/{sync,vendors,pricefile,overrides}.go`、`sync_test.go`
- `ee/internal/pricing/persistence/{store.go,store_test.go,cost_test.go,README.md}`
- `ee/internal/pricing/README.md`
- `ee/internal/app/{pricing.go,pricing_test.go,README.md}`，以及 `identity.go` 第 1–60 行
- `ee/scripts/pricing-smoke.py`、`ee/scripts/pricing-smoke-test.py`（按 diff 全文）、`identity-smoke.py` 第 1–140 行
- `ee/Makefile`、`ee/README.md`、`checklist.yaml`

**只读了 diff**
- `vendors_test.go`：本轮只有一处字面量展开。

**未执行**
- 按只读约束，没跑 go build/vet/test、Python 测试、冒烟、preflight。
- 没读 `model.go`、`overrides_test.go`、`pricefile_test.go`，本轮没有改动它们。
- 没读 `cn-prices.json`、价格核对记录、预飞事实表。
- 按要求没读代码评审记录和方案评审记录。
- 没评审 workbench 工具与生成物，包括 `code_review.py` 及其测试。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无。上一轮的重复已消除 | R1、R2 处置行 |
| 职责与实现质量 | 改名残留（F1）；README 写入实施过程叙述（F3） | F1、F3 |
| 架构与项目一致性 | 无新问题。拆分后依赖更窄 | 见下文 |
| 契约一致性 | `ee/README.md` 的模块表与冒烟命令没有随本案同步；新增回归脚本没有入口（F2） | F2 |

**架构符合性（对照 EE 后端架构 §4、§5、§7）**

- **§4 模块内职责**
  - 根包继续承载规则。`Service.New` 只做校验和组装，见 `sync.go:69-98`。
  - `persistence/` 只做库类型适配，三个适配器都不含业务判断，见 `store.go:16-100`。
- **§7 适配归位**：`Catalog` 只包 `*modelcatalog.ModelCatalog`（`store.go:55`），`Overrides` 和 `Providers` 只包 `configstore.ConfigStore`（`store.go:17-19,75`）。它们都是库类型，不是运行中的宿主对象，没有进 `host`，归位正确。
- **§7 复用完整操作**：表写入和目录更新的先后仍由 `Sync` 编排（`sync.go:153-194`）。拆分后，适配器的注释明确写了“顺序由 `pricing.Service` 协调”（`store.go:57`），`NewStore(config, catalog)` 那种签名暗示“CRUD 会联动目录”的误读已经消除。
- **§7 生命周期**：三个构造函数都只连接宿主已创建的依赖，注释写明生命周期归宿主（`store.go:16,21,54,74,77`）。没有 Close，也没有后台任务。
- **app 职责**：`assemblePricing`（`app/pricing.go:39-76`）只读环境、构造、调用一次 `Sync` 并分流错误。删掉不可达分支后更薄，没有承载运行期业务。

**依赖关系（对照架构 §5、编码规范「依赖关系」）**

- **实际 import**
  - 根包：`sync.go:4-11`、`vendors.go:4-9`、`pricefile.go:4-11` 只用标准库；`overrides.go:4-9` 在标准库之外只加 `google/uuid`。本轮根包没有新增 import，`sync_test.go:7` 新增的 `math` 也是标准库。
  - `persistence/store.go:9-14`：导入根包加上游 configstore、tables、modelcatalog、schemas，方向是适配层指向根包。
  - `app/pricing.go:11-16`：删掉了 `math`，其余不变。
- **窄接口与显式注入**
  - 使用方接口 `overrideStore`、`catalog`、`providerLister`、`logger` 全部定义在 `sync.go:19-35`。
  - app 通过 `pricing.Deps` 分别注入三个构造结果（`app/pricing.go:60-65`），不再从一个对象的方法二级取用（上一轮的 `store.Catalog()`）。这符合编码规范“不从其他对象的字段取用”。
- **导出面**
  - `ValidRate` 被包外 `app/pricing.go:33` 使用，有用途注释。
  - `Overrides`、`Catalog`、`Providers` 及其构造函数被 app 使用，都有注释。
  - 没有新增无使用方的导出符号。
- **跨模块数据与资源所有权**：只经 ConfigStore 的公开 CRUD 和 ModelCatalog 的增量方法访问上游资源；不创建、不关闭共享资源；不接触其他 EE 模块。

## 出口清单

### 本轮修复项

**F1 改名残留：循环变量遮蔽类型名，接收者名沿用旧类型**（开放式复读发现）

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:118-121`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:19`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/persistence/store.go:27,40,45,50`
- **代码事实**
  - R3 把类型改名为 `match` 后，`Sync` 的 `for _, match := range matches` 在循环体内把类型名遮蔽成了变量，`match.VendorID`、`match.Provider` 读起来像在访问类型。
  - S2 把 `Store` 改名为 `Overrides` 后，四个方法的接收者仍叫 `s`；同文件新写的 `Catalog`、`Providers` 用的是 `c`、`p`。
- **维护影响**
  - 读者要先分辨 `match` 指类型还是变量。
  - 以后在这个循环里需要构造 `match{}`（比如拆分或补一个识别结果），会得到“match is not a type”这类令人困惑的编译错误。
  - 接收者 `s` 暗示旧的 `Store`，与同文件另外两个适配器的命名规则不一致。
- **最小修正**
  - 二选一：类型改名为 `vendorMatch`，或循环变量改为 `hit`。
  - `Overrides` 的接收者统一改为 `o`。
  - 只改名，不改行为。

**F2 新增回归脚本无运行入口；`ee/README.md` 未随本案同步**（`ee/README.md` 不在 diff 文件清单内，属于本案造成的受影响边界）

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke-test.py:1-121`，其中局部导入在 `:68`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/Makefile:42-51`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/README.md:11-19`、`:34-35`、`:40`
- **代码事实**
  - 对全仓 grep `pricing-smoke-test`，只命中 `方案v2.md:400` 的实施记录。Makefile、`checklist.yaml`、`ee/README.md`、`ee/docs/国内定价.md` 都没有引用或说明它。
  - 这个脚本 patch 了 `smoke.identity_smoke.socket`、`PricingNode.call`、`smoke.tempfile.mkdtemp` 等内部符号（:30-35），依赖 `pricing-smoke.py` 的内部结构。
  - `ee/README.md` 的代码结构表（:11-19）没有 `internal/pricing` 一行。
  - `:34` 仍写 `smoke` 是“品牌 + SQLite 身份”，而 `Makefile:43` 第一条执行的是国内定价。
  - 命令清单没有 `smoke-pricing`（`Makefile:50`），`:40` 的冒烟说明也只提到品牌和身份两个脚本。
- **维护影响**
  - 没有入口的回归脚本，在 `pricing-smoke.py` 下次重构时不会被任何人运行，会先于被测对象腐坏。
  - 新人从 `ee/README.md` 找模块和冒烟命令时看不到国内定价，也不知道这组失败现场回归存在。
  - 局部导入与同目录脚本在文件头导入的写法不一致（`identity-smoke.py:18`）。
- **最小修正**
  - 给 `pricing-smoke-test.py` 一个入口：它不需要二进制和端口，可以放在 `smoke-pricing` 目标里先于 HTTP 冒烟执行，或列入 `checklist.yaml` 的必过命令，二选一。
  - 在 `ee/README.md` 补三处：模块表加 `internal/pricing` 一行；冒烟命令列表补 `smoke-pricing` 并把 `smoke` 的说明改为包含国内定价；:40 补一句定价冒烟及其失败现场回归脚本。
  - 把 `import sqlite3` 移到文件头。

**F3 包 README 写入实施过程叙述**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/README.md:35`
- **代码事实**：原文是“`sync_test.go` 超过 300 行已评估：本轮仅因字段展开与构造配置回归增至 305 行……暂不拆分”。现在 `sync_test.go` 确为 305 行，但“本轮”和具体行数都是一次实施过程的状态。
- **维护影响**
  - 包 README 是长期文档，“本轮”在以后读者眼里没有参照。
  - 下次测试增删几行，这句就与现状矛盾（编码规范「注释写清用途、约束和原因」要求及时删除失效说明）。
  - 流程.md「知识沉淀」规定实施过程只留 Workbench，不外溢到长期文档。
- **最小修正**
  - 改成不随行数变化的约束表述，例如：“`sync_test.go` 围绕同一同步服务共享内存夹具，超过 300 行仍保持单文件；按场景分组增长明显时再拆。”
  - 过程性的评估结论已在方案实施偏差记录中，README 不再重复。

### 建议取舍项

- 无

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [国内定价]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- `ee/internal/app/pricing.go:66-68`：`pricing.New` 的任何错误现在都导致拒启。原本记日志后继续启动的非 `ErrConfig` 错误（文件无效、依赖为 nil）在 app 调用时虽不可达，但语义已从 §9 的“价格文件无效 → 跳过同步”变为拒启，需核实是否可接受、测试是否覆盖。
- `ee/internal/pricing/sync.go:87,96`：`New` 不再对 `VendorMap` 键做小写归一。若调用方传入的映射不是来自 `ParseVendorMap` 而含大写键，`vendors.go:80` 会静默匹配不上。当前唯一生产调用方合规，是否需要边界断言需核实。
- `ee/scripts/pricing-smoke-test.py:30-35`：回归测试 patch 了 socket、`PricingNode.call`、`mkdtemp` 等内部实现，能否可信地代表真实失败路径需核实。

## 台账对账

**涉及的已有 Finding**
- **FIND-012**（attach 惯性点）：本轮没有改动 `bootstrap.go`；`assemblePricing` 删掉一个分支后更薄，未恶化。
- FIND-013 至 FIND-021：本轮未触及。
- 本轮不新增 Finding。

**看似可改但有理由保留**

| 事项 | 保留理由 |
|---|---|
| `validateVendorMap` 被 `ParseVendorMap`（`vendors.go:38`）和 `New`（`sync.go:87`）对同一映射各调一次 | 这是同一函数被两个导出边界调用，不是两份规则。`ParseVendorMap` 先校验，app 才能在错误里带上 `EE_PRICING_VENDOR_MAP` 前缀（`app/pricing.go:51`，由 `pricing_test.go:55` 断言）；`New` 作为导出构造函数的边界保留一次，正是上一轮 R2 的最小修正 |
| `New` 对 `Load` 已校验过的文件再做一次 `validate()`（`sync.go:70`） | `New` 是导出构造函数，调用方也可能直接构造 `PriceFile` 而不经过 `Load`（`cost_test.go:42-62` 就是这样）。R1 只要求消除 `New` 内部的重复校验 |
| `toUSDPerToken` 内再判一次 `ValidRate(rate)`（`overrides.go:59`） | 纯函数自带输入守卫，且引用的是同一判定函数。`Service.file.Rates` 会被同包测试直接改写（`sync_test.go:122,166`） |
| `pricing-smoke.py` 的 `main` 用双层 try（:190-202） | 必须先 `stop` 回收子进程，再决定删目录还是保留现场；合并成一层会让 `else` 里的 `rmtree` 先于 `finally` 里的 `stop` 执行 |
| `persistence` 的三个适配器仍放在一个 `store.go` | 三者合计 164 行，行转换函数 `toTable/fromTable` 由 `Overrides` 和 `Catalog` 共用；拆文件只会增加跳转，不符合编码规范「文件组织以阅读为先」 |
| 上一轮“看似可改但有理由保留”表中各项 | 本轮未恶化，不再重提 |
