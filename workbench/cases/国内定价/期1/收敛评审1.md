---
title: 国内定价 期1 收敛评审 #1
type: 收敛评审
case: 国内定价
phase: 1
round: 1
verdict: 有Blocking
created: 2026-09-13
---

> 评审者与实施上下文隔离。基线 `f371548b5444693d7e259033bec9f4a467e3748b` 到 `380b87f1a3ea70606a0f8cbbdbd65fdd996c05cd`（工作树干净，无未跟踪文件）。风险等级 L2。

## 结论

本次改动的架构方向正确。`ee/internal/pricing` 根包只依赖标准库和 uuid；`persistence` 负责适配上游 ConfigStore 与 ModelCatalog；`app` 只做启动装配和错误分流。写库再更新内存目录，走的是上游自己的完整操作，也没有另起基础设施。依赖方向、窄接口和显式注入都符合 EE 后端架构。

问题在局部实现：

- **重复实现规则（违反编码规范硬规则）**：汇率"有限正数"的判定写了四份，而且表达式不统一。手工映射的小写归一、查重和校验在 `ParseVendorMap`、`New`、`matchVendors` 里对同一个对象重复做。
- **不该导出的符号**：`Match` 在包外没有使用方，却导出了。
- **注释与说明和现状不符**：Makefile 帮助文字、app README、冒烟脚本的说明。
- **风格**：有几处位置式或超长的单行结构体字面量。

以上都是范围内、无需裁决的本轮修复项。修复并定向复核前，verdict 为有Blocking。没有需要用户裁决的结构或契约问题。

## 事实来源

**执行的命令**
- `git status --porcelain --untracked-files=all`：输出为空，没有未跟踪文件，也没有未提交改动。
- `git diff --stat f371548b5..380b87f1a`：共 38 个文件。评审范围内的 24 个文件都在其中，其余是 workbench 过程文档与生成物，不在本次范围。
- 对 `ee/Makefile`、`ee/internal/app/bootstrap.go`、`product/README.md` 执行了 `git diff`，看新旧差异。

**读了哪些规则和裁决**
- 根 `AGENTS.md`、`workbench/AGENTS.md`、`规范/流程.md`、`流程/收敛评审.md`、`流程/收敛评审审查方向.md`、`项目/编码规范.md`、`templates/收敛评审.md`、`product/架构/EE后端架构.md`、`workbench/findings.md`。
- `workbench/cases/国内定价/期1/方案v2.md` 第 55–401 行，含 §3–§9、「Gate 决策结果」「实施偏差记录」。
- `product/需求/国内定价/产品方案.md` 全文。

**同类实现与上游证据**
- EE 装配惯例：`ee/internal/app/identity.go` 全文。环境变量常量逐条带注释（:23-28），配置错误直接返回。
- EE 窄接口惯例：`ee/internal/host/auth.go:27-63`、`ee/internal/identity/model.go:211-261`、`ee/internal/identity/persistence/diagnostics.go:18-20`（grep 读取）。
- 冒烟夹具惯例：`ee/scripts/identity-smoke.py:37-127` 的 `Node`。`ee/scripts/branding-smoke.py:38` 的 `Fixture(identity_smoke.Node)` 没有重写 `start`。
- 上游写覆盖的完整链路：
  - 管理接口先写库再更新内存，见 `transports/bifrost-http/handlers/governance.go:4690,4696,4830,4840`。
  - 内存更新最终走 `transports/bifrost-http/server/server.go:1883-1896` → `ModelCatalog.UpsertPricingOverrides/DeletePricingOverride`（`framework/modelcatalog/pricing.go:133-139`）。
  - 启动时上游已把库里的覆盖装入目录，见 `transports/bifrost-http/lib/config.go:3418`；全量替换入口在 `config.go:5230`，EE 没有使用。
- 上游存储语义：
  - `framework/configstore/rdb.go:3104-3107`：注释写明 Update 兼作 create-or-update。
  - `rdb.go:3133-3134`：Delete 找不到行时返回 `ErrNotFound`。
  - `rdb.go:3091-3092`：Create 经 `parseGormError` 映射错误。

**可比实现的差异**：见四维核查和出口清单。起手表覆盖、开放式复读结果、阅读范围与未执行项附在本节末尾。

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | **发现** R1：汇率"有限正数"写了 4 处 |
| 重复 | 同一规则多份实现且互不注明 | **发现** R2：映射归一化、查重、校验重复；R6：冒烟 `start` 抄写父类但没注明 |
| 重复 | 重造已有 helper | 无。`persistence/store.go:106` 的 `pointer` 把空串转成 nil，和通用取址语义不同；`overrides.go:82` 注明了用标准库 JSON 的原因 |
| 重复 | 派生常量无推导 | 无。`tokensPerMillion`、`namespaceID` 都是源值，并有注释（`overrides.go:11-16`） |
| 职责与实现质量 | 魔数 | 无。前缀、命名空间、单位都是命名常量；脚本里的 1e6 属于契约字面量，按规范可以直接写 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用。Go 侧没有新增 HTTP 出口；脚本里的 200/201 是测试字面量 |
| 职责与实现质量 | 无名的错误形状 | 无。失败用 `ErrConfig`、`ErrRowExists`、`ErrRowMissing` 哨兵表达 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无。`sync.go:165-186` 的 switch 加冲突转 Update 读得顺，外层 `err` 只在同一循环内复用 |
| 职责与实现质量 | 文件集中或拆分 | **发现**（小）R3：`logger` 接口放在 `vendors.go`，与 `Deps` 的其余三个接口（`sync.go`）分开。其余文件按价格文件、识别、行生成、同步分组，各 50–210 行，拆分合理 |
| 职责与实现质量 | 多余状态或守卫 | **发现** R1/R2：已核实是对同一对象的重复校验。S1：深拷贝范围大于实际需要 |
| 职责与实现质量 | 同一效果做两次 | **发现** R2：映射键小写与查重做了两次。启动时 Upsert 未变的行是方案 §4.5 规定的，不算 |
| 职责与实现质量 | 注释与现状矛盾 | **发现** R6：Makefile 帮助文字、app README 的 Bootstrap 条目、冒烟脚本的类说明；R2：`Options` 注释写"已解析"，但 `New` 又解析一遍 |
| 架构与项目一致性 | 接缝形状与同类不同 | **发现**（小）R5：环境变量常量没有逐条注释，和 `identity.go` 不一致。其余逐项一致：配置错误拒启、宿主 logger 注入、复用上游 CRUD 与目录入口、不建表 |
| 架构与项目一致性 | 架构符合性 | 无。详见四维核查 |
| 架构与项目一致性 | 依赖关系 | 无方向问题。**发现** R3：`Match` 在包外无使用方却导出，违反硬规则 |
| 架构与项目一致性 | 绕过既有出口 | 无。表写入走 ConfigStore 公开方法，内存走 `UpsertPricingOverrides/DeletePricingOverride`，与上游 handler 同链；没有迁移 |
| 架构与项目一致性 | 把上游现状当契约 | 无。依赖的 Update 兼作创建、Delete 缺行返回 `ErrNotFound`，上游源码注释都有明示（`rdb.go:3106-3107,3133`），并由哨兵映射隔离 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | 无。四家主机表（`ee/docs/国内定价.md:24-29`）与 JSON 一致（`cn-prices.json:10-13,621-624,651-654,660-663`）；日志行格式在 `sync.go:207`、文档 :46、冒烟 :135/:149 三处一致；回滚文件经 `pricefile.go:62-65` 可通过校验 |
| 契约一致性 | 状态码口径不一 | 不适用。没有新增接口 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。缓存价 nil 与 0 分得清：`pricefile.go:37`、`overrides.go:72`，测试见 `overrides_test.go:56-70`。`EE_PRICING_USD_CNY` 设为空串时拒启（`app/pricing.go:26-31`），与文档"有限正数"相符 |

### 开放式复读

已按"半年后要改这里的人"的视角，把范围内所有文件重读一遍。表外发现两条：

- **R4**：位置式或超长的单行结构体字面量。
- **S2**：`persistence.Store` 一个类型承担三种适配角色，并用导出方法返回未导出类型。

其余文件没有第一眼让人皱眉的问题。

### 实际阅读范围与未执行项

**完整读过**
- `ee/internal/pricing/{model,overrides,pricefile,sync,vendors}.go` 及对应的 4 个 `_test.go`
- `ee/internal/pricing/persistence/{store.go,store_test.go,cost_test.go,README.md}`
- `ee/internal/pricing/README.md`
- `ee/internal/app/{bootstrap.go,pricing.go,pricing_test.go,README.md,identity.go}`
- `ee/docs/国内定价.md`、`ee/scripts/pricing-smoke.py`、`product/需求/国内定价/产品方案.md`
- `ee/Makefile` 与 `product/README.md` 的 diff

**部分读过**
- `ee/internal/pricing/data/cn-prices.json`：读了第 1–60 行，另 grep 厂商 id 和主机。价格数值是否正确属于数据核对，不在本评审范围。

**未执行**
- 按只读约束，没跑 go build/vet/test、冒烟、preflight。
- 没读 `workbench/evidence/国内定价-期1-价格核对.md` 和 `checklist.yaml`。
- 按要求没读方案评审和代码评审记录。
- 没评审 workbench 生成物。
- 没跑 `git rev-parse`；HEAD 取自会话 git 快照，diff 目标用的是显式 commit `380b87f1a`。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 汇率判定写了 4 份，映射归一化和校验重复 3 处，冒烟 `start` 抄写父类未注明 | R1、R2、R6 |
| 职责与实现质量 | 同一对象反复校验；符号导出面偏大，`logger` 位置分散；单行字面量难读；深拷贝范围过宽 | R1–R4、S1 |
| 架构与项目一致性 | **架构符合性**：一致。**依赖关系**：方向一致，仅 `Match` 多余导出。环境变量注释与 identity 惯例不一致 | 见下文，R3、R5、S2 |
| 契约一致性 | 无 | 起手表契约三行 |

**架构符合性（对照 EE 后端架构 §3/§4/§6/§7）**

- **业务模块**：新增 `ee/internal/pricing/`，模块根包承载价格模型、校验、识别、行生成和同步规则（§4 表"模块根包"）。`persistence/` 承载库类型适配。没有 http、plugin 或 worker 子包，因为功能不从这些入口触发（§6：入口按调用来源决定）。
- **§7 适配归位**：ConfigStore 和 ModelCatalog 是 framework 库类型，不是运行中的宿主对象。`app/pricing.go:37` 只接收 `configstore.ConfigStore` 和 `*modelcatalog.ModelCatalog`，不碰 `*BifrostHTTPServer`，所以没进 `internal/host`，归位正确。
- **app 职责**：`app/pricing.go:37-71` 只读环境、构造、调用一次 `Sync`，并按 §9 失败策略分流。增删改的业务编排在 `pricing.Service.Sync`（`sync.go:114-209`），app 没有承载运行期业务。
- **§7 复用完整操作**：EE 的写入是"先写库再增量更新目录"。`sync.go:167-205` 对应上游 handler 的 `governance.go:4690→4696` 和 `server.go:1887`。EE 没有用全量替换的 `SetPricingOverrides`（`lib/config.go:5230`），不会覆盖管理员的行。
- **§7 生命周期**：`persistence/store.go:16-24` 只持有宿主创建的依赖，没有 Close，也没有后台任务。
- **装配位置**：`bootstrap.go:58` 在品牌之后、Start 之前接入，与 §1 的装配顺序一致。

**依赖关系（对照 §5、编码规范「依赖关系」）**

- **实际 import**
  - 根包：`model.go:4`、`overrides.go:4-9`、`pricefile.go:4-11`、`sync.go:4-11`、`vendors.go:4-9`，只有标准库和 `google/uuid`。根包测试同样只用标准库。
  - `persistence/store.go:9-14`：导入根包加 configstore、tables、modelcatalog、schemas，方向是适配层指向根包。
  - `app/pricing.go:12-16`：导入根包、persistence、configstore、modelcatalog、schemas。
- **接口与注入**：使用方窄接口 `overrideStore`、`catalog`、`providerLister` 定义在根包（`sync.go:19-31`），经 `Deps` 构造函数显式注入（`sync.go:40-45,64`）。不从全局 Server 或 Config 取依赖。
- **类型分工**：业务类型不带标签（`model.go:28-50`、`overrides.go:36-44`）。文件 DTO 独立定义（`pricefile.go:19-42`），表行转换在 persistence（`store.go:112-125`）。
- **跨模块数据**：只经上游 ConfigStore 的公开 CRUD 访问上游拥有的覆盖表，与上游 handler 同一出口。不接触其他 EE 模块的数据，没有双向依赖。
- **日志**：经宿主 logger 注入，没用标准库 `log`。
- **例外**：`Match` 导出面多余，见 R3。

## 出口清单

### 本轮修复项

**R1 汇率"有限正数"规则四处实现，同一对象重复校验**

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/app/pricing.go:31`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:77`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/pricefile.go:94`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/overrides.go:59`
  - 附带：`sync.go:65` 与 `sync.go:95`、`app/pricing.go:57-63`
- **代码事实**
  - 同一条判定写了四份，表达式还不一样：app 写的是 `rate <= 0 || IsNaN || IsInf`，根包三处写的是 `!finiteNonnegative(x) || x == 0`。
  - `New` 对同一份文件调了两次 `validate()`：一次在 :65，拷贝后在 :95 又一次。
  - `Load` 已校验过的文件进入 `New` 后又被校验，于是 `app/pricing.go:61` 的"initialization failed"分支只在 deps 为 nil 或文件无效时可达，而 app 调用时这两种情况都不会出现。
- **维护影响**：违反编码规范"同一条规则只在一处实现"。以后若允许汇率为 0 以外的边界值，或调整精度约束，需要同步改 4 处且无人提示。读者也会误以为 `Load` 之后文件还可能无效。
- **最小修正**
  - 根包保留一个判定函数（如 `validRate(float64) bool`）。app 读取环境变量时复用它：导出一个包 `ErrConfig` 的解析函数，或导出该判定函数。`validate`、`toUSDPerToken`、`New` 引用同一函数。
  - `New` 内只保留一次 `validate()`。
  - app 中不可达的非 `ErrConfig` 初始化分支，按保留后的校验情况决定是否删除。

**R2 手工映射的归一化、查重、校验在三处重复**

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:37-45`（`ParseVendorMap`）
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:98-108`（`New`）
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:63-65`（`matchVendors`）
  - 注释：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:33`
- **代码事实**
  - `ParseVendorMap` 已完成小写归一、查重和 `validateVendorMap`。`app/pricing.go:47,56` 把结果原样交给 `New`。
  - `New` 又小写一遍、查重一遍、`validateVendorMap` 一遍。
  - 每次 `Sync` 时，`matchVendors` 对同一个 `s.mapping` 和 `s.file` 再校验一遍。
  - `Options` 的注释称 VendorMap 是"已解析的部署配置"，与 `New` 重新解析的行为矛盾。
- **维护影响**：若映射规则变化（例如键改为保留大小写，或允许一对多），要改三处。三处错误文案重复，重复的 `duplicate vendor mapping` 在 `New` 里对已归一化的 map 永远不会触发。
- **最小修正**
  - 在 `Options` 注释写明 VendorMap 须来自 `ParseVendorMap`。
  - 删除 `sync.go:98-105` 的二次归一化和查重，只在 `New` 保留一次 `validateVendorMap`，作为导出构造函数的边界。
  - 删除 `matchVendors` 开头对同一对象的重复校验（`vendors.go:63-65`）。

**R3 `Match` 无包外使用方却导出；`logger` 接口与其余依赖接口分处两文件**

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:18-19`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:21-24`
  - 对照：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:19-31`
- **代码事实**
  - `grep` 显示 `Match` 只在 `vendors.go:62,82,89,110` 使用，ee 内没有 `pricing.Match` 引用。
  - `Deps.Log` 的接口类型定义在 `vendors.go`，`Deps` 另外三个依赖接口定义在 `sync.go`。
- **维护影响**
  - 违反硬规则"包外没有使用方的符号不导出"，导出面会被误当作模块公开契约。
  - 读 `Deps` 时，要跨文件才能找齐注入的四个接口。
- **最小修正**：`Match` 改为未导出（如 `match`）；把 `logger` 接口移到 `sync.go`，与 `overrideStore`、`catalog`、`providerLister` 放在一起。

**R4 位置式或超长单行结构体字面量**（开放式复读发现）

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:109`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/persistence/store.go:24`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/overrides.go:87`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/persistence/store.go:117`
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/persistence/store.go:124`
- **代码事实**
  - `&Service{deps.Overrides, deps.Catalog, deps.Providers, deps.Log, copyFile, mapping}` 按位置初始化 6 个字段，`&Store{config, catalog}` 同样按位置初始化。
  - `makeOverride`、`fromTable`、`toTable` 各把 10 个以上字段写在一行，单行超过 300 字符。
- **维护影响**
  - `Service` 调整字段顺序或插入字段时，位置式字面量可能静默错位：同类型的字段（`overrideStore` 与 `providerLister` 都是接口）编译器不一定能拦住。
  - 双向转换是否对称，要横向滚动逐字段比对才能看出。
- **最小修正**：改为带字段名的字面量，按字段换行（gofmt 风格）。`fromTable` 与 `toTable` 字段顺序保持一致，便于对照。

**R5 定价环境变量常量没有逐条注释，与 identity 装配惯例不一致**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/app/pricing.go:19-23`
- **对照**：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/app/identity.go:23-28`
- **代码事实**：identity 的每个环境变量常量都注明含义、默认值和空值行为；定价的三个常量没有任何注释。方案 §4.6 要求"环境变量常量与读取方式对齐 `app/identity.go`"。
- **维护影响**：单看代码不知道 `EE_PRICING_FILE` 为空时用嵌入文件，也不知道汇率单位是"1 美元兑多少人民币"，得去翻 `ee/docs`。
- **最小修正**：按 identity 的写法给三个常量补一行中文注释，说明含义、单位和未设置时的行为。

**R6 说明文字与现状不符（三处）**

- **(a) Makefile 帮助文字与执行顺序相反**
  - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/Makefile:42-45`
  - 事实：帮助文字写"验证品牌……再跑 SQLite 身份与国内定价冒烟"，实际第一条执行的是 `pricing-smoke.py`，然后才是品牌和身份。
  - 修正：调整命令顺序或文字，二选一。
- **(b) app README 的 Bootstrap 条目已过时**
  - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/app/README.md:7`
  - 事实：条目仍写"最后 `attach` 品牌路由并复用同一份 EE 鉴权中间件"。定价已接入 attach，只在文末单独成段，而 `bootstrap.go:16` 的注释已经更新。
  - 修正：在该条末尾补"及国内定价同步"。
- **(c) 冒烟脚本的类说明与实现不符**
  - 位置：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke.py:23`、`:48-73`
  - 事实：类说明写"复用现有 HTTP/登录夹具"，但 `start` 实际是 `identity-smoke.py:58-81` 的整段抄写（环境变量前缀过滤、Popen、就绪轮询），只多了定价变量注入和 `log_offset`，而且没有注明来源。
  - 修正：在 `start` 上加一句"抄自 `identity_smoke.Node.start`：父类会剥掉 `EE_*` 变量，而方案 §8 禁止修改父脚本；父类的就绪检查或隔离规则变化时需同步"。

**维护影响（R6 共同）**：说明与现状矛盾，会误导运维查冒烟失败顺序、新人读装配流程，或父夹具修改后漏同步。

### 建议取舍项

**S1 `New` 的深拷贝范围大于需要**

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:71-94`
- **代码事实**：`New` 深拷贝了 `Rates`、`Vendors`、`EndpointHosts`、`Models` 和每个 `CacheReadInputCost` 指针。但 `New` 只改写 `Rates[CNY]`，`Service` 此后不改文件，app 调完 `New` 也不再持有 `file`（`app/pricing.go:42,56`）。
- **维护影响**：24 行防御性拷贝会让读者以为别处会修改价格目录；以后给 `ModelPrice` 加指针字段时，还得记得补拷贝。
- **最小修正**：只为 `Rates` 建新 map，再应用汇率覆盖，其余字段直接引用。
- **反证条件**：若已计划让 `Service` 长期持有、并由调用方在运行期复用或修改同一个 `PriceFile`（例如后续做热重载），完整深拷贝有保留价值。
- **去向**：由实施者决定采纳或撤回，不挂账。

**S2 `persistence.Store` 一个类型承担三种适配角色**（开放式复读发现）

- **位置**：`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/persistence/store.go:16-24,55-59`
- **代码事实**：`Store` 自身的 CRUD 方法只用 `config` 字段；`catalog` 字段只为经 `Catalog()` 构造 `catalogAdapter`。导出方法 `Catalog()`、`Providers()` 返回未导出类型，app 需要 `store.Catalog()`、`store.Providers()` 这样二级取用（`app/pricing.go:56`）。
- **维护影响**：`NewStore(config, catalog)` 的签名暗示 CRUD 会联动内存目录，实际并不联动（联动在 `Sync` 里）。返回未导出类型的导出方法，在包外无法命名其类型，写测试或替换时不方便。
- **最小修正**：拆成 `NewOverrides(config)`、`NewCatalog(catalog)`、`NewProviders(config)` 三个独立的小构造函数，由 app 分别注入 `Deps`。
- **反证条件**：若后续需要在同一个适配器里保证"写表与更新目录"的原子顺序或共享状态（例如加进程内锁），合在一个 `Store` 更合适。
- **去向**：采纳与否由实施者决定。不采纳时不必立债，因为事后拆分只涉及本包和 app 一行。

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [国内定价]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- `ee/internal/app/pricing_test.go:25-31` 的 `pricingLogger` 嵌入了 nil 的 `schemas.Logger`：被测路径一旦新增 Info/Debug 调用会 panic，而不是断言失败。测试是否稳健需核实。
- `ee/internal/pricing/sync.go:176` 用 `reflect.DeepEqual` 比较整行（含 `RequestTypes` 顺序、空串与 nil 的往返）来判断"未变"。PostgreSQL 往返的一致性，冒烟只在 SQLite 上覆盖过。
- `ee/internal/pricing/sync.go:201-206`：表操作成功后目录 Upsert 失败，导致内存与表不一致，文档（`ee/docs/国内定价.md:53`）已写。这一失败语义是否可接受需核实。
- `ee/docs/国内定价.md:58` 称 `source_of_truth=config.json` 时"无费用为零窗口"，依赖 `lib/config.go:3550` 的清理先于 attach。时序需核实。
- `ee/internal/pricing/vendors.go:92-104` 的主机匹配，对大小写、结尾点号、IDN 主机和带端口地址的边界需核实。

## 台账对账

**涉及的已有 Finding**
- **FIND-012**（attach 惯性点）：本次在 `bootstrap.go:42,58` 为 attach 增加一个 `log` 参数和一行 `assemblePricing` 调用，定价的装配细节放在独立的 `app/pricing.go`。attach 仍是薄装配，未恶化，继续观察。
- FIND-013 至 FIND-021：本次未触及。

**看似可改但有理由保留**

| 事项 | 保留理由 |
|---|---|
| `model.go:23-24` 保留未选用的 `HighestTierPeakRate` | `pricing_rule` 是价格文件契约字段，方案 §4.2 规定校验"在枚举内"，§9 写明 A1、A2 切换只改数据文件，不改代码 |
| 目录适配（内存 ModelCatalog）放在 `persistence/` | 方案 §4.1 已批准的布局；按架构 §7，库类型适配归子包而非 `host`，只是名字不完全贴合 |
| 取价规则文字在 `ee/internal/pricing/README.md:16-27` 与 `ee/docs/国内定价.md:7-18` 逐字重复 | §9「A1 取价细则」明确要求两处都写 |
| `vendors.go:108` 是中文日志，其余日志为英文 | 方案 §4.3 规定了原文，产品验收第 7 条和冒烟 `pricing-smoke.py:136` 按原文断言 |
| `PricingNode.start` 整段重写（结构本身，不含 R6(c) 的注释缺口） | 父类会剥掉 `EE_*` 变量，方案 §8 禁止修改 `identity-smoke.py`；只要求补注释（R6(c)），不要求改结构 |
| `app/pricing.go:57-69` 两处 `errors.Is(err, pricing.ErrConfig)` | §9 失败策略澄清：配置错误可能在 `New` 或 `Sync` 内发现（多命中在 `Sync`），两个调用点各需分流 |
| `data/cn-prices.json` 超过 500 行 | 这是数据文件，不是代码；包 README :35 已说明，拆分会改变外部替换文件的契约 |
| 启动时 Upsert 未变化的行（`sync.go:201`，上游 `config.go:3418` 已装载过） | 方案 §4.5 第 4 步规定一次 Upsert 整个期望集，结果幂等 |
