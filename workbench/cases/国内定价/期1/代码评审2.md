---
title: 国内定价 期1 代码评审 #2
type: 代码评审
case: 国内定价
phase: 1
round: 2
verdict: 需修改
created: 2026-09-13
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 380b87f1a3ea70606a0f8cbbdbd65fdd996c05cd 到候选 c7f8ed235f8cc295a06d06905a7464a7746014a8；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：需修改。** 上一轮 R1“smoke 失败现场丢失”已修复并亲核关闭；本轮从转交项确认一处生产行为缺陷：官方域名带末尾点号时识别失败，重启会删除已有定价覆盖。其余差量未发现有证据支持的行为回归。构建、三项真实 HTTP smoke、六项脚本回归及串行全量 race 均通过；真实 key 调用与页面人工核对仍待 Gate 2。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 实际范围 | `380b87f1..c7f8ed23` 的 EE 差量恰为指定 16 个文件，均已通读完整现状；开始时无未跟踪文件 |
| 声明修改 | 汇率校验复用、映射校验调整、只复制 Rates、三个独立适配器、smoke 生命周期修复，与实施偏差第 5 条对应 |
| 接口面 | 新增 `ValidRate`、`NewOverrides/NewCatalog/NewProviders`、取消导出的 `Match` 均有偏差登记；调用方已迁移 |
| 产品边界 | 未新增 HTTP API、表、依赖；上游五目录零差异；价格数据、内置厂商边界及既有身份/品牌脚本未改 |
| 多做／少做／未登记偏差 | 未发现额外功能偏差；官方域名识别存在 R2 所述验收缺口 |
| 验收覆盖 | 重启幂等、手工覆盖保留、非法文件继续启动并保留旧行、未知域名提示经 smoke 验证；费用引擎与未收录模型有测试支持。真实请求日志费用、实际页面人工核对未执行 |

已批准的 A1 取价、Kimi 缺价、逐行部分提交、多节点运行期内存差异不重新提出。

## 3. 本轮修复项

**R2 · P2：带末尾点号的官方域名被误判，已有覆盖随重启删除。**

- **位置：** [vendors.go:127](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:127)，匹配处 [vendors.go:92](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:92)，删除后果在 [sync.go:183](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/sync.go:183)。
- **代码事实：** `endpointHost` 只转小写，保留 DNS 根域末尾的 `.`；该结果无法与无末尾点的白名单相等或后缀匹配。
- **触发条件与后果：** 无手工映射，将接入地址改为 `https://dashscope.aliyuncs.com.:443/compatible-mode/v1` 后重启。亲测同一隔离数据库：普通域名创建 2 行，大写加端口保持 2 行，末尾点号地址变成 0 行；日志明确出现 `deleted=2 unrecognized=[myqwen mystery]`。因此同一官方主机的等价写法会失去覆盖，回退原查价路径，可能重新零计费。
- **最小修正：** 对解析出的主机及白名单主机统一去掉一个合法的末尾根域点，再比较；补末尾点、末尾点加端口、重启保留覆盖的回归，保留现有伪后缀拒配断言。

上一轮修复项逐条处置：

| 上轮项 | 处置与证据 |
|---|---|
| R1：失败现场丢失、金额断言缺上下文 | **已修复。** [pricing-smoke.py:194](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke.py:194) 在 finally 回收进程，仅成功删除目录；第 128 行输出模型、字段、实际值及预期值。六项回归通过；本轮 R2 的真实失败也保留了配置、价格文件、数据库及包含三次启动的完整日志 |

## 4. 需用户裁决／有依据撤回

**需用户裁决：无新增事项。**

收敛侧五条转交逐条处置：

| 转交项 | 处置及依据 |
|---|---|
| `pricingLogger` 嵌入 nil Logger | **撤回作为本轮必修缺陷。** nil 嵌入事实存在，但当前两组测试路径均在成功同步的 Info 调用前返回，只调用已实现的 Error/Warn；app race 测试通过。未发现当前假通过；不据此保证未来新增日志调用仍安全 |
| `reflect.DeepEqual` 与 PostgreSQL 往返 | **撤回数据库往返必然不一致的质疑。** `RequestTypesJSON`、patch 均为 text；BeforeSave/AfterFind 保留数组顺序；适配器把 nil 作用域指针统一还原为空串；比较不含时间戳。SQLite 实际重启为 `unchanged=2`。PostgreSQL 未实测，不能宣称其集成验证通过 |
| 表成功、目录 Upsert 失败 | **撤回。** 方案 §9 明确批准记录错误、继续启动并保留已提交行；`Sync` 返回带阶段的错误，app 输出 error；上游目录先完成全部转换再替换内存。故障测试通过 |
| `source_of_truth=config.json` 的启动时序 | **撤回时序质疑。** 已追到 `LoadConfig` 内清理 → 上游 Bootstrap 返回 → EE attach 同步 → `server.Start` 中 `net.Listen`。正常重建发生在监听前；失败仍按已批准的降级策略处理 |
| 主机大小写、末尾点、IDN、端口 | **修复：列入 R2，仍未关闭。** 大写及数字端口已实测正确；末尾点已复现误判并删行。IDN 未做规范化，现有官方白名单均为 ASCII，未确认其他 IDN 误配，不扩大结论 |

## 5. 转收敛评审／挂账

- 方案实施偏差第 5 条的预飞限制说明与预飞事实表的“全绿”状态对齐：转收敛评审。

**挂账：无。**

## 6. 开放式复读的表外发现

已按“每个文件出问题时如何发现、如何恢复”复读全部 16 个差量文件，重新追查配置拒启、同步中止、目录失败、重启恢复和脚本回收路径。**无新增表外发现。** R2 属起手表及转交项内发现。

## 7. 起手表覆盖

| 信号 | 结果 |
|---|---|
| 验收标准有未覆盖项 | **发现：** R2；第 1、2 条真实调用与页面人工核对待 Gate 2 |
| 方案、实施记录、diff 三方不一致 | 无新增功能不一致；预飞状态文字转交 |
| 多出的功能、公共接口、抽象或依赖 | 无未登记项 |
| 偏差未登记 | 无新增范围偏差 |
| 失败路径被吞或降级成假成功 | 无；批准的降级保留 error 日志 |
| 空值、空集合与非空无分支 | 无；nil 网络配置、空厂商、空目录、可选缓存均有处理 |
| 取消或超时后仍产生副作用 | 无已证实缺陷；CRUD 传递 context，无新增后台同步任务 |
| 重发产生两份副作用 | 无；稳定 ID、冲突处理及两次重启通过 |
| 事务外写后读／多方仅一方回滚 | 无未批准问题；逐行提交与部分保留符合裁定 |
| 开关关闭、旧数据、旧客户端无处理 | 无；空目录清理、手工行保护、内置厂商跳过已核 |
| 昂贵检查在前／校验缺层 | **发现：** R2 的主机归一化缺口 |
| 路由保护、认证关闭行为 | 无新增绕过；沿用管理路由 EE 鉴权链 |
| 敏感信息进日志或响应 | 无新增已证实问题；识别日志输出主机而非完整凭据 URL |
| 热路径新增 I/O、无界缓冲或循环 | 无；新增工作在启动期 |
| 新配置、环境依赖、部署步骤未登记 | 无；三项环境变量及重启要求已说明 |
| 服务端无法追溯失败原因 | 无；阶段错误与 app 日志可追溯 |
| 对外泄露内部错误原文 | 无新增对外错误出口 |
| 脚本失败现场被清理 | 无；上轮 R1 已亲核关闭 |
| 新规则或失败路径无测试 | **发现：** 缺少 R2 对应的末尾点及覆盖保留回归 |
| 断言绑定实现而非行为 | 无影响结论的问题；主体断言检查行、金额、错误及重启结果 |
| 失败信息无法定位条件 | 无本轮新增问题；金额诊断已修 |
| 返回值、错误未检查 | 无新增；上轮已撤回项未恶化 |
| fixture 依赖个人环境 | 无；隔离数据库、随机端口、虚假 key |
| mock 结果写成真实环境结论 | 无；生命周期回归与真实 HTTP smoke 分开验证 |
| 整体替换模块的 mock | 无影响结论的问题；流程替换仅用于生命周期测试，实际 smoke 另跑 |
| 调试输出、临时后门、本机配置 | 无 |
| 未使用代码、无关改动 | 无范围内确认问题；旧构造器调用已迁移 |
| 生成物与源不一致 | 无；543 个 UI 文件与 embed 副本逐字节一致 |

## 8. 验证记录

以下 `S` 指实际已设置的 `TMPDIR`：

`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/workbench/tmp/codereview/国内定价-期1-代码评审2/scratch`

| 检查 | 实际命令 | 结果 |
|---|---|---|
| 格式 | `cd ee && test -z "$(gofmt -l ./internal ./cmd)"` | 0 |
| Vet | `cd ee && GOWORK=off go vet ./...` | 0 |
| Race 首跑 | `cd ee && GOWORK=off go test -race -count=1 ./...` | 1；app、branding/http 链接遇磁盘 `errno=28` |
| Race 串行复跑 | `cd ee && GOWORK=off go test -race -count=1 -p 1 ./...` | 0，全部通过 |
| UI 依赖 | `cd ui && npm ci` | 0；输出 14 项依赖漏洞提示，未修改依赖 |
| UI 构建 | `make -C ee build-ui` | 0；embed 文件集合及内容一致 |
| 二进制 | `cd ee && GOWORK=off go build -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 0 |
| 定价 smoke | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/pricing-smoke.py --binary "$TMPDIR/bifrost-http"` | 0，全部 PASS |
| 品牌回归 | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/branding-smoke.py --binary "$TMPDIR/bifrost-http"` | 0 |
| 身份回归 | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | 0 |
| JSON | `python3 -c "import json,sys;json.load(open('ee/internal/pricing/data/cn-prices.json'))"` | 0 |
| 根包依赖 | `! grep -l 'gorm.io\|maximhq/bifrost' ee/internal/pricing/*.go` | 0 |
| 上游零差异 | `git diff --quiet f371548b5444693d7e259033bec9f4a467e3748b -- core framework plugins transports ui` | 0 |
| Workbench | `python3 workbench/tools/build_views.py --check` | 0，124 篇校验通过 |
| 空白检查 | `git diff --check` | 0；额外检查 EE 差量亦通过 |
| 脚本回归 | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/pricing-smoke-test.py -v` | 0，6 项通过 |

另外逐项运行 checklist 的存在检查及原始 grep：**13 个必存产物、14 个必含符号、5 个禁留检查全部通过。**

主机探针使用 `PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'`，内存加载现有 smoke 模块，复用 `PricingNode`、`price_file` 和 `main`；依次写入隔离配置的三个主机并启动、登录、查询覆盖，断言始终存在 2 行。结果：

```text
dashscope.aliyuncs.com        → 2 行，created=2
DASHSCOPE.ALIYUNCS.COM:443    → 2 行，unchanged=2
dashscope.aliyuncs.com.:443   → 0 行，deleted=2，断言失败，退出 1
```

[失败现场](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/workbench/tmp/codereview/国内定价-期1-代码评审2/scratch/ee-pricing-smoke-hs50vru2)已检查：四类文件均保留，完整日志 155,360 字节，含三次同步；只读查询数据库确认覆盖行数为 0。

版本与范围记录：

- 开始：`feat/domestic-pricing`，HEAD 为 `c7f8ed235f8cc295a06d06905a7464a7746014a8`，状态干净。
- 期间外部操作新增并提交了收敛评审记录及视图；未读取该评审内容。
- 结束：HEAD 为 `f266198cb37618c5fba641b571f74270fbb320fe`；工作树、索引干净，EE 与候选 `c7f8ed23` 零差异。
- 二进制 SHA-256：`f148a7d37f5b5f062e55603f0f7338bf479bb912f6342e755cd45d34fc1fbdf2`。

实际阅读：指定规则、验收口径、方案、checklist、findings、两个证据和代码评审1；16 个差量文件全文；另追读未修改的定价模型与测试、app 装配测试、共用 smoke 夹具、构建脚本，以及上游覆盖 CRUD、表钩子、目录更新和监听/鉴权链。

未执行与限制：**checklist 无跳过项**；未运行生成报告的总入口。未进行真实 key 调用、页面人工核对、PostgreSQL 实例测试、dev server 或已有实例操作。`ps` 因沙箱权限失败，无法独立扫描全局进程；脚本回收路径及六项回归中的子进程退出断言已验证。全程未修改代码、索引或报告，未暂存、提交、推送。

verdict: 需修改
