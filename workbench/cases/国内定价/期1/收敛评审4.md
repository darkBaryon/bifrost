---
title: 国内定价 期1 收敛评审 #4
type: 收敛评审
case: 国内定价
phase: 1
round: 4
verdict: 有Blocking
created: 2026-09-13
---

> 评审者与实施上下文隔离。本轮是差量复核：上一轮候选 `f6311fd4bbffec6b59adc5fa03f343a3053f6448`，本轮候选 `1089ea233e80a7dd919ca3f85ea9d5e65a7a6bf4`，工作树干净，没有未跟踪文件。基线 `f371548b5444693d7e259033bec9f4a467e3748b`，风险等级 L2。

## 结论

上一轮的 R1（a）、R1（b）都已修复。

本轮 ee 改动有四处：
- 使用说明补了归一规则；
- Makefile 帮助文字与执行步骤对齐；
- `validate()` 复用 `normalizeHost` 拒绝非法白名单；
- 冒烟新增一次不改配置的重启。

结构和依赖方向不变。主机归一仍然只有 `vendors.go:131-133` 一份实现，文件校验直接调用它，没有另写一套"去点"规则，这个方向是对的。

问题出在同一提交里。R1（a）在使用说明里新写的那句，与 (c) 落地的校验行为不一致：
- 使用说明 `ee/docs/国内定价.md:24` 写"白名单……多个末尾点不视为等价"，读者会理解为"写了只是匹配不上"；
- 实际上 `pricefile.go:134-137` 会把这类白名单（以及以点开头的白名单）判为整份价格文件无效，跳过全部厂商的同步；
- 包 README `:8` 已写明校验规则，使用说明没有写。

这是范围内的纯文字修正，不需要用户裁决。修复并经定向复核之前，verdict 保持有Blocking。

## 事实来源

**执行的命令**
- `git status --porcelain --untracked-files=all`：输出为空。
- `git rev-parse HEAD`：`1089ea233e80a7dd919ca3f85ea9d5e65a7a6bf4`。
- `git diff --stat f6311fd4b..1089ea233`：共 14 个文件。
  - 6 个在评审范围内，与任务清单一致：`ee/Makefile`、`ee/docs/国内定价.md`、`ee/internal/pricing/{README.md,pricefile.go,pricefile_test.go}`、`ee/scripts/pricing-smoke.py`。
  - 其余是 workbench 的评审记录、生成物和方案v2 偏差记录，不在评审范围。
- `git diff f6311fd4b..1089ea233 -- ee workbench/cases/国内定价/期1/方案v2.md`：逐个 hunk 对照。
- `git log --oneline f6311fd4b..1089ea233`：3 个提交，其中只有 `1089ea233` 改动 ee。

**读过的规则与裁决**
- 根 `AGENTS.md`、`workbench/AGENTS.md`。
- 规范：`规范/流程.md`、`流程/收敛评审.md`、`流程/收敛评审审查方向.md`、`项目/编码规范.md`。
- `templates/收敛评审.md`。
- `product/架构/EE后端架构.md` §4–§7。
- `workbench/findings.md` 全文。
- `方案v2.md`：§9「Gate 决策结果」（:277-298）、「实施记录」与「实施偏差记录」第 1–7 条（:372-416）。
- 上一轮记录 `收敛评审3.md` 全文。

**同类实现与对照**
- `ee/internal/pricing/vendors.go` 全文：`normalizeHost` 在 :130-133；匹配调用点在 :91、:127。
- `ee/scripts/identity-smoke.py`：用 grep 核对 :42-43 的 `CookieJar` 夹具和 :120-126 的 `login`，以确认 `pricing-smoke.py:151` 的"复用已持久化的登录会话"注释说的是实际机制。
- `ee/internal/pricing/sync.go:195`：同步日志格式的唯一出处，冒烟断言按它书写。

**关键假设核实**
- **归一规则只有一份实现**：对 `ee/` 做 grep `normalizeHost|HasPrefix\(normalized|HasSuffix\(normalized`，只命中 `pricefile.go:134-135` 和 `vendors.go:91,127,131`。校验和匹配调用的是同一个函数。
- **白名单非法时整份文件失效**：`parsePriceFile` 在 `validate()` 出错时返回 `PriceFile{}`（`pricefile.go:97-99`）。使用说明 `:54` 规定文件校验失败时"记 error，不同步"。所以 `a.com..`、`.a.com`、`.` 的后果是整份文件被拒，而不是这一条匹配不上。`pricefile_test.go:17-19,43-45` 断言了这一点。
- 本轮结论不依赖上游行为。

### 上一轮本轮修复项的处置

| 项 | 处置 | 证据 |
|---|---|---|
| R1（a）使用说明缺归一规则 | 已修复；新增文字引出本轮 R1 | `ee/docs/国内定价.md:24` 新增一段："主机名与白名单均忽略大小写，并各去掉一个末尾根域点；多个末尾点不视为等价"，与 `vendors.go:127,131-133` 的匹配行为一致。这句写于 (c) 新增白名单校验的同一提交，对白名单一侧的描述已与校验行为不符，见本轮 R1 |
| R1（b）`smoke-pricing` 帮助文字缺回归步骤 | 已修复 | `ee/Makefile:50` 改为"先跑国内定价失败现场回归，再做隔离 SQLite 同步、重启幂等与汇率冒烟"，与 :51-52 的执行顺序一致，也与 `ee/README.md:36` 同口径 |

### 起手表覆盖

| 维度 | 信号 | 结果 |
|---|---|---|
| 重复 | 同一数字或限制多处出现 | 无代码问题。`pricing-smoke.py:155` 用 `len(models)`，而 :143、:165、:189 用字面量 `2`，写法不统一，保留理由见台账对账 |
| 重复 | 同一规则多份实现且互不注明 | 无。校验（`pricefile.go:134`）直接调用 `normalizeHost`，没有再写一遍"小写并去一个点" |
| 重复 | 重造已有 helper | 无 |
| 重复 | 派生常量无推导 | 不适用。本轮没有新增常量 |
| 职责与实现质量 | 魔数 | 无 |
| 职责与实现质量 | 状态码、错误码裸数字 | 不适用。没有接口出口改动 |
| 职责与实现质量 | 无名的错误形状 | 无。校验错误沿用同一函数内 `fmt.Errorf("%s: …", v.ID, …)` 的形状 |
| 职责与实现质量 | 紧凑但绕的控制流 | 无。`pricefile.go:131-137` 是两个连续判断：先查原始字符，再查归一后的形状，顺序自然。冒烟 :151-156 新增的重启是一段线性步骤，没有并进后面的循环，可读 |
| 职责与实现质量 | 文件集中或拆分 | 无。`pricefile.go` 为 157 行，校验留在 `validate()`；`pricefile_test.go` 为 132 行，新用例并入原表驱动表 |
| 职责与实现质量 | 多余状态或守卫 | 无。`:131` 的 `TrimSpace==""` / `ContainsAny` 查原始字符，`:135` 查归一后首尾的点，两者防的不是同一种输入，不重复 |
| 职责与实现质量 | 同一效果做两次 | 无 |
| 职责与实现质量 | 注释与现状矛盾 | 无。`pricing-smoke.py:151` 的注释与 `identity-smoke.py:42-43,120-126` 的 cookie 夹具一致；:157 和 :166 的 PASS 文案与步数一致；`Makefile:50` 已对齐 |
| 架构与项目一致性 | 接缝形状与同类不同 | 无。白名单校验留在文件校验里，与 `endpoint_hosts` 的原有校验（:127-133）同处，没有下沉到匹配阶段或 app |
| 架构与项目一致性 | 架构符合性 | 无，见四维核查 |
| 架构与项目一致性 | 依赖关系 | 无，见四维核查 |
| 架构与项目一致性 | 绕过既有出口 | 不适用。本轮没有存储或接口改动 |
| 架构与项目一致性 | 把上游现状当契约 | 无。本轮不依赖上游行为 |
| 契约一致性 | 后端类型、前端类型、开发指南三方不一致 | **发现** R1：`ee/docs/国内定价.md:24` 对白名单的描述与 `pricefile.go:134-137` 的校验不一致，也缺少包 README `:8` 已写的校验规则 |
| 契约一致性 | 状态码口径不一 | 不适用 |
| 契约一致性 | 省略、空、值三态语义不对应 | 无。空白名单仍由 `:127` 的 `len==0` 与 `:131` 的 `TrimSpace` 拒绝；`normalizeHost(".")==""` 由 :135 拒绝，匹配端 `vendors.go:92` 的 `host != ""` 守卫不变 |

### 开放式复读

已按"半年后要改这里的人"的视角，把 6 个范围内文件的完整现状重读一遍：

- **`pricefile.go`**：:136 的错误文字写明了原因（"normalized host must be nonempty and must not start or end with a dot"），维护者不必去翻 `normalizeHost` 就知道拒绝的原因。两段 `invalid endpoint host %q` 前缀相同、后缀不同，日志检索时仍能归到一类。没有让人皱眉的地方。
- **`pricefile_test.go`**：新增的 5 个用例并入已有表，:43-45 的"不返回部分目录"断言对所有非法用例生效，与 `Load` 注释 :44 的承诺对应。没有让人皱眉的地方。
- **`pricing-smoke.py`**：步骤顺序是首跑 → 原配置重启 → 两次等价地址重启 → 非法文件 → 汇率变化，按函数顺序读下来就是覆盖清单。唯一会让人停一下的是 :155 与 :165 的 `unchanged` 写法不一样，已在起手表记录，按保留处理。
- **`Makefile`、`pricing/README.md`**：没有让人皱眉的地方。
- **`ee/docs/国内定价.md`**：维护外部价格文件的人读到 :24，会以为 `.dashscope.aliyuncs.com` 这类白名单"只是匹配不上"。这就是 R1，已在起手表记录。

表外发现：无。

### 实际阅读范围与未执行项

**完整读过**
- `ee/Makefile`
- `ee/docs/国内定价.md`
- `ee/internal/pricing/{README.md,pricefile.go,pricefile_test.go,vendors.go}`
- `ee/scripts/pricing-smoke.py`

**部分读过**
- `product/架构/EE后端架构.md` §4–§7。
- `方案v2.md` :277-298 与 :372-420。
- 用 grep 读取：`ee/scripts/identity-smoke.py` 中 cookie、login、call、stop 相关行；`ee/internal/pricing/sync.go:195`。

**未执行**
- 按只读约束，没跑 go build、vet、test，没跑 Python 冒烟、回归脚本和 preflight。
- 没读 `sync.go` 其余部分、`overrides.go`、`model.go`、`persistence/`、`app/pricing.go`、`cn-prices.json`；本轮没有改动它们。
- 按要求没读代码评审记录和方案评审记录。
- 没评审 workbench 生成物与评审记录。

## 四维核查

| 维度 | 发现 | 证据 |
|---|---|---|
| 重复 | 无。校验复用唯一的归一函数 | `pricefile.go:134`、`vendors.go:131-133`；grep 没有其他实现 |
| 职责与实现质量 | 无 | `pricefile.go:130-138`、`pricing-smoke.py:151-156` |
| 架构与项目一致性 | 无变化 | 见下文 |
| 契约一致性 | 使用说明对白名单的归一描述与校验行为不符（R1） | `ee/docs/国内定价.md:24` 对照 `pricefile.go:134-137`、`pricing/README.md:8` |

**架构符合性（对照 EE 后端架构 §4、§7）**
- **§4 根包承载规则**：白名单合法性属于价格文件契约，放在根包 `pricefile.go` 的 `validate()`，由同包 `vendors.go` 的非导出 `normalizeHost` 提供归一。没有下沉到 `persistence`，也没有由 app 预处理。
- **§7 适配归位**：根包仍只依赖标准库，本轮没有触碰适配子包和宿主接入。

**依赖关系（对照架构 §5、编码规范「依赖关系」）**
- **import 不变**：`pricefile.go:4-11` 与改动前相同；`pricefile_test.go:4-9` 只用标准库。
- **包内调用**：`pricefile.go` → `vendors.go` 是同包文件之间的调用，不形成包依赖；`normalizeHost` 仍不导出。
- **注入与资源所有权**：没有新增导出符号、接口或共享资源。冒烟脚本仍复用 `identity_smoke.Node` 夹具，没有修改父脚本（方案 §8）。

## 出口清单

### 本轮修复项

**R1 使用说明对白名单的归一描述与文件校验不一致**

- **位置**
  - `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/docs/国内定价.md:24`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/pricefile.go:134-137`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/README.md:8`
  - 对照 `/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/docs/国内定价.md:37,54`
- **代码事实**
  - 使用说明 :24 用同一句话描述 `base_url` 主机和白名单："各去掉一个末尾根域点……多个末尾点不视为等价"。
  - 对 `base_url`，这句话成立：主机名不能匹配。
  - 对 `endpoint_hosts`，实际行为是 `validate()` 返回错误：归一后为空、以点开头或以点结尾（如 `.`、`a.com..`、`.a.com`）都会被拒，`parsePriceFile` 返回空目录，按 :54 跳过整次同步。
  - 包 README :8 写了"主机归一后须非空且首尾无点"，使用说明没有写。
- **维护影响**
  - 使用说明是维护外部 `EE_PRICING_FILE` 的依据（:37）。读者按 :24 会判断"多个末尾点或前导点的白名单只是这一条匹配不上"，实际却是整份文件失效、所有厂商都停止同步。
  - 失败模式被说轻了，从单条不匹配变成了全局跳过，排查时要从日志倒推到源码。
  - 同一条规则在包 README 与使用说明里再次分成完整和缺项两份，正是上一轮 R1（a）想消除的状态。
- **最小修正**
  - 把 :24 拆成两句，只改文字：
    - "`base_url` 主机名与白名单均忽略大小写，并各去掉一个末尾根域点（`dashscope.aliyuncs.com.` 等价）；`base_url` 带多个末尾点时不匹配。"
    - "`endpoint_hosts` 归一后为空、以点开头或以点结尾（如 `a.com..`、`.a.com`）时整份价格文件校验失败，按下文'价格文件解析/校验失败'处理。"

### 需用户裁决的 Blocking

- 无

### 立债(REF:立 `triggered_by: [国内定价]` 的需求进 reports/)

- 无

### 挂账(Finding:登记 findings.md,带四要件)

- 无

### 转代码评审(范围外发现,一行一条,不出结论)

- `ee/internal/pricing/pricefile.go:131`：原始字符检查只拒绝空格（`ContainsAny(host, "/: @")`），制表符、换行等其他空白夹在主机中间时能否通过校验、是否会静默匹配失败，需核实。
- `ee/scripts/pricing-smoke.py:152-154`：原配置重启后不重新登录，直接用上一进程签发的会话读取覆盖行；需核实该断言是否依赖会话在 SQLite 中跨进程有效，以及会话失效时失败信息能否定位。

## 台账对账

**涉及的已有 Finding**
- FIND-012：本轮没有改动 `bootstrap.go` 与 `app/pricing.go`，未恶化。
- FIND-013 至 FIND-021：本轮未触及。
- 本轮不新增 Finding。

**看似可改但有理由保留**

| 事项 | 保留理由 |
|---|---|
| `pricing-smoke.py:155` 用 `unchanged={len(models)}`，而 :143、:165、:189 用字面量 `2` | 编码规范允许脚本按契约独立书写字面量。夹具模型数一旦变化，字面量断言会立即失败，不会静默漂移。单独统一写法没有维护收益，下次触碰这些断言时顺手统一即可 |
| `pricefile.go:131` 与 :135 是两个 `if`，没有合成一个条件 | 前者检查原始字符，后者检查归一后的形状，错误文字也不同（:136 说明原因）。合并会变成一行长条件，丢掉原因说明 |
| `validate()` 只校验白名单、不把归一结果写回 `Vendor.EndpointHosts`，匹配时（`vendors.go:91`）再归一一次 | 上一轮已保留：业务模型保存文件原值，规则只在 `normalizeHost` 一处。本轮校验复用同一函数，没有使这项取舍恶化 |
| 原配置重启（:151-156）单独成段，没有并入 :158 的地址循环 | 这一步不改配置、不重新登录，与循环体的"改地址、写配置、登录"步骤不同，强行并入需要加条件分支。单独成段并配 PASS 输出更直接 |
| 上一轮"看似可改但有理由保留"表中各项 | 本轮未恶化，不再重提 |
