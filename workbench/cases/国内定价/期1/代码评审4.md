---
title: 国内定价 期1 代码评审 #4
type: 代码评审
case: 国内定价
phase: 1
round: 4
verdict: 通过
created: 2026-09-14
---

> 独立评审者：Codex 新会话（gpt-6-astra / high），由 tools/code_review.py 启动，与实施上下文隔离；以下为其最终回复原文。
> 基线 f6311fd4bbffec6b59adc5fa03f343a3053f6448 到候选 a2ca7af7a7ef04eab1988e5b84c53d1bba55d2c0；评审前后 workbench 之外的工作树与提交均未变动。

## 1. 总体判断

**verdict：通过。** 已对候选 `a2ca7af7a7ef04eab1988e5b84c53d1bba55d2c0` 完成差量复核，未发现本轮必修缺陷。新增白名单校验、原配置重启验证与实施声明一致；失败可观测、旧覆盖保留及恢复路径均有亲核证据。checklist 14 项命令全部通过，包括全量 race、构建和三项自包含 HTTP smoke。真实模型请求、日志费用与控制台页面人工核对仍待 Gate 2，本结论不代替产品验收。

## 2. 对照方案与接口面对账

| 对账项 | 结果 |
|---|---|
| 实际范围 | `f6311fd4..a2ca7af7` 的 EE 差量恰为指定六个文件，全部通读并复读；评审前后工作树、索引干净，无未跟踪文件 |
| 白名单校验 | 归一后为空、首尾带点时整份文件无效；对应实施偏差第 7(c) 条 |
| 原配置重启 | 增加一次不修改配置、价格文件和定价环境变量的重启；保留两次等价域名重启，对应第 7(d) 条 |
| 文字修改 | Makefile 帮助、README 和使用说明的变化均已登记 |
| 接口面 | 无新增公共符号、HTTP 路由、表、依赖或环境变量 |
| 多做／少做／未登记偏差 | 未发现 |
| 未修改区域影响 | 数据文件、换算、同步、持久化、装配及既有身份／品牌脚本无差量；已追读校验调用链和重启依赖，无需恢复全量评审 |

验收七条中，覆盖金额、重启幂等、手工行保护、非法文件保留旧行、未知域名提示已通过 HTTP smoke；费用引擎及未收录模型回退有测试。真实调用日志与页面显示的人工对照未执行。

本轮相关失败路径：

| 触发条件 | 最终处理与证据 |
|---|---|
| 文件读取、解析或新增主机校验失败 | 返回空目录；app 记录 error，跳过同步及映射校验，继续启动 |
| 合法文件前已有覆盖，随后加入非法白名单 | 三种非法主机均实测整份跳过、旧行完整保留；没有同步成功日志 |
| 修正非法白名单 | 实测恢复同步，`created=0 updated=0 deleted=0 unchanged=2` |
| 部署配置错误 | 既有 `ErrConfig` 分流保持不变，入口记录原因并退出 |
| 存储／目录失败 | 既有错误日志及逐行部分提交策略保持不变，符合 §9 裁定 |
| smoke 启动、超时或断言失败 | 沿既有 `finally` 回收自有进程，失败保留现场并重新抛出异常；六项回归通过 |

## 3. 本轮修复项

**无新增修复项。**

上一轮 `代码评审3.md` 的「本轮修复项」明确无新增项，其中回溯事项处置如下：

| 上轮事项 | 本轮处置与证据 |
|---|---|
| R2：末尾点误判并删除已有覆盖 | **已修复，未回归。** [vendors.go:132](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/vendors.go:132) 仍只去掉一个末尾点；[vendors_test.go:36](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/vendors_test.go:36) 起的识别正反例全部通过；[pricing-smoke.py:158](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke.py:158) 两次等价域名重启均保留 UUID、金额及手工行，日志计数符合预期 |
| 上轮同时检查的 R1：失败现场保留 | **已修复，未回归。** [pricing-smoke.py:193](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke.py:193) 的生命周期未改；六项失败现场回归亲跑通过 |

新增行为的关闭证据：

- [pricefile.go:134](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/pricefile.go:134)：非法白名单拒绝；[pricefile_test.go:17](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/internal/pricing/pricefile_test.go:17) 起五个新增用例通过，错误分支同时验证不返回部分目录。
- [pricing-smoke.py:151](/Users/xinyue/VSCode/ws_2026/bifrost-pricing/ee/scripts/pricing-smoke.py:151)：原配置重启真实通过，覆盖 UUID、金额、手工行及本次启动日志均被断言。

## 4. 需用户裁决／有依据撤回

**需用户裁决：无新增事项。** 既定 Gate 2 实测仍待完成。

**有依据撤回：无新增事项。** 已裁决的取价近似、收录限制、部分提交和多节点运行期内存差异不重提。

收敛评审 #5「转代码评审」原文为“无”，没有待修复、撤回或挂账条目。

## 5. 转收敛评审／挂账

转收敛评审：无新增项。

挂账：无。

## 6. 开放式复读的表外发现

已重新通读六个文件，检查故障如何暴露、旧状态如何保留、修正后如何恢复，重点复查整份文件失败范围、重启日志区间及脚本进程回收。

**已做开放式复读，无表外发现。**

## 7. 起手表覆盖

以下结果限本轮差量及受影响区域。

| 方面／信号 | 结果 |
|---|---|
| 对照方案：验收标准有未覆盖项 | **发现：** 真实请求日志费用及页面人工核对仍待 Gate 2 |
| 方案、实施记录、diff 三方不一致 | 无 |
| 多出的功能、公共接口、抽象或依赖 | 无 |
| 偏差未登记 | 无 |
| 行为：失败被吞或降级成假成功 | 无；非法白名单实测记录 error 且不输出同步成功日志 |
| 空值、空集合与非空无分支 | 无；空目录继续合法，非法主机返回空结果 |
| 取消或超时后仍产生写入或副作用 | 无新增问题；新增校验无存储副作用，脚本沿既有回收路径 |
| 重发产生两份副作用 | 无；三次重启行数、UUID 及日志计数通过 |
| 事务外写后读／多方只有一方回滚 | 无未批准变化；既有逐行提交策略未改，部分失败恢复测试通过 |
| 开关关闭、旧数据、旧客户端无处理 | 无；旧覆盖保留、空目录清理及手工行保护有处理 |
| 昂贵检查先于廉价检查／校验缺层 | 无新增问题；新增白名单检查在存储操作前完成 |
| 路由保护、认证关闭行为 | 不适用；本轮没有路由或鉴权变化，身份／品牌回归通过 |
| 敏感信息进日志或响应 | 无新增问题；新错误输出厂商及白名单主机，没有新增凭据读取 |
| 热路径新增 I/O、无界缓冲或循环 | 无；生产改动限启动期校验 |
| 配置、环境依赖或部署步骤未登记 | 无 |
| 可观测：服务端无法追溯失败原因 | 无；错误包含厂商、原始主机及原因 |
| 对外泄露内部错误原文 | 无新增对外错误出口 |
| 脚本失败现场被清理 | 无；六项生命周期回归通过 |
| 测试：新规则或失败路径无测试 | 无；五个新增用例及补充真实 HTTP 失败／恢复检查通过 |
| 断言绑定实现而非行为 | 无影响结论的问题；断言错误、返回目录、覆盖值和重启结果 |
| 失败信息无法定位条件 | 无新增问题；子测试名、断言位置及保留现场可定位 |
| 返回值、错误未检查 | 无本轮新增遗漏 |
| fixture 依赖个人环境 | 无；临时 SQLite、随机端口、虚假 key |
| mock 结果写成真实环境结论 | 无；生命周期测试、HTTP smoke 与 Gate 2 分别限定证明范围 |
| 整体替换模块的 mock | 无新增；既有故障注入仅用于生命周期验证 |
| 残留：调试输出、临时后门、本机配置 | 无新增 |
| 未使用代码、无关改动 | 无范围内确认问题 |
| 生成物与源不一致 | 无；543 个 UI 文件与 embed 副本逐字节一致 |

## 8. 验证记录

环境：`go1.27.1 darwin/arm64`。`TMPDIR` 为：

`/Users/xinyue/VSCode/ws_2026/bifrost-pricing/workbench/tmp/codereview/国内定价-期1-代码评审4/scratch`

使用 `cp -cR /tmp/bifrost-pricing-go-cache.jEGuAC "$TMPDIR/go-cache"` 创建独立缓存副本；以下 Go 命令均指定 `GOCACHE="$TMPDIR/go-cache"`。测试使用 `-count=1`，未复用测试结果。

| checklist 项 | 实际命令 | 结果 |
|---|---|---|
| ee-fmt | `cd ee && test -z "$(gofmt -l ./internal ./cmd)"` | 0 |
| ee-vet | `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go vet ./...` | 0 |
| ee-tests-race | `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go test -race -count=1 -p 1 ./...` | 0，全部通过 |
| ui-dependencies | `cd ui && npm_config_cache="$TMPDIR/npm-cache" npm ci` | 0 |
| ui-embed | `make -C ee build-ui` | 0 |
| ee-binary | `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go build -p 1 -o "$TMPDIR/bifrost-http" ./cmd/bifrost-http` | 0 |
| pricing-smoke | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/pricing-smoke.py --binary "$TMPDIR/bifrost-http"` | 0，全部 PASS |
| branding-regression | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/branding-smoke.py --binary "$TMPDIR/bifrost-http"` | 0 |
| identity-regression | `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/identity-smoke.py --binary "$TMPDIR/bifrost-http" --database sqlite` | 0 |
| pricefile-valid-json | `python3 -c "import json,sys;json.load(open('ee/internal/pricing/data/cn-prices.json'))"` | 0 |
| pricing-root-stdlib | `! grep -l 'gorm.io\|maximhq/bifrost' ee/internal/pricing/*.go` | 0 |
| upstream-untouched | `git diff --quiet f371548b5444693d7e259033bec9f4a467e3748b -- core framework plugins transports ui` | 0 |
| workbench | `python3 workbench/tools/build_views.py --check` | 0，130 篇文档通过 |
| diff-whitespace | `git diff --check` | 0 |

补充亲核：

- `cd ee && GOCACHE="$TMPDIR/go-cache" GOWORK=off go test -count=1 ./internal/pricing -run 'TestPriceFile|TestMatchVendors' -v`：通过，包含五个新增白名单用例。
- `PYTHONDONTWRITEBYTECODE=1 python3 ee/scripts/pricing-smoke-test.py -v`：六项通过。
- `make -n -C ee smoke-pricing`：确认先执行失败现场回归，再执行 HTTP smoke。
- 通过 `PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'` 内存调度 checklist：13 个 `test -e`、14 项必含 grep、5 项禁留 grep 均符合预期；禁留检查均退出 1、零命中。
- 同样用内存 Python 导入 `pricing-smoke.py`，运行自有夹具：在合法厂商后追加三种非法白名单，并同时设置未知映射，逐次验证整份跳过、旧行完整保留及 error 日志；改回大写且带单个末尾点的合法白名单后验证零变更恢复。全部通过，未创建测试源码文件。
- 内存 Python 比较 `ui/out` 与 embed：543 个文件逐字节一致。
- `git diff --check f6311fd4bbffec6b59adc5fa03f343a3053f6448..a2ca7af7a7ef04eab1988e5b84c53d1bba55d2c0 -- ee`：0。
- npm 提示 4 项依赖漏洞；脚本回归出现既有 SQLite 连接 `ResourceWarning`。均未导致检查失败，本轮未改相关依赖或连接逻辑。

版本绑定：评审开始和结束均为 `feat/domestic-pricing`、指定候选 `a2ca7af7a7ef04eab1988e5b84c53d1bba55d2c0`；`git status --porcelain=v1 --untracked-files=all` 为空，工作树及索引 diff 均为空。

二进制 SHA-256：`bd0677117107b190548614c2f89cc08d788c7e3b96fcf1d7c8b42800c7413f85`。

实际阅读：指定规则、验收报告、方案及决策／偏差记录、findings、checklist、两份证据、代码评审3；六个差量文件全文及复读；相关 vendors、sync、overrides、persistence 适配与费用测试、app 装配和测试、入口错误出口、smoke 生命周期及构建配置。未读取其他旧代码评审或收敛评审记录。

未执行：**checklist 无跳过项。** 未直接运行 preflight 总入口或 `make smoke-pricing`；前者检查逐项执行，后者会调用修改 `go.work` 的 workspace 目标，已拆开执行构建与两个测试脚本。未做真实 key 调用、页面人工核价、官网重新核价或 PostgreSQL 验证；分别属于 Gate 2、未改动的数据范围或需连接实例的检查。两个 PostgreSQL 测试 DSN 均未设置。

未创建或修改报告、源码、跟踪文件或索引；未启动 dev server、操作已有实例，未暂存、提交或推送。构建与测试产物按授权写入 scratch 或 gitignore 目录。

verdict: 通过
